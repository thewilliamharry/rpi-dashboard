---
phase: 01-behavioral-safety-runtime-ownership
plan: 23
type: execute
wave: 17
depends_on:
  - 01-22
files_modified:
  - dashboard/beacon/worker_main.py
  - dashboard/app.py
  - tests/test_worker_ownership_matrix.py
  - tests/test_runtime_ownership.py
  - tests/test_outbound_policy.py
  - tests/test_module_boundaries.py
autonomous: true
gap_closure: true
requirements:
  - FND-04
threat_model:
  asvs_level: 1
  block_on: high
  completion_invariant: "The actual production callback/effect inventory, admission registry, same-transaction database coverage, non-SQL effect coverage, takeover cases, and current-B controls are bijective; any missing row or surface fails the hard closure gate."
  applicable_threats:
    - "T-01-110 | severity=high | disposition=mitigate"
    - "T-01-111 | severity=high | disposition=mitigate"
    - "T-01-112 | severity=high | disposition=mitigate"
    - "T-01-113 | severity=high | disposition=mitigate"
    - "T-01-114 | severity=medium | disposition=mitigate"
    - "T-01-115 | severity=low | disposition=accept"
    - "T-01-SC | severity=low | disposition=accept"
must_haves:
  truths:
    - "Every mutation-capable S1-S3/J1-J9 startup or scheduled callback enters through one production inventory-driven admission wrapper; LeaseLost from any callback closes all admission before non-blocking scheduler shutdown."
    - "Finalization drains every admitted category before Chromium/proxy cleanup, exact-epoch release, or process-global reset, retaining the complete Plan 19 ordinary-return/signal/error/startup-failure/immediate-successor contract."
    - "Stale preview work publishes no title, thumbnail BLOB/error, capture/completion event, file artifact, or authoritative browser result after B acquires; current B publishes normally."
    - "A stale transition performs no webhook POST after B owns; a current bounded delivery reserves the exact epoch through its timeout, uses a deterministic non-secret delivery key, and cannot overlap successor acquisition."
    - "The hard closure gate compares the actual production callback/effect inventory to takeover, admission, transaction, effect, and current-B coverage and fails with exact missing/excess row IDs or surfaces."
    - "Complete Plan 19 and Wave 14 regressions, owner-free web controls, compatibility/API/UI/theme behavior, migration/recovery, and outbound/TLS policy all pass in the authoritative full suite."
  artifacts:
    - path: "dashboard/beacon/worker_main.py"
      provides: "Production callback inventory, inventory-driven startup/scheduler dispatch, all-category admission/revocation/drain, and ordered finalization"
      exports: ["WORKER_CALLBACK_INVENTORY", "WorkerAdmission", "build_scheduler", "run_worker"]
    - path: "dashboard/app.py"
      provides: "Authority-ordered preview publication and bounded webhook delivery gateways"
    - path: "tests/test_worker_ownership_matrix.py"
      provides: "Green end-to-end takeover/effect matrix and hard production-to-coverage bijection gate"
    - path: "tests/test_runtime_ownership.py"
      provides: "All-category admission/drain plus complete Plan 19 lifecycle regression proof"
    - path: "tests/test_outbound_policy.py"
      provides: "Strict bounded current-owner and zero-stale-owner webhook evidence"
    - path: "tests/test_module_boundaries.py"
      provides: "Static production callback/effect boundary and compatibility-edge assertions"
  key_links:
    - from: "dashboard/beacon/worker_main.py::WORKER_CALLBACK_INVENTORY"
      to: "build_scheduler() and run_worker() startup dispatch"
      via: "the same immutable descriptors supply callback IDs, actual callables, scheduler metadata, admission categories, and declared surfaces"
      pattern: "WORKER_CALLBACK_INVENTORY|admission_category|scheduler_id"
    - from: "dashboard/app.py webhook gateway"
      to: "dashboard/beacon/queues.py::renew_worker_authority"
      via: "exact-epoch reservation spans the already bounded connect-plus-read timeout immediately before strict pinned POST"
      pattern: "renew_worker_authority|Idempotency-Key|OutboundPurpose.WEBHOOK"
    - from: "tests/test_worker_ownership_matrix.py hard closure gate"
      to: "production inventory and tests/worker_ownership_contract.py"
      via: "set equality across actual callbacks/effects and takeover/admission/transaction/effect/B-control coverage"
      pattern: "WORKER_CALLBACK_INVENTORY|PRODUCTION_OWNERSHIP_INVENTORY|TAKEOVER_CASE_REGISTRY"
---

<objective>
Complete the system-wide ownership closure by controlling callback admission, lifecycle drain, every non-SQL effect, and inventory-to-evidence drift.

Purpose: Plan 01-22 makes SQLite authoritative at every write, but already-submitted jobs and external effects still require ordering under the same epoch. This plan makes the production inventory drive all dispatch, drains all categories safely, fences preview publication and webhook delivery, and refuses closure unless actual callbacks/effects and proof are exactly bijective.
Output: Production callback registry, universal admission/drain behavior, authority-ordered preview/webhook effects, exhaustive current-B controls, a hard closure gate, and complete Plan 19/Wave 14/full-suite regression evidence.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-CONTEXT.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-RESEARCH.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-REVIEW.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-VERIFICATION.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-19-PLAN.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-19-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-20-PLAN.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-20-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-21-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-22-SUMMARY.md
@tests/worker_ownership_contract.py
@tests/test_worker_ownership_matrix.py
@dashboard/beacon/worker_authority.py
@dashboard/beacon/worker_main.py
@dashboard/worker.py
@dashboard/app.py
@dashboard/beacon/queues.py
@dashboard/beacon/outbound.py
@dashboard/beacon/previews.py
@tests/test_runtime_ownership.py
@tests/test_durable_queues.py
@tests/test_outbound_policy.py
@tests/test_module_boundaries.py

<interfaces>
Plan 01-21/01-22 handoff to consume unchanged:
- `PRODUCTION_OWNERSHIP_INVENTORY` and `TAKEOVER_CASE_REGISTRY` contain P0/S1-S3/J1-J9/L1 and all database/effect/admission/current-B coverage identifiers.
- `WorkerAuthority` is the sole post-acquisition identity; `assert_current_worker_authority` and worker variants already make every database subset green.
- Explicit owner-free web operations remain separate and cannot be routed through callback admission or WorkerAuthority.
- Plan 20 row-token/revision/deadline/coalescing predicates and Plan 19 finalization order are non-negotiable regression floors.

Interfaces this plan must make authoritative:
- `WORKER_CALLBACK_INVENTORY` is immutable production data containing P0/S1-S3/J1-J9/L1 classifications, actual operation field/callable, startup or scheduler ID, scheduler metadata, unique admission category, database surfaces, effect surfaces, and ownership requirement.
- `run_worker()` post-acquisition startup and `build_scheduler()` consume that registry; there is no second raw callback list.
- `WorkerAdmission` accepts every ownership-required category, atomically closes all categories, reports active counts for deterministic tests, and drains without holding scheduler, lifecycle, SQLite, screenshot, browser, or proxy locks.
- Non-SQL effect gateways accept `WorkerAuthority`, use only declared effect IDs, and are the only production call sites permitted to publish preview results or send transition webhooks.
</interfaces>
</context>

<handoff_contract>
## Cross-plan closure contract

- Plan 01-23 must not reinterpret Plan 01-21 rows or weaken Plan 01-22 database assertions. It completes the still-RED admission, drain, effect, and closure-bijection assertions against the same registry.
- Plans 01-21, 01-22, and 01-23 together are one FND-04 closure. A green subset or one fixed CR-01 symptom is insufficient.
- Final closure requires the focused matrix, complete Plan 19 lifecycle matrix, complete Wave 14 durable-queue matrix, owner-free web controls, outbound/module/compatibility/UI gates, and the unpartitioned repository suite.
</handoff_contract>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive every mutating callback through universal admission and drain</name>
  <read_first>
    - tests/worker_ownership_contract.py: P0/S1-S3/J1-J9/L1 rows, admission IDs, and ownership classifications
    - tests/test_worker_ownership_matrix.py: still-RED callback/admission/current-B cases
    - dashboard/beacon/worker_main.py: current scan/preview-only admission, startup sequence, nine scheduler registrations, stop request, and finalizer
    - dashboard/worker.py: actual operation bindings that the production registry must classify
    - tests/test_runtime_ownership.py: complete Plan 19 terminal matrix and Wave 14 preview-only drain case
    - .planning/phases/01-behavioral-safety-runtime-ownership/01-19-SUMMARY.md: ordinary release/signal/exception/successor invariants
    - .planning/phases/01-behavioral-safety-runtime-ownership/01-20-SUMMARY.md: close-before-stop and drain-before-browser invariant
  </read_first>
  <files>dashboard/beacon/worker_main.py, tests/test_worker_ownership_matrix.py, tests/test_runtime_ownership.py</files>
  <action>
    Define immutable production `WORKER_CALLBACK_INVENTORY` entries for P0, S1-S3, J1-J9, and L1. For each ownership-required row, include the actual operation binding, startup/scheduler identity, established executor/trigger/interval/coalescing/max-instance/misfire metadata, a unique admission category, database surfaces, and effect surfaces. Refactor `run_worker()` post-acquisition startup and `build_scheduler()` to consume these entries. Preserve exactly the current nine scheduler IDs and scheduling parameters; P0 remains before acquisition and L1 remains lifecycle-owned.

    Generalize `WorkerAdmission` from scan/preview counters to every S1-S3/J1-J9 category. One admitted wrapper must atomically reject after closure, increment before invoking the actual authority-taking callback, decrement in `finally`, and on `LeaseLost` from any nested transaction/effect close all admission before calling the existing `shutdown(wait=False)` request. Already-submitted scheduler callbacks must still cross admission when they begin and be rejected after closure. Startup callbacks use the same wrapper before scheduler construction. Do not hold worker-start, scheduler, SQLite, screenshot, Chromium, or proxy locks while invoking, closing, or draining.

    Extend `_finalize_worker_lifecycle()` to close admission idempotently and drain all active categories on the lifecycle thread before browser/proxy shutdown, exact-epoch release, and total process-global reset. Never wait for APScheduler from an executor. Add deterministic held-callback cases for S1-S3 and J1-J9, loss from non-heartbeat callbacks, callbacks submitted before closure but entering after closure, normal return, signal, KeyboardInterrupt/SystemExit, scheduler error, each post-acquisition startup failure, browser cleanup failure, stale release, and immediate successor acquisition. Every category must have a current-B success control. Preserve Plan 19 exception semantics: suppress only `LeaseLost` from stale release and always reset globals.
  </action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -k "admission or drain or callback_inventory or current_b or worker_releases or stop_worker or finalizer" -x</automated>
  </verify>
  <acceptance_criteria>
    - Production startup and all nine scheduler jobs are inventory-driven; every S1-S3/J1-J9 callback crosses its declared admission category with the exact authority.
    - LeaseLost from any callback closes admission before non-blocking scheduler stop, and submitted work cannot enter after closure.
    - Every active category drains before browser/proxy cleanup, exact-epoch release, or global reset, without lock inversion or executor self-wait.
    - The complete Plan 19 terminal/startup/signal/successor matrix and current-B controls pass, not only the former preview-held case.
  </acceptance_criteria>
  <done>All production callbacks share one inventory-driven admission, revocation, and drain lifecycle without weakening durable SQLite authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Order thumbnail publication, webhooks, and every inventoried non-SQL effect under authority</name>
  <read_first>
    - tests/worker_ownership_contract.py: `EFFECT_SURFACES`, J3/J4/J5/J6/J7/J9 effect declarations, pause stages, and B controls
    - tests/test_worker_ownership_matrix.py: still-RED preview-publication, filesystem, browser lifecycle, and webhook rows
    - dashboard/app.py: `_legacy_fetch_thumbnail`, preview processor, transition/event/alert helpers, discovery and uptime transition loops
    - dashboard/beacon/queues.py: Plan 01-22 authority renewal/assertion and preview-result transaction APIs
    - dashboard/beacon/outbound.py: immutable webhook request plan, strict TLS/pinning/no-redirect policy, and bounded connect/read timeouts
    - dashboard/beacon/previews.py: browser/proxy context cleanup and retrieval-only HTTP/WSS gates
    - tests/test_outbound_policy.py: strict webhook and browser transport fixtures
    - tests/test_runtime_ownership.py: drain-before-browser ordering and exact-epoch successor controls
  </read_first>
  <files>dashboard/app.py, tests/test_worker_ownership_matrix.py, tests/test_runtime_ownership.py, tests/test_outbound_policy.py</files>
  <action>
    Refactor preview capture so browser/title/screenshot/timing results remain in memory until the Plan 01-22 preview-result transaction validates both WorkerAuthority and Wave 14 row/revision/lease/deadline predicates. `_legacy_fetch_thumbnail()` must not independently persist `preview_capture`. Insert capture and completion events with queue completion and service title/thumbnail BLOB/error publication in the same fenced transaction. A stale result closes its browser/proxy resources but produces no late event, BLOB/error, title, queue result, file, or publication recorder entry. Preserve `ThumbnailRepository` and `/api/thumbnail`; do not introduce a thumbnail filesystem store. Keep `filesystem_publication` in the inventory with no producer and fail if a producer appears later.

    Split transition persistence and delivery into explicit authority-taking worker gateways. Persist transition state/events only through Plan 01-22 transactions. Immediately before a webhook POST, use the exact authority to renew/reserve the durable lease for at least the immutable request plan's bounded connect plus read timeout and cleanup margin. After that reservation returns, perform no wait, probe, SQLite commit, redirect, or unrelated work before the single strict pinned POST. Because the reservation remains unexpired for the complete bounded call, B cannot acquire during delivery; if B already owns, reservation fails and the transport receives zero calls. Send one deterministic `Idempotency-Key` derived from stable transition identity and containing no owner token. Persist alert success/failure afterward only in a fresh authority-fenced transaction; loss after delivery must not overwrite B.

    Preserve D-09/D-10/D-11: webhook purpose is HTTPS-only, verified TLS, approved pinned address, and redirect-free, while the trusted-LAN service exception remains purpose-specific. Preserve safe destination-free error behavior and cooldown semantics. Add event-controlled tests for A held before final effect authorization, B takeover then zero A delivery/publication, B success, and an in-progress current-A bounded delivery that prevents B acquisition until the request returns and then permits ordinary takeover. Cover every non-empty effect set in the production inventory and assert undeclared effect IDs are rejected. Use deterministic events and injected clocks, never sleeps.
  </action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py tests/test_outbound_policy.py -k "webhook_effect or preview_publication or filesystem_publication or browser_resource or current_b" -x</automated>
  </verify>
  <acceptance_criteria>
    - Stale preview work publishes no queue result, service title, thumbnail BLOB/error, capture/completion event, file artifact, or authoritative browser effect after B acquisition; B publishes once.
    - Stale transition work makes zero webhook calls after B owns; current bounded delivery prevents takeover until return, uses one stable non-secret key, and records its result only if still authoritative.
    - Every production effect declaration has a zero-A takeover assertion and current-B control; no undeclared effect gateway is accepted.
    - Strict webhook TLS/pinning/no-redirect, browser GET/HEAD-only and WSS-blocking policy, cooldown, redaction, and thumbnail API behavior remain green.
  </acceptance_criteria>
  <done>Every current non-SQL effect is either reversible and drained or irreversibly executed only while the exact durable epoch remains current through its bounded completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the hard production-to-coverage closure gate and full regression proof</name>
  <read_first>
    - tests/worker_ownership_contract.py: complete expected inventory and coverage identifiers
    - dashboard/beacon/worker_main.py: actual production registry, startup dispatch, scheduler construction, admission, and finalizer
    - dashboard/worker.py: actual `WorkerOperations` bindings
    - dashboard/app.py: authority-taking transaction and effect gateways plus owner-free web variants
    - dashboard/beacon/queues.py: authority assertion, worker variants, owner-free variants, and Wave 14 predicates
    - dashboard/beacon/previews.py and dashboard/beacon/outbound.py: actual browser/proxy and webhook effect boundaries
    - tests/test_worker_ownership_matrix.py: all A takeover, surface, admission, effect, and B-control assertions
    - tests/test_runtime_ownership.py and tests/test_durable_queues.py: Plan 19 and Wave 14 regression matrices
    - tests/test_module_boundaries.py: package/legacy dependency-direction assertions
    - .planning/phases/01-behavioral-safety-runtime-ownership/01-CONTEXT.md: D-01 through D-16 and deferred discovery scope
  </read_first>
  <files>tests/test_worker_ownership_matrix.py, tests/test_module_boundaries.py</files>
  <action>
    Implement a hard closure test that instantiates actual `dashboard.worker.build_worker_operations()`, constructs the actual scheduler without starting it, and compares production `WORKER_CALLBACK_INVENTORY` to `PRODUCTION_OWNERSHIP_INVENTORY` and `TAKEOVER_CASE_REGISTRY`. Require exact set equality for P0/S1-S3/J1-J9/L1 IDs, operation fields/bindings, startup stages, nine scheduler IDs/callables/settings, ownership classifications, admission categories, database surfaces, effect surfaces, takeover cases, per-surface transaction assertions, per-callback admission assertions, per-effect assertions, and current-B controls. Duplicate, missing, excess, raw registered, matrix-only, or unclassified rows must fail with exact IDs/surfaces.

    Add static boundary checks that only `dashboard/worker.py` imports the legacy app edge; package worker modules remain independent; every production mutating `WorkerOperations` binding targets an authority-taking variant; every scheduled/startup mutator is inventory-dispatched; webhook transport is reachable only through the authorized effect gateway; preview capture cannot persist before the fenced result transaction; and post-acquisition filesystem publication has no undeclared producer. These checks protect the declared actual inventory from drifting away from code rather than trusting comments.

    Run the entire ownership matrix, every current-B control, complete Plan 19 lifecycle suite, complete Wave 14 queue suite, module/outbound/compatibility/API/UI safety gates, and the authoritative unpartitioned repository suite. Preserve D-01 compatibility and API fields; D-02 regression evidence; D-03 UI scope; D-04 human-only production database inventory; D-05/D-06 backup and retention; D-07 preparation gate; D-08 offline recovery; D-09/D-10/D-11/D-12 TLS behavior and indicator; D-13 interactive stale dashboard; D-14 durable queues; D-15 immediate metadata; and D-16 single monitoring-gap recovery. Do not add deferred discovery capability, schema/migration/recovery changes, UI redesign, dependencies, Compose changes, or a second ownership mechanism. Closure fails if any high-severity threat or any inventory row lacks executable evidence.
  </action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py tests/test_durable_queues.py tests/test_module_boundaries.py tests/test_outbound_policy.py tests/test_release_contract.py tests/test_api_and_auth.py tests/test_ui_safety_integration.py -x &amp;&amp; uv run --project dashboard python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The hard gate is exactly bijective across actual callbacks/effects and takeover/admission/transaction/effect/current-B coverage and fails on any missing or excess row/surface.
    - Every S1-S3/J1-J9 stale-A case has zero post-B database/effect delta and every corresponding B control succeeds.
    - Complete Plan 19 and Wave 14 matrices pass, including normal shutdown, signals, errors, startup failures, immediate successor, queue row/revision/deadline/recovery/coalescing, and owner-free web behavior.
    - Compatibility/API/UI/theme, migration/recovery, outbound/TLS, and the full repository suite pass; no high or critical ASVS L1 threat remains open.
  </acceptance_criteria>
  <done>FND-04 and CR-01 are closed as a system-wide production invariant with drift-proof executable evidence, not as independent callback symptom fixes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| scheduler/startup dispatch -> operation | Submitted callbacks can begin after local closure unless every actual entry uses the inventory wrapper. |
| running callback -> lifecycle cleanup | Cleanup must not release authority or close Chromium while admitted work can still publish. |
| Worker A -> successor Worker B | A may retain probe results, browser bytes, row leases, or process locks after B becomes durable owner. |
| worker transition -> external webhook | A remote POST cannot share the SQLite transaction and needs a bounded authority reservation through completion. |
| preview capture -> authoritative publication | Browser output is untrusted until epoch and row/revision predicates pass together. |
| production inventory -> test coverage | A newly added callback/effect must fail closure until takeover, admission, transaction/effect, and B-control evidence exists. |

## STRIDE Threat Register — OWASP ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-110 | Tampering / Elevation of Privilege | raw or unclassified callback bypass | high | mitigate | Tasks 1 and 3 make one production inventory drive startup/scheduler dispatch and enforce exact callback-to-coverage equality. |
| T-01-111 | Denial of Service | partial admission closure or drain deadlock | high | mitigate | Task 1 covers every category, closes before non-blocking stop, drains on the lifecycle thread without conflicting locks, and proves all Plan 19 paths. |
| T-01-112 | Tampering / Repudiation | late thumbnail/event/file publication | high | mitigate | Task 2 stages preview data and commits publication only with epoch plus row/revision fencing; filesystem producer set is explicitly empty and statically guarded. |
| T-01-113 | Tampering / Repudiation | stale or overlapping webhook delivery | high | mitigate | Task 2 reserves the exact epoch through bounded strict delivery, emits zero calls after B ownership, uses a stable delivery key, and fences result events. |
| T-01-114 | Denial of Service | safety achieved by disabling current work | medium | mitigate | Every inventory row has a successful current-B operation/effect control and full compatibility regression coverage. |
| T-01-115 | Information Disclosure | epoch or destination leakage | low | accept | Tokens never enter delivery keys/logs/events/API, and existing safe webhook errors remain destination-free under focused tests. |
| T-01-SC | Tampering | package supply chain | low | accept | No npm/pip/cargo installation or dependency/lockfile change occurs. |

Every high-severity entry is dispositioned `mitigate` and mapped to executable evidence. The hard closure gate blocks completion when any mitigation or row is missing.
</threat_model>

<artifacts_produced>
## Artifacts Produced

- `dashboard/beacon/worker_main.py::WORKER_CALLBACK_INVENTORY` — actual P0/S1-S3/J1-J9/L1 callback, scheduler, admission, mutation, and effect registry used by production dispatch.
- Universal category-aware admission/revocation/drain and Plan 19-preserving finalization.
- Authority-ordered thumbnail/event publication and strict bounded webhook effect gateways in `dashboard/app.py`.
- `tests/test_worker_ownership_matrix.py` hard actual-production-to-coverage bijection plus exhaustive A/B matrix.
- Complete Plan 19, Wave 14, outbound, module-boundary, compatibility, API, UI/theme, and full-suite closure evidence.
</artifacts_produced>

## Gap Closure Source Coverage Audit

| Source | ID/item | Plans | Status | Coverage / exclusion |
|---|---|---|---|---|
| GOAL | Safe continued use/upgrades with dependable independently maintainable web, worker, persistence, and outbound responsibilities | existing + 21-23 | COVERED | Existing delivered behavior remains; 21-23 close the sole-worker blocker without changing public experience. |
| REQ | FND-01 compatibility behavior | existing + 23 | COVERED | Task 3 reruns compatibility/API/UI/service/preview/uptime/event regressions. |
| REQ | FND-02 explicit module boundaries | existing + 22-23 | COVERED | Explicit authority/worker/web variants and static dependency/effect gates strengthen the established boundary. |
| REQ | FND-03 side-effect-free web import | existing + 23 | COVERED | Import/lifecycle regressions remain in the full gate; no web import starts work. |
| REQ | FND-04 sole worker and durable persisted coordination | 21-23 | COVERED | One exhaustive contract, same-transaction SQLite fencing, universal admission/drain, effect ordering, and hard bijection close CR-01 system-wide. |
| REQ | FND-05 migrations | existing + 21/23 controls | COVERED WITH EXTERNAL CHECK OUTSTANDING | P0 remains pre-epoch under its locks; the D-04 operator inventory remains external and fail-closed. |
| REQ | FND-06 backup/recovery | existing + 21/23 controls | COVERED | P0 and offline recovery boundaries remain unchanged and their regressions run. |
| REQ | FND-07 outbound/TLS policy | existing + 23 | COVERED | Task 2 retains strict pinned webhook and retrieval-only preview policy while ordering authority. |
| REQ | OPS-05 security tests | existing + 23 | COVERED | Outbound/module/full gates plus stale/current effect controls run. |
| RESEARCH | Existing Flask/SQLite/APScheduler/Playwright stack; no new dependency | 21-23 | COVERED | Plans use existing modules, dataclasses/threading, real SQLite, pytest, and current transport/browser seams. |
| RESEARCH | SQLite is cross-process authority; process locks are local only | 21-23 | COVERED | Plan 22 makes transaction-local epoch proof authoritative; Plan 23 uses local admission only for cancellation/drain. |
| RESEARCH | Preparation before ownership; durable queues; bounded pools/coalescing; serialized Chromium | 21-23 | COVERED | P0 and all scheduling/queue settings remain, and effect/drain ordering preserves Chromium ownership. |
| RESEARCH | Real SQLite takeover/restart tests and bounded external effects | 21-23 | COVERED | The matrix uses real lease acquisition and deterministic effects; webhook authorization spans bounded timeouts. |
| CONTEXT | D-01 preserve routes, fields, and relied-upon behavior | 22-23 | COVERED | Internal worker variants only; compatibility/API/UI full gates run. |
| CONTEXT | D-02 protect confirmed bug correction with tests | 21-23 | COVERED | RED inventory/matrix becomes a permanent hard closure gate. |
| CONTEXT | D-03 only directly supporting UI changes | 21-23 | COVERED | No UI file or visual behavior change. |
| CONTEXT | D-04 inventory actual legacy databases | existing | EXCLUDED FROM CODE GAP | Remains the documented human fail-closed pre-upgrade check; repository planning cannot inspect operator files. |
| CONTEXT | D-05 verified backup before schema change | existing + P0 control | COVERED | Preparation/backup ordering is preserved. |
| CONTEXT | D-06 retain three verified automatic backups | existing + P0 control | COVERED | Retention is unchanged. |
| CONTEXT | D-07 partial migration never starts worker | existing + P0 control | COVERED | Preparation remains before acquisition/startup/scheduler. |
| CONTEXT | D-08 one supported recovery path | existing | COVERED | Offline catalog-bound recovery is unchanged. |
| CONTEXT | D-09 permissive TLS only for eligible LAN services | existing + 23 | COVERED | Service-purpose posture remains separate. |
| CONTEXT | D-10 webhooks never inherit LAN exception | existing + 23 | COVERED | Authority ordering retains verified strict webhook purpose. |
| CONTEXT | D-11 revalidate every redirect hop | existing + 23 | COVERED | Service redirects remain re-planned; webhook redirects remain refused. |
| CONTEXT | D-12 TLS-unverified visible and separate from downtime | existing + 23 gate | COVERED | No status/UI change; full UI safety suite runs. |
| CONTEXT | D-13 stale worker leaves dashboard interactive | 22-23 | COVERED | Owner-free controls and stale warning regressions remain green. |
| CONTEXT | D-14 durable scan/preview requests survive outage with expiry | 22-23 | COVERED | Explicit owner-free enqueue plus Wave 14 deadline/recovery/coalescing gates. |
| CONTEXT | D-15 metadata persists immediately while worker unavailable | 22-23 | COVERED | Metadata plus preview revision remains one owner-free transaction. |
| CONTEXT | D-16 recovery clears stale warning and records one monitoring gap | 22-23 | COVERED | S1/S2 fencing retains one current-owner gap/heartbeat outcome and rejects stale duplicates. |
| CONTEXT | Broader tenth-port discovery | none | EXCLUDED | Explicit Deferred Idea; no discovery capability is added. |
| GAP | 01-VERIFICATION Truth 5 and 01-REVIEW CR-01 | 21-23 | COVERED | Exhaustive inventory/RED proof → all SQLite writes → all callbacks/effects → hard full closure. |

Audit result: every in-scope goal, phase requirement, research constraint, locked D-01 through D-16 decision, and actionable verification/review gap is covered by existing plans plus this strictly dependent 21→22→23 closure. The external D-04 database inventory and deferred discovery capability remain correctly excluded.

<verification>
- <automated>dashboard/.venv/bin/python -m pytest -q tests/test_worker_ownership_matrix.py -x</automated>
- <automated>dashboard/.venv/bin/python -m pytest -q tests/test_runtime_ownership.py tests/test_durable_queues.py -x</automated>
- <automated>dashboard/.venv/bin/python -m pytest -q tests/test_module_boundaries.py tests/test_outbound_policy.py tests/test_release_contract.py tests/test_api_and_auth.py tests/test_ui_safety_integration.py -x</automated>
- <automated>uv run --project dashboard python -m pytest -q</automated>
- Confirm with `git diff --name-only` that Plan 01-23 changes are confined to its six declared files.
</verification>

<success_criteria>
- FND-04/CR-01 is closed system-wide: after B acquisition, A creates no database, thumbnail/file publication, event, browser-publication, or webhook effect through any actual post-acquisition callback.
- Every worker write remains same-BEGIN-IMMEDIATE fenced; every mutating callback is universally admitted/drained; every irreversible effect is authority-ordered through bounded completion.
- The hard closure gate fails any missing/excess callback, surface, takeover case, transaction assertion, admission assertion, effect assertion, or current-B control.
- Complete Plan 19 normal/exception/signal/startup/successor and Wave 14 row/revision/deadline/recovery/coalescing regressions pass.
- Owner-free web operations, D-01/D-09 through D-16 behavior, migration/recovery, outbound/TLS, public API, UI/theme, and the full suite pass unchanged.
- No high or critical ASVS L1 threat remains open and no schema, migration, dependency, deployment, tracking, roadmap, or deferred-feature change is made.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-safety-runtime-ownership/01-23-SUMMARY.md` when done.
</output>
