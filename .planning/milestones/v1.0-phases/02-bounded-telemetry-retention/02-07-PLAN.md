---
phase: 02-bounded-telemetry-retention
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - dashboard/app.py
  - dashboard/beacon/telemetry.py
  - tests/test_telemetry_retention.py
  - tests/test_historical_telemetry_api.py
autonomous: true
gap_closure: true
requirements: [TEL-02, TEL-04, TEL-05]

must_haves:
  truths:
    - "A real worker host sample creates canonical metric-specific stream evidence for cpu, ram, disk, and temp, and each metric's historical response reads that same evidence per D-05 and D-06."
    - "Storage-pressure suspension and recovery create and close one truthful per-metric collection_gap instead of an unreadable shared host gap per D-11 and D-12."
    - "Worker retention and the historical API derive tier cutoffs, retention bounds, and point budget from one immutable RetentionPolicy composed from the validated deployed Settings per D-01, D-02, and D-08."
    - "A configured telemetry point budget is enforced in repository limits, overflow checks, response composition, and the returned point_budget field; no default constant can disagree with deployed policy."
  artifacts:
    - path: "dashboard/beacon/telemetry.py"
      provides: "Canonical host metric identities and a Settings-to-RetentionPolicy composition contract"
      exports: ["HOST_METRICS", "host_stream_key", "RetentionPolicy"]
    - path: "dashboard/app.py"
      provides: "Production worker-to-history wiring through per-metric streams and one settings-backed policy"
      contains: "worker_collect_system_stats|_telemetry_policy|api_telemetry_history"
    - path: "tests/test_telemetry_retention.py"
      provides: "Real worker sampling and storage-pressure identity regression evidence"
    - path: "tests/test_historical_telemetry_api.py"
      provides: "Configured tier and point-budget parity regression evidence"
  key_links:
    - from: "dashboard/app.py::worker_collect_system_stats"
      to: "dashboard/app.py::api_telemetry_history"
      via: "dashboard.beacon.telemetry.host_stream_key(metric) used for stream, pressure-gap, rollup coverage, and request coverage identity"
      pattern: "host_stream_key"
    - from: "dashboard/beacon/config.py::Settings"
      to: "dashboard/beacon/telemetry.py::RetentionPolicy"
      via: "the sole _telemetry_policy composition seam consumed by worker cleanup and historical reads"
      pattern: "_telemetry_policy"
    - from: "dashboard/app.py::api_telemetry_history"
      to: "dashboard/beacon/repositories.py"
      via: "policy.point_budget and policy-derived cutoffs supplied before bounded repository reads"
      pattern: "policy.point_budget"
---

<objective>
Repair the production tracer so one real worker observation flows through canonical host metric identity and the same settings-backed retention policy to a truthful historical response.

Purpose: Close the two production-wiring defects that make valid persisted evidence appear unavailable or select a different tier/budget from the worker's deployed policy.
Output: Per-metric host stream wiring, one explicit Settings-to-RetentionPolicy factory/value, and production-path regression tests for all four host metrics, storage pressure, configured tier cutoffs, and response budgets.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md
@.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md
@.planning/phases/02-bounded-telemetry-retention/02-05-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-06-SUMMARY.md
@dashboard/app.py
@dashboard/beacon/config.py
@dashboard/beacon/telemetry.py
@dashboard/beacon/repositories.py
@tests/test_telemetry_retention.py
@tests/test_historical_telemetry_api.py

<interfaces>
- `dashboard.beacon.telemetry.RetentionPolicy` already owns raw/five-minute/retention days, point budget, storage thresholds, batch size, and retry bounds as an immutable validated value.
- `dashboard.app._telemetry_policy()` is the existing process composition seam; worker cleanup already consumes it, while `api_telemetry_history()` incorrectly constructs defaults and uses `POINT_BUDGET` directly.
- `dashboard.app.worker_collect_system_stats(authority, now=None, persist_history=None)` writes one `stats_history` row containing cpu/ram/disk/temp and must register availability for each metric stream in the same authority-fenced transaction.
- `dashboard.beacon.telemetry._roll_host_raw()` reads coverage with `(stream_kind='host', stream_key=metric)`, so the writer and API must use that exact key convention.
</interfaces>
</context>

## Artifacts this phase produces

- `HOST_METRICS` and `host_stream_key(metric)` as the single fixed host identity contract shared by sampling, coverage, pressure gaps, rollups, and history reads.
- A Settings-backed `RetentionPolicy` factory used by the existing `_telemetry_policy()` composition seam.
- Production integration tests that invoke `worker_collect_system_stats()` under a real current `WorkerAuthority`, then call `GET /api/telemetry/history` for cpu, ram, disk, and temp.
- API tests with non-default validated tier days and point budget proving that response selection matches worker policy.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace all four real worker host metrics through canonical stream evidence</name>
  <reversibility rating="costly">Host stream keys are durable coverage identities consumed by rollups and later analytics, so changing them again requires coordinated persistence and query behavior.</reversibility>
  <files>dashboard/beacon/telemetry.py, dashboard/app.py, tests/test_telemetry_retention.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-05, D-06, D-07, D-11, and D-12 define per-stream availability and pressure-gap truthfulness.
    - `.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md` — use the exact production writer-to-reader failure and required all-metric evidence.
    - `dashboard/app.py` — inspect `_telemetry_policy()`, `worker_collect_system_stats()`, `_worker_write_transaction()`, and `api_telemetry_history()` before editing.
    - `dashboard/beacon/telemetry.py` — reuse `record_observation()`, `open_storage_pressure_gap()`, `close_storage_pressure_gap()`, and `_coverage_for()`.
    - `tests/test_telemetry_retention.py` — reuse `UtcClock`, `seed_successive_worker_epochs()`, `load_app()`, and real SQLite assertions.
  </read_first>
  <behavior>
    - "A current worker sample creates `telemetry_streams` rows for exactly `host:cpu`, `host:ram`, `host:disk`, and `host:temp`, and each matching API request reports the sample as observed evidence."
    - "A suspended sample opens pressure state for all four metric keys; the next allowed sample closes four half-open `collection_gap` intervals with detail `storage_pressure` and does not create a shared writer-only host stream."
    - "A stale worker epoch still fails before changing raw history, stream, coverage, pressure state, or current host state."
  </behavior>
  <action>Write the failing production-path regression first. Add `HOST_METRICS = ('cpu', 'ram', 'disk', 'temp')` and a strict `host_stream_key(metric)` validator in `dashboard/beacon/telemetry.py`; use this contract in `_roll_host_raw()` instead of a separate tuple. In `worker_collect_system_stats()`, keep the single atomic `stats_history` row and current `system_stats` update, but loop over the four fixed metrics when closing/opening storage-pressure gaps and recording observations. For an allowed historical sample, close the matching metric pressure gap and call `record_observation()` with that metric key; treat a non-null metric value as observed and an explicit null sample as unknown without inventing a numeric value. For suspension, open one gap per metric in the same retention-state value and persist the combined state once after the loop. Make `api_telemetry_history()` derive the host coverage key through `host_stream_key(metric)`, matching `_roll_host_raw()` coverage lookups. Preserve the existing shared raw-row rollup job identity `host:host`, which coordinates atomic deletion of the row containing all four metrics; do not confuse that internal job key with the canonical per-metric coverage identities. The regression must acquire a real worker authority, patch deterministic psutil/temperature/storage inputs, invoke the production worker function, then request all four metrics and assert stream rows, observed coverage, pressure-gap start/end, and stale-authority rejection.</action>
  <acceptance_criteria>
    - One public helper defines the four accepted host metric stream keys and all production sampling/rollup/history coverage paths use it per D-05 and D-06.
    - A normal worker sample followed by four historical requests proves cpu, ram, disk, and temp are queryable through the production writer, not direct test seeding.
    - A suspend/recover sequence produces four metric-specific `storage_pressure` gaps with exact half-open bounds and no new shared `host:host` stream or coverage row per D-11 and D-12.
    - The raw host bucket's internal retry/job key remains shared so all observed metric aggregates are still verified before the one `stats_history` source row is deleted per D-09.
    - Stale-worker assertions remain before every mutation.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py -k "host and (stream or pressure or worker or production)" -x</automated>
  </verify>
  <done>A real worker host observation and its pressure gaps reach every metric-specific history reader through one canonical identity without weakening shared-bucket deletion or worker fencing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route worker and API through one deployed retention policy</name>
  <reversibility rating="costly">Tier and response-budget values are shared storage/API contracts, though their validated deployment values remain configurable.</reversibility>
  <files>dashboard/beacon/telemetry.py, dashboard/app.py, tests/test_historical_telemetry_api.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-01, D-02, D-08, D-10, and the agent-discretion budget contract must remain intact.
    - `.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md` — repair CR-02 at the actual route composition point.
    - `dashboard/beacon/config.py` — consume the validated immutable `Settings` telemetry fields; do not parse environment values again.
    - `dashboard/beacon/telemetry.py` — keep `RetentionPolicy` the sole value object and preserve its relational validation.
    - `dashboard/app.py` — worker cleanup already calls `_telemetry_policy()`; make the API use the same value for every cutoff and cap.
    - `tests/test_historical_telemetry_api.py` — use `load_app(extra_env=...)` to exercise deployed non-default settings through Flask.
  </read_first>
  <behavior>
    - "Validated non-default raw/five-minute/retention days drive both worker retention policy and API tier/expiry boundaries."
    - "A non-default point budget drives resolution selection, every repository limit and overflow assertion, the response ceiling, and the serialized `point_budget` field."
    - "Malformed or relationally invalid environment combinations still fall back through `load_settings()` before the policy factory is called."
  </behavior>
  <action>Add one explicit `RetentionPolicy.from_settings(settings)` factory (or equivalently named module factory returning that immutable value) that copies every telemetry field already validated by `load_settings()` and performs no environment access. Make `_telemetry_policy()` delegate only to that factory. In `api_telemetry_history()`, construct the policy once before resolution selection; replace the default `RetentionPolicy()` and every direct `POINT_BUDGET` use with `policy.raw_days`, `policy.five_minute_days`, `policy.retention_days`, and `policy.point_budget`. Pass `policy.point_budget + 1` to all four bounded repository reads, compare composed output to `policy.point_budget`, and serialize the same value. Keep `POINT_BUDGET` only as the default field value for direct policy callers/tests, not as production route configuration. Add Flask tests loaded with a valid non-default 8/31/91-day policy and small point budget; seed data around configured tier and retention boundaries and prove the API selects/query-limits those configured tiers, reports the configured budget, and returns no more points than allowed. Assert `_telemetry_policy()` equals the policy supplied to `worker_cleanup_history()` so worker and API cannot drift.</action>
  <acceptance_criteria>
    - `_telemetry_policy()` has one Settings-to-value implementation and both worker cleanup and history requests consume it.
    - API cutoffs, retention coverage, resolution selection, repository sentinel limits, final overflow assertion, and response metadata all use the same `policy` instance per D-01, D-02, and D-08.
    - Non-default valid settings change observable API tier/budget behavior; invalid settings retain the established validated defaults.
    - No new environment parser, schema change, endpoint, or frontend dependency is introduced.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_historical_telemetry_api.py tests/test_telemetry_retention.py -k "policy or settings or budget or configured or production or host" -x</automated>
  </verify>
  <done>The worker and historical API apply one validated deployed retention policy, including the response-point budget, with executable non-default configuration evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker sample -> authority-fenced SQLite transaction | Real host values and pressure decisions become durable raw, stream, coverage, and runtime-state evidence only under the current worker epoch. |
| validated Settings -> retention policy | Deployment strings have already been validated; one immutable policy must carry the same values to worker mutations and API reads. |
| browser query -> bounded history repository | Untrusted range/selector values may read only the configured tiers and budget through existing fixed query shapes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-G01 | Tampering | host stream identity | high | mitigate | Use one fixed metric allowlist/helper across worker observations, pressure gaps, rollup coverage, and API lookup; retain exact WorkerAuthority assertion before writes. |
| T-02-G02 | Denial of Service | configured historical point budget | high | mitigate | Carry the validated positive budget through resolution, every repository sentinel limit, composition assertion, and response metadata. |
| T-02-G03 | Repudiation | storage-pressure coverage | medium | mitigate | Persist one explicit half-open per-metric gap with `storage_pressure` detail and verify it through the production writer-to-reader test. |
| T-02-G04 | Information Disclosure | history validation errors | low | accept | Existing fixed validation messages reveal no SQL, path, worker token, or configuration secret; telemetry values are intended trusted-LAN output. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---------------|---------------|-----------------------|
| V4 Access Control | Applicable | Preserve exact durable WorkerAuthority for every host mutation and existing Host/Origin protection for the GET history route; add no mutation route or alternate scheduler owner. |
| V5 Validation, Sanitization and Encoding | Applicable | Validate host metrics through one fixed allowlist, consume only validated Settings, require positive/relational policy values, and bind all range, metric, and limit values through existing fixed repository shapes. |
| V2 Authentication | Not applicable | Beacon remains account-free on a trusted LAN and this repair adds no authentication mechanism. |
| V3 Session Management | Not applicable | Worker sampling and stateless history reads create no cookie, token, login, or server session. |
| V6 Stored Cryptography | Not applicable | The repair stores local operational telemetry and introduces no credential, key, encryption, signature, or cryptographic protocol. |
</threat_model>

<verification>
- Run the focused Phase 2 suites: `uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py tests/test_historical_telemetry_api.py`.
- Run the worker authority regression that covers stale/current host writes: `uv run --project dashboard python -m pytest -q tests/test_runtime_ownership.py tests/test_worker_ownership_matrix.py -k "worker or authority or telemetry"`.
</verification>

<success_criteria>
- Production host samples and storage-pressure intervals are readable for cpu, ram, disk, and temp through matching metric stream identities.
- The historical API and retention worker use identical settings-backed tier, retention, retry, pressure, batch, and point-budget values.
- Non-default deployed settings change both storage and read behavior coherently while all bounds remain validated and response work remains capped.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-07-SUMMARY.md` when done.
</output>
