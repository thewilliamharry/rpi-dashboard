---
phase: 02-bounded-telemetry-retention
plan: 10
type: execute
wave: 10
depends_on: [02-09]
files_modified:
  - dashboard/beacon/migrations.py
  - dashboard/beacon/support_floor.json
  - tests/fixtures/legacy/support-floor.json
  - tests/fixtures/legacy/current-v6.db
  - tests/test_migrations.py
  - tests/test_historical_telemetry_api.py
autonomous: true
gap_closure: true
requirements: [TEL-04]

must_haves:
  truths:
    - "An existing version-6 database that stores host stream, coverage, and pressure-gap evidence under host:host upgrades transactionally to equivalent cpu, ram, disk, and temp evidence per D-05, D-11, and D-12."
    - "After upgrade, each host metric history request sees the migrated collection-gap evidence rather than reporting not_yet_monitored or an unsupported unknown interval."
    - "Migration re-entry is idempotent: it neither duplicates nor widens canonical coverage, and a partial overlap keeps metric-specific evidence authoritative while filling only genuinely uncovered legacy intervals."
    - "Legacy host:host is removed from telemetry_streams, telemetry_coverage, and telemetry_retention_state.pressure_gaps, but remains untouched in telemetry_rollup_jobs as the approved shared raw-row job identity."
    - "An injected migration failure rolls back all stream, coverage, pressure-state, and version changes while preserving the verified backup/recovery path."
  artifacts:
    - path: "dashboard/beacon/migrations.py"
      provides: "Version 7 compatibility data migration from shared host evidence to canonical metric streams"
      contains: "_migration_7_canonical_host_streams"
    - path: "dashboard/beacon/support_floor.json"
      provides: "Evidence-backed admission of the canonical version-6 schema into target version 7"
      contains: "current-v6.db"
    - path: "tests/fixtures/legacy/current-v6.db"
      provides: "Seeded version-6 database with legacy host stream, coverage, pressure state, and shared rollup-job evidence"
    - path: "tests/test_migrations.py"
      provides: "Preservation, overlap, idempotence, support-floor, and rollback proof for Migration 7"
    - path: "tests/test_historical_telemetry_api.py"
      provides: "Seeded upgrade-through-Flask regression for all four host metrics"
  key_links:
    - from: "dashboard/beacon/migrations.py::_migration_7_canonical_host_streams"
      to: "telemetry_streams|telemetry_coverage|runtime_state"
      via: "one fixed, versioned SQLite migration transaction that expands only the legacy host identity"
      pattern: "host:host|cpu|ram|disk|temp"
    - from: "tests/fixtures/legacy/current-v6.db"
      to: "dashboard/beacon/support_floor.json"
      via: "test-derived structural fingerprint with minimum_schema_version 6 and target_version 7"
      pattern: "current-v6"
    - from: "tests/test_historical_telemetry_api.py"
      to: "GET /api/telemetry/history"
      via: "compatibility init runs Migration 7 before cpu/ram/disk/temp coverage reads"
      pattern: "api/telemetry/history"
  flagged_assumptions:
    - "TEL-01 unclassified: this gap plan changes availability metadata only; the already-verified 90-day host/service/event expiry and bounded batch behavior remain the retention authority and must pass the full regression suite."
    - "TEL-03 unclassified: Migration 7 never deletes stats_history, service_checks, events, or rollup source evidence; aggregate-before-delete remains owned by run_retention_batch() and is regression-only here."
    - "TEL-05 unclassified: the repair does not change resolution selection or the Settings-derived point budget; the existing bounded route contract remains authoritative and must pass unchanged."
---

<objective>
Upgrade already-persisted shared host availability evidence into the canonical per-metric contract and prove that evidence remains visible through the production history API.

Purpose: Close the compatibility hole left when new writers/readers moved from `host:host` to `host:cpu|ram|disk|temp`, without losing, duplicating, widening, or partially publishing historical evidence.
Output: Migration 7, a canonical seeded version-6 support-floor fixture, transactional/idempotent migration evidence, and an all-metric Flask regression.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md
@.planning/phases/02-bounded-telemetry-retention/02-PATTERNS.md
@.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md
@.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md
@.planning/phases/02-bounded-telemetry-retention/02-03-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-07-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-09-SUMMARY.md
@dashboard/beacon/migrations.py
@dashboard/beacon/support_floor.json
@dashboard/beacon/telemetry.py
@dashboard/beacon/repositories.py
@dashboard/app.py
@tests/test_migrations.py
@tests/test_historical_telemetry_api.py

<interfaces>
- `dashboard.beacon.migrations.MIGRATIONS` currently ends at version 6; `run_migrations()` admits exact support-floor fingerprints, creates verified backups for `schema_changing=True`, and applies each migration under `BEGIN IMMEDIATE` before recording its version.
- `telemetry_streams` is keyed by `(stream_kind, stream_key)`; `telemetry_coverage` stores half-open unavailable intervals; `runtime_state['telemetry_retention_state']` stores a JSON object with `state` and `pressure_gaps`.
- `dashboard.beacon.telemetry.host_stream_key(metric)` defines canonical host coverage keys for exactly cpu, ram, disk, and temp; `host:host` is intentionally retained only by raw-host rows in `telemetry_rollup_jobs`.
- `dashboard.app.init_db()` delegates schema/data upgrades to the migration gate before `GET /api/telemetry/history` performs exact per-metric coverage lookups.
</interfaces>
</context>

<spec_less_edge_assumptions>

| Probe item | Resolution in this plan set |
|---|---|
| TEL-01 unclassified | Explicitly assumed unaffected because no retained observation/event or expiry predicate changes; full suite is mandatory. |
| TEL-02 adjacency | Resolved in Plan 02-11: only exact touching 300-second pending intervals coalesce. |
| TEL-02 empty | Resolved in Plan 02-11: no source rows produce no pending interval. |
| TEL-02 ordering | Resolved in Plan 02-11: host/service pending intervals are deterministically ascending. |
| TEL-03 unclassified | Explicitly assumed unaffected because Migration 7 does not touch source observations or aggregate deletion. |
| TEL-04 adjacency | Resolved here by half-open coverage subtraction/coalescing and in Plan 02-11 by exact pending adjacency. |
| TEL-04 empty | Resolved here: absent legacy rows are a no-op; Plan 02-11 returns an empty pending tuple for empty input. |
| TEL-04 ordering | Resolved here: migrated coverage is non-overlapping and ascending; Plan 02-11 preserves deterministic pending order. |
| TEL-05 unclassified | Explicitly assumed unaffected because selector, resolution, point-budget, and overflow contracts do not change; full suite is mandatory. |

</spec_less_edge_assumptions>

## Artifacts this phase produces

- New migration symbol `_migration_7_canonical_host_streams(conn)` and registry entry `Migration(7, 'canonical_host_streams', True, ...)`.
- New migration helper `_subtract_covered_segments(start_ts, end_ts, blockers)` for deterministic half-open legacy-coverage clipping.
- New fixture `tests/fixtures/legacy/current-v6.db` with migration versions 1-6 and seeded legacy shared-host evidence.
- New migration tests `test_current_v6_legacy_host_state_migrates_once_without_overlap()` and `test_migration_seven_failure_rolls_back_legacy_host_state()`.
- New API test `test_legacy_host_upgrade_is_visible_to_all_metric_history_endpoints()`.
- No new endpoint, response field, external dependency, or product-side public API symbol.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a seeded legacy host database through Migration 7 and all four history endpoints</name>
  <reversibility rating="one-way">Migration 7 removes obsolete shared stream/coverage/state identities after preserving their evidence; undoing the canonical on-disk identity later requires another data migration, but the operator already approved the per-metric contract in Plan 02-07.</reversibility>
  <files>dashboard/beacon/migrations.py, dashboard/beacon/support_floor.json, tests/fixtures/legacy/support-floor.json, tests/fixtures/legacy/current-v6.db, tests/test_migrations.py, tests/test_historical_telemetry_api.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-05, D-08, D-11, and D-12 require durable, candid per-stream gap evidence and stored-data compatibility.
    - `.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md` — implement the exact remaining legacy `host:host` upgrade and API-visible proof.
    - `.planning/phases/02-bounded-telemetry-retention/02-07-SUMMARY.md` — retain canonical metric identities and the shared raw-rollup job exception already approved.
    - `dashboard/beacon/migrations.py` — append version 7 through the existing support-floor, verified-backup, exclusive-maintenance, and one-transaction runner.
    - `dashboard/beacon/support_floor.json` and `tests/fixtures/legacy/support-floor.json` — preserve every admitted fingerprint and advance all declared targets coherently.
    - `dashboard/beacon/telemetry.py` — match the exact `host_stream_key()` contract and retention-state JSON shape without importing Flask into migrations.
    - `tests/test_migrations.py` — extend canonical-fixture, preservation, rerun, concurrent-contender, and rollback patterns rather than bypassing the migration gate.
    - `tests/test_historical_telemetry_api.py` — reuse the real Flask client and exact coverage-envelope assertions after pointing `DB_PATH` at the upgraded seeded database.
  </read_first>
  <behavior>
    - "A seeded version-6 `host:host` stream becomes four canonical stream rows; started_ts is the earliest evidence boundary, last_observed_ts is the latest known observation, and current metric-specific cadence/miss state wins ties."
    - "For each metric, existing metric-specific coverage wins conflicting overlap; legacy coverage fills only uncovered half-open fragments, exact duplicates disappear, and adjacent equal reason+detail fragments coalesce without extending beyond the union of pre-upgrade evidence."
    - "A legacy `pressure_gaps['host:host']` entry becomes cpu/ram/disk/temp entries using the earliest applicable start, preserves unrelated JSON/service keys, and removes the obsolete shared pressure key."
    - "No shared host stream/coverage/state key remains after success, while every `telemetry_rollup_jobs` row, including `host:host`, is byte-equivalent in meaning."
    - "Running the migration helper again and running `run_migrations()` again are no-ops; an injected failure after transformation rolls back all four tables/state/version changes and leaves a verified recovery snapshot."
    - "After compatibility initialization, cpu, ram, disk, and temp history requests return the migrated collection_gap interval with its original detail and exact requested bounds."
  </behavior>
  <action>Write the seeded upgrade and API RED regressions first. Produce `tests/fixtures/legacy/current-v6.db` by applying migrations 1-6 to the canonical Phase 1 fixture, then seed a shared host stream, duplicate/adjacent legacy coverage, a partially overlapping metric-specific interval, `telemetry_retention_state` with shared plus unrelated pressure keys, and a shared raw-host rollup job. Derive its structural fingerprint in the existing inventory path, add only that evidence-backed version-6 entry with `minimum_schema_version: 6`, and advance every packaged/test support-floor `target_version` to 7. Add `_subtract_covered_segments()` and `_migration_7_canonical_host_streams()` as fixed internal migration helpers. For each of cpu, ram, disk, and temp, insert or merge the shared stream deterministically: keep the earliest `started_ts`, the latest non-null observation, and take cadence/consecutive-miss/open-gap fields from the row with the later observation while ties favor existing metric-specific state. Expand legacy coverage in ascending `(start_ts, end_ts, id)` order; existing metric-specific intervals have precedence, so subtract every existing/previously-added half-open blocker, insert only uncovered fragments, de-duplicate exact rows, and coalesce only adjacent/overlapping fragments whose reason and detail match. This must never create overlapping canonical intervals or extend coverage outside the pre-upgrade union. Parse only the exact `telemetry_retention_state` JSON row; fail the migration if the target object, `pressure_gaps`, or legacy timestamp is malformed so the backup/recovery path handles it rather than discarding evidence. Expand the shared pressure start to all four metric keys using the earliest valid start, preserve all unrelated JSON keys and service gaps, and remove the shared key. Only after all four canonical outputs are complete, delete legacy `host:host` rows from `telemetry_streams` and `telemetry_coverage`; never update/delete `telemetry_rollup_jobs`. Register Migration 7 with verified-backup behavior and rely on the existing caller-owned transaction for atomic commit/version publication. In the API regression, clone the seeded v6 fixture, use relative-to-test timestamps within the accepted 90-day range, invoke compatibility `init_db()` to run the actual migration, then call the existing GET endpoint for all four metrics and assert exact migrated coverage, canonical stream metadata, exact requested bounds, removal of obsolete shared state, and preservation of the shared rollup job.</action>
  <acceptance_criteria>
    - Migration 7 is versioned, exact-fingerprint admitted, verified-backup protected, transactional, and the only production path that rewrites legacy host availability identities.
    - The canonical v6 fixture and tests prove stream metadata, coverage, pressure state, unrelated state, and the shared rollup job before and after upgrade.
    - Canonical coverage is ascending, half-open, non-overlapping, duplicate-free, and no wider than the union of legacy plus pre-existing metric-specific evidence.
    - Direct helper re-entry and a second migration-runner call change no rows, coverage bounds, version, or backup count.
    - An injected post-transform failure leaves the exact v6 evidence/version intact and records only the existing redacted recovery marker against a verified backup.
    - All four real Flask metric requests expose the migrated collection-gap evidence; no read compatibility fallback or new response field is introduced.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_migrations.py tests/test_historical_telemetry_api.py -k "current_v6 or migration_seven or legacy_host_upgrade" -x</automated>
  </verify>
  <done>Every supported already-persisted shared-host evidence record upgrades once into canonical metric streams and remains truthfully visible through the existing bounded API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| admitted version-6 SQLite -> Migration 7 | Durable operator evidence crosses a data-rewriting upgrade and must be preserved atomically. |
| runtime_state JSON -> canonical pressure state | Only one named row/key may be parsed and rewritten; malformed state must fail closed. |
| migrated SQLite -> history API | Read-only metric endpoints must expose the preserved evidence without a compatibility bypass or new mutation path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-G13 | Tampering | Migration 7 stream/coverage rewrite | high | mitigate | Admit only the exact current-v6 fingerprint, create a verified backup, transform all targets inside one `BEGIN IMMEDIATE`, preserve canonical-over-legacy precedence, and publish version 7 only on commit. |
| T-02-G14 | Tampering | `telemetry_retention_state` mutation | high | mitigate | Read and update only the exact runtime-state row, validate JSON/object/map/integer types, preserve unrelated keys, and roll back on malformed data. |
| T-02-G15 | Repudiation | shared-to-canonical evidence lineage | medium | mitigate | Retain deterministic stream merges, half-open clipped coverage, unchanged rollup jobs, migration-version evidence, and before/after fixture assertions. |
| T-02-G16 | Denial of Service | exclusive migration work | medium | mitigate | Process only one fixed shared host identity across four fixed metrics, use existing range indexes and ordered bounded-by-database evidence, and keep all work inside the established offline maintenance boundary. |
| T-02-G17 | Information Disclosure | migration/API failures | low | accept | Existing recovery markers and API validation remain redacted; no row values, SQL, filesystem paths, or worker tokens are exposed. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---|---|---|
| V4 Access Control | Applicable as preservation | The mutation is reachable only through the existing local upgrade gate under exclusive maintenance; the history route remains GET-only behind existing Host/Origin protections. |
| V5 Validation, Sanitization and Encoding | Applicable | Fixed metric keys and fixed table/state names are used; persisted JSON types and interval bounds are validated; all values remain bound parameters. |
| V2 Authentication | Not applicable | Local migration authority and trusted-LAN read behavior introduce no account or login mechanism. |
| V3 Session Management | Not applicable | Migration and stateless history reads create no cookie, token, or server session. |
| V6 Stored Cryptography | Not applicable | The migration preserves operational evidence and introduces no secret, key, encryption, signature, or cryptographic protocol. |
</threat_model>

<source_coverage_audit>

| Source | ID | Feature / decision | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Accurate bounded history with trustworthy gaps and retention | 02-10, 02-11 plus executed 02-01–02-09 | COVERED |
| REQ | TEL-01 | Rolling bounded 90-day retention | Executed 02-04/02-08; full regression in 02-10/02-11 | COVERED |
| REQ | TEL-02 | Detailed recent evidence and documented aggregates | Executed 02-04/02-09; exact five-minute pending edges in 02-11 | COVERED |
| REQ | TEL-03 | Aggregate before source deletion | Executed 02-04/02-08; Migration 7 explicitly does not touch sources | COVERED |
| REQ | TEL-04 | Explicit known/unknown/gap/expiry history | 02-10 migration/API tracer and 02-11 exact pending intervals | COVERED |
| REQ | TEL-05 | Appropriate bounded server resolution | 02-11 keeps exact bounded pending disclosure; existing policy contract regression remains green | COVERED |
| RESEARCH | — | Versioned transactional migration with preserved data and exact support floor | 02-10 | COVERED |
| RESEARCH | — | Fixed parameterized bounded repository reads and half-open intervals | 02-11 | COVERED |
| RESEARCH | — | No new service, package, ORM, scheduler, or external backend | Both plans | COVERED |
| CONTEXT | D-01–D-04 | Locked tiers, events, and aggregate fields | Executed 02-02–02-09; unchanged and regression-gated | COVERED |
| CONTEXT | D-05 | Explicit availability reasons | 02-10 and 02-11 | COVERED |
| CONTEXT | D-06–D-08 | Cadence, no interpolation, exact requested bounds | Executed 02-05–02-09; migrated evidence and API regression in 02-10 | COVERED |
| CONTEXT | D-09 | Pending disclosure and source preservation | 02-11 plus executed 02-08/02-09 | COVERED |
| CONTEXT | D-10 | Storage guardrails | Executed 02-04/02-07; unchanged | COVERED |
| CONTEXT | D-11–D-12 | Pressure gaps and recovery | 02-10 preserves and canonicalizes durable pressure state | COVERED |
| CONTEXT | Deferred | 365-day hourly retention | Excluded by explicit deferral | EXCLUDED |

</source_coverage_audit>

<verification>
- Focused migration/API gate: `uv run --project dashboard python -m pytest -q tests/test_migrations.py tests/test_historical_telemetry_api.py`.
- Compatibility gate: `uv run --project dashboard python -m pytest -q tests/test_release_contract.py tests/test_telemetry_retention.py`.
- Full project gate: `uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- Existing shared host stream, coverage, and pressure evidence upgrades into all four canonical metric streams exactly once.
- Metric-specific overlap remains authoritative, legacy uncovered evidence is preserved without gaps/overlap/duplicate widening, and unrelated state is unchanged.
- The shared raw-host rollup job identity remains intact while obsolete shared availability identities disappear.
- Failure rolls back the whole migration, and successful upgrade evidence is visible through every existing metric endpoint.
- Focused and full project suites pass without weakened or deleted assertions.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-10-SUMMARY.md` when done.
</output>
