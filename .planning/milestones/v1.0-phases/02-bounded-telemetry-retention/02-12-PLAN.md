---
phase: 02-bounded-telemetry-retention
plan: 12
type: execute
wave: 12
depends_on: [02-11]
files_modified:
  - dashboard/beacon/migrations.py
  - tests/test_migrations.py
autonomous: true
gap_closure: true
requirements: [TEL-04]

must_haves:
  truths:
    - "Migration 7 distinguishes an absent legacy host:host pressure key from a present value and treats only absence as a legitimate pressure-state no-op."
    - "A present legacy host:host pressure value is accepted only when it is an integer and not a boolean; null, booleans, strings, floats, arrays, and objects fail closed per D-05, D-11, and D-12."
    - "Every malformed legacy pressure value leaves schema version 6 and the exact pre-migration telemetry_streams, telemetry_coverage, telemetry_retention_state, and telemetry_rollup_jobs evidence intact."
    - "A valid integer pressure timestamp expands to cpu, ram, disk, and temp, removes only the obsolete pressure key, and preserves unrelated retention state."
    - "Migration 7 remains idempotent, and host:host remains intentionally unchanged in telemetry_rollup_jobs as the shared raw-host job identity."
  artifacts:
    - path: "dashboard/beacon/migrations.py"
      provides: "Presence-aware, fail-closed validation and guarded expansion of the Migration 7 legacy pressure key"
      contains: "_migration_7_canonical_host_streams"
    - path: "tests/test_migrations.py"
      provides: "Focused JSON-null rollback regression plus malformed-value, absence, valid-integer, and idempotency evidence"
      contains: "test_migration_seven"
  key_links:
    - from: "dashboard/beacon/migrations.py::_migration_7_canonical_host_streams"
      to: "runtime_state['telemetry_retention_state'].pressure_gaps"
      via: "key-membership check followed by non-boolean integer validation before the first migration mutation"
      pattern: "host:host|isinstance.*bool|isinstance.*int"
    - from: "dashboard.beacon.migrations.run_migrations"
      to: "schema_migrations and legacy telemetry evidence"
      via: "the existing caller-owned Migration 7 transaction publishes version 7 only after validation and transformation complete"
      pattern: "Migration\(7|BEGIN IMMEDIATE|ROLLBACK"
    - from: "tests/test_migrations.py"
      to: "telemetry_streams|telemetry_coverage|runtime_state|telemetry_rollup_jobs|schema_migrations"
      via: "before/after snapshots around a real version-6 fixture upgrade"
      pattern: "MigrationPreparationError|MAX\(version\)"
  flagged_assumptions:
    - "No Phase 2 SPEC exists; the edge contract is taken from 02-VERIFICATION.md, 02-REVIEW.md, the approved Migration 7 plan/summary, and D-05/D-11/D-12."
    - "TEL-01, TEL-02, TEL-03, and TEL-05 are unchanged: this repair neither changes retention cutoffs, rollup ownership/deletion, resolution selection, nor point budgets; the full suite remains the regression gate."
---

<objective>
Make Migration 7 fail closed for a present JSON-null or otherwise malformed legacy host pressure value while preserving the already-approved successful migration contract.

Purpose: Close the last TEL-04 blocker by preventing a malformed durable gap identity from being stranded behind a published schema version.
Output: A presence-aware Migration 7 guard and test-first proof for null, the remaining malformed JSON types, absence, valid integer expansion, rollback, idempotency, and intentional rollup-job identity.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md
@.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md
@.planning/phases/02-bounded-telemetry-retention/02-10-PLAN.md
@.planning/phases/02-bounded-telemetry-retention/02-10-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-11-SUMMARY.md
@dashboard/beacon/migrations.py
@tests/test_migrations.py

<interfaces>
- `dashboard.beacon.migrations._migration_7_canonical_host_streams(conn)` reads the exact `telemetry_retention_state` row, expands shared host availability evidence into `_CANONICAL_HOST_METRICS`, removes obsolete stream/coverage identities, and deliberately never mutates `telemetry_rollup_jobs`.
- `run_migrations(Settings(...))` applies Migration 7 under the existing `BEGIN IMMEDIATE`/rollback boundary, records schema version 7 only after the migration returns successfully, creates the existing verified backup, and surfaces a failed apply as `MigrationPreparationError` with a redacted recovery marker.
- `tests/fixtures/legacy/current-v6.db` contains the supported version-6 shared-host stream, coverage, pressure state, unrelated state, and shared raw-host rollup job used by the existing Migration 7 tests.
- Python `bool` is a subclass of `int`; the accepted timestamp predicate must therefore reject booleans explicitly before accepting integer values.
</interfaces>
</context>

<spec_less_edge_assumptions>

No Phase 2 SPEC exists. These unresolved migration edges are binding for this gap plan:

| Edge | Required behavior |
|---|---|
| Present JSON null | Invalid persisted legacy state; fail before transformation and roll back without publishing version 7. |
| Present Boolean | Invalid even though Python treats booleans as integers. |
| Present string, float, array, or object | Invalid; fail through the same durable-state validation boundary. |
| Absent legacy key | Legitimate no-op for pressure-state expansion/removal; other valid Migration 7 stream and coverage work may complete normally. |
| Present valid integer | Expand to the four canonical metric keys using the existing earliest-start merge and remove the legacy pressure key. |
| Repeated valid migration | Direct helper re-entry and a second migration-runner call remain no-ops. |
| Any malformed value | No partial stream, coverage, state, rollup-job, or schema-version mutation is observable after failure. |

</spec_less_edge_assumptions>

## Artifacts this phase produces

- A narrow correction inside the existing `_migration_7_canonical_host_streams(conn)` implementation; no new production symbol, table, migration version, endpoint, response field, package, or external service.
- A dedicated JSON-null rollback regression and a compact malformed legacy-value matrix in `tests/test_migrations.py`.
- Explicit absence, valid-integer expansion/removal, re-entry idempotency, and unchanged `telemetry_rollup_jobs` evidence using the existing version-6 fixture.

## Reversibility

No new one-way decision or checkpoint is required. This plan only enforces the Migration 7 compatibility contract already approved and executed in Plan 02-10; the repair is a local, reversible validation change and does not alter the canonical on-disk design.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace malformed legacy pressure state through Migration 7 rollback</name>
  <files>tests/test_migrations.py, dashboard/beacon/migrations.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md` — reproduce the exact JSON-null blocker and preserve the stated successful Migration 7 truths.
    - `.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md` — apply the presence-vs-value root-cause correction without widening the migration.
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-05, D-11, and D-12 require truthful durable pressure-gap evidence and prohibit invented recovery.
    - `.planning/phases/02-bounded-telemetry-retention/02-10-SUMMARY.md` — preserve canonical metric expansion, overlap precedence, idempotency, backup behavior, and the intentional shared raw-rollup job identity.
    - `dashboard/beacon/migrations.py` — change only `_migration_7_canonical_host_streams()` and retain the existing caller-owned transaction/version registry.
    - `tests/test_migrations.py` — extend the real `current-v6.db` patterns and exact before/after rollback assertions rather than mocking away the migration runner.
  </read_first>
  <behavior>
    - "A dedicated null regression writes a present host:host JSON null, runs the real Migration 7 gate, receives MigrationPreparationError, and observes schema version 6 plus byte-for-byte unchanged retention-state JSON and row-for-row unchanged legacy telemetry evidence."
    - "A compact fresh-fixture matrix rejects both booleans, a numeric-looking string, a float, an array, and an object as malformed present legacy values."
    - "Removing the legacy pressure key entirely is accepted; pressure state is otherwise preserved while valid legacy stream/coverage evidence follows the existing canonical migration path."
    - "A non-boolean integer follows the existing four-metric earliest-start expansion, removes host:host only from pressure_gaps/stream/coverage identities, and preserves unrelated keys plus telemetry_rollup_jobs host:host."
    - "Direct helper re-entry and a second run_migrations() call make no further changes after a valid successful upgrade."
  </behavior>
  <action>Write the RED evidence first. Add one focused test named for the JSON-null failure: copy `current-v6.db`, replace only `pressure_gaps['host:host']` with JSON null, commit the fixture mutation, and snapshot sorted complete rows from `telemetry_streams`, `telemetry_coverage`, and `telemetry_rollup_jobs`, the exact unparsed `telemetry_retention_state.value`, and the applied schema-version rows. Run the real `run_migrations()` path and require `MigrationPreparationError`; after reopening the database, compare every snapshot exactly, assert the maximum schema version is still 6, and retain the existing verified-backup/redacted-marker assertions with `ValueError` as the reason class. Expand the existing malformed-pressure coverage into fresh-fixture `subTest` cases for `True`, `False`, a numeric-looking string, a float, a list, and an object; each case must prove the same no-partial-publication invariants, not only that an exception was raised. Add or extend a focused absence case that removes the key rather than assigning null and proves pressure-state handling is a no-op while the other valid Migration 7 conversions succeed. Retain the existing valid-integer and re-entry assertions as the positive oracle. Only after the tests fail for the null case, update `_migration_7_canonical_host_streams()` to compute key presence independently from the retrieved value, reject a present value when it is a boolean or is not an integer, and gate canonical expansion plus legacy-key removal on presence rather than non-nullness. Perform this validation before stream, coverage, state, or version mutation. Preserve D-05/D-11/D-12 semantics, unrelated JSON keys, the existing canonical earliest-start merge, the fixed cpu/ram/disk/temp set, the caller-owned transaction, Migration 7 idempotency, and the deliberate `telemetry_rollup_jobs` host:host identity. Do not add timestamp range rules, a new migration version, a compatibility read fallback, or any new public interface.</action>
  <acceptance_criteria>
    - A present JSON null and every other listed malformed legacy value fail through `MigrationPreparationError` with a `ValueError` recovery reason before Migration 7 can publish.
    - Each malformed case leaves the exact pre-run stream rows, coverage rows, raw retention-state JSON, rollup-job rows, and version rows intact with maximum schema version 6.
    - Absence is distinguishable from null and remains a successful pressure-state no-op; a valid non-boolean integer still expands and removes the legacy pressure key exactly once.
    - Existing canonical metric merge/coverage behavior, verified backup handling, direct-helper idempotency, runner idempotency, and intentional raw-host rollup-job identity remain covered and green.
    - No production file other than `dashboard/beacon/migrations.py` changes, and no assertion is weakened or deleted.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_migrations.py -k "migration_seven or current_v6_legacy_host_state" -x</automated>
  </verify>
  <done>Migration 7 either converts a valid/absent legacy pressure entry completely or rejects malformed persisted state without any partial mutation or schema advancement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| persisted version-6 SQLite -> Migration 7 | Operator telemetry evidence crosses a versioned data-rewrite boundary and must never be partially published. |
| retention-state JSON -> canonical pressure-gap map | Persisted JSON is untrusted durable input; key presence and exact JSON value type must be validated before mutation. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-G18 | Tampering | `_migration_7_canonical_host_streams()` legacy pressure validation | high | mitigate | Separate membership from value retrieval, accept only a non-boolean integer when the legacy key is present, and validate before any migration write. |
| T-02-G19 | Tampering | Migration 7 transaction/version publication | high | mitigate | Keep the existing caller-owned transaction and prove every malformed case restores exact table/state snapshots with schema version 6. |
| T-02-G20 | Repudiation | malformed-state failure evidence | medium | mitigate | Exercise a named null regression plus a deterministic type matrix and retain the existing redacted recovery marker, verified backup, and reason-class assertions. |
| T-02-G21 | Denial of Service | persisted-state validation | low | accept | Validation inspects one fixed runtime-state row and one fixed key with no loop over attacker-sized external input or new network surface. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---|---|---|
| V5 Validation, Sanitization and Encoding | Applicable | Persisted JSON crosses a trust boundary; key presence and the exact non-boolean integer contract are validated before any durable mutation. |
| V4 Access Control | Applicable as preservation | Migration execution remains reachable only through the existing local upgrade gate and exclusive maintenance boundary. |
| V2 Authentication | Not applicable | No account, credential, or authentication flow is introduced. |
| V3 Session Management | Not applicable | No session or token state is introduced. |
| V6 Stored Cryptography | Not applicable | No secret or cryptographic material is read, written, or transformed. |
</threat_model>

<source_coverage_audit>

| Source | ID | Feature / decision | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Accurate, bounded 90-day telemetry with trustworthy gaps and retention | Executed 02-01–02-11 plus fail-closed migration evidence in 02-12 | COVERED |
| REQ | TEL-01 | Rolling bounded 90-day retention | Executed 02-04/02-08; unchanged and full-suite gated by 02-12 | COVERED |
| REQ | TEL-02 | Detailed recent observations and documented aggregates | Executed 02-04/02-09/02-11; unchanged and regression-gated | COVERED |
| REQ | TEL-03 | Aggregate before source deletion | Executed 02-04/02-08/02-09; 02-12 proves Migration 7 does not partially mutate evidence | COVERED |
| REQ | TEL-04 | Explicit known/unknown/gap/expiry semantics | 02-12 rejects malformed legacy pressure-gap state or converts it completely | COVERED |
| REQ | TEL-05 | Appropriate bounded server resolution | Executed 02-01/02-06/02-07/02-11; unchanged and full-suite gated | COVERED |
| RESEARCH | — | Existing SQLite/versioned migration stack, no new package/service/scheduler | 02-12 changes the existing Migration 7 helper and runner tests only | COVERED |
| RESEARCH | — | Aggregate-before-delete, bounded reads, pressure controls, and sparse explicit coverage | Executed 02-04–02-11; fail-closed persisted pressure validation in 02-12 | COVERED |
| RESEARCH | — | Preserve stored data through the verified-backup transactional migration gate | Exact malformed-state rollback snapshots and marker/backup assertions in 02-12 | COVERED |
| CONTEXT | D-01–D-04 | Locked tier, expiry, event, and aggregate contracts | Executed 02-02–02-09; unchanged and full-suite gated | COVERED |
| CONTEXT | D-05 | Explicit unavailable-interval vocabulary | Legacy pressure-gap state either canonicalizes completely or fails closed in 02-12 | COVERED |
| CONTEXT | D-06–D-10 | Cadence, coverage, requested bounds, retry, and pressure-entry rules | Executed 02-04–02-11; unchanged and regression-gated | COVERED |
| CONTEXT | D-11–D-12 | Durable storage-pressure gaps and truthful recovery | Presence-aware validation, valid expansion, and exact rollback in 02-12 | COVERED |
| CONTEXT | Deferred | 365-day hourly retention | Explicitly excluded from this gap closure | EXCLUDED |

</source_coverage_audit>

<verification>
- Focused Migration 7 gate: `uv run --project dashboard python -m pytest -q tests/test_migrations.py -k "migration_seven or current_v6_legacy_host_state" -x`.
- Full project gate: `uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- Migration 7 distinguishes absent from present-null legacy pressure state and accepts only a present non-boolean integer timestamp.
- Null, booleans, strings, floats, arrays, and objects all fail without changing version-6 telemetry evidence or schema version.
- Valid integer conversion, absence behavior, idempotency, canonical host availability identities, and the shared raw-rollup job identity remain correct.
- Focused Migration 7 tests and the full project suite pass without weakened or removed assertions.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-12-SUMMARY.md` when done.
</output>
