---
phase: 03-advanced-current-diagnosis
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - dashboard/beacon/diagnosis.py
  - dashboard/beacon/repositories.py
  - dashboard/advanced.js
  - tests/test_advanced_diagnosis_api.py
  - tests/test_advanced_ui.py
autonomous: true
gap_closure: true
requirements: [TEL-06, DIA-01, DIA-03, DIA-08, UX-02]

must_haves:
  truths:
    - "After a bounded advanced-current fetch fails, the connection warning is visibly active while the last successful diagnosis and timestamp remain unchanged per D-09 and D-13; the next successful fetch clears only that connection warning."
    - "Worker warning visibility is derived from the server-classified worker freshness, uses J1's immutable five-second heartbeat cadence rather than metric sampling cadence, and remains independent of connection and recovery warnings per D-09 through D-11."
    - "The safety cluster keeps connection, worker, and recovery in the approved order, and any combination of the three can remain visible without one condition suppressing another."
    - "The Advanced browser suite loads /advanced, /advanced.js, and /advanced.css through Beacon's Flask WSGI routes; commit 1ab5db7's already-deployed asset fix receives regression coverage and is not reimplemented."
    - "A service with unknown availability uses the regular full-probe cadence, not the down-only recheck cadence, and remains freshness unknown when no valid probe timestamp exists per D-10/D-11."
    - "Malformed or legacy pinned_order evidence cannot fail GET /api/advanced/current; it falls back to deterministic service ordering while validated integers retain D-06 ordering."
    - "Gap truncation is true only when more rows exist than the response cap; zero, one, exactly 48, and 49-or-more rows report truthful item count and truncation evidence."
    - "The current diagnosis endpoint remains parameterless, GET-only, no-store, bounded, effect-free, and free of new packages, external services, monitoring mutations, or history work."
  artifacts:
    - path: "dashboard/beacon/diagnosis.py"
      provides: "Immutable-heartbeat worker safety, unknown-service cadence, safe pinned ordering, and exact gap metadata composition"
      contains: "worker_stale"
    - path: "dashboard/beacon/repositories.py"
      provides: "Sentinel-bounded gap read that distinguishes exactly-at-cap from over-cap evidence"
      contains: "gaps_truncated"
    - path: "dashboard/advanced.js"
      provides: "Browser-owned connection failure state plus independent server worker/recovery warning rendering"
      contains: "connectionUnavailable"
    - path: "tests/test_advanced_diagnosis_api.py"
      provides: "Non-default metric cadence, unknown service, corrupt metadata, exact-cap, and asset-route regressions"
      contains: "worker_safety"
    - path: "tests/test_advanced_ui.py"
      provides: "Production-route Playwright coverage for connection/worker/recovery safety transitions"
      contains: "production_routes"
  key_links:
    - from: "dashboard/advanced.js::refreshCurrentDiagnosis"
      to: "#connection-banner"
      via: "failed GET marks route-local connectionUnavailable and successful GET clears it without replacing the retained snapshot"
      pattern: "connectionUnavailable|renderSafety"
    - from: "dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis"
      to: "dashboard/beacon/worker_main.py::WORKER_CALLBACK_INVENTORY[J1]"
      via: "callback_schedule_evidence supplies the fixed five-second heartbeat cadence independently of metric_sample_seconds"
      pattern: "J1|callback_schedule_evidence|worker_stale"
    - from: "dashboard/beacon/repositories.py::read_pipeline_evidence"
      to: "dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis"
      via: "repository fetches one sentinel row beyond the requested gap cap, slices returned items, and passes an explicit truncation boolean"
      pattern: "gap_limit|gaps_truncated"
    - from: "tests/test_advanced_ui.py"
      to: "dashboard/app.py advanced document and asset routes"
      via: "a real local WSGI server serves the production page bundle while Playwright intercepts only the internal JSON fixture when a deterministic payload/failure is required"
      pattern: "make_server|/advanced.css|/api/advanced/current"
  edge_cases:
    - requirement_id: "TEL-06"
      category: "adjacency"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved TEL-06 adjacency row but supplied no predicate; this plan does not infer one."
    - requirement_id: "TEL-06"
      category: "empty"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved TEL-06 empty row but supplied no predicate; this plan does not infer one."
    - requirement_id: "TEL-06"
      category: "ordering"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved TEL-06 ordering row but supplied no predicate; this plan does not infer one."
    - requirement_id: "DIA-01"
      category: "unclassified"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-01 row without a descriptor or predicate; this plan does not infer one."
    - requirement_id: "DIA-02"
      category: "unclassified"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-02 row without a descriptor or predicate; this plan does not infer one."
    - requirement_id: "DIA-03"
      category: "adjacency"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-03 adjacency row but supplied no predicate; this plan does not infer one."
    - requirement_id: "DIA-03"
      category: "empty"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-03 empty row but supplied no predicate; this plan does not infer one."
    - requirement_id: "DIA-03"
      category: "ordering"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-03 ordering row but supplied no predicate; this plan does not infer one."
    - requirement_id: "DIA-08"
      category: "adjacency"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-08 adjacency row but supplied no predicate; this plan does not infer one."
    - requirement_id: "DIA-08"
      category: "empty"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-08 empty row but supplied no predicate; this plan does not infer one."
    - requirement_id: "DIA-08"
      category: "ordering"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved DIA-08 ordering row but supplied no predicate; this plan does not infer one."
    - requirement_id: "UX-02"
      category: "boundary"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved UX-02 boundary row but supplied no predicate; this plan does not infer one."
    - requirement_id: "UX-02"
      category: "precision"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — the spec-less probe reported an unresolved UX-02 precision row but supplied no predicate; this plan does not infer one."
  prohibitions:
    - requirement_id: "TEL-06"
      category: "values"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — descriptor-less values prohibition retained for TEL-06 review; no source predicate was supplied."
    - requirement_id: "DIA-01"
      category: "safety"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — descriptor-less safety prohibition retained for DIA-01 review; no source predicate was supplied."
    - requirement_id: "DIA-02"
      category: "values"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — descriptor-less values prohibition retained for DIA-02 review; no source predicate was supplied."
    - requirement_id: "DIA-03"
      category: "values"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — descriptor-less values prohibition retained for DIA-03 review; no source predicate was supplied."
    - requirement_id: "DIA-08"
      category: "safety"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — descriptor-less safety prohibition retained for DIA-08 review; no source predicate was supplied."
    - requirement_id: "UX-02"
      category: "values"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION — descriptor-less values prohibition retained for UX-02 review; no source predicate was supplied."
---

<objective>
Close the verified truthful-current-diagnosis defects while preserving the already-deployed production asset repair.

Purpose: Make connection, worker, recovery, service cadence, durable ordering, and bounded gap evidence trustworthy under real production routes without changing the advanced workspace's read-only scope.
Output: End-to-end safety transitions, immutable heartbeat freshness, safe legacy/corrupt metadata handling, exact truncation disclosure, and production-route regression coverage.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md
@.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md
@.planning/phases/03-advanced-current-diagnosis/03-RESEARCH.md
@.planning/phases/03-advanced-current-diagnosis/03-PATTERNS.md
@.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md
@.planning/phases/03-advanced-current-diagnosis/03-02-SUMMARY.md
@.planning/phases/03-advanced-current-diagnosis/03-04-SUMMARY.md
@dashboard/beacon/diagnosis.py
@dashboard/beacon/repositories.py
@dashboard/beacon/worker_main.py
@dashboard/beacon/config.py
@dashboard/advanced.js
@dashboard/app.py
@dashboard/Dockerfile
@tests/test_advanced_diagnosis_api.py
@tests/test_advanced_ui.py
@tests/test_module_boundaries.py

<interfaces>
- `freshness_state(now, sample_ts, cadence_seconds)` is the sole four-state classifier: fresh through one cadence, aging through four cadences, stale beyond four cadences, and unknown for invalid timestamp/cadence evidence.
- `WORKER_CALLBACK_INVENTORY` declares J1 as the scheduled heartbeat with `trigger_kwargs=(('seconds', 5),)`; J2 alone substitutes `settings.metric_sample_seconds` in `callback_schedule_evidence()`.
- `Settings.worker_ready_seconds` defaults to 20, matching the J1 four-cadence stale boundary; `Settings.metric_sample_seconds` configures host metrics and must not configure worker heartbeat freshness.
- `read_pipeline_evidence(conn, now, gap_limit=48, stream_limit=64, pending_limit=32)` owns fixed/capped SQL and currently returns only the capped gap list; this plan adds explicit sentinel-derived truncation evidence without unbounding the read.
- `get_current_diagnosis()` returns `safety.recovery_required`; this plan adds `safety.worker_stale` derived from the already-composed server worker freshness while connection availability remains browser-owned because a failed HTTP fetch cannot return a server payload.
- `advanced.js::refreshCurrentDiagnosis()` preserves `state.snapshot` and `state.lastSuccessLabel` in its catch path; the catch path may update only route-local connection warning state and error copy.
- Commit `1ab5db7` already makes `serve_advanced_css()` use `send_file('advanced.css')`, packages all three advanced assets in `dashboard/Dockerfile`, and adds route/manifest regressions. Treat this as established baseline, not implementation work.
- `tests/test_advanced_ui.py` currently serves files through `SimpleHTTPRequestHandler`; the production Flask app and its security/static adapters are the required document/asset test boundary.
</interfaces>
</context>

## Artifacts this phase produces

- New internal helper `dashboard.beacon.diagnosis._safe_pinned_order(value, fallback)` for bounded deterministic durable-metadata projection.
- New internal repository evidence key `gaps_truncated`, produced from a one-row sentinel beyond `gap_limit` and consumed by `compose_pipeline_diagnosis()`.
- New public diagnosis field `safety.worker_stale`, derived exclusively from `pipeline.worker.freshness.state`.
- New route-local browser state field `connectionUnavailable`; it is memory-only and never enters `localStorage` or the API payload.
- New focused regression tests for production WSGI asset routes, all safety-warning combinations/transitions, non-default metric cadence, unknown service cadence, corrupt `pinned_order`, and exact/over-cap gap disclosure.
- No new file path, package, database schema, endpoint, request parameter, mutation control, or external integration.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace truthful connection and worker safety through production routes</name>
  <reversibility rating="reversible">The change is confined to an additive JSON safety field, route-local browser state, and test harness wiring; it can be reverted without data migration or external contract breakage.</reversibility>
  <files>tests/test_advanced_diagnosis_api.py, tests/test_advanced_ui.py, dashboard/beacon/diagnosis.py, dashboard/advanced.js</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` — implement D-09/D-10/D-11/D-13 exactly: independent connection/worker/recovery warnings, server freshness evidence, no causal claim, and retained last-success data.
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` — preserve the static warning order, exact refresh-failure copy, fixed refresh choices, and independently visible safety conditions.
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` — close only the still-open safety/freshness fault; treat the production CSS finding as closed by commit 1ab5db7 and add regression evidence only.
    - `dashboard/beacon/worker_main.py` — use immutable J1 schedule metadata as the worker heartbeat cadence; do not substitute J2 metric sampling settings.
    - `dashboard/beacon/diagnosis.py` — derive one `worker_stale` boolean from the server-composed worker freshness without merging it with recovery-required.
    - `dashboard/advanced.js` — keep the successful snapshot/lastSuccessLabel in the failed-fetch path and make connection availability route-local rather than a field expected from a successful API response.
    - `dashboard/app.py` and `dashboard/Dockerfile` — retain the deployed advanced route/asset adapters and image manifest from 1ab5db7 unchanged.
    - `tests/test_advanced_ui.py` — replace the file-server boundary with the real Flask WSGI application; intercept only `/api/advanced/current` when deterministic success/failure payloads are needed.
    - `tests/test_advanced_diagnosis_api.py` — extend the current real-route and temporary-SQLite harness rather than creating a second application bootstrap.
  </read_first>
  <behavior>
    - "RED first: an API regression loads with METRIC_SAMPLE_SECONDS set to a non-default value, seeds worker heartbeat ages at 5, 20, and 21 seconds, and proves the worker cadence remains 5 while safety.worker_stale changes only after the J1 four-cadence boundary."
    - "RED first: a Playwright regression receives a successful snapshot, then a 503, then a successful snapshot; connection becomes hidden, visible, then hidden while the prior evidence/time survive the failure."
    - "Worker-stale and recovery-required remain visible during the connection failure, and the subsequent success clears only conditions whose new evidence is healthy."
    - "The Playwright document, script, and stylesheet are retrieved through Flask's production routes with nonempty content, expected MIME types, CSP, and frame protection; only internal JSON is fixture-routed."
  </behavior>
  <action>Write the failing API and browser regressions first. In `compose_pipeline_diagnosis()`, obtain worker cadence from J1's immutable interval schedule through the existing callback inventory/schedule helper, never from `settings.metric_sample_seconds`; add `safety.worker_stale` in `get_current_diagnosis()` by treating server freshness `stale` and `unknown` as unavailable while leaving `recovery_required` independently marker-derived. In `advanced.js`, add memory-only `state.connectionUnavailable`; make `renderSafety()` use that field for the connection banner and the server `safety.worker_stale`/`safety.recovery_required` fields for the other banners. Set connection unavailable in the failed-fetch path, call only the safety/error renderers there so D-13's prior snapshot and last-success time remain intact, and clear it before rendering the next successful snapshot. Do not persist connection state or infer worker state in the browser. Replace `tests/test_advanced_ui.py`'s `SimpleHTTPRequestHandler` with a real local WSGI server around the temporary Beacon Flask app, retain Playwright route interception solely for `/api/advanced/current` fixture transitions, and assert document/assets actually came from the production adapters. Keep the existing Docker packaging and `serve_advanced_css()` implementation unchanged while verifying them through the focused route/manifest tests.</action>
  <acceptance_criteria>
    - Non-default metric sampling cannot change `pipeline.worker.expected_cadence_seconds`, the 20-second readiness-aligned aging boundary, or the 21-second stale warning result.
    - A failed advanced GET visibly activates connection warning without altering the last successful snapshot/time, worker warning, or recovery warning; the next success clears connection and applies only the new server conditions.
    - Connection, worker, and recovery banners retain the exact approved DOM order and independently pass all eight boolean combinations.
    - Playwright loads the advanced page bundle through the Flask production adapters, and focused coverage proves `advanced.css` is nonempty and production-packaged without rewriting the closed fix.
    - The endpoint remains GET-only/no-store/effect-free and the browser still issues one same-origin current-diagnosis GET per refresh.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -k "worker_safety or connection_warning or safety_combinations or production_routes or production_image_manifest" -x</automated>
  </verify>
  <done>The operator receives truthful, independent connection/worker/recovery warnings through the real production route stack, with immutable heartbeat freshness and retained evidence on refresh failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden current service ordering and exact bounded gap disclosure</name>
  <reversibility rating="reversible">The changes adjust internal read-model normalization and sentinel query shape without changing persisted data or accepting new input.</reversibility>
  <files>tests/test_advanced_diagnosis_api.py, dashboard/beacon/repositories.py, dashboard/beacon/diagnosis.py</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` — preserve D-06 operational ordering, D-09 evidence separation, D-10 freshness vocabulary, and D-11 non-causal semantics.
    - `.planning/phases/03-advanced-current-diagnosis/03-RESEARCH.md` — apply Pitfall 1's latest-probe evidence and fixed full/down probe schedules, plus the bounded one-read contract.
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` — close the unknown cadence, unsafe pinned-order coercion, and exactly-at-cap truncation warnings together.
    - `dashboard/beacon/diagnosis.py::compose_service_diagnosis` — normalize durable service facts before `operational_service_key()` sorts them.
    - `dashboard/beacon/repositories.py::read_pipeline_evidence` — preserve fixed parameterized `ORDER BY`/`LIMIT` SQL and return at most the configured cap.
    - `tests/test_advanced_diagnosis_api.py` — seed malformed SQLite metadata and exact 0/1/48/49 gap cardinalities through the real API contract.
  </read_first>
  <behavior>
    - "A service with no classified availability and no probe timestamp exposes the regular 300-second full-probe cadence plus unknown freshness; only a classified offline service exposes the 60-second down-recheck cadence."
    - "Validated pinned integers 0 and 65535 are preserved; null, boolean, out-of-range, numeric-text, and corrupt-text durable values use the service port as deterministic fallback and never produce HTTP 500."
    - "Gap responses contain at most 48 items; 0, 1, and exactly 48 rows report truncated false, while 49 or more report truncated true and still return the deterministically ordered first 48 rows."
    - "Malformed metadata affects only that service's presentation ordering and does not change availability, freshness, other services, SQLite contents, or the read-only API contract."
  </behavior>
  <action>Add failing table-driven API tests first. In `compose_service_diagnosis()`, choose 60 seconds only when current availability is definitively offline and use the regular 300-second full-probe cadence for online or unknown availability; let absent probe time keep freshness unknown. Add `_safe_pinned_order(value, fallback)` that accepts only non-boolean integers within the existing 0..65535 metadata contract and otherwise returns the validated service port, so durable corruption cannot escape as a serialization failure or reorder all services ahead of configured entries. In `read_pipeline_evidence()`, normalize the bounded `gap_limit`, request one additional sentinel row with the existing deterministic ordering, return only the first `gap_limit` items, and return `gaps_truncated` from whether that sentinel existed. In `compose_pipeline_diagnosis()`, consume that explicit boolean instead of inferring truncation from returned length. Do not repair or mutate corrupt metadata during this GET, change database schema, expand the cap, alter pending aggregation semantics, or add a second query transaction.</action>
  <acceptance_criteria>
    - Unknown, online, and offline service fixtures expose 300, 300, and 60 second expected cadences respectively, with freshness still determined only by valid timestamp/cadence evidence.
    - Every malformed/legacy pinned-order fixture returns HTTP 200, preserves the validated port fallback and deterministic D-06 order, and leaves SQLite byte-for-byte logically unchanged.
    - Exact 0/1/48/49 gap fixtures prove item count, ordering, cap, and truncation truth without a count scan or unbounded read.
    - Existing service, pipeline, GET-only, query-rejection, asset-route, UI, and module-boundary suites remain green.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py -k "service_cadence or pinned_order or gap_truncation or bounded" -x</automated>
  </verify>
  <done>Every current service and bounded gap list remains truthful and available even at exact caps or with legacy/corrupt presentation metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser fetch -> safety cluster | An HTTP failure is browser-observable evidence and must not be confused with server worker/recovery facts or a successful refresh. |
| Flask/read model -> SQLite | Durable timestamps, metadata affinity values, and capped row collections may be absent or malformed but must remain bounded and non-mutating. |
| Flask static adapters -> browser | The production document bundle must be served with substantive content and existing security headers rather than bypassed by a test-only file server. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-22 | Spoofing | `advanced.js` safety transitions | high | mitigate | Model connection failure in route-local state, worker/recovery from server facts, preserve independent banners, and exercise all combinations plus fail/recover transitions in Playwright. |
| T-03-23 | Spoofing | worker heartbeat diagnosis | high | mitigate | Derive cadence from immutable J1 metadata, add non-default metric-cadence regressions, and compare the four-cadence result with the established readiness boundary. |
| T-03-24 | Denial of Service | pipeline gap reader | medium | mitigate | Fetch only cap plus one under fixed ordering, slice to cap before projection, retain one read transaction, and test exact/over-cap cardinalities. |
| T-03-25 | Tampering | corrupt `pinned_order` SQLite value | medium | mitigate | Accept only validated non-boolean bounded integers in the read model, fall back to service port, and prove the GET performs no repair write. |
| T-03-26 | Spoofing | unknown service cadence | medium | mitigate | Use full-probe cadence unless a service is definitively offline, keep absent timestamps unknown, and test all three availability states. |
| T-03-27 | Information Disclosure | host/service evidence in production-route tests | low | accept | Tests use temporary local SQLite fixtures and the product intentionally exposes current local metrics to its trusted-LAN operator; no secret or external endpoint is introduced. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---|---|---|
| V4 Access Control | Applicable | Production route tests retain trusted-host middleware and the advanced surface remains GET-only. |
| V5 Validation, Sanitization and Encoding | Applicable | Durable integers/timestamps are normalized, capped queries use fixed SQL, and browser evidence renders through existing text DOM APIs. |
| V2 Authentication | Not applicable | Trusted-LAN scope introduces no account or identity system. |
| V3 Session Management | Not applicable | Connection state is memory-only and presentation preferences retain their existing non-session contract. |
| V6 Stored Cryptography | Not applicable | No secret, key, or cryptographic state is handled. |
</threat_model>

<gap_coverage>

| Verification finding | Disposition | Closure evidence |
|---|---|---|
| Production advanced CSS/assets missing | CLOSED BEFORE THIS PLAN by deployed commit 1ab5db7 | Task 1 production-route and image-manifest regressions only; no reimplementation. |
| Connection and worker safety inputs are unwired | IMPLEMENT in Task 1 | Browser failure/recovery transition plus server `safety.worker_stale`, all warning combinations. |
| Worker cadence incorrectly follows metric sampling | IMPLEMENT in Task 1 | Non-default metric cadence proves J1 remains five seconds and stale begins beyond 20 seconds. |
| Unknown service uses down-recheck cadence | IMPLEMENT in Task 2 | Unknown/online/offline table proves 300/300/60 seconds. |
| Corrupt/legacy pinned order can return 500 | IMPLEMENT in Task 2 | Durable malformed-value matrix proves HTTP 200, deterministic fallback, no write. |
| Exactly-at-cap gaps falsely report truncation | IMPLEMENT in Task 2 | 0/1/48/49 cardinality matrix proves sentinel semantics. |
| Browser tests bypass production routes | IMPLEMENT in Task 1 | Real Flask WSGI document/asset boundary; fixture interception limited to internal JSON. |
| Pi 15-minute responsiveness/continuity check | DEFERRED ONLY TO ORDERED GAP PLAN 03-06 | Blocking target-hardware checkpoint after automated closure ships. |

</gap_coverage>

<source_coverage_audit>

| Source | ID | Feature / requirement / decision | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Enter from either theme and quickly diagnose current Pi, every service, settings, and collection health | 03-01 through 03-05; Pi proof in 03-06 | COVERED | Gap plans repair truthfulness without replacing the workspace. |
| REQ | TEL-06 | Retention, resolution, pressure, worker freshness, gaps, and job health | 03-02/03-03; 03-05 Tasks 1-2; 03-06 | COVERED | Worker/gap truth and target-load proof close remaining TEL-06 evidence. |
| REQ | DIA-01 | Dedicated advanced page from either theme | 03-01/03-03/03-04; 03-05 Task 1 | COVERED | Production route regression covers the deployed bundle. |
| REQ | DIA-02 | Current host facts and freshness | 03-01/03-02/03-03 | COVERED | No open host defect; retained by focused/full regression gates. |
| REQ | DIA-03 | Every service's current diagnostic evidence | 03-02/03-04; 03-05 Task 2 | COVERED | Unknown cadence and corrupt ordering are explicitly closed. |
| REQ | DIA-08 | Effective settings and local-only controls | 03-02/03-03/03-04; 03-05 Task 1; 03-06 | COVERED | Safety/refresh stays read-only and Pi load is checked. |
| REQ | UX-02 | Theme-preserving dashboard/advanced movement | 03-04; 03-05 Task 1 | COVERED | Real production document routes retain round-trip coverage. |
| RESEARCH | — | One bounded, parameterless, GET-only current snapshot | 03-01/03-02; 03-05 Task 2 | COVERED | Sentinel read remains fixed/capped and no mutation is added. |
| RESEARCH | — | Durable worker-owned job evidence under exact epoch | 03-02 | COVERED | Gap work consumes existing job evidence and changes no writer/schema. |
| RESEARCH | — | Server-derived host/service/worker/stream freshness | 03-01/03-02; 03-05 Tasks 1-2 | COVERED | Worker uses J1; unknown service uses full-probe schedule. |
| RESEARCH | — | Separate connection, worker, recovery, TLS, availability, gaps, pending, expiry, and pressure facts | 03-02/03-03/03-04; 03-05 Task 1 | COVERED | Safety producers now match consumers without conflation. |
| RESEARCH | — | Responsive vanilla UI, local preferences, no mutation/history/package | 03-03/03-04; 03-05 regression gates | COVERED | No new dependency or control surface. |
| RESEARCH | — | Frequent bounded reads must not create Pi sampling gaps | 03-02 query shape; 03-06 target checkpoint | COVERED | Human-only target-hardware evidence is ordered, not omitted. |
| CONTEXT | D-01 | Overview-first five-section workspace | 03-03 | COVERED | Unchanged. |
| CONTEXT | D-02 | Exceptions before subsystem summaries | 03-03 | COVERED | Unchanged. |
| CONTEXT | D-03 | Persistent section navigation | 03-03 | COVERED | Unchanged. |
| CONTEXT | D-04 | Same-tab route, Dashboard return, theme/preferences/scroll | 03-04; 03-05 Task 1 production route | COVERED | Unchanged behavior under real routes. |
| CONTEXT | D-05 | Compact sortable expandable service table | 03-04 | COVERED | Unchanged. |
| CONTEXT | D-06 | Operational priority and pinned order | 03-02/03-04; 03-05 Task 2 | COVERED | Corrupt values receive deterministic port fallback. |
| CONTEXT | D-07 | Exact collapsed/expanded service evidence | 03-02/03-04; 03-05 Task 2 | COVERED | Correct cadence remains visible. |
| CONTEXT | D-08 | Multiple expansions and collapse-all | 03-04 | COVERED | Unchanged. |
| CONTEXT | D-09 | Distinct connection/worker/recovery and pipeline conditions | 03-02/03-03; 03-05 Task 1 | COVERED | Open blocker directly closed. |
| CONTEXT | D-10 | Four-state freshness with age/time/cadence | 03-01/03-02/03-03; 03-05 Tasks 1-2 | COVERED | J1 and unknown-service evidence corrected. |
| CONTEXT | D-11 | No cause inference; show worker and stream facts together | 03-02/03-03; 03-05 Task 1 | COVERED | Browser uses server worker state only. |
| CONTEXT | D-12 | Actionable gaps global, older gaps in Pipeline | 03-02/03-03; 03-05 Task 2 | COVERED | Exact truncation disclosure retains bounded detail. |
| CONTEXT | D-13 | Fixed auto-refresh, pause, refresh now, last update | 03-03; 03-05 Task 1 | COVERED | Failure state now drives connection warning truthfully. |
| CONTEXT | D-14 | Persist only explicit preferences | 03-03/03-04; 03-05 Task 1 | COVERED | Connection state is explicitly memory-only. |
| CONTEXT | D-15 | Service search/filters/count/clear | 03-04 | COVERED | Unchanged. |
| CONTEXT | D-16 | Theme-aware density defaults and parity | 03-03/03-04; 03-05 production-route regression | COVERED | Unchanged. |
| PROBE | 13 rows | TEL-06 (3), DIA-01 (1), DIA-02 (1), DIA-03 (3), DIA-08 (3), UX-02 (2) | 03-05 frontmatter | FLAGGED-UNVERIFIED | Descriptor-less rows are surfaced verbatim by ID/category; no predicates are invented. |
| PROHIBITION | values/safety only | Descriptor-less fallback prohibitions | 03-05 frontmatter | FLAGGED-UNVERIFIED | Only values/safety categories are retained; no predicate is invented. |

</source_coverage_audit>

<verification>
- Safety/production tracer: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -k "worker_safety or connection_warning or safety_combinations or production_routes or production_image_manifest" -x`.
- Diagnosis edges: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py -k "service_cadence or pinned_order or gap_truncation or bounded" -x`.
- Phase regression: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -x`.
- Full project gate: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- All still-open code-proven verifier gaps and warnings are closed with deterministic tests; commit 1ab5db7's asset delivery remains unchanged and regression-proven.
- Connection, worker, and recovery warnings are truthful, independent, and ordered under success, failure, and recovery transitions.
- Worker freshness uses immutable heartbeat evidence, unknown services use full-probe cadence, corrupt pinned order is total, and exactly-at-cap lists are not called truncated.
- The production Flask route stack, bounded one-read API, GET-only/browser-local controls, UI-SPEC copy/state contract, and all six phase requirements remain covered.
- No high-severity threat is accepted or transferred; the two high threats have executable API/Playwright mitigations.
</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-05-SUMMARY.md` when done.
</output>
