---
phase: 03-advanced-current-diagnosis
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified: []
autonomous: false
gap_closure: true
requirements: [TEL-06, DIA-02, DIA-03, DIA-08]

must_haves:
  truths:
    - "On a 64-bit Raspberry Pi running the production Compose deployment with representative monitored services, the advanced workspace remains responsive throughout a continuous 15-minute run at the allowed five-second refresh interval."
    - "Host sampling, service checks, worker heartbeat, and pipeline evidence show no visible collection gap during the observation, and the advanced snapshot does not create sustained SQLite lock/busy contention."
    - "The operator records start/midpoint/end API latency plus container CPU/RAM evidence and explicitly approves or rejects the run; an unrecorded observation cannot complete the phase."
  artifacts: []
  key_links:
    - from: "five-second /advanced browser polling"
      to: "GET /api/advanced/current -> one bounded SQLite read"
      via: "production Compose web container under simultaneous worker sampling/probing"
      pattern: "/api/advanced/current"
    - from: "worker sampling/probing evidence"
      to: "Advanced Host/Services/Pipeline freshness and collection-gap surfaces"
      via: "timestamps, server freshness, durable gaps, job state, and worker heartbeat observed for the entire run"
      pattern: "freshness|collection gap|worker heartbeat"
---

<objective>
Complete the target-hardware behavior gate that automated fixtures cannot establish.

Purpose: Prove five-second advanced reads remain usable and do not starve essential monitoring or create sustained SQLite contention on the actual Raspberry Pi deployment.
Output: A blocking operator verdict with recorded resource/latency observations; no source or configuration artifact is created.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md
@.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md
@.planning/phases/03-advanced-current-diagnosis/03-VALIDATION.md
@.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md
@.planning/phases/03-advanced-current-diagnosis/03-05-SUMMARY.md

<interfaces>
- The UI-SPEC permits a five-second refresh interval and requires one bounded GET per tick while the worker continues its own immutable heartbeat, host sampling, probes, and scheduled jobs.
- `/readyz` returns 200 only while the durable worker heartbeat is within the established readiness threshold; `/api/advanced/current` exposes host/service/worker/stream freshness and collection gaps without triggering work.
- `docker compose ps`, `docker compose logs`, and `docker stats --no-stream` are read-only operational evidence after the already-authorized production deployment is running.
- This plan is verification-only: no product code, database schema/data, preference default, Compose setting, monitored-service configuration, or acceptance threshold may be changed to make the run pass.
</interfaces>
</context>

## Artifacts this phase produces

- None. The executor records the operator's measured start/midpoint/end latency, CPU/RAM snapshots, visible freshness/gap observations, log review, and approval/rejection in `03-06-SUMMARY.md`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Observe five-second Advanced diagnosis under representative Pi load</name>
  <precondition>A 64-bit Raspberry Pi is running the production Compose image that includes completed Plan 03-05; `docker compose ps` shows web and worker running, `curl -fsS http://127.0.0.1:8080/readyz` succeeds, and representative configured/discovered services are present in Advanced diagnosis.</precondition>
  <files>None — verification-only checkpoint; record evidence in 03-06-SUMMARY.md after approval or rejection.</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-VALIDATION.md` — execute the only manual Phase 3 behavior exactly: representative Pi polling/SQLite-read overhead for 15 minutes at five seconds.
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` — use the stated expected result: responsive interaction, no visible sampling gap, and no sustained SQLite contention.
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` — use the supported five-second selector, server freshness/gap evidence, and local-only interactions without changing monitoring settings.
    - `.planning/phases/03-advanced-current-diagnosis/03-05-SUMMARY.md` — confirm all automated safety/freshness/edge/production-route regressions passed before target observation.
  </read_first>
  <what-built>Phase 3's production Advanced diagnosis page, including the Plan 03-05 truthful safety/freshness closure, running as the normal web and worker Compose services on Raspberry Pi hardware.</what-built>
  <action>Before the timer starts, record `docker compose ps`, one `docker stats --no-stream` snapshot for web and worker, one `/api/advanced/current` response time using curl's total-time output, and the displayed Host/Services/Pipeline freshness and gap state. In the browser select the five-second interval and leave scheduled updates running for 15 continuous minutes. During the run, use Overview, Host, Services, and Pipeline; exercise a service filter, sort, detail disclosure, and at least three `Refresh now` actions distributed near the start, midpoint, and end. At approximately 7.5 minutes and again after 15 minutes, record the same curl response time and container CPU/RAM snapshot. Keep the page visible for failure/error evidence and review `docker compose logs --since 20m web worker` after the run for recurring SQLite busy/locked errors or worker callback failures. Do not restart containers, pause updates, change cadences, change monitored services, clear gaps, or alter database/configuration during the observation.</action>
  <how-to-verify>
    1. Confirm the page continues to accept section, filter, sort, disclosure, and manual-refresh input throughout the full 15 minutes; no UI stall persists through a five-second refresh cycle.
    2. Confirm Host, every representative service, worker heartbeat, and relevant pipeline streams continue advancing or remain truthfully fresh/aging according to their displayed cadence; no new actionable collection gap or stale worker condition appears because of the polling run.
    3. Confirm the error/connection warning does not recur under normal LAN connectivity and the page never replaces prior evidence with invented healthy/zero values.
    4. Confirm the post-run web/worker logs contain no recurring SQLite busy/locked error and no sequence of failed background jobs attributable to the advanced reads.
    5. Provide the start/midpoint/end API total-time values, the three web/worker CPU/RAM snapshots, the representative service count, and any warning/gap/log observation with the resume response.
  </how-to-verify>
  <acceptance_criteria>
    - The observation lasts 15 uninterrupted minutes at the UI's five-second setting on target Raspberry Pi hardware with representative monitored services.
    - Interaction remains responsive, all scheduled/manual refreshes settle, and no warning indicates lost API connection under otherwise healthy LAN conditions.
    - Essential host/service/worker/stream evidence continues within its displayed cadence without a new visible/actionable collection gap.
    - Logs show no sustained SQLite contention or recurring callback failure caused by the advanced read workload.
    - Start/midpoint/end latency and CPU/RAM measurements plus the operator verdict are recorded; any failure rejects the checkpoint and returns concrete evidence for another gap plan.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -x</automated>
    <human-check>Perform the numbered 15-minute Raspberry Pi observation above and compare the recorded behavior with every acceptance criterion.</human-check>
  </verify>
  <done>The operator has approved a recorded 15-minute target-Pi run showing responsive five-second Advanced diagnosis with continuous essential sampling and no sustained SQLite contention, or has rejected it with concrete evidence for replanning.</done>
  <resume-signal>Reply `approved` with the start/midpoint/end API latency, web/worker CPU/RAM snapshots, representative service count, and gap/log observations; otherwise describe the first failing time and evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser polling -> production Flask/SQLite | The lowest supported refresh interval repeatedly crosses the production read path while worker writes continue. |
| Pi runtime evidence -> acceptance decision | Operator-visible state, container metrics, response latency, and logs must be recorded rather than replaced by fixture assumptions. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-28 | Denial of Service | five-second advanced polling | medium | mitigate | Use the shipped fixed interval for a bounded 15-minute production observation, exercise interactions, record latency/resources, and reject on UI stalls, sampling gaps, or recurring contention. |
| T-03-29 | Spoofing | target-hardware acceptance evidence | medium | mitigate | Require start/midpoint/end measurements, live freshness/gap observations, post-run logs, and explicit operator approval; fixtures alone cannot complete the checkpoint. |
| T-03-30 | Information Disclosure | local container metrics/logs | low | accept | Evidence remains with the local operator and summary; the procedure requires no secret, opaque worker epoch, outbound upload, or external service. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---|---|---|
| V4 Access Control | Applicable | Observation uses existing GET/read surfaces and local Compose diagnostics; no mutation/control endpoint is added. |
| V5 Validation, Sanitization and Encoding | Applicable | Acceptance requires concrete measured values and rejects missing evidence rather than inferring a pass. |
| V2 Authentication | Not applicable | Trusted-LAN target deployment has no account feature in this phase. |
| V3 Session Management | Not applicable | The browser carries presentation preferences only. |
| V6 Stored Cryptography | Not applicable | No cryptographic material is accessed or recorded. |
</threat_model>

<verification>
- Automated preflight: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -x`.
- Target behavior: blocking 15-minute Raspberry Pi observation with recorded start/midpoint/end latency and CPU/RAM, responsive interactions, continuous freshness/gap evidence, and post-run SQLite/job log review.
</verification>

<success_criteria>
- The only behavior left unverified by repository fixtures is explicitly observed on target hardware and receives a recorded operator verdict.
- Five-second production polling remains responsive and does not create visible essential-sampling gaps or sustained SQLite contention.
- Failure cannot be waived: any missing measurement, stale/gap condition attributable to the run, recurring contention, or persistent UI stall returns evidence for a new gap plan.
- No source, schema, configuration, monitored-service, or runtime threshold change is made during this verification-only plan.
</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-06-SUMMARY.md` when done.
</output>
