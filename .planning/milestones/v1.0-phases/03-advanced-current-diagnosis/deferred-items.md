# Phase 03 — Deferred Items

Out-of-scope discoveries found while executing this phase. Logged, not fixed.

Scope of this file: out-of-scope discoveries made while executing this phase,
plus two sets recorded by plan 03-16 — the round-3 `03-REVIEW.md` findings that
gap-closure round deliberately did not implement (rows 2-6), and the seventeen
round-1 findings carried forward in `03-REVIEW.md` as still open and not
re-argued (row 7). It does not record every review finding: the findings that
were implemented are recorded in their own plans' summaries, and the round-1
carried-forward set's full argument lives in `03-REVIEW.md`'s own table, not
here.

| # | Found during | File | Issue | Why deferred |
|---|--------------|------|-------|--------------|
| 1 | 03-09 Task 2 | `dashboard/advanced.js` (`renderServices` latency cell, `stableServiceSort` latency branch) | A service whose `latency_ms` is `null` renders as `0 ms` and sorts as the fastest service, because `Number(null)` is `0` and therefore passes `Number.isFinite`. The failure class is available on the same row but is never shown. | Pre-existing behaviour in the services table; not caused by, and not in the file regions touched by, plan 03-09's exception-copy or sort-persistence changes. Belongs with the service-table presentation work (03-02/03-04), not with this gap-closure plan. |
| 2 | 03-16 Task 2 | `dashboard/beacon/worker_main.py:296-302` | Review `WR-04` (round 3): the `started` bookkeeping write still gates the work, so a failure to *record* that a job began prevents J1 (heartbeat and worker-lease renewal), J2, J3/J4, J8 and J9 from running at all. | Not in either `03-VERIFICATION.md` gap's `missing` list, and the review's proposed remedy is not safe to adopt as a side effect of a gap-closure round. Logging and proceeding would run the callback after a write that failed *before* the worker-authority assertion inside `_write_job_health_transition` could complete, so the dispatch would proceed without a confirmed lease — which touches Phase 1's duplicate-execution contract. That trade needs its own decision, with the lease semantics re-argued explicitly. |
| 3 | 03-16 Task 2 | `dashboard/advanced.js:263-272` | Review `IN-02` (round 3): `formatServiceGapEvidence` prefers `block.count` over `items.length` and `block.open_count` over the derived open count, with no cross-check, so a server that disagreed with itself would print a declared number over a visibly different list. | Latent, and the reachable half is already closed. The server derives `count`, `open_count` and `items` from one list (`diagnosis.py`), so nothing today can disagree. Task 2's type-based `finiteMeasurement` removes the one shape where a declared count could win over a non-empty list — a non-numeric count now falls back to the items it is rendered beside, pinned by `test_non_numeric_server_values_never_become_a_measurement`. A full cross-check plus a mismatch policy (which value wins, and what the operator is told about the disagreement) is a design decision, not a verified gap. |
| 4 | 03-16 Task 2 | `dashboard/beacon/diagnosis.py:360, 393, 461` | Review `IN-04` (round 3): `attach_service_collection_gaps` documents in-place semantics, mutates in place, returns `services` anyway, and `get_current_diagnosis` discards the return. | Cosmetic. Both shapes are correct in isolation and the call site is consistent with the mutating one; no operator-facing effect and no correctness risk either way. Not reachable as a defect from the current server. |
| 5 | 03-16 Task 2 | `dashboard/beacon/diagnosis.py:381-386` | Review `IN-06` (round 3): `service['collection_gaps']['items']` is the same list object as `pipeline['streams']['items'][i]['gaps']`, so each gap row is serialized up to three times in a payload polled as often as every 5 s, and the per-service count is bounded by nothing while `pipeline.gaps.count` is capped at 48. | Bounded overall (≤48 coverage rows + ≤64 synthetic open gaps) and self-consistent, so it is not a recurrence of the Gap-3 defect. Removing the duplicate means changing the payload shape and the Pipeline streams region — scope this gap-closure round does not own, and a payload change this round explicitly forbids itself. Not reachable as operator-facing untruth. |
| 6 | 03-16 Task 2 | `dashboard/advanced.js:529-544` | Review `IN-08` (round 3): an unmeasured latency ranks as `POSITIVE_INFINITY`, so a descending sort puts every unmeasured service above the genuinely slowest measured one; and two unmeasured services compare as `Infinity - Infinity === NaN`, with stability preserved only because `NaN` is falsy. | The ranking is a deliberate choice, already pinned in both directions by the passing `test_unmeasured_latency_and_duration_never_rank_or_read_as_zero` — the cell shows the failure class rather than a number, so a down service at the top is defensible. The tie-break is correct today; `Number.isNaN(result) ? 0 : result` would make it deliberate rather than accidental, which is a readability change with no behavioural delta and no operator-facing effect. |
| 7 | 03-16 Task 2 | see `03-REVIEW.md` `## Carried forward from round 1 (still open, not re-argued)` | The seventeen round-1 review findings the reviewer verified as still present in the current tree: `CF-WR-01`, `CF-WR-02`, `CF-WR-03`, `CF-WR-04`, `CF-WR-05`, `CF-WR-07`, `CF-WR-08`, `CF-WR-09`, `CF-WR-10`, `CF-IN-01`, `CF-IN-02`, `CF-IN-03`, `CF-IN-04`, `CF-IN-05`, `CF-IN-06`, `CF-IN-07`, `CF-IN-08`. The full argument, severity and cited lines for each are in that table; they are not restated here. | Outside the scope of this gap-closure round, which is authorised by `03-VERIFICATION.md`'s two gaps — none of the seventeen appears in either. Carried open, not closed and not dismissed; recorded here so the set is visible to the next executor and the next verifier rather than silently absent. Two sit in code this round edits and were considered and left: `CF-WR-10` (the row-spread hazard at the collection-gap branch of `compose_active_exceptions`) — plan 03-15 Task 2 adds a neighbouring branch built from explicit keys, so it neither fixes nor worsens the hazard; and `CF-IN-07` (`countLabel` mapping a missing count to `0`) — the sibling of the `finiteMeasurement` path 03-16 Task 2 rewrites, left unchanged because `countLabel` is shared with the Pipeline collection-gaps region and its callers now pass a count already normalised by the type-based rule. |
| 8 | 03-17 Task 1 | `dashboard/app.py:1819-1825` (`worker_process_scan_requests` discovery-busy branch), `dashboard/app.py:1333-1334` (`_legacy_do_uptime_check` under `_uptime_lock` contention) | Two further `return False` sites that `dispatch_callback` still maps to a durable `('failed','CallbackReturnedFalse')` row and a `job_failed` operator card. The first fires when `worker_run_discovery` reports `busy` and the scan claim is requeued; the second fires when J3 and J4 overlap on `ThreadPoolExecutor(2)` and the later probe finds `_uptime_lock` already held. Neither is a fault: both mean "another run already owns this work". | Neither appears in any of `03-VERIFICATION.md`'s seven round-4 `missing` bullets, which name only the two empty-queue claim checks this task fixed. Both are transient and self-clearing on the very next scheduled tick (2 s for J5, 1 min for J4), unlike the empty-queue case which is permanent on an idle Pi and never clears. Changing either touches its own requeue/retry semantics -- the busy branch's interaction with `requeue_scan_for_worker` and the lock branch's contract with the overlapping probe -- which deserves its own review rather than arriving as a side effect of the two-line idle-queue fix made here. |
| 9 | 03-19 Task 3 | `dashboard/app.py` (`worker_process_scan_requests` discovery-busy branch), `dashboard/app.py` (`_legacy_do_uptime_check` under `_uptime_lock` contention) | Both of row 8's two sites are now CLOSED. Each returns `None` instead of `False`, so `dispatch_callback`'s unchanged `if result is False:` mapping records a durable `succeeded` outcome rather than a fabricated `('failed','CallbackReturnedFalse')` row and `job_failed` card. Pinned by `test_a_discovery_busy_scan_requeue_is_not_reported_as_a_failure` and `test_an_uptime_lock_collision_is_not_reported_as_a_failure`, which also assert the requeue and lock contracts row 8 flagged as the reason to defer: the busy claim still reads `status='queued'` afterwards, and the losing probe still yields to the holder. | Closing both sites means a busy discovery lock or a contended uptime probe never again shows the operator a `Background job failed` card for a run that merely lost a lock to a concurrent probe of the same kind. |
