---
phase: 03.1-planned-maintenance-recognition
plan: 02
subsystem: infrastructure
tags: [tzdata, zoneinfo, docker-compose, dst, timezone]

# Dependency graph
requires:
  - phase: 03.1-planned-maintenance-recognition/03.1-01
    provides: "Settings.timezone field and _resolved_timezone fail-closed helper, satisfied on a dev machine by the host's own system tzdata"
provides:
  - "tzdata==2026.3 pinned in dashboard/pyproject.toml and resolved in dashboard/uv.lock, so ZoneInfo resolves inside the built container which has no /usr/share/zoneinfo"
  - "TZ environment plumbing through docker-compose.yml to both the worker and web services, defaulting to UTC"
  - "TimezoneEnvironmentTests: an unmocked regression that fails inside any environment missing the IANA database"
affects: [03.1-03-window-crud, 03.1-04-overrun-detection, 03.1-06-suggestion-detector, 03.1-07-active-maintenance-display]

# Actuals (#2632)
actuals:
  tokens: 1300
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: ["tzdata==2026.3 (PyPI, github.com/python/tzdata)"]
  patterns:
    - "docker-compose.yml services that need a per-service environment addition on top of the shared &beacon-environment anchor re-declare `environment: {<<: *beacon-environment, NEWKEY: value}` rather than duplicating the whole map by hand -- verified via `docker compose config` and a direct YAML-merge parse, both showing every original key preserved plus the addition."

key-files:
  created: []
  modified:
    - dashboard/pyproject.toml
    - dashboard/uv.lock
    - docker-compose.yml
    - tests/test_maintenance_windows.py

key-decisions:
  - "Task 1 (pre-resolved by human operator): approved installing tzdata==2026.3 after reviewing pypi.org/project/tzdata -- confirmed Homepage/Source both point at github.com/python/tzdata (the CPython org's own mirror), maintainer is a CPython core datetime-sig developer, release history runs 2016-2026, and the SUS verdict was driven solely by unknown-downloads, not any provenance/freshness signal."
  - "TZ is plumbed via a per-service `environment: {<<: *beacon-environment, TZ: \"${TZ:-UTC}\"}` override on worker and web only -- not by adding TZ to the shared top-level &beacon-environment anchor -- because the plan's acceptance test reads docker-compose.yml as text and asserts the TZ token appears literally within each service's own block; a shared-anchor-only addition would not satisfy that textual assertion even though the merged runtime config would still be correct. recovery and data-init deliberately do not receive TZ, matching the plan's scope."

patterns-established:
  - "A regression that imports zoneinfo.ZoneInfo and asserts a real UTC-offset difference between a January and July instant, left completely unmocked -- this is the only test in the suite that would fail inside a container lacking the IANA database, per Pitfall 2 in 03.1-RESEARCH.md."

requirements-completed: [MNT-01]

coverage:
  - id: T1
    description: "A named IANA zone (America/New_York, Europe/London) resolves via zoneinfo.ZoneInfo in the project's runtime environment, exercised both through pytest and a standalone CLI invocation"
    requirement: MNT-01
    verification:
      - kind: unit
        ref: "tests/test_maintenance_windows.py#TimezoneEnvironmentTests::test_a_named_iana_zone_resolves_in_this_environment"
        status: pass
      - kind: cli
        ref: "uv run --project dashboard python -c \"from zoneinfo import ZoneInfo; ZoneInfo('America/New_York'); ZoneInfo('Europe/London')\""
        status: pass
    human_judgment: false
  - id: T2
    description: "load_settings reads TZ into Settings.timezone on a valid zone, and fails closed to the literal UTC (raising nothing) on an unresolvable, empty, or absent TZ value"
    requirement: MNT-01
    verification:
      - kind: unit
        ref: "tests/test_maintenance_windows.py#TimezoneEnvironmentTests (4 tests: reads_the_tz_environment_key, falls_back_to_utc_on_an_unresolvable_zone, falls_back_to_utc_on_an_empty_tz, plus the standalone load_settings({'TZ': 'Not/AZone'}) CLI check)"
        status: pass
    human_judgment: false
  - id: T3
    description: "tzdata is pinned in both dashboard/pyproject.toml and the uv-regenerated dashboard/uv.lock; docker-compose.yml supplies TZ to both worker and web; dashboard/Dockerfile contains no apt-get tzdata line"
    requirement: MNT-01
    verification:
      - kind: unit
        ref: "tests/test_maintenance_windows.py#TimezoneEnvironmentTests (test_the_compose_file_supplies_tz_to_the_worker_and_web_services, test_the_project_pins_the_iana_database_as_a_python_dependency)"
        status: pass
      - kind: source-grep
        ref: "grep -c tzdata dashboard/pyproject.toml (1), grep -c tzdata dashboard/uv.lock (6), grep -c TZ docker-compose.yml (2), grep -Eic 'apt-get.*tzdata' dashboard/Dockerfile (0)"
        status: pass
      - kind: config-validation
        ref: "docker compose config -- resolves TZ: UTC for both worker and web with every pre-existing environment key intact"
        status: pass
    human_judgment: false
  - id: T4
    description: "Full existing suite stays green after the dependency and compose changes"
    verification:
      - kind: unit
        ref: "uv run --project dashboard python -m pytest -q -- 331 passed, 454 subtests passed, 0 failed (up from 325/454 on 03.1-01, +6 new TimezoneEnvironmentTests)"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-08-22
status: complete
---

# Phase 03.1 Plan 02: Container Timezone Summary

**`tzdata==2026.3` pinned through `uv.lock` (never an apt package) closes the built-container gap where `python:3.12-slim-bookworm` ships no `/usr/share/zoneinfo`, and the operator's real `TZ` now reaches both the worker and web containers with a UTC default.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 2 (Task 1: pre-resolved package-legitimacy checkpoint; Task 2: pin + plumb + test)
- **Files modified:** 4 (0 created)

## Accomplishments
- `dashboard/pyproject.toml`: added `tzdata==2026.3` to `dependencies`, matching the exact equality-pin style every neighbouring dependency (`psutil==7.2.2`, `requests==2.34.2`, etc.) already uses.
- `dashboard/uv.lock`: regenerated via `uv lock --project dashboard` (never hand-edited) — resolved 51 packages, `tzdata` entry carries its sdist/wheel hashes and `2026.3` version pin (verified: `grep -c tzdata dashboard/uv.lock` → 6 matches across the dependency-graph and package-metadata sections).
- `docker-compose.yml`: `worker` and `web` services each gained `environment: {<<: *beacon-environment, TZ: "${TZ:-UTC}"}` — the nested YAML merge-key pattern preserves every existing shared env var (verified by direct YAML parse and by `docker compose config`, which resolves `TZ: UTC` for both services with no other keys lost) while satisfying the plan's textual acceptance check that `TZ` appears literally inside each service's own block. `recovery` and `data-init` deliberately do not receive `TZ`, exactly as the plan specifies.
- `dashboard/Dockerfile`: confirmed unchanged (read-only per the plan) — `grep -Eic 'apt-get.*tzdata' dashboard/Dockerfile` returns 0; the IANA database reaches the image exclusively through the existing frozen `uv sync` step, never a system package.
- `tests/test_maintenance_windows.py`: new `TimezoneEnvironmentTests` class, all six methods named in the plan, all passing:
  - `test_a_named_iana_zone_resolves_in_this_environment` — real, unmocked `ZoneInfo` UTC-offset comparison across a January/July instant for both `America/New_York` and `Europe/London`; this is the one test that fails inside a container lacking the IANA database.
  - `test_load_settings_reads_the_tz_environment_key`
  - `test_load_settings_falls_back_to_utc_on_an_unresolvable_zone`
  - `test_load_settings_falls_back_to_utc_on_an_empty_tz` (covers both empty-string and key-absent)
  - `test_the_compose_file_supplies_tz_to_the_worker_and_web_services` — textual assertion that `TZ` appears within both the `worker:` and `web:` blocks of `docker-compose.yml`.
  - `test_the_project_pins_the_iana_database_as_a_python_dependency` — textual assertion that `tzdata` appears in `dashboard/pyproject.toml` and that `dashboard/Dockerfile` contains no `apt-get`-based tzdata install.

## Task Commits

Task 1 (`checkpoint:human-verify`, `gate="blocking-human"`) was pre-resolved by the human operator before this dispatch. The operator's approval evidence (Homepage/Source both resolving to `github.com/python/tzdata`, maintainer `belopolsky` of the CPython datetime-sig team, ten-year release history, SUS verdict driven solely by `unknown-downloads`) is recorded verbatim in the dispatch context and reproduced above in `key-decisions`. It carries no independent file change and is recorded here rather than as its own commit, per the executor's checkpoint-handling instructions — matching the precedent 03.1-01's Task 1 set.

1. **Task 2: Pin tzdata and plumb TZ into the containers** - `0eb0309` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `dashboard/pyproject.toml` - `tzdata==2026.3` added to `dependencies`
- `dashboard/uv.lock` - regenerated via `uv lock`, records the resolved `tzdata` hash
- `docker-compose.yml` - `TZ: "${TZ:-UTC}"` added to the `worker` and `web` services' `environment:` blocks
- `tests/test_maintenance_windows.py` - `TimezoneEnvironmentTests` (6 new tests)

## Decisions Made
- **Task 1's package-legitimacy verdict (pre-resolved, approved):** documented in frontmatter `key-decisions` above; the confirmed version `2026.3` is the one pinned.
- **Per-service `environment:` override rather than a shared-anchor-only addition:** chosen so the plan's textual acceptance test (grepping `docker-compose.yml` for `TZ` inside each service's own block) is satisfied honestly, rather than relying on YAML-anchor semantics the text-based test cannot see. Verified both by parsing the merged YAML directly (all quotes preserved plus `TZ`) and by `docker compose config`.

## Deviations from Plan

None - plan executed exactly as written. Task 1's checkpoint was pre-resolved per the dispatch context; Task 2's action, verify, and acceptance-criteria steps were followed as specified with no auto-fixes required.

## Issues Encountered
None.

## User Setup Required
Per the plan's `user_setup` block: the operator must set the `TZ` environment variable (e.g. via the shell or a `.env` file that runs `docker compose`) to the Pi's own configured IANA zone name — the output of `timedatectl show -p Timezone --value` on the host. `docker-compose.yml` now defaults `TZ` to `UTC` when the operator sets nothing, so the deployment remains functional (just timezone-naive) without this step; window matching only honours the Pi's real local time once it is set. No action was taken on the operator's behalf, per the plan's discretion note — this is documented, not performed, since the actual host zone name is not knowable from inside this dispatch.

## Next Phase Readiness
- The container time-zone gap the tracer (03.1-01) named as its precondition is now closed: `ZoneInfo` resolves inside the built image via the pinned `tzdata` dependency, not just on a developer machine with system tzdata.
- `Settings.timezone` (landed in 03.1-01) now has a real operator-facing input path all the way from `docker-compose.yml`'s `TZ` default through to the worker process.
- No blockers for 03.1-03 (window CRUD), 03.1-04 (overrun detection), 03.1-06 (suggestion detector), or 03.1-07 (active maintenance display) — all of which depend on `resolve_timezone`/`Settings.timezone` continuing to behave correctly in the built container, which this plan verifies.

---
*Phase: 03.1-planned-maintenance-recognition*
*Completed: 2026-08-22*

## Self-Check: PASSED
- FOUND: dashboard/pyproject.toml (tzdata==2026.3 present)
- FOUND: dashboard/uv.lock (tzdata hash present)
- FOUND: docker-compose.yml (TZ present in worker and web blocks)
- FOUND: tests/test_maintenance_windows.py (TimezoneEnvironmentTests present, 24 tests total in file, all passing)
- FOUND commit: 0eb0309
