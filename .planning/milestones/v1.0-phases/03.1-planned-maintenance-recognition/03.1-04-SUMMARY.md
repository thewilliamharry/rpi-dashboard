---
phase: 03.1-planned-maintenance-recognition
plan: 04
subsystem: monitoring
tags: [sqlite, worker-authority, maintenance-windows, alerting]

# Dependency graph
requires:
  - phase: 03.1-planned-maintenance-recognition/03.1-01
    provides: "maintenance_windows table (migration 9), get_open_down_transition repository read, suppression tagging on the down transition (maintenance_grace_until frozen at suppression time)"
  - phase: 03.1-planned-maintenance-recognition/03.1-03
    provides: "upsert_maintenance_windows (used by this plan's mid-outage-edit regression test)"
provides:
  - "overrun_already_raised/mark_overrun_raised: caller-transaction bookkeeping repository functions"
  - "MAINTENANCE_OVERRUN_EVENT_TYPE ('maintenance_overrun') folded into the existing down-only probe loop (_legacy_do_uptime_check, J4), never a new worker callback"
  - "The MNT-04 overrun outage event: down_since_ts (from services.state_since) and ts (raise instant) as two separate durable fields, suppressed_reason always NULL, dispatched through the existing transition alert sender outside the write transaction"
affects: [03.1-05-editor-ui, 03.1-06-suggestion-detector, 03.1-07-active-maintenance-display]

# Actuals (#2632)
actuals:
  tokens: 7200
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Overrun idempotency read-then-set (overrun_already_raised -> insert -> mark_overrun_raised) shares the exact _mutation_write_transaction block that already writes the services UPDATE for that row, so the guard is race-free by construction under the existing _uptime_lock single-flight guard (Pattern 3)."
    - "overrun_raised_ts bookkeeping reset folds into the three pre-existing UPDATE services CASE WHEN is_online != ? clauses (mirroring state_since's own reset condition) rather than a fourth UPDATE statement (Pitfall 9)."
    - "_dispatch_overrun_alert mirrors _handle_state_transition's worker-vs-legacy authority dispatch, but calls only the alert-sending half (worker_send_transition_alert / _send_transition_alert) since the overrun event was already inserted in-transaction -- calling the full transition handler again would insert a spurious second event."

key-files:
  created: []
  modified:
    - dashboard/beacon/repositories.py
    - dashboard/app.py
    - tests/test_maintenance_windows.py

key-decisions:
  - "Task 1 (pre-resolved by human operator): option-a schema shape confirmed -- dedicated maintenance_overrun event type carrying down_since_ts (from services.state_since at raise time) and ts (raise instant) as separate durable fields, suppressed_reason always NULL, idempotency held in services.overrun_raised_ts. No independent file change; recorded here per the executor's checkpoint-handling instructions."
  - "The overrun's down_since_ts is read from the initial per-row SELECT's s.state_since column (added to that query in this plan) rather than re-queried inside the write transaction -- safe because the branch only runs when previous_online==0 and online_int==0 (no transition this poll), so state_since cannot have changed for this port between the initial read and the write under the single _uptime_lock holder."
  - "The overrun's details string is rendered at write time via a new _overrun_detail helper (datetime + beacon_maintenance.resolve_timezone) matching the UI-SPEC Copywriting Contract template ('Down since {..}; window and grace expired at {..}.'); the down_since_ts/ts columns remain the authoritative machine-readable instants per D-08, this string is not re-parsed by any consumer."
  - "_dispatch_overrun_alert is a new small wrapper, not a reuse of _handle_state_transition, because the latter always inserts its own state_change event -- calling it for an overrun would write a second, incorrect event row. The wrapper duplicates only the worker/legacy authority selection _handle_state_transition already does, then delegates to the same underlying sender functions transitions already use."

patterns-established:
  - "A worker-vs-legacy dispatch wrapper (_dispatch_overrun_alert) that skips the event-insert half of the transition funnel and calls only the alert-sending half -- the template for any future effect that must fire after an already-inserted event without re-inserting it."

requirements-completed: [MNT-04]

coverage:
  - id: D1
    description: "A service still unavailable past its covering window's end plus grace produces exactly one maintenance_overrun event, never repeating across repeated down-only polls of the same down period"
    requirement: MNT-04
    verification:
      - kind: integration
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_a_covered_service_still_down_past_grace_raises_one_outage"
        status: pass
      - kind: integration
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_repeated_polls_raise_exactly_one_overrun"
        status: pass
      - kind: unit
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_no_overrun_is_raised_one_second_before_the_boundary"
        status: pass
    human_judgment: false
  - id: D2
    description: "The overrun event carries down_since_ts (from services.state_since) and ts (raise instant) as two separate, distinct, durable fields, and is never tagged suppressed"
    requirement: MNT-04
    verification:
      - kind: integration
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_the_overrun_event_carries_both_timestamps_separately"
        status: pass
      - kind: unit
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_the_overrun_event_is_never_tagged_as_suppressed"
        status: pass
    human_judgment: false
  - id: D3
    description: "An unsuppressed down period never raises an overrun, and a service that transitioned this same poll is never evaluated for one, even against stale bookkeeping (Pitfall 8)"
    requirement: MNT-04
    verification:
      - kind: unit
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_an_unsuppressed_down_period_never_raises_an_overrun"
        status: pass
      - kind: unit
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_a_service_that_transitioned_this_poll_is_not_evaluated_for_overrun"
        status: pass
    human_judgment: false
  - id: D4
    description: "A later, second down period raises its own overrun because overrun_raised_ts is cleared on the recovery transition (Pitfall 9 / T-03.1-14), and editing a window mid-outage does not move the boundary already frozen on the down period's own event row (T-03.1-16)"
    requirement: MNT-04
    verification:
      - kind: integration
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_a_second_down_period_raises_its_own_overrun"
        status: pass
      - kind: integration
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_editing_the_window_mid_outage_does_not_move_the_boundary"
        status: pass
    human_judgment: false
  - id: D5
    description: "The overrun dispatches through the same transition alert sender (cooldown and critical-only gates, not a parallel mechanism), and the webhook effect stays outside the open write transaction (T-03.1-17)"
    requirement: MNT-04
    verification:
      - kind: integration
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_the_overrun_dispatches_through_the_existing_alert_sender"
        status: pass
      - kind: unit
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_the_alert_is_dispatched_outside_the_write_transaction"
        status: pass
    human_judgment: false
  - id: D6
    description: "Availability arithmetic is byte-identical with and without an overrun row present -- the service_checks insert and the availability computation are untouched by the overrun branch"
    requirement: MNT-04
    verification:
      - kind: unit
        ref: "tests/test_maintenance_windows.py#OverrunTests::test_availability_is_unchanged_by_an_overrun"
        status: pass
    human_judgment: false
  - id: D7
    description: "Source assertions: the overrun bookkeeping reset folds into the existing three UPDATE services statements (no fourth introduced), and mark_overrun_raised has exactly one call site"
    verification:
      - kind: other
        ref: "sed -n '/^def _legacy_do_uptime_check/,/^def [a-z_]*(/p' dashboard/app.py | grep -c 'UPDATE services SET' -> 3"
        status: pass
      - kind: other
        ref: "grep -c 'mark_overrun_raised' dashboard/app.py -> 1"
        status: pass
    human_judgment: false
  - id: D8
    description: "Full existing suite stays green after the overrun branch, alert dispatch wrapper, and 12 new OverrunTests"
    verification:
      - kind: unit
        ref: "uv run --project dashboard python -m pytest -q -- 379 passed, 454 subtests passed, 0 failed (up from 367/454 baseline, +12 new tests)"
        status: pass
    human_judgment: false

duration: 55min
completed: 2026-08-22
status: complete
---

# Phase 03.1 Plan 04: Overrun Detection Summary

**A service still down past its covering maintenance window's end plus grace now raises exactly one truthful, never-suppressed `maintenance_overrun` outage event -- carrying both `down_since_ts` (from `services.state_since`) and `ts` (the raise instant) as separate durable fields -- folded into the existing down-only probe loop with idempotency race-free under the existing `_uptime_lock`, and dispatched through the existing transition alert sender outside any open write transaction.**

## Performance

- **Duration:** ~55 min
- **Tasks:** 2 (Task 1: pre-resolved schema decision; Task 2: overrun detection implementation)
- **Files modified:** 3 (0 created)

## Accomplishments
- `dashboard/beacon/repositories.py`: `overrun_already_raised(conn, port)` and `mark_overrun_raised(conn, port, now)` -- caller-transaction bookkeeping functions matching the module's established contract (no own transaction, operate on the caller's connection).
- `dashboard/app.py`: `MAINTENANCE_OVERRUN_EVENT_TYPE` constant (`'maintenance_overrun'`); the initial per-row SELECT in `_legacy_do_uptime_check` gained `s.state_since` so the overrun's `down_since_ts` can be read from it directly; an overrun branch inside the SAME `_mutation_write_transaction` block that already performs the `UPDATE services` and `service_checks` insert, guarded on `previous_online==0 and online_int==0` (still down, no transition this poll -- Pitfall 8), reading the frozen boundary off `get_open_down_transition` and never live window configuration; `overrun_raised_ts` bookkeeping reset folded into all three pre-existing `UPDATE services` `CASE WHEN is_online != ?` clauses alongside `state_since`'s own reset (Pitfall 9, no fourth UPDATE statement introduced); `_overrun_detail` helper rendering the Copywriting Contract's detail string; `_dispatch_overrun_alert` -- a worker-vs-legacy dispatch wrapper mirroring `_handle_state_transition`'s authority selection but calling only the alert-sending half (`worker_send_transition_alert`/`_send_transition_alert`), since the overrun event is already inserted in-transaction and must not be inserted twice.
- `tests/test_maintenance_windows.py`: `OverrunTests` (12 tests, all named in the plan's behavior block), driving the REAL `do_uptime_check` probe cycle via a mocked `_probe_http` rather than stubbing the function under test -- covers exactly-once idempotency across repeated polls, both timestamps durable and distinct, never suppressed, boundary-second precision (one second before vs. exactly at), an unsuppressed down period never overrunning, a same-poll transition never evaluated despite stale bookkeeping, a second down period raising its own overrun, a mid-outage window edit not moving the already-committed boundary, alert dispatch through the existing cooldown/critical-only gates, the webhook effect outside the write transaction (proven by a non-blocking `_db_lock.acquire` inside the mocked sender), and availability arithmetic unchanged between a covered and an uncovered service subjected to the identical transition sequence.

## Task Commits

Task 1 (checkpoint:decision) was pre-resolved by the human operator before this dispatch (option-a, recorded in frontmatter `key-decisions` above); it carries no independent file change and is recorded here rather than as its own commit, per the executor's checkpoint-handling instructions.

1. **Task 2: Raise exactly one truthful overrun outage past window and grace** - `e99ec5f` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `dashboard/beacon/repositories.py` - `overrun_already_raised`, `mark_overrun_raised`
- `dashboard/app.py` - `MAINTENANCE_OVERRUN_EVENT_TYPE`; `s.state_since` added to the uptime-check SELECT; overrun branch inside `_legacy_do_uptime_check`'s per-row write transaction; `overrun_raised_ts` reset folded into the three existing `UPDATE services` statements; `_overrun_detail`; `_dispatch_overrun_alert`; overrun collection/dispatch loop mirroring the existing `transitions` pattern
- `tests/test_maintenance_windows.py` - `OverrunTests` (12 new tests)

## Decisions Made
See frontmatter `key-decisions` above for the Task 1 pre-resolved schema shape, the `down_since_ts` source (initial-SELECT `state_since`, safe under the still-down guard), the `_overrun_detail` rendering approach, and the `_dispatch_overrun_alert` wrapper rationale.

## Deviations from Plan

None - plan executed exactly as written. The one addition beyond the plan's literal action text -- adding `s.state_since` to the initial per-row SELECT -- is a direct, necessary consequence of the plan's own action step 3 ("`down_since_ts` set from this row's `state_since`"), since that column was not previously selected by this query; it is not a deviation from scope, just the concrete mechanism for an instruction the plan already specified.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `overrun_already_raised`/`mark_overrun_raised` and the `maintenance_overrun` event shape are now the proven foundation the active-maintenance display plan (03.1-07) renders `Down since`/`Raised at` from, and the events feed reveal control (03.1-05, running in parallel in a sibling worktree) must never suppress.
- No blockers for 03.1-05, 03.1-06, or 03.1-07.

---
*Phase: 03.1-planned-maintenance-recognition*
*Completed: 2026-08-22*

## Self-Check: PASSED
- FOUND: dashboard/beacon/repositories.py (overrun_already_raised, mark_overrun_raised present)
- FOUND: dashboard/app.py (MAINTENANCE_OVERRUN_EVENT_TYPE present, overrun branch present, _dispatch_overrun_alert present)
- FOUND: tests/test_maintenance_windows.py (OverrunTests present, 12 tests, all passing; 68 tests total in file)
- FOUND commit: e99ec5f
