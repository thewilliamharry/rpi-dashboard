---
phase: 03.1-planned-maintenance-recognition
plan: 10
type: execute
wave: 7
depends_on: ["03.1-07"]
files_modified:
  - dashboard/advanced.js
  - dashboard/advanced.css
  - tests/test_advanced_ui.py
autonomous: true
requirements: [MNT-03, MNT-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The advanced services table gains no new column: maintenance is a new possible value of the existing Status column, preserving Phase 3's fixed six-column contract."
    - "The status vocabulary helper gains one new recognised value and continues to fall closed to its unknown value for anything else — a maintenance value is never collapsed into offline or online."
    - "Active confirmed maintenance is never added to the Overview active-exceptions list or its count; it exists only inside the Services section and its row detail (D-07)."
    - "The service is still listed with its true state duration and failure class — it is never removed from the failure surface (D-06)."
    - "Because the advanced workspace is strictly read-only, none of the new evidence is interactive and the Services detail states in plain text where windows are managed, with no link implying an action is performed from this workspace (D-01)."
    - "empty (advanced services): the no-services-configured case is Phase 3's existing services-table empty state, unchanged by this phase."
    - "loading (advanced services): uses Phase 3's established polling contract — fixed selectable interval, pause, refresh now, and a visible last-updated time. No new loading affordance."
    - "error (advanced services): uses Phase 3's established route-failure and exception vocabulary; this phase adds no new failure mode to the read path."
    - "populated (advanced services): maintenance becomes a new value of the existing Status column, never a new column, plus up to three new evidence rows appended after the existing collection-gap evidence row."
    - "partial (advanced services): no blank cells — the Maintenance evidence row always renders, stating there is no active window when none is; the Maintenance attribution row always renders; only the down-since and raised-at row is conditional, and only on a genuinely open overrun."
    - "overflow (advanced services): the new evidence rows reuse the existing two-column evidence grid with its anywhere-wrapping rule, unchanged."
    - "zero-one-many (advanced services): inherited from Phase 3's services table; this phase adds a status value, not a new collection."
    - "long-text (advanced services): the same existing wrap rule applies, and the down-since and raised-at values are always two separate lines, never concatenated."
    - "empty (attribution): the zero-attribution case still renders explicit no-downtime-was-attributed copy rather than omitting the row."
    - "populated (attribution): a single evidence row with the exact contracted copy, plus the explicit rule that no second competing percentage is ever rendered at any weight on any surface (D-09)."
    - "overflow (attribution): reuses the existing evidence grid and its anywhere-wrapping rule."
    - "long-text (attribution): the same wrap rule applies, and the headline availability figure is a separate, fixed-width element that is never affected."
    - "overflow (overrun outage on this surface): the down-since and raised-at values render as two separate evidence rows or lines and are never merged into one truncatable string (D-08)."
    - "The maintenance status colour on this surface is status text only: it is never applied to a button, a link, or any other interactive element, and this phase does not widen the workspace's existing accent reservation."
  prohibitions:
    - "The advanced workspace must never gain a mutation endpoint, an editing affordance, or a link that implies an action is performed from it."
    - "No seventh column may be added to the services table."
    - "Active confirmed maintenance must never appear in the Overview active-exceptions list or its count."
    - "No second, adjusted, excluding-maintenance percentage may be rendered at any weight on this surface."
    - "An evidence row this phase adds must never render blank; it states its explicit none case instead."
    - "The maintenance status colour must never be applied to an interactive element on this surface."
  artifacts:
    - path: "dashboard/advanced.js"
      provides: "The maintenance status value and the three maintenance evidence rows"
      contains: "maintenance"
    - path: "dashboard/advanced.css"
      provides: "The maintenance status-dot colour reusing the existing shared token"
      contains: "maintenance"
  key_links:
    - from: "dashboard/advanced.js"
      to: "dashboard/beacon/diagnosis.py"
      via: "the status cell and evidence rows render the derived availability, maintenance, overrun, and attribution blocks the snapshot publishes"
      pattern: "maintenance"
---

<objective>
Expand outward to the read-only workspace: give the operator, in the one place built for
investigation, the full derivation behind a calm status — which window is covering, until when,
what it cost the availability figure, and how long an overrun has really been running.

Everything on this surface is text. The workspace stays strictly GET-only (D-01), so the Services
detail names where windows are managed instead of offering a way to manage them here. Attribution
explains a dip and never softens it: the headline figure is untouched and no competing percentage is
rendered at any weight (D-09).

Purpose: satisfy MNT-03's disclosure obligation and MNT-04's investigation obligation on the surface
that exists for exactly that.

Output: the new status value, three always-honest evidence rows, the status-dot colour reusing the
existing shared token, and regressions in the advanced UI suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.1-planned-maintenance-recognition/03.1-CONTEXT.md
@.planning/phases/03.1-planned-maintenance-recognition/03.1-UI-SPEC.md
@.planning/phases/03.1-planned-maintenance-recognition/03.1-PATTERNS.md
@.planning/phases/03.1-planned-maintenance-recognition/03.1-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The maintenance status value in the existing Status column</name>
  <files>dashboard/advanced.js, dashboard/advanced.css</files>
  <read_first>
    - dashboard/advanced.js lines 490-515 (the file being modified; `serviceAvailability` and `serviceFreshness` — the lowercase vocabulary mapping and its fall-closed unknown branch)
    - dashboard/advanced.js — the services-table row builder and how it renders the status cell's dot and word from that helper's return value
    - dashboard/advanced.css lines 1-40 (the file being modified; the existing status-dot rules and the `.evidence-row` / `.evidence-list` rules)
    - dashboard/style.css — grep for the maintenance status token declaration in both themes, and for an existing consumer of it, to confirm the token name to reuse
    - dashboard/beacon/diagnosis.py (the maintenance availability literal as plan 03.1-07 publishes it)
    - .planning/phases/03.1-planned-maintenance-recognition/03.1-UI-SPEC.md sections "Color", "Layout and Responsive Contract" (`/advanced` services table and Overview), "Interaction Contract" (`/advanced` services table and detail), and "Copywriting Contract"
  </read_first>
  <action>
1. In `dashboard/advanced.js`, add one branch to `serviceAvailability` recognising the maintenance
   literal and returning it as its own value. Do not fold it into the offline branch and do not touch
   the existing fall-closed unknown return, which continues to catch anything unrecognised.

2. Render the new value in the existing Status column exactly like its siblings — a status dot
   followed by the lowercase word `maintenance`, matching the existing lowercase vocabulary. Add no
   column and change no column order.

3. `dashboard/advanced.css` — add the status-dot and status-text colour rule for the new value,
   reusing the existing shared maintenance token already declared for both themes in the main
   stylesheet. Add no new custom property and apply the token to no button, link, or other
   interactive element. Do not widen the workspace's existing accent reservation.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `awk '/function serviceAvailability/,/^  }/' dashboard/advanced.js | grep -c maintenance` returns at least 1, and the helper's final fall-closed return is unchanged.
    - Source assertion: `grep -c maintenance dashboard/advanced.css` returns at least 1.
    - Source assertion: `dashboard/advanced.css` declares no new custom property in this diff — `git diff -U0 dashboard/advanced.css | grep -Ec '^\+\s*--[a-z0-9-]+:'` returns 0.
    - Behavior assertion (live DOM): a snapshot whose service carries the maintenance availability renders a status cell whose text is the lowercase maintenance word and whose dot computes the maintenance token's colour.
    - Behavior assertion (live DOM): the services table still has exactly six columns and their headers are unchanged.
    - Behavior assertion (live DOM): a service carrying an unrecognised availability literal still renders the existing unknown value.
  </acceptance_criteria>
  <done>The Status column gains one new value rendered like its siblings in the shared maintenance colour, the fixed six-column contract is intact, the fall-closed unknown branch still catches anything unrecognised, and no new token or interactive colour use is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Three always-honest evidence rows in the Services detail</name>
  <files>dashboard/advanced.js</files>
  <read_first>
    - dashboard/advanced.js lines 90-115 (the file being modified; the evidence-row builder and its class assignment) and the service detail composition around the collection-gap evidence row, which the new rows are appended after
    - dashboard/advanced.js — the existing helper that formats the collection-gap evidence string, as the model for an always-rendered row with an explicit none case
    - dashboard/beacon/diagnosis.py (the maintenance evidence block, the overrun block, and the attribution block as plan 03.1-07 publishes them, including their exact key names and value types)
    - dashboard/advanced.css (the `.evidence-row` two-column grid and its anywhere-wrapping rule — reused unchanged)
    - .planning/phases/03.1-planned-maintenance-recognition/03.1-UI-SPEC.md "Interaction Contract" (`/advanced` services table and detail; Availability attribution) and the full "Copywriting Contract"
  </read_first>
  <action>
1. Add a formatter for the Maintenance evidence value. When the snapshot's maintenance block reports
   active coverage, compose the Copywriting Contract's active sentence from the covering window's
   start and end clock times, its weekday set rendered as day names, its grace in minutes, and the
   coverage-end instant as a timestamp. When it does not, use the contract's inactive sentence.
   Never return an empty string and never omit the row.

2. Add a formatter for the Maintenance attribution value. When the attributed seconds are greater
   than zero, compose the contract's nonzero sentence from the attributed duration and the period
   length. When they are zero, use the contract's zero sentence. Never omit the row. Render no
   percentage of any kind — the attributed portion is stated as a duration, and this surface carries
   no second availability figure.

3. Append, after the existing collection-gap evidence row and using the same evidence-row builder
   and class:
   - a Maintenance row, always rendered, from the first formatter;
   - a down-since row and a separate raised-at row, rendered only while the snapshot reports an open
     overrun for that service, each carrying its own exact timestamp on its own line, never merged
     into one string;
   - a Maintenance attribution row, always rendered, from the second formatter;
   - a plain-text management pointer carrying the contract's sentence naming where windows are
     managed. It is text, not a link, and it triggers nothing.

4. Use the existing evidence-row builder and grid for every one of these; add no new layout
   primitive and no interactive element.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'evidence-row' dashboard/advanced.js` is greater than it was before this task, and every class name introduced in the diff already exists in the stylesheets — `git diff -U0 dashboard/advanced.js | grep -Eo "className = '[a-z-]+'" | sort -u` lists only names that `grep -h` finds in `dashboard/advanced.css` or `dashboard/style.css`.
    - Source assertion: the four contracted maintenance-evidence strings appear verbatim in `dashboard/advanced.js` — one grep per string, each returning at least 1.
    - Source assertion: the management pointer is not a link — `grep -B2 -A2 'managed from the main dashboard' dashboard/advanced.js | grep -Eic "createElement\('a'\)|href"` returns 0. This is a D-01 guard, so it must inspect real neighbouring lines: the copy string and its terminating `;` share one line in `advanced.js`'s established `addEvidence(grid, 'Label', 'text');` idiom, so `awk '/managed from the main dashboard/,/;/'` collapses to that single line. Measured on fixtures in both styles: the `awk` form returns 0 for a compliant plain-text pointer AND 0 for a version built with `document.createElement('a')` plus `link.href` — fully vacuous; the `grep -B2 -A2` form returns 0 for the compliant version and 2 for the link version.
    - Behavior assertion (live DOM): a service with no active window renders the Maintenance row with the contracted inactive sentence, not a blank cell.
    - Behavior assertion (live DOM): a service with active coverage renders the contracted active sentence containing its window's times, weekdays, grace, and end instant.
    - Behavior assertion (live DOM): a service with zero attributed seconds renders the contracted zero sentence, and the row is present.
    - Behavior assertion (live DOM): with an open overrun, two distinct rows render, their two timestamp strings differ, and neither contains the other.
    - Behavior assertion (live DOM): with no open overrun, neither of those two rows exists.
    - Behavior assertion (live DOM): no element in the service detail is a link or a button.
  </acceptance_criteria>
  <done>The Services detail carries an always-present Maintenance row and an always-present attribution row with explicit none cases, two separate overrun timestamp rows only while an overrun is open, and a plain-text pointer to where windows are managed — all built from the existing evidence grid with nothing interactive.</done>
</task>

<task type="auto">
  <name>Task 3: Advanced-workspace regressions</name>
  <files>tests/test_advanced_ui.py</files>
  <read_first>
    - tests/test_advanced_ui.py (the file being modified; its harness, its stubbed snapshot fixtures, and how existing tests assert rendered strings and computed styles)
    - dashboard/advanced.js and dashboard/advanced.css as they now stand
    - .planning/phases/03.1-planned-maintenance-recognition/03.1-UI-SPEC.md "UI Considerations" (the eight advanced-services rows and the four attribution rows this task pins)
  </read_first>
  <behavior>
    - `test_a_maintenance_service_renders_the_new_status_value`.
    - `test_the_services_table_still_has_six_columns`.
    - `test_an_unrecognised_availability_still_renders_the_unknown_value`.
    - `test_a_maintenance_service_is_absent_from_the_active_exceptions_list_and_count`.
    - `test_an_overrun_service_reappears_in_the_active_exceptions_list`.
    - `test_a_maintenance_service_still_shows_its_true_state_duration_and_failure_class`.
    - `test_the_maintenance_evidence_row_always_renders` — assert both the active and inactive
      sentences in their respective fixtures and assert neither cell is blank.
    - `test_the_attribution_row_always_renders_including_zero`.
    - `test_the_attribution_row_renders_no_percentage` — assert the rendered string contains no
      percent sign and that the detail contains exactly zero percentage-shaped values.
    - `test_no_second_availability_figure_exists_anywhere_on_the_page` — enumerate the rendered text
      of the whole workspace and assert at most one percentage-shaped value per service row.
    - `test_the_overrun_rows_render_as_two_separate_values`.
    - `test_the_overrun_rows_are_absent_without_an_open_overrun`.
    - `test_the_management_pointer_is_plain_text_and_not_actionable`.
    - `test_the_service_detail_contains_no_interactive_element`.
    - `test_long_evidence_values_wrap_rather_than_truncating` — assert the evidence cell's scroll
      width does not exceed its client width for a deliberately long weekday rendering.
    - `test_the_maintenance_colour_is_applied_to_no_interactive_element` — enumerate every button and
      link in the workspace and assert none computes the maintenance token's colour, at rest or
      hovered.
    - `test_no_workspace_route_accepts_a_mutation_method` — assert the client issues only safe
      methods and that no rendered element targets a non-safe method.
  </behavior>
  <action>
Add the assertions above to `tests/test_advanced_ui.py`, following that module's existing harness and
stubbed-snapshot style. Seed the maintenance, overrun, attribution, and unknown-literal cases through
the stubbed advanced snapshot. Name each test exactly as listed so the acceptance selectors resolve
to real, collected tests. For colour assertions, compare computed styles against the token value
resolved from the document root rather than asserting a literal hex string.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py` exits 0 with every test named in the behavior block collected and passing (file-scoped selector; no `-k` expression).
    - `uv run --project dashboard python -m pytest -q` exits 0.
    - Behavior assertion: a maintenance service appears in the services table and does not appear in the active-exceptions list or its count.
    - Behavior assertion: once coverage lapses into an overrun, the same service does appear in the active-exceptions list.
    - Behavior assertion: every service detail renders a Maintenance row and an attribution row with non-empty text, in both the active and inactive fixtures.
    - Behavior assertion: the workspace renders at most one percentage-shaped value per service row.
    - Behavior assertion: the service detail contains no anchor and no button element.
    - Behavior assertion: no interactive element in the workspace computes the maintenance token's colour, at rest or hovered.
  </acceptance_criteria>
  <done>The new status value, the exception exclusion and its lapse, the always-rendered evidence rows, the absent second percentage, the separated overrun timestamps, the non-actionable pointer, the wrapping rule, and the colour reservation are each pinned by a named test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Advanced snapshot JSON → browser DOM | Service names, window schedules, and timestamps are rendered into the operator's page. |
| Read-only workspace boundary | This workspace must remain incapable of causing an effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-41 | Elevation of Privilege | An editing affordance or action-implying link appearing on the read-only workspace | high | mitigate | The management pointer is plain text; the detail is asserted to contain no anchor and no button, and the client is asserted to issue only safe methods. |
| T-03.1-42 | Repudiation | An unresolved failure hidden from the exception summary because the maintenance exclusion outlived its coverage | high | mitigate | The exclusion is a consequence of the server's derived availability, which reverts the moment coverage lapses; pinned by `test_an_overrun_service_reappears_in_the_active_exceptions_list`. |
| T-03.1-43 | Repudiation | A second, softer availability figure appearing alongside the true one | high | mitigate | Attribution is rendered as a duration sentence with no percent sign, and the workspace is asserted to render at most one percentage-shaped value per service row. |
| T-03.1-44 | Tampering | Script injection through a service name or window schedule rendered into an evidence row | high | mitigate | Every value continues to be assigned through text properties on document-created nodes via the existing evidence-row builder, which this plan reuses unchanged. |
| T-03.1-45 | Information Disclosure | A blank evidence cell implying an absence the pipeline never established | medium | mitigate | Both new always-present rows have explicit none copy, following Phase 3's no-blank-cell discipline; pinned by two named tests. |
</threat_model>

<verification>
- `uv run --project dashboard python -m pytest -q` is green.
- The workspace remains read-only with nothing interactive added.
- Maintenance is a status value, not a column, and never enters the exception summary while active.
- Attribution explains without competing.
</verification>

<success_criteria>
- MNT-03's disclosure obligation is met on the investigation surface.
- MNT-04's outage is fully legible with both timestamps kept separate.
- D-07's exception exclusion holds while active and lapses the moment coverage does.
</success_criteria>

<output>
Create `.planning/phases/03.1-planned-maintenance-recognition/03.1-10-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

Every symbol phase 03.1 creates, across all ten plans. Listed in full in each plan so an executor
never has to guess a name another plan already fixed.

**New module:** `dashboard/beacon/maintenance.py`
**New test module:** `tests/test_maintenance_windows.py`
**New dependency:** `tzdata` (PyPI), pinned in `dashboard/pyproject.toml` and `dashboard/uv.lock`

**Migration (`dashboard/beacon/migrations.py`):** `_migration_9_planned_maintenance`,
`Migration(9, 'planned_maintenance', True, _migration_9_planned_maintenance)`

**New SQL table `maintenance_windows`:** `id`, `port`, `start_minute`, `duration_minutes`,
`weekdays`, `grace_minutes`, `enabled`, `created_ts`, `updated_ts`; index
`idx_maintenance_windows_port` on `(port, enabled)`

**New SQL columns (additive):** `events.suppressed_reason`, `events.maintenance_grace_until`,
`events.down_since_ts`, `services.overrun_raised_ts`

**`dashboard/beacon/maintenance.py`:** `MAINTENANCE_REASON`, `MAINTENANCE_MIN_OCCURRENCES`,
`MAINTENANCE_SUGGESTION_WEEKDAYS`, `MAINTENANCE_OCCURRENCE_LOOKBACK_DAYS`, `Window`,
`resolve_timezone`, `parse_weekdays`, `format_weekdays`, `window_from_row`,
`_local_occurrence_epochs`, `coverage`, `detect_suggestion`, `suggestion_overlaps_enabled_window`,
`attributed_downtime_seconds`

**`dashboard/beacon/repositories.py`:** `get_maintenance_windows`, `upsert_maintenance_windows`,
`get_open_down_transition`, `overrun_already_raised`, `mark_overrun_raised`,
`get_maintenance_suggestion_evidence`, `read_maintenance_windows_by_port`,
`read_service_offline_intervals`

**`dashboard/beacon/config.py` — new `Settings` fields:** `timezone`,
`maintenance_default_grace_minutes`, `maintenance_windows_per_port_max`,
`maintenance_start_tolerance_seconds`, `maintenance_duration_tolerance_seconds`,
`maintenance_suggestion_lookback_days`

**New environment keys:** `TZ`, `MAINTENANCE_DEFAULT_GRACE_MINUTES`,
`MAINTENANCE_WINDOWS_PER_PORT_MAX`, `MAINTENANCE_START_TOLERANCE_SECONDS`,
`MAINTENANCE_DURATION_TOLERANCE_SECONDS`, `MAINTENANCE_SUGGESTION_LOOKBACK_DAYS`

**`dashboard/app.py`:** `MAINTENANCE_OVERRUN_EVENT_TYPE` (value `maintenance_overrun`),
`_maintenance_suppression_decision`, `_validate_maintenance_windows`; `_legacy_insert_event` gains
the keyword parameters `suppressed_reason`, `maintenance_grace_until`, `down_since_ts`; the
service-meta PUT accepts the payload field `maintenance_windows`; the services list gains the
fields `availability`, `maintenance_until`, `maintenance_attributed_seconds`; the events route
publishes `suppressed_reason`, `maintenance_grace_until`, `down_since_ts`

**`dashboard/beacon/web.py`:** `metadata_response` gains the `windows` and `suggestion` keys

**`dashboard/beacon/diagnosis.py`:** `MAINTENANCE_AVAILABILITY` (value `maintenance`); per-service
`maintenance`, `overrun`, and `maintenance_attribution` blocks; `SCHEMA_VERSION` advanced by one

**New CSS classes (`dashboard/style.css`):** `.meta-maintenance`, `.meta-window-list`,
`.meta-window-row`, `.meta-window-row.is-disabled`, `.meta-window-count`, `.meta-window-empty`,
`.meta-window-remove`, `.meta-weekday-chip`, `.meta-suggestion`, `.svc-maintenance`, `.evt-pill`,
`.evt-pill-expected`, `.evt-reveal`

**New markup ids (`dashboard/index.html`):** `meta-suggestion`, `meta-window-count`,
`meta-window-empty`, `meta-window-list`, `meta-window-add`

**New JS functions (`dashboard/app.js`):** `renderMaintenanceWindows`, `addMaintenanceWindowRow`,
`readMaintenanceWindows`, `renderMaintenanceSuggestion`, the Monday-first weekday constant table,
and the suppressed-entry reveal state

**New JS (`dashboard/advanced.js`):** the `maintenance` branch of `serviceAvailability`, the
maintenance-evidence formatter, and the maintenance-attribution formatter

**Extended test modules:** `tests/test_migrations.py`, `tests/test_uptime_integration.py`,
`tests/test_ui_contract.py`, `tests/test_ui_states.py`, `tests/test_advanced_ui.py`,
`tests/test_advanced_diagnosis_api.py`

## Planner Assumptions

Adopted from `03.1-RESEARCH.md` on the researcher's recommendation and recorded here rather than
taken silently. None was separately ruled on by the operator; each is one line away from being
changed because every numeric one is a configurable setting rather than an inline literal.

| # | Assumption | Adopted as |
|---|-----------|-----------|
| A1 | Detector start tolerance 15 minutes, duration tolerance 10 minutes | `maintenance_start_tolerance_seconds` = 900, `maintenance_duration_tolerance_seconds` = 600 |
| A2 | Suggestion evidence lookback 21 days | `maintenance_suggestion_lookback_days` = 21 |
| A3 | Suggested weekday set defaults to all seven days; the operator narrows it via the Adjust path | `MAINTENANCE_SUGGESTION_WEEKDAYS` |
| A4 / Q4-7a | The covering boundary is frozen onto the down transition's own event row at suppression time; editing or disabling a window mid-outage does not retroactively change the committed down period, and a future down period picks up the edited window | `events.maintenance_grace_until` |
| A5 | Overrun detection folded into the existing down-only probe loop rather than a new worker callback | plan 03.1-04 task 2 |
| A6 | `tzdata` added as a pinned dependency plus a `TZ`-sourced `Settings.timezone` failing closed to UTC, with compose plumbing | plans 03.1-01 and 03.1-02 |
| Q3-7b | Suppressed transitions count as detector evidence, but the suggestion output is withheld when the detected cluster substantially overlaps an already-enabled window for that port | plan 03.1-06 |
| Planner | The per-service window cap and its operator-facing message are not covered by the UI-SPEC Copywriting Contract; the cap is a security backstop and its message is planner-authored | `maintenance_windows_per_port_max` = 50 |

**Edge-probe rows carried as explicit assumptions** (the two `unclassified` rows the probe could not
classify are manual-review nudges, surfaced here rather than dropped):

- **MNT-02 / unclassified** — planner assumption: the detector's boundary behaviour is exercised by
  explicit tests for exactly three versus two qualifying occurrences and for a cluster that straddles
  the configured start-tolerance boundary (`DetectorTests`, plan 03.1-06). Flagged for manual review.
- **MNT-04 / unclassified** — planner assumption: the overrun boundary behaviour is exercised by
  explicit tests at the frozen boundary and at the frozen boundary minus one second, plus repeated
  polls producing exactly one event (`OverrunTests`, plan 03.1-04). Flagged for manual review.

The remaining eight edge-probe rows (MNT-01 adjacency, empty, ordering, idempotency, concurrency;
MNT-03 adjacency, empty, ordering) are resolved into concrete acceptance criteria and appear as
`must_haves.truths` in plans 03.1-01 and 03.1-03.
