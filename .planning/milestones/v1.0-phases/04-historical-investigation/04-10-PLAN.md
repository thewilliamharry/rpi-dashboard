---
phase: 04-historical-investigation
plan: 10
type: execute
wave: 9
depends_on: ["04-05", "04-09"]
files_modified:
  - dashboard/advanced.html
  - dashboard/advanced.js
  - tests/test_history_investigation_ui.py
  - .planning/REQUIREMENTS.md
  - .planning/phases/04-historical-investigation/04-UI-SPEC.md
autonomous: true
gap_closure: true
requirements: [DIA-05, HIS-04, DIA-06, DIA-08]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A custom range whose typed local time does not exist because the clock jumps forward (DST spring-forward gap) is rejected with a visible message naming the clock change, and no history or incidents request is issued for it (closes WR-02, restores Observable Truth #2 / DIA-05)"
    - "A custom range typed during the DST fall-back ambiguous hour still parses and still round-trips to the text the operator typed — the fix rejects only times that cannot exist, never times that merely occur twice"
    - "parseLocalRangeInput never returns an instant that does not render back to the text the operator entered"
    - "When the Event type filter narrows the incidents list on a rule other than row-kind matching, the Incidents section states that rule on screen rather than leaving the operator to infer it from a changed count (restores Observable Truth #5 / HIS-04's UI half)"
    - "When no filter narrows the episode list, the disclosure note is absent — the surface never carries a standing caveat that would train the operator to ignore it"
    - "REQUIREMENTS.md records DIA-05, DIA-06, DIA-08 and HIS-04 at the state the passing tests actually support, with no requirement marked satisfied ahead of its evidence"
    - "The range control's invalid-range validation reuses the server-side HistoricalRange rejection copy, Apply is disabled during loading without hiding the fields, Back renders only when the stack is non-empty, and long custom-range labels wrap within the header row rather than overlapping controls (04-UI-SPEC.md UI Considerations, E1 — resolved explicit)"
    - "The incidents list keeps its documented empty and error copy, its N of M count and Clear all filters zero-one-many coverage, its explicitly-defined open/overrun/flapping row states, and its wrapping of long service names and error-class text (04-UI-SPEC.md UI Considerations, E4 — resolved explicit)"
  artifacts:
    - path: "dashboard/advanced.js"
      provides: "DST-gap rejection in parseLocalRangeInput, its message in validateCustomRange, and the episode_scope disclosure in renderIncidentsSection"
      exports: ["NONEXISTENT_LOCAL_TIME", "parseLocalRangeInput", "validateCustomRange", "renderIncidentsSection"]
      contains: "NONEXISTENT_LOCAL_TIME"
    - path: "dashboard/advanced.html"
      provides: "the incidents episode-scope disclosure region"
      contains: "incidents-episode-scope"
    - path: "tests/test_history_investigation_ui.py"
      provides: "DST spring-forward rejection, fall-back round-trip, and episode-scope disclosure coverage"
      min_lines: 4500
    - path: ".planning/REQUIREMENTS.md"
      provides: "reconciled DIA-05 / DIA-06 / DIA-08 / HIS-04 records"
      contains: "DIA-08"
  key_links:
    - from: "dashboard/advanced.js"
      to: "dashboard/app.py"
      via: "renderIncidentsSection reads the episode_scope key 04-09 added to GET /api/events/history"
      pattern: "episode_scope"
    - from: "dashboard/advanced.js"
      to: "dashboard/advanced.html"
      via: "applyCustomRange writes the DST rejection message into #range-error"
      pattern: "range-error"
    - from: "dashboard/advanced.js"
      to: "dashboard/advanced.html"
      via: "renderIncidentsSection writes the narrowing disclosure into #incidents-episode-scope"
      pattern: "incidents-episode-scope"
---

<objective>
Close the two remaining items in `04-VERIFICATION.md`: the custom local-time parser silently
accepts an instant for a wall-clock time that never occurs (WR-02 / DIA-05), and — following
04-09's server change — the Incidents surface must state the rule by which the Event type filter
now narrows incidents rather than letting the operator infer it from a changed count. Then make
`REQUIREMENTS.md` honest about what the passing tests actually support.

Purpose: the phase already ships a DST-tick detector that labels the absent hour on the axis, so
accepting a time inside that same absent hour in the range fields is the one input path where the
edge case is not handled. And a view that silently shows less than the truth is the exact failure
mode this phase exists to prevent — a narrowing rule the operator cannot see is the softer version
of the same problem.
Output: a rejecting `parseLocalRangeInput`, a named message in `validateCustomRange`, an
`#incidents-episode-scope` disclosure region, three new Playwright regressions, and reconciled
requirement records.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-historical-investigation/04-CONTEXT.md
@.planning/phases/04-historical-investigation/04-VERIFICATION.md
@.planning/phases/04-historical-investigation/04-REVIEW.md
@.planning/phases/04-historical-investigation/04-UI-SPEC.md
@.planning/phases/04-historical-investigation/04-09-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols this plan **creates**:

- `dashboard/advanced.js`: module constant `NONEXISTENT_LOCAL_TIME` (string value
  `nonexistent-local-time`), exported on `window.__historyRangeTestHooks`; function
  `renderEpisodeScopeNote(scope, filters)`.
- `dashboard/advanced.html`: element id `incidents-episode-scope`.
- Copy strings (all three added as rows to `04-UI-SPEC.md`'s Copywriting Contract):
  - `That local time does not exist on this date — the clock jumps forward here (DST). Enter a time outside the absent hour.`
  - `Incident rows are grouped from every state change in this range; the Event type filter keeps only incidents with a matching event during the incident.`
  - `Expected-maintenance filtering applies to each incident's own opening event, so an incident that began during maintenance is filtered as expected maintenance.`
- `tests/test_history_investigation_ui.py`: class `CustomRangeDstGapTests`, and test functions
  `test_spring_forward_absent_local_time_is_rejected_with_a_named_message`,
  `test_spring_forward_absent_local_time_issues_no_history_request`,
  `test_fall_back_ambiguous_local_time_still_parses_and_round_trips`,
  `test_ordinary_local_time_round_trips_unchanged`,
  `test_event_type_narrowing_is_disclosed_in_the_incidents_section`,
  `test_no_narrowing_leaves_the_episode_scope_note_absent`.

Symbols this plan **changes in place**:

- `parseLocalRangeInput(text)` — return type widens from `number | null` to
  `number | null | NONEXISTENT_LOCAL_TIME`. Six existing Playwright tests call it through
  `window.__historyRangeTestHooks`; all pass well-formed, existing times and are unaffected, but
  read them before editing rather than assuming.
- `validateCustomRange(startTs, endTs)` — gains a first branch ahead of the existing
  finite/ordering/span/future checks.
- `renderIncidentsSection()` — gains the disclosure render.
- `_events_history_fixture(start_ts, end_ts, episodes=None, events=None)` in
  `tests/test_history_investigation_ui.py` — gains a `narrowed_by=None` keyword and an
  `episode_scope` key so every existing incidents test keeps returning a payload shaped like the
  post-04-09 route.

Symbols this plan does **not** touch: `formatLocalRangeInput`, `localWallClockMinutes`,
`dstAnnotations`, `setInvestigationRange`, `pushRange`/`popRange`, `validHistoryRange`,
`incidentQueryParams`, `focusIncident`, `clusterMarkers`.

## Planner assumptions surfaced (not silently taken)

1. **A sentinel, not a bare `null`.** `04-VERIFICATION.md`'s `missing:` item offers "return `null`
   (or a sentinel `validateCustomRange` turns into a visible message)". Returning bare `null` would
   surface the existing `Enter both a start and an end time.` message, which is false — the
   operator did enter both. Under a phase whose contract is candour, the wrong reason is worse than
   no reason, so the sentinel branch is taken.
2. **`DIA-04` is deliberately not promoted.** The verifier recommends it, but DIA-04 is connected
   to no gap in this closure set. Promoting it here would repeat commit 96bd3ca's premature
   promotion of DIA-08. It is left for re-verification.
3. **`COVERAGE.md` is not regenerated and no new matrix is produced** — the phase's existing
   no-external-API declaration still holds.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject a local time that does not exist, with a message that names why</name>
  <reversibility rating="costly">
    Widens the return type of `parseLocalRangeInput`, an exported test hook with six existing
    Playwright callers, and adds a new client-only rejection string to a control whose other
    messages are deliberately verbatim copies of server rejections. Not one-way: no persisted
    data, no API contract, no external consumer — but reverting means touching the parser, the
    validator, the test hooks, the new tests and the Copywriting Contract together.
  </reversibility>
  <read_first>
    - dashboard/advanced.js lines 1714-1746 (`parseLocalRangeInput` — the `CUSTOM_RANGE_TEXT_PATTERN`,
      the `targetMinutes` derivation and the three-iteration convergence loop that is the defect site)
    - dashboard/advanced.js lines 1747-1781 (`formatLocalRangeInput` and `validateCustomRange` — note
      that every existing message is a verbatim copy of a server rejection string, which is why the
      new message must be documented as the one deliberate client-only exception)
    - dashboard/advanced.js lines 2549-2600 (`localWallClockMinutes` and `dstAnnotations` — the
      naive-local-minutes technique the round-trip check reuses, and the existing `absent` /
      `spring-forward` vocabulary the new copy must agree with)
    - dashboard/advanced.js lines 1915-1935 (`applyCustomRange` — the single call site that turns a
      validation result into `#range-error` text and suppresses the request)
    - dashboard/advanced.js lines 114-140 (`validHistoryRange` — the other `validateCustomRange`
      caller; confirm its `Number.isInteger` guard means a sentinel can never reach it from storage)
    - dashboard/advanced.js lines 3588-3596 (`window.__historyRangeTestHooks` — the export surface)
    - dashboard/advanced.html lines 44-60 (the range control markup: `#range-start`, `#range-end`,
      `#apply-custom-range`, `#range-error`)
    - tests/test_history_investigation_ui.py lines 4199-4325 (`HistoryDstAnnotationLondonTests` —
      `_last_sunday`, `_spring_forward_epoch`, `_fall_back_epoch`, `_config_fixture`, `_dst_fixture`
      and `_load_history_with_range`; the new class reuses these rather than hard-coding a date)
    - tests/test_history_investigation_ui.py lines 1537-1600 (the existing custom-range apply tests —
      the `page.evaluate` idiom for driving the hooks and asserting the issued request)
    - .planning/phases/04-historical-investigation/04-REVIEW.md WR-02 "Fix" paragraph
  </read_first>
  <files>dashboard/advanced.js, tests/test_history_investigation_ui.py</files>
  <behavior>
    Write these as failing tests in a new class `CustomRangeDstGapTests` in
    `tests/test_history_investigation_ui.py` BEFORE editing `dashboard/advanced.js`, confirm the
    first two fail against the current tree, then implement. The class serves `/api/config` with
    timezone `Europe/London` and derives every date from
    `HistoryDstAnnotationLondonTests._spring_forward_epoch()` / `._fall_back_epoch()` — never a
    hard-coded calendar date. In `Europe/London` the spring-forward transition is at 01:00 UTC on
    the last Sunday of March and the local clock jumps 01:00 → 02:00, so the absent local hour is
    `01:00`–`01:59` on the date `_last_sunday(year, 3).strftime('%Y-%m-%d')` returns.

    - `test_spring_forward_absent_local_time_is_rejected_with_a_named_message`: type the
      spring-forward date with time `01:30` into `#range-start` and a plainly valid earlier-day
      time into `#range-end`, click `#apply-custom-range`, and assert `#range-error` is visible with
      textContent exactly equal to the DST rejection string listed in "Artifacts this phase
      produces". Also assert
      `window.__historyRangeTestHooks.parseLocalRangeInput('<date> 01:30')` returns
      `window.__historyRangeTestHooks.NONEXISTENT_LOCAL_TIME`.
    - `test_spring_forward_absent_local_time_issues_no_history_request`: record every request URL the
      page route handler sees; click `#apply-custom-range` with the absent time; assert the count of
      `/api/telemetry/history` and `/api/events/history` requests is unchanged from before the click.
    - `test_fall_back_ambiguous_local_time_still_parses_and_round_trips`: for the fall-back date with
      time `01:30` (the hour that occurs twice in `Europe/London`), assert
      `parseLocalRangeInput` returns a finite number and that
      `formatLocalRangeInput(parseLocalRangeInput(text))` equals `text` — the fix must not turn an
      ambiguous time into a rejected one.
    - `test_ordinary_local_time_round_trips_unchanged`: same round-trip assertion for a mid-June
      date at `14:05`, proving the new check adds no rejection on the ordinary path.
  </behavior>
  <action>
In `dashboard/advanced.js`, immediately above `parseLocalRangeInput`, add the module constant
`NONEXISTENT_LOCAL_TIME` with the string value `nonexistent-local-time`, and a comment stating that
it is a distinct third outcome from `null`: `null` means "this text is not a range input at all"
while the sentinel means "this text is a well-formed local time that the configured zone never
reaches".

In `parseLocalRangeInput`, keep the existing pattern match, component bounds check,
`targetMinutes` derivation and three-iteration convergence loop exactly as they are. After the
loop, before the existing `Number.isFinite(candidate)` return, add the round-trip verification: if
`candidate` is not finite return `null` as today, and if
`localWallClockMinutes(Math.round(candidate))` is not strictly equal to `targetMinutes` return
`NONEXISTENT_LOCAL_TIME`. Document in the function comment that the loop converges for an
unambiguous time and for the fall-back ambiguous hour (both instants render the typed wall clock),
so the only input that reaches the new branch is a wall-clock time the zone skips.

In `validateCustomRange`, add a new first branch ahead of the existing finite check: when either
argument is strictly equal to `NONEXISTENT_LOCAL_TIME`, return `{valid: false, message: <the DST
rejection string>}` using the exact string listed in "Artifacts this phase produces", including its
em dash and its parenthesised `(DST)`. Add a comment recording that this is the one deliberate
exception to this function's rule that every message is a verbatim copy of a server rejection
string: the server takes epoch integers and therefore has no equivalent rejection to copy, and
falling through to `Enter both a start and an end time.` would state a false reason.

Add `NONEXISTENT_LOCAL_TIME` to the `window.__historyRangeTestHooks` object alongside
`parseLocalRangeInput`, `formatLocalRangeInput` and `validateCustomRange`.

Make no change to `applyCustomRange` — it already routes a `{valid: false}` result into
`#range-error` and returns without calling `setInvestigationRange`, which is exactly the required
behaviour once the sentinel produces an invalid result.

Then make the four `<behavior>` tests pass.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest "tests/test_history_investigation_ui.py::CustomRangeDstGapTests" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest "tests/test_history_investigation_ui.py::CustomRangeDstGapTests" -q` exits 0 with 4 tests collected.
    - `test_spring_forward_absent_local_time_is_rejected_with_a_named_message` and
      `test_spring_forward_absent_local_time_issues_no_history_request` were confirmed FAILING
      against the pre-change `dashboard/advanced.js`, and that fact is recorded in `04-10-SUMMARY.md`.
    - `grep -c 'NONEXISTENT_LOCAL_TIME' dashboard/advanced.js` returns at least 4 (declaration, the
      parser return, the validator branch, the test-hook export).
    - `grep -F -c 'the clock jumps forward here (DST)' dashboard/advanced.js` returns 1 — the copy
      lives in exactly one place, not duplicated between the parser and the validator.
    - `uv run --project dashboard python -m pytest "tests/test_history_investigation_ui.py::HistoryDstAnnotationLondonTests" "tests/test_history_investigation_ui.py::HistoryDstAnnotationUtcTests" -q` exits 0 — the existing DST axis-annotation coverage is untouched.
    - `git diff -U0 tests/test_history_investigation_ui.py | grep '^-.*def test_'` prints nothing —
      no pre-existing test was removed or renamed.
  </acceptance_criteria>
  <done>
    Typing a local time inside the spring-forward absent hour produces a visible message naming the
    clock change and issues no request; typing a time inside the fall-back ambiguous hour still
    works exactly as before.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Incidents section states the rule by which its list was narrowed</name>
  <reversibility rating="reversible">
    An additive hidden `<p>` region, a render function that writes into it, and a test fixture
    keyword. Removing it restores the previous DOM exactly; nothing persists and no contract
    changes.
  </reversibility>
  <precondition>
    `GET /api/events/history` returns an `episode_scope` object with `grouped_from` and
    `narrowed_by` keys — delivered by plan 04-09 Task 2. Assert before starting:
    `grep -c 'episode_scope' dashboard/app.py dashboard/beacon/incidents.py` returns a non-zero
    count for at least one of the two files. If it does not, halt: 04-09 has not landed.
  </precondition>
  <read_first>
    - dashboard/beacon/incidents.py as rewritten by 04-09 — specifically `EPISODE_GROUPING_SOURCE`,
      `filter_episodes` and the `episode_scope` construction in `compose_incidents_response`, so the
      UI reads the field's real shape rather than this plan's description of it
    - .planning/phases/04-historical-investigation/04-09-SUMMARY.md (the recorded Task 1 checkpoint
      decision — this task assumes option-a was selected; if the summary records option-b, halt and
      report `REPLAN REQUIRED — option-b selected`)
    - dashboard/advanced.js lines 1044-1080 (`incidentQueryParams` — how a single dropdown value maps
      to either an `event_type` or a `maintenance` query parameter, which determines when each
      disclosure sentence applies) and lines 1085-1105 (`incidentsElement`,
      `beginIncidentsLoadingState`, `updateMatchingIncidentCount`)
    - dashboard/advanced.js lines 1342-1393 (`renderIncidentsSection` — the loading/error/empty/
      truncated element lifecycle the new region must join, and the two parallel fetches behind the
      `N of M` count)
    - dashboard/advanced.html lines 177-200 (the Incidents section markup: `#incident-filters`, the
      seven `event_type:` and two `maintenance:` options, `#matching-incident-count`,
      `#incidents-truncated` — the closest precedent for a hidden explanatory line and its class)
    - dashboard/advanced.css — the `.hist-range-custom-label` rule `#incidents-truncated` already
      uses, so the new region reuses an existing class rather than introducing a style
    - tests/test_history_investigation_ui.py lines 139-150 (`_events_history_fixture`) and lines
      2795-2835 (`_incidents_route`, `_goto_incidents` — the fixtures the new tests reuse)
    - .planning/phases/04-historical-investigation/04-UI-SPEC.md sections "Incidents Contract" →
      "Filters (HIS-04, Claude's Discretion resolved)" and "Copywriting Contract"
  </read_first>
  <files>dashboard/advanced.html, dashboard/advanced.js, tests/test_history_investigation_ui.py, .planning/phases/04-historical-investigation/04-UI-SPEC.md</files>
  <behavior>
    Add both tests to the existing `HistoryInvestigationUiTests` class alongside the other
    `_incidents_route`-based incidents tests:

    - `test_event_type_narrowing_is_disclosed_in_the_incidents_section`: stub
      `/api/events/history` to return `narrowed_by: ['event_type']` with one episode. Open the
      Incidents section, select the `event_type:alert_sent` option in `#incident-event-type-filter`,
      and assert `#incidents-episode-scope` is visible with textContent exactly equal to the
      event-type disclosure string listed in "Artifacts this phase produces". Separately, with
      `narrowed_by: ['maintenance']` and the `maintenance:exclude` option selected, assert the
      region carries the maintenance disclosure string. With `narrowed_by: ['event_type',
      'maintenance']`, assert the region carries both sentences joined by a single space, in that
      order.
    - `test_no_narrowing_leaves_the_episode_scope_note_absent`: with the default
      `_events_history_fixture` payload (`narrowed_by: []`), assert `#incidents-episode-scope` has
      the `hidden` attribute and empty textContent, both on first render and after selecting and
      then clearing a filter via `#clear-incident-filters`.
  </behavior>
  <action>
In `dashboard/advanced.html`, inside `#incidents-section`, add
`<p id="incidents-episode-scope" class="hist-range-custom-label" aria-live="polite" hidden></p>`
immediately after the `<p id="matching-incident-count">` line and before `#incidents-loading`, so
the sentence explaining the count sits with the count. Reuse the `hist-range-custom-label` class
that `#incidents-truncated` already uses; add no new CSS.

In `dashboard/advanced.js`, add `renderEpisodeScopeNote(scope, filters)` directly above
`renderIncidentsSection`. It reads `#incidents-episode-scope`, treats a missing element as a no-op,
and builds the sentence list from `scope.narrowed_by` when that is an array:
- when it includes `event_type`, append the event-type disclosure string;
- when it includes `maintenance`, append the maintenance disclosure string;
- both sentences, in that order, joined by a single space, when both apply.
When the resulting list is empty, set `textContent` to the empty string and `hidden` to `true`.
Otherwise set `textContent` to the joined sentences and `hidden` to `false`. Assign through
`textContent` only; this file currently reaches the DOM exclusively through `textContent` and
attribute assignment, and that must remain true (the acceptance gate below counts the markup-
assignment APIs and requires zero, so do not name them in a source comment either).

Wire it in `renderIncidentsSection`: call `renderEpisodeScopeNote(payload.episode_scope || {},
filters)` immediately after `updateMatchingIncidentCount(...)`, so the note is set on every
successful render including the zero-episode render. Add the region to
`beginIncidentsLoadingState`'s reset list so a stale note never survives into the next request's
loading state, and clear it on the fetch-failure branch alongside the list, matching how
`#incidents-truncated` is handled today.

In `tests/test_history_investigation_ui.py`, extend `_events_history_fixture` with a
`narrowed_by=None` keyword and add
`'episode_scope': {'grouped_from': 'all_state_changes_in_range', 'narrowed_by': narrowed_by or []}`
to the returned dict, so every pre-existing incidents test keeps producing a payload shaped like
the post-04-09 route without any of those tests being edited.

In `.planning/phases/04-historical-investigation/04-UI-SPEC.md`, append two rows to the
Copywriting Contract table — `Incidents event-type narrowing note` and
`Incidents maintenance narrowing note` — carrying the two strings verbatim, and add one line under
the table recording that both were added by gap-closure plan 04-10 to disclose the episode-scope
semantics 04-09 introduced.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest "tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_event_type_narrowing_is_disclosed_in_the_incidents_section" "tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_no_narrowing_leaves_the_episode_scope_note_absent" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest "tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_event_type_narrowing_is_disclosed_in_the_incidents_section" "tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_no_narrowing_leaves_the_episode_scope_note_absent" -q` exits 0.
    - `grep -c 'incidents-episode-scope' dashboard/advanced.html` returns 1.
    - `grep -c 'incidents-episode-scope' dashboard/advanced.js` returns at least 1.
    - `grep -c 'renderEpisodeScopeNote' dashboard/advanced.js` returns at least 3 (definition, the
      `renderIncidentsSection` call, and the loading/error reset path).
    - `grep -c 'innerHTML\|insertAdjacentHTML' dashboard/advanced.js` returns 0.
    - `grep -F -c 'grouped from every state change in this range' dashboard/advanced.js` returns 1.
    - `uv run --project dashboard python -m pytest tests/test_history_investigation_ui.py -q` exits 0
      with a test count strictly greater than 126 + 4 (the pre-existing 126 plus Task 1's four).
    - `git diff -U0 tests/test_history_investigation_ui.py | grep '^-.*def test_'` prints nothing.
  </acceptance_criteria>
  <done>
    Selecting an Event type or an expected-maintenance option makes the Incidents section state, on
    screen, the rule by which the list was narrowed; with no filter applied the region is absent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the requirement records with the evidence that now exists</name>
  <reversibility rating="reversible">Documentation-only; every edit is a single table cell or checkbox and is trivially revertible.</reversibility>
  <precondition>
    All three gate commands below exit 0 before any record is edited — a requirement record must
    never move ahead of its evidence:
    `uv run --project dashboard python -m pytest tests/test_incidents_api.py -q`,
    `uv run --project dashboard python -m pytest tests/test_history_investigation_ui.py -q`,
    `uv run --project dashboard python -m pytest -q`.
    If any exits non-zero, halt and report which, and record HIS-04 and DIA-05 as unsatisfied
    rather than editing anything upward.
  </precondition>
  <read_first>
    - .planning/REQUIREMENTS.md lines 38-52 (the DIA and HIS line-item checkboxes — DIA-04 line 39,
      DIA-05 line 40, DIA-06 line 41, DIA-08 line 43, HIS-04 line 50, HIS-05 line 51) and lines
      118-165 (the traceability table rows and the DIA-08 split note beneath it)
    - .planning/phases/04-historical-investigation/04-VERIFICATION.md "Requirements Coverage" table —
      the verifier's per-requirement determination and the exact wording of its DIA-08 and HIS-04
      findings
    - .planning/phases/04-historical-investigation/04-09-SUMMARY.md and this plan's own Task 1 and
      Task 2 results — the evidence the record changes are being made against
    - .planning/phases/04-historical-investigation/04-UI-SPEC.md "Copywriting Contract" table —
      including the two rows Task 2 appended, so the third row lands in the same table in the same
      format rather than starting a second one
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/phases/04-historical-investigation/04-UI-SPEC.md</files>
  <action>
Make exactly these edits to `.planning/REQUIREMENTS.md`, and no others.

**DIA-08 — fix the checkbox/table mismatch.** The line-item checkbox on the `**DIA-08**` line reads
`- [ ]` while the traceability row already reads `Complete`. `04-VERIFICATION.md` confirms DIA-08 is
functionally satisfied (the range preference from 04-01 plus `historyFilters`/`validHistoryFilters`
from 04-07, both persisted under `beacon-advanced-preferences-v1`). Change the checkbox to `- [x]`
so the two records agree. Leave the traceability row and the DIA-08 split note beneath the table
unchanged.

**DIA-05 — promote on the evidence Task 1 just produced.** Change the `**DIA-05**` line-item
checkbox from `- [ ]` to `- [x]` and the traceability row's status cell from `Pending` to
`Complete`. DIA-05's "validated" clause is now satisfied: the one confirmed hole (a nonexistent
local time silently producing an inaccurate range) is closed and pinned by
`CustomRangeDstGapTests`.

**DIA-06 — promote, caveat resolved.** `04-VERIFICATION.md` determined DIA-06's synchronization
mechanism satisfied and recommended promotion "with a recorded caveat" — the caveat being that a
synchronized view could still synchronize wrong data until HIS-04's gap closed. Plan 04-09 closed
it. Change the `**DIA-06**` line-item checkbox from `- [ ]` to `- [x]` and the traceability row from
`Pending` to `Complete`.

**HIS-04 — make the record earned rather than assumed.** HIS-04 currently reads `- [x]` and
`Complete`, which `04-VERIFICATION.md` determined was not true at verification time. It is true now
only because 04-09 landed and its ten regression tests pass. Leave the checkbox and the traceability
status at their satisfied values, and add one line immediately beneath the traceability table,
after the existing DIA-08 split note, recording the history in plain terms: that HIS-04 was found
broken by `04-VERIFICATION.md` (CR-01/CR-02/WR-01 — the service and event-type filters could hide
or fabricate incident status), that it was restored by gap-closure plans 04-09 and 04-10, and that
its evidence is the named route-level regression tests in `tests/test_incidents_api.py`. Without
that line the record silently reads as though HIS-04 was never in question.

**HIS-05 — no change.** `04-VERIFICATION.md` found `focusIncident` correct at the mechanism level
and did not revert it; its noted dependency on HIS-04 is discharged by 04-09.

**DIA-04 — no status change, but record why.** Its promotion is a verification recommendation
unconnected to any gap in this closure set, so it is deliberately left for re-verification rather
than promoted from a gap-closure plan. Leave the checkbox at `- [ ]` and the traceability row at
`Pending`, and add one line beneath the traceability table stating that `04-VERIFICATION.md`
determined DIA-04 satisfied (the six-preset ladder, its active-state indication and its validated
persisted preference, untouched by any confirmed defect) and that it is held for re-verification
rather than promoted here. Without that line a re-verifier reads `Pending` as an unclosed gap and
re-opens work that is already done.

Then, in `.planning/phases/04-historical-investigation/04-UI-SPEC.md`, append one row to the
Copywriting Contract table — `Custom range — nonexistent local time` — carrying the DST rejection
string from Task 1 verbatim, and note beneath the table that it is the one client-only message in
the range control, added by gap-closure plan 04-10 because the server takes epoch integers and has
no equivalent rejection to copy.
  </action>
  <verify>
    <automated>bash -c 'set -e; R=.planning/REQUIREMENTS.md; U=.planning/phases/04-historical-investigation/04-UI-SPEC.md; grep -q "^- \[x\] \*\*DIA-05\*\*" $R; grep -q "^- \[x\] \*\*DIA-06\*\*" $R; grep -q "^- \[x\] \*\*DIA-08\*\*" $R; grep -q "^- \[x\] \*\*HIS-04\*\*" $R; grep -q "^- \[ \] \*\*DIA-04\*\*" $R; grep -q "| DIA-05 | Phase 4 | Complete |" $R; grep -q "| DIA-06 | Phase 4 | Complete |" $R; grep -q "| DIA-04 | Phase 4 | Pending |" $R; grep -q "04-09" $R; grep -q "DIA-04" $R; grep -qF "the clock jumps forward here (DST)" $U; echo RECORDS-OK'</automated>
  </verify>
  <acceptance_criteria>
    - The `<automated>` record check above prints `RECORDS-OK` and exits 0. The full 727-test suite is not this task's gate — it is this task's `<precondition>`, run once before any record moves, and the phase-level gate in `<verification>`.
    - `grep -c '^- \[x\] \*\*DIA-05\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[x\] \*\*DIA-06\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[x\] \*\*DIA-08\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[x\] \*\*HIS-04\*\*' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '^- \[ \] \*\*DIA-04\*\*' .planning/REQUIREMENTS.md` returns 1 — DIA-04 was not
      promoted.
    - The reason DIA-04 stayed Pending is recorded in the file, not just in this plan:
      `grep -c 'DIA-04' .planning/REQUIREMENTS.md` returns at least 3 (the line item, the
      traceability row, and the new held-for-re-verification note).
    - `grep -c '| DIA-05 | Phase 4 | Complete |' .planning/REQUIREMENTS.md` returns 1 and
      `grep -c '| DIA-06 | Phase 4 | Complete |' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '| DIA-04 | Phase 4 | Pending |' .planning/REQUIREMENTS.md` returns 1.
    - `grep -c '04-09' .planning/REQUIREMENTS.md` returns at least 1 — the HIS-04 history line names
      the plan that restored it.
    - `grep -F -c 'the clock jumps forward here (DST)' .planning/phases/04-historical-investigation/04-UI-SPEC.md` returns 1.
    - `uv run --project dashboard python -m pytest -q` exits 0 with at least 727 passing.
  </acceptance_criteria>
  <done>
    Every requirement record in `REQUIREMENTS.md` touched by this gap set states what the passing
    tests support, the DIA-08 checkbox/table mismatch is gone, and HIS-04's record carries the
    history of having been found broken and restored rather than reading as though it was always
    fine.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator keyboard → `#range-start` / `#range-end` | Free text is parsed into an epoch integer that becomes a query parameter on two GET routes |
| `GET /api/events/history` response → DOM | Server-supplied `episode_scope.narrowed_by` values select which fixed copy string is rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10-01 | Tampering | `parseLocalRangeInput` → `setInvestigationRange` | medium | mitigate | `CUSTOM_RANGE_TEXT_PATTERN` still gates the shape and the component bounds check still gates the values; the new round-trip verification adds a further rejection and can only ever narrow what reaches `setInvestigationRange`. `applyCustomRange` issues no request on an invalid result, gated by `test_spring_forward_absent_local_time_issues_no_history_request` |
| T-04-10-02 | Tampering (XSS) | `#incidents-episode-scope` render path | high | mitigate | The rendered text is chosen from two fixed client-side string literals by membership test on `scope.narrowed_by`; no server value is ever concatenated into the sentence, and assignment is through `textContent` only. Gate: `grep -c 'innerHTML\|insertAdjacentHTML' dashboard/advanced.js` returns 0 |
| T-04-10-03 | Tampering (untrusted stored state) | `validHistoryRange` reading `beacon-advanced-preferences-v1` | medium | mitigate | The new sentinel is a string; `validHistoryRange` already requires `Number.isInteger(custom.start_ts)` and `Number.isInteger(custom.end_ts)` before it consults `validateCustomRange`, so a hostile stored value carrying the sentinel resolves to the documented 24h default and no request is built from it. Gate: the pre-existing `test_hostile_stored_custom_range_yields_24h_default` still exits 0 |
| T-04-10-04 | Information Disclosure | the new rejection message | low | accept | The message names only the operator's own typed time and a timezone property already visible on the axis via `dstAnnotations`; it reveals nothing about the host, the database or any service |
| T-04-10-05 | Repudiation | requirement records edited ahead of evidence | medium | mitigate | Task 3 carries a `<precondition>` requiring all three test commands to exit 0 before any record moves upward, and an acceptance criterion pinning DIA-04 at `Pending` so the task cannot quietly widen its own scope |
| T-04-10-SC | Tampering (supply chain) | package-manager installs | n/a | accept | This plan installs zero packages; `COVERAGE.md`'s no-external-API declaration for this phase still holds and is not regenerated |
</threat_model>

<verification>
- `uv run --project dashboard python -m pytest "tests/test_history_investigation_ui.py::CustomRangeDstGapTests" -q` exits 0
- `uv run --project dashboard python -m pytest tests/test_history_investigation_ui.py -q` exits 0
- The server side 04-09 delivered is unaffected: `uv run --project dashboard python -m pytest tests/test_incidents_api.py -q` exits 0
- Full suite green and grown: `uv run --project dashboard python -m pytest -q` exits 0 with at least 727 passing
- No debt markers introduced: `grep -n -E 'TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER' dashboard/advanced.js dashboard/advanced.html` prints nothing
- No causal-language leak: `grep -in 'root cause\|caused by\|because of\|due to' dashboard/advanced.js dashboard/advanced.html` prints nothing outside comments and tests, matching the 04-08 gate

</verification>

<success_criteria>
- A local time inside the DST spring-forward absent hour is rejected with a message naming the clock change, and issues no request (WR-02 closed, DIA-05 satisfiable)
- A local time inside the DST fall-back ambiguous hour still parses and round-trips unchanged
- The Incidents section states on screen the rule by which the Event type or expected-maintenance filter narrowed the list, and shows nothing when nothing narrowed it
- `REQUIREMENTS.md` records DIA-05, DIA-06, DIA-08 and HIS-04 at states the passing tests support, HIS-04's record carries its found-broken-and-restored history, and DIA-04 is untouched
</success_criteria>

<output>
Create `.planning/phases/04-historical-investigation/04-10-SUMMARY.md` when done.
</output>
