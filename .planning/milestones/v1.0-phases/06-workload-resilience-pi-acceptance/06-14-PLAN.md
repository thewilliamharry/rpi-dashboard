---
phase: 06-workload-resilience-pi-acceptance
plan: 14
type: execute
wave: 9
depends_on: ["06-13"]
gap_closure: true
gap_ids: [G-06-1]
files_modified:
  - .planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
autonomous: false
requirements: [OPS-07, OPS-01]

user_setup:
  - service: raspberry-pi-target
    why: "OPS-07's success criterion names Raspberry Pi-class hardware, and every defect this round closed was visible only on real hardware. No part of this plan may be satisfied by a simulated, developer-machine, or --self-test run (PROH-OPS-07-02). There is no Pi access from the planning or execution environment, and 06-06 already correctly refused to treat a coincidentally-reachable raspi.local as a verified target."
    dashboard_config:
      - task: "Pull this gap-closure round's build on the Pi and rebuild: from `~/projects/rpi-dashboard`, `git pull` then `docker compose up -d --build`"
        location: "The Raspberry Pi host running Beacon (repo at ~/projects/rpi-dashboard)"
      - task: "Resolve the database volume path with `docker inspect beacon-web --format '{{ range .Mounts }}{{ .Source }}{{ end }}'` (currently /var/lib/docker/volumes/rpi-dashboard_dashboard-data/_data); reading it requires sudo"
        location: "The Raspberry Pi host"
      - task: "Run the concurrency-1 control pass, then the 600s concurrency-8 acceptance pass, with `sudo dashboard/.venv/bin/python tests/pi_load_acceptance.py`, and attach both JSON reports as phase evidence"
        location: "The Raspberry Pi host"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A third Raspberry Pi-class acceptance run against the fixed build returns overall_passed true, with every exercised route's p95 inside its own declared budget under the same scenario the failing run used"
    - "That run's CPU column is a real measurement for the first time in this phase — non-zero under load, with cpu_sampling.all_samples_zero false for both roles"
    - "Cadence still holds on the same run, with assertions.cadence.passed true and J1-J4 never stale, so the responsiveness fix did not cost the phase its first success criterion"
    - "The run's resource evidence is Beacon's own, with every sampled PID cross-checked against docker inspect on the live host"
    - "D-DEBT-06-02's revisit trigger is evaluated for the first time on a usable CPU reading, and the result is recorded whichever way it falls"
    - "OPS-07 is not promoted to Complete by this round in REQUIREMENTS.md; promotion belongs to independent re-verification"
  artifacts:
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md"
      provides: "The third run's outcome recorded against the entries it bears on: D-DEBT-06-06 discharged on a working CPU reading, D-DEBT-06-02's trigger evaluated, D-DEBT-06-01 updated with what the chosen fix actually achieved on hardware"
      contains: "D-DEBT-06-06"
  key_links:
    - from: "the third acceptance report"
      to: "tests/pi_load_acceptance.py cpu_sampling"
      via: "06-11's provenance block is what lets this checkpoint distinguish a genuine low-CPU reading from the structural zero that made the previous two runs' CPU columns meaningless"
      pattern: "cpu_sampling"
    - from: "the third acceptance report"
      to: ".planning/REQUIREMENTS.md line 155"
      via: "the report is the evidence OPS-07 will eventually close on, but this round records it without promoting the requirement — the promotion is an independent verifier's to make"
      pattern: "OPS-07"
  prohibitions:
    - id: PROH-OPS-07-01
      requirement: OPS-07
      statement: "The acceptance harness must never be tuned to pass. No response-time budget, cadence threshold, or memory limit may be adjusted in response to what this run measures. A failing run is recorded as failing."
    - id: PROH-OPS-07-02
      requirement: OPS-07
      statement: "A harness run that did not execute against Raspberry Pi-class hardware must never be recorded as satisfying the Pi-class acceptance criterion. A --self-test smoke run is never acceptance evidence; the report must carry run_kind acceptance on a Pi-class host."
    - id: PROH-OPS-07-07
      requirement: OPS-07
      statement: "The CPU figure must be Beacon's own per-process measurement. If this run's CPU column is still all zeros, that is recorded as a measurement still broken — never rounded up to a plausible-looking number and never read as a low-CPU deployment."
    - id: PROH-OPS-07-08
      requirement: OPS-07
      statement: "A gap-closure round must never record its own requirement complete. REQUIREMENTS.md's OPS-07 row stays Pending through this plan regardless of how the hardware run turns out; only independent re-verification may promote it. This follows the project's own precedent, where TEL-06 was deliberately left open by 03-17 for exactly this reason."
    - id: PROH-OPS-07-10
      requirement: OPS-07
      statement: "The acceptance scenario's own parameters are part of the contract, not tuning surface. The acceptance pass runs at --duration 600 and --concurrency 8 against the full route set, exactly as the failing run did. Lowering the duration, lowering the concurrency, or exercising fewer routes to obtain a pass is prohibited as surely as widening a budget is."
---

<objective>
Run the acceptance test on real hardware a third time and record what it establishes.

Purpose: this is where the phase closes or does not. The second run failed with three of six routes
over budget and a CPU column that had never been a measurement. `06-11` made the CPU sampling real,
`06-12` attributed the residual per-request cost, and `06-13` implemented the fix the user chose
against that attribution. None of those claims is worth anything until Pi-class hardware says so —
and `PROH-OPS-07-02` means a `--self-test` run, however green, is not that evidence.

This cannot be performed from the execution environment: there is no Pi access, and `06-06` already
correctly refused to treat a coincidentally-reachable `raspi.local` as a verified target. It is planned
as a blocking human checkpoint with exact commands and an exact pass condition.

Prior-phase decision disposition: `06-CONTEXT.md` `D-01` (WAL only, `_db_lock` untouched) is honoured
here by construction — this plan runs a harness and writes one documentation file; it changes no code
and no lock call site. Whether `D-01`'s fence was lifted at all is `06-13` Task 1's recorded decision,
not this plan's. `06-CONTEXT.md` `D-02` (preview retry budget and thumbnail TTL) was implemented in
`06-01`/`06-02`/`06-03` and is untouched by this round.

Output: two attached hardware reports, a debt record matching what they establish, and `REQUIREMENTS.md`
left exactly as it is.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-CONTEXT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-UAT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-VERIFICATION.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Third Pi acceptance run, with a control pass</name>
  <read_first>
    - `06-VERIFICATION.md` § Truth 5 — the exact figures this run is measured against: `/api/services` p95 2465.9ms (budget 500ms), `/api/scan-status` p95 1797.4ms (budget 500ms), `/api/advanced/current` p95 2742.1ms (budget 2000ms), and the concurrency-1 control's `/api/services` p50 of 289.0ms.
    - `06-DEBT.md` § `D-DEBT-06-06` — the CPU defect this run is the first to test a fix for, and § `D-DEBT-06-02` — the revisit trigger that has now failed to be evaluable for two consecutive rounds.
    - `06-13-SUMMARY.md` — which fix path the user chose and what it changed, so this run's outcome can be attributed to it.
    - `06-11-SUMMARY.md` — the `cpu_sampling` provenance block this checkpoint's pass condition reads.
    - `tests/pi_load_acceptance.py` — the current CLI, including the `--worker-container` / `--web-container` arguments `06-07` added.
  </read_first>
  <precondition>The Pi at `~/projects/rpi-dashboard` has this round's commits (through `06-13`) pulled and rebuilt with `docker compose up -d --build`, and `docker ps` shows both `beacon-web` and `beacon-worker` running. Halt and report if either container is absent or if `git log --oneline -1` on the Pi does not match the repository's current HEAD — a run against a stale build measures the defect, not the fix.</precondition>
  <what-built>
`06-11` fixed the acceptance harness's CPU sampling. `_live_role_processes` now holds one
`psutil.Process` object per PID for the whole run and hands the same object back on every tick, so
`cpu_percent(interval=None)` finally has a prior-tick baseline to diff against instead of being
psutil's meaningless first call every time. The process *set* is still re-derived from the container
on every tick, so a mid-run gunicorn respawn is still sampled. Each role's summary now carries a
`cpu_sampling` block recording whether its CPU column is a measurement or a structural zero.

`06-12` profiled `/api/services`'s residual per-request cost and attributed it to named
sub-computations with measured growth ratios against a measured stored-check-row ratio, producing
`06-PROFILE.md`.

`06-13` put the fix path to the user as a blocking decision against that profile, implemented what was
chosen, and pinned the deployment's gunicorn worker/thread topology at the source level so it cannot
drift from the model this evidence is gathered under.
  </what-built>
  <how-to-verify>
On the Pi, from `~/projects/rpi-dashboard`:

**1. Confirm the build under test is this round's.**
```
git log --oneline -5
docker ps --format '{{.Names}}\t{{.Status}}'
```
Both `beacon-web` and `beacon-worker` must be present and running, and the log must show `06-13`'s
commits. A stale build measures the defect, not the fix.

**2. Resolve the database path** (the harness needs it, and reading it needs sudo):
```
docker inspect beacon-web --format '{{ range .Mounts }}{{ .Source }}{{ end }}'
```
Currently `/var/lib/docker/volumes/rpi-dashboard_dashboard-data/_data`. The database is
`dashboard.db` inside that directory.

**3. Control pass — concurrency 1, 120s.** Directly comparable with the 289.0ms figure the previous
control pass measured:
```
sudo dashboard/.venv/bin/python tests/pi_load_acceptance.py \
  --concurrency 1 --duration 120 \
  --base-url http://127.0.0.1 \
  --db /var/lib/docker/volumes/rpi-dashboard_dashboard-data/_data/dashboard.db \
  --output beacon-control-c1-round3.json
```
The number that matters: `/api/services` p50. It was **289.0ms**. Record whatever it is now.

**4. Acceptance pass — the real one. Same scenario as the failing run, unchanged.**
```
sudo dashboard/.venv/bin/python tests/pi_load_acceptance.py \
  --duration 600 --concurrency 8 \
  --base-url http://127.0.0.1 \
  --db /var/lib/docker/volumes/rpi-dashboard_dashboard-data/_data/dashboard.db \
  --output beacon-acceptance-round3.json
```
Do not lower `--duration` or `--concurrency`, and do not restrict the route set. The scenario is the
contract (`PROH-OPS-07-10`); a pass obtained by asking less of the deployment is not a pass.

**5. Check the report against the pass condition.**
- `run_kind` is `acceptance`; `host_machine` is `aarch64`; `host_node` is `raspi`.
- `overall_passed` is `true` and `failure_reasons` is empty.
- Every route's p95 is inside its budget: `/api/services` and `/api/scan-status` under 500ms,
  `/api/thumbnail-status` under 750ms, `/api/thumbnail/<port>` under 1500ms, `/api/history` and
  `/api/advanced/current` under 2000ms.
- No value sits near 10010ms. That figure is the harness's own 10-second client cap, meaning those
  requests never returned at all.
- `assertions.cadence.passed` is `true` with J1-J4 never `stale`. This held on both prior runs and
  must continue to — a responsiveness fix that costs the phase its cadence criterion is not a fix.
- **`assertions.resources.summary.web.cpu_sampling`** and the same block for `worker`:
  `all_samples_zero` is `false` and `nonzero_sample_count` is greater than zero for at least the `web`
  role under load. `peak_cpu_percent` and `mean_cpu_percent` are non-zero and plausible for a
  deployment serving this load. If the CPU column is still all zeros, say so — `PROH-OPS-07-07`
  forbids reading that as a low-CPU deployment, and it means `06-11`'s fix did not work on hardware.
- **`assertions.resources.resource_targets`** names `beacon-web` and `beacon-worker` and lists the PIDs
  sampled. Cross-check them against the live containers:
  ```
  docker inspect --format '{{.State.Pid}}' beacon-web beacon-worker
  ps -o pid,ppid,pcpu,rss,args -p <each PID from the report>
  ```
  Every sampled PID must trace to a Beacon container.
- `scenario` in the report records `duration_seconds` 600 and `concurrency` 8 — read it back and
  confirm, since this is the field that would show a scenario reduction (`PROH-OPS-07-10`).

**6. Attach both JSON reports** as phase evidence.

Report back: the control pass's `/api/services` p50, the acceptance pass's `overall_passed`, every
route's p95, both roles' `cpu_sampling` blocks and CPU figures, the `scenario` block, and the resolved
PIDs per role.
  </how-to-verify>
  <acceptance_criteria>
    - Both reports exist and are attached as phase evidence.
    - The acceptance report carries `run_kind: "acceptance"` on an `aarch64` / `raspi` host (`PROH-OPS-07-02`).
    - The acceptance report's `scenario` shows `duration_seconds` 600 and `concurrency` 8 — the same scenario the failing run used, unreduced (`PROH-OPS-07-10`).
    - Every sampled PID in `resource_targets` traces to `beacon-web` or `beacon-worker` under the operator's own `docker inspect` cross-check.
    - Both roles' `cpu_sampling.all_samples_zero` is recorded, and the `web` role's `peak_cpu_percent` / `mean_cpu_percent` are recorded verbatim — the first usable CPU reading this phase has produced, or a recorded statement that the column is still broken.
    - `assertions.cadence.passed` and the J1-J4 freshness states are recorded.
    - `overall_passed` and each route's p95 are recorded verbatim, whatever they are — a failing run is a valid outcome of this task and feeds Task 2, which records it as failing rather than writing around it.
    - No budget in `tests/pi_load_acceptance.py` was edited before, during, or after the run (`PROH-OPS-07-01`); `git status` on the Pi is clean apart from the two report files.
  </acceptance_criteria>
  <resume-signal>Paste the control pass's `/api/services` p50, the acceptance report's `overall_passed`, the per-route p95 table, both roles' `cpu_sampling` blocks and CPU figures, the `scenario` block, and the `resource_targets` block. Then type "approved" if the run passed, or describe what failed.</resume-signal>
  <done>A third real Pi acceptance run has produced a report whose resource evidence is verifiably Beacon's and whose CPU column is, for the first time in this phase, a measurement — and its results are recorded whichever way they fell.</done>
</task>

<task type="auto">
  <name>Task 2: Record what the third run established, and leave OPS-07 alone</name>
  <files>.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md</files>
  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` in full — its three-section structure (Deferred / Decided / Discharged), its per-entry table format, and the "what would need to be true to proceed / close this entry" convention every entry follows. Match it exactly rather than introducing a second shape.
    - Task 1's two reports and the recorded figures.
    - `06-13-SUMMARY.md` — the chosen fix path and what it changed, so the run's outcome is attributed to a specific change rather than to the round in general.
    - `.planning/REQUIREMENTS.md` lines 66-72 and 149-155 — the OPS-07 checkbox and its traceability row, both of which stay as they are.
    - `.planning/STATE.md` § Key Decisions — the project's own precedent that a gap-closure round may not record its own requirement complete (TEL-06, deliberately not promoted by `03-17`). This entry follows it.
  </read_first>
  <action>
Update `06-DEBT.md` so a later reader inherits what the third hardware run actually established.

**`D-DEBT-06-06`** — this is the entry the round exists to close on the measurement side. If the run's
`cpu_sampling.all_samples_zero` is `false` and the CPU figures are non-zero and plausible, move the
entry to the Discharged section and record: the root cause as `06-VERIFICATION.md` located it (a fresh
`psutil.Process` per tick discarding the primed baseline), the fix `06-11` landed, and the actual
figures the third run produced for both roles. If the CPU column is still all zeros, the entry stays
in the Deferred section and records that a second fix attempt also failed on hardware, with the
figures — `PROH-OPS-07-07` forbids recording anything else as a resolution.

**`D-DEBT-06-02`** — its stated revisit trigger, "an acceptance run showing sustained CPU percent that
starves the host", has never once been evaluable: the first round measured another program entirely
and the second measured a structural zero. If the third run produced a usable CPU reading, evaluate
the trigger for the first time and record the verdict either way — the trigger firing (a `cpus:` cap
now warranted) and the trigger not firing (the original rationale confirmed by measurement rather than
merely asserted) are both real results, and the entry should say which. If the reading is still
unusable, record that the trigger has now failed to be evaluable for three consecutive rounds.

**`D-DEBT-06-01`** — record what the chosen fix achieved on hardware. If the acceptance run passed,
state that the entry's live-suspect analysis is confirmed to the extent that removing the named cause
removed the failure, and restate what would still reopen the lock question — the entry's own two named
reopening conditions, updated with this run's numbers. If the run still failed, record which routes
remain over budget and whether the pattern now matches the entry's second reopening test (the unlocked
`/api/advanced/current` recovering while the locked routes stay over budget), which is the comparison
that would newly and specifically implicate `_db_lock` itself.

**`D-DEBT-06-03`** — still open and unchanged unless the operator actually captured a `beacon.inventory`
journal-mode reading during this deployment. Do not let the surrounding activity imply it was
addressed; the previous round made exactly this mistake available and correctly refused it.

**`D-DEBT-06-05`** — unchanged. The flaky ownership-fencing test is not this round's work.

**Add one new Deferred-section entry recording the requirement's disposition.** State that OPS-07's
evidence now exists but that this round does not promote it: `REQUIREMENTS.md` line 72's checkbox and
line 155's traceability row both stay as they are, because a gap-closure round recording its own
requirement complete is self-certification. Name the project's own precedent — `03-17` deliberately
left TEL-06 open for the same reason, and `.planning/STATE.md` records that as a key decision — and
state what closes the entry: an independent verification round reading the third run's reports and
promoting OPS-07 itself.

Add no entry for anything this round fixed. `06-11`, `06-12` and `06-13` closed real work; recording
completed work as debt would misstate the phase's position.

Do not edit `.planning/REQUIREMENTS.md` in this plan. It is deliberately absent from `files_modified`.
  </action>
  <verify>
    <automated>D=.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md; for k in D-DEBT-06-01 D-DEBT-06-02 D-DEBT-06-03 D-DEBT-06-05 D-DEBT-06-06 PROH-OPS-07-08 TEL-06; do grep -q -- "$k" "$D" || { echo "MISSING: $k"; exit 1; }; done; grep -qi "beacon-acceptance-round3" "$D" || { echo "MISSING: round-3 report name"; exit 1; }; R=.planning/REQUIREMENTS.md; grep -qE '^\| OPS-07 \| Phase 6 \| Pending \|' "$R" || { echo "OPS-07 traceability row must still read Pending"; exit 1; }; grep -qF -- '- [ ] **OPS-07**' "$R" || { echo "OPS-07 checkbox must still be unchecked"; exit 1; }; git diff --quiet -- "$R" || { echo "REQUIREMENTS.md must not be modified by this plan"; exit 1; }; echo ROUND3_RECORD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `D-DEBT-06-06` is either moved to Discharged with the third run's actual non-zero CPU figures for both roles, or left Deferred with a recorded statement that the fix did not work on hardware and the figures that show it.
    - `D-DEBT-06-02` records whether its revisit trigger was evaluated for the first time and what the verdict was — including the not-fired verdict, which is a real result, not an absence.
    - `D-DEBT-06-01` records what the chosen fix achieved on hardware and, if routes remain over budget, whether the pattern matches its second reopening test.
    - `D-DEBT-06-03` and `D-DEBT-06-05` are unchanged.
    - A new entry records that OPS-07 is deliberately not promoted by this round, names the TEL-06 precedent from `.planning/STATE.md`, and states that an independent verification round closes it.
    - `.planning/REQUIREMENTS.md` is byte-identical: `git diff --quiet -- .planning/REQUIREMENTS.md` exits 0, its OPS-07 checkbox is still unchecked, and its traceability row still reads Pending (`PROH-OPS-07-08`).
    - Both round-3 report filenames appear in the debt record so a later reader can locate the evidence.
    - The verify command prints `ROUND3_RECORD_OK`.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>The phase's debt record matches what the third hardware run established, and OPS-07 remains a requirement an independent verifier will decide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Pi deployment | Elevated, interactive commands are run against a live deployment holding real telemetry |
| acceptance report → requirement sign-off | The report is the evidence OPS-07 will eventually close on; readers will not have watched the run |
| gap-closure round → its own requirement | A round that both produces evidence and promotes the requirement it satisfies is certifying itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-70 | Repudiation | acceptance evidence provenance | high | mitigate | The verification step requires cross-checking every PID in `resource_targets` against `docker inspect` on the live host, so the resource pass is confirmed against the deployment rather than accepted from the report; `PROH-OPS-07-02` forbids attaching any non-hardware run and `run_kind` is derived from the invocation, never settable |
| T-06-71 | Tampering | the acceptance scenario reduced to obtain a pass | high | mitigate | `PROH-OPS-07-10` binds; the run is driven at `--duration 600` and `--concurrency 8` against the full route set, and the checkpoint reads the report's own `scenario` block back as an acceptance criterion so a reduction is visible in the evidence itself |
| T-06-72 | Tampering | budgets widened in response to a failing run | high | mitigate | `PROH-OPS-07-01` binds; an acceptance criterion requires `git status` on the Pi to be clean apart from the two report files, so a budget edit made to obtain a pass cannot hide |
| T-06-73 | Repudiation | this round promoting its own requirement | high | mitigate | `PROH-OPS-07-08` binds; the verify gate asserts `.planning/REQUIREMENTS.md` is byte-identical with OPS-07 still unchecked and still Pending, following the project's own TEL-06 precedent recorded in `.planning/STATE.md` |
| T-06-74 | Repudiation | a still-broken CPU column recorded as a low-CPU result | high | mitigate | `PROH-OPS-07-07` binds; `06-11`'s `cpu_sampling.all_samples_zero` field makes the condition readable from the report, and the checkpoint requires it to be recorded verbatim either way |
| T-06-75 | Denial of Service | the 600s load run against a live deployment | medium | accept | Operator-initiated on the operator's own device against its loopback interface, bounded by `--duration`, generating exactly the load OPS-07 exists to characterise; the two previous runs completed without impairing the deployment |
| T-06-76 | Information Disclosure | acceptance reports attached as phase evidence | low | mitigate | Reports carry route latencies, PIDs, host identifiers and job-health rows — no credentials and no service payloads; confirm before attaching that no `--base-url` credential was passed on the command line |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing; it rebuilds the existing compose stack from the repository's already-audited manifests, and `dashboard/uv.lock` / `dashboard/pyproject.toml` are unchanged by this round |
</threat_model>

## Flagged Assumptions (spec-less probe fallback)

No SPEC.md exists for this phase, so `## Edge Coverage` and `## Prohibitions` were absent. The
deterministic edge probe ran over all 5 phase requirement IDs and returned 5 applicable / 0 resolved /
5 unresolved, every row `category: unclassified`. An `unclassified` row is never auto-resolved with a
backstop marker and never auto-dismissed, so all five are surfaced here and none were authored into
`must_haves.truths` from the probe. No-silent-drop equality holds: 5 surfaced == 0 authored + 5 flagged.

| # | Requirement | Probe status | Flagged assumption carried into this round |
|---|---|---|---|
| 1 | OPS-01 | unresolved (unclassified) | Assumed: this round's fix does not change J1–J4 cadence. Not established by the probe; tested here directly by `assertions.cadence.passed` and the J1-J4 freshness states on the third hardware run. |
| 2 | OPS-02 | unresolved (unclassified) | Assumed: this round touches neither the preview retry budget nor the degraded state. Not established by the probe; covered only by the full suite staying green in `06-11` through `06-13`. |
| 3 | OPS-03 | unresolved (unclassified) | Assumed: this round touches neither the thumbnail TTL nor the store byte budget. Not established by the probe; covered only by the full suite staying green in `06-11` through `06-13`. |
| 4 | OPS-04 | unresolved (unclassified) | Assumed: no change in this round widens database access concurrency. Not established by the probe; covered by `06-CONTEXT.md` `D-01`'s fence, `PROH-OPS-04-02`, and `PROH-OPS-04-05`. |
| 5 | OPS-07 | unresolved (unclassified) | Assumed: the six routes in `ROUTE_BUDGETS_MS` are the representative set and their declared budgets and scenario parameters are the contract. Not established by the probe; `PROH-OPS-07-01` and `PROH-OPS-07-10` forbid revisiting either in this round. |

**Canon-referral breadcrumbs (surfaced by the prohibition recall, deliberately not minted):** elevated
(`sudo`) execution of the harness against a live deployment is canon operational security owned by
`/gsd-secure-phase`; not minted here. Secret handling on the `--base-url` command line is canon
credential hygiene owned by `/gsd-secure-phase` and covered by `T-06-76`'s mitigation; not minted here.

## Artifacts this phase produces

| Symbol | File | Kind |
|---|---|---|
| `beacon-control-c1-round3.json` | Pi host, attached as phase evidence | concurrency-1 control report (new) |
| `beacon-acceptance-round3.json` | Pi host, attached as phase evidence | 600s concurrency-8 acceptance report (new, supersedes the failing second run) |
| `D-DEBT-06-06` (moved or updated) | `06-DEBT.md` | entry discharged on a working CPU reading, or held open with hardware figures |
| `D-DEBT-06-02` (updated) | `06-DEBT.md` | entry whose revisit trigger is evaluated for the first time on a usable reading |
| `D-DEBT-06-01` (updated) | `06-DEBT.md` | entry recording what the chosen fix achieved on hardware |
| the OPS-07 non-promotion entry | `06-DEBT.md` | new Deferred-section entry (requirement disposition, TEL-06 precedent) |
| `PROH-OPS-07-08` | this plan's frontmatter | new prohibition (a gap-closure round may not promote its own requirement) |
| `PROH-OPS-07-10` | this plan's frontmatter | new prohibition (the acceptance scenario's own parameters are contract, not tuning surface) |

<verification>
- Two JSON reports from the Pi are attached as phase evidence: a concurrency-1 control pass and a 600s concurrency-8 acceptance pass.
- The acceptance report shows `run_kind: "acceptance"` on an `aarch64` / `raspi` host, with `scenario.duration_seconds` 600 and `scenario.concurrency` 8.
- Every PID in `resource_targets` is confirmed against `docker inspect` on the live host to belong to `beacon-web` or `beacon-worker`.
- Both roles' `cpu_sampling` blocks and CPU figures are recorded verbatim, whichever way they fell.
- `assertions.cadence.passed` is recorded with the J1-J4 freshness states — OPS-01 is re-proven on the same run.
- `06-DEBT.md` reflects the hardware outcome across `D-DEBT-06-01`, `D-DEBT-06-02` and `D-DEBT-06-06`, leaves `D-DEBT-06-03` and `D-DEBT-06-05` unchanged, and adds the OPS-07 non-promotion entry.
- `.planning/REQUIREMENTS.md` is byte-identical, with OPS-07 still unchecked and still Pending.
- `git diff -- dashboard/ tests/` is empty for this plan: no code changed.
</verification>

<success_criteria>
- A real Pi acceptance run against the fixed build returns `overall_passed: true` with every route's p95 inside budget, at the same scenario the failing run used.
- That run's CPU column is a measurement for the first time in this phase, and `D-DEBT-06-02`'s revisit trigger is evaluated on it.
- Cadence still holds on the same run, so the responsiveness fix did not cost the phase its first success criterion.
- The debt record matches what hardware established, and OPS-07 is left for an independent verifier to promote.
</success_criteria>

<output>
Create `.planning/phases/06-workload-resilience-pi-acceptance/06-14-SUMMARY.md` when done
</output>
