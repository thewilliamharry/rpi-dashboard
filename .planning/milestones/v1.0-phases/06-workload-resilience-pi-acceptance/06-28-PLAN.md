---
phase: 06-workload-resilience-pi-acceptance
plan: 28
type: execute
wave: 29
depends_on: ["06-27"]
gap_closure: true
gap_ids: [G-06-1]
files_modified:
  - .planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md
  - .planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [OPS-07, OPS-04, OPS-02]

estimate:
  tokens: 64000
  raw_tokens: 64000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`06-SECURITY.md` describes the code that is in HEAD: every closed threat's cited evidence is re-checked against the current tree, and any citation invalidated by the `ea8689e` revert is corrected rather than left standing"
    - "`T-06-24` and `T-06-101` are **verified** as already re-closed on HEAD's evidence by `06-32` Task 3 (`852209d`) — not re-done. `06-32`'s Security Audit Trail entry is read as the worked example of the re-closure this plan owes for every other affected row, and this plan confirms its four cited tests still exist and pass rather than trusting the record"
    - "Every remaining register row whose evidence cites `06-25`'s original range-join shape, `06-29`'s reshape-era shape, or the round-5 narrowed shape as **live request-path** evidence is re-closed on `06-31`'s current shape (the Python producer `_uptime_summary` fed the state-change reduction) or reopened — the obligation `PROH-OPS-07-21` states, now extended past `06-32`'s two rows to the whole register"
    - "The threats `06-25` introduced (a dynamic `IN` list, a second producer of a rendered contract, an uncapped aggregation) are registered with severities and dispositions, not left unnamed"
    - "A fourth `06-25`-lineage threat is registered: the recursive `expanded` CTE's intermediate row count is data-dependent where the prior shape's was fixed at `ports x UPTIME_BUCKETS`, bounded by `segments + len(ports) * (UPTIME_BUCKETS - 1)`, with `06-29`'s own measured 22,247 rows against a computed bound of 23,583 carried as evidence rather than re-derived"
    - "That fourth threat's disposition is scoped to what HEAD actually runs: `06-31` reverted the route wiring, so it is mitigated against a **retained, tested, unreferenced** code path (`D-DEBT-06-24`), NOT against the live request path — the materiality reduction is stated in the row, not silently inherited"
    - "The register's registration ledger is stated completely and accurately: which threat ranges are already registered, which this plan adds, and which remain outstanding — with no range silently omitted and none registered twice"
    - "`PROH-OPS-04-05` prerequisite 4's formal `/gsd-secure-phase 06` re-run is performed here against `06-31`'s shape — `06-32` explicitly declined to claim it — and the parts of it tied to a worker-count increase remain named rather than implied discharged"
    - "The ROADMAP's Phase 6 block states rounds 6 through 8 honestly: what `06-25`..`06-32` did, that option C was refuted twice and reverted rather than left reading as the standing remedy, what the acceptance run measured, and that `06-23`/`06-24` remain superseded"
    - "`STATE.md`'s Current Position, blockers and session-continuity reflect the round's actual end state, including the closed runbook blocker"
    - "OPS-07 remains Pending in both halves of `REQUIREMENTS.md`, and this plan states the check that proves it did not touch the file"
    - "The `06-LOCK-AUDIT.md` line-pinning scope decision is made by this plan, informed by `D-DEBT-06-25`: that entry closed the *question* (retain `(function, line)` for round 7, on stated evidence) but did NOT execute the `(function, ordinal)` re-pinning, which remains this plan's to take or decline. No duplicate debt entry is created; the decision is recorded against `D-DEBT-06-25` and the recurrence count is stated as four realignments, not three"
    - statement: "No threat previously recorded as closed in `06-SECURITY.md` is quietly reopened or dropped: the register's row count and IDs are compared before and after"
      verification: backstop
  artifacts:
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md"
      contains: "`T-06-24`/`T-06-101` verified as already re-closed by `06-32` (not re-done), every remaining stale citation corrected against `06-31`'s shape, the unregistered threat ranges registered with severities and dispositions, the recursive-CTE threat registered and scoped to an unreferenced code path, and an audit-trail entry for this round"
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md"
      contains: "the `(function, ordinal)` re-pinning decision recorded against the existing `D-DEBT-06-25` (no duplicate entry), plus the round-6/round-7 consolidation of `D-DEBT-06-19`/`-20`/`-21` that respects `06-32`'s existing closures"
    - path: ".planning/ROADMAP.md"
      contains: "a round-6 section for Phase 6 describing waves 20-23, the plans, the outcome, and the standing supersession of `06-23`/`06-24`"
    - path: ".planning/STATE.md"
      contains: "Current Position, blockers and session continuity updated to the round's real end state"
  key_links:
    - "The `ea8689e` revert -> `06-SECURITY.md`: the register currently cites `test_api_services_lock_scope_is_database_reads_only`, a test that the revert removed, so a threat is recorded closed on evidence that is not in the tree"
    - "`06-25`'s new query -> `T-06-24`: adding a read inside an existing `_db_lock` block is exactly the change class the invariant exists to distinguish from a widened boundary, and it must be re-checked rather than assumed benign"
    - "The pinning decision -> a fourth realignment: three `app.py` rounds have each paid the same cost, and leaving it unrecorded is how it reaches four"
  prohibitions:
    - id: PROH-OPS-07-21
      requirement: OPS-07
      statement: "A security register entry may never cite evidence that is not in the tree it describes. When a revert removes a test, a fixture or a code shape that a closed threat was closed on, the entry is re-closed on current evidence or reopened — never left standing because its verdict is still believed correct. This obligation now spans three reverts, not one: `ea8689e` (round 5's narrowing), `06-31`'s revert of `06-25`'s route wiring, and by extension `06-29`'s reshape, which that revert also took off the request path. `06-32` Task 3 discharged it for `T-06-24` and `T-06-101` on HEAD's evidence; this plan discharges it for every remaining row and verifies rather than repeats `06-32`'s two."
      status: flagged
      verification: unverified
    - id: PROH-OPS-04-05
      requirement: OPS-04
      statement: "Raising gunicorn's worker count grants a second OS process unserialized concurrent write access to the same database file without any line of `_db_lock` changing. Such a change requires the cross-process audit, a `mem_limit` re-derived from measured per-worker RSS, and `/gsd-secure-phase 06` re-run. This round changes no worker count and engages none of that; the prerequisite's outstanding parts are named, never marked discharged by association."
      status: canon
      verification: prior-round
    - id: PROH-OPS-04-06
      requirement: OPS-04
      statement: "Only computation over already-fetched data may leave `_db_lock`; no database access may. `06-25` moves nothing out at all, so this round must leave the invariant that enforces it untouched and passing."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-08
      requirement: OPS-07
      statement: "A gap-closure round must never record its own requirement complete. `REQUIREMENTS.md`'s OPS-07 row stays Pending; only an independent verification round may promote it."
      status: canon
      verification: prior-round
---

<objective>
Re-close the security boundary on the code that is actually in HEAD, and leave the record coherent.

**This plan deliberately re-expresses `06-24`'s intent.** `06-24` — "Re-close the security boundary
(`/gsd-secure-phase 06`) and record what round 5 changed" — is superseded by the `ea8689e` revert
and is not executed. Its *intent* is still required, and more so than when it was written, so it is
re-expressed here rather than revived. The supersession record stays coherent: `06-24` remains
superseded and unexecuted; this plan carries its purpose against a different change.

**What round 7 changed under this plan's feet, stated before anything else.** This plan was written
in round 6, when `/api/services` ran `06-25`'s bulk SQL aggregation. It does not any more. `06-31`
reverted that route wiring and fed the restored Python producer `_uptime_summary` a state-change-only
reduction; `06-32` measured the result at 34.927ms against the 56.820ms bar (PASS) and, in the same
round, performed part of this plan's own work: it registered `T-06-158`..`T-06-164` and re-closed
`T-06-24` and `T-06-101` on HEAD's current evidence (`852209d`). Three consequences run through
everything below:

1. **`T-06-24` and `T-06-101` are already done.** This plan VERIFIES them and reads `06-32`'s
   Security Audit Trail entry as the worked example of the re-closure it owes elsewhere. It does not
   repeat that work, and it must not reopen either row merely because the paragraphs below were
   written before `06-32` ran.
2. **`read_uptime_strips_by_port` and `UPTIME_STRIP_QUERY` are still in the tree, fully tested, and
   have no production caller** (`D-DEBT-06-24`). Every threat about the SQL reader is therefore a
   threat about retained, unreferenced code — real, but materially less so than a request-path
   threat, and the register must say which.
3. **Round 7's threats are registered; several earlier rounds' are not.** See the registration
   ledger in Task 1; it is stated explicitly because the naive reading of this plan's original
   `T-06-120`..`T-06-145` scope would both miss ranges and duplicate rows.

**Why the security pass is genuinely owed, not ceremonial.** `06-SECURITY.md`'s `T-06-24` row was
closed partly on evidence a revert removed — the defect this plan exists to catch, now demonstrated
rather than hypothetical. Read as the historical record of why (`06-32` has since corrected this
specific row; the reasoning is retained because it generalizes to the rows still uncorrected). It
cited
`LockScopePreservationTests::test_api_services_lock_scope_is_database_reads_only` — the narrowed-shape
pin — and `NarrowedShapeConcurrentAccessTests`. Those two are in different positions and the row
treats them alike, which is itself part of the problem:

- The narrowed-shape pin is **gone as a test**. `ea8689e` backed the narrowing out and
  `tests/test_lock_profile.py` now carries the pre-narrowing containment pin restored verbatim from
  `1a4db68^`. The name survives only inside comments at `tests/test_lock_profile.py:643` and
  `:1930`, so a bare grep for it across `tests/` returns hits and a careless check will conclude it
  is still there. Search for its **definition**, not its name.
- `NarrowedShapeConcurrentAccessTests` **survives**, at `tests/test_workload_resilience.py:945`. It
  is not removed. What is stale about it is its subject: its docstring describes the hazard the
  `06-20` narrowing created — rows materialized inside `_db_lock` and consumed after the block and
  connection close — and that shape is no longer what `api_services` does. It is live evidence for a
  build that is not HEAD, which is a different correction from a removed citation and must be
  written as one.

Either way a high-severity threat is recorded closed on evidence that does not describe the tree.
That is a register describing a build that no longer exists, and it is exactly the defect class this
project keeps finding: a guard, or a claim, asserting something it cannot detect.

**And `T-06-101` (high) carried the identical defect.** It was closed on
`HeldRegionCompositionTests::test_services_held_region_is_sql_dominated_after_narrowing` — a measured
composition guard on the reverted narrowing — and on the same removed narrowed-shape pin. `06-32`
re-closed it, re-scoping the row from "detects an undisclosed narrowing regression" (moot: the
narrowing was reverted overtly, by decision) to "the held region's measured Python share stays under
its calibrated ceiling even with `_uptime_summary` running inside the lock again." Verify that
re-scoping still describes HEAD; do not redo it.

**And the boundary's contents changed twice more since.** `06-25` added one database read inside
`api_services`' existing `with _db_lock` block and removed a Python computation from it; `06-31`
then reverted that read, restored the Python producer inside the same block, renamed the scope pin's
`required_calls` back, and forced a fourth realignment of all 28 rows of `06-LOCK-AUDIT.md`
(`D-DEBT-06-25`, +42 net lines). Neither widens a concurrency boundary — but "neither widens it" is a
claim to be re-checked against HEAD, not assumed, and it must now be checked against `06-31`'s shape
rather than `06-25`'s. `PROH-OPS-04-05`'s prerequisite 4 exists for precisely this re-check, and
remains outstanding: `06-32` recorded its own re-closure as a SUMMARY-level correction of two stale
citations, explicitly **not** the formal `/gsd-secure-phase 06` re-run. That re-run is this plan's.

**What this plan does not do.** It does not promote OPS-07 (`PROH-OPS-07-08`). It does not decide
option A. It does not change a worker count, a budget, or `_db_lock`'s scope. And it does not mark
`PROH-OPS-04-05` discharged: only the parts this round's changes touch are re-closed, and whatever
remains outstanding is named as outstanding.

Purpose: `PROH-OPS-04-05` prerequisite 4, plus the record consolidation round 6 owes.
Output: a register that describes HEAD, a debt entry for a recurrence, and a coherent roadmap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-LOCK-AUDIT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-31-SUMMARY.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-32-SUMMARY.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-27-SUMMARY.md
</context>

<flagged_assumptions>

## Spec-less probe rows — all 5 surfaced, none resolved

No `06-SPEC.md` exists, so all five probe rows came back `unclassified` and stay **unresolved**.
Neither auto-resolved nor auto-dismissed. No-silent-drop check: 5 probe rows == 5 entries below.

| # | Requirement | Probe status | Assumption, stated so it can be falsified |
|---|---|---|---|
| 1 | OPS-01 | unresolved (unclassified) | Assumed: a documentation-only round cannot affect sampling cadence. Basis: `files_modified` contains no source file. |
| 2 | OPS-02 | unresolved (unclassified) | Carried because the security re-audit re-reads the preview-queue threat rows. Assumed: their mitigations are unchanged since `06-03`; Task 1 re-checks each cited artifact exists in HEAD rather than assuming it. |
| 3 | OPS-03 | unresolved (unclassified) | Same basis: the thumbnail-store threat rows are re-checked for stale citations, not re-derived. |
| 4 | OPS-04 | unresolved (unclassified) | The core of this plan. Assumed at entry, verified at exit: neither `06-25`'s added read nor `06-31`'s revert of it widens a concurrency boundary — the claim must now be checked against `06-31`'s shape, which is what HEAD runs, not `06-25`'s. `test_no_database_access_escapes_the_db_lock` and the four-times-realigned 28-site audit are the falsifiers, both re-run in Task 1. |
| 5 | OPS-07 | unresolved (unclassified) | Assumed: consolidating the record cannot change the measurement. Verified by `git diff --quiet -- .planning/REQUIREMENTS.md` and by leaving every acceptance artifact from `06-27` unedited. |

</flagged_assumptions>

<artifacts_this_phase_produces>

This plan creates no new symbol and no new file. It edits four existing planning documents:
`06-SECURITY.md`, `06-DEBT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.

New identifiers introduced into the phase record (not into source):

- Threat IDs registered in `06-SECURITY.md` from the emitting plans' `<threat_model>` blocks. The
  ledger is stated in full because the register is **not** simply "round 6's twenty-six" any more —
  Task 1 carries the authoritative version and the greps that establish it:
  - **`T-06-120`..`T-06-145`** (26) — round 6: `06-25` emits `T-06-120`..`T-06-126` plus `T-06-144`
    and `T-06-145`, `06-26` emits `T-06-127`..`T-06-131`, `06-27` emits `T-06-132`..`T-06-137`, and
    **this plan emits `T-06-138`..`T-06-143`**. This plan's own six are registered by this plan; a
    register whose purpose is to describe HEAD may not omit the threats of the change that writes
    it, and `T-06-141` (high — an unscoped write destroying other phases' ROADMAP entries) is one.
  - **`T-06-146`..`T-06-151`** (6) — `06-29`'s reshape. Unregistered.
  - **`T-06-152`..`T-06-157`** (6) — `06-30`'s decision round. Unregistered.
  - **`T-06-158`..`T-06-164`** (7) — `06-31`'s revert-plus-reduction. **ALREADY REGISTERED by
    `06-32` Task 3.** Verified, never re-added.
  - **`T-06-165`..`T-06-171`** (7) — `06-32` itself. Unregistered.
  - One further ID, the recursive-CTE row this plan mints (see Task 1), taking the next free number
    after `T-06-171`.
- Prohibition IDs `PROH-OPS-07-15` .. `PROH-OPS-07-22`, minted across `06-25`..`06-28`. Rounds 6.5
  and 7 minted further IDs through at least `PROH-OPS-07-29`; this plan mints none and cites those.
- No new `D-DEBT-06-` entry for the `06-LOCK-AUDIT.md` pinning question: `D-DEBT-06-25` already
  exists and holds it. This plan records its decision against that entry.

The full plan-set artifact list is in `06-25-PLAN.md`.

</artifacts_this_phase_produces>

<reversibility>

| Decision this plan implements | Rating | Rationale |
|---|---|---|
| Verify `06-32`'s re-closure of `T-06-24`/`T-06-101` rather than redo it | `reversible` | A register entry; reopening either is an edit. Verification is strictly cheaper than re-derivation and `06-32`'s own audit-trail entry states the four cited tests, so the check is mechanical |
| Take or decline the `(function, ordinal)` re-pinning, informed by `D-DEBT-06-25` | `reversible` | `D-DEBT-06-25` closed the *question* for round 7 (retain `(function, line)`) but did not execute the re-pinning; it is still this plan's to take. Either way is reversible, but executing it edits `tests/test_lock_profile.py`'s comparison mechanism and `06-LOCK-AUDIT.md`'s identity column — the file carrying `T-06-24`'s closure evidence — which is why it is a stated decision rather than a maintenance edit |
| Consolidate the ROADMAP and `STATE.md` narrative | `reversible` | Documentation; scoped `Edit` only, never a whole-file rewrite |

No `one-way` decision is implemented, so no `checkpoint:decision` is inserted.

</reversibility>

<tasks>

<task type="auto">
  <name>Task 1: Re-close the security boundary against HEAD, and correct what the revert invalidated</name>

  <files>.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md</files>

  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md` in full — the threat
      register's column shape, the Security Audit Trail's per-round entry format, and specifically
      the `T-06-24`, `T-06-101`, `T-06-103` and `T-06-112` rows whose cited evidence must each be
      checked against the tree. `T-06-101` is at line 69.
    - `tests/test_lock_profile.py` `LockScopePreservationTests` and `LockScopeInvariantTests` as they
      stand after `06-25` Task 3 — the actual names of the tests now in the tree, so a citation names
      something that exists.
    - `dashboard/beacon/repositories.py` `UPTIME_STRIP_QUERY` and `read_uptime_strips_by_port` — the
      surface being registered. Note before writing any row: this code is **retained and has no
      production caller** as of `06-31`'s revert (`D-DEBT-06-24`). Confirm that by grepping
      `dashboard/` for callers, not by assuming either way.
    - `dashboard/app.py` `api_services` — to confirm by reading, not by assuming, which producer is
      on the request path at HEAD. It is `_uptime_summary` (the Python producer), fed a
      state-change-only reduction of `checks_by_port`; it is not `read_uptime_strips_by_port`.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md` § Security Audit Trail,
      the `06-32` entry — the worked example of the re-closure this task owes for every remaining
      row, including its before/after register counts (35 -> 42) and its definition-grep evidence.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` `D-DEBT-06-24`,
      `D-DEBT-06-25`, `D-DEBT-06-26` — the retention rationale for the unreferenced SQL reader, the
      pinning decision as `06-32` left it, and this plan's own amendment list.
    - `tests/test_workload_resilience.py` line 945 — `NarrowedShapeConcurrentAccessTests` and its
      class docstring. It survives the revert; read it to see that what is stale is its subject, not
      its existence.
    - The `<threat_model>` block of every plan whose threats are not yet in the register:
      `06-25-PLAN.md`, `06-26-PLAN.md`, `06-27-PLAN.md`, **this plan**, `06-29-PLAN.md`,
      `06-30-PLAN.md` and `06-32-PLAN.md`. Read `06-31-PLAN.md`'s block too, but only to confirm its
      seven rows already match what `06-32` registered — not to re-register them. The registration
      ledger in the action below is authoritative over this list if the two ever disagree.
    - `06-RESEARCH.md` § Security Domain — the ASVS category table this phase audits against, and the
      V5 Input Validation row that governs the new dynamic `IN` list.
  </read_first>

  <action>
    Run the security re-close pass for phase 06 — the `/gsd-secure-phase 06` workflow's substance —
    at ASVS level 1, blocking on `high`. Update `06-SECURITY.md` in place with scoped edits; never
    rewrite the file wholesale, because it carries five rounds of closure evidence.

    **First, count and record.** Before editing, record the number of rows in the threat register and
    the full set of threat IDs. Record both again after. No previously-closed threat may be dropped,
    and none may be silently reopened; if a verdict changes, it changes with a stated reason.

    **Re-check every closed threat's cited evidence against HEAD.** For each row with disposition
    `mitigate` and status `closed`, confirm the artifact it cites — a test name, a fixture path, a
    source construct — exists in the current tree. This is the pass's real work, and it has a known
    positive to find:

    - `T-06-24` and `T-06-101` — **VERIFY, do not redo.** `06-32` Task 3 (`852209d`) already
      re-closed both on HEAD's evidence, and its Security Audit Trail entry states exactly what each
      now cites. Confirm the four tests it names still exist as definitions and still pass in this
      task's own verify command; confirm `NarrowedShapeConcurrentAccessTests` is still present at
      `tests/test_workload_resilience.py:945` and still not cited as live containment evidence.
      Record the confirmation. Change neither row unless a check fails — and if one does, that
      failure is the finding, reported as such rather than quietly repaired. In particular, do not
      reopen either row on the strength of this plan's own round-6-era prose above; that prose is
      retained as the historical statement of the defect, and `06-32` corrected it.
    - Then apply the same treatment `06-32` applied to those two rows to **every remaining row** that
      cites a shape no longer on the request path. Three shapes are now off it: round 5's narrowed
      lock (`ea8689e`), `06-25`'s original range-join `UPTIME_STRIP_QUERY`, and `06-29`'s reshaped
      index-arithmetic-plus-recursive-expansion form (`06-31`'s revert). A row citing any of them as
      **live request-path** evidence is re-closed on `06-31`'s current shape — the Python producer
      `_uptime_summary` fed the state-change-only reduction — or reopened with the reason stated. A
      row citing them as evidence about *retained, unreferenced* code is not wrong, but must say so.
    - `T-06-112` cites `AdvancedCurrentCostTests::test_payload_is_unchanged_by_the_round_5_remedy` and
      `tests/fixtures/advanced_current_pre_remedy_golden.json`. The remedy it guards was also
      reverted. Check whether the test and fixture survive; record what you find, and mark the row's
      status honestly — closed on surviving evidence, or reopened with the reason.
    - `T-06-103` cites the three `api_services_pre_narrowing_*_golden.json` fixtures and
      `ApiServicesOutputEquivalenceTests`. These survive and `06-25` re-exercised them against a new
      change, which strengthens rather than invalidates the row; annotate it with that.

    **Register the unregistered threats — from the ledger, not from memory.** Establish the ledger
    first, by reading the register rather than trusting this plan: list the register's current IDs,
    then list the IDs each plan's `<threat_model>` emits, then register the difference. As measured
    at this amendment (2026-09-06, register at 42 rows) the difference is:

    | Range | Count | Source plan | Action |
    |---|---|---|---|
    | `T-06-120`..`T-06-145` | 26 | `06-25`/`06-26`/`06-27`/**this plan** | REGISTER |
    | `T-06-146`..`T-06-151` | 6 | `06-29` | REGISTER |
    | `T-06-152`..`T-06-157` | 6 | `06-30` | REGISTER |
    | `T-06-158`..`T-06-164` | 7 | `06-31` | **ALREADY REGISTERED by `06-32` — verify only** |
    | `T-06-165`..`T-06-171` | 7 | `06-32` | REGISTER |

    That is **45 rows to add and 7 to verify**, taking the register from 42 to 87 plus the one new
    row minted below. If the measured ledger differs from this table when the task runs, the measured
    ledger wins and the divergence is recorded in the SUMMARY — a later round may have registered
    more. Do not add a row whose ID is already present; a duplicate is the same integrity defect as
    an omission, in the opposite direction.

    Within `T-06-120`..`T-06-145`, registering this plan's own `T-06-138`..`T-06-143` is not
    optional: `T-06-141` is a high-severity unscoped-write hazard against `ROADMAP.md` that Task 2
    runs directly into, and a register that omits the threats of the change writing it is the same
    "describes a build that is not HEAD" defect at one remove. Each row carries its category,
    component, severity and disposition, and each mitigation names the specific artifact that carries
    it rather than generic advice. Five deserve particular care because they are the round's
    genuinely new surface:
    - the dynamic `IN` list in `UPTIME_STRIP_QUERY` (Tampering, high) — closed on placeholder-only
      parameterization and the test asserting no port literal reaches the rendered SQL, which is the
      V5 Input Validation control `06-RESEARCH.md` names;
    - two producers of one rendered contract (Repudiation, critical) — closed on the differential
      oracle and `PROH-OPS-07-16`;
    - an uncapped aggregation (Information Disclosure, high) — closed on bounded-by-construction plus
      the over-cap value assertion, and on `PROH-OPS-07-17`;
    - `T-06-144`, an input-set widening (Repudiation, high) — the new producer taking a boundary
      sample from a check older than `CHECK_RETENTION_SECONDS`, which `all_checks` has never
      returned. Closed on the reader's caller-supplied retention floor, the two beyond-retention
      differential cases, mutation (a) proving they discriminate, and the `_route_input_rows` helper
      that feeds the oracle the route's subset. `PROH-OPS-07-22`. Record that no golden fixture
      covers this, so `T-06-121`'s mitigations do not reach it;
    - `T-06-145`, a NULL `online` rendering as a plausible number (Information Disclosure, medium) —
      closed on the reader raising rather than coercing, and on the test asserting both producers
      refuse the same input.

    **Scope every SQL-reader threat to what HEAD actually runs.** All five above, and every other row
    describing `UPTIME_STRIP_QUERY` or `read_uptime_strips_by_port`, describe code that `06-31` took
    off the request path. It is retained, fully tested, and has **no production caller**
    (`D-DEBT-06-24`). Each such row states that explicitly. This is a real materiality reduction —
    an injection or disclosure surface reachable only by a future caller is not the same exposure as
    one reachable by an HTTP request today — and it is stated rather than assumed in either
    direction: the threats are not downgraded to `accept` merely because the code is unreferenced,
    because the guards keeping them closed are exactly what make re-wiring it safe later.

    **Register the fourth `06-25`-lineage threat, which no round has yet recorded.** `06-25`'s three
    (dynamic `IN` list, second producer, uncapped aggregation) are named in this plan's own
    `must_haves`; `06-GUARD-DECISION.md` §7 identified a fourth that was never added. Mint it at the
    next free ID after `T-06-171` and register it: **the recursive `expanded` CTE's intermediate row
    count is data-dependent**, where the shape it replaced (`requested_ports CROSS JOIN buckets`) was
    fixed at `ports x UPTIME_BUCKETS` regardless of the underlying data. Category Denial of Service,
    severity **low**, disposition mitigate. Its bound is
    `segments + len(ports) * (UPTIME_BUCKETS - 1)` — provable because a port's segments partition
    `[start, now]` contiguously, so each internal bucket boundary splits exactly one segment,
    contributing at most one extra expanded row per boundary crossed. Carry `06-29`'s already-measured
    figure as evidence rather than re-deriving it: **22,247 expanded rows against a computed bound of
    23,583**, on the 8-service/8-day profiled shape. Closed on `UptimeStripBoundednessTests`
    (`tests/test_services_route_scaling.py:1240`) and `PROH-OPS-07-17`.

    **State that threat's materiality change explicitly, and do not carry §7's framing forward
    unaltered.** `06-GUARD-DECISION.md` §7 raised this threat against `06-29`'s reshape while that
    reshape was wired to `/api/services`, and assumed it would still be live when this plan executed.
    It is not. The row's disposition is therefore "mitigated against a **retained, tested,
    unreferenced** code path", NOT "mitigated against the live request path" — the severity is `low`
    rather than `medium` for that reason, and the row says so in a sentence a later reader cannot
    miss. `UptimeStripBoundednessTests` still passes and still proves the bound; what changed is that
    no HTTP request reaches the code it bounds. Record both halves: the guard is real, and the
    exposure it guards is currently unreachable from outside.

    **State `PROH-OPS-04-05`'s position precisely.** Prerequisite 4 asks for `/gsd-secure-phase 06`
    to re-run because `T-06-24`'s closure evidence is invalidated by construction under a worker-count
    change. This round changes no worker count. Record that the re-run has now happened and what it
    covered; record equally that prerequisites tied to a worker-count increase remain unengaged and
    undischarged, because that change has not been made. Do not let a re-run performed for one reason
    read as clearing a prerequisite scoped to another. Note that `06-32` recorded its own re-closure
    as a SUMMARY-level correction of two stale citations and stated in terms that it was **not** the
    formal re-run prerequisite 4 requires; this task is that re-run, and it is the first thing in
    this phase that may claim to be.

    **Add a Security Audit Trail entry** in the file's existing per-round format, following the
    `06-32` entry. What changed since that entry: nothing in `dashboard/` or `tests/` — `06-31`'s
    revert-plus-reduction remains the tree, and this plan's own edits are documentation. What this
    entry records is therefore the **audit**, not a code delta: the formal `/gsd-secure-phase 06`
    re-run against `06-31`'s shape, the 45 rows registered and 7 verified, the recursive-CTE row
    minted and scoped to unreferenced code, every re-closure or reopening performed, and what remains
    outstanding. State the register's before and after counts and ID sets, as `06-32`'s entry does.
  </action>

  <verify>
    <automated>uv run --project dashboard python -m pytest tests/test_lock_profile.py tests/test_services_route_scaling.py -q && grep -c "^| T-06-" .planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md</automated>
  </verify>

  <acceptance_criteria>
    - The threat register's pre-edit and post-edit row counts and ID sets are both recorded in the
      SUMMARY, and no pre-existing ID is absent from the post-edit set.
    - For every closed `mitigate` row, the cited test or fixture name is confirmed present in the
      tree, or the row is corrected; the SUMMARY lists each row checked and the outcome.
    - The round-5 narrowed-shape pin is searched for as a **definition**, not as a bare name:
      `grep -rn "def test_api_services_lock_scope_is_database_reads_only" tests/` returns nothing,
      while a bare-name grep returns two comment hits at `tests/test_lock_profile.py:643` and
      `:1947`. **Both line numbers are re-derived by running the grep, never trusted from this
      plan:** the second was `:1930` when this plan was written and moved to `:1947` under `06-31`'s
      +42-line edit — the same `(function, line)` drift `D-DEBT-06-25` records, reaching this plan's
      own acceptance criteria. Record what the grep actually returns and note any further drift.
      Both results are recorded in the SUMMARY, because the bare-name grep is the check that would
      wrongly conclude the pin survives. Every occurrence of the name in `06-SECURITY.md` then sits
      inside a sentence identifying it as withdrawn by the revert — never presented as live closure
      evidence. The SUMMARY quotes each occurrence and its surrounding sentence so the judgement is
      auditable rather than asserted. <!-- planner-discipline-allow: test_api_services_lock_scope_is_database_reads_only -->
      <!-- planner-discipline-allow: def test_api_services_lock_scope_is_database_reads_only -->
    - `NarrowedShapeConcurrentAccessTests` is confirmed **present** at
      `tests/test_workload_resilience.py:945`, and `06-SECURITY.md` does not describe it as removed.
      Where a row cites it, the row states that the class survives but that the shape its docstring
      describes was reverted, so it is not live evidence for HEAD's containment posture.
    - `T-06-101`'s row is confirmed to carry `06-32`'s re-scoping (the held region's measured Python
      share stays under its calibrated ceiling with `_uptime_summary` inside the lock again), and
      `HeldRegionCompositionTests::test_services_held_region_is_sql_dominated_after_narrowing` is
      confirmed to exist and pass. The SUMMARY records the confirmation, or — if a check fails — the
      failure and the resulting reopening. The row is not left closed on the reverted narrowing's
      premise, and is not re-resolved a second time when the first resolution still holds.
    - `T-06-24`'s row cites at least `test_no_database_access_escapes_the_db_lock` and
      `test_every_db_lock_site_is_covered_by_the_audit`, and both pass in this task's verify command.
    - Every register row citing `UPTIME_STRIP_QUERY` or `read_uptime_strips_by_port` states that the
      code is retained with no production caller (`D-DEBT-06-24`). The SUMMARY lists those rows and
      quotes the scoping sentence from each, so the materiality claim is auditable.
    - The newly minted recursive-CTE row states its bound
      (`segments + len(ports) * (UPTIME_BUCKETS - 1)`), carries `06-29`'s measured 22,247 against the
      computed 23,583, and states in its own sentence that it is mitigated against an unreferenced
      code path rather than the live request path. The SUMMARY quotes that sentence.
    - The registration ledger is satisfied and stated: `T-06-120`..`T-06-145`, `T-06-146`..`T-06-151`,
      `T-06-152`..`T-06-157` and `T-06-165`..`T-06-171` are all present with no gap in any range,
      `T-06-158`..`T-06-164` are present exactly once each (verified, not re-added), and the newly
      minted recursive-CTE row is present. Each carries a severity in {critical, high, medium, low}
      and a disposition in {mitigate, accept, transfer}. The SUMMARY states the register's row count
      before and after; the "after" count equals "before" plus the number of rows actually added, and
      that arithmetic is shown.
    - No duplicate IDs: the register's ID list, sorted, contains no repeated value. Checked by
      comparing `grep -o "^| T-06-[0-9]*" | sort | wc -l` against the same pipeline with `sort -u`;
      the two must be equal, and both figures are recorded.
    - The set includes this plan's own `T-06-138`..`T-06-143`.
    - Every `accept` disposition carries a written rationale; no row is left without one.
    - The `PROH-OPS-04-05` paragraph distinguishes what this re-run covered from what remains
      outstanding, and does not mark the prerequisite discharged.
    - A round-6 Security Audit Trail entry exists in the file's existing format.
    - `git diff -- dashboard/ tests/` is empty: this task changes no source.
  </acceptance_criteria>

  <done>`06-SECURITY.md` describes HEAD — `06-31`'s Python producer on the request path and the SQL reader retained but unreferenced — rather than any of the three shapes since reverted; `T-06-24` and `T-06-101` are verified as already re-closed rather than redone; every unregistered threat range is registered exactly once with severities and dispositions; the recursive-CTE threat is registered and scoped to an unreferenced code path; and `PROH-OPS-04-05`'s remaining scope is named rather than implied clear.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the record — debt, roadmap, and state</name>

  <files>.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md, .planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` — the three-section structure
      (Deferred / Decided / Discharged) and each entry's table format, plus `D-DEBT-06-19`, `-20`,
      `-21` as `06-27` left them.
    - `.planning/ROADMAP.md` Phase 6 block in full — the criterion-5 amendment note, the per-round
      section format ("Gap-closure round", "Second gap-closure round", ...), the plan checklist, and
      the wave blocks. Round 6's section matches that format.
    - `.planning/STATE.md` — Current Position, Blockers/Concerns, Session Continuity, and the
      `stopped_at` / `last_activity_desc` frontmatter fields.
    - `.planning/phases/06-workload-resilience-pi-acceptance/.continue-here.md` — the checkpoint whose
      `remaining_work` items 7 and 8 this round acted on, so the update says which are closed. Item 4
      (escalate option A) is **dead, not outstanding**: option A is refuted twice and "not
      reconstructible" (`D-DEBT-06-21` round-7 addendum, `PROH-OPS-07-29`). Items 1-3 describe option
      C, which `06-31` reverted. Record them as closed-by-refutation, never as pending work.
    - `06-27-SUMMARY.md` — the measured outcome every consolidation statement must trace to.
  </read_first>

  <action>
    Three documents, all edited with scoped replacements. **Never rewrite `ROADMAP.md` or `STATE.md`
    wholesale** — a whole-file write destroys phase entries outside the diff window.

    **`06-DEBT.md`.** **Do NOT add a new entry for the `06-LOCK-AUDIT.md` pinning recurrence.**
    `D-DEBT-06-25` already exists and holds it, written by `06-32`: it records four realignments
    (`06-19`, `06-20`, `06-25`, `06-31`) rather than the three this plan originally counted, both
    options with their reversibility, and a decision — retain `(function, line)` for round 7, at a
    measured +42 net lines — taken on stated evidence rather than deferred. A duplicate entry would
    be the register defect this plan exists to prevent.

    What `D-DEBT-06-25` explicitly did NOT do is execute the `(function, ordinal)` re-pinning, which
    it hands back to this plan. Make that decision here and record it as an addendum under
    `D-DEBT-06-25`, not as a new entry:
    - **Decline it** — retain `(function, line)` and accept a further realignment as the bounded
      cost, as `D-DEBT-06-25` did for round 7. This is the only option this plan can execute:
      `files_modified` contains no source file, and an acceptance criterion below requires
      `git diff -- dashboard/ tests/` to be empty.
    - **Recommend taking it** — record that `(function, ordinal)` should be adopted, and state
      plainly that executing it needs its own plan, because it rewrites `06-LOCK-AUDIT.md`'s identity
      column AND the comparison in
      `tests/test_lock_profile.py::test_every_db_lock_site_is_covered_by_the_audit` that enforces it
      — a source change outside this plan's scope, against the file carrying `T-06-24`'s closure
      evidence. A recommendation is not an execution; do not blur the two.
    State the reason either way, and state which of the two was chosen rather than leaving it
    inferable. Note that this plan's own Task 1 acceptance criteria already paid this cost once: a
    cited line number (`:1930`) drifted to `:1947` under `06-31`'s edit, and this plan's amendment
    had to correct it.

    Then consolidate the OPS-07 entries against `06-27`'s measured result. **`D-DEBT-06-21` is
    already closed by `06-32`** — option C refuted twice, the operator's choices recorded, and the
    `service_rollups` path re-refuted as "not reconstructible". Do not reopen or restate it; append
    only `06-27`'s Pi-class outcome as a dated addendum, since every figure in it today is dev-host.
    `D-DEBT-06-20`'s closing clause is annotated with which half is now satisfied. `D-DEBT-06-19`'s
    rollup remediation stays refuted with a pointer to how its cost-model reframe was acted on — by
    `06-31`'s input reduction, which passed, not by option C, which did not. Delete nothing. Add a
    short note to `D-DEBT-06-13` if the round observed the known-flaky set behaving differently, and
    say so only if it did.

    **`.planning/ROADMAP.md`.** Using scoped `Edit` calls only:
    - Read the Phase 6 block first and record what is already there. As of this amendment the
      checklist rows for `06-25` .. `06-32` **already exist** with their verdict sub-bullets, and
      `06-29`..`06-32` are already marked executed. Do not re-add them. What is missing is narrative
      structure, not rows.
    - Update the Phase 6 `**Plans**:` prose line to state the executed count including this round,
      and keep the existing sentence that the phase does not seal until an independent verification
      round promotes OPS-07 (`PROH-OPS-07-08`).
    - Tick `06-27` and `06-28`'s checklist rows and add each a verdict sub-bullet in the established
      format, leaving `06-23` and `06-24` unchecked exactly as they are.
    - Round sections currently stop at the fifth gap-closure round, with round 6 described in prose
      and wave blocks running to 23. Rounds 6.5 (`06-29`/`06-30`, waves 24-25), 7 (`06-31`/`06-32`,
      waves 26-27) and this round (`06-27`/`06-28`, waves 28-29) have checklist rows but no sections.
      Add them in the same format as the prior five: a short header stating what each round did and
      why, then its wave blocks with each plan's dependency stated as load-bearing rather than
      incidental. Round 6.5 is the reshape and the keep-vs-revert decision; round 7 is the revert
      plus the input reduction and the PASS that followed; this round is the Pi-class acceptance
      attempt and the security re-audit.
    - Correct the round-6 header's framing where it survives: round 6 pursued `D-DEBT-06-21`'s
      option C on the re-measured attribution in `06-PROFILE-2.md`, and option C was **refuted twice
      and reverted**. Do not leave the section reading as though option C were the standing remedy.
    - Close with a paragraph naming the prohibitions minted across these rounds (`PROH-OPS-07-15`
      through at least `PROH-OPS-07-29`; derive the actual ceiling by grepping rather than trusting
      this number) and restating that OPS-07 was deliberately not promoted.
    - State that `06-23` and `06-24` remain superseded by the `ea8689e` revert and were not executed,
      and that `06-28` re-expresses `06-24`'s intent against a different change so the supersession
      record stays coherent.
    - Do not touch criterion 5, its amendment note, or any route budget.

    **`.planning/STATE.md`.** Using scoped `Edit` calls only:
    - Frontmatter: update `stopped_at`, `last_activity`, `last_activity_desc`, and the plan counts in
      `progress`.
    - Current Position: state the round's real end state — plans executed, what `06-27` measured, and
      that OPS-07 remains Pending awaiting an independent verification round.
    - Blockers/Concerns: **remove** the "Unrecorded runbook" blocker, which `06-27` closed by writing
      `06-ACCEPTANCE-RUNBOOK.md`, and say where it went. Update the OPS-07 blocker to `06-27`'s
      Pi-class result; it was already rewritten by `06-32` to the round-7 PASS, so **edit it, do not
      re-derive it**, and do not reintroduce the stale-profile clause that earlier rounds removed.
      Leave the phase-03 unsatisfiable-acceptance-criterion concern untouched; it is not this round's.
    - Accumulated Context / Decisions: append this round's decisions in the existing one-line format.
      Note that round 6's decisions (the promote-not-add-alongside choice for the strip's producer,
      the preserved bucket origin and the rejected epoch alignment, the Python-side rounding
      boundary) describe option C, which `06-31` reverted — record them as history if they are not
      already present, never as standing decisions. This round's own decisions are the
      stop-before-acceptance gate, whatever `06-27`'s Pi-class gate word produced, and the
      `(function, ordinal)` re-pinning decision taken or declined above.
    - Session Continuity: point at whatever the next action actually is, derived from `06-27`'s
      outcome — an independent verification round if the run passed, or the option-A decision round
      if it did not.
  </action>

  <verify>
    <automated>git diff --quiet -- .planning/REQUIREMENTS.md && grep -c "^### D-DEBT-06-" .planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md && grep -c "^### Phase " .planning/ROADMAP.md && grep -c "06-2[5-8]-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --quiet -- .planning/REQUIREMENTS.md` succeeds: OPS-07 is untouched in both halves
      of the file (`PROH-OPS-07-08`).
    - `grep -c "^### Phase " .planning/ROADMAP.md` returns the same count as before this task, proving
      no phase entry was destroyed by an unscoped write; record the before and after counts in the
      SUMMARY.
    - `grep -c "06-2[5-8]-PLAN.md" .planning/ROADMAP.md` returns **at least 4** (it is 4 before this
      task; the new round sections may name those plans again, which is expected and not a failure),
      and `06-23`/`06-24` are still present and still unchecked. Record the before and after counts.
    - `grep -c "06-3[0-2]-PLAN.md" .planning/ROADMAP.md` is non-decreasing: rounds 6.5 and 7's
      checklist rows already exist and must survive the edit.
    - The `06-DEBT.md` entry count is **non-decreasing** — 26 before this task — and no prior entry
      heading is absent. It is expected NOT to increase: the pinning entry this plan once intended to
      create already exists as `D-DEBT-06-25`. If the count did increase, the SUMMARY names the entry
      added and why it is not a duplicate of an existing one. Record both counts.
    - `D-DEBT-06-25` carries this plan's `(function, ordinal)` decision as a dated addendum — taken or
      declined, with a reason — and no second entry covering the same question exists. The SUMMARY
      quotes the addendum.
    - `D-DEBT-06-21` is not reopened: its `06-32` closure status line is unchanged, and anything this
      round adds to it is a clearly-marked dated addendum carrying `06-27`'s Pi-class figures.
    - The `STATE.md` blockers list no longer contains an entry about the acceptance-harness command
      path being unwritten, and `06-ACCEPTANCE-RUNBOOK.md` exists. Checked by reading the
      Blockers/Concerns section, not by a text gate over the whole file.
    - `STATE.md`'s OPS-07 blocker no longer asserts that scoping is blocked on a fresh profile — that
      clause is superseded by `06-PROFILE-2.md` and `06-PROFILE-3.md`.
    - The ROADMAP carries a section for each of rounds 6.5, 7 and this round, with wave blocks
      through 29, and names the prohibitions minted across them from `PROH-OPS-07-15` to whatever
      grepping the phase directory shows as the current ceiling. It states that `06-23`/`06-24`
      remain superseded while `06-28` re-expresses `06-24`'s intent, and its round-6 framing no
      longer presents option C as the standing remedy.
    - Every statement about what `06-27` measured traces to a figure in `06-27-SUMMARY.md` or
      `06-ACCEPTANCE-C3-RUN3.md`; none is inferred.
    - `git diff -- dashboard/ tests/` is empty: this task changes no source.
  </acceptance_criteria>

  <done>The debt register carries the `(function, ordinal)` decision as an addendum to `D-DEBT-06-25` rather than a duplicate entry, and the consolidated OPS-07 entries respect `06-32`'s existing closures; the ROADMAP's Phase 6 block tells rounds 6, 6.5, 7 and 8's story in the phase's own format without losing a prior entry or leaving option C reading as the standing remedy; and `STATE.md` describes where the project actually is — with the runbook blocker gone and OPS-07 still Pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> a future round's decisions | A register entry or a roadmap sentence becomes an input to work nobody re-derives; the integrity risk is a stale claim believed current |
| scoped edit -> whole-document integrity | `ROADMAP.md` and `STATE.md` carry entries for every phase; an unscoped write destroys what is outside the diff window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-138 | Repudiation | A threat recorded closed on evidence removed by `ea8689e` | high | mitigate | Task 1 re-checks every closed `mitigate` row's cited artifact against the tree and corrects or reopens; `PROH-OPS-07-21` |
| T-06-139 | Tampering | A closed threat silently dropped from the register during editing | high | mitigate | Row count and full ID set recorded before and after, compared, and both recorded in the SUMMARY |
| T-06-140 | Repudiation | `PROH-OPS-04-05` read as discharged because a re-run performed for a different reason happened | medium | mitigate | Task 1 states covered scope and outstanding scope in separate sentences, and an acceptance criterion forbids marking the prerequisite discharged |
| T-06-141 | Denial of Service | An unscoped write to `ROADMAP.md` or `STATE.md` destroying entries for other phases | high | mitigate | Scoped `Edit` only, stated in the action; `grep -c "^### Phase "` before and after is an acceptance criterion |
| T-06-142 | Spoofing | A consolidation sentence asserting a measurement the round did not take | high | mitigate | Every claim about `06-27`'s result must trace to `06-27-SUMMARY.md` or the acceptance artifact; no inferred figure |
| T-06-143 | Tampering | npm/pip/cargo installs | high | accept | No package install occurs; this plan edits four Markdown files and runs the existing suite. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `06-SECURITY.md` cites no test or fixture absent from the tree, and `T-06-24` is re-closed on
  evidence that passes in this plan's own verify command.
- Threat register row count and ID set are non-decreasing across the edit; every range in Task 1's
  registration ledger is complete with no gap and no duplicate — including this plan's own
  `T-06-138`..`T-06-143`, `06-29`'s, `06-30`'s and `06-32`'s ranges, and the newly minted
  recursive-CTE row — while `T-06-158`..`T-06-164` appear exactly once each, as `06-32` left them.
- `T-06-24` and `T-06-101` are verified as still resolved against HEAD on `06-32`'s evidence, not
  re-resolved; `NarrowedShapeConcurrentAccessTests` is recorded as surviving rather than removed.
- Every row describing the SQL reader states that the code is retained with no production caller.
- `grep -c "^### Phase " .planning/ROADMAP.md` is unchanged: no phase entry was lost.
- `06-DEBT.md`'s entry count is strictly greater and no prior heading is absent.
- `git diff --quiet -- .planning/REQUIREMENTS.md` succeeds: OPS-07 stays Pending.
- `git diff -- dashboard/ tests/` is empty: no source changed in this plan.
- Full suite: no NEW failures outside `D-DEBT-06-13`'s known-flaky set.
</verification>

<success_criteria>
The security register describes the build in HEAD — `06-31`'s Python producer on the request path,
the SQL reader retained and unreferenced — rather than any of the three shapes since reverted;
`06-24`'s intent is discharged against this round's change with the supersession record left
coherent; every threat range emitted since round 6 is registered exactly once, with the
recursive-CTE threat added and scoped to unreferenced code; the `(function, ordinal)` re-pinning is
decided rather than deferred a sixth time; and the roadmap, debt register and state file agree with
each other and with what was measured.
</success_criteria>

<output>
Create `.planning/phases/06-workload-resilience-pi-acceptance/06-28-SUMMARY.md` when done.
Record: the threat-register row counts and ID sets before and after, with the add/verify arithmetic
shown and the duplicate check (`sort | wc -l` vs `sort -u | wc -l`); the measured registration ledger
and any divergence from Task 1's table; the per-row citation check results; the definition-grep and
bare-name-grep results for the narrowed-shape pin, with the actual line numbers found and any drift
from `:643`/`:1947`; the confirmation (or failure) of `06-32`'s `T-06-24`/`T-06-101` re-closures; the
scoping sentence quoted from each SQL-reader row; the newly minted recursive-CTE row's ID and its
unreferenced-path sentence; the `### Phase ` count before and after; the `D-DEBT-06-` count before
and after (expected unchanged at 26); the `(function, ordinal)` decision and its addendum text; and
the next action `STATE.md` now points at.
</output>
