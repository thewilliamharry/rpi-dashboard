---
phase: 06-workload-resilience-pi-acceptance
plan: 32
type: execute
wave: 27
depends_on: ["06-31"]
files_modified:
  - .planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-5.md
  - .planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
  - .planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [OPS-07, OPS-01, OPS-02, OPS-03, OPS-04]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`06-PROFILE-5.md`'s pass condition, projection and all outcome branches are on disk and committed BEFORE the measurement runs, in their own commit"
    - "The measurement uses `06-26`/`06-29`'s guard verbatim — same harness, seed 20260902, 8 services, 8 days, 5 repeats, 3 invocations — and states its delta against 56.820ms FIRST"
    - "A result at or above 56.820ms is recorded as a FAILURE with the stop-and-revert branch executed, not rationalised"
    - "`D-DEBT-06-21` carries the round-7 re-refutation of the `service_rollups` premise next to its round-6 original, including the bucket-alignment finding the original did not carry"
    - "`read_uptime_strips_by_port` and `UPTIME_STRIP_QUERY` being unreferenced by production is recorded as debt with the operator's retention rationale, not left to be rediscovered as dead code"
    - "The `06-LOCK-AUDIT.md` pinning recurrence — open in `06-28` since round 6 — is closed with a decision, its fourth realignment recorded and the reason a fifth was avoided stated"
    - "`06-27` and `06-28` are NOT edited; the amendments each needs are enumerated in `06-DEBT.md` so neither is left describing a build that no longer exists"
    - "OPS-07 stays Pending in BOTH halves of `REQUIREMENTS.md`, and this plan states the check that proves the file was not touched"
    - statement: "No threat previously recorded as closed in `06-SECURITY.md` is quietly reopened or dropped: the register's row count and IDs are compared before and after"
      verification: backstop
  artifacts:
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-5.md"
      contains: "pass condition, pre-registered projection, all outcome branches, provenance, the three-invocation table against 56.820ms / 69.191ms / 236.265ms, the attribution of where cost moved, the honesty block, and an explicit verdict"
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md"
      contains: "`D-DEBT-06-21` updated with the round-7 re-refutation, `D-DEBT-06-23` updated with the measured outcome, and new entries for the retained unreferenced reader, the lock-audit pinning decision, and the `06-27`/`06-28` amendment list"
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md"
      contains: "the round-7 threats registered with severities and dispositions, and every citation invalidated by the revert re-closed on the current tree's evidence or reopened"
    - path: ".planning/ROADMAP.md"
      contains: "a round-7 section for Phase 6 describing waves 26-27, the plans, the measured outcome, and the standing supersession of `06-23`/`06-24`"
    - path: ".planning/STATE.md"
      contains: "Current Position, blockers and session continuity updated to the round's real end state, with the OPS-07 blocker rewritten against the round-7 measurement"
  key_links:
    - "`06-31`'s commit -> `06-PROFILE-5.md`: the measurement must run against that exact build, named by SHA, or the number attributes to nothing"
    - "`06-PROFILE-5.md`'s verdict -> `PROH-OPS-07-20` -> `06-27`: only a PASS lifts `06-27`'s local stop condition, and even then `06-27` needs its build SHAs amended before it can run"
    - "the revert -> `06-SECURITY.md`: `06-25`'s and `06-29`'s threats were registered against code that is no longer on the request path, so their dispositions are re-derived rather than left standing"
  prohibitions:
    - id: PROH-OPS-07-26
      requirement: OPS-07
      statement: "A cost fix's pass condition is the baseline it must beat, fixed and written down before the measurement, and never re-based afterwards. `/api/services`' bar is the pre-`06-25` mean of 56.820ms, not `06-25`'s 236.265ms regression or `06-29`'s 69.191ms. A result that beats the later figures but not 56.820ms is a FAILURE of the fix."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-20
      requirement: OPS-07
      statement: "An acceptance run must never be spent on a build whose own cheap local predictor measured worse. `06-27`'s Pi time stays blocked until the local gate reads PASS."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-08
      requirement: OPS-07
      statement: "No plan self-certifies OPS-07. It stays Pending in BOTH halves of `REQUIREMENTS.md` pending an independent verification round on hardware the operator runs."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-09
      requirement: OPS-07
      statement: "Every millisecond measured on a development host is NOT Raspberry Pi latency evidence. The 289.0ms p50 from the Pi control pass at concurrency 1 remains the sole authority on absolute per-request cost."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-19
      requirement: OPS-07
      statement: "A bucket's disappearance from a profile is never reported as a saving until the cost it moved to has been measured."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-21
      requirement: OPS-07
      statement: "A security register entry may never cite evidence not present in the tree it describes."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-27
      requirement: OPS-07
      statement: "An unmet acceptance criterion in an executed plan is recorded against that plan, and the original plan and summary are left unedited so both the specification and the omission stay legible."
      status: canon
      verification: prior-round
    - id: PROH-OPS-07-29
      requirement: OPS-07
      statement: "A remediation premise already recorded as refuted in `06-DEBT.md` may not be re-proposed without re-verifying it against source first, and the re-verification must be written down next to the original refutation."
      status: flagged
      verification: unverified
---

<objective>
Measure `06-31`'s build against the bar it must beat, under a pass condition written before the run,
and put the round on the record so an eighth round cannot start from the same refuted premise.

**The projection, pre-registered here and not derived after the fact (`PROH-OPS-07-26`).** ~37ms,
band 34-42ms, **projected verdict PASS**, against a pass bar of strictly below **56.820ms**. This
projection is unlike rounds 1-6's: it is a *reproduction of a measurement the planner already took on
the exact shipping shape* — a worktree at HEAD with option C's route wiring reverted and the
reduction applied — which measured **36.943ms** mean over three invocations, spread 1.222ms. The same
bench reproduced the `79e051e` baseline at 57.012ms against `06-PROFILE-3.md`'s 56.820ms, a 0.34%
agreement, so the host and harness are consistent with the reference. A result above the band is
therefore news about the executor's implementation diverging from the benched one, not noise, and the
FAIL branch below is written to be executed rather than argued around.

**The record this round owes.** Round 7 was scoped on the claim that `service_rollups` already holds
the strip's 168 hourly buckets. `D-DEBT-06-21` refuted that claim on 2026-09-05, and `06-31`
re-refuted it independently: 0 rows at `bucket_seconds=3600`, 0 rows of any tier inside the window,
because the rollup ladder begins where the strip ends. That a refuted premise was re-proposed anyway,
one round later, is the round's most transferable finding — more so than the milliseconds — and
`PROH-OPS-07-29` exists so the debt register is consulted before a premise is inherited. This plan
also closes the `06-LOCK-AUDIT.md` pinning recurrence that `06-28` has carried as an open decision
since round 6, and enumerates what `06-27` and `06-28` each now need, since the append contract
forbids editing either.

Purpose: convert `06-31`'s change into admissible evidence and a legible record.
Output: `06-PROFILE-5.md`, an updated debt and security register, an honest ROADMAP and STATE.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-4.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-GUARD-DECISION.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-31-SUMMARY.md
</context>

<flagged_assumptions>

## Spec-less probe rows — all 5 surfaced, none resolved

No `06-SPEC.md` exists, so all five probe rows came back `unclassified` and stay **unresolved**.
Neither auto-resolved nor auto-dismissed. No-silent-drop check: 5 probe rows == 5 entries below.

| # | Requirement | Probe status | Assumption, stated so it can be falsified |
|---|---|---|---|
| 1 | OPS-01 | unresolved (unclassified) | Assumed: the profiler builds its own database through `load_app` and starts no worker, so running it cannot perturb metric-sampling or service-check cadence. Basis: `06-PROFILE-3.md` and `06-PROFILE-4.md` both ran this way without a cadence effect. |
| 2 | OPS-02 | unresolved (unclassified) | Assumed: this plan edits only planning artifacts, so preview retry and the degraded state are untouched. Basis: `files_modified` contains no source file. |
| 3 | OPS-03 | unresolved (unclassified) | Assumed: the thumbnail store is untouched, same basis. |
| 4 | OPS-04 | unresolved (unclassified) | Assumed: `06-31`'s revert changed no concurrency boundary, so the security register's concurrency threats need re-citation but not re-derivation. Carried rather than dismissed because Task 2's whole job is to check that citation against the tree rather than assume it. `PROH-OPS-04-02`. |
| 5 | OPS-07 | unresolved (unclassified) | Assumed: a dev-host `wall_ms_unprofiled` delta is directionally informative about Pi-class per-request cost. Explicitly NOT established — `06-27` segment A remains the falsifier, and `PROH-OPS-07-09` forbids treating this plan's milliseconds as Pi evidence. A PASS here lifts `06-27`'s local stop condition; it does not pass OPS-07. |

</flagged_assumptions>

<artifacts_this_phase_produces>

New files this plan creates:

- `.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-5.md`

New records inside existing files: `D-DEBT-06-24`, `D-DEBT-06-25` and `D-DEBT-06-26` in `06-DEBT.md`;
threats `T-06-165` onward in `06-SECURITY.md`; a round-7 section in `.planning/ROADMAP.md`.

No source file is created, renamed or removed. `06-PROFILE.md`, `06-PROFILE-2.md`, `06-PROFILE-3.md`
and `06-PROFILE-4.md` are retained unedited, per this phase's convention of never deleting a
measurement. `06-27-PLAN.md` and `06-28-PLAN.md` are NOT edited, per the append contract.
`06-25-PLAN.md` and `06-25-SUMMARY.md` stay unedited per `PROH-OPS-07-27`.

</artifacts_this_phase_produces>

<reversibility>

| Decision this plan implements | Rating | Rationale |
|---|---|---|
| Record the measurement under a pass condition fixed before the run | `reversible` | An artifact; its discipline is the point, and nothing in the tree depends on it |
| Close the `06-LOCK-AUDIT.md` pinning recurrence with a decision rather than a fourth silent absorption | `costly` | A recorded decision about how a guard is maintained is a standing claim on future rounds; reversing it means re-litigating why line-pinning is acceptable. Flagged, not gated — the alternative is a fifth silent realignment, which `06-28` already named as the failure mode |
| Leave OPS-07 Pending | `reversible` | `PROH-OPS-07-08`; no code or requirement state changes |

No `one-way` decision is implemented, so no `checkpoint:decision` is inserted. The one-way decision
this round turns on was taken at `06-31` Task 1.

</reversibility>

<tasks>

<task type="auto">
  <name>Task 1: Write the pass condition and the projection, commit them, and only then measure</name>

  <files>.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-5.md</files>

  <precondition>`06-31`'s production commit is HEAD, its SHA is known, and `uv run --project dashboard python -m pytest -q` is green — a cost number taken against a red suite is inadmissible.</precondition>

  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-4.md` in full — the structure
      this report follows: pass condition, pre-registered projection, outcome branches, provenance,
      headline table, attribution table, honesty block, "what this report may not conclude", stop
      condition. Note that its skeleton was committed before its measurement in commit `0b06e24`, and
      that this task repeats that discipline rather than reinventing it.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-3.md` § headline — the
      56.820ms pre-`06-25` reference and the 236.265ms `06-25` reference, both at seed 20260902,
      8 services, 8 days, 5 repeats, 3 invocations.
    - `tests/services_route_profile.py` lines 60-80 — `PI_CONTROL_PASS_P50_MS` and `HONESTY_CAVEAT`,
      the text the honesty block must carry.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-31-SUMMARY.md` — the shipping commit
      SHA and the observed mutation results, both of which this report cites.
  </read_first>

  <action>
    **Step one, in its own commit, before running anything.** Write `06-PROFILE-5.md` containing the
    pass condition, the projection, and all three outcome branches fully worded, with the measurement
    sections left as empty placeholders. Commit it. State in the file that it was committed before
    the run and name the commit, exactly as `06-PROFILE-4.md` names `0b06e24`. Six rounds of this
    phase produced numbers whose interpretation was settled after they arrived; this ordering is the
    countermeasure and it is not optional.

    The pass condition: mean `wall_ms_unprofiled` across three invocations strictly below
    **56.820ms**, the pre-`06-25` baseline measured on this host at this seed at this shape. Not
    below 236.265ms, and not below 69.191ms. A result that beats those but not 56.820ms is a FAILURE
    (`PROH-OPS-07-26`).

    The projection: **~37ms, band 34-42ms, projected verdict PASS**, sourced to the planner's
    pre-planning bench of this exact shape at 36.943ms mean over three invocations, spread 1.222ms,
    with the `79e051e` baseline reproduced at 57.012ms against 56.820ms in the same bench. State
    plainly that this is a reproduction rather than an extrapolation, and that the band is narrow for
    that reason — so a result outside it is evidence the executor's implementation differs from the
    benched one.

    The three branches, worded before the run:

    **PASS** — mean strictly below 56.820ms. The reduction closes `06-25`'s regression AND beats the
    pre-`06-25` baseline, which no round of this phase has previously done. The projection is
    confirmed. `06-27`'s local stop condition under `PROH-OPS-07-20` is lifted; `06-27` still cannot
    run as written, because its build SHAs describe a build that no longer exists — Task 3 enumerates
    that amendment. OPS-07 is NOT promoted (`PROH-OPS-07-08`).

    **PASS-PROJECTION-REFUTED** — mean strictly below 56.820ms but outside the 34-42ms band. The fix
    passes and the planner's projection is REFUTED; record the refutation as a refutation, in its own
    sentence, and attribute the gap before treating the pass as settled. `PROH-OPS-07-19`: do not
    report a bucket's disappearance as a saving until the cost it moved to has been measured.

    **FAIL** — mean at or above 56.820ms. This is a failure of the fix, stated as the headline
    sentence, whatever it beats. Execute the stop-and-revert branch: revert `06-31`'s production
    commit, confirm the suite returns green at the reverted state, record the revert and the measured
    number in this report and in `06-DEBT.md`, and leave `06-27` blocked under `PROH-OPS-07-20`.
    Do not adjust the bar, the harness, the seed, the shape or the repeat count in response
    (`PROH-OPS-07-01`, `PROH-OPS-07-10`, `PROH-OPS-07-26`). Do not open a fourth branch for a
    near-miss.

    **Step two, after that commit exists.** Run the guard verbatim as `06-26` and `06-29` ran it —
    `tests/services_route_profile.py`, seed 20260902, `--services 8 --days 8 --repeats 5
    --min-attributed 0`, three invocations back to back on one host — and fill in the placeholders.
    Report the delta against 56.820ms FIRST and the deltas against 69.191ms and 236.265ms after it,
    subordinate. Include the per-bucket attribution table comparing this run's `tottime_ms` against
    `06-PROFILE-4.md`'s, and attribute where `uptime_sweep`'s cost went rather than reporting only
    that it fell (`PROH-OPS-07-19`); the planner's bench measured `uptime_sweep` at 236.666ms
    tottime before and 12.291ms after, a 19.3x fall, with every other bucket flat within noise, so a
    materially different attribution is itself a finding. Carry the honesty block verbatim from
    `06-PROFILE-4.md`'s form: every millisecond here is dev-host, none of it is Pi latency evidence,
    and the 289.0ms Pi control-pass p50 remains the sole authority on absolute cost
    (`PROH-OPS-07-09`). Carry a "what this report may not conclude" section saying nothing about Pi
    latency and nothing about whether OPS-07 passes.
  </action>

  <verify>
    <automated>uv run --project dashboard python -m pytest -q</automated>
    <automated>git log --oneline -- .planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE-5.md</automated>
    <automated>git status --short .planning/REQUIREMENTS.md tests/ dashboard/</automated>
  </verify>

  <acceptance_criteria>
    - `git log` for `06-PROFILE-5.md` shows at least two commits, the earliest of which predates the
      measurement and contains the pass condition, the projection and all three branches — verifiable
      by `git show <first-sha>:...06-PROFILE-5.md` containing the projection and no measured mean.
    - The verdict section states the delta against 56.820ms before any other comparison appears.
    - The report names the build SHA measured, the host, the seed, the shape, the repeat count and
      the invocation count, and reports `attributed_pct` for each invocation.
    - The third verify command prints nothing: `.planning/REQUIREMENTS.md`, `tests/` and `dashboard/`
      are untouched by this task (`PROH-OPS-07-08`). On the FAIL branch this criterion is read
      against the revert commit instead, and the revert is recorded rather than hidden.
    - The honesty block and the "what this report may not conclude" section are both present.
  </acceptance_criteria>

  <done>`06-PROFILE-5.md` records a verdict against a pass condition that was on disk and committed before the number existed, with the delta against 56.820ms stated first and the cost movement attributed.</done>

  <reversibility rating="reversible">An artifact. On the FAIL branch the accompanying code revert is a single `git revert`.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The debt record — re-refute the premise next to its original, and close the pinning recurrence</name>

  <files>.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md</files>

  <precondition>Task 1 is complete and `06-PROFILE-5.md` carries a verdict.</precondition>

  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` — `D-DEBT-06-21` in full
      (its round-6 refutation of the `service_rollups` premise, its four-option table, and the
      round-6 addendum recording option C's measured outcome), plus `D-DEBT-06-22` and
      `D-DEBT-06-23`. Confirm the highest existing entry number before minting new ones.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-GUARD-DECISION.md` § 7 — the
      enumerated amendments `06-27` and `06-28` each need, which this task extends rather than
      restates.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-31-SUMMARY.md` — the probe figures
      and the mutation results this task cites.
  </read_first>

  <action>
    **Update `D-DEBT-06-21` in place, appending rather than rewriting**, with a round-7 section. It
    must record three things. First, that the entry's own refutation was independently reproduced in
    round 7: driving the worker's `run_retention_batch` to convergence on the profiled 8-service
    8-day shape rolled 2,304 buckets across 73 batches and left `service_rollups` with 0 rows at
    `bucket_seconds=3600`, 2,304 rows at `bucket_seconds=300` all aged 7.00-8.00 days, and 0 rows of
    any tier inside the strip window, against 22,247 `service_checks` rows covering that window
    exactly. Second, a finding this entry did NOT previously carry: because `UPTIME_WINDOW_SECONDS`
    is exactly `UPTIME_BUCKETS * 3600`, the sliding origin puts every rendered boundary at offset
    `now % 3600` from the epoch-hour grid, so 168 of 168 buckets straddle an epoch hour and
    apportioning hour-aligned totals changed 150-157 of the 168 rendered values with a worst absolute
    error of 0.461 — meaning option A could not render this strip even if the tier were populated,
    which strengthens this entry's rejection from "not currently available" to "not reconstructible".
    Third, that the entry's status changes to reflect the round-7 remedy actually taken and its
    measured result from `06-PROFILE-5.md`.

    **Update `D-DEBT-06-23`** with the round-7 outcome. Its finding — that the SQL formulation's cost
    floor already equalled the Python sweep it replaced — stands and is now sharper: the Python sweep
    was never the irreducible cost, it was the cost of sweeping 25,278 points whose information
    content was 72. Record that the sweep's cost was reduced 19.3x without touching the sweep itself,
    and that this reframes the entry's conclusion from "per-request computation cannot reach the
    budget" to "per-request computation over the raw point stream cannot reach the budget". State
    which parts of the entry that supersedes and which stand.

    **Mint `D-DEBT-06-24` — the retained unreferenced reader.** `read_uptime_strips_by_port`,
    `UPTIME_STRIP_QUERY` and their guards remain in the tree and are no longer called by production.
    Record the operator's rationale from `06-GUARD-DECISION.md` §8 verbatim (a proven-correct
    implementation and the evidence base for the next round, not throwaway work), what would need to
    be true to delete them, and the risk of leaving them: a later reader mistaking them for live
    code. Name the guards that keep passing over them so their maintenance cost is visible.

    **Mint `D-DEBT-06-25` — close the `06-LOCK-AUDIT.md` pinning recurrence.** `06-28` has carried
    this as an open decision since round 6 and it has now cost a fourth realignment. Record both
    options `06-28` framed, the decision taken, and the specific reason this round paid the cost once
    rather than twice: the revert and the reduction landed as a single commit, so the producer name
    in `LockScopePreservationTests` and the line numbers in rows 20-28 each moved once. State the net
    line delta actually applied and that rows 1-19 were unaffected because the edit sits below
    `api_services`' `with _db_lock` at line 2875.

    **Mint `D-DEBT-06-26` — the amendments `06-27` and `06-28` now need.** Neither plan is edited;
    the append contract forbids it, and this entry is the only way neither is left describing a build
    that no longer exists. Extend `06-GUARD-DECISION.md` §7's list rather than restating it. For
    `06-27`: its segment A "after" build must name `06-31`'s commit rather than `06-29`'s `8a84139`
    or `06-25`'s `9da5e5e`; its decision gate must reference `06-PROFILE-5.md`'s verdict rather than
    re-deriving a comparison; and its `PROH-OPS-07-20` stop condition resolves according to
    `06-PROFILE-5.md` — on PASS the block lifts, on FAIL it stands, and Task 1's verdict is the
    authority either way. For `06-28`: its enumerated threat list gains the round-7 threats, its
    fourth threat about the recursive CTE's data-dependent intermediate row count now describes code
    that is no longer on the request path and must be re-scoped accordingly, and its
    `PROH-OPS-07-21` obligation now extends to any `06-SECURITY.md` entry citing `06-25`'s or
    `06-29`'s route wiring.

    Do not edit `06-27-PLAN.md`, `06-28-PLAN.md`, `06-25-PLAN.md` or `06-25-SUMMARY.md`
    (`PROH-OPS-07-27` and the append contract). Do not delete or rewrite any existing debt entry;
    this file's convention is to append and annotate.
  </action>

  <verify>
    <automated>git status --short .planning/phases/06-workload-resilience-pi-acceptance/06-27-PLAN.md .planning/phases/06-workload-resilience-pi-acceptance/06-28-PLAN.md .planning/phases/06-workload-resilience-pi-acceptance/06-25-PLAN.md .planning/phases/06-workload-resilience-pi-acceptance/06-25-SUMMARY.md</automated>
    <automated>uv run --project dashboard python -m pytest -q tests/test_lock_profile.py</automated>
  </verify>

  <acceptance_criteria>
    - The first verify command prints nothing: none of the four protected plan or summary files was
      modified.
    - `grep -c "^### D-DEBT-06-" 06-DEBT.md` returns three more than it did before this task, and
      `D-DEBT-06-24`, `D-DEBT-06-25` and `D-DEBT-06-26` each exist exactly once.
    - `D-DEBT-06-21`'s round-6 text is still present unchanged; the round-7 material is additive and
      dated, verifiable with `git diff` showing only insertions in that entry's region.
    - `D-DEBT-06-26` names both `06-27` and `06-28` and, for each, at least the amendments listed in
      this task's action plus those already in `06-GUARD-DECISION.md` §7.
    - `test_every_db_lock_site_is_covered_by_the_audit` still passes, proving this task's edits to
      `06-DEBT.md` did not disturb `06-LOCK-AUDIT.md`'s table.
  </acceptance_criteria>

  <done>The refuted premise carries its round-7 re-refutation and the alignment finding next to the round-6 original; the pinning recurrence is closed with a decision; and both blocked plans have their amendments enumerated without either being edited.</done>

  <reversibility rating="costly">Closing the pinning recurrence is a standing claim on how a guard is maintained; the rest is additive record.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The security register, the roadmap and the state — described against the tree that exists</name>

  <files>.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md, .planning/ROADMAP.md, .planning/STATE.md</files>

  <precondition>Tasks 1 and 2 are complete.</precondition>

  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md` — the full register:
      its row count, its ID range, and specifically every entry whose evidence cites `06-25`'s or
      `06-29`'s route wiring, `T-06-24` and `T-06-101`, and the audit-trail section's format.
    - `.planning/ROADMAP.md` § Phase 6 — the Plans block, its per-round narration style, the
      criterion-5 amendment note, and the plan checklist that `06-31` and `06-32` must join.
    - `.planning/STATE.md` — the Current Position section, the OPS-07 blocker paragraph written for
      round 6, and the Session Continuity block.
  </read_first>

  <action>
    **`06-SECURITY.md`.** Record the register's row count and ID set before editing, and again after,
    and state both in `SUMMARY.md` — no threat previously closed may be quietly reopened or dropped.
    Register the round-7 threats `06-31` declared (`T-06-158` through `T-06-164`) with their
    severities and dispositions, continuing the existing `T-06-1xx` numbering from the current
    maximum rather than reusing an ID. Then re-check every entry whose closing evidence cites code
    the revert removed from the request path: `06-25`'s dynamic `IN` list, its second producer of the
    rendered contract, its uncapped aggregation, and `06-29`'s recursive-CTE row-count bound. Those
    threats are not simply closed by the revert — the code is still in the tree, merely unreferenced
    by production — so each must be re-closed on the current tree's evidence with its reachability
    stated, or reopened, never left citing a request path that no longer exists (`PROH-OPS-07-21`).
    Re-check `T-06-24` and `T-06-101` on the same basis, since `06-31` rewrote the containment pin
    they cite.

    **`.planning/ROADMAP.md`.** Add a round-7 section to Phase 6 in the existing narration style:
    waves 26-27, the two plans, why the round's scoped remedy was rejected and on what measured
    evidence, what `06-PROFILE-5.md` returned against the 56.820ms bar, and that `06-23`/`06-24`
    remain superseded. Update the Plans line's executed count and append `06-31` and `06-32` to the
    plan checklist with one-line objectives. Leave the criterion-5 amendment note and every existing
    entry untouched — edit only the sections named here, and never rewrite the file wholesale.

    **`.planning/STATE.md`.** Rewrite the OPS-07 blocker paragraph against round 7's actual end
    state: what the round measured, that the `service_rollups` remedy is refuted for the second time
    with the alignment finding added, and what the next step is — which on a PASS is `06-27` after
    its SHA amendment, and on a FAIL is a fresh decision. Update Current Position and Session
    Continuity. Add the round's transferable decisions to the decisions list, including that a
    premise recorded as refuted in the debt register must be re-verified before it is inherited
    (`PROH-OPS-07-29`), and that a differential can prove a reduction correct but never prove it
    present (`PROH-OPS-07-28`).

    **OPS-07 stays Pending.** Do not touch `.planning/REQUIREMENTS.md`. `PROH-OPS-07-08` scopes
    promotion to an independent verification round on hardware the operator runs, and no plan in this
    round is that round. State the check that proves the file was not touched.
  </action>

  <verify>
    <automated>git status --short .planning/REQUIREMENTS.md</automated>
    <automated>uv run --project dashboard python -m pytest -q</automated>
    <automated>git diff --stat .planning/ROADMAP.md .planning/STATE.md .planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md</automated>
  </verify>

  <acceptance_criteria>
    - The first verify command prints nothing: `.planning/REQUIREMENTS.md` is unedited and OPS-07 is
      Pending in both halves (`PROH-OPS-07-08`).
    - `06-SECURITY.md`'s row count after the edit equals the before count plus exactly the number of
      threats added, with no ID removed; both counts and both ID sets are stated in `SUMMARY.md`.
    - Every threat added carries a severity from critical/high/medium/low and a disposition from
      mitigate/accept/transfer.
    - No `06-SECURITY.md` entry cites `06-25`'s or `06-29`'s route wiring as live request-path
      evidence; each such citation is re-closed on the current tree or reopened, and the entries
      touched are listed in `SUMMARY.md`.
    - `.planning/ROADMAP.md`'s diff is confined to the Phase 6 Plans block, its round narration and
      the plan checklist — the criterion-5 amendment note and all other phases are unchanged, shown
      by the third verify command's line counts being consistent with a scoped edit rather than a
      rewrite.
    - The full suite is green, proving the planning-artifact edits did not break the tests that read
      `06-LOCK-AUDIT.md` and the golden fixtures.
  </acceptance_criteria>

  <done>The security register describes the tree that exists, the roadmap and state describe the round that happened, and OPS-07 is still Pending with the proof that nothing promoted it.</done>

  <reversibility rating="reversible">Planning artifacts only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a measured number -> a future round's decision | The integrity risk is misattribution and post-hoc reinterpretation, not compromise |
| a security register entry -> the tree it describes | An entry citing evidence absent from the tree records a threat as closed on nothing |
| a refuted premise -> a future round's scoping | Round 7 was scoped on a premise round 6 had already refuted; the register is the control that failed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-165 | Repudiation | `06-PROFILE-5.md`'s number re-based against 69.191ms or 236.265ms to manufacture a pass | high | mitigate | `PROH-OPS-07-26`. Pass bar, projection and all three branch wordings are committed before the measurement, in their own commit, and an acceptance criterion requires the 56.820ms delta to appear before any other comparison |
| T-06-166 | Tampering | A near-miss result argued into a fourth branch rather than executing the stop-and-revert | high | mitigate | Only three branches are worded, the FAIL branch names the revert as an action rather than a recommendation, and `PROH-OPS-07-01`/`-10` forbid touching the bar, harness, seed or shape in response |
| T-06-167 | Information Disclosure | A `06-SECURITY.md` entry left citing `06-25`/`06-29` route wiring as live evidence | high | mitigate | `PROH-OPS-07-21`. Task 3 re-checks every such citation against the current tree, and the before/after row counts and ID sets are compared and recorded |
| T-06-168 | Repudiation | An eighth round re-proposing the `service_rollups` remedy for a third time | medium | mitigate | `PROH-OPS-07-29`. `D-DEBT-06-21` gains the round-7 re-refutation plus the alignment finding, which upgrades the rejection from "not currently populated" to "not reconstructible at any population" |
| T-06-169 | Information Disclosure | `read_uptime_strips_by_port` being read later as live production code | medium | mitigate | `D-DEBT-06-24` records it as deliberately retained, unreferenced by production, with the operator's rationale and the deletion condition |
| T-06-170 | Elevation of Privilege | OPS-07 promoted by a plan in its own round | high | mitigate | `PROH-OPS-07-08`. `.planning/REQUIREMENTS.md` is outside `files_modified` for both round-7 plans, and an acceptance criterion in each requires an empty `git status` on it |
| T-06-171 | Tampering | npm/pip/cargo installs | high | accept | No package install occurs. `06-RESEARCH.md` § Standard Stack: "Installation: None. No new packages this phase." No `[ASSUMED]`/`[SUS]` entry exists, so no legitimacy checkpoint is required |
</threat_model>

<verification>
- `06-PROFILE-5.md`'s pass condition and projection exist in a commit that predates the measurement.
- The verdict states its delta against 56.820ms first.
- `.planning/REQUIREMENTS.md` is unedited; OPS-07 is Pending in both halves.
- `06-27-PLAN.md`, `06-28-PLAN.md`, `06-25-PLAN.md` and `06-25-SUMMARY.md` are unedited.
- `06-SECURITY.md`'s row count and ID set are stated before and after, and no ID was dropped.
- `uv run --project dashboard python -m pytest -q` is green.
</verification>

<success_criteria>
- The round's cost claim is recorded as evidence taken under a pre-registered pass condition, with
  the FAIL branch executed if it applies.
- The refuted premise carries its second, independent refutation next to the first, plus the
  alignment finding that makes the rejection structural.
- The `06-LOCK-AUDIT.md` pinning recurrence is closed rather than absorbed a fourth time.
- `06-27` and `06-28` have their amendments enumerated without either plan being edited.
- OPS-07 remains Pending, awaiting an independent verification round on the operator's hardware.
</success_criteria>

<output>
Create `.planning/phases/06-workload-resilience-pi-acceptance/06-32-SUMMARY.md` when done.
</output>
