---
phase: 01-behavioral-safety-runtime-ownership
plan: 08
type: execute
wave: 6
depends_on:
  - 01-05
  - 01-06
  - 01-07
files_modified:
  - dashboard/index.html
  - dashboard/app.js
  - dashboard/style.css
  - dashboard/beacon/web.py
  - tests/test_ui_contract.py
  - tests/test_ui_states.py
autonomous: true
requirements:
  - FND-01
  - FND-04
  - FND-07
  - OPS-05
threat_model:
  asvs_level: 1
  block_on: high
  completion_invariant: "Plan completion is blocked while any applicable high-severity threat is unresolved or its linked mitigation evidence is missing."
  register_location: "<threat_model> STRIDE Threat Register"
  applicable_threats:
    - "T-01-37 | severity=high | disposition=mitigate"
    - "T-01-38 | severity=high | disposition=mitigate"
    - "T-01-39 | severity=medium | disposition=mitigate"
    - "T-01-40 | severity=medium | disposition=mitigate"
    - "T-01-41 | severity=low | disposition=accept"
  asvs_l1_controls:
    - "V4 Access Control and V5 Validation, Sanitization and Encoding -> T-01-37, T-01-38"
    - "V7 Error Handling and Logging and V8 Data Protection -> T-01-39, T-01-40, T-01-41"
  mitigation_task_links:
    - "T-01-37, T-01-38 -> Task 1 Render ordered monitoring, recovery, scan, and preview states; Task 2 Show TLS posture and prove all UI consideration states in both themes"
    - "T-01-39 through T-01-41 -> Task 1 Render ordered monitoring, recovery, scan, and preview states; Task 2 Show TLS posture and prove all UI consideration states in both themes"
  verification_evidence:
    - "T-01-37, T-01-38 -> pytest tests/test_ui_states.py tests/test_ui_contract.py"
    - "T-01-39 through T-01-41 -> pytest tests/test_ui_states.py tests/test_api_and_auth.py tests/test_runtime_ownership.py"
must_haves:
  truths:
    - "UI consideration—empty: no services shows `No HTTP services discovered` plus `Run a scan to look for configured services.`; missing previews keep the labelled fallback; no events shows `no recent incidents`."
    - "UI consideration—loading: initial status says `connecting`/`waiting for worker`; scan/preview work uses explicit queued/running copy; metadata Save disables only while submitting and preserves the modal."
    - "UI consideration—error: browser disconnect, worker stale, recovery required, blocked destination, metadata save failure, preview failure, and request expiry have distinct text, warning treatment, and recovery guidance."
    - "UI consideration—populated: service cards retain availability, latency, uptime, metadata, and independent TLS/preview state; Events renders `Monitoring gap recorded` with `Worker unavailable for {duration}.`."
    - "UI consideration—partial: last readable data remains visible with stale/recovery warning; `TLS unverified` never replaces ONLINE/OFFLINE; recovered monitoring creates a bounded gap event."
    - "UI consideration—overflow: warning copy wraps inside the page inset; service names/tags/event detail use existing ellipsis; `TLS unverified` may wrap without obscuring title or `Edit service`."
    - statement: "UI consideration—zero-one-many: visual UI-state tests verify zero, one, and several services/events preserve readable cards, labels, controls, and grid spacing in dark and light modes."
      verification: backstop
    - "UI consideration—long-text: service names/tags/event detail truncate with ellipsis; safe errors wrap in the modal and never expose raw internal target data."
    - "D-12: every service using the trusted-LAN TLS exception has a persistent non-interactive `TLS unverified` badge whether online or offline, without changing uptime or card availability styling."
    - "D-13 through D-16: stale monitoring leaves the whole page interactive; queued work is candid, metadata saves immediately, expiry is visible, recovery clears warnings, and the persisted gap appears in Events."
  artifacts:
    - path: "tests/test_ui_states.py"
      provides: "Playwright-backed light/dark, narrow/desktop, zero/one/many safety-state coverage"
    - path: "dashboard/index.html"
      provides: "Stable ordered safety-warning cluster and accessible status regions"
    - path: "dashboard/app.js"
      provides: "Durable queue/recovery/TLS/error state rendering and focus/announcement behavior"
    - path: "dashboard/style.css"
      provides: "Theme-parity warning, queue, expiry, TLS posture, overflow, and narrow-modal styles"
  key_links:
    - from: "dashboard/app.js"
      to: "dashboard/beacon/web.py"
      via: "existing scan/services/events/metadata polling responses with additive durable state"
      pattern: "api/scan-status|api/services|api/events|api/service-meta"
    - from: "dashboard/app.js"
      to: "dashboard/index.html"
      via: "stable IDs for connection, worker, recovery, feedback, scan, services, events, and metadata modal regions"
      pattern: "safety-warning-cluster|page-feedback"
    - from: "tests/test_ui_states.py"
      to: "dashboard/style.css"
      via: "computed layout/contrast/visibility checks in both themes and supported widths"
      pattern: "light|720"
  prohibitions:
    - statement: "Safety UI must not imply queued work has already run, retry expired work automatically, hide readable data during monitoring failure, or offer an in-browser override for blocked outbound/recovery policy."
      status: unverified
      flagged: true
      note: "Descriptor-less prohibition; carry as an unresolved judgment item through verification."
---

<objective>
Complete the narrow Phase 1 browser experience for stale monitoring, durable queue states, migration recovery, safe outbound failures, and trusted-LAN TLS posture in both themes.

Purpose: Make the new runtime/safety behavior truthful and usable without redesigning the dashboard or entering advanced-analytics scope.
Output: Ordered accessible safety warnings, queue/expiry state, metadata-outage flow, TLS posture badge, safe errors, and automated UI-state coverage.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-CONTEXT.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-RESEARCH.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-UI-SPEC.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-05-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-06-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-07-SUMMARY.md

<interfaces>
Existing DOM/API contracts:
- `.btn-scan`, `#scan-pip`, `#scan-label`, `#connection-banner`, `#services-grid`, `#events-panel`, `#meta-modal`, `#meta-error`, and the page-level feedback region.
- Additive API fields from prior plans: worker freshness/recovery, durable scan status/deadline, preview status/revision/deadline, service `tls_unverified`, and safe outbound reason/message.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render ordered monitoring, recovery, scan, and preview states</name>
  <reversibility rating="reversible">The safety presentation consumes additive API state and existing DOM regions; copy/layout can change without persistence migration.</reversibility>
  <read_first>
    - dashboard/index.html (topbar, connection banner, services/events panels, metadata modal, feedback region)
    - dashboard/app.js (`updateScanStatus`, service/event rendering, metadata submit, polling, focus return)
    - dashboard/style.css (theme variables, banner, scan, service, event, modal, and 720px rules)
    - dashboard/beacon/web.py (durable status/error payloads from Plans 05-07)
    - tests/test_ui_contract.py (existing dependency-free UI source contracts)
    - .planning/phases/01-behavioral-safety-runtime-ownership/01-UI-SPEC.md (exact copy, hierarchy, colors, spacing, accessibility, and scope)
  </read_first>
  <files>dashboard/index.html, dashboard/app.js, dashboard/style.css, dashboard/beacon/web.py, tests/test_ui_states.py</files>
  <behavior>
    - Test 1: connection, worker-stale, and recovery-required warnings render in locked order and leave all existing controls/loaded data interactive.
    - Test 2: scan shows exact queued/running/completed/expired copy and disables only during HTTP submission or active running state, not while queued/expired.
    - Test 3: per-service preview state shows queued/running/failed/expired independently from ONLINE/OFFLINE and saved metadata.
    - Test 4: stale metadata edit warns before submit, saves/refreshes the card immediately, returns focus, and announces the exact queued/non-queued success copy.
    - Test 5: worker stale→fresh removes warning, announces recovery once, and shows the persisted monitoring-gap event.
  </behavior>
  <action>
    Stabilize one full-width `#safety-warning-cluster` immediately below the topbar and above dashboard content. Preserve the exact internal order: existing connection alert first, worker-stale warning second, migration/recovery warning third. Use the locked copies from UI-SPEC. Worker warning is polite live status; recovery warning is non-dismissable. Keep system metrics, scan, service cards, events, links, metadata editor, and theme toggle available with last readable data.

    Render durable scan states exactly: `Scan queued — runs when monitoring resumes`; `{stage} {percent}% · {found} found`; `{found} found · {relative time}`; `Scan request expired — it was not run. Scan again.` Disable `.btn-scan` only while POST submission is in flight or one scan is `running`; queued and expired states remain enabled. Never automatically retry expired work.

    Add the non-interactive per-service preview line beneath details/tags with exact applicable copy: `Preview refresh queued`, `Refreshing preview`, `Preview refresh failed — saved settings are unaffected`, or `Preview refresh expired — save service details to request a new preview.` Keep it independent from availability.

    In the metadata modal, when worker stale, show `Monitoring is paused. Your service details will be saved now; preview refresh will run after recovery.` above actions. On save update the card before close, return focus to the service's edit control, and announce `Service details saved. Preview refresh queued.` or `Service details saved.`. On worker recovery announce `Monitoring resumed. The outage was recorded in Events.` once and render event title `Monitoring gap recorded` plus secondary `Worker unavailable for {duration}.`.

    Extend `beacon.web` only where a missing additive status field prevents these exact states; do not change route names or remove compatibility fields. Style warnings/queue states with the declared theme colors, existing variables, text labels, 4.5:1 contrast, wrapping, and no hover-only information.
  </action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_ui_states.py -k "warning or scan or preview or metadata or recovery" -x</automated>
  </verify>
  <acceptance_criteria>
    - Warning order and every locked copy string match UI-SPEC exactly.
    - Stale/recovery warnings do not overlay/hide content or disable links, editor, scan, previews, or theme controls.
    - Scan queued is enabled; submission/running is disabled; expired is enabled and never auto-retried.
    - Preview state never changes ONLINE/OFFLINE, card availability color, or uptime display.
    - Metadata save remains immediate under stale worker, preserves modal input on errors, updates the card, returns focus, and announces through polite feedback.
    - Recovery announcement occurs once per stale→fresh transition and Events contains the persisted bounded gap.
  </acceptance_criteria>
  <done>The operator can distinguish browser, worker, recovery, scan, and preview states and continue local work with candid queue/recovery feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Show TLS posture and prove all UI consideration states in both themes</name>
  <reversibility rating="reversible">TLS and error presentation is additive browser markup/style backed by persisted policy state.</reversibility>
  <read_first>
    - dashboard/app.js (service title/action row, edit button, metadata errors, empty/event rendering)
    - dashboard/style.css (service badges/actions, light/dark service cards, overflow, modal errors, narrow rules)
    - dashboard/index.html (empty placeholders, modal labels/actions)
    - dashboard/beacon/web.py (service `tls_unverified` and safe error payload)
    - tests/test_ui_contract.py (existing source assertions)
    - tests/test_ui_states.py (harness created in Task 1)
    - .planning/phases/01-behavioral-safety-runtime-ownership/01-UI-SPEC.md (`UI Considerations`, TLS badge, copy advisory, and narrow scope)
  </read_first>
  <files>dashboard/app.js, dashboard/style.css, tests/test_ui_contract.py, tests/test_ui_states.py</files>
  <behavior>
    - Test 1: `tls_unverified=true` renders outlined `TLS unverified` after the port badge and before `Edit service`, online and offline, with the exact accessible title.
    - Test 2: blocked target/TLS/save errors keep modal values, focus inline error, show only the safe exact message plus retry guidance, and offer no override.
    - Test 3: zero, one, and several services/events remain readable at desktop and 720px in dark/light themes with all text/control/status states visible.
    - Test 4: long names/tags/events ellipsize, warning/error copy wraps, TLS badge wraps without covering controls, and narrow modal actions are full-width and at least 44px.
    - Test 5: availability uses ONLINE/OFFLINE text plus color; warning/TLS states use text plus warning color and meet 4.5:1 contrast.
  </behavior>
  <action>
    In the service title/action row, render a persistent outlined `TLS unverified` badge after the port badge and before the edit control whenever `service.tls_unverified` is true. Set its accessible title to `TLS certificate is not verified for this trusted local service.` Keep it non-interactive and visible online/offline without red card styling or uptime changes. Because this task touches the existing edit control, change its visible/accessible copy to the UI advisory `Edit service`.

    Map blocked URL, redirect, DNS, webhook, and TLS-policy errors to `Beacon could not use that destination. Review the service details and try again.` Keep the modal open, preserve inputs, focus the inline error, and provide no override/proceed action. Ensure raw URL/IP/certificate/redirect/stack data is absent from DOM text/title/data attributes.

    Complete empty/populated/partial/overflow/long-text states using the exact UI-SPEC rules. Preserve the dashboard information architecture, theme toggle, compact analytics, service cards, event panel, system sans/mono fonts, existing 1px borders/state pips, and light/dark density. Do not add a UI framework, font/icon download, route, sidebar, advanced-workspace navigation, backup list, restore selector, backup deletion, or migration retry control.

    Build `tests/test_ui_states.py` as a bounded Playwright-backed Flask/temporary-SQLite test: seed zero, one, and several services/events; exercise dark/light at desktop and 720px; assert computed visibility, order, bounding boxes, wrapping/ellipsis, 44px narrow actions, focus movement, status text, and contrast. Use deterministic local fixtures and close browser/server/database resources in `finally`.
  </action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_ui_states.py tests/test_ui_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Badge copy/order/title match UI-SPEC and remain visible for online/offline services without affecting availability.
    - Existing edit control reads `Edit service`.
    - Safe failure copy is exact; no override control or raw internal target/certificate/redirect/stack detail reaches the DOM.
    - Zero/one/many service and event cases pass in both themes at desktop and 720px.
    - Narrow modal actions are full-width and at least 44px; no safety state depends on hover or color alone.
    - No advanced analytics navigation, broad dashboard redesign, dependency, destructive recovery control, or backup-management UI is introduced.
  </acceptance_criteria>
  <done>Trusted-LAN TLS posture and every covered/backstop UI state are accessible, truthful, theme-parity, responsive, and confined to Phase 1 safety scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API state/errors → browser DOM | Persisted operational/network state becomes operator-visible text, attributes, and controls. |
| browser mutation → safe API feedback | Invalid service targets and metadata errors must not leak internal details or offer bypass. |
| recovery state → browser | A destructive operational condition is shown read-only without creating a browser action path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-37 | Information disclosure | error rendering | high | mitigate | Exact safe copy; DOM tests reject raw URL/IP/certificate/redirect/stack content and data attributes. |
| T-01-38 | Tampering / Elevation of privilege | policy/recovery UI | high | mitigate | No override, restore, backup deletion, migration retry, or destination bypass controls in browser. |
| T-01-39 | Spoofing | worker/queue/TLS status | medium | mitigate | Render server-persisted state with explicit labels; keep connection, freshness, availability, trust posture, and queue status separate. |
| T-01-40 | Repudiation | recovery/gap feedback | medium | mitigate | UI references the persisted monitoring-gap event and catalog-backed offline recovery path. |
| T-01-41 | Denial of service | long/overflow UI content | low | accept | Bounded API fields plus wrapping/ellipsis and layout tests keep the trusted-LAN dashboard usable. |
</threat_model>

<verification>
- Run `dashboard/.venv/bin/python -m pytest -q tests/test_ui_states.py tests/test_ui_contract.py tests/test_api_and_auth.py tests/test_runtime_ownership.py tests/test_durable_queues.py tests/test_outbound_policy.py`.
- Run `dashboard/.venv/bin/python -m pytest -q`.
- Confirm the backstop zero/one/many test produces explicit evidence for both themes and both target widths.
</verification>

<success_criteria>
- All seven covered UI considerations and the zero/one/many backstop are represented and automated.
- Monitoring/recovery/queue/TLS states are separate, candid, accessible, and non-blocking.
- Metadata saves and focus/feedback remain correct during worker outage and safe failures.
- Both themes preserve existing information architecture and expose identical Phase 1 safety information.
- No advanced-analytics redesign or destructive browser recovery surface appears.
</success_criteria>

## Artifacts this phase produces

- `tests/test_ui_states.py` — `UiStateTests` Playwright/Flask harness for light/dark, desktop/720px, zero/one/many, warning order, focus, wrapping, contrast, and safe-error assertions.
- `dashboard/index.html` — stable `#safety-warning-cluster` and ordered connection/worker/recovery live regions.
- `dashboard/app.js` — queue state renderers, preview status renderer, recovery transition feedback, monitoring-gap event renderer, TLS badge renderer, `Edit service` copy, and safe modal error/focus behavior.
- `dashboard/style.css` — safety warning, durable queue, preview status, TLS badge, overflow, contrast, and narrow action rules.

## Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Safe continued use/upgrades with dependable independently maintainable web, worker, persistence, and outbound responsibilities | 01-01 through 01-08 | COVERED | Tracer precedes bounded expansions across every responsibility. |
| REQ | FND-01 | Compatibility tests protect current dashboard/data behavior | 01-01, 01-02, 01-03, 01-06, 01-08 | COVERED | Route/field/data/UI contracts precede extraction. |
| REQ | FND-02 | Explicit module boundaries | 01-02, 01-03 | COVERED | Web/data then monitoring/preview/worker composition. |
| REQ | FND-03 | Side-effect-free web import | 01-01, 01-02, 01-03 | COVERED | Fresh-process import tracer plus boundary tests. |
| REQ | FND-04 | Sole worker scheduler and durable coordination | 01-01, 01-03, 01-06 | COVERED | Explicit main, owner lease, queue leases, expiry/recovery. |
| REQ | FND-05 | Versioned transactional idempotent migrations | 01-04 | COVERED | Inventory-gated registry, lock, rollback, rerun tests. |
| REQ | FND-06 | Verified backup and failed-upgrade recovery | 01-04, 01-05 | COVERED | Three-backup retention and one offline atomic restore path. |
| REQ | FND-07 | One outbound target/TLS policy | 01-07, 01-08 | COVERED | All HTTP/HTML/browser/webhook callers plus visible posture. |
| REQ | OPS-05 | Outbound/DNS/redirect/TLS/mutation tests | 01-07, 01-08 | COVERED | Table-driven and overlapping-operation coverage. |
| RESEARCH | stack | Retain Flask, SQLite, APScheduler, requests, Playwright, Compose, vanilla JS; no new package | 01-02 through 01-08 | COVERED | No install task or package legitimacy gate is required. |
| RESEARCH | architecture | Factory + compatibility adapter + Flask-free domains + explicit worker main | 01-01 through 01-03 | COVERED | Dependency direction and entrypoint tests included. |
| RESEARCH | persistence | Inventory support floor, online backups, migration registry, worker gate | 01-04 | COVERED | Production inventory is a visible precondition/user setup. |
| RESEARCH | recovery | Network-isolated offline Compose restore | 01-05 | COVERED | Idempotency/interruption/concurrency specified. |
| RESEARCH | coordination | Owner/row leases, deadlines, attempts, latest preview revision, gap event | 01-06 | COVERED | 15/30-minute UI-contract ages selected. |
| RESEARCH | outbound | Purpose policy, all-address DNS, per-hop redirects, strict webhook, browser interception | 01-07 | COVERED | LAN TLS exception remains narrow and visible. |
| RESEARCH | validation | Runtime, migration, backup, queue, outbound, Compose, and UI-state tests | 01-01 through 01-08 | COVERED | Every code task has an automated under-60-second focused command. |
| CONTEXT | D-01 | Preserve routes, fields, behavior through compatibility adapters | 01-01, 01-02, 01-03 | COVERED | Costly reversibility recorded. |
| CONTEXT | D-02 | Fix confirmed bugs only with explicit intended behavior/tests | 01-01 | COVERED | Compatibility task encodes fail-first correction rule. |
| CONTEXT | D-03 | UI changes only for corrected/compatibility behavior | 01-01, 01-08 | COVERED | Narrow UI contract and exclusions are explicit. |
| CONTEXT | D-04 | Inventory actual database variants before support floor | 01-04 | COVERED | Flagged precondition/user setup prevents silent assumption. |
| CONTEXT | D-05 | Verified backup before every schema-changing migration | 01-04 | COVERED | Online backup and integrity gate. |
| CONTEXT | D-06 | Retain latest three verified migration backups | 01-04 | COVERED | Catalog-only bounded cleanup. |
| CONTEXT | D-07 | Worker never operates on partial migration | 01-03, 01-04, 01-06 | COVERED | Preparation then owner lease then scheduler ordering. |
| CONTEXT | D-08 | One clear supported recovery path | 01-05, 01-08 | COVERED | Offline command; browser remains read-only. |
| CONTEXT | D-09 | Preserve trusted-LAN permissive TLS | 01-07, 01-08 | COVERED | Purpose-specific exception and posture badge. |
| CONTEXT | D-10 | Narrow TLS exception; webhooks not exempt | 01-07 | COVERED | Separate allowlists and strict webhook plan. |
| CONTEXT | D-11 | Revalidate every redirect | 01-07 | COVERED | New immutable plan per hop. |
| CONTEXT | D-12 | Persistent TLS indicator, not downtime | 01-07, 01-08 | COVERED | Storage plus exact UI/accessibility behavior. |
| CONTEXT | D-13 | Interactive dashboard with stale warning | 01-01, 01-08 | COVERED | Tracer then complete ordered UI. |
| CONTEXT | D-14 | Durable queues, recovery, visible bounded expiry | 01-06, 01-08 | COVERED | 15/30-minute state machines and UI. |
| CONTEXT | D-15 | Immediate metadata persistence while worker unavailable | 01-02, 01-06, 01-08 | COVERED | Atomic metadata/latest-preview revision plus feedback. |
| CONTEXT | D-16 | Clear warning and persist monitoring gap on recovery | 01-01, 01-06, 01-08 | COVERED | Deduplicated event and one-shot UI announcement. |

Deferred broader discovery port coverage is excluded by CONTEXT.md and does not appear in any task.

## Spec-less Probe Resolution

| Probe item | Resolution |
|------------|------------|
| FND-01 unclassified | Resolved as exact route/field/persisted behavior compatibility truths and tests in 01-01/01-02. |
| FND-02 unclassified | Resolved as explicit import-direction/factory/domain boundary truths and tests in 01-02/01-03. |
| FND-03 unclassified | Resolved as fresh-process no-side-effect truth and test in 01-01. |
| FND-04 unclassified | Resolved as persisted owner/row lease, contender, takeover, and duplicate-execution truths/tests in 01-06. |
| FND-05 unclassified | Resolved as support-floor, ordered transactional version, rerun, rollback, and contender truths/tests in 01-04. |
| FND-06 idempotency | Resolved explicitly for repeated backup, migration rerun, and repeated restore in 01-04/01-05. |
| FND-06 concurrency | Resolved with shared upgrade lock, staged atomic restore, interruption, and contender behavior in 01-04/01-05. |
| FND-07 unclassified | Resolved as one purpose-aware policy across every outbound caller in 01-07. |
| OPS-05 concurrency | Resolved with immutable per-request TLS/redirect state and barrier-controlled mutation/network overlap tests in 01-07. |

## Descriptor-less Prohibition Recall

Kept product/value/safety prohibitions are authored as descriptor-less `status: unverified`, `flagged: true` entries in Plans 01, 02, 04, 05, 07, and 08. They have no automated descriptor fields, are never treated as resolved, and remain unresolved judgment items for verification. Routine import purity, transaction correctness, queue idempotency, and error handling candidates were dropped from prohibition recall because they are engineering/edge-test concerns already planned explicitly. SSRF, injection/path traversal, generic TLS, and generic mutation-security candidates are canon security items covered by each plan's STRIDE register and `$gsd-secure-phase`; they were breadcrumbed here rather than duplicated as bespoke prohibitions. No prohibition was auto-dismissed or resolved.

<output>
Create `.planning/phases/01-behavioral-safety-runtime-ownership/01-08-SUMMARY.md` when done.
</output>
