---
phase: 01-behavioral-safety-runtime-ownership
plan: 11
type: execute
wave: 8
depends_on:
  - 01-09
files_modified:
  - dashboard/beacon/queues.py
  - dashboard/app.py
  - tests/test_durable_queues.py
autonomous: true
gap_closure: true
requirements:
  - FND-04
threat_model:
  asvs_level: 1
  block_on: high
  completion_invariant: "Plan completion is blocked until the high-severity scan ownership and abandoned-work risks have fenced, deadline-aware automated evidence."
  register_location: "<threat_model> STRIDE Threat Register"
  applicable_threats:
    - "T-01-48 | severity=high | disposition=mitigate"
    - "T-01-49 | severity=high | disposition=mitigate"
    - "T-01-50 | severity=medium | disposition=mitigate"
  mitigation_task_links:
    - "T-01-48 -> Task 1, Task 2"
    - "T-01-49 -> Task 2"
    - "T-01-50 -> Task 1"
must_haves:
  truths:
    - "Per D-14, a scan that runs longer than the 30-second lease renews ownership until it reaches a terminal state."
    - "Expired running scans are recovered during normal claim polling without requiring a worker restart."
    - "A scan whose deadline has elapsed becomes terminal rather than returning to the queue."
    - "A worker that lost its lease cannot complete or fail work now owned by another worker."
  artifacts:
    - path: dashboard/beacon/queues.py
      provides: "Transactional scan lease renewal, prompt recovery, and fenced terminal transitions"
    - path: dashboard/app.py
      provides: "Long-running discovery execution under an active lease heartbeat"
    - path: tests/test_durable_queues.py
      provides: "Deterministic long-scan and multi-worker lease regression coverage"
  key_links:
    - from: dashboard/app.py
      to: dashboard/beacon/queues.py
      via: "process_scan_requests starts renewal before discovery and uses the same owner token for completion"
      pattern: "renew_scan_lease|finish_scan|fail_scan"
    - from: dashboard/beacon/queues.py
      to: scan_requests
      via: "claim transaction recovers expired rows before choosing the next queued request"
      pattern: "lease_expires_at|deadline_at|owner_token"
---

<objective>
Close the FND-04 durable scan-queue lease gap so lengthy scans stay owned, abandoned work recovers promptly, and stale workers are fenced from terminal writes.

Purpose: A scheduled scan must survive realistic discovery duration and worker contention without duplicate execution, indefinite `running` rows, or late results from a former owner.
Output: Heartbeated scan execution, claim-time recovery, deadline-aware terminal transitions, and deterministic concurrency tests.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-CONTEXT.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-RESEARCH.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-VERIFICATION.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-09-SUMMARY.md
@dashboard/beacon/queues.py
@dashboard/app.py
@tests/test_durable_queues.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one scan beyond its lease with an active heartbeat</name>
  <files>dashboard/beacon/queues.py, dashboard/app.py, tests/test_durable_queues.py</files>
  <read_first>
    - dashboard/beacon/queues.py: `claim_scan`, `renew_scan_lease`, `finish_scan`, and `fail_scan` ownership predicates
    - dashboard/app.py: `process_scan_requests` discovery call and terminal transitions
    - tests/test_durable_queues.py: queue clock, connection, and worker test helpers
  </read_first>
  <behavior>
    - Initial RED contract: add the deterministic cases below before production edits; they must fail on the missing lease behavior rather than on collection, import, setup, or wall-clock timing.
    - "A controlled scan lasting more than 30 seconds renews before each lease expires and ends in `completed`."
    - "Each renewal is conditioned on the original request id, owner token, `running` state, and unexpired request deadline."
    - "If renewal reports lost ownership, the former worker neither completes nor fails the row."
  </behavior>
  <action>Write the failing deterministic long-scan test first. Add a queue-level lease-heartbeat abstraction with injectable monotonic clock/wait controls for tests and production defaults derived from the claimed lease. Start it before invoking discovery in `process_scan_requests`, renew at a bounded interval shorter than the 30-second lease, stop and join it before the terminal transition, and carry one immutable owner token through renew/finish/fail. Treat a zero-row renewal as loss of authority: record the condition and suppress all later terminal writes from that worker. Keep the discovery implementation outside the queue module and preserve the worker's bounded scheduling behavior per D-14.</action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_durable_queues.py -k "long_scan or lease_heartbeat or lost_ownership"</automated>
  </verify>
  <acceptance_criteria>The deterministic clock advances one real scan execution beyond 30 seconds, observes lease renewals before expiry, and proves the row reaches `completed` only while the original worker still owns it.</acceptance_criteria>
  <done>Long-running scans retain a live lease, and a worker that loses its lease cannot write a terminal result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover abandoned scans during each claim transaction</name>
  <files>dashboard/beacon/queues.py, tests/test_durable_queues.py</files>
  <read_first>
    - dashboard/beacon/queues.py: `claim_scan`, `recover_queues`, row states, deadlines, and transactional update helpers
    - tests/test_durable_queues.py: restart recovery, deadline, and ownership-fencing cases
  </read_first>
  <behavior>
    - Initial RED contract: add the deterministic fake-clock and two-connection cases below before production edits; they must fail on recovery/ownership assertions and use no real `sleep`.
    - Claim-time recovery: given a `running` row whose lease expired at T=30 but whose request deadline is T=90, a claim at T=31 requeues and claims that row in the same SQLite write transaction with a new owner token.
    - Deadline expiry: given a `running` row with both lease and request deadline at or before T=31, the same claim transaction sets its terminal state to `expired`, clears lease ownership, and never returns it as claimable.
    - Lease renewal beyond 30 seconds: a claimed row driven by the injected clock through T=0, T=20, T=40, and T=61 renews before each lease boundary and accepts exactly one `completed` terminal result at T=61 without wall-clock waiting.
    - Renewal loss and fencing: after a second connection takes over with a new token, renewal, finish, and fail calls using the former token each update zero rows and leave the new owner's `running` row unchanged.
    - Terminal ownership: the current token may complete or fail once; every later terminal call from either token updates zero rows and preserves the first terminal outcome.
  </behavior>
  <action>Extract one `_recover_expired_scan_leases_in_transaction` operation and call it from both startup recovery and the beginning of every `claim_scan` transaction. Within the transaction, convert expired `running` rows with a live request deadline back to `queued`, clear ownership fields, and convert rows past their request deadline to the existing terminal expiry representation. Select and claim after recovery in the same write transaction so a second worker cannot interleave. Add a two-connection test proving prompt recovery without process restart, same-poll takeover, terminal behavior after the 30-second request deadline, and fencing of the former owner's completion/failure per D-14.</action>
  <verify>
    <automated>dashboard/.venv/bin/python -m pytest -q tests/test_durable_queues.py -k "recover or expired or fencing or takeover"</automated>
  </verify>
  <acceptance_criteria>An abandoned request is recovered by ordinary polling, a past-deadline request is terminal, and only the current owner can write the outcome.</acceptance_criteria>
  <done>Scan requests cannot remain indefinitely `running`, and takeover does not permit stale-worker writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker thread → durable queue | Concurrent workers mutate shared ownership and terminal state in SQLite. |
| scheduler clock → lease state | Time and deadlines determine whether work may continue or be reclaimed. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-48 | Tampering | Scan lease ownership | high | mitigate | Condition renew and terminal writes on request id, owner token, running state, and valid deadline; automated two-worker fencing tests block completion. |
| T-01-49 | Denial of Service | Expired running rows | high | mitigate | Recover expired rows in every claim transaction and make past-deadline rows terminal; deterministic polling tests block completion. |
| T-01-50 | Repudiation | Lease-loss outcome | medium | mitigate | Log lease loss with request identity while excluding secrets and suppress unauthorized terminal writes. |
</threat_model>

## Artifacts this phase produces

- A production scan heartbeat that keeps ownership valid through discovery.
- Claim-time recovery that does not depend on worker startup.
- Deterministic proof of deadline and ownership fencing behavior.

<verification>
- `dashboard/.venv/bin/python -m pytest -q tests/test_durable_queues.py`
- `dashboard/.venv/bin/python -m pytest -q tests/test_release_contract.py -k scan`
</verification>

<success_criteria>
- A scan lasting more than the configured lease remains owned and reaches a terminal state.
- Ordinary polling promptly recovers abandoned scans.
- Past-deadline requests terminate, and stale owner tokens cannot mutate their outcome.
</success_criteria>

<output>
Create `.planning/phases/01-behavioral-safety-runtime-ownership/01-11-SUMMARY.md` when done.
</output>
