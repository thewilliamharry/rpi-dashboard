---
phase: 02-bounded-telemetry-retention
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [TEL-01, TEL-02, TEL-03]

must_haves:
  truths:
    - "The operator explicitly approves the additive on-disk aggregate/coverage contract before migration 5 makes later reversal require another migration."
    - "Approval preserves D-01 through D-04 and D-09 exactly: 7-day raw, 5-minute through day 30, hourly through day 90, full events through day 90, and aggregate-before-delete."
    - "A request to revise stops execution before any telemetry schema or source-deletion behavior changes."
  artifacts: []
  key_links:
    - from: "02-02 approval"
      to: "02-03 migration task"
      via: "blocking one-way-door decision gate"
      pattern: "approve-contract"
  flagged_assumptions:
    - "The per-metric host-row and per-service bucket schema is the lowest-cost stable on-disk representation for later Phase 4 consumers; the checkpoint exists because reversing it after populated rollups requires a data migration."
  prohibitions: []
---

<objective>
Confirm the irreversible storage contract that implements D-01, D-02, D-03, D-04, and D-09 before any schema or deletion-capable retention code lands.

Purpose: Honor the enabled reversibility gate for the Phase 2 on-disk format and evidence-expiry door.
Output: An explicit approve-or-revise decision consumed by Plan 02-03.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md
@.planning/phases/02-bounded-telemetry-retention/02-01-SUMMARY.md
@dashboard/beacon/migrations.py
</context>

## Artifacts this phase produces

- No repository file or production symbol is created by this checkpoint plan.
- The plan summary records `approve-contract` or stops with `revise-contract`; Plan 02-03 may execute only after approval.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the additive telemetry evidence contract</name>
  <reversibility rating="one-way">Once migration 5 stores aggregates and the worker expires source evidence under the approved tiers, changing the format or expiry policy requires a migration and deleted evidence cannot be recovered.</reversibility>
  <files>None; this task records a decision only.</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-01 through D-04 and D-09 are non-negotiable product decisions.
    - `.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md` — review the additive tables, half-open bucket rules, and transactional rollup recommendation.
    - `.planning/phases/02-bounded-telemetry-retention/02-01-SUMMARY.md` — confirm the tracer response envelope that consumes these records.
    - `dashboard/beacon/migrations.py` — verify migration 5 will extend the existing locked, backed-up registry.
    - `tests/test_migrations.py` — verify legacy preservation remains the migration gate.
  </read_first>
  <decision>Approve the additive migration-5 representation and half-open deletion boundaries used by the remaining Phase 2 plans.</decision>
  <context>The proposed contract stores host aggregates as one row per metric/bucket, service aggregates as one row per service/bucket, sparse unavailable intervals in a coverage ledger, and retry state keyed by stream/bucket. It keeps raw rows at and after the 7-day cutoff, 5-minute buckets whose end is after the 30-day cutoff, hourly buckets whose end is after the 90-day cutoff, and events exactly at the 90-day cutoff. Only a source bucket whose aggregate has been verified inside the same current-worker transaction can be deleted.</context>
  <options>
    <option id="approve-contract">
      <name>Approve contract</name>
      <pros>Implements every locked tier and aggregate field with stable keys, idempotent retries, explicit coverage, and a direct Phase 4 query path.</pros>
      <cons>Changing populated table shapes or cutoff semantics later requires another migration; expired evidence cannot be recreated.</cons>
    </option>
    <option id="revise-contract">
      <name>Revise plans</name>
      <pros>Stops before the one-way door so table shape, default storage budget, or bucket identity can be reconsidered.</pros>
      <cons>Phase execution pauses and Plans 02-03 through 02-06 must be revised before implementation.</cons>
    </option>
  </options>
  <action>Review the exact contract above against D-01 through D-04 and D-09. Select `approve-contract` only if the normalized aggregate rows, sparse coverage ledger, retry key, half-open cutoff ownership, and permanent event/raw expiry behavior are acceptable. Select `revise-contract` to stop before migration; do not reinterpret or weaken any locked decision.</action>
  <acceptance_criteria>
    - The baseline tracer and Phase 1 migration tests are green before the decision is accepted.
    - The recorded decision is exactly `approve-contract` or `revise-contract`.
    - `revise-contract` halts the phase before Plan 02-03; `approve-contract` authorizes only the contract stated here.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_historical_telemetry_api.py tests/test_migrations.py -x</automated>
  </verify>
  <done>The one-way storage and expiry contract is explicitly approved, or execution has stopped for replanning without changing data.</done>
  <resume-signal>Select: approve-contract or revise-contract</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved design -> persistent SQLite format | A human-approved representation becomes a durable migration and deletion contract in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | aggregate-before-delete design | high | mitigate | Approval requires one current-worker transaction that writes, reads back, and verifies the uniquely keyed aggregate before deleting its exact source range. |
| T-02-05 | Denial of Service | allocation and WAL design | high | mitigate | Approval includes fixed batch bounds, DB/WAL/SHM accounting, allocation plus free-space guardrails, and recovery hysteresis. |
| T-02-06 | Repudiation | irreversible operator decision | medium | mitigate | The blocking summary records the selected signal before migration 5 may execute. |
</threat_model>

<verification>
- Confirm Plan 02-01 tests are green before presenting the decision.
- Do not execute any later plan unless this checkpoint completes with `approve-contract`.
</verification>

<success_criteria>
- The on-disk, tier-boundary, and deletion contract is consciously approved before irreversible work.
- No repository state changes when the operator selects revision.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-02-SUMMARY.md` with the selected signal when done.
</output>
