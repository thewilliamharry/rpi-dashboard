---
phase: 02-bounded-telemetry-retention
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - dashboard/beacon/migrations.py
  - dashboard/beacon/support_floor.json
  - tests/fixtures/legacy/support-floor.json
  - tests/fixtures/legacy/current-v4.db
  - tests/test_migrations.py
autonomous: true
requirements: [TEL-01, TEL-02, TEL-03]

must_haves:
  truths:
    - "Every supported legacy database and a canonical Phase 1 version-4 database migrate transactionally to telemetry schema version 5 with all original row values intact."
    - "Migration 5 creates uniquely keyed host/service rollups, stream cadence state, sparse coverage, retry/failure state, and range/due indexes without creating tables at worker startup."
    - "A migration failure rolls back all version-5 schema/data work and leaves the verified Phase 1 recovery path usable."
    - "The confirmed production fixture retains the existing service-port identity used by `service_checks`; Phase 2 neither fabricates nor silently migrates to a new service identity."
  artifacts:
    - path: "dashboard/beacon/migrations.py"
      provides: "Additive telemetry schema migration 5"
      contains: "_migration_5_bounded_telemetry"
    - path: "tests/fixtures/legacy/current-v4.db"
      provides: "Canonical current-schema upgrade fixture"
    - path: "tests/test_migrations.py"
      provides: "Legacy/current preservation, idempotence, index, and rollback evidence"
  key_links:
    - from: "dashboard/beacon/migrations.py"
      to: "dashboard/beacon/support_floor.json"
      via: "exact current-v4 structural fingerprint admitted alongside the frozen historical floor"
      pattern: "current-v4"
    - from: "dashboard/beacon/migrations.py"
      to: "telemetry_rollup_jobs"
      via: "Migration 5 table and due-retry index"
      pattern: "telemetry_rollup_jobs"
  flagged_assumptions:
    - "TEL-01 unclassified edge remains unresolved: the canonical Phase 1 v4 fingerprint is assumed to cover every database already upgraded by the shipped Phase 1 migrations; the test must derive and compare that fingerprint rather than trusting the manifest text."
    - "TEL-03 unclassified edge remains unresolved: SQLite commit/rollback plus verified-backup tests are assumed to cover process interruption timing; verifier must still review crash points around migration publication."
    - "The existing service port remains the durable stream key because the confirmed production fixture and current schema expose no stronger identity; if fixture inspection contradicts this, execution must stop for a schema decision rather than inventing a key transition."
  prohibitions:
    - statement: "MUST NOT remove unaggregated observations or individual operational events early in order to satisfy a migration, storage budget, or cleanup shortcut."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Install the additive telemetry storage contract approved in Plan 02-02 while preserving the Phase 1 migration support floor and all stored data, per D-02, D-03, D-04, and D-09.

Purpose: Give retention and historical reads durable, indexed, idempotent records before any source row can become deletion-eligible.
Output: Migration 5, a canonical current-v4 upgrade fixture/fingerprint, updated support manifests, and preservation/rollback tests.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md
@.planning/phases/02-bounded-telemetry-retention/02-PATTERNS.md
@.planning/phases/02-bounded-telemetry-retention/02-02-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-04-SUMMARY.md
@dashboard/beacon/migrations.py
@dashboard/beacon/support_floor.json
@tests/test_migrations.py
</context>

<probe_coverage>
TEL-02 adjacency is explicit: all time ranges are half-open `[start, end)`; a source row exactly at the 7-day cutoff remains raw; only a closed bucket whose end is at or before the cutoff is rollup-eligible; 5-minute and hourly ownership uses bucket end, not local time. Empty buckets create no aggregate and make no source deletion. Equal bucket keys are idempotent upserts, never duplicate rows. The TEL-01/TEL-03 unclassified rows remain flagged assumptions above.
</probe_coverage>

## Artifacts this phase produces

- New fixture `tests/fixtures/legacy/current-v4.db` generated from the canonical Phase 1 migrations and containing representative non-empty data.
- New migration symbol `_migration_5_bounded_telemetry()` and registry entry `Migration(5, 'bounded_telemetry', True, ...)`.
- New tables `telemetry_streams`, `host_metric_rollups`, `service_rollups`, `telemetry_coverage`, and `telemetry_rollup_jobs`.
- New indexes `idx_checks_port_ts`, `idx_host_rollups_range`, `idx_service_rollups_range`, `idx_telemetry_coverage_range`, and `idx_telemetry_rollup_jobs_due`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add migration 5 with preservation and rollback proof</name>
  <reversibility rating="one-way">Populated aggregate, coverage, and retry tables are an on-disk contract; changing their keys or column semantics later requires a versioned data migration.</reversibility>
  <files>dashboard/beacon/migrations.py, dashboard/beacon/support_floor.json, tests/fixtures/legacy/support-floor.json, tests/fixtures/legacy/current-v4.db, tests/test_migrations.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-02-SUMMARY.md` — assert the checkpoint selected `approve-contract`; halt otherwise.
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-02 through D-05 and D-09 define irreversible data semantics.
    - `.planning/phases/01-behavioral-safety-runtime-ownership/01-04-SUMMARY.md` — preserve the locked, backed-up, transactional migration path.
    - `dashboard/beacon/migrations.py` — append to `MIGRATIONS`; retain upgrade then exclusive-maintenance ordering.
    - `dashboard/beacon/support_floor.json` — preserve the four evidence-backed historical entries.
    - `tests/fixtures/legacy/support-floor.json` — keep the test manifest byte-equivalent in meaning to the packaged manifest.
    - `tests/test_migrations.py` — extend all-fixture preservation, current rerun, and rollback cases.
  </read_first>
  <behavior>
    - "Every old fixture and the canonical v4 fixture retains service, metadata, host sample, service check, event, and queue row values after migration 5."
    - "Rerunning a version-5 database applies no migration and creates no new backup."
    - "An injected migration-5 failure leaves no telemetry table/version row committed and writes the existing redacted recovery marker against a verified backup."
  </behavior>
  <action>Start with migration tests, inspect the confirmed `tests/fixtures/legacy/operator/production.db` service/check identity, and halt if it disproves the existing `(ts, port)` / service-port key; do not invent an opaque identity migration. Generate `tests/fixtures/legacy/current-v4.db` through migrations 1-4 with representative data and compute its inventory fingerprint using the existing classifier. Add that exact current-schema entry to both support-floor manifests while retaining all four frozen historical entries and updating their declared target to 5; this does not broaden unsupported legacy shapes. Add `_migration_5_bounded_telemetry(conn)` and append version 5. Create `telemetry_streams(stream_kind, stream_key, started_ts, cadence_seconds, last_observed_ts, consecutive_misses, open_gap_start_ts, PRIMARY KEY(stream_kind, stream_key))`; `host_metric_rollups(metric, bucket_start, bucket_seconds, min_value, max_value, avg_value, latest_value, sample_count, observed_seconds, gap_seconds, unknown_seconds, PRIMARY KEY(metric, bucket_start, bucket_seconds))`; `service_rollups(service_port, bucket_start, bucket_seconds, online_seconds, offline_seconds, unknown_seconds, gap_seconds, latency_min, latency_max, latency_avg, check_count, failure_class_counts_json, PRIMARY KEY(service_port, bucket_start, bucket_seconds))`; `telemetry_coverage(id, stream_kind, stream_key, start_ts, end_ts, reason, detail)` with `end_ts > start_ts` and reason constrained to `collection_gap`, `unknown`, `expired`, or `not_yet_monitored`; and `telemetry_rollup_jobs(stream_kind, stream_key, bucket_start, bucket_seconds, state, attempt_count, next_retry_ts, last_error_class, updated_ts, PRIMARY KEY(stream_kind, stream_key, bucket_start, bucket_seconds))` with state constrained to `pending`, `failed`, or `succeeded`. Add the five named composite indexes, including `service_checks(port, ts)`. Use integer UTC seconds and half-open intervals; add non-negative checks for counts/durations and positive checks for cadence/bucket sizes. Do not populate aggregate rows from legacy data during migration and do not delete or rewrite any existing telemetry/event row. Preserve the one-transaction migration, verified pre-migration backup, recovery marker, and idempotent registry behavior.</action>
  <acceptance_criteria>
    - `MIGRATIONS[-1]` is version 5 and every table, constraint, primary key, and named index exists after upgrading each fixture.
    - The generated v4 fixture fingerprint is asserted against both manifests; no prior supported fingerprint disappears.
    - The confirmed production fixture proves the existing service port is the retained stream key; contradictory evidence halts before migration.
    - All representative pre-upgrade row values survive and every new table begins empty.
    - Migration rerun is a no-op; injected failure rolls back version 5 and retains the existing verified recovery evidence.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_migrations.py -x</automated>
  </verify>
  <done>All supported databases have a safe, indexed version-5 telemetry schema with no data loss, duplicate application, or worker-startup DDL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing database -> migration 5 | Durable user data crosses an irreversible schema upgrade and must survive byte-equivalent in meaning. |
| support manifest -> migration admission | Only exact evidence-backed fingerprints may enter the schema-changing path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | migration 5 / support floor | high | mitigate | Admit only the four frozen historical fingerprints plus a test-derived canonical v4 fingerprint; retain verified backup, transaction rollback, and redacted recovery marker. |
| T-02-08 | Denial of Service | telemetry range/due indexes | medium | mitigate | Create composite range and retry-due indexes in the versioned migration so later bounded reads/batches do not scan growing tables. |
| T-02-09 | Repudiation | rollup job state | medium | mitigate | Persist unique bucket identity, attempt count, next retry, error class, and updated timestamp; retries overwrite the same job identity. |
</threat_model>

<verification>
- Run `uv run --project dashboard python -m pytest -q tests/test_migrations.py` after the task.
- Run the Phase 2 quick suite to ensure the tracer reads a newly migrated database.
</verification>

<success_criteria>
- Migration 5 is additive, transactional, idempotent, backed up, and tested against every supported shape.
- Legacy rows survive and the telemetry tables/indexes are ready before retention code writes them.
- No schema creation occurs in a worker callback or request path.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-03-SUMMARY.md` when done.
</output>
