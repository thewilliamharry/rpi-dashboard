---
phase: 02-bounded-telemetry-retention
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - dashboard/app.py
  - dashboard/beacon/telemetry.py
  - dashboard/beacon/worker_main.py
  - tests/worker_ownership_contract.py
  - tests/test_telemetry_retention.py
autonomous: true
requirements: [TEL-01, TEL-03, TEL-04]

must_haves:
  truths:
    - "The existing sole-worker J8 callback performs bounded retention, retry, event expiry, scan-rate cleanup, and pressure recovery under the exact current WorkerAuthority epoch."
    - "After two missed configured cadence boundaries, a collection gap begins at the first missed boundary and closes at the next valid historical observation per D-06."
    - "Storage suspension skips only new historical rows; current host/service state continues updating, and one `collection_gap` with detail `storage_pressure` records the unpersisted interval per D-11."
    - "A stale worker can mutate none of the raw, rollup, coverage, stream, job, pressure-state, event, or cleanup tables; the current successor can complete the same work."
  artifacts:
    - path: "dashboard/app.py"
      provides: "Authority-fenced retention and observation adapters"
      contains: "worker_cleanup_history"
    - path: "dashboard/beacon/worker_main.py"
      provides: "Complete callback-to-telemetry-table mutation inventory"
      contains: "WORKER_CALLBACK_INVENTORY"
    - path: "tests/worker_ownership_contract.py"
      provides: "Frozen Phase 2 worker mutation surface contract"
    - path: "tests/test_telemetry_retention.py"
      provides: "Real-SQLite stale/current epoch, gap, suspension, and recovery evidence"
  key_links:
    - from: "dashboard/beacon/worker_main.py"
      to: "dashboard/app.py"
      via: "existing J8 `cleanup_history(authority)` dispatch and worker binding"
      pattern: "cleanup_history"
    - from: "dashboard/app.py"
      to: "dashboard/beacon/telemetry.py"
      via: "`_worker_write_transaction` invokes retention and observation helpers after epoch assertion"
      pattern: "run_retention_batch|record_observation"
    - from: "tests/worker_ownership_contract.py"
      to: "WORKER_CALLBACK_INVENTORY"
      via: "bijective callback/table surface assertion"
      pattern: "J8"
  flagged_assumptions:
    - "A5 remains explicit: the all-service five-minute callback is the expected service cadence and the down-only one-minute callback is supplemental evidence; tests must ensure healthy services do not receive fabricated one-minute gaps."
    - "Indeterminate service-result mapping remains conservative: existing Boolean false results stay observed offline; only an explicit `online is None` adapter result becomes `unknown` until the product defines additional error-class mappings."
  prohibitions:
    - statement: "MUST NOT let storage pressure or retention failure stop live/current monitoring, hide the degraded interval, or reconstruct missed historical values after recovery."
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Wire the retention engine into Beacon's sole worker, register truthful cadence/pressure gaps at observation time, and extend Phase 1 epoch fencing across every new mutation, per D-06 and D-09 through D-12.

Purpose: Make bounded retention operational without introducing a scheduler owner, process-local authority, sampling gap, or stale-worker write.
Output: Production worker adapters, explicit gap/pressure state transitions, updated callback mutation inventory, and real takeover evidence.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md
@.planning/phases/02-bounded-telemetry-retention/02-PATTERNS.md
@.planning/phases/02-bounded-telemetry-retention/02-04-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-21-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-22-SUMMARY.md
@.planning/phases/01-behavioral-safety-runtime-ownership/01-23-SUMMARY.md
@dashboard/app.py
@dashboard/beacon/worker_main.py
@tests/worker_ownership_contract.py
</context>

<probe_coverage>
TEL-04 adjacency, empty, and ordering are resolved operationally: coverage uses half-open intervals; a valid observation at an interval end closes the old gap and starts observed coverage without overlap; adjacent rows coalesce only when state and detail match; an empty stream is entirely `not_yet_monitored`; one explicit indeterminate result creates one `unknown` interval; all intervals sort by `(start_ts, end_ts, state, detail)`. The conservative online/unknown mapping remains a flagged assumption.
</probe_coverage>

## Artifacts this phase produces

- New telemetry symbols `record_observation()`, `record_unknown_interval()`, `detect_collection_gaps()`, `read_retention_state()`, `write_retention_state()`, `measure_storage()`, and `close_storage_pressure_gap()`.
- New `dashboard.app` helper `_telemetry_persistence_decision()` plus authority-aware observation calls inside `worker_collect_system_stats()`, the full-service probe transaction, and `worker_cleanup_history()`.
- Expanded `WORKER_CALLBACK_INVENTORY`/`WORKER_CONTRACT` database surfaces for `telemetry_streams`, `host_metric_rollups`, `service_rollups`, `telemetry_coverage`, `telemetry_rollup_jobs`, and `runtime_state`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run retention, cadence gaps, and pressure recovery under WorkerAuthority</name>
  <reversibility rating="costly">These adapters determine which worker writes count as historical evidence and are coupled to durable gap/state semantics across monitoring and retention.</reversibility>
  <files>dashboard/app.py, dashboard/beacon/telemetry.py, tests/test_telemetry_retention.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-06 and D-09 through D-12 are locked behavior.
    - `.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md` — preserve five-minute all-service cadence, supplemental down checks, and storage gap semantics.
    - `dashboard/app.py` — reuse `_worker_write_transaction()`, `worker_collect_system_stats()`, `_legacy_do_uptime_check()`, and `worker_cleanup_history()`.
    - `dashboard/beacon/telemetry.py` — call Plan 04 policy/retention functions with an open fenced connection.
    - `dashboard/beacon/config.py` — compose one `RetentionPolicy` from validated settings.
    - `tests/test_telemetry_retention.py` — add red tests for real authority, cadence, and pressure paths before implementation.
  </read_first>
  <behavior>
    - "Worker A after Worker B takeover raises `LeaseLost` before changing any telemetry table; Worker B completes the same callback."
    - "Two missed host or full-service cadence boundaries open one gap at the first miss; a later valid observation closes it exactly; a one-minute down-only run does not create healthy-service misses."
    - "Suspended history leaves `system_stats` and service current-state fields current while raw-history counts stop and a storage-pressure gap grows, then recovery closes the gap without backfill."
  </behavior>
  <action>Keep `dashboard/worker.py` and the existing `cleanup_history` operation binding unchanged; replace only the body/adjacent helpers behind `worker_cleanup_history(authority, now=None)`. Measure database, WAL, SHM, and filesystem free bytes outside SQLite, then enter `_worker_write_transaction(authority, now=now)`, persist the pressure transition in existing `runtime_state` key `telemetry_retention_state`, call `run_retention_batch(conn, policy, now, snapshot)`, detect two-miss cadence gaps, expire events through the Plan 04 policy, and retain the existing bounded `scan_rate_hits` cleanup. Extend telemetry with `record_observation()` and `detect_collection_gaps()`: host stream key `host` uses `METRIC_HISTORY_SECONDS`; service stream key is the decimal port and cadence is 300 seconds from the full-service callback. The first missed boundary is `last_observed_ts + cadence`; confirm only when `now >= last_observed_ts + 2 * cadence`; close at the next persisted observation. Treat down-only checks as supplemental observations and never as expected cadence. In `worker_collect_system_stats`, always update `system_stats`; insert `stats_history` and advance/close host coverage only when historical persistence is allowed. In the authoritative service probe transaction, always update `services` current fields; insert `service_checks` and advance coverage only when allowed. An explicit `online is None` records `unknown`; existing false Boolean results remain observed offline. On suspension, open/extend `collection_gap` with detail `storage_pressure` for affected streams, continue current-state work, and leave existing unaggregated rows intact. On recovery below both thresholds, close those gaps, return state to normal, and resume only new historical writes—no interpolation or backfill. Every write occurs after `assert_current_worker_authority` in the same transaction.</action>
  <acceptance_criteria>
    - `worker_cleanup_history` performs one bounded retention transaction and preserves scan-rate cleanup without the old 1/8/14-day deletes.
    - Real Worker A/B tests prove stale epoch rejection and current successor success for retention, retry, gap, pressure, event-expiry, and raw-history writes.
    - Host and full-service gaps begin/end exactly per D-06; down-only callbacks do not create false gaps.
    - Pressure suspension preserves current `system_stats`/service state, all old unrolled rows, and a truthful gap; recovery closes the gap without manufacturing rows.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py -k "worker or epoch or cadence or gap or suspension or recovery" -x</automated>
  </verify>
  <done>The existing worker callback owns all retention and observation-state changes with durable epoch fencing and candid pressure/gap behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the production worker mutation inventory over telemetry tables</name>
  <files>dashboard/beacon/worker_main.py, tests/worker_ownership_contract.py, tests/test_telemetry_retention.py</files>
  <read_first>
    - `.planning/phases/01-behavioral-safety-runtime-ownership/01-21-SUMMARY.md` — preserve the frozen inventory methodology.
    - `.planning/phases/01-behavioral-safety-runtime-ownership/01-23-SUMMARY.md` — production callback descriptors remain the sole dispatch source.
    - `dashboard/beacon/worker_main.py` — update only declared database surfaces; keep one J8 callback and current scheduling bounds.
    - `tests/worker_ownership_contract.py` — mirror every actual table touched by S3/J2/J3/J4/J8.
    - `tests/test_telemetry_retention.py` — integrate the new tables into current/stale callback evidence.
  </read_first>
  <behavior>
    - "The static production-to-contract gate reports no telemetry table mutation outside an inventoried worker callback."
    - "J8 remains one scheduled callback with `max_instances=1`, coalescing, the metrics executor, and bounded work per invocation."
    - "Lease loss follows the existing close-admission/stop path and does not continue retry or cleanup writes."
  </behavior>
  <action>Expand the production and frozen contract database-surface tuples without adding a scheduler or callback owner. S3/J2 metric callbacks add `telemetry_streams`, `telemetry_coverage`, and `runtime_state`; J3/J4 service callbacks add the same telemetry state/coverage tables to their existing service surfaces; J8 adds `host_metric_rollups`, `service_rollups`, `telemetry_streams`, `telemetry_coverage`, `telemetry_rollup_jobs`, and `runtime_state` to its existing raw/event/scan-rate surfaces. Keep callback identifier J8, handler `cleanup`, scheduler id `cleanup`, metrics executor, hourly interval, coalescing, and `max_instances=1`; retry due times remain data-driven and a callback processes only due jobs. Add a static bijection assertion and real-SQLite current/stale controls in `tests/test_telemetry_retention.py`; do not remove or relax any Phase 1 inventory row, admission classification, queue predicate, or effect surface.</action>
  <acceptance_criteria>
    - Production and frozen callback surface sets match exactly and name every new telemetry table touched by S3/J2/J3/J4/J8.
    - There is still one scheduler owner and one retention callback; no process-local retention lock or startup DDL exists.
    - The actual J8 dispatch raises through the existing `LeaseLost` shutdown path for stale authority and succeeds for current authority.
    - Phase 1 ownership matrix tests remain green without deleted, skipped, or reclassified cases.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -k "telemetry or inventory or registry or callback_coverage or database" -x</automated>
  </verify>
  <done>All telemetry mutations are visible in the immutable callback contract and inherit Phase 1 admission, drain, and durable-epoch behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler callback -> shared SQLite | A worker process may mutate telemetry only while its exact durable owner epoch remains current. |
| storage state -> observation writer | Pressure may suppress historical persistence but must not suppress current-state monitoring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Tampering | stale `worker_cleanup_history` / observation writer | critical | mitigate | Assert exact WorkerAuthority after `BEGIN IMMEDIATE` in every mutation transaction and preserve the Phase 1 LeaseLost admission/shutdown path. |
| T-02-15 | Denial of Service | J8 retention callback | high | mitigate | Keep one coalesced/max-one callback, process only 32 due buckets, perform storage measurement outside the write lock, and retain indexed due selection. |
| T-02-16 | Denial of Service | suspended persistence | high | mitigate | Continue current host/service writes, suspend only new historical rows after reserve exhaustion, and recover with two-condition hysteresis. |
| T-02-17 | Repudiation | collection/pressure gaps | medium | mitigate | Persist half-open reason/detail intervals and deterministic state transitions in the same fenced transaction as observation admission. |
</threat_model>

<verification>
- Run the focused real-SQLite worker selection after each task.
- Run `uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py tests/test_runtime_ownership.py tests/test_worker_ownership_matrix.py tests/test_durable_queues.py` at wave end.
</verification>

<success_criteria>
- Rollup-before-delete, retry, gap detection, pressure suspension/recovery, and raw/event expiry execute only under current worker authority.
- Live monitoring remains useful during storage pressure and the missing historical interval remains explicit.
- The production callback inventory and ownership evidence cover every new durable surface.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-05-SUMMARY.md` when done.
</output>
