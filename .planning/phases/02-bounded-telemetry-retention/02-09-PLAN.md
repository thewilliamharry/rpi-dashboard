---
phase: 02-bounded-telemetry-retention
plan: 09
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - dashboard/beacon/repositories.py
  - dashboard/app.py
  - tests/test_historical_telemetry_api.py
autonomous: true
gap_closure: true
requirements: [TEL-02, TEL-03, TEL-04]

must_haves:
  truths:
    - "Retained host and service source observations remain queryable while bounded asynchronous compaction is backlogged, pending, or failed per D-07 and D-09."
    - "Raw and five-minute fallback evidence never duplicates a replacement five-minute/hourly aggregate; the highest completed tier owns each exact source interval and half-open tier boundaries appear once."
    - "Intervals backed by preserved source evidence are `observed` in the exhaustive coverage partition, while pending/failed aggregation remains a separate bounded disclosure and never masquerades as ordinary unknown per D-05."
    - "Fallback and pending queries retain fixed selector shapes, bound values, SQLite-side display grouping, prompt connection closure, and the configured point-budget ceiling."
  artifacts:
    - path: "dashboard/beacon/repositories.py"
      provides: "Bounded non-overlapping lower-tier fallback and derived/durable aggregation-pending reads"
      exports: ["get_host_telemetry", "get_service_telemetry", "get_pending_aggregation"]
    - path: "dashboard/app.py"
      provides: "Policy-aware host/service pending and fallback composition through the existing GET route"
      contains: "api_telemetry_history"
    - path: "tests/test_historical_telemetry_api.py"
      provides: "Backlog, pending, failed, replacement, boundary, host, and service API regression matrix"
  key_links:
    - from: "dashboard/beacon/repositories.py::get_host_telemetry"
      to: "stats_history|host_metric_rollups"
      via: "fallback reads lower-tier rows outside their normal age band only when no completed replacement covers that interval"
      pattern: "NOT EXISTS|bucket_seconds"
    - from: "dashboard/beacon/repositories.py::get_service_telemetry"
      to: "service_checks|service_rollups"
      via: "the same highest-completed-tier ownership rule for service evidence"
      pattern: "NOT EXISTS|service_port"
    - from: "dashboard/app.py::api_telemetry_history"
      to: "dashboard/beacon/repositories.py::get_pending_aggregation"
      via: "selector, policy cutoffs, display resolution, and configured budget passed to bounded pending disclosure"
      pattern: "get_pending_aggregation"
    - from: "dashboard.beacon.telemetry.partition_coverage"
      to: "fallback SourceSegment values"
      via: "preserved lower-tier source intervals participate as observed evidence before coverage complement is labeled"
      pattern: "source_segments"
---

<objective>
Keep preserved telemetry visible until its replacement aggregate exists, without duplicates, false unknown coverage, unbounded reads, or a new schema.

Purpose: Close the asynchronous compaction window for ordinary backlog and failed/pending jobs so the API remains truthful at every tier boundary for host and service history.
Output: Fixed bounded fallback queries, highest-completed-tier ownership, derived plus durable pending disclosure, and a full regression/final-suite gate.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-RESEARCH.md
@.planning/phases/02-bounded-telemetry-retention/02-PATTERNS.md
@.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md
@.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md
@.planning/phases/02-bounded-telemetry-retention/02-06-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-07-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-08-SUMMARY.md
@dashboard/beacon/repositories.py
@dashboard/beacon/telemetry.py
@dashboard/app.py
@tests/test_historical_telemetry_api.py

<interfaces>
- `_tier_ranges(start_ts, end_ts, cutoffs)` currently assigns exclusive hourly/five-minute/raw age bands and is the direct cause of preserved lower-tier evidence becoming invisible.
- `get_host_telemetry()` and `get_service_telemetry()` return immutable `SourceSegment` values already grouped to the selected display bucket; `compose_historical_response()` merges segments by display bucket and preserves aggregate fields.
- `get_pending_aggregation()` currently returns only durable `pending`/`failed` job rows for an exact stream key; raw host jobs use shared key `host`, while hourly host jobs use the requested metric.
- `partition_coverage()` already labels intervals backed by `source_segments` as observed and keeps aggregation state outside the five-state coverage vocabulary.
</interfaces>
</context>

## Artifacts this phase produces

- Host raw and five-minute fallback query shapes for retained evidence older than its normal tier, guarded by completed replacement existence.
- Equivalent service fallback query shapes with time-weighted/latency/failure aggregation preserved.
- A selector-aware `get_pending_aggregation()` that combines durable failed/pending jobs with bounded derived backlog intervals and understands the shared raw-host job key.
- API regressions for ordinary backlog, persisted pending and failed jobs, completed replacement precedence, exact 7/30-day boundaries, host/service parity, coverage state, and configured caps.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serve non-duplicating host evidence while compaction is awaiting work</name>
  <reversibility rating="costly">Repository tier ownership and pending response semantics are consumed by later analytics, but the repair remains behind the existing schema and response envelope.</reversibility>
  <files>dashboard/beacon/repositories.py, dashboard/app.py, tests/test_historical_telemetry_api.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-01, D-02, D-05, D-07, D-08, and D-09 define source preservation, tier boundaries, coverage, and pending truthfulness.
    - `.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md` — repair the exact awaiting-compaction omission and keep source evidence queryable.
    - `dashboard/beacon/repositories.py` — extend fixed `HOST_QUERY_SHAPES`, `_tier_ranges()`, `_checked_rows()`, and pending reads without caller-controlled SQL.
    - `dashboard/beacon/telemetry.py` — preserve `SourceSegment`, weighted host composition, and the five-state coverage vocabulary.
    - `dashboard/app.py` — pass the Plan 02-07 policy cutoffs, resolution, selector, and configured limit through the existing short read transaction.
    - `tests/test_historical_telemetry_api.py` — add real SQLite backlog/replacement/boundary cases before implementation.
  </read_first>
  <behavior>
    - "A raw cpu/ram/disk/temp row older than the configured raw cutoff remains returned and observed when its 300-second replacement does not exist."
    - "A 300-second host rollup older than the configured five-minute cutoff remains returned when its hourly replacement does not exist."
    - "When a matching completed replacement exists, only that highest completed tier contributes; exact raw/five-minute cutoff timestamps appear once."
    - "Ordinary backlog is disclosed as pending, and durable shared raw-host or metric-specific hourly pending/failed state overrides the derived entry without duplication."
  </behavior>
  <action>Write the host backlog matrix first. Replace exclusive-only host routing with normal ownership plus bounded fallback ranges: keep hourly before `five_minute_start_ts`, five-minute between the configured cutoffs, and raw at/after `raw_start_ts`; additionally query raw below `raw_start_ts` and 300-second rollups below `five_minute_start_ts` only where no higher completed rollup for the requested metric covers the same half-open interval. Implement the precedence in fixed module-owned SQL using bound metric, range, bucket, and limit values; raw fallback must exclude either 300- or 3,600-second replacement coverage, and five-minute fallback must exclude hourly coverage. Group every fallback in SQLite to `display_bucket_seconds`, return `SourceSegment(60|300, ...)`, and retain the sentinel overflow behavior. Extend `get_pending_aggregation()` to accept the validated selector, policy cutoffs, display resolution, and limit. For host requests, combine durable raw-bucket jobs keyed `host:host`, durable hourly jobs keyed `host:<metric>`, and derived jobless raw/5-minute backlog buckets. Coalesce adjacent derived pending intervals, let a durable pending/failed row override the same bucket, preserve attempt/due/error metadata, and reject overflow instead of silently truncating. Update `api_telemetry_history()` to pass these validated values without widening the response envelope. Tests must cover all four host metrics, raw and five-minute backlog, shared raw-job versus metric hourly-job lookup, pending/failed states, completed replacements, and both exact cutoff boundaries.</action>
  <acceptance_criteria>
    - Preserved host raw/5-minute observations outside their normal age band return as points and make their intervals observed per D-05 and D-07.
    - A replacement aggregate suppresses the corresponding lower-tier contribution, so counts/averages and exact cutoff rows are not duplicated per D-02.
    - `aggregation_pending` covers jobless backlog plus durable `pending`/`failed` host jobs; shared raw-host job identity is visible to every affected metric while hourly jobs remain metric-specific per D-09.
    - Every host query remains fixed-shape, parameterized, grouped in SQLite, bounded by the configured policy, and closed before response composition.
    - No schema migration, source mutation, interpolation, or new coverage-state value is introduced.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_historical_telemetry_api.py -k "host and (backlog or pending or failed or replacement or boundary or fallback)" -x</automated>
  </verify>
  <done>Every retained host observation stays visible and correctly labeled until a completed higher-tier replacement atomically takes ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same awaiting-compaction contract to services and close the phase suite</name>
  <reversibility rating="costly">Service fallback and pending semantics share the historical API contract used by later service investigations.</reversibility>
  <files>dashboard/beacon/repositories.py, dashboard/app.py, tests/test_historical_telemetry_api.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-04, D-05, D-07, and D-09 require time-weighted service evidence, explicit unknown/gap meaning, and preserved failed sources.
    - `.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md` — CR-04 explicitly requires host and service asynchronous compaction coverage.
    - `dashboard/beacon/repositories.py` — mirror Task 1 ownership rules in fixed `SERVICE_QUERY_SHAPES` without materializing dense checks in Python.
    - `dashboard/beacon/telemetry.py` — preserve service duration sums, latency sample weighting, failure counts, and source-resolution disclosure.
    - `tests/test_historical_telemetry_api.py` — extend the same table-driven boundary/state matrix for a range-checked service port.
    - `.planning/phases/02-bounded-telemetry-retention/02-VALIDATION.md` — execute both focused Phase 2 files and the complete project suite before summary.
  </read_first>
  <behavior>
    - "Retained raw service checks and 300-second service rollups remain queryable outside their normal age bands until a completed higher tier exists."
    - "Fallback preserves time-weighted online/offline/unknown durations, latency denominator, check count, and failure-class counts without duplicate replacement contribution."
    - "Jobless backlog and durable pending/failed service jobs are disclosed separately while preserved checks/rollups remain observed; exact tier boundaries remain half-open."
    - "The focused Phase 2 suites and complete project suite pass after all five verified repairs."
  </behavior>
  <action>Extend the Task 1 lower-tier ownership rule to `SERVICE_QUERY_SHAPES` and `get_service_telemetry()`. Query raw service checks below the configured raw cutoff only when no 300/3,600-second service rollup covers that interval; query 300-second service rollups below the five-minute cutoff only when no hourly replacement covers them. Preserve the existing SQLite interval-overlap calculation, left-boundary preceding state, latency sample denominator, check/failure counts, display grouping, ascending order, and limit sentinel. Extend derived/durable pending composition for the selected decimal service port, with durable state overriding the same derived bucket and adjacent equivalent pending ranges coalescing. Add exact raw/five-minute boundary, ordinary backlog, persisted pending, persisted failed-before/after-due, completed replacement, mixed raw/5-minute/hourly, empty, and point-budget cases. Assert fallback-backed intervals are observed, unavailable intervals retain the existing exhaustive five-state vocabulary, and `aggregation_pending` explains work state without replacing coverage. Run both focused Phase 2 suites, then the complete project suite; investigate any regression rather than weakening an existing Phase 1/2 assertion.</action>
  <acceptance_criteria>
    - Host and service behavior is symmetric at raw and five-minute backlog boundaries, including completed replacement precedence and non-duplication.
    - Service fallback retains every D-04 duration, latency, check, and failure field and never treats completed offline evidence as unknown.
    - Pending/failed service work remains separately visible with durable metadata while its preserved observations are still returned per D-09.
    - Focused Phase 2 telemetry/API tests pass, followed by `uv run --project dashboard python -m pytest -q` with no skipped, deleted, or weakened regression.
    - The existing route shape, selector validation, Host/Origin protections, SQLite connection lifetime, and configured budget remain intact.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q</automated>
  </verify>
  <done>Host and service history remains bounded, non-duplicating, and candid throughout asynchronous backlog/failure windows, and the complete project regression suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated selector/range/policy -> fallback repository | Untrusted browser values may select only fixed host metric or service port query shapes and bounded half-open ranges. |
| lower-tier evidence -> completed replacement | Repository precedence must prevent duplicate contribution while never hiding preserved evidence before replacement exists. |
| durable/derived work state -> response | Pending and failed aggregation metadata must explain asynchronous work without corrupting the five-state coverage partition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-G09 | Tampering | cross-tier fallback ownership | high | mitigate | Use fixed half-open replacement-existence predicates so the highest completed tier contributes each interval once; regress raw/5-minute/hourly boundaries for host and service. |
| T-02-G10 | Denial of Service | backlog range reads | high | mitigate | Select display resolution first, group in SQLite, cap every fixed query and coalesced pending result with the configured sentinel, and close the connection before composition. |
| T-02-G11 | Spoofing | coverage/pending classification | medium | mitigate | Feed preserved fallback segments into coverage as observed and disclose pending/failed state separately with durable metadata precedence. |
| T-02-G12 | Information Disclosure | repository/overflow errors | low | accept | Existing fixed 400 responses expose no SQL, path, worker epoch, or raw exception text; trusted-LAN telemetry remains intended output. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---------------|---------------|-----------------------|
| V4 Access Control | Applicable | Keep all fallback access behind the existing GET-only Flask route and Host/Origin middleware; repository functions remain read-only and receive an already-open managed connection. |
| V5 Validation, Sanitization and Encoding | Applicable | Retain exact host/metric or service/port validation, fixed module-owned SQL, bound selector/range/cutoff/bucket/limit values, positive configured caps, and strict overflow failure. |
| V2 Authentication | Not applicable | Beacon remains account-free on its trusted LAN and this read-only repair adds no authentication mechanism. |
| V3 Session Management | Not applicable | Historical reads are stateless and create no cookie, token, login, or server session. |
| V6 Stored Cryptography | Not applicable | The response contains intended local operational data and adds no secret, credential, encryption, signature, or cryptographic protocol. |
</threat_model>

<verification>
- Run the focused Phase 2 suites: `uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py tests/test_historical_telemetry_api.py`.
- Run migration/release compatibility around the unchanged schema and route: `uv run --project dashboard python -m pytest -q tests/test_migrations.py tests/test_release_contract.py`.
- Run the complete project suite after all gap plans: `uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- Ordinary backlog plus pending/failed host and service compaction never hides preserved observations.
- Completed replacements take ownership once with no double-counted point, coverage interval, or pending entry at exact tier boundaries.
- All reads and disclosures remain fixed-query, point-bounded, promptly closed, and compatible with the existing response contract.
- Every confirmed Phase 2 verification gap has an executable regression and the complete project suite passes.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-09-SUMMARY.md` when done.
</output>
