---
phase: 02-bounded-telemetry-retention
plan: 11
type: execute
wave: 11
depends_on: [02-10]
files_modified:
  - dashboard/beacon/repositories.py
  - tests/test_historical_telemetry_api.py
autonomous: true
gap_closure: true
requirements: [TEL-04, TEL-05]

must_haves:
  truths:
    - "One unaggregated 300-second host or service rollup is disclosed as exactly [bucket_start, bucket_start + 300), never as a 3,600-second interval."
    - "Two exact touching five-minute pending buckets coalesce into their exact union; non-touching or overlapping rows are not widened under an adjacency shortcut."
    - "Empty host/service pending input returns no pending range, and reverse-seeded input produces deterministic ascending half-open output."
    - "Durable pending/failed metadata still overrides the same exact derived bucket, response work remains policy-bounded, and the repository remains read-only and parameterized."
  artifacts:
    - path: "dashboard/beacon/repositories.py"
      provides: "Exact five-minute derived pending ranges and strict adjacency coalescing"
      contains: "_pending_source_rows|_coalesce_pending|get_pending_aggregation"
    - path: "tests/test_historical_telemetry_api.py"
      provides: "Host/service exact-width, adjacency, empty, ordering, and durable-precedence regressions"
  key_links:
    - from: "dashboard/beacon/repositories.py::_pending_source_rows"
      to: "host_metric_rollups|service_rollups"
      via: "fixed bucket_seconds=300 projections using bucket_start + 300"
      pattern: "bucket_start + 300"
    - from: "dashboard/beacon/repositories.py::_coalesce_pending"
      to: "GET /api/telemetry/history aggregation_pending"
      via: "stable half-open ordering and equality-only adjacency"
      pattern: "start_ts.*end_ts"
    - from: "tests/test_historical_telemetry_api.py"
      to: "get_pending_aggregation"
      via: "real SQLite host/service 300-second rows and Flask response assertions"
      pattern: "aggregation_pending"
  flagged_assumptions:
    - "TEL-01 unclassified: repository-only pending disclosure cannot change 90-day expiry or storage bounds; the full suite verifies those existing contracts."
    - "TEL-03 unclassified: this plan performs no SQLite mutation and cannot delete source evidence; existing aggregate-before-delete tests remain mandatory."
    - "TEL-05 unclassified beyond the verified defect: resolution selection and the Settings-backed point budget remain unchanged; only false pending interval width/order is repaired."
---

<objective>
Correct derived five-minute pending intervals for both host and service history and make their empty, adjacency, and ordering semantics exact.

Purpose: Prevent `aggregation_pending` from claiming 3,300 seconds of work for which no preserved five-minute source exists while retaining bounded, separate work-state disclosure.
Output: Two corrected fixed query projections, strict half-open coalescing, and host/service repository-to-Flask regressions.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md
@.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md
@.planning/phases/02-bounded-telemetry-retention/02-REVIEW.md
@.planning/phases/02-bounded-telemetry-retention/02-09-SUMMARY.md
@.planning/phases/02-bounded-telemetry-retention/02-10-SUMMARY.md
@dashboard/beacon/repositories.py
@dashboard/beacon/telemetry.py
@dashboard/app.py
@tests/test_historical_telemetry_api.py

<interfaces>
- `_pending_source_rows(conn, stream_kind, stream_key, start_ts, end_ts, cutoffs, limit)` derives jobless raw and five-minute backlog using fixed module-owned SQL; only the two `bucket_seconds=300` projections have the false hourly end.
- `_coalesce_pending(rows)` returns response dictionaries sorted by `(start_ts, end_ts, state)`; derived entries are `pending`, while durable entries carry attempt/due/error metadata and override an exact derived `(start_ts, end_ts)` key.
- `get_pending_aggregation(...)` is read-only, selector-validated, sentinel-bounded, and consumed unchanged by the existing GET route.
</interfaces>
</context>

<probe_coverage>
- TEL-02 adjacency: exact touching 300-second intervals coalesce only when `next.start_ts == previous.end_ts`; a gap or overlap is not treated as adjacency.
- TEL-02 empty: zero eligible source/durable rows returns `()` and the API serializes an empty `aggregation_pending` array.
- TEL-02 ordering: reverse-seeded host/service buckets return deterministic ascending half-open intervals.
- TEL-04 adjacency: pending work never extends beyond the union of exact contributing source buckets.
- TEL-04 empty: absence of pending work creates no unavailable coverage reason and no pending range.
- TEL-04 ordering: host/service pending disclosure stays stable and separate from the already ordered five-state coverage partition.
- TEL-01, TEL-03, and TEL-05 unclassified assumptions remain explicitly flagged in frontmatter with their regression rationale.
</probe_coverage>

## Artifacts this phase produces

- New production symbols/migrations/files: none; this plan corrects existing `_pending_source_rows()` and `_coalesce_pending()` behavior only.
- New test `test_host_five_minute_pending_uses_exact_half_open_intervals_and_true_adjacency()`.
- New test `test_service_five_minute_pending_uses_exact_half_open_intervals_and_true_adjacency()`.
- New test `test_empty_and_reverse_seeded_five_minute_pending_ranges_are_deterministic()` (table-driven for host and service).

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace exact host and service five-minute backlog through the existing history response</name>
  <reversibility rating="reversible">The change corrects two local read projections and a coalescing predicate behind the existing response contract; undo requires no schema or data migration.</reversibility>
  <files>dashboard/beacon/repositories.py, tests/test_historical_telemetry_api.py</files>
  <read_first>
    - `.planning/phases/02-bounded-telemetry-retention/02-CONTEXT.md` — D-02, D-05, D-07, D-08, and D-09 require exact retained tiers, no invented intervals, exact requested bounds, and separate pending disclosure.
    - `.planning/phases/02-bounded-telemetry-retention/02-VERIFICATION.md` — repair both named `five_query` projections and prove exact/coalesced ranges.
    - `.planning/phases/02-bounded-telemetry-retention/02-09-SUMMARY.md` — preserve completed-tier ownership, derived/durable precedence, source visibility, and configured caps.
    - `dashboard/beacon/repositories.py` — change only the two five-minute end projections and exact pending adjacency; retain fixed SQL, placeholders, limits, and durable metadata precedence.
    - `tests/test_historical_telemetry_api.py` — extend the existing host/service five-minute fallback tests with direct repository and Flask envelope assertions.
    - `dashboard/app.py` — verify the route still serializes repository output without a second interval transform.
  </read_first>
  <behavior>
    - "For host and service, one 300-second source at B returns one derived pending entry [B, B+300)."
    - "For host and service, sources at B and B+300 coalesce to [B, B+600); sources at B and B+600 remain two ordered entries."
    - "For host and service, reverse insertion order yields the same ascending result and no source rows yield an empty tuple/array."
    - "An exact durable pending/failed row replaces its derived entry and retains state, attempt_count, next_retry_ts, and error_class; no different-width interval is accidentally swallowed."
  </behavior>
  <action>Write the host/service RED matrix first using real `host_metric_rollups` and `service_rollups` rows with `bucket_seconds=300`, deliberately reverse insertion order, exact touching buckets, a one-bucket gap, empty tables, and exact durable overrides. In both fixed five-minute SQL shapes inside `_pending_source_rows()`, project `end_ts` as `bucket_start + 300` while retaining the existing `bucket_seconds=300` predicate, half-open requested-range intersection, completed-hour replacement exclusion, ascending order, and bound limit. Tighten `_coalesce_pending()` so only two `pending` entries whose boundaries exactly touch coalesce; do not use overlap-or-touch comparison. Keep deterministic `(start_ts, end_ts, state)` sorting, leave non-adjacent/overlapping entries separate, retain durable exact-key precedence, and preserve the sentinel overflow failure. Assert direct repository tuples and the existing Flask `aggregation_pending` JSON for both selectors; pending intervals must not add or replace a five-state coverage interval. Run the focused matrix, then every Phase 2 test and the full suite without weakening boundary, fallback, migration, worker-authority, or point-budget assertions.</action>
  <acceptance_criteria>
    - Both 300-second projections return exact half-open bucket ends and no 3,600-second derived range remains for a five-minute source.
    - Exact adjacency, non-adjacency, overlap, empty input, reverse seed order, and exact durable override are asserted for host and service.
    - Coalesced output equals only the union of truly adjacent contributing buckets and remains deterministic ascending.
    - Repository functions remain read-only, fixed-shape, parameterized, sentinel-bounded, and promptly closed by the unchanged route.
    - Existing coverage vocabulary, selector validation, configured resolution/point budget, completed-tier ownership, and shared raw-host job behavior remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_historical_telemetry_api.py -k "five_minute and pending" -x</automated>
  </verify>
  <done>Host and service five-minute backlog is disclosed over exactly the source intervals that exist, with candid empty, adjacency, ordering, and durable-state behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| validated selector/range -> SQLite repository | Untrusted request values may reach only fixed, bound, budget-limited read queries. |
| persisted source/job rows -> pending response | Read-only projection must not overstate work, merge false adjacency, or alter durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-G18 | Spoofing | derived `aggregation_pending` interval | high | mitigate | Project exact 300-second bucket ends and prove host/service one, adjacent, gapped, and empty cases through the real response. |
| T-02-G19 | Tampering | pending coalescing | high | mitigate | Coalesce only equality-touching pending boundaries, preserve durable exact-key precedence, and never mutate SQLite in repository reads. |
| T-02-G20 | Denial of Service | pending backlog query | high | mitigate | Keep fixed selector SQL, bound cutoffs/ranges/limits, ascending indexed queries, configured sentinel limits, and overflow rejection. |
| T-02-G21 | Information Disclosure | repository/validation errors | low | accept | Existing fixed 400 responses expose no SQL, path, worker epoch, or raw exception detail; telemetry is intended trusted-LAN output. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---|---|---|
| V4 Access Control | Applicable as preservation | Pending reads remain behind the existing GET-only route and Host/Origin handling; this plan adds no SQLite mutation path. |
| V5 Validation, Sanitization and Encoding | Applicable | Preserve exact host/metric or service/port validation, fixed query text, bound values, positive limits, and strict overflow handling. |
| V2 Authentication | Not applicable | The trusted-LAN read repair adds no account or authentication mechanism. |
| V3 Session Management | Not applicable | Pending history reads remain stateless and create no session. |
| V6 Stored Cryptography | Not applicable | The response contains intended operational metadata and no secret or cryptographic material. |
</threat_model>

<verification>
- Focused exact pending gate: `uv run --project dashboard python -m pytest -q tests/test_historical_telemetry_api.py -k "five_minute and pending" -x`.
- Phase 2 gate: `uv run --project dashboard python -m pytest -q tests/test_telemetry_retention.py tests/test_historical_telemetry_api.py tests/test_migrations.py`.
- Full project gate: `uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- Host and service derived five-minute work is exactly 300 seconds per source bucket.
- Only exact touching pending buckets coalesce; empty and reverse-seeded cases remain candid and deterministic.
- Durable metadata precedence and all response/query bounds remain intact.
- Focused, Phase 2, migration, and full project suites pass.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-telemetry-retention/02-11-SUMMARY.md` when done.
</output>
