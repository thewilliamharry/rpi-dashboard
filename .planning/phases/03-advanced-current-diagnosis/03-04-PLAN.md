---
phase: 03-advanced-current-diagnosis
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - dashboard/advanced.html
  - dashboard/advanced.js
  - dashboard/advanced.css
  - dashboard/index.html
  - dashboard/app.js
  - tests/test_advanced_ui.py
autonomous: true
requirements: [DIA-01, DIA-03, DIA-08, UX-02]

must_haves:
  truths:
    - "UI-15 empty/services: zero matches renders `No services match these filters` and `Clear filters to view every monitored service, or wait for the next discovery result.` without replacing the table with cards."
    - "UI-16 loading/services: the service table keeps semantic headers and labelled skeleton rows while current diagnosis is loading."
    - "UI-17 error/services: refresh failure retains the prior table/filter/count/expansion evidence, labels the failed update, and keeps retry available."
    - "UI-18 populated/services: every snapshot service renders one collapsed row with the required fixed columns and one associated expandable diagnostic row."
    - "UI-19 partial/services: missing latency, error, tags, health rule, timestamps, or gap evidence is explicit and never drops the service or corrupts unrelated fields."
    - "UI-20 overflow/services: below 960px the labelled table region scrolls horizontally, Name + port stays sticky, long values remain accessible, and no fixed field is hidden."
    - "UI-21 zero-one-many/services: matching copy is always `N of M services`, including zero and one, and the underlying total remains truthful through filters."
    - "UI-22 long-text/services: collapsed values use accessible one-line truncation where specified and expanded detail exposes complete service names, failure classes, tags, rules, timestamps, TLS, errors, and gap evidence."
    - "Collapsed rows show Name + port, textual status/marker, latency or failure class, up/down-since duration, criticality, and freshness; expanded rows show all D-07 evidence."
    - "Default order is critical failures, other failures, stale/unknown, then healthy with pinned-order/port ties; keyboard user sorts announce direction and remain active until clear, refresh, or Reset operational order per D-06."
    - "Search matches service name, port, or tag; Status, Criticality, Freshness, and Tag filters combine immediately, persist as local preferences, show N of M, and clear together per D-15."
    - "Multiple details remain expanded through filtering/sorting, every disclosure exposes Show details/Hide details and aria-expanded, Collapse all details appears only when needed, and expanded ports never persist per D-08/D-14."
    - "TLS posture is a trust annotation separate from textual availability, freshness, failure, and gap evidence in every theme and density."
    - "The dashboard top bar has an ordinary same-tab `Advanced diagnosis` link in both themes; `/advanced` has a clear `Dashboard` return and both directions preserve beacon-theme per D-04/UX-02."
    - "Dashboard scroll position is captured before entry and restored once on return when practical; stale session values do not cause repeated or cross-tab restoration."
    - "At exactly 960px the desktop rail/table contract applies and at 959px the horizontal navigation/sticky first-column contract applies; at exactly 720px summaries retain desktop grouping and at 719px they stack."
    - "Every compact/icon control remains at least 44px by 44px at and around supported width boundaries, while table rows remain at least 44px before expanded detail."
    - "Freshness and sorting use raw server seconds/numeric values; relative prose may round for readability but exact timestamp/cadence/latency evidence and deterministic tie order remain available."
    - "Light and dark expose the identical sections, fields, filters, settings, refresh actions, service interactions, empty/error states, and navigation; density changes only layout."
    - "All 36 approved UI considerations are represented across Plans 03-03 and 03-04 and pass the combined dark/light desktop/narrow fixture matrix."
  artifacts:
    - path: "dashboard/advanced.js"
      provides: "Service filtering, operational/user sorting, semantic table rendering, multi-disclosure state, matching counts, and reset/collapse actions"
      contains: "renderServices"
    - path: "dashboard/advanced.html"
      provides: "Semantic service filters/table/disclosure controls and Dashboard return link"
      contains: "Collapse all details"
    - path: "dashboard/advanced.css"
      provides: "Fixed service columns, sticky identity, labelled horizontal scrolling, truncation/detail wrapping, and accessible interactive states"
      contains: "position: sticky"
    - path: "dashboard/index.html"
      provides: "Same-tab Advanced diagnosis top-bar entry"
      contains: "href=\"/advanced\""
    - path: "dashboard/app.js"
      provides: "One-shot dashboard scroll capture/restore while retaining current theme and polling behavior"
      contains: "sessionStorage"
    - path: "tests/test_advanced_ui.py"
      provides: "UI-15 through UI-22 service matrix, keyboard/accessibility, round-trip theme/scroll, exact breakpoint, and full capability-parity coverage"
      contains: "test_theme_or_return"
  key_links:
    - from: "dashboard/advanced.js::renderServices"
      to: "snapshot.services"
      via: "local filter -> operational/user stable sort -> semantic tbody/detail rows; no network mutation"
      pattern: "applyServiceFilters|sortServices|aria-expanded|aria-sort"
    - from: "dashboard/advanced.js::savePreferences"
      to: "service filter controls"
      via: "persist only query/status/criticality/freshness/tag values; expansions and user sort remain transient"
      pattern: "filters|expanded|beacon-advanced-preferences-v1"
    - from: "dashboard/index.html"
      to: "/advanced"
      via: "ordinary same-tab anchor with a narrow click hook that records current scroll in sessionStorage"
      pattern: "Advanced diagnosis|/advanced"
    - from: "dashboard/app.js"
      to: "dashboard scroll position"
      via: "one-shot sessionStorage restore after Dashboard return without changing beacon-theme or polling"
      pattern: "sessionStorage|scrollTo|beacon-theme"
    - from: "tests/test_advanced_ui.py"
      to: "03-UI-SPEC.md"
      via: "named source/Playwright assertions enumerate UI-01 through UI-36 and both breakpoint sides"
      pattern: "UI-01|UI-36|959|960|719|720"
  edge_cases:
    - requirement_id: "UX-02"
      category: "boundary"
      statement: "At 960px the desktop rail applies and at 959px the narrow tabs/sticky identity apply; at 720px summaries remain grouped and at 719px stack; 44px targets hold at every tested boundary."
      verification: "backstop"
    - requirement_id: "UX-02"
      category: "precision"
      statement: "Theme/scroll round trips preserve exact stored theme and one-shot integer scroll offset; service sort/freshness operate on raw numeric evidence and use pinned order then port for exact ties rather than rounded display text."
      verification: "backstop"
  prohibitions:
    - requirement_id: "DIA-03"
      category: "product"
      status: "unresolved"
      statement: "The advanced Services section must not regress into preview cards, hide a required diagnostic column at narrow widths, or reduce multi-service comparison to one open row."
    - requirement_id: "UX-02"
      category: "transparency"
      status: "unresolved"
      statement: "Theme or density must not remove data, filters, settings, refresh controls, navigation, diagnostics, or important state copy."
    - requirement_id: "DIA-08"
      category: "safety"
      status: "unresolved"
      statement: "Service rows and filters must not become links or controls for scans, metadata edits, retries, restarts, cleanup, reconfiguration, or remote service actions."
---

<objective>
Finish Phase 3 with the compact all-service diagnosis table, exact local filtering/comparison interactions, and a theme/scroll-preserving round trip from the existing dashboard.

Purpose: Make every monitored service diagnosable at once while proving the full approved workspace remains equivalent, accessible, and usable across themes and viewport boundaries.
Output: Complete Services interactions, dashboard/advanced navigation continuity, exact breakpoint/accessibility behavior, the final eight UI considerations, and the phase-wide source coverage gate.
</objective>

<execution_context>
@/Users/william/.codex/gsd-core/workflows/execute-plan.md
@/Users/william/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md
@.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md
@.planning/phases/03-advanced-current-diagnosis/03-RESEARCH.md
@.planning/phases/03-advanced-current-diagnosis/03-VALIDATION.md
@.planning/phases/03-advanced-current-diagnosis/03-PATTERNS.md
@.planning/phases/03-advanced-current-diagnosis/03-03-SUMMARY.md
@dashboard/index.html
@dashboard/app.js
@dashboard/advanced.html
@dashboard/advanced.js
@dashboard/advanced.css
@tests/test_advanced_ui.py
@tests/test_ui_states.py
@tests/test_ui_contract.py

<interfaces>
- Plan 03-02 `snapshot.services[]` supplies all required current fields and the default operational order; Plan 03-03 `state` already carries filters, sort, expanded ports, preferences, and one snapshot/timer owner.
- Existing dashboard `applyTheme(light)` toggles `document.documentElement.classList`, updates `aria-pressed`, and writes only `beacon-theme`; navigation changes must not duplicate or rename this contract.
- Existing dashboard top bar is `dashboard/index.html` lines 18-27; add an ordinary anchor without changing warning markup/order, polling, scan, metadata, or theme controls.
- Service table sorting/disclosure uses native buttons, `aria-sort` on the active column header, `aria-expanded` on each details button, stable detail-row IDs, and the existing polite `advanced-status` announcer.
- Session scroll state is tab-local, one-shot, and non-sensitive; use `sessionStorage`, validate a finite non-negative integer, restore once, remove the key, then focus without causing a second scroll when feasible.
</interfaces>
</context>

## Artifacts this phase produces

- New `dashboard/advanced.js` functions `applyServiceFilters()`, `operationalServiceCompare()`, `stableServiceSort()`, `renderServices()`, `toggleServiceDetails()`, `collapseAllDetails()`, `resetOperationalOrder()`, and `updateMatchingCount()`.
- New service UI state fields `serviceSort`, `expandedPorts`, and persisted `filters.{query,status,criticality,freshness,tags}`; sort, expansions, and transient errors remain memory-only.
- New `dashboard/advanced.html` service search/status/criticality/freshness/tag controls, `Clear all filters`, `Reset operational order`, `Collapse all details`, `N of M services`, semantic table, labelled scroll region, and stable detail/live-region IDs.
- New advanced CSS classes for sticky Name + port, fixed visible columns, collapsed ellipsis, expanded wrapped details/tags, 44px rows/controls, and keyboard focus/selected-sort states.
- New dashboard top-bar anchor `#advanced-diagnosis-link` and tab-local scroll key `beacon-dashboard-scroll-position`; helper functions `captureDashboardScroll()` and `restoreDashboardScroll()` preserve existing `beacon-theme` behavior.
- New `tests/test_advanced_ui.py` cases for `UI-15`–`UI-22`, `services`, `filters`, `keyboard_sort`, `multi_disclosure`, `theme_or_return`, `breakpoint_boundary`, `precision`, and the combined `UI-01`–`UI-36` coverage inventory.
- No new API field, database table, package, external service, mutation route, monitoring callback, or history chart is created by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the sortable, filterable, multi-expand service diagnosis table</name>
  <files>tests/test_advanced_ui.py, dashboard/advanced.html, dashboard/advanced.js, dashboard/advanced.css</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` — implement D-05/D-06/D-07/D-08/D-15 exactly and retain D-09 through D-12 evidence separation.
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` — use fixed collapsed columns, exact empty copy, user-sort lifetime, filter/count/clear actions, multi-disclosure, TLS annotation, sticky narrow identity, and UI-15 through UI-22.
    - `.planning/phases/03-advanced-current-diagnosis/03-02-SUMMARY.md` — consume the stable service payload/default order and do not reconstruct missing server evidence.
    - `.planning/phases/03-advanced-current-diagnosis/03-03-SUMMARY.md` — extend the established page state/preferences/renderer without creating a second fetch or timer.
    - `dashboard/advanced.js` — use the existing route-local state, status announcer, DOM construction, and savePreferences projection.
    - `tests/test_advanced_ui.py` — add service fixtures and keyboard/browser assertions without removing the prior 28 UI consideration cases.
  </read_first>
  <behavior>
    - "Zero, one, many, partial, long, loading, and failed service fixtures satisfy UI-15 through UI-22 in both themes and at desktop/narrow widths."
    - "Default operational order, every sortable column, ascending/descending toggle, stable ties, reset, refresh reset, and filter-clear reset are keyboard tested and announced."
    - "Search/name/port/tag plus combinable status/criticality/freshness/tag filters update N of M immediately, persist only values, and never issue a request."
    - "Two or more rows can remain open, keep stable aria-expanded/detail associations through local reorder/filter, and collapse together; no expansion persists after reload."
  </behavior>
  <action>Write UI-15 through UI-22 and interaction tests first, then complete the Services section using the existing snapshot only. Build native search/select/checkbox controls for name-port-tag search and combinable Status, Criticality, Freshness, and Tag filters; apply them locally, deduplicate services, always show `N of M services`, persist only filter values under the existing preference schema, and implement exact Clear all behavior per D-15. Render a real table with fixed collapsed Name + port, Status, Latency / failure class, State duration, Criticality, and Freshness columns; use server values and non-colour text/markers, stable accessible names, and D-06 default order with pinned/port ties. Implement native sort buttons and `aria-sort`; user activation toggles direction, announces field/direction, and supersedes operational order until Clear all, a successful snapshot refresh, or Reset operational order. Render one associated detail row per service containing complete name/failure text, tags, effective health rule, exact probe timestamp, selected cadence, TLS trust annotation, last error, freshness, and collection-gap evidence. Track expanded ports in one in-memory Set, permit multiple, expose Show details/Hide details with aria-expanded, and show Collapse all details only while nonempty; do not persist expansions or transient selection. Below 960px keep the table in a labelled horizontal-scroll region with sticky Name + port, never switch to cards or omit required fields. Keep long content accessible via programmatic names/full detail and preserve UI-17 prior-state behavior after refresh failure.</action>
  <acceptance_criteria>
    - UI-15 through UI-22 pass for zero/one/many/loading/error/partial/overflow/long fixtures in light/dark desktop/narrow runs.
    - Every service has one collapsed and one associated detail row with all D-07 fields; missing data is explicit and no service is lost or duplicated.
    - Operational and user sorts are deterministic, keyboard operable, correctly announced, and reset only at the documented boundaries.
    - All five filter dimensions combine locally, update/persist the matching values/count, and Clear all restores the complete operationally ordered table.
    - Multiple disclosures, Collapse all, sticky narrow identity, full accessible long text, TLS separation, and 44px targets are proven in Chromium.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -k "services or filters or keyboard_sort or multi_disclosure or operational_order" -x</automated>
  </verify>
  <done>The operator can compare, search, filter, sort, and expand every current service without losing required evidence or triggering monitoring work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the same-tab dashboard round trip with theme and scroll continuity</name>
  <files>tests/test_advanced_ui.py, dashboard/index.html, dashboard/app.js, dashboard/advanced.html, dashboard/advanced.js</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` — implement D-04 and D-14 while leaving dashboard behavior/data intact.
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` — use exact `Advanced diagnosis` and `Dashboard` copy, same-tab links, beacon-theme continuity, one-shot scroll restore, and focus behavior.
    - `.planning/phases/03-advanced-current-diagnosis/03-PATTERNS.md` — modify only the topbar/theme initialization analogs and do not touch dashboard polling/warnings.
    - `dashboard/index.html` — add one ordinary top-bar link without changing existing warning, metric, service, scan, or metadata DOM.
    - `dashboard/app.js` — add narrow scroll capture/restore around the existing theme/DOMContentLoaded logic without creating another initialization path.
    - `dashboard/advanced.js` — reuse `beacon-theme` and the existing Dashboard link; do not write theme into the advanced preference object.
    - `tests/test_advanced_ui.py` — add a real dashboard-to-advanced-to-dashboard Playwright round trip for both saved themes and scroll positions.
  </read_first>
  <behavior>
    - "A dashboard opened with saved light or dark theme shows Advanced diagnosis, follows it in the same tab, and the advanced root has the identical theme class/state."
    - "Dashboard follows an ordinary same-origin link back, restores a valid captured scroll position once, focuses the return target without undoing the scroll when feasible, and removes the session value."
    - "Missing, malformed, negative, non-finite, or stale scroll values are ignored and cleared without affecting theme, navigation, or dashboard polling."
    - "The dashboard warning order, polls, scan/metadata controls, previews, and compact analytics remain unchanged."
  </behavior>
  <action>Add round-trip RED browser/source tests, then insert one ordinary same-tab `<a>` labelled `Advanced diagnosis` in the existing dashboard top bar and keep the advanced header's ordinary `Dashboard` anchor. Add a narrowly scoped dashboard click handler that records the current non-negative integer scroll offset in tab-local session storage immediately before navigation. On dashboard initialization, validate and consume that value once, restore it after layout is ready, remove it even when invalid, and focus the Advanced diagnosis link with prevent-scroll when the browser supports it; never persist it in localStorage or reuse it across subsequent loads. Apply the existing `beacon-theme` key and `.light` class before advanced paint, keep the key name/value contract unchanged in both documents, and do not mirror it into advanced preferences. Preserve D-04 same-tab navigation and every existing dashboard poll, warning, service card, scan, metadata, preview, and compact analytic behavior; no route query, history state, or server session is added.</action>
  <acceptance_criteria>
    - Both saved themes survive dashboard -> advanced -> dashboard with no flash-induced final mismatch and no capability difference.
    - Exact link copy/hrefs are present, navigation remains same-tab, and keyboard activation follows normal link semantics.
    - A valid scroll offset restores once and is removed; invalid/missing values are harmless, removed, and never affect theme or polling.
    - Existing dashboard UI contract/state suites remain green with warning order and all compact capabilities unchanged.
    - No live data, warning, filter, expansion, or theme value is added to the tab-local scroll record.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py tests/test_ui_contract.py tests/test_ui_states.py -k "theme_or_return or navigation or scroll or topbar or warning" -x</automated>
  </verify>
  <done>The operator can enter and leave Advanced diagnosis from either theme in the same tab and return to the prior dashboard context without changing existing dashboard behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close breakpoint, precision, accessibility, and 36-consideration parity</name>
  <files>tests/test_advanced_ui.py, dashboard/advanced.html, dashboard/advanced.js, dashboard/advanced.css</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` — treat every approved layout, copy, state, keyboard, focus, density, colour, typography, spacing, and UI-01 through UI-36 item as binding.
    - `.planning/phases/03-advanced-current-diagnosis/03-VALIDATION.md` — finish desktop/narrow dark/light Playwright coverage and keep focused/full feedback commands below 60 seconds where specified.
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` — re-check D-01 through D-16 and ensure no deferred historical/control scope entered the page.
    - `.planning/phases/03-advanced-current-diagnosis/03-03-SUMMARY.md` — retain UI-01 through UI-14/UI-23 through UI-36 evidence.
    - `dashboard/advanced.html` — verify native semantics, focus targets, labels, table relationships, and exact copy.
    - `dashboard/advanced.js` — verify keyboard/polite announcements, raw numeric sorting, server freshness use, and transient-state boundaries.
    - `dashboard/advanced.css` — test both sides of exact 960/720 breakpoints, 44px targets, sticky cells, reflow, wrapping, and theme/density parity.
    - `tests/test_advanced_ui.py` — add an explicit 36-ID inventory so a future omission fails mechanically.
  </read_first>
  <behavior>
    - "Tests enumerate UI-01 through UI-36 exactly once and exercise their owning fixture/assertion without gaps or duplicates."
    - "959/960 and 719/720 viewport pairs prove the exact rail/tab, sticky table, and summary-stack boundaries; controls/rows measure at least 44px."
    - "Keyboard-only section navigation, filters, sorting, disclosure, collapse/reset/clear, refresh/pause, density/range, and return links have visible focus, correct roles/states, and polite announcements."
    - "Raw numeric timestamps/cadence/latency/order drive decisions while exact evidence remains exposed; colour is never the sole state signal."
    - "A capability inventory is identical in comfortable/compact light/dark runs and all negative source contracts remain true."
  </behavior>
  <action>Add the explicit 36-consideration inventory and boundary/precision/parity RED checks, then close any markup/controller/style gaps they reveal. At exactly 960px keep the desktop rail; at 959px use horizontal tabs and sticky Name + port. At exactly 720px retain grouped summaries; at 719px stack one column. Measure every compact/icon control and collapsed row at 44px minimum, make selected tabs remain visible, reflow header controls, contain long warning/navigation/update/table/settings evidence, and preserve all fields. Verify native buttons/links/selects/table semantics, label every scrolling region, set and announce aria-sort/aria-expanded/selected section, move section focus, and keep visible focus using the accent role. Drive sorting and freshness display from raw server numeric evidence; display relative prose without losing exact timestamp/cadence/latency in detail/tooltip, and keep stable port ties. Run one light/dark comfortable/compact capability inventory that compares section destinations, fields, filters, controls, settings, interactions, and state copy. Keep status text plus a non-colour marker, existing safety order, local-only controls, no inline/remote asset, no history chart/fetch, no mutation fetch/action, no preview-card service conversion, and no persistence of expansions/live evidence.</action>
  <acceptance_criteria>
    - The UI consideration inventory contains exactly UI-01 through UI-36, each linked to a passing state assertion and no unresolved/backstop consideration.
    - Exact 959/960 and 719/720 tests plus 44px measurements pass in Chromium without overlap, missing fields, or inaccessible overflow.
    - All interactive elements work by keyboard with correct native semantics, focus, state attributes, and announcements; every state has text/non-colour evidence.
    - Raw-value precision/tie tests pass and exact timestamps/cadence/latency remain available despite relative/rounded presentation.
    - Light/dark and density capability inventories are identical, and focused Phase 3 plus full project suites pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -k "ui_consideration or breakpoint or precision or accessibility or parity" -x</automated>
    <human_check>On Raspberry Pi-class hardware with representative services, run the 5-second advanced refresh for 15 minutes and record that interaction stays responsive and no visible sampling gap or sustained SQLite contention appears; this is the only manual capacity check from 03-VALIDATION.md.</human_check>
  </verify>
  <done>All 36 approved UI considerations, exact edge boundaries, accessibility interactions, raw-value precision, and theme/density capability parity are mechanically represented and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| service snapshot -> local table interactions | Untrusted/long service metadata is filtered, sorted, announced, and rendered without becoming executable or operational input. |
| dashboard <-> advanced navigation | Theme and tab-local scroll state cross document loads and must not become a server/session or durable live-data channel. |
| responsive visual state -> operator judgment | Layout, color, truncation, and rounding must not hide fields or misrepresent service/security/freshness evidence. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-17 | Tampering | service table controls | high | mitigate | Filter/sort/disclosure entirely in memory, source-test no mutation/action fetch, and render metadata through textContent/native attributes only. |
| T-03-18 | Spoofing | service availability/TLS/freshness presentation | high | mitigate | Keep separately labelled typed facts, non-colour markers, exact detail evidence, raw numeric comparisons, and deterministic sort ties. |
| T-03-19 | Denial of Service | service filtering/rendering | medium | mitigate | Operate on the already bounded snapshot, deduplicate once, avoid network requests per interaction, and test long/many fixtures at both widths. |
| T-03-20 | Information Disclosure | theme/scroll/filter browser storage | low | accept | Theme/presentation/filter/one-shot scroll are intentional local-device preferences in a trusted-LAN single-operator product; no snapshot, error, secret, or worker evidence is stored. |
| T-03-21 | Spoofing | theme/density/responsive capability | medium | mitigate | Compare exact capability inventories and UI-01 through UI-36 across themes, densities, and both sides of the locked breakpoints. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|---|---|---|
| V4 Access Control | Applicable | Service interactions remain presentation-only and expose no action/mutation route. |
| V5 Validation, Sanitization and Encoding | Applicable | DOM text rendering, stable numeric comparators, defensive session/local storage parsing, and fixed option/filter values. |
| V2 Authentication | Not applicable | Navigation carries no identity or account state. |
| V3 Session Management | Not applicable | Tab-local scroll restoration is not an authenticated session and is consumed once. |
| V6 Stored Cryptography | Not applicable | No secret or cryptographic material is handled. |
</threat_model>

<source_coverage_audit>

| Source | ID | Feature / decision | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | — | Enter from either theme and quickly diagnose current Pi, every service, effective settings, and collection health | 03-01 tracer, 03-02 snapshot/evidence, 03-03 workspace, 03-04 service/nav closure | COVERED |
| REQ | TEL-06 | Retention, resolution, pressure, worker, gaps, and background-job health | 03-02 API + 03-03 Pipeline UI | COVERED |
| REQ | DIA-01 | Dedicated advanced page from either theme | 03-01 route/tracer + 03-03 shell + 03-04 round trip | COVERED |
| REQ | DIA-02 | Current host metrics, identity, sample time, freshness | 03-01 tracer + 03-02 full contract + 03-03 Host UI | COVERED |
| REQ | DIA-03 | Every service's current status, latency/failure, duration, criticality, tags, rule | 03-02 service projection + 03-04 service table/details | COVERED |
| REQ | DIA-08 | Effective settings plus local presentation/refresh/range/filter preferences, no remote control | 03-02 read-only payload + 03-03 controls/preferences + 03-04 filters/no-actions | COVERED |
| REQ | UX-02 | Clear dashboard/advanced movement without losing theme | 03-04 same-tab links/theme/scroll tests | COVERED |
| RESEARCH | — | One bounded GET-only `/api/advanced/current` SQLite snapshot | 03-01/03-02 | COVERED |
| RESEARCH | — | Durable worker-owned job outcome evidence under exact authority | 03-02 Migration 8/dispatch integration | COVERED |
| RESEARCH | — | Separate dependency-free `/advanced` document, JS, and CSS | 03-01/03-03 | COVERED |
| RESEARCH | — | Server-derived cadence freshness with exact evidence | 03-01 helper + 03-02 all streams + 03-03/03-04 rendering | COVERED |
| RESEARCH | — | TLS/availability/freshness/gaps/pending/expiry remain distinct | 03-02 typed API + 03-03/03-04 UI | COVERED |
| RESEARCH | — | No monitoring mutation, remote control, history scan/chart, client fan-out, or new package | All plans' contracts/tests/threat mitigations | COVERED |
| RESEARCH | — | Bounded current queries exclude thumbnails and legacy uptime/history work | 03-02 query instrumentation | COVERED |
| RESEARCH | — | Responsive, accessible vanilla UI and local allowlisted preferences | 03-03/03-04 | COVERED |
| CONTEXT | D-01 | Overview-first five-section workspace | 03-03 Task 1 | COVERED |
| CONTEXT | D-02 | Active exceptions before three summaries | 03-03 Task 1 | COVERED |
| CONTEXT | D-03 | Persistent section navigation with replaced detail | 03-03 Task 1 | COVERED |
| CONTEXT | D-04 | Same-tab /advanced, Dashboard return, theme/preferences, scroll restore | 03-04 Task 2 | COVERED |
| CONTEXT | D-05 | One compact sortable expandable service table, no preview cards | 03-04 Task 1 | COVERED |
| CONTEXT | D-06 | Operational priority with pinned-order preservation | 03-02 Task 2 + 03-04 Task 1 | COVERED |
| CONTEXT | D-07 | Exact collapsed/expanded service fields | 03-02 Task 2 + 03-04 Task 1 | COVERED |
| CONTEXT | D-08 | Multiple expansion plus collapse all | 03-04 Task 1 | COVERED |
| CONTEXT | D-09 | Distinct warning/safety/freshness/pipeline conditions | 03-02 Task 2 + 03-03 Tasks 1-2 | COVERED |
| CONTEXT | D-10 | Four-state freshness, age, exact time, cadence | 03-01/03-02/03-03 | COVERED |
| CONTEXT | D-11 | No stale-data causal inference; show worker-fresh/stream-stale together | 03-02 Task 2 + 03-03 Task 2 | COVERED |
| CONTEXT | D-12 | Actionable gaps global; older resolved gaps in Pipeline | 03-02 Task 2 + 03-03 Task 2 | COVERED |
| CONTEXT | D-13 | Fixed auto-refresh, pause, refresh now, last update | 03-03 Task 3 | COVERED |
| CONTEXT | D-14 | Persist exact explicit preferences, not expansions/transient state | 03-03 Task 3 + 03-04 Tasks 1-2 | COVERED |
| CONTEXT | D-15 | Name/port/tag search plus combinable filters/count/clear | 03-04 Task 1 | COVERED |
| CONTEXT | D-16 | Comfortable light/compact dark defaults with override/capability parity | 03-03 Task 3 + 03-04 Task 3 | COVERED |
| UI | UI-01–UI-14 | Workspace, Overview, and Host state considerations | 03-03 Task 1/3 | COVERED |
| UI | UI-15–UI-22 | Service table/filter/detail state considerations | 03-04 Task 1 | COVERED |
| UI | UI-23–UI-30 | Pipeline state considerations | 03-03 Task 2 | COVERED |
| UI | UI-31–UI-36 | Settings state considerations | 03-03 Task 2/3 | COVERED |
| PROBE | TEL-06 edges | adjacency, empty/null/single, stable equal ordering | 03-02 must_haves/tests | COVERED |
| PROBE | DIA-01/DIA-02 | explicit unclassified flagged assumptions | 03-01/03-02 must_haves | COVERED |
| PROBE | DIA-03 edges | exact freshness adjacency, zero/one/null services, stable ties | 03-02/03-04 must_haves/tests | COVERED |
| PROBE | DIA-08 edges | combinable adjacency, empty/corrupt values, canonical order | 03-02/03-03/03-04 | COVERED |
| PROBE | UX-02 edges | exact layout/target boundaries and numeric/storage precision | 03-04 Tasks 2-3 | COVERED |
| PROBE | Product-value/safety prohibitions | no effects/actions, causal claims, evidence conflation, cards, theme capability loss, or phase-crossing charts | descriptor-less unresolved prohibitions in all four plans | COVERED |

</source_coverage_audit>

<verification>
- Service and round-trip gate: `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k "services or theme_or_return or breakpoint or accessibility or parity" -x`.
- Focused Phase 3 gate: `uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -x`.
- Existing UI compatibility gate: `uv run --project dashboard python -m pytest -q tests/test_ui_contract.py tests/test_ui_states.py tests/test_ui_safety_integration.py -x`.
- Full phase gate: `uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- All six Phase 3 requirement IDs and all four roadmap success criteria have direct automated evidence.
- D-01 through D-16 and UI-01 through UI-36 are implemented without scope reduction or silent omission.
- Every spec-less edge probe and kept descriptor-less prohibition is explicit in must_haves and mapped to implementation/review evidence.
- The operator can diagnose every service, filter/sort/compare multiple details, and round-trip from either theme with scroll/theme continuity.
- No high-severity threat remains accepted or transferred, and the complete focused/full suites pass before UAT.
</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-04-SUMMARY.md` when done.
</output>
