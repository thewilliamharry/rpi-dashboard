---
phase: 03-advanced-current-diagnosis
plan: 07
type: execute
wave: 7
depends_on: [03-06]
files_modified:
  - dashboard/beacon/diagnosis.py
  - dashboard/beacon/repositories.py
  - dashboard/advanced.js
  - tests/test_advanced_diagnosis_api.py
  - tests/test_advanced_ui.py
autonomous: true
gap_closure: true
requirements: [TEL-06, DIA-02, DIA-03, DIA-08]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An active telemetry_streams gap with a non-null open_gap_start_ts and zero telemetry_coverage rows is synthesized into exactly one open, actionable pipeline.gaps item and promoted into the exceptions list; a stream with open_gap_start_ts NULL and zero coverage rows still returns an empty gaps list for that stream."
    - "Missing or stale host evidence (no system_stats row, or a sample older than 4x the expected cadence) always produces a host_freshness exception; the Overview's 'reporting normally' / 'No active exceptions' copy never renders while that exception -- or any other exception -- is present."
    - "Pending-aggregation and telemetry-stream reads report truncation exactly at their bound and never before it: 31 and 32 pending rows both report truncated=false with all rows returned, 33 reports truncated=true with only the first 32; 64 streams report truncated=false with all 64 returned, 65 reports truncated=true with only the first 64, with any open-gap or stale stream surviving the cap ahead of quiet ones."
    - "Zero pending jobs or zero telemetry streams return an explicit empty-but-truthful collection (items: [], count: 0, truncated: false), and a stream with zero telemetry_coverage rows but a non-null open_gap_start_ts is never silently treated as gap-free."
    - "A slower, older /api/advanced/current response or error can never be applied after a faster, newer response has already been applied; existing coverage-derived gap items keep their established ordering with synthesized open-gap items deterministically appended rather than interleaved."
    - "The current-diagnosis endpoint remains parameterless, GET-only, no-store, bounded, and effect-free; no new package, external service, monitoring mutation, or history/range work is introduced by this gap-closure plan."
  artifacts:
    - path: "dashboard/beacon/diagnosis.py"
      provides: "Open-stream gap synthesis, host-freshness exception promotion, and sentinel-derived streams/pending truncation shape"
      contains: "host_freshness"
    - path: "dashboard/beacon/repositories.py"
      provides: "Sentinel-bounded stream/pending reads with active-gap/stale-first ordering"
      contains: "streams_truncated"
    - path: "dashboard/advanced.js"
      provides: "Streams truncation disclosure and refresh request-generation guard"
      contains: "requestGeneration"
    - path: "tests/test_advanced_diagnosis_api.py"
      provides: "Open-gap, host-freshness, and pending/stream boundary regressions"
      contains: "streams_truncated"
    - path: "tests/test_advanced_ui.py"
      provides: "Overview/Pipeline truthful-evidence and reverse-order refresh regressions"
      contains: "requestGeneration"
  key_links:
    - from: "dashboard/beacon/repositories.py::read_pipeline_evidence"
      to: "dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis"
      via: "sentinel rows (stream_limit+1 / pending_limit+1) become explicit streams_truncated/pending_truncated booleans consumed instead of inferring truncation from list length"
      pattern: "streams_truncated|pending_truncated"
    - from: "dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis (open_gap_start_ts)"
      to: "dashboard/beacon/diagnosis.py::compose_active_exceptions"
      via: "a synthesized open-stream gap item is appended to pipeline.gaps.items with actionable=true so the existing exception-promotion loop surfaces it without new logic"
      pattern: "open_gap_start_ts|collection_gap"
    - from: "dashboard/beacon/diagnosis.py::get_current_diagnosis (host)"
      to: "dashboard/beacon/diagnosis.py::compose_active_exceptions"
      via: "the composed host payload's freshness state is passed as a new required argument so stale/unknown host evidence becomes an exception before the Overview 'normal' copy can render"
      pattern: "host_freshness"
    - from: "dashboard/advanced.js::refreshCurrentDiagnosis"
      to: "state.requestGeneration"
      via: "every triggered refresh captures its own generation id and both the success and failure branches discard a response belonging to a superseded generation"
      pattern: "requestGeneration"
  edge_cases:
    - requirement_id: "DIA-01"
      category: "unclassified"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION -- the spec-less probe reported an unresolved DIA-01 row without a descriptor or predicate; DIA-01 is SATISFIED per 03-VERIFICATION.md and untouched by this gap-closure plan, so no predicate is inferred."
    - requirement_id: "DIA-02"
      category: "unclassified"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION -- the spec-less probe reported an unresolved DIA-02 row without a descriptor or predicate; DIA-02 is SATISFIED per 03-VERIFICATION.md, and this plan's only host-evidence change (CR-02) is captured explicitly in must_haves.truths above rather than inferred here."
    - requirement_id: "UX-02"
      category: "boundary"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION -- the spec-less probe reported an unresolved UX-02 boundary row but supplied no predicate; UX-02 is SATISFIED per 03-VERIFICATION.md and untouched by this gap-closure plan, so no predicate is inferred."
    - requirement_id: "UX-02"
      category: "precision"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION -- the spec-less probe reported an unresolved UX-02 precision row but supplied no predicate; UX-02 is SATISFIED per 03-VERIFICATION.md and untouched by this gap-closure plan, so no predicate is inferred."
  prohibitions:
    - requirement_id: "TEL-06"
      category: "safety"
      status: "flagged-unverified"
      statement: "MUST NOT report pipeline/collection health as complete, gap-free, or within its stated cap when durable evidence (an open telemetry_streams gap, or a sentinel-detected truncated pending/stream read) contradicts that claim -- kept because this is the exact silent failure CR-01/CR-03/CR-04 exhibited, not routine engineering."
    - requirement_id: "DIA-08"
      category: "safety"
      status: "flagged-unverified"
      statement: "MUST NOT render the Overview 'reporting normally' / 'No active exceptions' copy while current host, service, or pipeline evidence is stale, unknown, or truncated -- kept because a false-normal claim could suppress operator attention to a real fault (CR-02)."
    - requirement_id: "DIA-08"
      category: "transparency"
      status: "flagged-unverified"
      statement: "MUST NOT apply a stale/superseded refresh response as current evidence; a slower older request completing after a newer one must never silently regress already-displayed diagnosis state, safety banners, or the last-success timestamp (WR-01)."
    - requirement_id: "DIA-01"
      category: "n/a"
      status: "dismissed"
      reason: "DIA-01 (navigation/theme continuity) is SATISFIED per 03-VERIFICATION.md and untouched by this plan; Stage 1 recall surfaced only routine engineering (e.g. a broken deep link) already owned by existing route/Playwright tests -- no bespoke values/safety/ethics candidate survives Stage 2."
    - requirement_id: "DIA-02"
      category: "n/a"
      status: "dismissed"
      reason: "DIA-02 (host metrics/freshness display) is SATISFIED per 03-VERIFICATION.md; the one live values/safety concern touching host evidence -- a false-normal Overview claim over stale/unknown host data -- is CR-02 and is captured under the DIA-08/TEL-06 prohibitions above rather than duplicated here."
    - requirement_id: "DIA-03"
      category: "n/a"
      status: "dismissed"
      reason: "DIA-03 (per-service diagnostic fields) is SATISFIED per 03-VERIFICATION.md; Stage 1 recall surfaced only routine engineering (e.g. a missing tag) already owned by existing service-table tests -- no bespoke values/safety/ethics candidate survives Stage 2."
    - requirement_id: "UX-02"
      category: "n/a"
      status: "dismissed"
      reason: "UX-02 (theme/scroll continuity) is SATISFIED per 03-VERIFICATION.md and untouched by this plan; no values/safety/ethics prohibition applies to a client-local presentation preference."
---

<objective>
Close all five open Phase 3 verification gaps -- CR-01, CR-02, CR-03, CR-04 (blockers) and WR-01 (warning) -- so `GET /api/advanced/current` never omits an active collection gap, never declares the system normal over stale/unknown host evidence, never misreports bounded-read truncation, and the browser never regresses newer evidence with a late older response.

Purpose: The phase's entire mandate is a *trustworthy* current-diagnosis snapshot (TEL-06, DIA-08); these five findings are the last ways the snapshot can silently lie about completeness or normality to the operator.
Output: Synthesized/promoted open-stream gap evidence, a host-freshness exception, sentinel-measured (not inferred) pending/stream truncation with active-evidence prioritized ahead of the cap, and a monotonic refresh-ordering guard in the browser -- each covered by a real regression test at its exact boundary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md
@.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md
@.planning/phases/03-advanced-current-diagnosis/03-RESEARCH.md
@.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md
@.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md
@.planning/phases/03-advanced-current-diagnosis/03-05-SUMMARY.md
@.planning/phases/03-advanced-current-diagnosis/03-06-SUMMARY.md
@dashboard/beacon/diagnosis.py
@dashboard/beacon/repositories.py
@dashboard/beacon/telemetry.py
@dashboard/advanced.js
@dashboard/app.py
@tests/test_advanced_diagnosis_api.py
@tests/test_advanced_ui.py

<interfaces>
- `telemetry_streams.open_gap_start_ts` (set by `telemetry.py::detect_collection_gaps`, cleared by `record_observation`) is the ONLY durable representation of a currently active gap; it is never itself a `telemetry_coverage` row, and a stream may simultaneously carry historical closed `telemetry_coverage` rows and a current `open_gap_start_ts`. `read_pipeline_evidence()` already selects `open_gap_start_ts` on every stream row -- no new query is required to read it.
- `compose_pipeline_diagnosis(evidence, settings, *, now)` currently builds `gaps` only from `evidence['gaps']` (closed `telemetry_coverage` rows) and returns a bare list for `streams`; this plan adds one more synthesis pass over `stream_records` for CR-01 and changes `streams`'s returned shape to `{items, count, truncated}` for CR-04, mirroring the existing `gaps`/`aggregation_pending` `{items, count, truncated}` shape exactly.
- `compose_active_exceptions(services, pipeline, *, recovery_required)` has no host argument; this plan adds `host` as its new first positional parameter and one exception branch mirroring the existing `worker_freshness` block shape (`{'kind', 'section', 'priority', 'state'}`) for CR-02.
- `read_pipeline_evidence(conn, *, now, gap_limit=48, stream_limit=64, pending_limit=32)` already proves the sentinel pattern for `gaps` (`gaps_truncated` = fetch `gap_limit+1`, slice, boolean); this plan replicates that exact pattern for `streams`/`pending` (CR-03, CR-04) instead of the current `len(evidence['pending']) >= 32` inference.
- `advanced.js`'s `state` object and `refreshCurrentDiagnosis()` are the sole callers of `apiFetch()`; both `setInterval` (`scheduleRefresh()`) and the `Refresh now` click bind to the same function with no ordering guard today. This plan adds `state.requestGeneration` and a guard at the top of both the success and catch branches (WR-01).
- No task in this plan changes `dashboard/app.py`, `dashboard/beacon/migrations.py`, adds a package, a route, a query parameter, or a database column/table/index -- `open_gap_start_ts` already exists from Migration 5 and no new durable evidence is required.
</interfaces>
</context>

<assumption_delta_decision>
Noun: `pipeline.streams` / `pipeline.aggregation_pending` truncation booleans.
Decision: promote -- the boolean shifts from an inferred `len(list) >= cap` truthiness to a sentinel-measured fact (fetch `cap+1` rows, slice to `cap`, return whether the `(cap+1)`th row existed). Rationale: the inferred form is provably wrong at the exact cap (CR-03 flags exactly 32 rows as truncated; CR-04 has no signal at all for streams), so this plan fully replaces the flawed derivation rather than adding a second signal alongside it -- there is no legitimate reason to keep the old inferred boolean once the sentinel exists.
</assumption_delta_decision>

## Artifacts this phase produces

- `dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis` gains a second synthesis pass appending one gap item per stream with a non-null `open_gap_start_ts` (fields: `stream_kind`, `stream_key`, `start_ts`, `end_ts`, `reason='collection_gap'`, `detail=None`, `open=True`, `actionable=True`).
- `dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis` changes the returned `streams` key from a bare list to `{items, count, truncated}`, matching the existing `gaps`/`aggregation_pending` shape.
- `dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis` changes `aggregation_pending.truncated`'s source from the inferred `len(evidence['pending']) >= 32` to the sentinel-measured `evidence['pending_truncated']`.
- `dashboard/beacon/diagnosis.py::compose_active_exceptions` gains a new required first parameter `host` and a new exception kind `host_freshness` (`{'kind': 'host_freshness', 'section': 'host', 'priority': 1, 'state': ...}`), appended whenever `host['freshness']['state']` is `'stale'` or `'unknown'`.
- `dashboard/beacon/diagnosis.py::get_current_diagnosis` updates its single `compose_active_exceptions(...)` call site to pass `host` as the new first argument.
- `dashboard/beacon/repositories.py::read_pipeline_evidence` returns two new dict keys, `streams_truncated: bool` and `pending_truncated: bool`, each computed from a one-row sentinel fetched beyond `stream_limit`/`pending_limit` and sliced back to the cap before return.
- `dashboard/beacon/repositories.py::read_pipeline_evidence`'s streams query changes its `ORDER BY` to rank rows with a non-null `open_gap_start_ts` first, then rows stale relative to the bound `now` parameter and their own `cadence_seconds`, then `stream_kind ASC, stream_key ASC` -- so active-gap/stale streams are never dropped by the cap ahead of quiet ones.
- `dashboard/advanced.js`'s top-level `state` object gains a new field `requestGeneration` (starts at `0`, incremented on every `refreshCurrentDiagnosis()` call).
- `dashboard/advanced.js::refreshCurrentDiagnosis` gains a per-call `requestId` capture and an early-return guard at the top of both its success and `catch` branches, discarding any response/error whose `requestId` no longer matches `state.requestGeneration`.
- `dashboard/advanced.js::renderPipeline` changes its `streams` local from reading `pipeline.streams` as a bare array to reading `pipeline.streams.items`, and changes the `Streams` heading text to include the new `count`/`truncated` disclosure via `countLabel(streamsField.count, 'stream')`.
- No new file, package, database migration, column, table, index, route, or query parameter is introduced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an open collection gap end-to-end without a coverage row (CR-01)</name>
  <reversibility rating="reversible">Confined to an additive in-memory synthesis pass over already-read evidence; no schema, query, or external-contract change. Fully revertible.</reversibility>
  <files>dashboard/beacon/diagnosis.py, tests/test_advanced_diagnosis_api.py, tests/test_advanced_ui.py</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` -- implement D-12 exactly (promote actionable/open gaps to the global exception summary; keep resolved older gaps in Pipeline detail) and D-11 (report only the observed open-interval fact, never a cause).
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` -- gap 1 (CR-01): "Synthesize and bound open stream gaps, merge them with closed coverage by stream, and add stream-only open-gap API/UI coverage."
    - `.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md` -- CR-01 section, `dashboard/beacon/diagnosis.py:204-215`: `compose_pipeline_diagnosis` builds gap items only from `evidence['gaps']`, so `compose_active_exceptions` has nothing to promote for a stream-only open gap.
    - `dashboard/beacon/diagnosis.py` (current `compose_pipeline_diagnosis` lines 184-263 and `compose_active_exceptions` lines 266-294) -- read the exact existing merge/exception logic before extending it.
    - `dashboard/beacon/telemetry.py:661-742` (`detect_collection_gaps`, `record_observation`) -- confirms `open_gap_start_ts` is durable active-gap evidence, is cleared to NULL only when the stream resumes and a closed `telemetry_coverage` row is written for the resolved interval, and a stream may carry both a closed historical row and a current open gap at once.
    - `dashboard/beacon/repositories.py::read_pipeline_evidence` (110-151) -- confirms `streams` already returns `open_gap_start_ts` per row unmodified by this task; no new query is required.
    - `tests/test_advanced_diagnosis_api.py::test_gap_truncation_uses_one_sentinel_beyond_the_response_cap` (398-425) -- existing pattern for seeding `telemetry_coverage`/`telemetry_streams` fixtures and asserting `pipeline.gaps` shape and exact item order; this task's new fixtures must not disturb this test's existing exact-order assertion.
    - `dashboard/advanced.js:276-281` (`addCollectionRegion(root, 'Collection gaps' ...)`) -- the existing generic gap-item renderer already emits `"${stream_kind}: ${stream_key} — ${open ? 'Open' : 'Resolved'} ${actionable ? 'actionable' : 'historical'} gap."`; this task must produce items with `open`/`actionable` fields that render correctly through this unmodified code.
  </read_first>
  <behavior>
    - "RED first: an API test seeds one telemetry_streams row with open_gap_start_ts set and zero telemetry_coverage rows for that stream; GET /api/advanced/current returns exactly one pipeline.gaps.items entry for that stream with open=true and actionable=true, and exactly one exceptions entry with kind='collection_gap' for that stream."
    - "RED first: a companion API subTest seeds the same stream with open_gap_start_ts=NULL and zero coverage rows; pipeline.gaps.items stays empty for that stream, count reflects only genuinely present items, and no collection_gap exception is produced for it."
    - "RED first: a third API subTest seeds one closed historical telemetry_coverage row for a stream AND sets that same stream's open_gap_start_ts to represent a later still-open interval; pipeline.gaps.items contains both the closed historical item (unchanged start_ts/end_ts/reason/detail) and the new synthesized open item -- merging never drops existing evidence."
    - "RED first: a Playwright test drives realistic API JSON (matching the API test's exact synthesized-item shape) through /advanced; the Pipeline 'Collection gaps' region shows the 'Open ... actionable gap.' copy for the synthesized item and the Overview exception count/list includes it, replacing the current false 'No active exceptions' state."
  </behavior>
  <action>In `compose_pipeline_diagnosis()` (`dashboard/beacon/diagnosis.py`), after the existing loop over `evidence['gaps']` that builds `gaps`/`stream['gaps']` (current lines ~204-217), add a second loop over `stream_records`: for every stream whose `open_gap_start_ts` is not None, synthesize exactly one additional gap item with `stream_kind`/`stream_key` copied from that stream row, `start_ts=stream['open_gap_start_ts']`, `end_ts=max(now, stream['open_gap_start_ts'])`, `reason='collection_gap'`, `detail=None`, `open=True`, `actionable=True` -- `actionable=True` implements D-12's rule that an open/ongoing gap is always promoted to the global exception summary, and the item states only the observed open interval with no inferred cause, per D-11. Append it to both `gaps` and `stream['gaps']`. Append these synthesized items AFTER the existing coverage-derived items in the order `stream_records` already provides -- do not sort or otherwise reorder the already-deterministic coverage-derived items, since `test_gap_truncation_uses_one_sentinel_beyond_the_response_cap` asserts their exact existing order. Build the returned `'gaps': {'items': gaps, 'count': len(gaps), 'truncated': ...}` exactly as today (unchanged truncation source, unchanged for this task). Do not modify `compose_active_exceptions()` in this task -- it already promotes every `pipeline['gaps']['items']` entry whose `actionable` is true, so the newly synthesized items are automatically promoted once emitted. Do not modify `dashboard/beacon/repositories.py` or `dashboard/advanced.js` in this task; the existing `read_pipeline_evidence()` stream query and the existing generic `addCollectionRegion()` gap-item renderer already carry the fields this task adds.</action>
  <acceptance_criteria>
    - An open stream with zero telemetry_coverage rows produces exactly one `pipeline.gaps.items` entry for that stream (`open: true`, `actionable: true`) and exactly one `collection_gap` exception for it.
    - A stream with `open_gap_start_ts` NULL and zero coverage rows produces zero gap items and zero exceptions for that stream.
    - A stream with one closed historical coverage row plus a currently open gap returns both items for that stream, and the closed item's original `start_ts`/`end_ts`/`reason`/`detail` are byte-identical to before this change.
    - `test_gap_truncation_uses_one_sentinel_beyond_the_response_cap`'s existing exact-order assertion over pure closed-coverage fixtures still passes unmodified.
    - The Playwright Pipeline region renders the synthesized item's `Open ... gap.` copy and the Overview region's exception count/list includes it instead of the "No active exceptions" / "reporting normally" copy.
    - No new SQL query, schema change, or new file is introduced; `dashboard/beacon/repositories.py` and `dashboard/advanced.js` are unmodified by this task.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -k "open_stream_gap or open_collection_gap or gap_truncation" -x</automated>
  </verify>
  <done>An active telemetry_streams gap with no telemetry_coverage row is synthesized, merged with any closed evidence for the same stream, and promoted end to end into the Overview exception list and Pipeline detail without a false "No active exceptions" state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote stale/unknown host to an active exception and make pending/stream truncation sentinel-measured (CR-02, CR-03, CR-04)</name>
  <reversibility rating="reversible">Signature/shape and SQL LIMIT/ORDER BY changes are internal read-model adjustments with no schema, migration, or external-contract change. Fully revertible.</reversibility>
  <files>dashboard/beacon/diagnosis.py, dashboard/beacon/repositories.py, dashboard/advanced.js, tests/test_advanced_diagnosis_api.py, tests/test_advanced_ui.py</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md` -- implement D-09 exactly (keep the new `host_freshness` exception distinct from `worker_freshness`/`recovery_required`, never merged), reuse D-10's existing four-state freshness vocabulary unchanged, and follow D-11 (state the observed host freshness fact only, never an inferred cause).
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` -- gaps 2-4 (CR-02, CR-03, CR-04): host-freshness must become an exception; pending truncation must be sentinel-derived not `>= 32`; streams need sentinel count/truncated metadata, rendering, and prioritized actionable evidence.
    - `.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md` -- CR-02/CR-03/CR-04 sections with exact file/line references and suggested fixes.
    - `dashboard/beacon/diagnosis.py::compose_active_exceptions` (266-294) and `::get_current_diagnosis` (320-343) -- current signature/call site to extend with a `host` argument; mirror the existing `worker_freshness` exception shape (`{'kind', 'section', 'priority', 'state'}`) exactly for the new `host_freshness` exception.
    - `dashboard/beacon/repositories.py::read_pipeline_evidence` (110-151) -- the existing `gaps_truncated` sentinel pattern (`gap_limit+1` fetch, slice, boolean) is the pattern to replicate verbatim for `pending`/`streams`.
    - `dashboard/advanced.js::renderPipeline` (207-296), specifically the `streams` local (214) and the `addCollectionRegion(root, 'Streams', ...)` call (257-274) -- current bare-list contract to change to the `{items, count, truncated}` shape already used by `gaps`/`aggregation_pending` (216, 276, 283).
    - `tests/test_advanced_ui.py::test_pipeline_and_settings_render_independent_truthful_regions` (284-336) -- contains the existing bare-array `'streams': [...]` fixture (line 294) that MUST be updated to the new `{items, count, truncated}` shape or this test will fail after this task's JS change.
    - `tests/test_advanced_diagnosis_api.py::test_gap_truncation_uses_one_sentinel_beyond_the_response_cap` (398-425) -- reuse this exact sentinel-boundary test pattern for the new pending/stream boundary tests.
  </read_first>
  <behavior>
    - "RED first: an API test parametrizes host evidence over (a) no system_stats row at all, (b) a system_stats row old enough to be stale (age greater than 4x cadence), (c) a fresh system_stats row; only (a) and (b) produce a host_freshness exception (state 'unknown'/'stale' respectively), (c) produces none."
    - "RED first: an API test seeds 0, 31, 32, and 33 non-succeeded telemetry_rollup_jobs rows; pipeline.aggregation_pending.count equals min(count, 32) and .truncated is true only when count is 33 (not at 0, 31, or 32)."
    - "RED first: an API test seeds 0, 64, and 65 telemetry_streams rows; pipeline.streams.count equals min(count, 64) and .truncated is true only when count is 65; when 65 rows are seeded and the alphabetically-last stream key carries open_gap_start_ts, that stream is still present among the returned 64."
    - "RED first: a Playwright test renders the updated pipeline.streams object shape ({items, count, truncated}) and a stale-host exceptions entry; the 'Streams' heading shows the count/truncated disclosure and the Overview never shows 'reporting normally' while the host exception is present."
  </behavior>
  <action>In `dashboard/beacon/diagnosis.py`, change `compose_active_exceptions(services, pipeline, *, recovery_required)` to `compose_active_exceptions(host, services, pipeline, *, recovery_required)`; immediately after the existing `recovery_required` append, add a host-freshness branch that appends `{'kind': 'host_freshness', 'section': 'host', 'priority': 1, 'state': host['freshness']['state']}` whenever `host['freshness']['state']` is `'stale'` or `'unknown'` (mirror the existing `worker_freshness` block exactly, same priority tier -- this keeps `host_freshness` a distinct exception kind per D-09, reuses the existing `fresh`/`aging`/`stale`/`unknown` vocabulary per D-10, and states only the observed `state` with no inferred cause per D-11). Update the single call site in `get_current_diagnosis()` to `compose_active_exceptions(host, services, pipeline, recovery_required=recovery_required)`. In `dashboard/beacon/repositories.py::read_pipeline_evidence`, normalize `stream_limit` and `pending_limit` into local variables the same way `gap_limit` already is normalized, fetch one sentinel row beyond each cap (`LIMIT <normalized>+1`), slice each result back to its cap, and return two new boolean keys `streams_truncated` and `pending_truncated` computed from whether the sentinel row existed -- mirroring the existing `gaps_truncated` pattern exactly. Additionally change the streams query's `ORDER BY` from plain `stream_kind ASC, stream_key ASC` to first rank by `(CASE WHEN open_gap_start_ts IS NOT NULL THEN 0 ELSE 1 END)`, then by `(CASE WHEN last_observed_ts IS NULL OR (? - last_observed_ts) > 4 * cadence_seconds THEN 0 ELSE 1 END)` bound to the existing `now` parameter, then `stream_kind ASC, stream_key ASC`, so active-gap and stale/unknown streams are never dropped by the cap ahead of quiet ones (bind `now` before the `LIMIT` value, matching `?` occurrence order in the query text). In `dashboard/beacon/diagnosis.py::compose_pipeline_diagnosis`, change the returned `'streams': stream_records` key to `'streams': {'items': stream_records, 'count': len(stream_records), 'truncated': bool(evidence.get('streams_truncated', False))}`, and change `'aggregation_pending'`'s `'truncated'` value from `len(evidence['pending']) >= 32` to `bool(evidence.get('pending_truncated', False))`. In `dashboard/advanced.js::renderPipeline`, change the `streams` local from `Array.isArray(pipeline.streams) ? pipeline.streams : null` to first read `const streamsField = pipeline.streams || {}` then `const streams = Array.isArray(streamsField.items) ? streamsField.items : null`, and change the `addCollectionRegion(root, 'Streams', streams, ...)` heading argument to `` `Streams (${countLabel(streamsField.count, 'stream')}${streamsField.truncated ? ', truncated' : ''})` `` to match the existing gaps/pending heading pattern exactly. In `tests/test_advanced_ui.py::test_pipeline_and_settings_render_independent_truthful_regions`, update the existing bare-array `'streams': [...]` fixture (line 294) to the new `{'items': [...], 'count': 1, 'truncated': False}` shape so the existing test continues to pass, and add an assertion that the new count/truncated heading text renders.</action>
  <acceptance_criteria>
    - Missing and stale host evidence each produce exactly one `host_freshness` exception with the correct `state`; fresh host evidence produces none.
    - Pending-aggregation truncation is `false` at 0/31/32 rows and `true` only at 33+ rows, always returning at most 32 items.
    - Stream truncation is `false` at 0/64 rows and `true` only at 65+ rows, always returning at most 64 items; an open-gap stream ranked alphabetically last still survives the cap at 65 rows.
    - `pipeline.streams` is `{items, count, truncated}` in the JSON payload and `dashboard/advanced.js` renders that shape's count/truncated disclosure without treating a populated-but-capped list as complete.
    - `test_pipeline_and_settings_render_independent_truthful_regions` is updated to the new fixture shape and passes.
    - Full existing regression suite for this phase remains green.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -k "host_freshness or pending_truncation or stream_truncation or pipeline_and_settings" -x</automated>
  </verify>
  <done>A stale or missing host is always an active exception, and pending/stream evidence discloses a sentinel-measured truncation boolean that is never wrong at the exact cap, with active/stale streams surviving the cap ahead of quiet ones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard current-diagnosis refresh against reverse-order network completion (WR-01)</name>
  <reversibility rating="reversible">A memory-only browser counter and two early-return guards; no persisted state, network contract, or fetch-call change. Fully revertible.</reversibility>
  <files>dashboard/advanced.js, tests/test_advanced_ui.py</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` -- gap 5 (WR-01, status partial): "Use a request generation guard or AbortController and test reverse-order responses."
    - `.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md` -- WR-01 section: "Track a monotonically increasing refresh request id ... apply a response/error only if it belongs to the newest request."
    - `dashboard/advanced.js::refreshCurrentDiagnosis` (620-635) and `::scheduleRefresh` (588-592) -- both `setInterval` and the `#advanced-refresh` click call this same function with no ordering guard; the success and catch paths currently unconditionally mutate shared `state`.
    - `dashboard/advanced.js` top-level `state` object literal (5-9) -- where the new counter field is declared alongside `snapshot`/`lastSuccessLabel`.
    - `tests/test_advanced_ui.py::test_direct_route_tracer_shows_loading_then_host_and_retains_last_success_on_failure` (59-85) -- existing pattern for `page.route` fixture control and asserting retained evidence.
  </read_first>
  <behavior>
    - "RED first: a Playwright test makes the page's initial automatic refresh (older request) resolve successfully with stale-host data only after a manually triggered second refresh (newer request) has already resolved with fresh-host data; the DOM must show fresh-host both immediately after the second resolves and after the first (stale) response later arrives."
    - "RED first: the same guard applies on the error path -- an older request's failure arriving after a newer request's success must not flip the connection banner or overwrite the retained snapshot."
  </behavior>
  <action>In `dashboard/advanced.js`, add `requestGeneration: 0` to the top-level `state` object literal. At the very top of `refreshCurrentDiagnosis()`, capture `const requestId = ++state.requestGeneration;` before the `await apiFetch()` call. Immediately after `apiFetch()` resolves (start of the `try` block's success path) and immediately at the start of the `catch` block, add a guard `if (requestId !== state.requestGeneration) return;` before any other statement in that branch, so a response or error belonging to a superseded request generation never touches `state.snapshot`, `state.lastSuccessLabel`, `state.connectionUnavailable`, the DOM, or `#advanced-refresh-error`. Do not change `scheduleRefresh()`, the `setInterval` cadence, or the `Refresh now` click handler -- both continue to call `refreshCurrentDiagnosis()` exactly as today; the new generation guard makes their overlap safe without adding `AbortController` or changing the fetch call itself.</action>
  <acceptance_criteria>
    - `state.requestGeneration` increments on every call to `refreshCurrentDiagnosis()`, including scheduled and manual triggers.
    - A slower older request's success resolving after a newer request's success never overwrites the DOM, `state.snapshot`, or `state.lastSuccessLabel` with older data.
    - A slower older request's failure resolving after a newer request's success never raises the connection banner or the refresh-error message for already-superseded evidence.
    - `advanced.js` still issues exactly one `fetch('/api/advanced/current', ...)` call per triggered refresh -- only response application changes, not the fetch call itself.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k "reverse_order or generation_guard" -x</automated>
  </verify>
  <done>A late response or error from an older refresh can never regress newer current-diagnosis evidence, the safety cluster, or the last-success timestamp on screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite durable evidence -> diagnosis composer | Durable open-gap, host, stream, and pending facts must be read completely and truthfully before being labeled complete or normal. |
| diagnosis composer -> browser JSON | The JSON payload is the operator's sole basis for trusting current collection health; a composer omission becomes an operator-visible false claim. |
| browser network completion order -> rendered state | Multiple in-flight refresh requests complete in arbitrary order; only the newest must ever be applied to shared browser state. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-31 | Spoofing | `compose_pipeline_diagnosis` / `compose_active_exceptions` (CR-01) | high | mitigate | Synthesize and promote every open `telemetry_streams` gap into `pipeline.gaps` and `exceptions`; API and Playwright regressions cover the open, empty, and merged-with-historical cases. |
| T-03-32 | Spoofing | `compose_active_exceptions` (CR-02) | high | mitigate | Add a `host_freshness` branch promoting stale/unknown host evidence to an exception; API test over missing/stale/fresh host; Playwright test proves Overview never shows normal copy while the exception is present. |
| T-03-33 | Information Disclosure | `read_pipeline_evidence` pending query (CR-03) | medium | mitigate | Sentinel-derived `pending_truncated` boolean fetched from `pending_limit+1` rows instead of inferred from list length; 0/31/32/33 boundary regression. |
| T-03-34 | Information Disclosure | `read_pipeline_evidence` streams query (CR-04) | medium | mitigate | Sentinel-derived `streams_truncated` boolean, active-gap/stale-first `ORDER BY`, and UI count/truncated disclosure; 0/64/65 boundary regression. |
| T-03-35 | Spoofing | `advanced.js::refreshCurrentDiagnosis` response ordering (WR-01) | medium | mitigate | Monotonic per-request generation guard discards a response/error belonging to a superseded request; reverse-order Playwright regression. |
| T-03-36 | Tampering | New stream `ORDER BY` `CASE` expression binds only `now` and the limit | low | accept | The query text remains fixed and fully parameterized (module-owned SQL string); no request-controlled value reaches SQL structure or is left unparameterized. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|----------------|---------------|------------------------|
| V4 Access Control | Applicable | The endpoint remains GET-only, parameterless, and behind existing same-host request middleware; no new route or mutation is added. |
| V5 Validation, Sanitization and Encoding | Applicable | All new SQL uses fixed, parameterized module-owned query text; browser rendering continues to use `.textContent`/`.replaceChildren` only. |
| V2 Authentication | Not applicable | Trusted-LAN scope; no account or identity system is introduced. |
| V3 Session Management | Not applicable | `state.requestGeneration` is an in-memory browser counter, not session state. |
| V6 Stored Cryptography | Not applicable | No cryptographic material is handled. |
</threat_model>

<gap_coverage>

| Verification finding | Disposition | Closure evidence |
|---|---|---|
| CR-01 open collection gaps invisible | IMPLEMENT in Task 1 | Stream-only open-gap synthesis, merge with historical evidence, and API + Playwright regressions for the open/empty/merged cases. |
| CR-02 stale/unknown host reported normal | IMPLEMENT in Task 2 | `host_freshness` exception branch; missing/stale/fresh API matrix plus Playwright normal-copy regression. |
| CR-03 pending exact-cap misclassified | IMPLEMENT in Task 2 | Sentinel-derived `pending_truncated`; 0/31/32/33 boundary regression. |
| CR-04 streams silently capped | IMPLEMENT in Task 2 | Sentinel-derived `streams_truncated`, active-gap/stale-first `ORDER BY`, and UI count/truncated disclosure; 0/64/65 boundary regression. |
| WR-01 old refresh can overwrite new | IMPLEMENT in Task 3 | Monotonic request-generation guard; reverse-order Playwright regression on both the success and failure paths. |

</gap_coverage>

<source_coverage_audit>

| Source | ID | Feature / requirement / decision | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | -- | Enter from either theme and quickly diagnose current Pi, every service, settings, and collection health | 03-01 through 03-06; final truthfulness closure in 03-07 | COVERED | The workspace itself is unchanged; 03-07 closes the last ways the collection-health evidence it displays could be untrue. |
| REQ | TEL-06 | Retention, resolution, pressure, worker freshness, gaps, and job health | 03-02/03-03; 03-05 Tasks 1-2; 03-07 Tasks 1-2 | COVERED | Open-gap synthesis and sentinel-measured truncation close the remaining TEL-06 evidence. |
| REQ | DIA-01 | Dedicated advanced page from either theme | 03-01/03-03/03-04; 03-05 Task 1 | COVERED (unchanged) | SATISFIED per 03-VERIFICATION.md; not touched by 03-07. |
| REQ | DIA-02 | Current host facts and freshness | 03-01/03-02/03-03; 03-07 Task 2 (host exception) | COVERED | SATISFIED per 03-VERIFICATION.md; 03-07 strengthens host evidence by promoting stale/unknown state to an exception rather than changing the display fields. |
| REQ | DIA-03 | Every service's current diagnostic evidence | 03-02/03-04; 03-05 Task 2; 03-07 Task 2 (service-keyed streams share the sentinel/prioritization fix) | COVERED | SATISFIED per 03-VERIFICATION.md; not independently reopened by any of the five gaps. |
| REQ | DIA-08 | Effective settings and local-only controls | 03-02/03-03/03-04; 03-05 Task 1; 03-07 Tasks 2-3 | COVERED | Overview truthfulness (CR-02) and refresh ordering (WR-01) are both DIA-08 territory (presentation/refresh preferences must never mislead). |
| REQ | UX-02 | Theme-preserving dashboard/advanced movement | 03-04; 03-05 Task 1 | COVERED (unchanged) | SATISFIED per 03-VERIFICATION.md; not touched by 03-07. |
| RESEARCH | -- | One bounded, parameterless, GET-only current snapshot | 03-01/03-02; 03-05 Task 2; 03-07 Tasks 1-2 | COVERED | Synthesis and sentinel reads remain inside the existing single `read_transaction`; no new query shape or route is added. |
| RESEARCH | -- | Pattern 2: server-derived freshness with evidence, never causal inference | 03-01/03-02/03-03; 03-05 Tasks 1-2; 03-07 Task 2 | COVERED | Host freshness now also drives an exception, reusing the existing `freshness_state()` classifier unchanged. |
| RESEARCH | -- | Pitfall 4: losing truthful stale/unknown semantics | 03-02/03-03; 03-07 Tasks 1-2 | COVERED | CR-01/CR-02 were exactly this pitfall recurring for stream-only gaps and host evidence; closed without merging distinct evidence types. |
| RESEARCH | -- | Frequent bounded reads must not create Pi sampling gaps | 03-02 query shape; 03-06 target checkpoint; 03-07 (sentinel adds one row per capped query, no new query) | COVERED | The sentinel pattern (`cap+1` fetch) was already proven safe for gaps in 03-05/03-06; streams/pending reuse it without adding a query. |
| CONTEXT | D-06 | Operational priority and pinned order | 03-02/03-04; 03-05 Task 2 | COVERED (unchanged) | Not touched by 03-07. |
| CONTEXT | D-09 | Distinct connection/worker/recovery and pipeline conditions | 03-02/03-03; 03-05 Task 1; 03-07 Task 2 | COVERED | `host_freshness` is added as its own distinct exception kind, never merged with `worker_freshness` or `recovery_required`. |
| CONTEXT | D-10 | Four-state freshness with age/timestamp/cadence | 03-01/03-02/03-03; 03-05 Tasks 1-2; 03-07 Task 2 | COVERED | Reuses the existing `freshness_state()` vocabulary unchanged; no new state is introduced. |
| CONTEXT | D-11 | No cause inference; show worker and stream facts together | 03-02/03-03; 03-05 Task 1; 03-07 Tasks 1-2 | COVERED | The synthesized open-gap item and the host exception each report only their own observed fact, never a cause. |
| CONTEXT | D-12 | Actionable gaps promoted to the global exception summary; older resolved gaps stay in Pipeline detail | 03-02/03-03; 03-05 Task 2; 03-07 Task 1 | COVERED | 03-07 Task 1 is the direct, previously-missing implementation of this decision for stream-only open gaps. |
| PROBE | 13 rows (edge-probe fallback) | TEL-06 (3), DIA-01 (1), DIA-02 (1), DIA-03 (3), DIA-08 (3), UX-02 (2) | 03-07 frontmatter `must_haves.truths` (9 rows resolved explicit) + `must_haves.edge_cases` (4 rows flagged-unverified) | RESOLVED (9) / FLAGGED (4) | The TEL-06/DIA-03/DIA-08 adjacency, empty, and ordering rows (9 total) are resolved into the three category truths above (pending/stream boundary, empty-collection/open-gap-with-no-coverage, and reverse-order-response) with real acceptance criteria from Tasks 1-3's tests. DIA-01/DIA-02 unclassified and UX-02 boundary/precision (4 rows) have no source predicate and are outside this plan's touched scope, so they remain flagged assumptions, never dismissed. |
| PROHIBITION | TEL-06 (1), DIA-08 (2) kept; DIA-01/DIA-02/DIA-03/UX-02 dismissed | Two-stage recall/precision pass: kept "must not assert unmeasured completeness/normality" (TEL-06 safety, DIA-08 safety) and "must not apply superseded evidence" (DIA-08 transparency); a canon-referral XSS/injection candidate was dropped as already covered by the existing `.textContent`-only DOM-safety test (`test_advanced_controller_tracer_is_same_origin_get_only_and_text_safe`), not minted here | 03-07 frontmatter `must_haves.prohibitions` | KEPT (3, flagged-unverified) / DISMISSED (4, reasoned) | Kept prohibitions are authored descriptor-less/flagged-unverified per the fallback authoring rule (no wired check, no human present to confirm); dismissed rows each carry a non-empty reason. |

</source_coverage_audit>

<verification>
- Task 1 tracer: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -k "open_stream_gap or open_collection_gap or gap_truncation" -x`.
- Task 2 boundaries: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py -k "host_freshness or pending_truncation or stream_truncation or pipeline_and_settings" -x`.
- Task 3 ordering: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k "reverse_order or generation_guard" -x`.
- Phase regression: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -x`.
- Full project gate: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q`.
</verification>

<success_criteria>
- All five open verification findings (CR-01, CR-02, CR-03, CR-04, WR-01) are closed with deterministic, boundary-pinned regression tests (31/32/33, 64/65, reverse-order, missing/stale host, stream-only open gap).
- `TEL-06` and `DIA-08` move from BLOCKED to SATISFIED in the next re-verification; `DIA-02`/`DIA-03` remain SATISFIED and are not regressed.
- The current-diagnosis endpoint remains GET-only, parameterless, no-store, bounded, and effect-free; no new package, route, schema object, or remote-control surface is introduced.
- No high-severity threat is accepted or transferred; both high threats (T-03-31, T-03-32) have executable API/Playwright mitigations.
</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-07-SUMMARY.md` when done.
</output>
