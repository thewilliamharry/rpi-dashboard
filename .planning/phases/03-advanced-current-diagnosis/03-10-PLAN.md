---
phase: 03-advanced-current-diagnosis
plan: 10
type: execute
wave: 3
depends_on: [03-08, 03-09]
files_modified:
  - dashboard/app.py
  - tests/test_advanced_diagnosis_api.py
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [TEL-06, DIA-01, DIA-02, DIA-03, DIA-08, UX-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GET /api/advanced/current answers a database maintenance window with a parseable JSON body and a 503 status rather than an unparseable HTML 500, so the browser can name the real cause instead of showing a connection-retry message."
    - "GET /api/advanced/current answers an operational SQLite failure with a parseable JSON body and a service-unavailable status carrying a fixed message; no exception text, stack trace, file path, or SQL fragment reaches the client."
    - "The endpoint stays parameterless and GET-only: an unexpected query parameter still returns 400 with its existing JSON error, POST/PUT/PATCH/DELETE are still rejected, and the success response still carries Cache-Control no-store."
    - "tests/test_advanced_diagnosis_api.py restores the process-global time.time it replaces: after the module runs, time.time returns the real wall clock, and the frozen test clock cannot leak into any later test module."
    - "Running the full suite with tests/test_advanced_diagnosis_api.py first and running it with that module deselected produce the same pass/fail result for every other module -- suite greenness is no longer order-dependent."
    - "REQUIREMENTS.md's traceability table reports each Phase 3 requirement at the status the verifier actually established: DIA-01, DIA-02, DIA-03 and UX-02 as Complete, and TEL-06 and DIA-08 still open until a re-verification confirms this gap-closure round, with every requirement still mapped to exactly one phase."
    - "No requirement is marked Complete on the strength of a plan summary alone; only independently verified requirements change status in this plan."
    - "The advanced workspace's assets, theme continuity, and dashboard return path are unchanged by this plan."
  artifacts:
    - path: "dashboard/app.py"
      provides: "Explicit maintenance and database-failure handling on the current-diagnosis route with a parseable JSON body"
      contains: "MaintenanceBusy"
    - path: "tests/test_advanced_diagnosis_api.py"
      provides: "A clock fixture that restores the process-global time.time on teardown"
      contains: "addCleanup"
    - path: ".planning/REQUIREMENTS.md"
      provides: "A traceability table matching the verified per-requirement result"
      contains: "TEL-06"
  key_links:
    - from: "dashboard/beacon/db.py::MaintenanceBusy raised through read_transaction"
      to: "dashboard/app.py::api_advanced_current response"
      via: "an explicit except branch converting the maintenance lease failure into a 503 JSON body the browser's apiFetch can parse"
      pattern: "MaintenanceBusy"
    - from: "dashboard/app.py 503 JSON error field"
      to: "dashboard/advanced.js::renderRefreshError"
      via: "apiFetch already throws the server-supplied error string, and plan 03-09 surfaces it in the error region"
      pattern: "error"
    - from: "tests/test_advanced_diagnosis_api.py clock fixture"
      to: "every later test module in the pytest process"
      via: "unittest.mock patch registered with addCleanup so the stdlib time.time is restored at teardown instead of surviving the process"
      pattern: "addCleanup"
    - from: "03-VERIFICATION.md per-requirement result"
      to: ".planning/REQUIREMENTS.md traceability table"
      via: "only independently verified requirements are promoted; blocked ones stay open until re-verification"
      pattern: "Complete"
  edge_cases:
    - requirement_id: "DIA-03"
      category: "adjacency"
      status: "resolved"
      verification: "backstop"
      statement: "Two services whose operational ordering keys compare exactly equal keep a deterministic, stable relative order and are never merged into one row."
    - requirement_id: "DIA-03"
      category: "empty"
      status: "resolved"
      verification: "backstop"
      statement: "Zero configured services, and a single service, each render a truthful table state with an explicit count rather than a blank or placeholder region."
    - requirement_id: "DIA-03"
      category: "ordering"
      status: "resolved"
      verification: "backstop"
      statement: "Services that compare equal under the default operational key retain a specified, stable output order across successive snapshots."
    - requirement_id: "UX-02"
      category: "boundary"
      status: "resolved"
      verification: "backstop"
      statement: "Moving between the dashboard and the advanced workspace at the boundary conditions -- no stored theme, a stored theme, and a stored scroll offset that is consumed exactly once -- preserves the theme choice in every case."
    - requirement_id: "UX-02"
      category: "precision"
      status: "resolved"
      verification: "backstop"
      statement: "A restored dashboard scroll offset is applied without accumulating rounding drift across repeated dashboard/advanced round trips."
    - requirement_id: "DIA-01"
      category: "unclassified"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION -- the spec-less edge probe returned an unclassified DIA-01 row with no category, predicate, or descriptor. DIA-01 is SATISFIED per 03-VERIFICATION.md and is untouched by this gap-closure round, so no predicate is invented for it. Surfaced, never dismissed."
    - requirement_id: "DIA-02"
      category: "unclassified"
      status: "flagged-unverified"
      statement: "FLAGGED ASSUMPTION -- the spec-less edge probe returned an unclassified DIA-02 row with no category, predicate, or descriptor. DIA-02 is SATISFIED per 03-VERIFICATION.md and is untouched by this gap-closure round, so no predicate is invented for it. Surfaced, never dismissed."
  prohibitions:
    - requirement_id: "TEL-06"
      category: "integrity"
      status: "flagged-unverified"
      statement: "MUST NOT let test scaffolding mutate process-global state so that suite greenness depends on execution order -- verification evidence has to be reproducible in isolation and in sequence, or a green suite is a claim nobody can rely on."
    - requirement_id: "DIA-08"
      category: "honesty"
      status: "flagged-unverified"
      statement: "MUST NOT record a requirement, plan, or phase as complete on the strength of an implementation claim rather than independent verification -- the traceability table is the project's own memory of what is actually true."
---

<objective>
Close the two adjacent issues the verifier directed into this gap-closure round and correct the project's own record: give `/api/advanced/current` real error handling so a maintenance window stops masquerading as a connection failure, stop the phase's test module leaking a frozen November-2023 clock into every later test module, and bring `REQUIREMENTS.md` back in line with what was actually verified.

Purpose: The verification report explicitly refuses to accept the green suite as evidence for any must-have, because the phase module permanently rebinds the stdlib `time.time` and suite greenness is therefore order-dependent. Until that is fixed, every future verification of this project has to fall back to hand reproduction. And a traceability table that disagrees with the verifier is how a phase gets marked done twice and shipped once.
Output: A route that fails legibly, a test module that leaves the process as it found it, and a requirements table that reports the verified truth rather than an aspiration.

Expansion plan: the phase tracer is plan 03-08 Task 1. This plan expands into the Flask route boundary, the test harness, and the planning record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-advanced-current-diagnosis/03-CONTEXT.md
@.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md
@.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md
@.planning/phases/03-advanced-current-diagnosis/03-08-SUMMARY.md
@.planning/phases/03-advanced-current-diagnosis/03-09-SUMMARY.md
@dashboard/app.py
@dashboard/beacon/db.py
@dashboard/beacon/diagnosis.py
@tests/test_advanced_diagnosis_api.py
@tests/helpers.py

<interfaces>
- `dashboard/beacon/db.py::connect_db` acquires a **shared** maintenance lease with a 30-second timeout (`_acquire_lock(..., fcntl.LOCK_SH, 30)`) and raises `MaintenanceBusy` when an exclusive maintenance holder outlives that window. It then sets `timeout=30` and `PRAGMA busy_timeout=30000` on the connection. `sqlite3.OperationalError` is equally reachable from the read itself.
- `dashboard/app.py` imports `MaintenanceBusy` nowhere today. It has two import branches -- a relative `from .beacon...` block and a `from beacon...` fallback for the Gunicorn-from-`dashboard/` layout -- and any new import must be added to BOTH branches or the container import path breaks.
- `dashboard/app.py::api_advanced_current` (2121-2132) validates `request.args`, calls `beacon_diagnosis.get_current_diagnosis(DB_PATH, SETTINGS, int(time.time()))`, and sets `Cache-Control: no-store`. It has no `try`. `get_current_diagnosis` opens its own connection through `read_transaction`, so it does NOT use `get_db()` and does not participate in `_db_lock`.
- `dashboard/advanced.js::apiFetch` parses a non-ok response as JSON and throws `new Error(body.error || 'HTTP {status}')`; plan 03-09 surfaces that message in the refresh error region. A JSON body with an `error` key is therefore the shape the client already understands -- no client change is required by this plan.
- `tests/test_advanced_diagnosis_api.py` assigns `self.appmod.time.time = lambda: ...` at 13 sites (39, 61, 111, 298, 335, 376, 400, 447, 522, 549, 585, 614, 641). `appmod.time` **is** `sys.modules['time']`, so each assignment mutates the stdlib module object. `tearDown` only calls `cleanup_db`, and `importlib.reload(dashboard.app)` in `tests/helpers.load_app` re-imports the already-mutated module, so the patch survives the whole pytest process.
- Line 111's assignment lives inside a test that builds its own `appmod`/`client` in a `try`/`finally` rather than using `setUp`'s, and it re-assigns the clock once per loop iteration. A per-test fixture must therefore support being re-pointed several times within one test, without stacking one patcher per call.
- `.planning/REQUIREMENTS.md`'s `## Traceability` table maps every requirement to exactly one phase and currently reports all six Phase 3 requirements as `Gaps Found` (commit `c753593` reverted an earlier premature promotion). `03-VERIFICATION.md` establishes DIA-01, DIA-02, DIA-03 and UX-02 as SATISFIED and TEL-06 and DIA-08 as BLOCKED.
</interfaces>
</context>

<deferred_with_rationale>
Recorded decisions on findings that are NOT implemented here. None is silently dropped.

- **`_db_lock` alignment on `/api/advanced/current` (`03-REVIEW.md` WR-08, second half) -- DEFERRED, reasoned.** The review is right that this is the only read route outside the app's `threading.Lock` discipline. Adopting it, however, would place a call that can block up to 30 seconds on the maintenance flock (`db.py:75`) *inside* a process-global lock shared by every other DB route, converting a maintenance window from "one slow route" into "the whole web process stalled" on a page that polls as often as every 5 seconds from up to 8 Gunicorn threads. That is a strictly worse operator outcome than the inconsistency it removes. The read is already serialized at the SQLite layer by its own shared maintenance lease and `busy_timeout=30000`, and Task 1 makes the maintenance case fail fast and legibly instead of hanging. Revisit only alongside a deliberate WAL-mode decision, which is a Phase 6 operations concern, not a Phase 3 diagnosis concern. Tracked as threat `T-03-62` with disposition `accept`.
- **`03-REVIEW.md` WR-07 (`test_refresh_generation_guard_is_declared_in_the_advanced_controller` asserts source text) -- DEFERRED, reasoned.** The review recommends deleting it. It is not in `03-VERIFICATION.md`'s anti-pattern table and the behaviour it nominally guards is genuinely covered by the two reverse-order Playwright regressions the verifier independently re-ran. Deleting a passing test in a gap-closure round adds risk without closing a gap; its cost is a future refactor friction, not an operator-visible defect. Recommend removal when the refresh controller is next refactored.
- **`03-REVIEW.md` IN-01 (full DOM rebuild per poll destroys focus and open selects) -- DEFERRED, reasoned.** Real, but it is a rendering-architecture change (row diffing or focus-aware skipping) well beyond a gap-closure round, and plan 03-09 removes the sharpest edge of it by preserving the operator's sort across the rebuild. Candidate for the Phase 5 experience work.
- **`03-REVIEW.md` IN-02 (client and server duplicate the D-06 ordering rule with divergent `pinned_order` fallbacks) -- DEFERRED, reasoned.** No observed incorrect output; the divergence is a maintainability hazard. Fold into the Phase 4 work that next touches service ordering.
- **`03-REVIEW.md` IN-03 (a service with `unknown` availability but fresh probe evidence produces no exception) -- DEFERRED, reasoned.** This is a genuine product question about what `unknown` availability should mean to the operator, not a defect against a written contract; `03-CONTEXT.md` D-11 forbids inferring a fault from it. Route through `/gsd-discuss-phase` rather than deciding it inside a gap-closure plan.
- **`03-REVIEW.md` IN-04 (`restoreDashboardScroll` sessionStorage key can outlive its navigation) and IN-05 (dead always-null job/settings fields) -- DEFERRED, reasoned.** Both are cosmetic, neither produces a false claim to the operator, and UX-02 and TEL-06 are verified on the surfaces they touch.
</deferred_with_rationale>

## Artifacts this phase produces

- `dashboard/app.py` -- new import of `MaintenanceBusy` in **both** the relative and the Gunicorn-fallback import branches.
- `dashboard/app.py::api_advanced_current` -- gains explicit exception handling returning a JSON body with an `error` key and a 503 status for a maintenance window, and a JSON body with a fixed message and a 503 status for an operational SQLite failure.
- `tests/test_advanced_diagnosis_api.py::AdvancedDiagnosisApiTests._freeze_clock` -- new test-class helper installing one `unittest.mock` patch of the stdlib `time.time` per test, registered with `addCleanup`, and re-pointable within a single test without stacking patchers.
- `tests/test_advanced_diagnosis_api.py` -- all 13 direct `appmod.time.time` assignments replaced by calls to that helper.
- `tests/test_advanced_diagnosis_api.py` -- new regression asserting the process-global clock is the real one after a test that froze it, and new regressions for the 503 maintenance and 503 database-failure responses.
- `.planning/REQUIREMENTS.md` -- the six Phase 3 rows of the `## Traceability` table updated to the verified status.
- No new file, package, route, endpoint, migration, column, table, or index is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the current-diagnosis route fail legibly instead of as an unparseable HTML 500 (WR-08, error-handling half)</name>
  <reversibility rating="reversible">Adds two except branches and one import to a single route handler; no lock discipline, schema, or contract change. Fully revertible.</reversibility>
  <files>dashboard/app.py, tests/test_advanced_diagnosis_api.py</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md` -- WR-08 for both halves of the finding, and the `<deferred_with_rationale>` block in this plan for why only the error-handling half is implemented.
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` -- the `dashboard/app.py` row of "Required Artifacts" (PARTIAL: no exception handling, `MaintenanceBusy` escapes as an HTML 500 the client cannot parse).
    - `dashboard/beacon/db.py` lines 55-90 -- `_acquire_lock`, its 30-second shared-lease timeout, the exact `MaintenanceBusy` raise site, and `connect_db`'s `timeout`/`busy_timeout` settings; this is what determines which exceptions are reachable.
    - `dashboard/app.py` lines 22-45 -- the two import branches (relative and Gunicorn fallback); the new import must appear in both or the container import path breaks.
    - `dashboard/app.py` lines 2121-2132 -- the current handler, its `request.args` 400 branch, and the `Cache-Control: no-store` header that must survive on the success path.
    - `dashboard/app.py` -- grep for an existing route that already converts a database exception into a JSON error response and mirror its status code, body key, and logging convention rather than inventing a new one; if none exists, mirror the existing `{'error': ...}` body shape used by this handler's own 400 branch.
    - `dashboard/advanced.js` lines 57-65 -- `apiFetch` parses a non-ok body as JSON and reads `.error`, which is the shape this task must produce; no client change is needed here because plan 03-09 already surfaces that string.
    - `tests/test_advanced_diagnosis_api.py::test_direct_route_tracer_preserves_middleware_assets_and_get_only_api` -- the existing route-contract regression whose GET-only and asset assertions must keep passing.
  </read_first>
  <behavior>
    - "RED first: a regression forces get_current_diagnosis to raise MaintenanceBusy and asserts GET /api/advanced/current returns 503 with a JSON content type and a body whose error field names a database maintenance window."
    - "RED first: a regression forces an sqlite3.OperationalError and asserts a 503 JSON response carrying a fixed message, and that the response body contains no exception text, file path, or SQL fragment."
    - "Regression: the success path still returns 200 with Cache-Control no-store; an unexpected query parameter still returns 400 with its existing JSON error; POST to the route is still rejected."
  </behavior>
  <action>In `dashboard/app.py`, import `MaintenanceBusy` from the beacon db module in BOTH the relative import block and the Gunicorn `from beacon...` fallback block, alongside the existing `connect_db` import. In `api_advanced_current`, keep the `request.args` validation and its 400 response exactly as they are, then wrap the `beacon_diagnosis.get_current_diagnosis(...)` call in a `try`. Add an `except MaintenanceBusy` branch returning a JSON body whose `error` value names a database maintenance window in progress, with HTTP status 503 -- this is the case the operator most needs distinguished, because today it reaches them as the browser's generic connection-retry copy. Add a second branch catching the operational SQLite failure class (`sqlite3.OperationalError`, or the narrowest already-imported base that covers it in this module) returning a JSON body with a fixed, non-revealing message and HTTP status 503; log the underlying exception through the module's existing logger rather than returning it, so no exception text, stack, file path, or SQL fragment reaches the client. Do not add a bare `except Exception` -- an unexpected failure class must stay loud rather than be swallowed into a 503 that hides a real bug. Do not wrap the call in `_db_lock`; see the `<deferred_with_rationale>` block, and do not remove or weaken the existing `Cache-Control: no-store` header or the GET-only route registration. Add the regressions described in the behavior block, forcing each exception by monkeypatching the diagnosis entry point on the reloaded app module within the test's own lifetime.</action>
  <acceptance_criteria>
    - A forced `MaintenanceBusy` yields HTTP 503 with a JSON content type and an `error` field naming a maintenance window.
    - A forced `sqlite3.OperationalError` yields HTTP 503 with a JSON content type and a fixed message; the response body contains no exception text, file path, or SQL fragment.
    - The success path still returns 200 with `Cache-Control: no-store`.
    - An unexpected query parameter still returns 400 with the existing JSON error body.
    - The route is still GET-only and the asset routes are unchanged; `test_direct_route_tracer_preserves_middleware_assets_and_get_only_api` passes unmodified.
    - `MaintenanceBusy` is imported in both import branches of `dashboard/app.py`.
    - `api_advanced_current` contains no bare catch-all exception handler and no `_db_lock` acquisition.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py -k "maintenance or route_tracer or unexpected_query or database_unavailable" -x</automated>
  </verify>
  <done>A maintenance window and a database failure each reach the operator as a parseable, correctly-named cause instead of an HTML 500 rendered as "check the connection warning, then try again".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop the phase test module leaking a frozen process-global clock (WR-05)</name>
  <reversibility rating="reversible">Test-harness change only; no production code path is touched. Fully revertible.</reversibility>
  <files>tests/test_advanced_diagnosis_api.py</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-REVIEW.md` -- WR-05 for the demonstration, the full list of 13 assignment sites, and the suggested `unittest.mock.patch` + `addCleanup` shape.
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` -- the "Behavioral Spot-Checks" row `Global clock isolation across modules` (FAIL) and the "Note on the green suite" paragraph explaining that the verifier refuses to accept suite greenness as evidence for any must-have because of this leak.
    - `tests/helpers.py::load_app` -- `importlib.reload(dashboard.app)` re-imports the already-mutated stdlib `time` module and therefore restores nothing; understand this before assuming reload is a teardown.
    - `tests/test_advanced_diagnosis_api.py` lines 1-20 (imports and `setUp`/`tearDown`) -- where the fixture is installed and where cleanup must be registered.
    - `tests/test_advanced_diagnosis_api.py` lines 96-120 -- the one test that builds its own `appmod`/`client` inside a `try`/`finally` and re-points the clock once per loop iteration; the fixture must support repeated re-pointing within a single test without stacking one patcher per call.
    - `tests/test_advanced_diagnosis_api.py` lines 39, 61, 298, 335, 376, 400, 447, 522, 549, 585, 614, 641 -- the remaining assignment sites, each of which sets the clock exactly once per test.
    - Any other test module that reads wall-clock time (grep the suite for `time.time` and for retention/lease/rate-limit cutoffs) -- to understand what the leak was silently affecting and to pick a second module for the order-independence regression.
  </read_first>
  <behavior>
    - "RED first: a regression asserts that after a test in this module has frozen the clock, the process-global time.time returns a value close to the real wall clock rather than the frozen fixture value -- this fails today because the patch never unwinds."
    - "Regression: every existing test in the module continues to observe exactly the frozen instant it previously observed; no assertion value changes."
    - "Regression: the worker-freshness test that re-points the clock three times within one test still observes each of its three instants in turn."
    - "Order-independence evidence: running this module followed by another wall-clock-sensitive module produces the same results as running that module alone."
  </behavior>
  <action>In `tests/test_advanced_diagnosis_api.py`, add a test-class helper -- name it `_freeze_clock(value)` -- that makes `time.time()` return the given value for the duration of the current test and restores the real function at teardown. Install exactly one `unittest.mock.patch` of `time.time` per test, started on first use and registered with `addCleanup(patcher.stop)` so restoration happens even when a test fails or raises; hold the frozen instant in a mutable container the patch reads, so a second or third call to the helper inside the same test re-points that container instead of starting another patcher. Preserve the real function as the fallback the patch returns when no instant has been set, so a test that never freezes the clock still sees real time. Replace all 13 `appmod.time.time = lambda: ...` assignments with calls to the helper, preserving each site's exact instant value so no existing assertion changes -- including the worker-freshness test at line 111, which re-points the clock once per loop iteration and must keep observing all three instants in order even though it constructs its own `appmod` in a `try`/`finally`. Patch the stdlib `time.time` by its module path rather than through an app-module attribute, so the intent (a process-global clock, deliberately frozen and deliberately restored) is explicit at the patch site. Add the leak regression and the order-independence evidence described in the behavior block. Change no production file and no assertion value.</action>
  <acceptance_criteria>
    - After the module's tests run, `time.time()` returns the real wall clock; the frozen fixture value does not survive teardown.
    - `tests/test_advanced_diagnosis_api.py` contains no direct assignment to `time.time` on a module object.
    - Every pre-existing assertion in the module passes with its value unchanged, including the three-instant worker-freshness loop.
    - Running the phase module followed by another wall-clock-sensitive module gives the same result as running that module alone.
    - The full suite passes both with the phase module first and with the phase module deselected.
    - No production file is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py -x && UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q --deselect tests/test_advanced_diagnosis_api.py</automated>
  </verify>
  <done>The suite's greenness stops depending on the order its modules happen to run in, so a future verifier can accept it as evidence instead of hand-reproducing every truth.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the requirements traceability record to the verified state (traceability correction)</name>
  <precondition>Plans 03-08 and 03-09 are executed and committed, and their verify commands pass -- this task records status, so it must run after the code changes it describes exist.</precondition>
  <reversibility rating="reversible">A documentation table edit in the planning record; no code, schema, or behaviour is affected. Fully revertible.</reversibility>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md` -- the "Requirements Coverage" table is the authority for this task: TEL-06 BLOCKED, DIA-01 SATISFIED, DIA-02 SATISFIED, DIA-03 SATISFIED, DIA-08 BLOCKED, UX-02 SATISFIED, plus the closing paragraph directing the correction.
    - `.planning/REQUIREMENTS.md` -- the whole `## Traceability` table, including the invariant stated above it that every v1 requirement maps to exactly one roadmap phase, and the status vocabulary already in use elsewhere in the table (`Complete`, `Gaps Found`, `Pending`).
    - `git log --oneline -3 -- .planning/REQUIREMENTS.md` -- commit `c753593` reverted an earlier premature promotion to `Complete`; read why before promoting anything, so this task does not repeat that mistake.
    - `.planning/ROADMAP.md` Phase 3 entry -- the phase's success criteria and plan list, which must stay consistent with whatever status this table reports.
  </read_first>
  <action>Edit only the six Phase 3 rows of the `## Traceability` table in `.planning/REQUIREMENTS.md`, using scoped replacements rather than rewriting the file. Set `DIA-01`, `DIA-02`, `DIA-03` and `UX-02` to `Complete`, because `03-VERIFICATION.md` establishes each as SATISFIED through independent reproduction, not through a plan summary claim. Leave `TEL-06` and `DIA-08` at `Gaps Found`: both are BLOCKED in the verification report, and although plans 03-08 and 03-09 are intended to close them, nothing may be promoted to `Complete` on the strength of an implementation claim -- only a re-verification of this gap-closure round may promote them, which is exactly the mistake commit `c753593` had to revert. Do not change any row belonging to another phase, do not change the phase column of any row, and do not add, remove, or rename a status value or a requirement id -- the one-requirement-to-one-phase invariant must still hold after the edit. Leave the requirement checklist items in the body of the file untouched; the traceability table is the record being corrected.</action>
  <acceptance_criteria>
    - `DIA-01`, `DIA-02`, `DIA-03` and `UX-02` each report `Complete` in the traceability table.
    - `TEL-06` and `DIA-08` each still report `Gaps Found`, pending re-verification of this round.
    - Every requirement in the table still maps to exactly one phase, and no row outside Phase 3 is modified.
    - The set of requirement ids and the set of status values used in the table are unchanged apart from the four promotions.
    - The diff touches only `.planning/REQUIREMENTS.md` and only within the `## Traceability` table.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE '^\| (DIA-01|DIA-02|DIA-03|UX-02) \| Phase 3 \| Complete \|' .planning/REQUIREMENTS.md)" = "4" && test "$(grep -cE '^\| (TEL-06|DIA-08) \| Phase 3 \| Gaps Found \|' .planning/REQUIREMENTS.md)" = "2" && echo TRACEABILITY_OK</automated>
  </verify>
  <done>The project's own record of what is true matches what was independently verified, and nothing is marked complete ahead of the evidence that would justify it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite maintenance lease -> HTTP response | An exclusive maintenance holder or an operational database failure must reach the operator as a named, parseable cause, not as an opaque server error. |
| server exception detail -> browser | Exception text, stack frames, file paths and SQL fragments must not cross into a client response. |
| test harness -> pytest process | A test fixture that mutates process-global state changes the meaning of every later module's result. |
| verification result -> planning record | The traceability table is consumed by later planning as fact; a status ahead of its evidence propagates a false premise. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-61 | Information Disclosure | `api_advanced_current` unhandled exceptions (WR-08) | medium | mitigate | Explicit `MaintenanceBusy` and operational-SQLite branches returning a fixed JSON message with the detail logged server-side; regression asserts no exception text, path, or SQL fragment appears in the body. |
| T-03-62 | Denial of Service | `_db_lock` discipline on the current-diagnosis route | medium | accept | Deferred with a written rationale (see `<deferred_with_rationale>`): adopting the lock would place a 30-second maintenance-flock wait inside a process-global lock shared by every DB route on a 5-second poll, which is worse than the inconsistency it removes. The read is already serialized by its own shared lease and `busy_timeout`; Task 1 makes the maintenance case fail fast. Revisit with a deliberate WAL decision in Phase 6. |
| T-03-63 | Repudiation | Order-dependent suite evidence from the leaked global clock (WR-05) | medium | mitigate | Patch-and-restore fixture with `addCleanup`; a leak regression plus an explicit both-orders suite run as the task's own verify command. |
| T-03-64 | Denial of Service | A bare catch-all handler hiding a real defect behind a 503 | medium | mitigate | Task 1 explicitly forbids `except Exception`; only the two named, reachable exception classes are caught, and the acceptance criteria assert the absence of a catch-all. |
| T-03-65 | Repudiation | Traceability status promoted ahead of verification | low | mitigate | Only independently verified requirements are promoted; TEL-06 and DIA-08 stay open until re-verification, mirroring the correction commit `c753593` already had to make once. |
| T-03-66 | Tampering | Supply chain (npm / pip / cargo installs) | n/a | accept | No package-manager install task exists in this plan; `unittest.mock` is stdlib and no dependency is added, removed, or upgraded. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|----------------|---------------|------------------------|
| V7 Error Handling and Logging | Applicable | Named exception branches only, fixed non-revealing client messages, underlying detail logged through the module's existing logger, no catch-all. |
| V4 Access Control | Applicable | The route stays GET-only and parameterless; the 400 query-parameter rejection is preserved. |
| V5 Validation, Sanitization and Encoding | Applicable | Error bodies are fixed module-owned strings; no request or exception text is interpolated into a response. |
| V2 Authentication / V3 Session Management / V6 Stored Cryptography | Not applicable | Trusted-LAN scope; no identity, session, or cryptographic material is introduced. |
</threat_model>

<gap_coverage>

| Verification finding | Disposition | Closure evidence |
|---|---|---|
| WR-08 -- no exception handling on `api_advanced_current`; `MaintenanceBusy` escapes as an unparseable HTML 500 | IMPLEMENT in Task 1 | Named 503 JSON branches for the maintenance and operational-database cases, with regressions asserting the status, content type, and absence of revealing detail. |
| WR-08 -- `_db_lock` discipline bypass | DEFER with recorded rationale | `<deferred_with_rationale>` and threat `T-03-62`; the alternative is a worse operator outcome and is coupled to a WAL decision outside this phase. |
| WR-05 -- global stdlib monkey-patch without teardown (`tests/test_advanced_diagnosis_api.py:18-19 et al.`) | IMPLEMENT in Task 2 | Patch-and-restore fixture with `addCleanup` across all 13 sites, a leak regression, and an explicit both-orders suite run. |
| Traceability correction directed by `03-VERIFICATION.md` | IMPLEMENT in Task 3 | Four verified requirements promoted, two blocked ones deliberately left open until re-verification. |
| `human_verification` items 1 and 2 from `03-VERIFICATION.md` | CARRIED FORWARD | Item 1 rides in plans 03-08 and 03-09 as an end-of-phase `human-check`; item 2 is carried below and is additionally converted into Task 2's automated both-orders command. |
| WR-07, IN-01, IN-02, IN-03, IN-04, IN-05 | DEFER with recorded rationale | `<deferred_with_rationale>`; each has a named reason and a named future home. None is silently dropped. |
| CR-02 / CR-03 / CR-04 / WR-01 (prior round) | DO NOT REPLAN -- confirmed closed | Task 2 changes only how the clock is installed, never an assertion value, so every prior boundary regression must pass with identical expectations. |

</gap_coverage>

<source_coverage_audit>

| Source | ID | Feature / requirement / decision | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | -- | Enter an advanced workspace from either theme and quickly diagnose the Pi, services, settings, and collection health | 03-01..03-07 built it; 03-08/03-09 make it truthful and legible; 03-10 makes its failure modes legible and its record accurate | COVERED | A route that fails as an unparseable 500 is a diagnosis surface that stops diagnosing exactly when something is wrong. |
| REQ | TEL-06 | Retention, resolution, pressure, worker freshness, collection gaps, job health | 03-08 (evidence); 03-10 Tasks 1-3 (failure legibility, evidence integrity, record) | COVERED | Left at `Gaps Found` until re-verification, deliberately. |
| REQ | DIA-08 | Effective settings plus supported presentation/refresh/filter preferences | 03-08/03-09 (behaviour); 03-10 Tasks 1 and 3 | COVERED | Left at `Gaps Found` until re-verification, deliberately. |
| REQ | DIA-01 | Open a dedicated advanced page from either theme | 03-01/03-03/03-04 (unchanged); 03-10 Task 3 records the verified result | COVERED | Asset routes explicitly unmodified by Task 1. |
| REQ | DIA-02 | Current host metrics, identity, sample time, freshness | 03-01/03-02/03-03 and 03-07 (unchanged); 03-10 Task 3 records the verified result | COVERED | Untouched by this plan's code changes. |
| REQ | DIA-03 | Every service's status, latency/failure, duration, criticality, tags, health rule | 03-02/03-04 (unchanged); 03-09 (sort survival); 03-10 Task 3 records the verified result | COVERED | Untouched by this plan's code changes. |
| REQ | UX-02 | Move between dashboard and advanced without losing theme | 03-04 (unchanged); 03-10 Task 3 records the verified result | COVERED | Theme and scroll paths explicitly unmodified. |
| RESEARCH | -- | GET-only, parameterless, effect-free bounded snapshot | 03-10 Task 1 | COVERED | The 400 and GET-only contracts are preserved as acceptance criteria. |
| RESEARCH | -- | Web and worker are separate processes sharing SQLite; reads must not take ownership | 03-10 Task 1 + `<deferred_with_rationale>` | COVERED | The lock-discipline decision is made explicitly against this constraint rather than by default. |
| CONTEXT | D-04 | Same-tab `/advanced` with a clear Dashboard return and preserved theme | unchanged | COVERED (unchanged) | No asset route or navigation path is touched. |
| CONTEXT | D-13 | Predictable auto-refresh with visible last-updated evidence | 03-10 Task 1 | COVERED | A legible 503 is what lets the refresh evidence stay honest during maintenance. |
| CONTEXT | all other D-NN | Workspace, service, freshness, preference decisions | 03-01..03-09 | COVERED (unchanged) | No decision is revisited; `03-CONTEXT.md` records no deferred ideas, so none can leak in. |
| UI-SPEC | Considerations row 1 | Workspace shell and navigation -- loading / error / overflow / long-text | 03-10 Task 1 must_haves | COVERED (explicit) | The error state now carries a real cause; the contracted retained-data copy is preserved by plan 03-09. |
| UI-SPEC | Considerations rows 2-6 | Overview, Host, Services, Pipeline, Settings | rows 2 and 4 in 03-09; row 5 in 03-08; rows 3 and 6 unchanged from 03-01..03-06 | COVERED (elsewhere / unchanged) | All six rows are accounted for across the three plans; none is dropped. |
| PROBE | DIA-03 adjacency/empty/ordering, UX-02 boundary/precision (5 of 13) | Edge-probe fallback rows | 03-10 `must_haves.edge_cases` (5 resolved, backstop) | RESOLVED (backstop) | These belong to truths already verified SATISFIED and untouched by this round; resolved as backstop rather than re-litigated, per the fallback's scope note. A backstop truth the verifier cannot confirm abstains to `human_needed`, never a silent pass. |
| PROBE | DIA-01 unclassified, DIA-02 unclassified (2 of 13) | Edge-probe fallback rows | 03-10 `must_haves.edge_cases` (2 flagged-unverified) | FLAGGED | An unclassified row is never auto-resolved and never auto-dismissed; both are surfaced as explicit planner assumptions. Full 13-row ledger is in `03-08-PLAN.md`. |
| PROHIBITION | TEL-06 integrity, DIA-08 honesty (2 kept) | Two-stage recall/precision pass | 03-10 `must_haves.prohibitions` | KEPT (2, flagged-unverified, descriptor-less) | Test scaffolding must not make evidence order-dependent; status must not be recorded ahead of verification. |

</source_coverage_audit>

<verification>
- Task 1 route failure modes: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py -k "maintenance or route_tracer or unexpected_query or database_unavailable" -x`
- Task 2 clock isolation, both orders: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py -x` then `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q --deselect tests/test_advanced_diagnosis_api.py`
- Task 3 traceability: the grep gate in the task's verify block, plus a read-through of the six Phase 3 rows.
- Phase regression: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q tests/test_advanced_diagnosis_api.py tests/test_advanced_ui.py tests/test_module_boundaries.py -x`
- Full project gate, run twice with the phase module in different positions: `UV_CACHE_DIR=/tmp/uv-cache-beacon-gap uv run --project dashboard python -m pytest -q`
- Carried-forward human verification (from `03-VERIFICATION.md`, `human_verification` item 2; `workflow.human_verify_mode` is `end-of-phase`):
  <human-check>Run the full suite with `tests/test_advanced_diagnosis_api.py` deselected, and again in isolation, and compare results. Expected: identical results in both orders. Why human: the module previously replaced the process-global `time.time` permanently, and deciding whether the restored fixture makes the suite's evidence acceptable is a maintainer call. Task 2's automated command now produces this evidence, but the acceptance of it remains the maintainer's.</human-check>
</verification>

<success_criteria>
- A maintenance window and an operational database failure each reach the browser as a parseable JSON body with a named cause, and neither leaks exception detail.
- The current-diagnosis route remains GET-only, parameterless, `no-store`, and free of any catch-all exception handler.
- `tests/test_advanced_diagnosis_api.py` leaves the process-global `time.time` as it found it, and the suite produces identical results with the module first and with it deselected.
- Every pre-existing assertion in the phase test module passes with its value unchanged; CR-02, CR-03, CR-04 and WR-01 remain closed.
- `REQUIREMENTS.md` reports DIA-01, DIA-02, DIA-03 and UX-02 as `Complete` and TEL-06 and DIA-08 as still open, with the one-requirement-to-one-phase invariant intact.
- Every remaining WARNING and INFO finding from `03-REVIEW.md` is either implemented in plans 03-08 through 03-10 or carries a written deferral rationale here; none is silently dropped.
- No high-severity threat is accepted or transferred.
</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-10-SUMMARY.md` when done.
</output>