---
phase: 03-advanced-current-diagnosis
plan: 21
type: execute
wave: 2
depends_on: ["03-20"]
files_modified:
  - dashboard/beacon/worker_main.py
  - dashboard/app.py
  - dashboard/beacon/diagnosis.py
  - tests/test_advanced_diagnosis_api.py
autonomous: true
gap_closure: true
requirements: [TEL-06]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An unrecognised discovery outcome literal fails closed and loud -- dispatch_callback raises and durably records a genuine job failure -- rather than silently mapping to succeeded by exclusion; a genuine 'busy' contention still maps to succeeded, and both directions are now pinned by a regression for J7 and J9, closing WR-01."
    - "The comment and log message at the uptime-lock contention site no longer claim the losing probe's work is equivalent to the winner's: when J3 (a full sweep) loses the lock to J4 (a down-only probe), a regression proves J3's own last_uptime_check is never advanced, so the coverage gap remains visible on the telemetry-coverage surface rather than being silently claimed as covered, closing WR-02."
    - "The job_outcome_unrecorded promotion floor derives its guarantee from the operator's own configured DISCOVERY_TIMEOUT_SECONDS, not merely from the hardcoded 900-second default, so a deployment configured above 900 no longer has a legitimately-running J7/J9 discovery promoted to a fabricated job_outcome_unrecorded card, closing WR-03."
  artifacts:
    - path: "dashboard/beacon/worker_main.py"
      provides: "_run_scheduled_discovery and _run_startup_discovery fail closed on any discovery outcome literal outside the documented 'completed' | 'busy' | 'failed' contract, instead of treating every non-'failed' value as success by exclusion"
      contains: "_discovery_outcome_verdict"
    - path: "dashboard/beacon/diagnosis.py"
      provides: "The job_outcome_unrecorded floor widens to cover an operator-configured DISCOVERY_TIMEOUT_SECONDS above the hardcoded 900-second default"
      contains: "discovery_timeout_seconds"
    - path: "tests/test_advanced_diagnosis_api.py"
      provides: "Regressions for all three warnings: the fail-closed discovery vocabulary (both directions, J7 and J9), the J3/J4 coverage-gap asymmetry, and the configurable floor"
      contains: "an_unrecognised_discovery_outcome_fails_closed_and_loud"
  key_links:
    - from: "services.run_discovery's own documented three-literal contract"
      to: "an explicit membership check instead of an exclusion comparison"
      via: "_discovery_outcome_verdict, shared by _run_scheduled_discovery and _run_startup_discovery, raising ValueError on any outcome outside {'completed','busy','failed'}"
      pattern: "_discovery_outcome_verdict"
    - from: "J3 losing _uptime_lock to a concurrent J4"
      to: "scan_state['last_uptime_check'] staying unadvanced"
      via: "the unchanged return None on lock contention, now pinned by a regression instead of merely asserted in a comment"
      pattern: "last_uptime_check"
    - from: "settings.discovery_timeout_seconds"
      to: "the job_outcome_unrecorded promotion boundary"
      via: "get_current_diagnosis threading discovery_timeout_seconds through compose_active_exceptions into _unrecorded_outcome_boundary's own floor computation"
      pattern: "discovery_timeout_seconds"
  prohibitions:
    - requirement_id: "TEL-06"
      category: "honesty"
      status: "flagged-unverified"
      verification: "judgment"
      statement: "MUST NOT present resolved, retention-expired, or otherwise inferred evidence to the operator as a current, open, actionable fault -- every operator-facing open/actionable/kind label must be derivable from the durable row it describes, never from a neighbouring row or a stream-level fact. Carried from 03-08. Task 2's corrected comment stops implying the losing uptime probe's coverage is equivalent to the winner's -- an honesty correction to the comment itself, not a behaviour change -- and Task 3 stops a configurable value the code silently ignored from producing a fabricated job_outcome_unrecorded card."
    - requirement_id: "TEL-06"
      category: "completeness"
      status: "flagged-unverified"
      verification: "judgment"
      statement: "MUST NOT suppress a genuine collection failure while narrowing false positives -- restricting promotion by reason must never cause a real collection_gap, or a reason value the code does not recognise, to go unreported to the operator. Carried from 03-08. Task 1 makes an unrecognised discovery outcome MORE visible (a raised, durably-recorded failure) rather than less; Task 3 widens, never narrows, the floor that protects a legitimately-running discovery from a fabricated promotion."
    - requirement_id: "TEL-06"
      category: "integrity"
      status: "flagged-unverified"
      verification: "judgment"
      statement: "MUST NOT let automatic background refresh silently override an operator's explicit presentation choice, and MUST NOT present a machine identifier or a placeholder as the operator's primary safety evidence when the server supplied real evidence. Carried from 03-09 and UPHELD through round 6. No task in this plan touches dashboard/advanced.js, the refresh loop, or any rendered copy; retained as a regression guard only."
    - requirement_id: "TEL-06"
      category: "integrity"
      status: "flagged-unverified"
      verification: "judgment"
      statement: "MUST NOT let test scaffolding mutate process-global state so that suite greenness depends on execution order. Carried from 03-10. Every new subTest and every new collaborator this plan's tasks install (`run_discovery` lambdas, `_uptime_lock` acquired directly and released in `try`/`finally`, `discovery_timeout_seconds` passed as an explicit argument) is either a fixture-local value never assigned to process-global state, or an existing pattern already governed by an established `try`/`finally` release; no new `addCleanup` is required, and both cross-module orders against `tests/test_runtime_ownership.py` are re-run as an acceptance criterion."
    - requirement_id: "TEL-06"
      category: "process"
      status: "flagged-unverified"
      verification: "judgment"
      statement: "MUST NOT record a requirement, plan, or phase as complete on the strength of an implementation claim rather than independent verification -- the traceability table is the project's own memory of what is actually true. Carried from 03-10. This plan, together with wave-1 sibling 03-20, closes CR-01 and all four review warnings from `03-19-REVIEW.md`, and therefore MUST NOT promote TEL-06 in .planning/REQUIREMENTS.md; every task's acceptance criteria pins both halves of the record unchanged. Only the next independent re-verification may promote it."
---

<objective>
Close the three remaining Warnings from `03-19-REVIEW.md` round 6 that wave-1 sibling `03-20` did not: the fail-open discovery-outcome vocabulary (WR-01), the misleading uptime-lock-contention comment (WR-02), and the promotion floor's silent disagreement with an operator-configured `DISCOVERY_TIMEOUT_SECONDS` (WR-03).

Purpose: `03-20` closed the one Critical finding (CR-01) and the one Warning directly adjacent to it (WR-04). The remaining three warnings are independent of each other and of CR-01, touching `worker_main.py`'s discovery dispatch, `app.py`'s uptime-contention comment, and `diagnosis.py`'s promotion-floor arithmetic respectively. Each is triaged explicitly below with a fix-now rationale; none met any of the three legitimate reasons to defer (context cost, missing information, or a dependency conflict), so none is deferred.

Output: `_run_scheduled_discovery` and `_run_startup_discovery` fail closed and loud on any discovery outcome literal outside the documented three-value contract, and a regression pins the `'busy'` success case that has been undefended since it was introduced. The uptime-lock-contention comment states the true asymmetry between a full sweep and a down-only probe instead of implying equivalence, and a regression proves the losing full sweep's own coverage is never silently claimed as advanced. The `job_outcome_unrecorded` floor derives its guarantee from the operator's own `DISCOVERY_TIMEOUT_SECONDS`, closing the exact reproduction `03-19-REVIEW.md` demonstrated (`DISCOVERY_TIMEOUT_SECONDS=1200` both failing the floor's own subtests and promoting a legitimately-running J9).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced-current-diagnosis/03-19-REVIEW.md
@.planning/phases/03-advanced-current-diagnosis/03-VERIFICATION.md
@dashboard/beacon/worker_main.py
@dashboard/beacon/diagnosis.py
@dashboard/app.py
@tests/test_advanced_diagnosis_api.py
</context>

## Artifacts this phase produces

- `dashboard/beacon/worker_main.py::_discovery_outcome_verdict` -- new shared helper, called by `_run_scheduled_discovery` and `_run_startup_discovery` on their work paths in place of `outcome != 'failed'`. Maps `'completed'`/`'busy'` to `True`, `'failed'` to `False`, and raises `ValueError` on any other literal.
- `dashboard/app.py::_legacy_do_uptime_check` -- its lock-contention comment is rewritten to state the true asymmetry between J3 and J4; no return-value or control-flow change.
- `dashboard/beacon/diagnosis.py::_unrecorded_outcome_boundary` -- gains a required `discovery_timeout_seconds` parameter; the floor becomes `max(UNRECORDED_OUTCOME_FLOOR_SECONDS, int(discovery_timeout_seconds) + 60)` before the existing cadence comparison.
- `dashboard/beacon/diagnosis.py::compose_active_exceptions` -- gains a required keyword-only `discovery_timeout_seconds` parameter, threaded into `_unrecorded_outcome_boundary`.
- `dashboard/beacon/diagnosis.py::get_current_diagnosis` -- passes `discovery_timeout_seconds=settings.discovery_timeout_seconds` to `compose_active_exceptions`.
- `tests/test_advanced_diagnosis_api.py` -- `test_the_real_discovery_dispatch_honours_the_return_value_vocabulary` gains two subTest cases per job (`'busy'` succeeds, an unrecognised literal fails closed and loud); `test_an_uptime_lock_collision_is_not_reported_as_a_failure` gains a `last_uptime_check` assertion; `test_a_job_stuck_without_an_outcome_becomes_an_operator_exception`'s `compose`/`compose_many` helpers and the two existing `compose_active_exceptions` call sites gain the new required argument, plus one new subTest proving the floor derives from a configured value rather than the hardcoded constant.
- No new file, route, endpoint, migration, column, table, index, or package. No server payload field is added.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close WR-01 -- fail closed and loud on an unrecognised discovery outcome</name>
  <reversibility rating="reversible">A pure refactor of one comparison into a shared, explicitly-checked helper. Every literal the production contract documents (`'completed' | 'busy' | 'failed'`) maps identically before and after; only a literal outside that contract changes behaviour, from silent success to a loud, durably-recorded failure. Single-file revert.</reversibility>
  <files>dashboard/beacon/worker_main.py, tests/test_advanced_diagnosis_api.py</files>
  <behavior>
    - _discovery_outcome_verdict('completed') and _discovery_outcome_verdict('busy') both return True.
    - _discovery_outcome_verdict('failed') returns False.
    - _discovery_outcome_verdict(anything else) raises ValueError naming the unrecognised literal.
    - _run_scheduled_discovery and _run_startup_discovery call _discovery_outcome_verdict(outcome) on their work paths in place of outcome != 'failed'; their skip-path return None sites are completely unchanged.
    - dispatch_callback('J7') and dispatch_callback('J9'), driven through the real handlers with a stubbed run_discovery returning 'busy', record a durable succeeded outcome and no job_failed exception -- the case round 6 left undefended by any test.
    - dispatch_callback('J7') and dispatch_callback('J9'), driven through the real handlers with a stubbed run_discovery returning an outcome outside the documented contract, raise ValueError, record a durable failed outcome with error_class='ValueError', and emit exactly one job_failed exception -- proving the new default direction is failure, not success.
  </behavior>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-19-REVIEW.md` `### WR-01` in full -- the finding, its own suggested fix, and its own suggested test addition, which this task's action follows closely.
    - `dashboard/beacon/worker_main.py`, `_run_scheduled_discovery` and `_run_startup_discovery` in full (currently lines 189-213; re-read live) -- the exact two `outcome != 'failed'` sites (currently lines 199 and 210) and the skip-path `return None` sites this task must not touch.
    - `dashboard/beacon/worker_main.py`, `dispatch_callback` (currently lines 301-397) -- confirms an exception raised by `_invoke_callback` (which both discovery handlers pass straight through) is caught by the `except Exception as exc:` branch, durably written as `('failed', _job_error_class(exc))`, and then re-raised via `raise work_error` -- so a `ValueError` here reaches the operator as a genuine `job_failed` and also propagates to the caller, exactly like any other worker-owned exception this module already handles.
    - `dashboard/app.py` line 68 (`DISCOVERY_TIMEOUT_SECONDS = SETTINGS.discovery_timeout_seconds`) and `dashboard/beacon/monitoring.py` lines 80-81 (`run_discovery`) -- confirms the production return contract is exactly `'busy' | 'completed' | 'failed'`, so this task changes detection, not the set of values production can actually return today.
    - `tests/test_advanced_diagnosis_api.py`, `test_the_real_discovery_dispatch_honours_the_return_value_vocabulary` in full (currently lines 395-466) -- the exact loop, `services_with` helper, and `job_failed_items` helper this task's two new subTest cases per job_id are added to, immediately after the existing two.
  </read_first>
  <action>In `dashboard/beacon/worker_main.py`, add a new module-level function `_discovery_outcome_verdict(outcome)` immediately above `_run_scheduled_discovery`. Its body: if `outcome` is `'completed'` or `'busy'`, return `True`; if `outcome` is `'failed'`, return `False`; otherwise raise `ValueError` with a message naming the unrecognised literal, e.g. an f-string containing `outcome!r`. Give it a docstring explaining that `run_discovery`'s documented contract is exactly `'busy' | 'completed' | 'failed'`, that `'busy'` is a genuine, expected success (another discovery already owns the work), and that excluding by comparison (`!= 'failed'`) let any future or unrecognised literal read as success by default, so this excludes by membership instead -- an outcome the contract does not name is reported loudly as a job failure, never silently as success.

Replace `_run_scheduled_discovery`'s `return outcome != 'failed'` with `return _discovery_outcome_verdict(outcome)`, and its now-stale two-line comment above that call (which asserted the exclusion-by-comparison reasoning this task replaces) with a one-line pointer to the new helper's own docstring. Make the identical replacement in `_run_startup_discovery`. Do not touch either function's `read_scan_state()` call, its skip-path `return None`, the 300-second recency-window condition, or any other line.

In `tests/test_advanced_diagnosis_api.py`, inside `test_the_real_discovery_dispatch_honours_the_return_value_vocabulary`'s `for job_id in ('J7', 'J9'):` loop, add two new `subTest` blocks immediately after the existing `case='a_genuine_skip_still_records_succeeded'` block, using the same `services_with` and `job_failed_items` helpers already defined in that test. First, `case='a_busy_discovery_lock_is_recorded_as_succeeded'`: build `services` with `run_discovery=lambda _authority, **_kwargs: 'busy'` and `read_scan_state=lambda: {}`; call `worker_main.dispatch_callback(services, job_id)` and assert it returns `True` with `assertIs`; read the durable row and assert `state='succeeded'`; assert `job_failed_items(job_id)` is empty. Second, `case='an_unrecognised_discovery_outcome_fails_closed_and_loud'`: build `services` with `run_discovery=lambda _authority, **_kwargs: 'unrecognised_outcome'` and `read_scan_state=lambda: {}`; wrap the dispatch in `self.assertRaises(ValueError)`; read the durable row and assert `state='failed'` and `error_class='ValueError'`; assert `len(job_failed_items(job_id)) == 1`.</action>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -k discovery_dispatch_honours_the_return_value_vocabulary -q` exits 0.
    - Within that test, for both `J7` and `J9`: a `'busy'` outcome produces `dispatch_callback(...) is True`, a durable `state='succeeded'` row, and no `job_failed` exception; an unrecognised outcome raises `ValueError`, produces a durable `state='failed'`, `error_class='ValueError'` row, and exactly one `job_failed` exception.
    - `test "$(grep -c '_discovery_outcome_verdict' dashboard/beacon/worker_main.py)" = "3"` succeeds -- the definition plus one call site in each of the two functions. Measured at plan time: currently zero.
    - `test "$(grep -c "outcome != 'failed'" dashboard/beacon/worker_main.py)" = "0"` succeeds. Measured at plan time: currently two.
    - `test "$(grep -c 'a_busy_discovery_lock_is_recorded_as_succeeded' tests/test_advanced_diagnosis_api.py)" = "1"` succeeds. Measured at plan time: currently zero.
    - `test "$(grep -c 'an_unrecognised_discovery_outcome_fails_closed_and_loud' tests/test_advanced_diagnosis_api.py)" = "1"` succeeds. Measured at plan time: currently zero.
    - `test "$(grep -c 'def test_' tests/test_advanced_diagnosis_api.py)" = "47"` succeeds -- unchanged by this task, which only adds subTests to an existing test method. Measured at plan time (start of this plan, after 03-20): 47.
    - `uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -q` exits 0.
    - `uv run --project dashboard python -m pytest tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -k discovery_dispatch_honours_the_return_value_vocabulary -q && test "$(grep -c '_discovery_outcome_verdict' dashboard/beacon/worker_main.py)" = "3" && test "$(grep -c "outcome != 'failed'" dashboard/beacon/worker_main.py)" = "0" && test "$(grep -c 'a_busy_discovery_lock_is_recorded_as_succeeded' tests/test_advanced_diagnosis_api.py)" = "1" && test "$(grep -c 'an_unrecognised_discovery_outcome_fails_closed_and_loud' tests/test_advanced_diagnosis_api.py)" = "1" && test "$(grep -c 'def test_' tests/test_advanced_diagnosis_api.py)" = "47" && uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -q && uv run --project dashboard python -m pytest tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -q && echo DISCOVERY_OUTCOME_FAILS_CLOSED</automated>
  </verify>
  <done>An unrecognised discovery outcome is reported loudly as a genuine job failure instead of silently as success, and 'busy' -- the one documented success value that was previously undefended by any test -- is now pinned in both directions for J7 and J9.</done>
</task>

<task type="auto">
  <name>Task 2: Close WR-02 -- state the true asymmetry when J3 loses the uptime lock to J4</name>
  <reversibility rating="reversible">A comment/log-message rewording plus one additional assertion in an existing test; the lock-contention return value, the requeue/lock semantics, and every other line of production behaviour are byte-identical before and after. Single-file revert.</reversibility>
  <files>dashboard/app.py, tests/test_advanced_diagnosis_api.py</files>
  <behavior>
    - _legacy_do_uptime_check's lock-contention branch still returns None on a held _uptime_lock -- completely unchanged behaviour.
    - The comment above that return no longer implies the losing probe's work is equivalent to the winner's; it names the J3 (only_down=False, every 5 min) / J4 (only_down=True, every 1 min) asymmetry explicitly and states that a down-only holder does not perform a full sweep.
    - A regression proves that when a full sweep (J3) yields to a concurrent probe holding the lock, scan_state['last_uptime_check'] is never advanced by that dispatch -- the coverage gap the comment now discloses honestly is not silently claimed as covered.
  </behavior>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-19-REVIEW.md` `### WR-02` in full -- the finding, the structural-collision arithmetic (300 is a multiple of 60), and its own suggested fix, which this task's action follows closely.
    - `dashboard/app.py`, `_legacy_do_uptime_check` in full (currently lines 1332-1458; re-read live) -- the exact lock-contention branch (currently lines 1333-1336) this task edits, and the `state_changes = {'last_down_check': now}` / `if not only_down: state_changes['last_uptime_check'] = now` block (currently lines 1448-1451) that confirms `last_uptime_check` is advanced only on a genuinely-run full sweep, never on a down-only probe and never on a branch that returned before reaching this point.
    - `dashboard/beacon/worker_main.py`, `WORKER_CALLBACK_INVENTORY`'s `J3`/`J4` entries (currently lines 86-87) -- confirms J3's `trigger_kwargs=(('minutes', 5),)` and J4's `trigger_kwargs=(('minutes', 1),)`, and that both share `executor='probes'` -> `ThreadPoolExecutor(2)` -- the structural collision this task's comment names.
    - `tests/test_advanced_diagnosis_api.py`, `test_an_uptime_lock_collision_is_not_reported_as_a_failure` in full (currently lines 517-560) -- the exact test this task extends with one additional assertion; every existing line in it is unchanged.
    - `dashboard/app.py`, `_read_scan_state`/`_update_scan_state` (currently lines 325-343) -- confirms `_read_scan_state()` is safe to call directly from a test with no worker-authority context set (it falls back to the plain `_db_lock` path), and that `_default_scan_state()`'s `last_uptime_check` default is `None`.
  </read_first>
  <action>In `dashboard/app.py`, rewrite `_legacy_do_uptime_check`'s lock-contention comment (currently the two lines above `return None` beginning "Another run already owns this work -- a concurrent uptime probe holding the lock is not a job failure. It self-clears on the next scheduled tick.") to state, in substance: "Another run already owns this work -- yielding here is not a job failure, so this still records succeeded, never a fabricated job_failed. That is NOT the same claim as 'the two runs are equivalent': J3 (only_down=False, every 5 min) and J4 (only_down=True, every 1 min) share this lock and collide by construction every fifth J4 tick, because 300 is a multiple of 60. A down-only holder does not perform a full sweep -- it never advances last_uptime_check. When J3 is the loser, that cycle's full-service coverage is skipped, not covered; the telemetry-coverage surface is what must disclose a real gap, never this return value." The comment must keep its opening four words exactly "Another run already owns" and must contain, verbatim, the phrase "collide by construction every fifth J4 tick" so both are independently greppable. Do not change the `return None` itself, the `_uptime_lock.acquire(blocking=False)` call, or any other line in the function.

In `tests/test_advanced_diagnosis_api.py`, extend `test_an_uptime_lock_collision_is_not_reported_as_a_failure`: after the existing `row = self._job_health_row('J3')` assertions and before the `diagnosis = ...` block, add `state = self.appmod._read_scan_state()` followed by `self.assertIsNone(state['last_uptime_check'])` -- proving the losing J3 dispatch never advanced full-sweep coverage. Update the test's docstring to add one sentence noting this pins the WR-02 asymmetry: the comment now discloses honestly (mention "WR-02" verbatim in the docstring) that a down-only holder's work is not equivalent to a full sweep's, so the loser's own coverage gap must remain visible elsewhere rather than being silently claimed as advanced by this dispatch.</action>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -k uptime_lock_collision -q` exits 0.
    - Within that test, after J3 yields to a held `_uptime_lock`, `self.appmod._read_scan_state()['last_uptime_check']` is `None`.
    - `test "$(grep -c 'collide by construction every fifth J4 tick' dashboard/app.py)" = "1"` succeeds. Measured at plan time: currently zero.
    - `test "$(grep -c 'Another run already owns this work' dashboard/app.py)" = "2"` succeeds -- unchanged in count; the scan-busy branch's comment (closed by 03-20) is untouched by this task, and this task's rewrite of the uptime comment keeps the same opening phrase. Measured at plan time: currently two.
    - `test "$(grep -c 'WR-02' tests/test_advanced_diagnosis_api.py)" = "1"` succeeds. Measured at plan time: currently zero.
    - `test "$(grep -c 'def test_' tests/test_advanced_diagnosis_api.py)" = "47"` succeeds -- unchanged by this task, which only extends an existing test method. Measured at plan time (start of this task): 47.
    - `uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -k uptime_lock_collision -q && test "$(grep -c 'collide by construction every fifth J4 tick' dashboard/app.py)" = "1" && test "$(grep -c 'Another run already owns this work' dashboard/app.py)" = "2" && test "$(grep -c 'WR-02' tests/test_advanced_diagnosis_api.py)" = "1" && test "$(grep -c 'def test_' tests/test_advanced_diagnosis_api.py)" = "47" && uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -q && echo UPTIME_CONTENTION_ASYMMETRY_DISCLOSED</automated>
  </verify>
  <done>The uptime-lock-contention comment states the true asymmetry between a full sweep and a down-only probe instead of implying equivalence, and a regression proves the losing full sweep's own coverage is never silently advanced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close WR-03 -- derive the outcome-unrecorded floor from the configured discovery timeout</name>
  <reversibility rating="costly">Widens a required-argument signature on a function two production call sites and two test call sites use, and changes the value of a load-bearing promotion boundary for any deployment whose DISCOVERY_TIMEOUT_SECONDS exceeds 840. No schema or payload change. Rated costly rather than reversible because a wrong arithmetic choice here is discovered late -- either a legitimately-running discovery is still promoted as a fabricated fault (too narrow) or a genuinely wedged job is never promoted at all (too wide) -- and both directions are the exact class of defect this phase exists to close.</reversibility>
  <files>dashboard/beacon/diagnosis.py, tests/test_advanced_diagnosis_api.py</files>
  <behavior>
    - _unrecorded_outcome_boundary(cadence_seconds, discovery_timeout_seconds) computes floor = max(UNRECORDED_OUTCOME_FLOOR_SECONDS, int(discovery_timeout_seconds) + 60) before applying the existing 4x-cadence comparison.
    - compose_active_exceptions requires discovery_timeout_seconds as a keyword-only argument and threads it into _unrecorded_outcome_boundary at its one call site.
    - get_current_diagnosis passes discovery_timeout_seconds=settings.discovery_timeout_seconds -- the operator's own configured value, not a hardcoded default.
    - With the default DISCOVERY_TIMEOUT_SECONDS=180, every existing subtest in test_a_job_stuck_without_an_outcome_becomes_an_operator_exception continues to pass with byte-identical results, because max(900, 180+60=240) still equals 900.
    - With a configured discovery_timeout_seconds=1200, a J9-shaped row running 1200 seconds old promotes nothing (the floor widens to max(900, 1260)=1260); with discovery_timeout_seconds reverted to 180 for the same 1200-second-old row, it promotes once (the floor stays 900) -- proving the floor genuinely derives from the input, not merely from the hardcoded constant.
  </behavior>
  <read_first>
    - `.planning/phases/03-advanced-current-diagnosis/03-19-REVIEW.md` `### WR-03` in full -- the finding, its own reproduction (`DISCOVERY_TIMEOUT_SECONDS=1200` both failing the floor's own subtests and promoting a legitimately-running J9), and its own suggested fix, which this task's action follows closely.
    - `dashboard/beacon/diagnosis.py` lines 35-54 (`UNRECORDED_OUTCOME_FLOOR_SECONDS`, `_unrecorded_outcome_boundary`) -- read the exact current text; the module comment above the constant currently describes only the hardcoded default and needs one added sentence noting the floor also widens for a configured timeout above it.
    - `dashboard/beacon/diagnosis.py` lines 422-476 (`compose_active_exceptions` in full) and line 468 (`_unrecorded_outcome_boundary(job.get('cadence_seconds'))`, the one call site) -- confirms this is the only production call site to update.
    - `dashboard/beacon/diagnosis.py` lines 504-530 (`get_current_diagnosis` in full) -- confirms `settings` is already an in-scope parameter, so no new plumbing is needed to reach `settings.discovery_timeout_seconds`.
    - `tests/test_advanced_diagnosis_api.py` lines 1533-1567 (`test_coverage_reason_outside_the_durable_enum_surfaces_as_indeterminate` in full) -- its one direct `compose_active_exceptions` call (currently lines 1560-1562) needs the new required argument; `pipeline['jobs']` is empty in this test so the boundary is never actually exercised, but the call must still supply a value.
    - `tests/test_advanced_diagnosis_api.py` lines 1568-1712 (`test_a_job_stuck_without_an_outcome_becomes_an_operator_exception` in full, including its `compose_many`/`compose` helpers at lines 1590-1598) -- the exact helpers this task extends with an optional `discovery_timeout_seconds` parameter, defaulting to `self.appmod.DISCOVERY_TIMEOUT_SECONDS` so every existing subTest's call site is unchanged.
  </read_first>
  <action>In `dashboard/beacon/diagnosis.py`, change `_unrecorded_outcome_boundary`'s signature to `_unrecorded_outcome_boundary(cadence_seconds, discovery_timeout_seconds)`. As its first statement, compute `floor = max(UNRECORDED_OUTCOME_FLOOR_SECONDS, int(discovery_timeout_seconds) + 60)`; replace every subsequent reference to the bare `UNRECORDED_OUTCOME_FLOOR_SECONDS` inside the function body with `floor`, so the existing `4 * cadence_seconds` comparison and the final fallback both use the widened value. Add one sentence to the module comment above `UNRECORDED_OUTCOME_FLOOR_SECONDS` noting that `_unrecorded_outcome_boundary` widens this floor further when the operator's own configured `DISCOVERY_TIMEOUT_SECONDS` exceeds it, so the guarantee holds for any configured value, not only the 180-second default.

Change `compose_active_exceptions`'s signature to add a required keyword-only parameter `discovery_timeout_seconds` after `now`. At its one call to `_unrecorded_outcome_boundary`, pass `job.get('cadence_seconds'), discovery_timeout_seconds`.

In `get_current_diagnosis`, add `discovery_timeout_seconds=settings.discovery_timeout_seconds` to its call to `compose_active_exceptions`.

In `tests/test_advanced_diagnosis_api.py`, update `test_coverage_reason_outside_the_durable_enum_surfaces_as_indeterminate`'s one `compose_active_exceptions` call to pass `discovery_timeout_seconds=self.appmod.DISCOVERY_TIMEOUT_SECONDS`. In `test_a_job_stuck_without_an_outcome_becomes_an_operator_exception`, change `compose_many`'s signature to `compose_many(jobs, *, discovery_timeout_seconds=None)`, and inside it pass `discovery_timeout_seconds=(self.appmod.DISCOVERY_TIMEOUT_SECONDS if discovery_timeout_seconds is None else discovery_timeout_seconds)` to `diagnosis.compose_active_exceptions`; leave `compose(job)` and every existing subTest's call site completely unchanged, since the new parameter defaults to the module's own configured value. Add one new `subTest(case='a_configured_discovery_timeout_widens_the_floor_it_used_to_ignore')`, placed immediately after the existing `case='a_full_discovery_timeout_run_promotes_nothing'` block: assert `compose_many([job_row(job_id='J9', cadence_seconds=None, last_started_ts=now - 1200)], discovery_timeout_seconds=1200) == []` (the widened floor covers it); then assert `[item['job_id'] for item in unrecorded_in(compose_many([job_row(job_id='J9', cadence_seconds=None, last_started_ts=now - 1200)], discovery_timeout_seconds=180))] == ['J9']` (the same 1200-second-old row promotes once the timeout reverts to the default, proving the floor genuinely derives from the input rather than from the hardcoded constant alone).</action>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -k 'job_stuck_without_an_outcome or coverage_reason_outside_the_durable_enum' -q` exits 0.
    - Within the new subTest, `discovery_timeout_seconds=1200` promotes nothing for a 1200-second-old J9 row; `discovery_timeout_seconds=180` promotes exactly `['J9']` for the identically-aged row.
    - `test "$(grep -c 'discovery_timeout_seconds' dashboard/beacon/diagnosis.py)" = "6"` succeeds. Measured at plan time: currently one.
    - `test "$(grep -c 'a_configured_discovery_timeout_widens_the_floor_it_used_to_ignore' tests/test_advanced_diagnosis_api.py)" = "1"` succeeds. Measured at plan time: currently zero.
    - `test "$(grep -c 'def test_' tests/test_advanced_diagnosis_api.py)" = "47"` succeeds -- unchanged by this task, which only adds one subTest to an existing test method and widens two helper signatures. Measured at plan time (start of this task): 47.
    - `uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -q` exits 0.
    - `uv run --project dashboard python -m pytest tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -q` exits 0, and both cross-module orders of `tests/test_advanced_diagnosis_api.py` against `tests/test_runtime_ownership.py` exit 0.
    - `uv run --project dashboard python -m pytest -q` exits 0, and reports strictly more than the pre-round baseline of 301 passed / 421 subtests (measured before 03-20 and 03-21 executed). Nothing previously green may go red.
    - `test "$(grep -c '^| TEL-06 | Phase 3 | Gaps Found |$' .planning/REQUIREMENTS.md)" = "1"` succeeds and `test "$(grep -c '^- \[ \] \*\*TEL-06\*\*' .planning/REQUIREMENTS.md)" = "1"` succeeds -- this round closes CR-01 and all four warnings but must leave TEL-06 open in both halves of the record; only independent re-verification may promote it.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -k 'job_stuck_without_an_outcome or coverage_reason_outside_the_durable_enum' -q && test "$(grep -c 'discovery_timeout_seconds' dashboard/beacon/diagnosis.py)" = "6" && test "$(grep -c 'a_configured_discovery_timeout_widens_the_floor_it_used_to_ignore' tests/test_advanced_diagnosis_api.py)" = "1" && test "$(grep -c 'def test_' tests/test_advanced_diagnosis_api.py)" = "47" && uv run --project dashboard python -m pytest tests/test_advanced_diagnosis_api.py -q && uv run --project dashboard python -m pytest tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -q && uv run --project dashboard python -m pytest -q && test "$(grep -c '^| TEL-06 | Phase 3 | Gaps Found |$' .planning/REQUIREMENTS.md)" = "1" && echo FLOOR_DERIVES_FROM_CONFIGURED_TIMEOUT</automated>
    <human-check>
      1) On the target Pi, open /advanced while a real collection gap is active and while host evidence is stale. Expected: the workspace shows the open gap and the stale host as real, correctly labelled exceptions, and shows no resolved or retention-expired interval as an open actionable gap. Carried forward unchanged from every verification report and every prior gap-closure plan's own human-check block since round 3; collected at the end-of-phase human checkpoint, not automated here.
      2) On the target Pi, start the worker and leave the system idle for one minute, then open /advanced and read the Overview "Active exceptions" region. Expected: no "Background job failed" card for J5 or J6, and no "Background job outcome not recorded" card for any job that is simply working. Recorded by round 4 specifically so a gap-closure round could not declare success without it being checked on real hardware; still NOT performed on real hardware through six prior rounds of automated reproduction.
      3) On the target Pi, with one monitored service that is either offline or serves a page with no `<title>`, leave the worker running for a normal preview poll cycle, then open /advanced. Expected: the service's own row on the Services surface shows its offline/gap-evidence state as before, and the Overview "Active exceptions" region shows no "Background job failed" card for J6 -- proving CR-01's fix holds on real hardware, not only in the synthetic reproduction this round's plans pinned.
    </human-check>
  </verify>
  <done>The job_outcome_unrecorded floor derives its guarantee from the operator's own configured DISCOVERY_TIMEOUT_SECONDS instead of silently disagreeing with it, closing the last of round 6's four review warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery outcome literal -> success/failure classification | An outcome outside `run_discovery`'s documented three-value contract must classify as a loud failure, never a silent success. |
| uptime-lock-contention disclosure -> operator-trusted comment | A comment describing a non-fault outcome must never claim two different probes' work is equivalent when it is not. |
| operator-configured DISCOVERY_TIMEOUT_SECONDS -> promotion-floor guarantee | The floor's own stated guarantee (sitting above the discovery timeout) must hold for the value the operator actually configured, not only the hardcoded default. |
| worker epoch authority -> SQLite write | No task may weaken `assert_current_worker_authority` or the admission/authority fence `dispatch_callback` already enforces. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-124 | Repudiation | `_run_scheduled_discovery` / `_run_startup_discovery` fail-open outcome mapping | medium | mitigate | Task 1's `_discovery_outcome_verdict` fails closed on any literal outside the documented contract; a regression proves both the undefended `'busy'` success case and the new fail-closed-and-loud case for J7 and J9. |
| T-03-125 | Repudiation | Uptime-lock-contention comment overstating equivalence between J3 and J4 | low | mitigate | Task 2 corrects the comment and pins, with a regression, that the losing full sweep's own coverage is never silently claimed as advanced. |
| T-03-126 | Repudiation | `job_outcome_unrecorded` floor silently disagreeing with a configured `DISCOVERY_TIMEOUT_SECONDS` | high | mitigate | Task 3 threads the operator's own configured value into the floor's own arithmetic; a regression proves the floor widens and narrows with the input in both directions. |
| T-03-127 | Denial of Service | Widening the floor so far that a genuinely wedged discovery is never promoted | low | accept | The widened floor is still `max(900, discovery_timeout_seconds + 60)` -- it grows only with the operator's own configured budget, and the existing `overdue_running_job_promotes_once` / `absent_cadence_past_the_floor_promotes_once` subtests (unmodified by this plan) continue to prove a genuinely wedged job is still promoted. |
| T-03-128 | Tampering | Worker authority fence and cross-module test-order independence across all three edits | high | mitigate | No task touches `_write_job_health_transition`, `assert_current_worker_authority`, or the admission/authority fence; `tests/test_worker_ownership_matrix.py` and `tests/test_runtime_ownership.py`, plus both cross-module orders, are re-run as acceptance criteria. |
| T-03-129 | Tampering | Supply chain (npm / pip / cargo installs) | n/a | accept | No package-manager install task exists in this plan. No dependency is added, removed, upgraded, or pinned. No `## Package Legitimacy Audit` is required. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|----------------|---------------|------------------------|
| V7 Error Handling and Logging | Applicable | An unrecognised outcome literal and a silently-disagreeing promotion floor both become loud, corrected signals instead of silent divergences from the documented contract. |
| V4 Access Control | Applicable | The worker authority fence and lease-loss shutdown paths are preserved verbatim and re-asserted by the ownership suites in every task. |
| V5 Validation, Sanitization and Encoding | Applicable | `_discovery_outcome_verdict` type-checks against the exact three-literal contract by membership, not by exclusion; the floor's arithmetic reads a typed settings attribute (`discovery_timeout_seconds`) rather than parsing free text. |
| V2 Authentication / V3 Session Management / V6 Stored Cryptography | Not applicable | Trusted-LAN scope; no identity, session, or cryptographic material is introduced. |
</threat_model>

<gap_coverage>

| Verification finding | Disposition | Closure evidence |
|---|---|---|
| WR-01 -- fail-open discovery-outcome mapping, undefended `'busy'` success case | IMPLEMENT in Task 1 (fix-now; see Warnings Triage) | `_discovery_outcome_verdict` fails closed by membership; two new subTest cases per job pin `'busy'` and an unrecognised literal in both directions. |
| WR-02 -- uptime-contention comment overstates equivalence | IMPLEMENT in Task 2 (fix-now; see Warnings Triage) | Comment rewritten to name the true asymmetry; a regression proves the losing full sweep's `last_uptime_check` is never advanced. |
| WR-03 -- hardcoded floor silently disagrees with a configured `DISCOVERY_TIMEOUT_SECONDS` | IMPLEMENT in Task 3 (fix-now; see Warnings Triage) | The floor's arithmetic now derives from the operator's own configured value; a regression proves both the widened and the narrowed direction. |
| CR-01, WR-04 | NOT IN THIS PLAN, closed in wave-1 sibling 03-20 | See `03-20-PLAN.md`'s own gap-coverage table. |
| Deferred -- DIA-08 `range` clause | NOT PLANNED, deliberate | Ruled a Phase 4 deferral by round 4 and re-confirmed every round since; no closure work exists for it in this round. |

</gap_coverage>

## Warnings Triage (this plan's share)

Continuing 03-20-PLAN.md's triage of all four review warnings; WR-04 was closed there. Each of the remaining three is triaged explicitly here with a fix-now / defer-with-record call and rationale, per the gap contract.

- **WR-01 -- FIX-NOW, in Task 1.** Rationale: the fail-open exclusion comparison (`outcome != 'failed'`) is a direct instance of this phase's central pattern -- an unguarded default direction of success -- and the fix is a small, pure, single-file refactor into an explicit membership check with no schema or contract change. The `'busy'` success case being undefended by any test for two full rounds is itself a completeness gap in the phase's own regression suite that this task closes alongside the code. None of the three legitimate reasons to defer applies.
- **WR-02 -- FIX-NOW, in Task 2.** Rationale: this is a documentation/comment correction plus one additional assertion in an existing test -- zero production behaviour changes, so there is no risk of introducing a new operator-facing defect, and leaving a misleading comment in place beside code this phase has repeatedly revisited would itself invite a future round to misread the trade-off. None of the three legitimate reasons to defer applies.
- **WR-03 -- FIX-NOW, in Task 3.** Rationale: the reviewer's own reproduction (`DISCOVERY_TIMEOUT_SECONDS=1200` both failing the floor's own subtests and promoting a legitimately-running J9) demonstrates this is a live, reachable defect for any deployment configured above the 840-second threshold implied by the current hardcoded floor, not a theoretical concern -- exactly the class of fabricated-fault untruth this phase exists to close. The fix is bounded to one function's arithmetic, one additional required parameter threaded through two call sites, and two test call-site updates; none of the three legitimate reasons to defer applies.

## Multi-Source Coverage Audit

```
SOURCE    | ID       | Feature/Requirement                                              | Plan  | Status  | Notes
--------- | -------- | ----------------------------------------------------------------- | ----- | ------- | -----
GOAL      | --       | Operator can diagnose the Pi, services, settings, collection health | 03-21 | COVERED | Closes the three remaining review warnings from `03-19-REVIEW.md` round 6, completing the full set alongside wave-1 sibling `03-20`; the other three ROADMAP truths are unaffected and already independently verified.
REQ       | TEL-06   | Effective retention/resolution/pressure/worker-freshness/collection-gaps/background-job-health | 03-21 | COVERED | The only requirement ID this plan's `requirements:` frontmatter carries; the other five phase requirements are unaffected by this round.
RESEARCH  | Pattern 3 | Durable job outcome evidence at worker dispatch | 03-21 | COVERED | Completes the outcome vocabulary's fail-closed direction and the promotion floor's own external-fact guarantee, per this task's own read_first pointers into `worker_main.py` and `diagnosis.py`.
RESEARCH  | Pitfall 2 | Making background job health process-local | 03-21 | COVERED (unaffected) | Durable persistence under worker authority is untouched by every task in this plan. |
CONTEXT   | D-11     | Do not infer a failed cause from stale data alone | 03-21 | COVERED (unaffected) | No task in this plan infers a failure cause from staleness; Task 3's floor widens the window a legitimately-running job is judged against, it does not change how a cause is inferred. |
CONTEXT   | D-09     | Keep simultaneous safety/freshness/pipeline conditions distinct | 03-21 | COVERED (unaffected) | `job_failed` and `job_outcome_unrecorded` remain distinct kinds; no task in this plan merges or reorders exception kinds. |
```

All rows COVERED; no `⚠ MISSING` row exists in this audit.

## Edge Coverage Disposition (specless probe fallback)

Same 13-edge report as `03-20` (requirement texts unchanged this round). This plan does not author any additional edge beyond the one `03-20` already authored (`TEL-06`/`adjacency`); the equality established there (13 = 1 authored + 12 flagged) is unaffected by this plan's three tasks, none of which touches a different requirement's surface:

| requirement_id | category | disposition |
|---|---|---|
| TEL-06 | empty | Flagged assumption -- resolved by `03-18-PLAN.md`'s original authoring; unaffected by this plan. |
| TEL-06 | adjacency | Authored by `03-20-PLAN.md` (CR-01's fix); unaffected by this plan's tasks, none of which touches `worker_process_preview_requests` or the J7/J9 skip-path boundary that edge's regression exercises. |
| TEL-06 | ordering | Flagged assumption -- resolved by round 5's `two_overdue_jobs_promote_in_stable_job_id_order` subtest, unmodified by this plan. |
| DIA-01 | unclassified | Flagged assumption -- already covered by 03-01/03-05; untouched by this plan. |
| DIA-02 | unclassified | Flagged assumption -- already covered by 03-02/03-06; untouched by this plan. |
| DIA-03 | adjacency | Flagged assumption -- already covered by 03-04/03-11/03-13; untouched by this plan. |
| DIA-03 | empty | Flagged assumption -- already covered by 03-04/03-11/03-13; untouched by this plan. |
| DIA-03 | ordering | Flagged assumption -- already covered by 03-04/03-11/03-13; untouched by this plan. |
| DIA-08 | adjacency | Flagged assumption -- already covered by 03-03/03-09; the `range` clause remains the explicit Phase-4 deferral this plan must not touch. |
| DIA-08 | empty | Flagged assumption -- already covered by 03-03/03-09; untouched by this plan. |
| DIA-08 | ordering | Flagged assumption -- already covered by 03-03/03-09; untouched by this plan. |
| UX-02 | unclassified | Flagged assumption -- already covered by 03-05/03-07; untouched by this plan. |
| UX-02 | empty | Flagged assumption -- already covered by 03-05/03-07; untouched by this plan. |

**`03-UI-SPEC.md` `## UI Considerations` (36 covered / 0 backstop / 0 unresolved).** No task in this plan touches `dashboard/advanced.js`, `advanced.html`, or `advanced.css`; all 36 rows remain resolved-explicit and unaffected.

<verification>

- `uv run --project dashboard python -m pytest -q` exits 0 and reports strictly more than the pre-round baseline of 301 passed / 421 subtests (measured before `03-20` and `03-21` executed). Nothing previously green may go red.
- `uv run --project dashboard python -m pytest tests/test_worker_ownership_matrix.py tests/test_runtime_ownership.py -q` passes, proving the authority fence and lease-fenced dispatch paths survive all three edits.
- `tests/test_advanced_diagnosis_api.py` passes alone and in both module orders against `tests/test_runtime_ownership.py`.
- No durable job-health row, no raised condition's own message, and no new log line or comment carries an exception message, path, or SQL fragment.
- `.planning/REQUIREMENTS.md` is unchanged by this plan.
- All three human-verification items are carried forward in Task 3's `<human-check>` block, not automated away.

</verification>

<success_criteria>

- An unrecognised discovery outcome fails closed and loud; `'busy'` is now pinned as a genuine success for both J7 and J9.
- The uptime-lock-contention comment states the true asymmetry between a full sweep and a down-only probe, and a regression proves the loser's coverage is never silently advanced.
- The `job_outcome_unrecorded` floor derives its guarantee from the operator's own configured `DISCOVERY_TIMEOUT_SECONDS`, closing the reviewer's own reproduced defect.
- All five carried-forward prohibitions from 03-08/03-09/03-10 are fully addressed by the combination of `03-20` and this plan.
- TEL-06 remains open in both halves of `.planning/REQUIREMENTS.md`; promotion is the next round's verifier's call, not this plan's.
- All three human-verification items (two carried forward, one new for CR-01) are documented in this plan's human-check block for the end-of-phase checkpoint.

</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-21-SUMMARY.md` when done.
</output>
