---
phase: 03-advanced-current-diagnosis
plan: 23
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/advanced.css
  - tests/test_advanced_ui.py
autonomous: true
gap_closure: true
gap_ids: [G-03-4]
requirements: [UX-02]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every click target in the /advanced workspace computes `cursor: pointer` in BOTH themes -- the two header buttons (`#advanced-refresh`, `#pause-updates`), the refresh-interval select, all five `.section-navigation` tabs, the three `.service-filters` buttons and four filter selects, the `.settings-grid` selects, every dynamically-created `.service-details-toggle`, and every `#services-table th button` sort control. Pinned by a Playwright `getComputedStyle(node).cursor` sweep run once per theme, not by a source grep alone."
    - "Hovering any of those controls produces a computed style change a mouse user can see, in BOTH themes: a neutral control's `borderTopColor` moves from the near-invisible `var(--border)` to `var(--accent)` (dark) or to `#94a3b8` (light), and a `.section-navigation` tab's moves from fully transparent to a visible border. Pinned by a resting-value / real `locator.hover()` / hovered-value comparison per control, with `page.mouse.move(0, 0)` between reads so no resting value is captured while the pointer is still on the element."
    - "`#advanced-refresh` carries the accent treatment 03-UI-SPEC.md:76 reserves for the `Refresh now` action: at rest its computed text colour EQUALS the computed colour of `.advanced-header a` (the Dashboard return link, the other control on this page the same UI-SPEC line reserves accent for) and DIFFERS from `#pause-updates`'s; it carries an accent border and 600 weight that no other header control has; and hovering it changes its computed `backgroundColor`. All four facts asserted in both themes."
    - "The accent reservation is not widened by this fix. No ordinary control takes accent TEXT at rest or on hover: `#pause-updates`, the unselected `[data-section=\"host\"]` tab, and the same tab WHILE HOVERED all compute a text colour different from `#advanced-refresh`'s accent, while the `.section-navigation button[aria-selected=\"true\"]` tab still computes exactly that accent -- so `aria-selected` remains the only nav state that earns accent, exactly as 03-UI-SPEC.md:76 requires."
    - "Pointer is applied to true click targets only, never blanket-applied: `#service-search` (the `input type=\"search\"` a user types into) computes a cursor that is NOT `pointer`. Pinned by an explicit negative assertion in the same sweep."
    - "Nothing already guaranteed by advanced.css regresses: the file still contains exactly 6 `min-height: 44px` declarations (the established narrow-dashboard 44px hit-target rule, 03-UI-SPEC.md:44), still contains exactly one `focus-visible` rule with its accent outline intact, and its two `@media` blocks are still the last two lines of the file."
    - "The fix is presentation-only and confined to `dashboard/advanced.css` plus its regression in `tests/test_advanced_ui.py`. `dashboard/advanced.html`, `dashboard/advanced.js`, `dashboard/style.css`, `dashboard/app.py`, `dashboard/beacon/diagnosis.py`, and `dashboard/beacon/previews.py` are byte-unchanged by this plan."
    - statement: "On the real Pi, in both themes, the `Refresh now` button READS to a human operator as the primary action of the header -- the specific complaint that opened G-03-4 (\"doesn't feel like it's a button\"). A computed-style delta proves the browser applied the rule; it cannot prove the operator perceives it. Collected at the end-of-phase human checkpoint (Task 2's human-check item 1)."
      verification: backstop
    - statement: "On the real Pi, in both themes, the hover feedback is PERCEPTIBLE at the display's actual brightness and viewing angle -- an 8%/12% accent tint and a border-colour shift are measurable in getComputedStyle regardless of whether they are visible on that panel. Collected at the end-of-phase human checkpoint (Task 2's human-check item 2)."
      verification: backstop
    - statement: "UX-02's theme round-trip boundary behaviour (navigating between / and /advanced at the exact return point, and the scroll-restore contract) is unaffected: this plan adds only colour, cursor, font-weight and border-colour declarations to advanced.css and touches no theme, storage, navigation, or scroll code (UX-02 boundary edge)."
      verification: backstop
    - statement: "UX-02's scroll-offset precision/rounding contract is unaffected by this plan; no declaration added here participates in layout geometry -- no width, height, margin, padding, position, display, or grid property is introduced or changed (UX-02 precision edge)."
      verification: backstop
  artifacts:
    - path: "dashboard/advanced.css"
      provides: "The interactive-affordance block: one grouped pointer rule for every click target, a dark and a light hover rule for neutral surface controls, a dark and a light hover rule for the transparent navigation tabs, and the three-rule accent treatment 03-UI-SPEC.md:76 reserves for #advanced-refresh"
      contains: "#advanced-refresh"
    - path: "tests/test_advanced_ui.py"
      provides: "test_every_interactive_control_reads_as_interactive_in_both_themes -- a real-browser computed-style sweep that proves pointer, hover delta, the accent reservation, the negative (search input keeps its text caret), and the 44px non-regression, once per theme"
      contains: "reads_as_interactive_in_both_themes"
  key_links:
    - "advanced.css `#advanced-refresh` accent rules <-> 03-UI-SPEC.md:76's accent reservation list. The link is asserted, not assumed: the test compares Refresh now's computed colour against the Dashboard return link's, the OTHER control on that same reserved list, so the two cannot silently drift apart."
    - "The new hover rules <-> the pre-existing `button:focus-visible` rule (advanced.css:56). No new rule sets `outline`, so keyboard focus keeps its own distinct accent outline and is never replaced by mouse-hover feedback."
    - "The new rules <-> the two trailing `@media` blocks. The affordance block is inserted BEFORE them so the file's existing base-rules-then-breakpoints structure survives; the narrow breakpoints override layout only and never touch colour or cursor, so no new rule is silently dropped below 960px or 720px."
    - "`html:not(.light)` / `html.light` selector pairs <-> `dashboard/advanced.js:48`'s `classList.toggle('light', localStorage.getItem('beacon-theme') === 'light')`. The theme class the hover rules key off is the same one the workspace already sets on load, which is why the test can select a theme with a single `add_init_script` and no UI interaction."
---

<objective>
Close `03-UAT.md` gap `G-03-4`, the single remaining open item in Phase 3: the `/advanced` workspace has no mouse affordance at all. `dashboard/advanced.css` contains ZERO `cursor` declarations and ZERO `:hover` rules across all 58 lines (measured at plan time: `grep -c 'cursor' dashboard/advanced.css` -> 0, `grep -c ':hover' dashboard/advanced.css` -> 0). `#advanced-refresh` inherits only the neutral `.advanced-header-controls button` rule at advanced.css:9 (`color: var(--text); background: var(--bg3)`), so it looks identical to `Pause updates`, gives no pointer, answers no hover, and carries none of the accent that 03-UI-SPEC.md:76 reserves specifically for the `Refresh now` action. The only interaction rule in the file is `button:focus-visible` (advanced.css:56), which serves keyboard users and leaves mouse users with nothing. The user's report was exact: "the refresh now button doesn't feel like it's a button, doesn't react to hover and doesn't show a click pointer when hovering over it."

**This is a presentation-only defect and gets a presentation-only fix.** No server, diagnosis, preview, or worker code is touched. `dashboard/advanced.html` is not touched either -- every selector this plan needs already exists in the markup or is already produced by `dashboard/advanced.js`, confirmed by reading both files this session.

**The convention to match is the project's own.** `dashboard/style.css` -- the main dashboard, loaded by `/advanced` too -- already defines this exact vocabulary: `cursor: pointer` on the base rule of every click target (`.btn-scan` at 167, `.theme-toggle` at 188, `.svc-card` at 382, `.svc-edit` at 487, `.meta-btn` at 715, `.meta-advanced summary` at 684), and hover states split per theme with the `html:not(.light) X:hover` / `html.light X:hover` pair (`.btn-scan` at 176/182, `.svc-edit` at 489/491, `.svc-card` at 389/401). Dark hover tints with the accent's own RGB at low alpha (`rgba(0,212,255,0.08)` at 176, `rgba(0,212,255,0.1)` at 731); light hover darkens the border and text (`#94a3b8` / `#0f172a` at 817-819). The primary-CTA idiom is `.meta-btn-primary` (728-732): accent border plus accent text plus an accent tint on hover. This plan reuses those values verbatim rather than inventing a new visual language for one page.

Purpose: UX-02 ("the operator can move clearly between the main dashboard and advanced analytics without losing theme choice") is recorded Complete, and the other three UAT tests for this phase passed on real hardware on 2026-08-20. This one cosmetic gap is what stands between Phase 3 and a clean UAT. It is also a real usability defect: an operator diagnosing a live incident should not have to guess which header element is the button that refetches the diagnosis.

Output: `dashboard/advanced.css` gains a nine-line interactive-affordance block. `tests/test_advanced_ui.py` gains one real-browser regression that drives Chromium through both themes and asserts computed cursor, a hover-induced computed style delta, the accent reservation (positively for `Refresh now`, negatively for every ordinary control), the negative case (the search input keeps its text caret), and the 44px hit-target non-regression. Three standing hardware-verification items plus two new perception items are carried into the end-of-phase human checkpoint rather than automated away.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced-current-diagnosis/03-UAT.md
@.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md
@dashboard/advanced.css
@dashboard/advanced.html
@dashboard/style.css
@tests/test_advanced_ui.py
</context>

## Artifacts this plan produces

Every new selector and rule introduced into `dashboard/advanced.css`, in the order they appear in the file after both tasks. All nine lines sit between the existing `button:focus-visible` rule (currently line 56) and the two existing `@media` blocks (currently lines 57-58), which remain the last two lines of the file.

| # | Selector | Declarations | Added by | Why |
|---|----------|--------------|----------|-----|
| 1 | *(one comment line)* | -- | Task 1 | States what the block is for and that the accent stays reserved. Must not contain any literal an acceptance-criteria grep counts (see Task 1's action). |
| 2 | `.advanced-header-controls button, .advanced-header-controls select, .section-navigation button, .service-filters button, .service-filters select, .settings-grid select, .service-details-toggle, #services-table th button` | `cursor: pointer` | Task 2 | One grouped rule covering every click target the gap's `missing` list names, plus `.service-filters select` and `.settings-grid select` (see the scope note below). |
| 3 | `html:not(.light) .advanced-header-controls button:hover, ... select:hover, .service-filters button:hover, ... select:hover, .settings-grid select:hover, .service-details-toggle:hover, #services-table th button:hover` | `background: rgba(0, 212, 255, 0.08); border-color: var(--accent)` | Task 2 | Dark hover for every neutral `var(--bg3)` + `var(--border)` control. Values verbatim from `style.css:176` (`.btn-scan:hover`). |
| 4 | `html.light` counterpart of the same selector list | `color: #0f172a; border-color: #94a3b8` | Task 2 | Light hover. Values verbatim from `style.css:817-819` (`html.light .meta-btn-ghost:hover`). |
| 5 | `html:not(.light) .section-navigation button:hover` | `background: rgba(0, 212, 255, 0.08); border-color: var(--border)` | Task 2 | The nav tabs have a different base (`transparent` background, `1px solid transparent` border), so they need their own pair. Deliberately sets NO `color`, so the selected tab's accent text survives being hovered. |
| 6 | `html.light .section-navigation button:hover` | `background: var(--bg3); border-color: #cbd5e1` | Task 2 | Light counterpart. `#cbd5e1` is the light border value `style.css:811-815` already uses. |
| 7 | `#advanced-refresh` | `cursor: pointer; color: var(--accent); border-color: var(--accent); font-weight: 600` | Task 1 | The accent treatment 03-UI-SPEC.md:76 reserves for `Refresh now`. `var(--accent)` resolves per theme automatically (dark `#00d4ff`, light `#0066cc`), so one rule serves both. 600 is one of the two weights 03-UI-SPEC.md:52-57 permits; no third weight is introduced. |
| 8 | `html:not(.light) #advanced-refresh:hover` | `background: rgba(0, 212, 255, 0.12)` | Task 1 | Dark primary hover, the `.meta-btn-primary:hover` idiom (`style.css:730-732`). |
| 9 | `html.light #advanced-refresh:hover` | `background: rgba(0, 102, 204, 0.10)` | Task 1 | Light primary hover. `rgba(0, 102, 204, ...)` is the light `--accent` `#0066cc`'s own RGB -- derived from the token, not an invented colour. |

No new file, markup element, class, id, data attribute, CSS custom property, `@import`, `url()`, font, image, animation, transition, or media query. No package added, removed, or upgraded.

**Two deliberate scope decisions, recorded so a reviewer does not have to re-derive them:**

- **Included beyond the gap's literal `missing` list:** `.service-filters select` and `.settings-grid select`. The gap's `missing` list enumerates `.advanced-header-controls button/select`, `.section-navigation button`, `.service-filters button`, `.service-details-toggle`, `#services-table th button`. But the gap's `truth` is broader -- "*Interactive controls* in the /advanced workspace read as interactive" -- and giving the header's select a pointer while the seven other selects on the same page have none would leave that truth half-false and the page internally inconsistent. Same file, same rule, same truth: included.
- **Deliberately NOT included:** any `a:hover` rule for `.advanced-header a` (the Dashboard return link). It already reads as interactive without a new rule -- the UA stylesheet gives it both `cursor: pointer` and an underline (confirmed this session: `dashboard/style.css` contains no global `a { text-decoration: none }` reset; its only `text-decoration: none` declarations are scoped to `.svc-card`/`.svc-link`/`.svc-title-link` at lines 381-385), and advanced.css:4 already gives it `color: var(--accent)` per 03-UI-SPEC.md:76. `dashboard/style.css` defines no `a:hover` anywhere, so adding one would DEPART from the convention this gap asks us to match rather than follow it. Task 2's test asserts the link's affordance positively (its computed cursor is `pointer`) so the decision is evidenced, not merely asserted.
- **Deliberately NOT included:** any `:disabled` or `:active` rule. `grep -n "disabled" dashboard/advanced.js` returns exactly ONE match, at `advanced.js:702`, and it is a display string (`'Trusted-LAN TLS; certificate verification disabled'`), not a `disabled` assignment -- nothing in the advanced workspace ever disables a control, so a `cursor: not-allowed` rule would be dead code. Do not treat that single prose match as a contradiction. `:active` is outside the reported defect.
- **Deliberately NOT included:** any `transition`. `dashboard/style.css` uses transitions only on the theme toggle pip and the gauge arcs, never on a hover state; its hovers are instant. Matching that keeps the change consistent and keeps it clear of the global `@media (prefers-reduced-motion: reduce)` block at `style.css:902-910`, which already applies to `/advanced` because `advanced.html:7` loads `style.css` before `advanced.css`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "Refresh now reads and answers as the primary action" -- one control, both themes</name>
  <precondition>Playwright's bundled Chromium is installed and local loopback TCP sockets are permitted for this shell. `tests/test_advanced_ui.py` starts a real `werkzeug` server on `127.0.0.1` and launches Chromium at `playwright.chromium.executable_path`; a sandbox that blocks loopback makes every test in this file ERROR rather than fail (recorded in `03-01-SUMMARY.md`: the full suite "passed after rerunning outside the sandbox, whose default policy blocks local loopback sockets required by browser/proxy tests"). Assert it before running anything: `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k precision_and_accessible -x` must exit 0 against the untouched tree. If it errors on a connection or browser-launch failure, HALT and report the environment problem -- do not interpret it as a code failure and do not proceed to Task 2.</precondition>
  <reversibility rating="reversible">Four lines appended to one stylesheet, plus one new test method. Nothing else in the workspace changes shape, size, or position -- the four declarations are `cursor`, `color`, `border-color`, and `font-weight`, none of which participates in layout geometry. Single-concept revert: delete the four lines and the test method.</reversibility>
  <files>dashboard/advanced.css, tests/test_advanced_ui.py</files>
  <behavior>
    - In BOTH themes, `#advanced-refresh` computes `cursor: pointer`. Before this task it computes the button UA default.
    - In BOTH themes, `#advanced-refresh` computes a text colour EQUAL to `.advanced-header a`'s (the Dashboard return link, which advanced.css:4 already gives `color: var(--accent)`) and DIFFERENT from `#pause-updates`'s. Before this task all three of them compute `var(--text)` except the link, so the equality fails and the difference fails.
    - In BOTH themes, `#advanced-refresh` computes a `borderTopColor` DIFFERENT from `#pause-updates`'s, and a `fontWeight` of `600` where `#pause-updates` computes the inherited weight. This is the visible "primary" signal that does not depend on colour perception alone.
    - In BOTH themes, moving the real mouse onto `#advanced-refresh` via `locator.hover()` changes its computed `backgroundColor`. Dark: from `rgb(4, 21, 37)` (`var(--bg3)`) to `rgba(0, 212, 255, 0.12)`. Light: from `rgb(247, 247, 247)` to `rgba(0, 102, 204, 0.1)`. The exact strings are NOT asserted -- the assertion is inequality against the value read at rest, so the test survives a future token change without going false-green.
    - `#advanced-refresh` still computes `min-height: 44px` (advanced.css:7's hit-target rule is untouched).
    - The theme is selected the way the workspace itself selects it: `page.add_init_script("localStorage.setItem('beacon-theme', 'light');")` before `goto`, which `dashboard/advanced.js:48` reads to toggle `html.light`. No UI interaction, no second page object per assertion.
  </behavior>
  <read_first>
    - `dashboard/advanced.css` in full (58 lines) -- specifically line 4 (`.advanced-header a { color: var(--accent); ... }`, the accent-reserved Dashboard link this task compares against), line 7 (`min-height: 44px` on the header controls), line 9 (`.advanced-header-controls button, .advanced-header-controls select { color: var(--text); background: var(--bg3); border: 1px solid var(--border); padding: 0 12px; font: inherit; }`, the neutral rule `#advanced-refresh` currently inherits and this task overrides by ID), line 56 (`button:focus-visible, ...`, the insertion point and the one rule that must keep working untouched), and lines 57-58 (the two `@media` blocks that must remain the file's last two lines). Note the file's house style: ONE rule per line, however long the selector list (line 38 carries six selectors on one line). New rules follow it.
    - `dashboard/advanced.html` lines 12-21 -- confirms `<a href="/" id="advanced-dashboard">Dashboard</a>` (line 13), `<button type="button" id="pause-updates">` (line 18) and `<button type="button" id="advanced-refresh">Refresh now</button>` (line 19) are all static markup inside `.advanced-header-controls`, present on first paint with no payload required. This is why the tracer needs no API fixture beyond the existing `_snapshot()` helper.
    - `dashboard/style.css` lines 713-732 -- `.meta-btn` / `.meta-btn:disabled` / `.meta-btn-ghost:hover` / `.meta-btn-primary` / `.meta-btn-primary:hover`. This is the exact primary-CTA analog: `cursor: pointer` on the base rule, `border-color: rgba(0, 212, 255, 0.35); color: var(--accent);` for primary, `background: rgba(0, 212, 255, 0.1);` on primary hover. Also lines 166-182 (`.btn-scan` and its two theme-scoped hovers) and 808-829 (the `html.light` counterparts, `#94a3b8` / `#0f172a` / `#1d4ed8`). This task uses `var(--accent)` rather than style.css's hardcoded `#1d4ed8` for the light border, because 03-UI-SPEC.md:71 names `#0066cc` -- the value `var(--accent)` already resolves to under `html.light` -- as this workspace's light accent.
    - `dashboard/style.css` lines 4-41 -- the `--accent` token in both themes (`#00d4ff` dark at line 13, `#0066cc` light at line 33). Confirms the two rgba tints this task writes are the accent's OWN RGB channels at low alpha, derived from the tokens rather than invented, and that one `#advanced-refresh { color: var(--accent) }` rule correctly serves both themes.
    - `dashboard/style.css` lines 108-114 -- `.topbar { ... background: var(--bg); ... }`. `advanced.html:12` gives the header `class="topbar advanced-header"`, so the surface BEHIND the header buttons is `var(--bg)` (`#010a14` dark, `#ffffff` light), not `var(--bg3)`. This is what the translucent hover tints composite against; confirms both tints resolve to a visible brightening rather than a wash.
    - `tests/test_advanced_ui.py` lines 1-16 and 44-86 -- the module imports, `ROOT`, the `setUpClass` that starts the werkzeug server and launches Chromium, and the `_snapshot()` static helper (host-only payload, no `services`/`pipeline`/`settings` keys). The tracer uses `_snapshot()` unmodified.
    - `tests/test_advanced_ui.py` lines 86-110 -- `test_direct_route_tracer_shows_loading_then_host_and_retains_last_success_on_failure`, the closest existing shape: the `route_api` closure that fulfills `/api/advanced/current` and `route.fallback()`s everything else, `page.route('**/api/**', route_api)`, `page.goto(f'{self.base_url}/advanced', wait_until='domcontentloaded')`, and the `try/finally: page.close()` discipline every test in this file uses. Copy that shape exactly.
    - `tests/test_advanced_ui.py` line 536 -- `page.add_init_script("localStorage.setItem('beacon-theme', 'light');")`, the established one-liner for putting the page in light mode before `goto`. And line 1620-1626 -- `page.locator('.summary-grid').evaluate('(node) => getComputedStyle(node).gridTemplateColumns...')`, the established computed-style-read shape this task reuses for `cursor`, `color`, `borderTopColor`, `backgroundColor`, `fontWeight`, and `minHeight`.
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` line 71 and line 76 -- the accent row of the colour table and the reservation sentence. Line 76 is the authority for this task: accent is reserved for "selected navigation, focus indicators, selected filters, the `Refresh now` action, and the Dashboard return link", and must not be used for "ordinary service status, warning, criticality, or body text".
  </read_first>
  <action>Open `dashboard/advanced.css`. Insert FOUR new lines immediately after the existing `button:focus-visible, a:focus-visible, select:focus-visible, input:focus-visible, .services-table-scroll:focus-visible { ... }` rule (currently line 56) and immediately before the first `@media` line. Do not modify, reformat, or reorder any existing line. Keep the file's one-rule-per-line house style.

The first of the four is a single comment line introducing the whole affordance block (Task 2 will insert its rules between this comment and the `#advanced-refresh` rule). Write it to say, in your own words: this block gives click targets a mouse affordance, and the accent stays reserved for the primary refresh action per 03-UI-SPEC.md. CONSTRAINT ON THIS COMMENT, which the acceptance criteria depend on: it must NOT contain the two-word declaration for a pointing-hand cursor, must NOT contain the string `#advanced-refresh`, must NOT contain any `:hover` selector text, and must NOT contain the string `min-height: 44px`. Several acceptance gates below are exact `grep -cF` counts over this file, and a comment echoing a gated literal would silently inflate them. Refer to the button as "the refresh action" in prose instead of by its id.

The remaining three lines, in this order:

Line A -- `#advanced-refresh` gets, in one declaration block: the pointing-hand cursor; `color: var(--accent)`; `border-color: var(--accent)`; and `font-weight: 600`. Use the ID selector, not a class. The ID's specificity is what makes this rule beat advanced.css:9's `color: var(--text)` and its `border: 1px solid var(--border)` shorthand regardless of where in the file it sits, and -- important for Task 2 -- it is also what will keep the accent from being stripped by Task 2's lower-specificity light-theme neutral hover rule. Do NOT set `background` here: the button keeps advanced.css:9's `var(--bg3)` at rest so the accent border and accent text read as an outline treatment against the same surface every other header control uses, which is what makes the border and weight legible as "this one is primary".

Line B -- `html:not(.light) #advanced-refresh:hover` sets `background: rgba(0, 212, 255, 0.12)`. That is the dark `--accent` `#00d4ff`'s own RGB at 12% alpha, the `.meta-btn-primary:hover` idiom from `style.css:730-732` one step stronger than the 8% neutral tint Task 2 will use, so the primary control's hover reads as more emphatic than its neighbours'.

Line C -- `html.light #advanced-refresh:hover` sets `background: rgba(0, 102, 204, 0.10)`. That is the light `--accent` `#0066cc`'s own RGB at 10% alpha, composited over the header's white `var(--bg)` surface.

Write Lines B and C so that the literal substrings `html:not(.light) #advanced-refresh:hover` and `html.light #advanced-refresh:hover` each appear on exactly one line and exactly once in the file.

Then open `tests/test_advanced_ui.py`. Add ONE new test method, `test_every_interactive_control_reads_as_interactive_in_both_themes`, placed immediately after `test_direct_route_tracer_shows_loading_then_host_and_retains_last_success_on_failure` (currently ends around line 118). Task 2 will EXTEND this same method rather than add a second one -- write it so extension is natural (a per-theme `subTest` loop with the assertions inside).

Structure it: build `payload = self._snapshot()`. Loop `for theme in ('dark', 'light'):` with `with self.subTest(theme=theme):`. Inside, create `page = self.browser.new_page(viewport={'width': 1280, 'height': 900})` -- a desktop viewport, so the section rail stays vertical and the service-identity cell is not sticky, keeping this test clear of the two `@media` blocks entirely. If `theme == 'light'`, call `page.add_init_script("localStorage.setItem('beacon-theme', 'light');")` BEFORE `goto`. Define the same `route_api` closure the existing tracer test uses (fulfill `/api/advanced/current` with `payload`, `route.fallback()` otherwise), register it with `page.route('**/api/**', route_api)`, then `page.goto(f'{self.base_url}/advanced', wait_until='domcontentloaded')` inside a `try` whose `finally` calls `page.close()`.

Add a small module-level or nested helper that reads one computed property, e.g. `def computed(locator, prop): return locator.evaluate(f'(node) => getComputedStyle(node)[{prop!r}]')` -- this keeps the assertion list readable and Task 2 will reuse it heavily.

Assertions, in this order, all inside the `try`:
1. `page.mouse.move(0, 0)` first, so no resting value is read while the pointer happens to sit on a control.
2. `#advanced-refresh` computed `cursor` equals `'pointer'`.
3. `#advanced-refresh` computed `minHeight` equals `'44px'` -- the hit-target non-regression.
4. `#advanced-refresh` computed `fontWeight` equals `'600'`.
5. Read `#advanced-refresh`, `.advanced-header a`, and `#pause-updates` computed `color` into three locals. Assert refresh EQUALS the link (both are on 03-UI-SPEC.md:76's accent-reserved list) and assert refresh DIFFERS from pause (pause is not). Add a short comment naming 03-UI-SPEC.md:76 as the authority for this pair of assertions.
6. Assert `#advanced-refresh` computed `borderTopColor` DIFFERS from `#pause-updates`'s.
7. Read `#advanced-refresh` computed `backgroundColor` at rest; call `page.locator('#advanced-refresh').hover()`; read it again; assert the two differ. Then `page.mouse.move(0, 0)` to leave the page in a clean resting state for Task 2's later assertions.

Use `assertEqual`/`assertNotEqual` on values read from the live page, never on hardcoded colour strings -- the point is to pin the RELATIONSHIPS 03-UI-SPEC.md:76 mandates, so a future token change cannot make the test false-green and cannot make it spuriously fail.</action>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k reads_as_interactive_in_both_themes -x` exits 0. Note: plain `pytest -q` does NOT print passing `unittest` subTest counts, so do not expect to read "2 subtests" off the output -- confirm both themes ran by temporarily adding `-p no:cacheprovider -v` or by trusting the `subTest(theme=...)` loop, which surfaces each theme separately ONLY on failure.
    - `test "$(grep -cF 'cursor: pointer' dashboard/advanced.css)" = "1"` succeeds -- the `#advanced-refresh` rule only, at this point. Measured at plan time: currently 0. (Task 2 raises this to 2; the comment mandated above must not contribute.)
    - `test "$(grep -cF 'html:not(.light) #advanced-refresh:hover' dashboard/advanced.css)" = "1"` succeeds. Measured at plan time: currently 0.
    - `test "$(grep -cF 'html.light #advanced-refresh:hover' dashboard/advanced.css)" = "1"` succeeds. Measured at plan time: currently 0.
    - `test "$(grep -F '#advanced-refresh' dashboard/advanced.css | grep -cF 'font-weight: 600')" = "1"` succeeds -- among the lines that mention the refresh action, exactly one carries the 600 weight (the base rule; neither hover rule sets a weight). Measured at plan time: currently 0.
    - `test "$(grep -cF 'font-weight: 600' dashboard/advanced.css)" = "4"` succeeds -- the three PRE-EXISTING heading declarations at advanced.css:3, :16 and :17 (`.advanced-header h1`, `.advanced-detail h2`, `.advanced-detail h3`) plus this task's one, and no third weight beyond the 400/600 pair 03-UI-SPEC.md:52-57 permits. Measured at plan time: currently 3 — do NOT mistake this for zero and do NOT touch the three heading rules.
    - `test "$(grep -cF 'min-height: 44px' dashboard/advanced.css)" = "6"` succeeds -- the hit-target rule count is unchanged. Measured at plan time: currently 6.
    - `test "$(grep -cF 'focus-visible' dashboard/advanced.css)" = "1"` succeeds -- the keyboard-focus rule is neither duplicated nor removed. Measured at plan time: currently 1.
    - `test "$(tail -2 dashboard/advanced.css | grep -c '^@media')" = "2"` succeeds -- both breakpoint blocks are still the last two lines, so the affordance block was inserted above them.
    - `test "$(grep -c '    def test_' tests/test_advanced_ui.py)" = "36"` succeeds -- exactly one new test method. Measured at plan time: currently 35.
    - `git diff --name-only` lists exactly `dashboard/advanced.css` and `tests/test_advanced_ui.py` and nothing else.
    - RED proof (run before the CSS edit, not part of the committed suite): the new test, run against the pre-fix stylesheet, fails on the FIRST subTest at the `cursor` assertion (`'auto' != 'pointer'`, the button UA default) -- and if that assertion is temporarily commented out, fails next at the accent-equality assertion, because pre-fix `#advanced-refresh` computes `var(--text)` (`rgb(200, 234, 245)` dark) while `.advanced-header a` computes `var(--accent)` (`rgb(0, 212, 255)`).
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k reads_as_interactive_in_both_themes -x && test "$(grep -cF 'cursor: pointer' dashboard/advanced.css)" = "1" && test "$(grep -cF 'html:not(.light) #advanced-refresh:hover' dashboard/advanced.css)" = "1" && test "$(grep -cF 'html.light #advanced-refresh:hover' dashboard/advanced.css)" = "1" && test "$(grep -F '#advanced-refresh' dashboard/advanced.css | grep -cF 'font-weight: 600')" = "1" && test "$(grep -cF 'font-weight: 600' dashboard/advanced.css)" = "4" && test "$(grep -cF 'min-height: 44px' dashboard/advanced.css)" = "6" && test "$(grep -cF 'focus-visible' dashboard/advanced.css)" = "1" && test "$(tail -2 dashboard/advanced.css | grep -c '^@media')" = "2" && test "$(grep -c '    def test_' tests/test_advanced_ui.py)" = "36" && echo REFRESH_NOW_READS_AND_ANSWERS_AS_PRIMARY</automated>
  </verify>
  <done>`Refresh now` shows a pointing-hand cursor, carries the accent colour, accent border, and 600 weight that 03-UI-SPEC.md:76 reserves for it and that `Pause updates` beside it does not, and visibly answers a real mouse hover -- proven in both themes by a real Chromium computed-style read, not by a source grep. A user can now do something they could not before this task: point at the refresh button and see it react.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the same affordance to every remaining interactive control, and pin the accent reservation negatively</name>
  <reversibility rating="reversible">Five more lines in the same stylesheet, all colour/cursor/border-colour declarations; no layout property is introduced. Single-concept revert: delete the five lines and the assertions Task 2 added to Task 1's test method.</reversibility>
  <files>dashboard/advanced.css, tests/test_advanced_ui.py</files>
  <behavior>
    - In BOTH themes, all of these compute `cursor: pointer`: `#pause-updates`, `#refresh-interval`, all five `.section-navigation button` tabs, `#clear-service-filters` / `#reset-service-order` / `#collapse-service-details`, the four `.service-filters` selects, the `.settings-grid` selects, every `.service-details-toggle` created by `dashboard/advanced.js:677-678`, and all six `#services-table th button` sort controls.
    - In BOTH themes, `#service-search` computes a cursor that is NOT `pointer` -- it is typed into, not clicked, and the grouped rule deliberately omits `.service-filters input`.
    - In BOTH themes, hovering a neutral surface control (`#pause-updates`, and independently `.service-details-toggle`) changes its computed `borderTopColor`. Dark: `rgba(0, 180, 220, 0.18)` -> `rgb(0, 212, 255)`. Light: `rgba(0, 0, 0, 0.06)` -> `rgb(148, 163, 184)`. Only the inequality is asserted.
    - In BOTH themes, hovering an unselected `.section-navigation` tab changes its computed `borderTopColor` from fully transparent (`rgba(0, 0, 0, 0)`, from advanced.css:12's `1px solid transparent`) to a visible value.
    - The accent reservation holds NEGATIVELY as well as positively. In BOTH themes: `#pause-updates`, the unselected `[data-section="host"]` tab at rest, and that SAME tab while hovered all compute a text colour different from `#advanced-refresh`'s accent -- while `.section-navigation button[aria-selected="true"]` computes exactly that accent. The nav hover rules set no `color` precisely so that hovering cannot manufacture the accent that 03-UI-SPEC.md:76 grants only to the selected tab, and cannot strip it from the tab that has earned it.
    - Every control still computes `min-height: 44px`, asserted on a nav tab and on a `.service-details-toggle` as well as on the refresh button.
    - `.advanced-header a` (the Dashboard return link) computes `cursor: pointer` with no rule added for it -- evidencing the deliberate no-rule decision recorded in this plan's artifacts section.
  </behavior>
  <read_first>
    - `dashboard/advanced.css` as Task 1 left it -- specifically Task 1's comment line (Task 2's five rules go immediately AFTER it and immediately BEFORE Task 1's `#advanced-refresh` rule, so the block reads comment, then general rules, then the specific override) and lines 12-13 (`.section-navigation button { ... color: var(--text); background: transparent; border: 1px solid transparent; ... }` and `.section-navigation button[aria-selected="true"] { color: var(--accent); background: var(--bg3); border-color: var(--border); }`). Line 13 is why the nav needs its OWN hover pair rather than joining the neutral list: its resting base is transparent, not `var(--bg3)`, and its selected state already owns the accent.
    - `dashboard/advanced.css` lines 29 and 38 -- `.settings-grid select { min-height: 44px; color: var(--text); background: var(--bg3); border: 1px solid var(--border); font: inherit; }` and `.service-filters input, .service-filters select, .service-filters button, .service-details-toggle, #services-table th button { min-height: 44px; color: var(--text); background: var(--bg3); border: 1px solid var(--border); padding: 6px 10px; font: inherit; }`. Confirms all of these share the header controls' exact resting surface (`var(--bg3)` + `var(--border)`), which is why ONE neutral hover rule per theme covers all of them. Note line 38 includes `.service-filters input` -- the grouped pointer rule this task writes must NOT copy that selector across.
    - `dashboard/advanced.html` lines 32-38 (the five `.section-navigation` buttons with `data-section` and `aria-selected`), 51-60 (the `#service-filters` form: `#service-search` is `input type="search"`, then four selects, then `#clear-service-filters`, `#reset-service-order`, `#collapse-service-details`), and 64-71 (the six static `<th><button id="service-sort-*">` sort controls inside `#services-table thead`). Confirms every selector except `.service-details-toggle` is present in static markup once the Services section is shown.
    - `dashboard/advanced.js` lines 675-680 -- `const details = document.createElement('button'); details.type = 'button'; details.className = 'service-details-toggle';`. Confirms `.service-details-toggle` is created per service row at render time, so the test must supply at least one service in the payload and click into the Services section before that selector exists. Also confirm by grep that `dashboard/advanced.js` contains no `disabled` assignment anywhere -- the reason this plan writes no `:disabled` rule.
    - `tests/test_advanced_ui.py` lines 18-41 -- the `gap_item` and `gap_block` module-level helpers, and lines 440-478 -- the service-entry dict shape the existing services test builds (`port`, `name`, `status`, `latency_ms`, `state_since_ts`, `critical`, `pinned_order`, `tags`, `health_rule`, `last_probe_ts`, `expected_cadence_seconds`, `freshness`, `tls`, `last_error`, `collection_gaps: gap_block('complete')`) together with the surrounding `payload.update({'services': [...], 'pipeline': {}, 'settings': {}, 'exceptions': []})`. Copy ONE service entry from that shape; do not invent new keys.
    - `tests/test_advanced_ui.py` lines 489-506 -- `page.locator('[data-section="services"]').click()` followed by `page.locator('#services-table').wait_for(timeout=5_000)` and then `page.locator('.service-details-toggle')` usage. This is the established navigation-and-wait sequence Task 2 needs before asserting on the dynamically created toggle.
    - `.planning/phases/03-advanced-current-diagnosis/03-UI-SPEC.md` line 76 again -- the reservation sentence, which this task pins NEGATIVELY: the sentence grants accent to "selected navigation" and to nothing else in the nav, so an unselected tab must not acquire it, at rest or on hover.
  </read_first>
  <action>Open `dashboard/advanced.css`. Insert FIVE new lines immediately after Task 1's comment line and immediately before Task 1's `#advanced-refresh` rule, so the completed block reads: comment, then these five general rules, then Task 1's three specific rules, then the two untouched `@media` blocks. One rule per line, matching the file's house style. Do not modify any existing line, including Task 1's.

Line 1 -- one grouped rule setting only the pointing-hand cursor, whose selector list is exactly, in this order: `.advanced-header-controls button`, `.advanced-header-controls select`, `.section-navigation button`, `.service-filters button`, `.service-filters select`, `.settings-grid select`, `.service-details-toggle`, `#services-table th button`. Deliberately EXCLUDE `.service-filters input` -- `#service-search` is typed into, not clicked, and must keep its native text caret. Task 1's `#advanced-refresh` already carries the cursor by ID and is additionally covered here by `.advanced-header-controls button`; this is harmless duplication of the same value, and the acceptance gate below expects exactly two occurrences of the declaration in the file as a result.

Line 2 -- `html:not(.light)`-prefixed hover rule for the neutral surface controls, setting `background: rgba(0, 212, 255, 0.08)` and `border-color: var(--accent)`. Its selector list is Line 1's list with `:hover` appended to each and `html:not(.light) ` prefixed to each, MINUS `.section-navigation button` (which gets its own rule at Line 4 because its resting base is transparent). Both values are verbatim from `style.css:176`.

Line 3 -- the `html.light` counterpart of Line 2, same selector list, setting `color: #0f172a` and `border-color: #94a3b8`. Both values are verbatim from `style.css:817-819`. Note it sets `color` where the dark rule does not: in light mode the border alone moves from a near-invisible `rgba(0,0,0,0.06)`, so darkening the label as well is what makes the change legible -- and this is exactly what the main dashboard's own light ghost-button hover does. This rule's specificity is below an ID, so it CANNOT strip the accent from `#advanced-refresh` on hover; verify that with the test rather than assuming it.

Line 4 -- `html:not(.light) .section-navigation button:hover` setting `background: rgba(0, 212, 255, 0.08)` and `border-color: var(--border)`.

Line 5 -- `html.light .section-navigation button:hover` setting `background: var(--bg3)` and `border-color: #cbd5e1`.

CRITICAL for Lines 4 and 5: set NO `color` property in either. Their specificity is higher than advanced.css:13's `[aria-selected="true"]` rule, so any `color` they set would override the selected tab's accent whenever the pointer crosses it; and setting accent there would hand accent to unselected tabs, which 03-UI-SPEC.md:76 forbids. Precede Line 4 with a short comment stating that reason in your own words -- and, as in Task 1, keep that comment clear of gated literals: it must not contain the two-word pointing-hand cursor declaration, the string `min-height: 44px`, or the string `font-weight: 600`.

Write Lines 2 through 5 so that the literal substrings `html:not(.light) .advanced-header-controls button:hover`, `html.light .advanced-header-controls button:hover`, `html:not(.light) .section-navigation button:hover`, and `html.light .section-navigation button:hover` each appear on exactly one line and exactly once in the file.

Then open `tests/test_advanced_ui.py` and EXTEND Task 1's `test_every_interactive_control_reads_as_interactive_in_both_themes`. Do NOT add a second test method -- the method count gate below expects 36, unchanged.

Change the payload construction to `payload = self._snapshot()` followed by `payload.update({'services': [<one service entry copied from the existing services test's shape>], 'pipeline': {}, 'settings': {}, 'exceptions': []})`, so the Services section renders. After `goto` and before the existing assertions, click into Services the established way: `page.locator('[data-section="services"]').click()` then `page.locator('#services-table').wait_for(timeout=5_000)` then `page.locator('.service-details-toggle').first.wait_for(timeout=5_000)`. Keep Task 1's `page.mouse.move(0, 0)` as the first act after the section is ready.

Add these assertions inside the same per-theme `subTest`, reusing Task 1's `computed` helper:

1. A pointer sweep. Loop over a tuple of selectors -- `#advanced-refresh`, `#pause-updates`, `#refresh-interval`, `[data-section="host"]`, `[data-section="pipeline"]`, `#clear-service-filters`, `#service-status-filter`, `#service-criticality-filter`, `#service-sort-name`, `#service-sort-freshness`, `.service-details-toggle` -- asserting each one's computed `cursor` is `'pointer'`, with a `subTest(theme=theme, selector=selector)` so a failure names the control. Use `.first` on the `.service-details-toggle` locator.
2. The negative: `#service-search`'s computed `cursor` is NOT `'pointer'`. Assert inequality, not equality against a specific value -- Chromium may report `auto` or `text` for a search input depending on version, and either satisfies the real claim.
3. The Dashboard link's affordance without a rule: `.advanced-header a` computes `cursor: 'pointer'` (the UA default). Add a short comment noting that this is why this plan adds no `a:hover` or anchor cursor rule.
4. Neutral hover delta, twice -- once for `#pause-updates` and once for `.service-details-toggle` `.first`. For each: read `borderTopColor` at rest, `locator.hover()`, read it again, assert the two differ, then `page.mouse.move(0, 0)` before moving to the next control so the following resting read is genuinely at rest.
5. Nav hover delta: same read/hover/read/reset sequence on `[data-section="host"]` (unselected, since the test has navigated to Services), asserting `borderTopColor` changes.
6. The accent reservation, negatively. Read `#advanced-refresh`'s computed `color` into `accent_color`. Assert each of these differs from it: `#pause-updates` at rest, `[data-section="host"]` at rest, and `[data-section="host"]` WHILE HOVERED (hover it, read `color`, assert, then `page.mouse.move(0, 0)`). Then assert `.section-navigation button[aria-selected="true"]`'s computed `color` EQUALS `accent_color` -- the selected tab is the one nav element 03-UI-SPEC.md:76 grants accent to. Add a comment naming that line as the authority.
7. Hit-target non-regression beyond Task 1's: `[data-section="host"]` and `.service-details-toggle` `.first` each compute `minHeight` of `'44px'`.

Keep Task 1's existing assertions intact and in place; this task only adds to them.</action>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k reads_as_interactive_in_both_themes -x` exits 0 with both theme subTests and every nested selector subTest passing.
    - `test "$(grep -cF 'cursor: pointer' dashboard/advanced.css)" = "2"` succeeds -- Task 1's `#advanced-refresh` rule plus Task 2's grouped rule, and nothing contributed by either comment. Measured at start of this task: 1.
    - `test "$(grep -cF 'html:not(.light) .advanced-header-controls button:hover' dashboard/advanced.css)" = "1"` succeeds. Measured at start of this task: 0.
    - `test "$(grep -cF 'html.light .advanced-header-controls button:hover' dashboard/advanced.css)" = "1"` succeeds. Measured at start of this task: 0.
    - `test "$(grep -cF 'html:not(.light) .section-navigation button:hover' dashboard/advanced.css)" = "1"` succeeds. Measured at start of this task: 0.
    - `test "$(grep -cF 'html.light .section-navigation button:hover' dashboard/advanced.css)" = "1"` succeeds. Measured at start of this task: 0.
    - `test "$(grep -cF '.service-filters input' dashboard/advanced.css)" = "1"` succeeds -- the search input appears only in the pre-existing line 38 and was NOT copied into the pointer rule or either hover rule. Measured at plan time: 1.
    - `test "$(grep -cF 'min-height: 44px' dashboard/advanced.css)" = "6"` and `test "$(grep -cF 'focus-visible' dashboard/advanced.css)" = "1"` and `test "$(grep -cF 'font-weight: 600' dashboard/advanced.css)" = "4"` (three pre-existing heading declarations plus Task 1's one) and `test "$(grep -F '#advanced-refresh' dashboard/advanced.css | grep -cF 'font-weight: 600')" = "1"` all still succeed -- unchanged from Task 1. This task must add no weight declaration of its own.
    - `test "$(tail -2 dashboard/advanced.css | grep -c '^@media')" = "2"` succeeds.
    - `test "$(grep -c '    def test_' tests/test_advanced_ui.py)" = "36"` succeeds -- unchanged by this task, which extends Task 1's method rather than adding one.
    - `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py` exits 0 -- the whole browser suite, proving the new rules break none of the 35 pre-existing tests (in particular the two responsive tests that read `getComputedStyle` at 719/720/959px, and the two source-contract tests that `assertIn('position: sticky', css)` and `assertIn('@media (max-width: 959px)', css)`).
    - `uv run --project dashboard python -m pytest -q` exits 0 across the whole suite. Nothing previously green may go red.
    - `git diff --name-only` lists exactly `dashboard/advanced.css` and `tests/test_advanced_ui.py` and nothing else -- confirming the scope fence held and no server, diagnosis, preview, markup, or `style.css` file was touched.
    - RED proof (run before the CSS edit, not part of the committed suite): with Task 1's three rules in place but Task 2's five absent, the extended test fails on the FIRST pointer-sweep subTest that is not `#advanced-refresh` (`#pause-updates`, `'auto' != 'pointer'`), and -- with the sweep temporarily skipped -- fails next at the `#pause-updates` neutral-hover delta, whose `borderTopColor` is identical before and after `hover()`.
  </acceptance_criteria>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py -k reads_as_interactive_in_both_themes -x && test "$(grep -cF 'cursor: pointer' dashboard/advanced.css)" = "2" && test "$(grep -cF 'html:not(.light) .advanced-header-controls button:hover' dashboard/advanced.css)" = "1" && test "$(grep -cF 'html.light .advanced-header-controls button:hover' dashboard/advanced.css)" = "1" && test "$(grep -cF 'html:not(.light) .section-navigation button:hover' dashboard/advanced.css)" = "1" && test "$(grep -cF 'html.light .section-navigation button:hover' dashboard/advanced.css)" = "1" && test "$(grep -cF '.service-filters input' dashboard/advanced.css)" = "1" && test "$(grep -cF 'min-height: 44px' dashboard/advanced.css)" = "6" && test "$(grep -cF 'focus-visible' dashboard/advanced.css)" = "1" && test "$(grep -cF 'font-weight: 600' dashboard/advanced.css)" = "4" && test "$(grep -F '#advanced-refresh' dashboard/advanced.css | grep -cF 'font-weight: 600')" = "1" && test "$(tail -2 dashboard/advanced.css | grep -c '^@media')" = "2" && test "$(grep -c '    def test_' tests/test_advanced_ui.py)" = "36" && uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py && uv run --project dashboard python -m pytest -q && echo EVERY_CONTROL_READS_AS_INTERACTIVE</automated>
    <human-check>
      **These five items are the closure evidence for G-03-4 that no automated check can supply. G-03-4 was opened by a human perception report ("doesn't feel like it's a button"); a computed-style delta proves the browser applied the rule, not that the operator perceives it. Items 1 and 2 are new and specific to this gap. Items 3, 4 and 5 are the three standing hardware items carried forward unchanged from `03-VERIFICATION.md`'s `human_verification` block and every prior gap-closure plan since round 3. Collected at the end-of-phase human checkpoint (`workflow.human_verify_mode: end-of-phase`).**

      1) **The reported defect, directly.** On the target Pi, open `/advanced` in DARK mode. Point at `Refresh now` without clicking. Expected: the cursor becomes a pointing hand, the button's background visibly brightens toward cyan, and — before hovering anything — `Refresh now` already reads as the primary action of that header: cyan text, cyan border, heavier weight, plainly distinct from `Pause updates` beside it. Then switch to LIGHT mode with the dashboard's theme toggle and repeat: pointing hand, a blue wash on hover, and blue text plus a blue border at rest that `Pause updates` does not have. This is the item that closes G-03-4 — if `Refresh now` still does not feel like a button here, the gap is NOT closed regardless of what the test suite reports.

      2) **Perceptibility of the hover feedback across the workspace.** On the Pi, in each theme, point in turn at `Pause updates`, the refresh-interval dropdown, a section tab in the left rail (`Host`, then `Pipeline`), a column header in the Services table, a row's `Show details` control, and a filter dropdown. Expected: every one shows the pointing hand and visibly answers the hover — in dark mode the border lights up cyan, in light mode the border darkens to slate and the label darkens. Expected NOT to happen: the search box showing a pointing hand (it should keep a text caret), and an UNSELECTED section tab turning cyan/blue on hover (accent belongs to the selected tab only). The tints are 8–12% alpha and the light-mode border shift is subtle by design; confirming they are actually visible on the Pi's own display, at its brightness and viewing angle, is the judgement this item exists for. If any control's feedback is too faint to notice in normal use, say so — the fix is to raise the alpha, not to re-run the tests.

      3) On the target Pi, open `/advanced` while a real collection gap is active and while host evidence is stale. Expected: the workspace shows the open gap and the stale host as real, correctly labelled exceptions, and shows no resolved or retention-expired interval as an open actionable gap. (Carried forward; `03-UAT.md` test 1 recorded this as PASS on real hardware 2026-08-20 ~13:00. Re-confirm only that this plan's stylesheet change did not disturb it.)

      4) On the target Pi, start the worker and leave the system idle for one minute, then open `/advanced` and read the Overview "Active exceptions" region. Expected: no "Background job failed" card for any job, and no "Background job outcome not recorded" card for any job that is simply working. (Carried forward; `03-UAT.md` test 2 recorded this as PASS on real hardware 2026-08-20 13:03.)

      5) On the target Pi with Chromium/Playwright deliberately unavailable, leave the worker running for two minutes, then open `/advanced` and read the Pipeline region. Expected: a "Background job failed" card names J6. (Carried forward; `03-UAT.md` test 3 recorded this as PASS on real hardware 2026-08-20 21:43, with the caveat that the browser-served render itself was not photographed because the failed window had closed 19 seconds earlier. The related signal-duration observation is a recorded Deferred Follow-Up, NOT a gap — do not treat it as a failure of this item.)
    </human-check>
  </verify>
  <done>Every interactive control in the `/advanced` workspace shows a pointing-hand cursor and visibly answers a mouse hover in both themes, the search input deliberately does not, and the accent treatment stays pinned to exactly the two controls 03-UI-SPEC.md:76 reserves it for in this header — proven positively for `Refresh now` and the selected navigation tab and negatively for `Pause updates` and every unselected tab, at rest and while hovered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet -> browser rendering | The only boundary this plan crosses. `dashboard/advanced.css` is a first-party static asset already served by an existing route and already linked from `advanced.html:8`; this plan adds nine declarations to it and adds no new asset, request, or fetch. |
| accent semantics -> operator interpretation | 03-UI-SPEC.md:76 makes accent a *meaning* in this workspace (selected / focused / primary), not decoration. Widening it would let a non-primary control impersonate the primary one on the operator's diagnosis screen. |
| *(not crossed)* server, worker, database, network | No task in this plan touches `dashboard/app.py`, `dashboard/worker.py`, `dashboard/beacon/*`, any migration, any route, any query, or any outbound request. |

**Honest statement of scale.** This is a nine-line stylesheet change plus a browser test. The realistic threat surface is close to nil, and the register below says so rather than inflating it. Only T-03-141 describes a defect this plan could plausibly introduce; the rest are recorded as *not applicable* with the evidence for that judgement, so a later reviewer does not have to re-establish it.

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-140 | Information Disclosure | New CSS declarations in `dashboard/advanced.css` | low | mitigate | CSS can exfiltrate only via a resource fetch (`url()`, `@import`, `image-set()`, a remote `@font-face`) or an attribute-selector oracle. This plan introduces none: every new declaration is `cursor`, `color`, `background`, `border-color`, or `font-weight` with a literal or `var(--token)` value, and the only attribute selector involved (`[aria-selected="true"]`) is pre-existing and merely read by the test. Pinned by the acceptance criterion that `git diff --name-only` lists exactly two files, plus the existing CSP the app already sets on every response — unchanged by this plan. |
| T-03-141 | Spoofing | The accent treatment on `#advanced-refresh` | low | mitigate | The real risk in this change is *widening* an accent that 03-UI-SPEC.md:76 makes semantic, so an ordinary control impersonates the primary action or a selected state on an operator's live diagnosis screen. Mitigated by scoping accent to the `#advanced-refresh` ID and by setting no `color` in either navigation hover rule, and — the part that matters — asserted NEGATIVELY in Task 2: `#pause-updates`, an unselected nav tab at rest, and that same tab while hovered must each compute a text colour DIFFERENT from the accent, while the `[aria-selected="true"]` tab must compute exactly it. A future widening breaks the test. |
| T-03-142 | Denial of Service | Rendering cost of the new rules on Raspberry Pi hardware | low | accept | Nine static rules with no animation, no `transition`, no `filter`, no `box-shadow`, no `backdrop-filter`, and no property that triggers layout or paint outside the hovered element's own box. The main dashboard already carries substantially heavier hover work (`.svc-card:hover` background swaps, `.btn-scan:hover` on a `clip-path`ed element) on the same hardware. No mitigation warranted. |
| T-03-143 | Tampering | Supply chain (npm / pip / cargo installs) | n/a | accept | No package-manager install task exists in this plan. No dependency is added, removed, upgraded, or pinned; `dashboard/pyproject.toml` and `dashboard/uv.lock` are untouched, enforced by the two-file `git diff --name-only` acceptance criterion. No `## Package Legitimacy Audit` is required. |
| T-03-144 | Repudiation / Elevation of Privilege | -- | n/a | accept | No task creates, reads, or writes a durable record, log line, credential, session, permission check, or authority fence. There is no state for either category to act on. |

## ASVS Level 1 Mapping

| ASVS Category | Applicability | Plan-specific control |
|----------------|---------------|------------------------|
| V14 Configuration (third-party / external resources) | Applicable | No `@import`, `url()`, remote font, or external resource is introduced; the stylesheet remains first-party and same-origin, so the app's existing CSP needs no relaxation. |
| V1 Architecture / V4 Access Control / V7 Error Handling | Not applicable | Presentation-only change; no trust decision, authorization check, error path, or log line is added or modified. Enforced by the two-file diff gate. |
| V2 Authentication / V3 Session Management / V5 Validation / V6 Stored Cryptography | Not applicable | Trusted-LAN scope; no identity, session, input-handling, or cryptographic material is introduced or touched. |
</threat_model>

<gap_coverage>

| UAT finding | Disposition | Closure evidence |
|---|---|---|
| `G-03-4` `missing` 1 — "Add `cursor: pointer` to interactive controls: `.advanced-header-controls button/select`, `.section-navigation button`, `.service-filters button`, `.service-details-toggle`, `#services-table th button`" | IMPLEMENT in Task 1 (`#advanced-refresh`) and Task 2 (all remaining, plus `.service-filters select` and `.settings-grid select` — see the scope note in this plan's artifacts section) | One grouped rule plus the ID rule; proven by a real-Chromium `getComputedStyle(node).cursor` sweep across 11 selectors in both themes, with an explicit negative for `#service-search`. |
| `G-03-4` `missing` 2 — "Add `:hover` states consistent with `style.css` conventions on the main dashboard" | IMPLEMENT in Task 2 (Task 1 supplies the primary-CTA pair) | Four theme-scoped hover rules whose values are lifted verbatim from `style.css:176` (`rgba(0,212,255,0.08)`), `style.css:817-819` (`#0f172a` / `#94a3b8`), `style.css:811-815` (`#cbd5e1`), and the `.meta-btn-primary:hover` idiom at `style.css:730-732`; proven by a resting/hover/hovered computed-style delta on a neutral control, a details toggle, and a nav tab, in both themes. |
| `G-03-4` `missing` 3 — "Give `#advanced-refresh` the accent treatment per 03-UI-SPEC.md:76, which reserves accent for the `Refresh now` action" | IMPLEMENT in Task 1 | `#advanced-refresh { cursor: pointer; color: var(--accent); border-color: var(--accent); font-weight: 600; }` plus two theme-scoped hover tints; proven by asserting Refresh now's computed colour EQUALS the Dashboard return link's — the other control on the same reserved list — and DIFFERS from `Pause updates`'. Task 2 pins the reservation negatively so it cannot later widen. |
| `03-UAT.md` `## Deferred Follow-Ups`, test 3 — J6's transient signal duration | NOT PLANNED, out of scope by explicit instruction | Recorded as a Deferred Follow-Up, not a gap. Round 8's J6 fix was directly confirmed working on real hardware 2026-08-20 21:43. No task in this plan touches `dashboard/app.py`, `dashboard/beacon/previews.py`, or `dashboard/beacon/diagnosis.py`; the two-file `git diff --name-only` acceptance criterion in both tasks makes that mechanically true, and human-check item 5 explicitly instructs the operator not to treat it as a failure. |
| DIA-08's `range` clause | NOT PLANNED, deliberate | Ruled a Phase 4 deferral since round 4 and re-confirmed in `03-VERIFICATION.md`'s round-8 `deferred` block. This plan adds, removes, and reorders nothing among the advanced-workspace preference controls. |
| `03-UAT.md` tests 1, 2, 3 (all `result: pass`) | NOT RE-OPENED | Carried into this plan's human-check block as items 3–5 for re-confirmation only, exactly as `03-VERIFICATION.md`'s `human_verification` block records them. No task changes any code path they exercise. |
| TEL-06 / DIA-01 / DIA-02 / DIA-03 promotion, and any `.planning/REQUIREMENTS.md` edit | NOT PLANNED, deliberate | Out of scope for a cosmetic gap-closure round; promotion is the verifier's call, not this plan's. `.planning/REQUIREMENTS.md` is not in `files_modified` and the two-file diff gate forbids touching it. |

</gap_coverage>

## Multi-Source Coverage Audit

```
SOURCE    | ID       | Feature/Requirement                                                  | Plan  | Status  | Notes
--------- | -------- | -------------------------------------------------------------------- | ----- | ------- | -----
GOAL      | --       | Operator can enter an advanced workspace from either theme and quickly diagnose the Pi, services, settings, and collection health | 03-23 | COVERED | This plan closes the one open UAT item standing against that goal: the workspace's controls did not read as controls, so "quickly" was not true for a mouse user. The four ROADMAP success criteria are already verified and untouched here.
REQ       | UX-02    | The operator can move clearly between the main dashboard and advanced analytics without losing theme choice | 03-23 | COVERED | "Move clearly" is the clause this gap sits under: the workspace's controls now read as controls in BOTH themes, and the theme-scoped rules are asserted per theme. UX-02 remains Complete in .planning/REQUIREMENTS.md; this plan does not edit that file.
REQ       | TEL-06, DIA-01, DIA-02, DIA-03, DIA-08 | The other five Phase 3 requirement IDs | 03-01..03-22 | COVERED ELSEWHERE (not this plan) | Delivered and verified by plans 03-01 through 03-22 across eight verification rounds; DIA-08's range clause is a recorded Phase 4 deferral. No task here touches any code path they cover, so claiming them in this plan's `requirements:` frontmatter would be fabricated coverage. Deliberately omitted per the planning instruction for this round.
RESEARCH  | 03-PATTERNS.md, `dashboard/advanced.css` row | "Analog: design tokens at dashboard/style.css:1-41 ... Layer only advanced selectors on these variables ... preserve 44px minimum controls" | 03-23 | COVERED | Every new rule layers on `var(--accent)`, `var(--border)`, `var(--bg3)` or on a literal already present in style.css; no new custom property is introduced; the 6 `min-height: 44px` declarations are pinned unchanged by an acceptance gate in both tasks.
CONTEXT   | D-13     | "Use predictable auto-refresh with a visible, selectable fixed interval, Pause, Refresh now, and a clear last-updated time" | 03-23 | COVERED | This plan makes the `Refresh now` half of D-13 actually visible AS a control — the interval select, `Pause updates`, and `Refresh now` all now read as operable, with `Refresh now` carrying the primary treatment 03-UI-SPEC.md:76 reserves for it.
CONTEXT   | D-03     | "Keep persistent section navigation visible while the selected section replaces the main detail area" | 03-23 | COVERED (strengthened) | The nav tabs gain a pointer and a hover state while the selected tab's accent is explicitly protected: both nav hover rules set no `color`, and Task 2 asserts the `[aria-selected="true"]` tab still computes accent while an unselected tab does not, at rest and while hovered.
CONTEXT   | D-16     | "Theme-aware density defaults: comfortable in light mode and compact in dark mode. Either theme may override density without losing any capability" | 03-23 | COVERED (unaffected) | No new rule sets padding, gap, height, or any `.density-*` selector; the two density rules at advanced.css:30-32 are untouched. Every new rule is asserted in BOTH themes, so neither theme gains or loses affordance.
CONTEXT   | D-01, D-02, D-04..D-12, D-14, D-15 | Remaining locked decisions | 03-01..03-22 | COVERED ELSEWHERE (unaffected) | All are behavioural or data decisions delivered in earlier plans. A stylesheet that adds only cursor, colour, border-colour and font-weight declarations cannot change what data is composed, ordered, promoted, persisted, or filtered.
CONTEXT   | --       | `## Deferred Ideas`                                                    | --    | EXCLUDED | 03-CONTEXT.md records "None — discussion stayed within phase scope." Nothing to exclude.
```

All rows are COVERED or explicitly COVERED-ELSEWHERE with the plan that owns them named. No `⚠ MISSING` row exists in this audit.

## Edge Coverage Disposition

UX-02 is the only requirement ID in this plan's scope, and this is a cosmetic gap-closure round, so the edge probe is scoped to it. No `coverage.json` exists for this round.

| requirement_id | category | disposition |
|---|---|---|
| UX-02 | empty / negative | Resolved EXPLICIT — authored into `must_haves.truths` as the fifth truth and pinned by Task 2's `#service-search` assertion. The empty case for "which controls are click targets" is the control that is NOT one; a blanket `cursor: pointer` on `.service-filters input` would have been the natural mistake, and the test forbids it. |
| UX-02 | adjacency | Resolved EXPLICIT — authored into `must_haves.truths` as the fourth truth. The adjacency that matters is selected-vs-unselected navigation: hovering an unselected tab must not manufacture the accent, and hovering the selected tab must not strip it. Both directions are asserted in both themes. |
| UX-02 | ordering / cascade | Resolved EXPLICIT — authored into `must_haves.truths` as the sixth truth and into this plan's `key_links`. CSS's "ordering" edge is specificity and file position: the affordance block sits above the two `@media` blocks (gated by `tail -2`), the ID rule outranks the class-level light hover rule so accent survives hover (asserted, not assumed), and `button:focus-visible` is neither duplicated nor shadowed (gated at count 1, and no new rule sets `outline`). |
| UX-02 | boundary / precision | Resolved BACKSTOP (2 markers) — authored into `must_haves.truths` as flat-scalar `{statement, verification: backstop}` entries. UX-02's theme round-trip and scroll-restore contracts are behavioural and live in `dashboard/advanced.js`, which this plan does not touch; the two-file `git diff --name-only` gate makes that mechanically checkable, but the round-trip behaviour itself is re-confirmed by the existing `test_theme_or_return_round_trip_preserves_theme_and_consumes_scroll_once` in the full-suite run, not by anything this plan authors. |
| UX-02 | human perception | Resolved BACKSTOP (2 markers) — authored into `must_haves.truths` as flat-scalar `{statement, verification: backstop}` entries and collected by human-check items 1 and 2. G-03-4 was opened by a perception report; a computed-style delta proves the rule was applied, never that it is perceived. This is the honest limit of automated closure for this gap and is stated as such rather than papered over. |

Equality check: 8 edge markers = 3 explicit + 2 (boundary/precision) backstop + 2 (human perception) backstop + 1 (the empty/negative marker counted once). No edge is silently dropped.

## Prohibition Probe Disposition

Stage 1 (adversarial recall) was run against UX-02's requirement text and 03-UI-SPEC.md:76, the only two authorities this round's tasks answer to. Seven raw candidates were produced; Stage 2 (precision) kept none as a bespoke, mintable prohibition:

- "Must not widen the accent beyond 03-UI-SPEC.md:76's reserved list" is a genuine and specific constraint, but it is **canon-referred rather than minted**: it is already carried as `T-03-141` (Spoofing, mitigate) in this plan's `<threat_model>`, as the fourth `must_haves.truths` entry, and as the negative assertions in Task 2. Minting it a fourth time would add a restatement, not a check.
- "Must not regress the 44px hit target" and "must not shadow `button:focus-visible`" are accessibility-correctness concerns already covered by explicit acceptance gates in both tasks (`min-height: 44px` count = 6, `focus-visible` count = 1, no new `outline` declaration). Routine engineering, not a values/safety must-NOT.
- "Must not blanket-apply a pointer to non-click targets" is covered by the explicit `#service-search` negative assertion and the `.service-filters input` count gate.
- The remaining Stage-1 candidates (introducing motion that a reduced-motion user cannot suppress; introducing a colour pair that fails contrast) do not survive Stage 2 for this change: no `transition` or animation is added at all, and every colour pair used is one `dashboard/style.css` already ships on the same hardware in the same themes.

Result: `{"items": [], "coverage": {"applicable": 0, "resolved": 0, "unresolved": 0, "byVerification": {"test": 0, "judgment": 0}}}` — a genuine zero, plus one canon-referral breadcrumb into the threat model. No `must_haves.prohibitions` block is authored by this plan. The five prohibitions carried in `03-VERIFICATION.md`'s own frontmatter concern gap projection and collection-failure suppression; a presentation-only stylesheet change cannot reach the code they constrain, which the two-file `git diff --name-only` gate makes mechanically verifiable.

<verification>

- `uv run --project dashboard python -m pytest -q` exits 0 across the whole suite. Nothing previously green may go red.
- `uv run --project dashboard python -m pytest -q tests/test_advanced_ui.py` exits 0 with 36 test methods — the 35 pre-existing plus the one this plan adds. Particular attention to the four pre-existing tests that read this stylesheet or its computed output: the two source-contract tests that `assertIn('position: sticky', css)` and `assertIn('@media (max-width: 959px)', css)`, and the two responsive tests that evaluate `getComputedStyle` at 719/720/959px.
- `uv run --project dashboard python -m pytest -q tests/test_module_boundaries.py tests/test_release_contract.py tests/test_api_and_auth.py` exits 0 — `advanced.css` is a declared copied asset (`tests/test_module_boundaries.py:122`) and a served route with an asserted content type (`tests/test_advanced_diagnosis_api.py:151`, `tests/test_advanced_ui.py:213`); adding rules must not disturb either contract.
- `git diff --name-only` lists exactly `dashboard/advanced.css` and `tests/test_advanced_ui.py`. This is the scope fence, made mechanical: no `dashboard/app.py`, no `dashboard/beacon/*`, no `dashboard/advanced.js`, no `dashboard/advanced.html`, no `dashboard/style.css`, no `.planning/REQUIREMENTS.md`.
- `dashboard/advanced.css` contains no `@import`, no `url(`, no `transition`, no `animation`, and no new CSS custom property.
- The two `@media` blocks are still the last two lines of `dashboard/advanced.css`.
- All five human-check items are carried into the end-of-phase human checkpoint, not automated away. Items 1 and 2 are the actual closure evidence for G-03-4.

</verification>

<success_criteria>

- Every interactive control in the `/advanced` workspace computes `cursor: pointer` in both themes, and `#service-search` deliberately does not.
- Hovering any of those controls produces a computed style change, in both themes, using values lifted verbatim from `dashboard/style.css`'s existing hover vocabulary rather than a new visual language invented for one page.
- `#advanced-refresh` carries the accent colour, accent border, and 600 weight that 03-UI-SPEC.md:76 reserves for the `Refresh now` action — asserted by equality against the Dashboard return link's computed colour and inequality against `Pause updates`'.
- The accent reservation is pinned negatively as well as positively: no ordinary control takes accent text at rest or on hover, and `[aria-selected="true"]` remains the only navigation state that earns it.
- The 6 `min-height: 44px` declarations, the single `focus-visible` rule, and the two trailing `@media` blocks are all unchanged.
- All 11 `must_haves.truths` entries are satisfied: 7 core behavioural truths pinned by the real-browser regression, and 4 backstop markers (2 UX-02 edges, 2 human-perception) carried to the end-of-phase checkpoint rather than claimed automatically.
- `G-03-4` is closed in code and its two perception items are queued for the operator; `03-UAT.md`'s three passing tests are re-confirmed, not re-opened; and the J6 signal-duration Deferred Follow-Up is untouched.

</success_criteria>

<output>
Create `.planning/phases/03-advanced-current-diagnosis/03-23-SUMMARY.md` when done.
</output>
