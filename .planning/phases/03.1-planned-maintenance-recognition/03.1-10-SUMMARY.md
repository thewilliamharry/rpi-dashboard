---
phase: 03.1-planned-maintenance-recognition
plan: 10
subsystem: monitoring-ui
tags: [vanilla-js, read-only-workspace, availability-attribution, playwright]

# Dependency graph
requires:
  - phase: 03.1-planned-maintenance-recognition/03.1-07
    provides: "MAINTENANCE_AVAILABILITY plus per-service maintenance/overrun/maintenance_attribution blocks on the advanced snapshot (SCHEMA_VERSION 4), and compose_active_exceptions left structurally untouched (D-07)"
  - phase: 03-advanced-current-diagnosis
    provides: "The /advanced workspace's GET-only route, evidence-row grid/builder (addEvidence, .evidence-row), and fail-closed serviceAvailability() vocabulary this plan extends"
provides:
  - "serviceAvailability()'s maintenance branch and the Status column's ● maintenance rendering"
  - "formatMaintenanceEvidence/formatMaintenanceAttribution: always-rendered evidence-row text, never blank, never a percentage"
  - "The Maintenance/Down since/Raised at/Maintenance attribution/management-pointer evidence rows in the Services detail"
affects: []

# Actuals (#2632)
actuals:
  tokens: 7222
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "The Maintenance colour is applied as a status-text class only (.service-status-maintenance), added conditionally in JS and reusing the existing --accent3 token unchanged in advanced.css -- no new custom property, matching D-06's calm-not-fault register."
    - "formatMaintenanceEvidence/formatMaintenanceAttribution both fail closed to their explicit inactive/zero sentence on any malformed or missing input, never a blank row -- the same no-blank-cell discipline formatServiceGapEvidence already established on this page."
    - "The overrun block's mere presence (service.overrun truthy) is the open-overrun signal, matching 03.1-07's own absent-vs-explicit-null design choice; the client performs no separate boolean check."

key-files:
  created: []
  modified:
    - dashboard/advanced.js
    - dashboard/advanced.css
    - tests/test_advanced_ui.py

key-decisions:
  - "The Maintenance windows management-pointer row and the four contracted maintenance-evidence copy strings are written as single-line addEvidence(...) calls, matching the file's own established dense one-liner idiom and satisfying the plan's D-01 guard (grep -B2 -A2 finds no createElement('a')/href near the pointer text)."
  - "A dedicated formatSpan() helper (adds a days branch) was introduced for the maintenance-attribution duration/period text rather than extending the existing formatDuration() (used elsewhere for state-duration cells, capped at hours) -- keeps that unrelated cell's behavior byte-identical and avoids any risk of an out-of-scope regression."
  - "_services_page test helper gained an optional exceptions= parameter (default unchanged, ()) rather than a second helper, so test_an_overrun_service_reappears_in_the_active_exceptions_list could supply a non-empty exceptions fixture without duplicating the route/navigation boilerplate."
  - "test_no_workspace_route_accepts_a_mutation_method asserts the page's one pre-existing form (03-advanced-current-diagnosis's client-side service-filters form) carries no action and no non-GET method, rather than asserting zero <form> elements exist -- the literal 'no form anywhere' assertion was a test bug caught by its own first run, not a plan requirement."

patterns-established:
  - "Local, file-scoped formatting helpers (WEEKDAY_NAMES, formatClockMinutes, formatWeekdayNames, formatSpan) rather than importing or duplicating app.js's own weekday vocabulary -- D-01 keeps the read-only workspace's JavaScript wholly independent of the editor's."

requirements-completed: [MNT-03, MNT-04]

coverage:
  - id: T1
    description: "serviceAvailability() recognises 'maintenance' as its own value (never folded into offline), the Status column renders it like its siblings with no new column and no column reorder, and the status-text colour reuses the existing --accent3 token with no new custom property"
    requirement: MNT-03
    verification:
      - kind: unit
        ref: "test_a_maintenance_service_renders_the_new_status_value, test_the_services_table_still_has_six_columns, test_an_unrecognised_availability_still_renders_the_unknown_value"
        status: pass
      - kind: other
        ref: "awk '/function serviceAvailability/,/^  }/' dashboard/advanced.js | grep -c maintenance -> 2; grep -c maintenance dashboard/advanced.css -> 2; git diff -U0 dashboard/advanced.css | grep -Ec '^\\+\\s*--[a-z0-9-]+:' -> 0"
        status: pass
    human_judgment: false
  - id: T2
    description: "Maintenance and Maintenance attribution evidence rows always render with explicit none/zero copy; Down since/Raised at render only while an overrun is open, as two separate rows; the management pointer is plain text, not a link; all four rows reuse the existing addEvidence()/.evidence-row builder with no new className introduced"
    requirement: MNT-03
    verification:
      - kind: unit
        ref: "test_the_maintenance_evidence_row_always_renders, test_the_attribution_row_always_renders_including_zero, test_the_overrun_rows_render_as_two_separate_values, test_the_overrun_rows_are_absent_without_an_open_overrun, test_the_management_pointer_is_plain_text_and_not_actionable"
        status: pass
      - kind: other
        ref: "grep -c 'evidence-row' dashboard/advanced.js: 1 -> 4; git diff -U0 dashboard/advanced.js | grep -Eo \"className = '[a-z-]+'\" | sort -u -> (empty); grep -B2 -A2 'managed from the main dashboard' dashboard/advanced.js | grep -Eic \"createElement\\('a'\\)|href\" -> 0"
        status: pass
    human_judgment: false
  - id: T3
    description: "17 named regressions pin the maintenance status value, the D-07 exception exclusion and its overrun-driven reversion, D-06 failure-surface retention, no-blank-cell/no-percentage evidence discipline, D-08's two-separate-values overrun rows, the D-01 plain-text pointer, no-interactive-element and no-maintenance-colour-on-interactive-element guards, wrap-not-truncate, and GET-only method discipline"
    requirement: MNT-04
    verification:
      - kind: unit
        ref: "tests/test_advanced_ui.py (all 17 named tests collected and passing, file-scoped selector, no -k)"
        status: pass
      - kind: unit
        ref: "uv run --project dashboard python -m pytest -q -- 485 passed, 454 subtests passed, 0 failed (baseline 468 passed/454 subtests; +17 from this plan's Task 3)"
        status: pass
    human_judgment: false

duration: ~25min
completed: 2026-08-22
status: complete
---

# Phase 03.1 Plan 10: Advanced-Workspace Maintenance Disclosure Summary

**The read-only `/advanced` workspace now discloses D-06's calm `maintenance` status, D-08's two independently-legible overrun timestamps, and D-09's duration-only attribution -- entirely as inert text, with the fixed six-column services table, the fail-closed status vocabulary, and the strictly GET-only surface all left structurally intact.**

## Performance

- **Duration:** ~25 min
- **Tasks:** 3
- **Files modified:** 3 (0 created)

## Accomplishments

- `dashboard/advanced.js`: `serviceAvailability()` gains a `maintenance` branch (never folded into `offline`, WR-01's fall-closed `unknown` default unchanged); the Status column's `<td>` conditionally carries `service-status-maintenance` for the colour rule. New local formatting helpers `WEEKDAY_NAMES`, `formatClockMinutes`, `formatWeekdayNames`, `formatSpan`, `formatMaintenanceEvidence`, `formatMaintenanceAttribution` -- all fail closed to their explicit inactive/zero sentence on any malformed input. `renderServices()` appends, after the existing Collection-gap evidence row and using the same `addEvidence`/`.evidence-row` builder: an always-present `Maintenance` row, a conditional `Down since`/`Raised at` pair (only while `service.overrun` is present), an always-present `Maintenance attribution` row, and a plain-text `Maintenance windows` management-pointer row naming where windows are managed (D-01: no link, no `createElement('a')`).
- `dashboard/advanced.css`: `.service-status-maintenance { color: var(--accent3); }` -- reuses the existing shared token (declared for both themes in `style.css`), introduces no new custom property, and is applied to status text only, never to a button or link.
- `tests/test_advanced_ui.py`: module-level fixture builders `maintenance_block`, `attribution_block`, `maintenance_service` (following the file's own `gap_item`/`gap_block` style); `_services_page` gained an optional `exceptions=` parameter (default unchanged); 17 new named tests covering every truth in the plan's `must_haves` and every threat in its `threat_model`.

## Task Commits

1. **Task 1: The maintenance status value in the existing Status column** - `9a781d6` (feat)
2. **Task 2: Three always-honest evidence rows in the Services detail** - `9e9cf3c` (feat)
3. **Task 3: Advanced-workspace regressions** - `c3de645` (test)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified

- `dashboard/advanced.js` - `serviceAvailability()` maintenance branch; `WEEKDAY_NAMES`, `formatClockMinutes`, `formatWeekdayNames`, `formatSpan`, `formatMaintenanceEvidence`, `formatMaintenanceAttribution`; status-cell `service-status-maintenance` className; four new evidence rows in `renderServices()`
- `dashboard/advanced.css` - `.service-status-maintenance` (reuses `--accent3`)
- `tests/test_advanced_ui.py` - `maintenance_block`, `attribution_block`, `maintenance_service` fixture builders; `_services_page(services, exceptions=())`; `_service_row_locator`, `_resolved_css_color` helpers; 17 named tests

## Decisions Made

See frontmatter `key-decisions` above for: the single-line `addEvidence(...)` idiom chosen to satisfy the D-01 link guard, the dedicated `formatSpan()` helper (vs. extending `formatDuration()`) to keep the unrelated state-duration cell byte-identical, the `_services_page` `exceptions=` parameter addition, and the `test_no_workspace_route_accepts_a_mutation_method` fix (assert the pre-existing filters `<form>` targets no mutation, rather than asserting zero forms exist on the page).

## Deviations from Plan

**1. [Rule 1 - Bug, caught in my own test authoring] `test_no_workspace_route_accepts_a_mutation_method` initially asserted zero `<form>` elements on the page.**
- **Found during:** Task 3, first test run
- **Issue:** The page already carries one pre-existing `<form id="service-filters">` (03-advanced-current-diagnosis's client-side filter controls), which is not a mutation surface -- it has no `action`, no non-GET `method`, and no submit control. My initial assertion (`page.locator('form').count() == 0`) was stricter than the plan's actual requirement ("no rendered element targets a non-safe method") and failed against legitimate pre-existing markup.
- **Fix:** Reworded the assertion to check every `<form>`'s `action`/`method` attributes instead of forbidding forms outright, plus a `button[type="submit"]` count of zero.
- **Files modified:** `tests/test_advanced_ui.py`
- **Commit:** `c3de645` (folded into the Task 3 commit; caught before that commit, not after)

No other deviations - the rest of the plan executed exactly as written.

## Issues Encountered

None beyond the self-caught test-assertion fix above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The advanced-workspace half of this phase's UI surface (MNT-03's disclosure obligation, MNT-04's investigation obligation) is complete and independently verified; `dashboard/index.html`, `dashboard/style.css`, and `dashboard/app.js` were deliberately left untouched, as instructed, since 03.1-09 was editing them in parallel.
- No blockers for the phase's remaining integration/closure work.

---
*Phase: 03.1-planned-maintenance-recognition*
*Completed: 2026-08-22*

## Self-Check: PASSED
- FOUND: dashboard/advanced.js (serviceAvailability maintenance branch, formatMaintenanceEvidence, formatMaintenanceAttribution, four new evidence rows present)
- FOUND: dashboard/advanced.css (.service-status-maintenance present, reuses --accent3, no new custom property)
- FOUND: tests/test_advanced_ui.py (17 named tests present, all passing; 53 tests total in file)
- FOUND commit: 9a781d6
- FOUND commit: 9e9cf3c
- FOUND commit: c3de645
