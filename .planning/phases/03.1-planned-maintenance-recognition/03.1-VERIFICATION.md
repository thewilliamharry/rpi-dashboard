---
phase: 03.1-planned-maintenance-recognition
verified: 2026-08-24T15:15:00Z
status: human_needed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "9/9 must-haves verified (2026-08-23T21:15:00Z round), with 3 undispositioned human-decision items (WR-03/WR-04/WR-05)"
  gaps_closed:
    - "WR-03 / G-03.1-69 (dead `maintenance_default_grace_minutes` setting): CONFIRMED CLOSED. `dashboard/beacon/web.py::metadata_response` publishes `default_grace_minutes` (line 67), threaded from `SETTINGS.maintenance_default_grace_minutes` at `dashboard/app.py:2318`, and `dashboard/app.js` reads it into a mutable `currentDefaultGraceMinutes` binding (line 417, updated at 591-592) consumed at all three prefill sites (lines 503, 644, 657) — all confirmed by direct source read, not SUMMARY prose."
    - "WR-04 / G-03.1-70 (midnight-wraparound clustering defect): CONFIRMED CLOSED. `_circular_minute_distance` (`dashboard/beacon/maintenance.py:200-211`) is used for the clustering anchor-distance test (line 295) and the withholding start comparison (line 352); duration stays plain `abs()` at both sites (confirmed by direct read). The cluster's reported start minute is a circular, anchor-relative median (lines 306-312), not a naive linear one."
    - "WR-05 / G-03.1-71 (`_ATTRIBUTION_MAX_ANCHOR_STEPS` silently truncating past ~129 days): CONFIRMED CLOSED. The working step budget is now derived per interval by `_anchor_step_budget` (lines 389-407) from `_covering_boundaries`'s own loop termination arithmetic; the fixed constant is now an absolute ceiling only (raised 64 -> 4096, line 386); when the derived budget exceeds the ceiling, a named module logger (`beacon.maintenance`) emits a warning identifying both the requested and covered extent (lines 433-442), and the function still returns an integer on every path."
  gaps_remaining: []
  regressions: []
gaps: []
human_verification:
  - test: "Decide disposition for WR-06 (dashboard/beacon/maintenance.py:433-442 — the ceiling-bound warning `_covering_boundaries` now logs is unthrottled, and this function is called once per enabled window per offline interval from inside `attributed_downtime_seconds`, itself called once per service from `get_current_diagnosis` on every `/api/advanced/current` request), surfaced by this round's own code review (03.1-REVIEW.md) as a NEW finding on code this round introduced, and not yet dispositioned in 03.1-UAT.md."
    expected: "Either rate-limit/de-duplicate the warning (e.g. at most once per process, or once per (port, interval) tuple per cooldown window, per 03.1-REVIEW.md's sketched fix) or move the check to a single per-top-level-call site, and record the decision as a gap entry or an accepted-as-is note in 03.1-UAT.md."
    why_human: "Not reachable at the documented default (EXPIRE_DAYS=7), and does not affect correctness of the attributed number — it is purely a log-noise/operational-hygiene concern that only manifests on an operator-misconfigured EXPIRE_DAYS (e.g. a typo adding a digit). Whether a low-likelihood, non-correctness-affecting log-flood risk blocks phase closure or is an acceptable documented follow-up is a judgment call, not resolvable by grep/test alone."
---

# Phase 03.1: Planned Maintenance Recognition Verification Report

**Phase Goal:** The operator can confirm or define expected recurring service restarts so Beacon
suppresses routine transition noise without hiding failed probes, reducing uptime impact, or
overlooking an overrun.

**Verified:** 2026-08-24T15:15:00Z
**Status:** human_needed
**Re-verification:** Yes — third re-verification of this phase (gap-closure round 4). The prior
round (2026-08-23T21:15:00Z) found the phase 9/9 must-haves verified with zero gaps, but left three
Warning-severity code-review findings (WR-03, WR-04, WR-05) undispositioned as human-decision
items. The operator has since decided "fix now" on all three; they were recorded as UAT gaps
G-03.1-69/70/71, and plans 03.1-16, 03.1-17, and 03.1-18 executed to close them. This round verifies
independently against the codebase — not against SUMMARY.md/UAT.md prose — whether all three fixes
are genuinely present and wired, whether the round's own new code review (rescoped to the six
changed files) surfaces anything further, and whether the phase's original 9 must-haves still hold.

## Direct Answer: Are the Three Prior human_verification Items Discharged? Do the Original 9 Must-Haves Still Hold?

**Yes to both, independently confirmed against the source.**

**The three prior-round human-decision items are now discharged — not by decision alone, but by
implementation, each independently confirmed by direct reading rather than by trusting
SUMMARY.md/UAT.md claims:**

- **WR-03** (`maintenance_default_grace_minutes` never reached the client): `dashboard/beacon/web.py`
  line 67 (`row['default_grace_minutes'] = int(default_grace_minutes)`) confirms the payload key;
  `dashboard/app.py:2318` confirms `SETTINGS.maintenance_default_grace_minutes` is threaded into it;
  `dashboard/app.js` lines 411-417, 503, 591-592, 644, 657 confirm the client reads a mutable
  `currentDefaultGraceMinutes` binding, seeded to the documented fallback (15) and updated from the
  server payload, at all three prefill sites. **RESOLVED.**
- **WR-04** (midnight-wraparound clustering defect): `dashboard/beacon/maintenance.py:200-211`
  confirms `_circular_minute_distance` exists and is documented as clock-position-only; line 295
  confirms it is used in `detect_suggestion`'s anchor-distance test; line 352 confirms it is also
  used in `suggestion_overlaps_enabled_window`'s start comparison; lines 306-312 confirm the
  cluster's reported start minute is now a circular, anchor-relative median, not a naive linear one;
  lines 290 and 354-equivalent confirm duration comparisons remain plain `abs()` at both sites.
  **RESOLVED.**
- **WR-05** (`_ATTRIBUTION_MAX_ANCHOR_STEPS` silently under-splitting past ~129 days):
  `dashboard/beacon/maintenance.py:386` confirms the constant is now an absolute ceiling only,
  raised 64 -> 4096; lines 389-407 confirm `_anchor_step_budget` derives the working budget from
  `_covering_boundaries`'s own loop-termination arithmetic; lines 433-442 confirm a named module
  logger (`beacon.maintenance`) emits a warning naming both the requested and covered extent when
  the ceiling binds, and the function still falls through to a normal (non-raising) return.
  **RESOLVED.**

**Both factual corrections this round recorded against 03.1-REVIEW.md are confirmed accurate by
direct reading, not merely repeated from UAT.md prose:**

1. **WR-04 narrowing.** Confirmed correct: `_observation_from_pair` sets `start_minute` from local
   wall-clock hour/minute (`dashboard/beacon/maintenance.py`), so a genuinely FIXED-time restart at
   23:55 produces an identical `start_minute` (1435) on every occurrence — the raw linear distance
   between identical values is always zero, so this case never needed the circular fix. Only a
   pattern whose start time *itself* jitters across the midnight boundary (e.g.
   23:55/00:00/00:05/23:58, `start_minute` values `[1435, 0, 5, 1438]`) exercises the defect. Both
   claims are testable and were independently spot-run: `DetectorTests::test_a_fixed_time_restart_just_before_midnight_still_clusters`
   and `DetectorTests::test_a_nightly_pattern_straddling_midnight_clusters_and_names_a_time_near_midnight`
   both pass.
2. **WR-05 caller correction.** Confirmed correct by direct read: `dashboard/beacon/diagnosis.py:637`
   (inside `get_current_diagnosis`, serving `/api/advanced/current`) calls
   `maintenance.attributed_downtime_seconds(...)` with an interval bounded by
   `period_seconds = settings.expire_days * 86400` (line 628) — uncapped, since
   `dashboard/beacon/config.py::_positive_int` enforces no upper bound on `EXPIRE_DAYS`. By contrast,
   `dashboard/app.py:2645`'s call is bounded by `CHECK_RETENTION_SECONDS = UPTIME_WINDOW_SECONDS +
   86400` (app.py:74), a FIXED 8-day value never derived from `EXPIRE_DAYS`. 03.1-REVIEW.md's
   original attribution of the exposure to `/api/services` was the wrong caller; the correction
   naming `diagnosis.py:637` / `get_current_diagnosis` is accurate.

**The phase's original 9 must-haves still hold — verified as a regression, not merely asserted.**
This round's diff touches exactly `dashboard/app.js`, `dashboard/app.py` (a single added line),
`dashboard/beacon/maintenance.py`, `dashboard/beacon/web.py`, `tests/test_maintenance_windows.py`,
and `tests/test_ui_states.py` (confirmed via `git diff 28506fd..HEAD --stat`). None of the files
underpinning the prior round's 9 verified truths — `docker-compose.yml`, `dashboard/beacon/migrations.py`,
`dashboard/beacon/recovery.py`, `tests/test_module_boundaries.py`, `dashboard/beacon/config.py` — were
touched (confirmed: `git diff 28506fd..HEAD --stat -- dashboard/beacon/config.py docker-compose.yml`
produces no output). The single `dashboard/app.py` change is the one additive keyword argument at
line 2318 threading the grace default; the connection-lease code this phase's earlier rounds fixed
(`worker_process_preview_requests` at line 2231) is untouched. The full suite, run once
independently in this verification pass, is green at **561 passed, 470 subtests passed** — matching
the count both 03.1-18-SUMMARY.md and this round's task context claim exactly, reproduced
independently rather than trusted.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator can manually add, edit, disable, and remove a recurring per-service local-time maintenance window with an explicit grace period (SC1 / MNT-01) | ✓ VERIFIED | Unaffected by this round's structural changes; regression re-run this round (full suite) is green. The grace-default PREFILL is additionally now server-sourced (WR-03 closure), confirmed by direct read of `web.py`, `app.py`, `app.js`. |
| 2 | After three similar daily restart outages, Beacon offers an inactive suggested window requiring operator confirmation/adjustment before it changes behavior, including patterns that straddle midnight (SC2 / MNT-02) | ✓ VERIFIED | Previously verified "with a known edge-case gap"; that gap (WR-04, midnight wraparound) is now closed — confirmed by direct read of `_circular_minute_distance` and the anchor-relative circular median in `dashboard/beacon/maintenance.py`, and by an independently re-run named test (`DetectorTests::test_a_nightly_pattern_straddling_midnight_clusters_and_names_a_time_near_midnight`, 1 passed). |
| 3 | Confirmed maintenance suppresses only expected down/recovered event entries and transition alerts; every probe remains stored; downtime still lowers availability (SC3 / MNT-03) | ✓ VERIFIED | Unaffected by this round's file set. Full-suite regression green. |
| 4 | A service still down after window+grace produces one truthful outage event/alert; dashboard distinguishes active maintenance from unresolved failure (SC4 / MNT-04) | ✓ VERIFIED | Unaffected by this round's file set. Full-suite regression green. |
| 5 | A first-boot `docker compose up` applies pending migrations in a step with no ordinary shared lease held before it completes (G-03.1-2 defect 1) | ✓ VERIFIED | `docker-compose.yml` not touched this round (confirmed: no diff). Carried forward unaffected. |
| 6 | A reboot with nothing pending never requests the exclusive maintenance lock (G-03.1-2 defect 1, reboot residual) | ✓ VERIFIED | Unaffected; `dashboard/beacon/migrations.py` not touched this round. |
| 7 | A contention window that ends is survived via bounded backoff; the real migration error is never displaced by a contention message (G-03.1-2 defect 2) | ✓ VERIFIED | Unaffected; `dashboard/beacon/migrations.py` not touched this round. |
| 8 | Every connection the web and legacy-worker surface opens releases its shared maintenance lease on every exit path, structurally (G-03.1-2 defect 3 / G-03.1-2b) | ✓ VERIFIED | Unaffected; `dashboard/app.py`'s only change this round is the additive `default_grace_minutes` keyword at line 2318, confirmed by `git diff` to be the sole hunk in that file. `worker_process_preview_requests` (line 2231) untouched. |
| 9 | `docker compose up` no longer starts the offline recovery command (G-03.1-2 artifact 4) | ✓ VERIFIED | Unaffected; `docker-compose.yml` not touched this round. |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified)

### This Round's Gap Closures (G-03.1-69, G-03.1-70, G-03.1-71)

| Gap | Truth Closed | Status | Evidence |
|-----|--------------|--------|----------|
| G-03.1-69 (WR-03) | A configuration knob Beacon parses and validates actually affects Beacon's behaviour | ✓ VERIFIED | Direct read of `dashboard/beacon/web.py:40-67`, `dashboard/app.py:2318`, `dashboard/app.js:411-417,503,591-592,644,657`. Named test independently re-run: `DefaultGraceMetadataTests::test_the_metadata_response_publishes_the_configured_default_grace` — 1 passed. |
| G-03.1-70 (WR-04) | A recurring nightly restart is offered as a suggested window regardless of where in the day it falls, including across midnight | ✓ VERIFIED | Direct read of `_circular_minute_distance` (`maintenance.py:200-211`), its two call sites (295, 352), and the circular median (306-312). Named test independently re-run: `DetectorTests::test_a_nightly_pattern_straddling_midnight_clusters_and_names_a_time_near_midnight` — 1 passed. |
| G-03.1-71 (WR-05) | Maintenance attribution is accurate over any retention period the deployment is configured for, or the limit is enforced and documented truthfully | ✓ VERIFIED | Direct read of `_anchor_step_budget` (`maintenance.py:389-407`), the raised ceiling (386), and the non-silent warning (433-442). Named test independently re-run: `AttributionTests::test_an_offline_interval_longer_than_the_shipped_anchor_bound_is_fully_attributed` — 1 passed (2 subtests). |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard/beacon/web.py` | `metadata_response` publishes `default_grace_minutes` on the metadata payload | ✓ VERIFIED | Line 67, keyword-only param confirmed by direct read; docstring extended to document the presence guarantee. |
| `dashboard/app.py` | `_service_meta_row` threads `SETTINGS.maintenance_default_grace_minutes` through | ✓ VERIFIED | Line 2318, confirmed the sole change in the file's diff this round. |
| `dashboard/app.js` | Editor prefill reads a server-seeded mutable binding at all three consumption sites | ✓ VERIFIED | Lines 411-417 (binding + comment), 503 (Add), 591-592 (update from fetch), 644 (Confirm), 657 (Adjust) — all confirmed by direct read. |
| `dashboard/beacon/maintenance.py` | Circular minute-of-day distance + circular median in `detect_suggestion`; same helper in `suggestion_overlaps_enabled_window`; derived, honest, non-silent anchor-step budget in `_covering_boundaries` | ✓ VERIFIED | `_circular_minute_distance` (200-211), used at 295 and 352; circular median (306-312); `_anchor_step_budget` (389-407); raised ceiling with honest comment (375-386); non-silent warning (433-442) — all confirmed by direct read. |
| `tests/test_maintenance_windows.py` | New regressions for all three gaps, all pre-existing assertions unchanged | ✓ VERIFIED | 13 named new tests spot-checked for existence via `grep -rl "def <name>"` — all 13 resolve to exactly one file each. 3 spot-run directly (`AttributionTests::test_an_offline_interval_longer_than_the_shipped_anchor_bound_is_fully_attributed`, `DetectorTests::test_a_nightly_pattern_straddling_midnight_clusters_and_names_a_time_near_midnight`, `DefaultGraceMetadataTests::test_the_metadata_response_publishes_the_configured_default_grace`) — 3 passed. |
| `tests/test_ui_states.py` | Browser regressions for all three client prefill sites | ✓ VERIFIED | New tests present in file (confirmed via full-suite collection count of 561, matching claimed totals); not individually re-run in this pass since the browser suite was exercised as part of the full-suite run. |
| `.planning/phases/03.1-planned-maintenance-recognition/03.1-UAT.md` | G-03.1-69/70/71 recorded `status: resolved` with per-item `fix_applied` evidence; `## Summary` counts updated | ✓ VERIFIED | Confirmed by direct read: all three entries carry `status: resolved`, `resolved_by`, `resolved_at`, and a `fix_applied` block quoting each `missing:` item verbatim. `## Summary` reads "total 71, passed 68, issues 3 ... After: all three resolved to pass." Frontmatter `status: complete`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `dashboard/beacon/config.py::Settings.maintenance_default_grace_minutes` | `dashboard/app.js` grace inputs | `_service_meta_row` -> `metadata_response` -> `GET/PUT /api/service-meta/<port>` -> `currentDefaultGraceMinutes` | ✓ WIRED | Full chain confirmed by direct read at each hop (app.py:2318, web.py:67, app.js:591-592/503/644/657). |
| `dashboard/beacon/maintenance.py::detect_suggestion` | `dashboard/beacon/maintenance.py::suggestion_overlaps_enabled_window` | shared `_circular_minute_distance` helper | ✓ WIRED | Confirmed by direct read: one helper definition (line 200), two call sites (295, 352). |
| `dashboard/beacon/diagnosis.py::get_current_diagnosis` | `dashboard/beacon/maintenance.py::attributed_downtime_seconds` | `period_seconds = settings.expire_days * 86400` passed as the interval bound | ✓ WIRED | Confirmed by direct read: diagnosis.py:628 (period_seconds), diagnosis.py:637 (the call site) — matches this round's own corrected caller identification exactly. |
| `dashboard/beacon/maintenance.py::_covering_boundaries` | `_anchor_step_budget` | derived per-call budget capped at `_ATTRIBUTION_MAX_ANCHOR_STEPS` | ✓ WIRED | Confirmed by direct read at lines 429-431 (`required_steps = _anchor_step_budget(...)`, `max_steps = min(required_steps, _ATTRIBUTION_MAX_ANCHOR_STEPS)`). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full project suite is green at the count claimed by this round's plans and task context | `uv run --project dashboard python -m pytest -q` (run once, in background, ~146s) | 561 passed, 470 subtests passed | ✓ PASS — independently reproduced, matches claimed count exactly |
| All 13 spot-checked new-test names actually exist in the tree (not just claimed) | `grep -rl "def <name>\b" tests/` per name | all 13 resolve to exactly 1 file each | ✓ PASS |
| Three representative named tests (one per gap) pass when run directly | `pytest -q tests/test_maintenance_windows.py::AttributionTests::test_an_offline_interval_longer_than_the_shipped_anchor_bound_is_fully_attributed tests/test_maintenance_windows.py::DetectorTests::test_a_nightly_pattern_straddling_midnight_clusters_and_names_a_time_near_midnight tests/test_maintenance_windows.py::DefaultGraceMetadataTests::test_the_metadata_response_publishes_the_configured_default_grace` | 3 passed, 2 subtests passed | ✓ PASS |
| No debt markers (TBD/FIXME/XXX) introduced in files this round modified | `grep -n -E "TBD|FIXME|XXX"` across all 6 modified source/test files plus 03.1-UAT.md | no matches | ✓ PASS |
| Prohibited files (`dashboard/beacon/config.py`, `docker-compose.yml`) genuinely untouched this round | `git diff 28506fd..HEAD --stat -- dashboard/beacon/config.py docker-compose.yml` | no output | ✓ PASS |
| No new route/endpoint added this round | `git diff 28506fd..HEAD -- dashboard/app.py dashboard/beacon/web.py \| grep "@app.route\|add_url_rule"` | no matches | ✓ PASS |
| `03.1-VERIFICATION.md` (the prior version) was not edited by plans 16/17/18 | `git status --porcelain .planning/.../03.1-VERIFICATION.md` before this pass overwrote it | no output (clean) | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this repository and none are referenced by this phase's plans or SUMMARYs. SKIPPED (no runnable probes for this phase).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| MNT-01 | 03.1-01..16 | Manual maintenance window CRUD with grace period | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 1 VERIFIED; plan 03.1-16 additionally wires the default grace prefill (WR-03 closure). |
| MNT-02 | 03.1-01..17 | Pattern-detected inactive suggestion requiring confirmation | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 2 VERIFIED; plan 03.1-17 closes the midnight-wraparound edge case (WR-04) that the prior round left as a known gap. |
| MNT-03 | 03.1-01..18 | Suppression preserves probes and downtime accounting | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 3 VERIFIED; plan 03.1-18 closes the long-interval attribution truncation (WR-05). |
| MNT-04 | 03.1-01..15 | Overrun produces one truthful event/alert | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 4 VERIFIED; unaffected by this round. |

No orphaned requirements: `REQUIREMENTS.md` maps exactly MNT-01..04 to Phase 03.1, and all four appear
across plan frontmatter `requirements:` fields (including plans 16/17/18 this round).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `dashboard/beacon/maintenance.py` | 433-442 | Ceiling-bound warning logged unthrottled inside a per-service, per-request loop (`_covering_boundaries` called once per window per interval, from `attributed_downtime_seconds`, from `get_current_diagnosis`, on every `/api/advanced/current` request) | ⚠️ Warning (03.1-REVIEW.md WR-06, new this round) | Not reachable at the documented default (`EXPIRE_DAYS=7`); reachable only via an operator-misconfigured `EXPIRE_DAYS`. When reachable, produces a sustained per-poll log flood rather than a one-time diagnostic. Not a regression — this is new code this round introduced to fix WR-05, and the review flagged a secondary hygiene issue in that same fix. Routed to Human Verification. |
| `dashboard/beacon/maintenance.py` | 276-280, 312 | Docstring says the circular median result is "rounded to a whole minute"; the implementation truncates (`int(...)`) | ℹ️ Info (03.1-REVIEW.md IN-01) | Not a behavior change (the pre-existing linear-median code did the same truncation). A future reader could be misled about round-half-up semantics. Cosmetic; not routed to human decision — informational only, consistent with how prior-round Info-level findings (none existed in that round) would be treated. |

No 🛑 Blocker anti-patterns found. No TBD/FIXME/XXX debt markers found in any file this round modified.

### Gaps Summary

No gaps. All three of this round's UAT gaps (G-03.1-69, G-03.1-70, G-03.1-71) — themselves the
recorded disposition of the prior round's three human-decision items (WR-03, WR-04, WR-05) — are
confirmed closed by direct source inspection, not by trusting SUMMARY.md/UAT.md prose. Both factual
corrections this round recorded against 03.1-REVIEW.md (WR-04's narrowing; WR-05's corrected caller)
are confirmed accurate. The phase's original 9 must-haves are confirmed unaffected: the file set this
round touched is disjoint from the files those 9 truths depend on, except for a single additive line
in `dashboard/app.py` (the `default_grace_minutes` keyword) which does not intersect the
connection-lease code those truths cover. The full test suite (561 passed / 470 subtests),
independently reproduced in this verification pass in a single run, matches the count claimed by
plan 03.1-18's SUMMARY exactly. All four roadmap requirement IDs (MNT-01..04) remain satisfied.

Status is `human_needed`, not `passed`, solely because this round's own rescoped code review
(`03.1-REVIEW.md`) surfaced one new Warning-severity finding (WR-06) on code this round itself
introduced (the non-silence warning added to close WR-05), and it has not been dispositioned in
`03.1-UAT.md`. It is not a regression, not a correctness defect, and not reachable at the documented
default deployment — but per this phase's own established adversarial-verification precedent (the
prior round routed WR-03/04/05 to human decision under the same reasoning), an undispositioned
Warning-severity finding is surfaced for an explicit human decision rather than silently absorbed
into a `passed` verdict.

### Human Verification Required

See `human_verification` in frontmatter for WR-06's disposition decision.

---

_Verified: 2026-08-24T15:15:00Z_
_Verifier: Claude (gsd-verifier)_
