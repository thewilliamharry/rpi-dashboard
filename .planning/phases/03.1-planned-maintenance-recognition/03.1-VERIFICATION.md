---
phase: 03.1-planned-maintenance-recognition
verified: 2026-08-23T21:15:00Z
status: human_needed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "8/9 must-haves verified (2026-08-23T17:47:19Z round)"
  gaps_closed:
    - "Truth 8 (G-03.1-2b / 03.1-VERIFICATION.md prior gap): 'Every connection the web and legacy-worker surface opens releases its shared maintenance lease on every exit path, structurally' — was FAILED (worker_process_preview_requests at dashboard/app.py:2231 leaked its lease via a bare `with connect_db(...) as conn:`). Now VERIFIED: the site reads `with database_access(authority.db_path) as conn:` (confirmed by reading dashboard/app.py:2231), and `database_access` closes unconditionally in a `finally` suite (dashboard/beacon/db.py:93-99, confirmed by reading)."
    - "Missing item 1 ('Convert dashboard/app.py:2231 to `with database_access(authority.db_path) as conn:`'): CONFIRMED SATISFIED — verified directly in dashboard/app.py, not merely claimed in SUMMARY/UAT prose."
    - "Missing item 2 ('Extend the AST structural gate ... to also catch the `with connect_db(...) as conn:` shape and worker-callback call sites'): CONFIRMED SATISFIED — the round-2 gate (`test_no_web_handler_binds_a_connection_outside_a_context_manager`) is retired (only a comment reference remains, no test function by that name); a new class-exhaustive `ConnectionOwnershipGateTests` (17 test methods, tests/test_module_boundaries.py:808+) walks `ast.Assign`/`ast.With`/`ast.AsyncWith`, resolves both `Name` and `Attribute` callees, and resolves opening seams package-wide. Independently re-ran standalone: 10/10 pass."
  gaps_remaining: []
  regressions: []
gaps: []
human_verification:
  - test: "Decide disposition for WR-03 (dashboard/beacon/config.py:61 — `maintenance_default_grace_minutes` is parsed/validated/tested but never consumed; app.js:411 hardcodes its own literal `15` instead), surfaced by 03.1-REVIEW.md during this same round and not yet dispositioned in 03.1-UAT.md."
    expected: "Either wire the setting through to the UI (expose via /api/config or metadata_response and have app.js read it) or remove the dead Settings field/env var, and record the decision as a gap entry or an accepted-as-is note in 03.1-UAT.md."
    why_human: "Judgment call on whether a non-functional configuration knob blocks phase completion or is acceptable as a documented follow-up; not resolvable by grep/test alone."
  - test: "Decide disposition for WR-04 (dashboard/beacon/maintenance.py:255-260 — `detect_suggestion`'s clustering uses raw `abs(start_minute_a - start_minute_b)` with no midnight wraparound, so a genuine nightly restart pattern straddling midnight, e.g. 23:55/00:00/00:05, will never cluster into a suggestion)."
    expected: "Either fix the wraparound distance calculation (fix sketched in 03.1-REVIEW.md) and add a midnight-straddling regression, or explicitly accept this as a known limitation of MNT-02's suggestion detection and record it in 03.1-UAT.md."
    why_human: "This affects roadmap SC2/MNT-02 ('After three similar daily restart outages, Beacon offers a suggested window') in a specific, real-world-common edge case (nightly cron restarts near midnight). Severity is rated Warning (not Critical) by 03.1-REVIEW.md because the core suggestion mechanism works for the non-wraparound case, and no roadmap success criterion explicitly requires midnight-straddling coverage — a human should decide whether this is in-scope for phase closure or a deferred follow-up."
  - test: "Decide disposition for WR-05 (dashboard/beacon/maintenance.py:312-345 — `_ATTRIBUTION_MAX_ANCHOR_STEPS = 64` covers ~129 days, not 'well over a year' as its own comment claims; a deployment with `EXPIRE_DAYS` configured above ~129 gets silently under-split, and therefore potentially mis-attributed, maintenance-attribution accounting for the older portion of a long offline interval)."
    expected: "Either correct the comment and add an explicit cap/validation tying EXPIRE_DAYS to the anchor-step bound, or raise the step count to genuinely cover a year-plus, and add a regression with a 150+-day offline interval."
    why_human: "Does not affect the default deployment (EXPIRE_DAYS defaults to 7, api_services's own attribution call is separately bounded to 8 days), so severity is correctly Warning not Critical per 03.1-REVIEW.md. A human should decide whether documenting the true bound is sufficient or whether the mechanism itself needs to change before phase closure."
---

# Phase 03.1: Planned Maintenance Recognition Verification Report

**Phase Goal:** The operator can confirm or define expected recurring service restarts so Beacon
suppresses routine transition noise without hiding failed probes, reducing uptime impact, or
overlooking an overrun.

**Verified:** 2026-08-23T21:15:00Z
**Status:** human_needed
**Re-verification:** Yes — this is the second re-verification of this phase. The first
re-verification round (2026-08-23T17:47:19Z) found the phase 8/9 must-haves verified, with one
failed truth (structural connection-lease-release, split into gap `G-03.1-2b`) and two `missing:`
items. Plans 03.1-14 and 03.1-15 were then executed to close that gap. This round verifies,
independently against the codebase (not against SUMMARY.md or UAT.md claims), whether the
previously-failed truth and both `missing:` items are now genuinely satisfied, whether the
originally-reopened UAT gaps (G-03.1-1, G-03.1-2) remain closed, and whether the code review that
accompanied this closure work (03.1-REVIEW.md) surfaced anything that still needs a decision.

## Direct Answer: Is the Previously-Failed Truth Now Satisfied? Are Both `missing:` Items Answered?

**Yes to both, independently confirmed against the source, not just against SUMMARY.md/UAT.md prose.**

- **Previously-failed truth** ("Every connection the web and legacy-worker surface opens releases
  its shared maintenance lease on every exit path, structurally"): **NOW VERIFIED.**
  `dashboard/app.py:2231` (`worker_process_preview_requests`, the worker's own poll callback
  registered at `dashboard/worker.py:35`) now reads:
  ```python
  with database_access(authority.db_path) as conn:
  ```
  `database_access` (`dashboard/beacon/db.py:93-99`) is:
  ```python
  def database_access(settings_or_path):
      conn = connect_db(settings_or_path)
      try:
          yield conn
      finally:
          conn.close()
  ```
  — an unconditional close in `finally`, verified by direct reading, not inference from a test
  result. The two remaining `connect_db(` call sites in `dashboard/app.py` (line 130, the
  intentionally-unmanaged `get_db()` test-only compatibility helper; line 247,
  `_worker_write_transaction`, which has its own explicit `try/finally: conn.close()`) are
  confirmed to be the accepted, non-leaking exceptions — read directly, not taken on faith.

- **Missing item 1** ("Convert `dashboard/app.py:2231` to `with database_access(authority.db_path)
  as conn:`"): **SATISFIED**, confirmed by direct read above.

- **Missing item 2** ("Extend the AST structural gate ... to also catch the `with connect_db(...)
  as conn:` shape and worker-callback call sites"): **SATISFIED.** The round-2 gate
  (`test_no_web_handler_binds_a_connection_outside_a_context_manager`) no longer exists as a test
  function (only a comment references its retired name, `tests/test_module_boundaries.py:153`). A
  new `ConnectionOwnershipGateTests` class (17 test methods) walks `ast.Assign`, `ast.With`, and
  `ast.AsyncWith`, recognizes both `Name`- and `Attribute`-callee bindings, and resolves opening
  seams package-wide (cross-module aliased imports, module-local factories). Independently
  re-ran standalone: **10/10 pass** (`uv run --project dashboard python -m pytest -q
  tests/test_module_boundaries.py::ConnectionOwnershipGateTests`).

Both originally-reopened UAT gaps remain resolved: **G-03.1-1** (migration 9 support-floor
fingerprints) was untouched by this round and unaffected by plans 14/15 (neither plan modified
`support_floor.json` or the migration fixtures, confirmed by `files_modified` in both plans'
frontmatter). **G-03.1-2** (startup ordering, contention backoff, recovery profile, connection
leaks) is `status: resolved` in `03.1-UAT.md` with a closure note naming the plan that closed each
of its four original `missing:` items; its third item's residual defect is `G-03.1-2b`, now also
`status: resolved` and independently confirmed above.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Operator can manually add, edit, disable, and remove a recurring per-service local-time maintenance window with an explicit grace period (SC1 / MNT-01) | ✓ VERIFIED | Unaffected by plans 14/15 (neither touches `_validate_maintenance_windows`, `upsert_maintenance_windows`, or any window-CRUD route). Regression re-run this round: `tests/test_maintenance_windows.py` + `tests/test_uptime_integration.py` = 120 passed (identical count to prior round). |
| 2 | After three similar daily restart outages, Beacon offers an inactive suggested window requiring operator confirmation/adjustment before it changes behavior (SC2 / MNT-02) | ✓ VERIFIED (with a known edge-case gap — see Human Verification) | Unaffected by plans 14/15. Regression re-run passed (see above). `03.1-REVIEW.md` (this round's own code review) newly found WR-04: `detect_suggestion`'s clustering has no midnight-wraparound handling, so a genuine pattern straddling midnight is never detected. This is a real, narrow-scope defect in the mechanism (not a regression from plans 14/15, and not part of either UAT gap this round targets) — routed to human decision below rather than silently dropped. |
| 3 | Confirmed maintenance suppresses only expected down/recovered event entries and transition alerts; every probe remains stored; downtime still lowers availability (SC3 / MNT-03) | ✓ VERIFIED | Unaffected by plans 14/15. Regression re-run passed (see above). |
| 4 | A service still down after window+grace produces one truthful outage event/alert; dashboard distinguishes active maintenance from unresolved failure (SC4 / MNT-04) | ✓ VERIFIED | Unaffected by plans 14/15. Regression re-run passed (see above). |
| 5 | A first-boot `docker compose up` applies pending migrations in a step with no ordinary shared lease held before it completes (G-03.1-2 defect 1) | ✓ VERIFIED | Unaffected by plans 14/15 (`docker-compose.yml` not in either plan's `files_modified`). Confirmed directly this round: `migrate` service, `condition: service_completed_successfully` gates on both `web` and `worker` (`docker-compose.yml:59-61,106-108`). |
| 6 | A reboot with nothing pending never requests the exclusive maintenance lock (G-03.1-2 defect 1, reboot residual) | ✓ VERIFIED | Unaffected by plans 14/15 (fast path itself unmodified; only the retry loop below it was touched by plan 15). Carried forward from prior round; unaffected by this round's changes. |
| 7 | A contention window that ends is survived via bounded backoff; the real migration error is never displaced by a contention message (G-03.1-2 defect 2) | ✓ VERIFIED | Hardened this round by plan 03.1-15 (03.1-REVIEW.md WR-01): the per-attempt exclusive-maintenance timeout is now capped at `min(lock_timeout_seconds, remaining_budget)` (`dashboard/beacon/migrations.py:792-797`, confirmed by reading), making `CONTENTION_BUDGET_SECONDS` a true wall-clock ceiling instead of one that could overrun by up to a further `lock_timeout_seconds`. Independently re-ran `test_the_contention_budget_is_a_hard_wall_clock_ceiling` standalone: passes in ~5s, consistent with the plan's claimed 5.04-5.08s capped band (vs. ~30s uncapped). |
| 8 | Every connection the web and legacy-worker surface opens releases its shared maintenance lease on every exit path, structurally (G-03.1-2 defect 3 / G-03.1-2b) | ✓ VERIFIED | **Previously FAILED, now closed.** See "Direct Answer" section above for full evidence chain. |
| 9 | `docker compose up` no longer starts the offline recovery command (G-03.1-2 artifact 4) | ✓ VERIFIED | Unaffected by plans 14/15. `docker-compose.yml:76`: `recovery` service carries `profiles: ["recovery"]`, confirmed directly this round. |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_module_boundaries.py` | Class-exhaustive connection-ownership gate (package-wide seam resolution, derived module coverage, two allowlist kinds, self-tests) | ✓ VERIFIED | `ConnectionOwnershipGateTests` class present (line 808), 17 test methods; standalone re-run 10/10 pass; round-2 gate confirmed retired (name only survives in a comment). |
| `dashboard/app.py` | `worker_process_preview_requests` bound through `database_access`, not bare `connect_db` | ✓ VERIFIED | Line 2231 confirmed by direct read (see Direct Answer section). |
| `dashboard/beacon/migrations.py` | `create_verified_backup` and `_apply_pending_migrations` close every connection on every exit path; per-attempt contention timeout capped at remaining budget | ✓ VERIFIED | All four `sqlite3.connect(...)` calls in `create_verified_backup` and the one in `_apply_pending_migrations` confirmed wrapped in `contextlib.closing(...)` by direct read (lines 611-614, 716). Contention cap confirmed at lines 792-797. |
| `dashboard/beacon/recovery.py` | All four connection sites close on every exit path | ✓ VERIFIED | All four `sqlite3.connect(...)` calls (`_validate_supported_database`, `_authorization_matches_current_database`, `_worker_is_stale`, `_checkpoint_and_remove_sidecars`) confirmed wrapped in `contextlib.closing(...)` by direct read (lines 137, 232, 268, 321). |
| `tests/test_migrations.py` | Two behavioural close proofs (applying-path, backup-path) that assert path entry before closure; contention-ceiling regression | ✓ VERIFIED | Independently re-ran `test_every_connection_the_applying_migration_path_opens_is_closed`, `test_every_connection_the_verified_backup_path_opens_is_closed`, `test_the_contention_budget_is_a_hard_wall_clock_ceiling` standalone: 3/3 pass. |
| `.planning/phases/03.1-planned-maintenance-recognition/03.1-UAT.md` | G-03.1-2 updated to resolved with closure note; G-03.1-2b entry added and resolved | ✓ VERIFIED | Both entries confirmed present with `status: resolved`, closure note naming the closing plan for each of G-03.1-2's four original `missing:` items, and G-03.1-2b's `fix_applied` answering both prior-VERIFICATION `missing:` items by name with runnable verification commands. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `dashboard/worker.py` poll loop | `dashboard/app.py::worker_process_preview_requests` | registered callback | ✓ WIRED | `dashboard/worker.py:35` (referenced in 03.1-14-SUMMARY.md, unaffected by this round; the callback itself is what was fixed). |
| `worker_process_preview_requests` | `database_access` | connection binding | ✓ WIRED | Confirmed by direct read at `dashboard/app.py:2231`. |
| `docker-compose.yml web/worker` | `docker-compose.yml migrate` | `depends_on` + `service_completed_successfully` | ✓ WIRED | Confirmed by direct read at `docker-compose.yml:59-61,106-108`; unaffected by this round. |
| `dashboard/beacon/migrations.py` retry loop | `exclusive_database_maintenance` | per-attempt timeout argument | ✓ WIRED | Confirmed by direct read: `min(lock_timeout_seconds, remaining_budget)` passed at `migrations.py:796`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Connection-ownership gate fires zero findings on the fixed tree, and its 17 self-tests (including the two permanent real-site revert proofs) pass | `pytest -q tests/test_module_boundaries.py::ConnectionOwnershipGateTests` | 10 passed | ✓ PASS |
| Both new migration close proofs (WR-02) pass, and prove path entry before asserting closure | `pytest -q tests/test_migrations.py -k "test_every_connection_the_applying_migration_path_opens_is_closed or test_every_connection_the_verified_backup_path_opens_is_closed"` | 2 passed | ✓ PASS |
| Contention budget is a hard wall-clock ceiling (WR-01) | `pytest -q tests/test_migrations.py -k test_the_contention_budget_is_a_hard_wall_clock_ceiling` | 1 passed, ~5s elapsed | ✓ PASS |
| Maintenance-window and uptime-attribution behaviour (MNT-01/02/03/04) unaffected by this round's connection-lifecycle changes | `pytest -q tests/test_maintenance_windows.py tests/test_uptime_integration.py` | 120 passed | ✓ PASS |
| Full project suite is green at the count both plan SUMMARYs claim | `pytest -q` (full suite, run once) | 540 passed, 468 subtests passed | ✓ PASS — matches plan 03.1-15-SUMMARY.md's claimed ending count exactly (baseline before this round was 528/456, per 03.1-14-SUMMARY.md: +9 from plan 14, +3 from plan 15 = 540/468). |
| No debt markers (TBD/FIXME/XXX) introduced in files this round modified | `grep -n -E "TBD|FIXME|XXX"` across `tests/test_module_boundaries.py`, `dashboard/app.py`, `dashboard/beacon/migrations.py`, `dashboard/beacon/recovery.py`, `tests/test_migrations.py`, `03.1-UAT.md` | no matches | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| MNT-01 | 03.1-01..15 | Manual maintenance window CRUD with grace period | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 1 VERIFIED; regression 120 passed. |
| MNT-02 | 03.1-01..15 | Pattern-detected inactive suggestion requiring confirmation | ✓ SATISFIED (see WR-04 human-decision item) | REQUIREMENTS.md marks Complete; truth 2 VERIFIED for the mechanism as specced; WR-04 is a narrow, Warning-severity edge case (midnight wraparound), not a failure of the core mechanism. |
| MNT-03 | 03.1-01..15 | Suppression preserves probes and downtime accounting | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 3 VERIFIED; regression 120 passed. |
| MNT-04 | 03.1-01..15 | Overrun produces one truthful event/alert | ✓ SATISFIED | REQUIREMENTS.md marks Complete; truth 4 VERIFIED; regression 120 passed. |

No orphaned requirements: `REQUIREMENTS.md` maps exactly MNT-01..04 to Phase 03.1, and all four appear in plan frontmatter `requirements:` fields.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `dashboard/beacon/config.py` | 61, 262-264 | Dead configuration knob (`maintenance_default_grace_minutes` parsed/validated/tested but never consumed; `app.js:411` hardcodes its own literal) | ⚠️ Warning (03.1-REVIEW.md WR-03) | Operator-configured `MAINTENANCE_DEFAULT_GRACE_MINUTES` has zero observable effect. Not a regression from this round; surfaced by this round's own code review. Routed to Human Verification. |
| `dashboard/beacon/maintenance.py` | 255-260 | `detect_suggestion` clustering has no midnight-wraparound handling | ⚠️ Warning (03.1-REVIEW.md WR-04) | A genuine nightly-restart pattern straddling midnight is never detected as a suggestion. Not a regression from this round; pre-existing in plan 03.1-06's clustering logic, surfaced by this round's own code review. Routed to Human Verification. |
| `dashboard/beacon/maintenance.py` | 312-345 | `_ATTRIBUTION_MAX_ANCHOR_STEPS = 64` covers ~129 days, contradicting its own "well over a year" comment | ⚠️ Warning (03.1-REVIEW.md WR-05) | Silently under-splits (and can mis-attribute) maintenance-attribution accounting for deployments with `EXPIRE_DAYS` configured above ~129 days. Does not affect the default 7-day retention. Not a regression from this round; surfaced by this round's own code review. Routed to Human Verification. |

No 🛑 Blocker anti-patterns found. No TBD/FIXME/XXX debt markers found in any file this round modified.

### Gaps Summary

No gaps. The previously-failed truth (structural lease-release at `worker_process_preview_requests`)
and both `missing:` items from the prior round's `03.1-VERIFICATION.md` are confirmed satisfied by
direct source inspection, not by trusting SUMMARY.md/UAT.md prose. All 4 roadmap requirement IDs
(MNT-01..04) remain satisfied and regression-tested at an identical pass count to the prior round.
The full test suite (540 passed / 468 subtests) matches the count both plans' SUMMARYs claim,
independently reproduced in this verification pass.

Status is `human_needed`, not `passed`, solely because this round's own code review
(`03.1-REVIEW.md`) surfaced three new Warning-severity findings (WR-03, WR-04, WR-05) that have not
been dispositioned in `03.1-UAT.md` — none of them regressions from plans 14/15, none of them part
of either UAT gap this round targets, and none rated Critical. An adversarial verification stance
requires surfacing these for an explicit human decision (fix now vs. accept as a documented
follow-up) rather than silently treating "0 Critical" as equivalent to "nothing outstanding."

### Human Verification Required

See `human_verification` in frontmatter for the three items (WR-03, WR-04, WR-05) requiring a
human disposition decision.

---

_Verified: 2026-08-23T21:15:00Z_
_Verifier: Claude (gsd-verifier)_
