---
phase: 05-theme-parity-analytics-experience
plan: 07
type: execute
wave: 5
gap_closure: true
depends_on: ["05-01", "05-04", "05-05"]
files_modified:
  - dashboard/advanced.js
  - dashboard/app.py
  - dashboard/beacon/diagnosis.py
  - tests/test_history_investigation_ui.py
  - tests/test_api_and_auth.py
  - tests/test_advanced_diagnosis_api.py
  - tests/test_ui_safety_integration.py
autonomous: true
requirements: [UX-06, UX-07]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A pending keyboard range anchor is abandoned by any intervening mouse-drag gesture: after Shift+Enter arms an anchor on a chart point, a mouse drag anywhere in the chart stack clears it, so a later plain Enter on any point target applies no range and issues no /api/telemetry/history request"
    - "The keyboard range gesture itself is unchanged and still works: Shift+Enter on one point followed by Enter on a second point, in one uninterrupted gesture, still applies the identical range the equivalent mouse drag applies"
    - "When an on-disk recovery marker exists and the worker heartbeat is merely aging (not yet stale), exactly one worker safety banner renders on each of / and /advanced, in both themes: the recovery banner, never the degraded banner alongside it"
    - "worker_degraded is false whenever recovery_required is true, on both /api/scan-status and get_current_diagnosis's safety block — the two surfaces agree because both apply the same guard, not because one masks the other"
    - "E7 (05-UI-SPEC UI Considerations, explicit): the two-focus-plus-Enter gesture reuses the mouse drag's own range-apply function and Escape cancels a pending anchor cleanly — and 'cleanly' now includes every path by which a mouse gesture supersedes a pending anchor, not only Escape and completion"
    - "E2 (05-UI-SPEC UI Considerations, explicit): the degraded state remains defined once, server-side, from the existing freshness tier; the mutual-exclusion guard added here is a server-side classification narrowing, never a client-side heuristic (05-CONTEXT D-01)"
    - "E4 (05-UI-SPEC UI Considerations, explicit): six-state distinctness across both themes holds at the banner layer too — no two worker safety banners asserting contradictory facts about the same heartbeat render at the same instant"
  prohibitions:
    - statement: "a range must never be applied from state the operator cannot see, and no keypress may act on an anchor armed before an intervening gesture"
      status: resolved
      verification: judgment
    - statement: "the keyboard range gesture must never be removed, disabled, or narrowed into a permanent no-op in order to close this defect — the stale anchor is what gets cleared, never the capability"
      status: resolved
      verification: judgment
    - statement: "the operator must never be shown a reassuring monitoring-continues reading beside a monitoring-is-paused reading about the same worker; when recovery is required it is the reassuring reading that yields, never the alarming one"
      status: resolved
      verification: judgment
    - statement: "the banner contradiction must never be resolved by hiding, softening, or deleting the recovery banner, nor by a client-side precedence rule in app.js or advanced.js — the server decides which state is true (05-CONTEXT D-01)"
      status: resolved
      verification: judgment
  artifacts:
    - dashboard/advanced.js
    - dashboard/app.py
    - dashboard/beacon/diagnosis.py
    - tests/test_history_investigation_ui.py
    - tests/test_api_and_auth.py
    - tests/test_advanced_diagnosis_api.py
    - tests/test_ui_safety_integration.py
  key_links:
    - "dashboard/advanced.js: beginDragSelect calls cancelKeyboardRangeAnchor before it un-hides the drag overlay"
    - "dashboard/advanced.js: cancelDragSelect calls cancelKeyboardRangeAnchor, so every drag exit path (pointerup via commitDragSelect, pointercancel, Escape) clears a pending anchor"
    - "dashboard/app.py: api_scan_status computes state['recovery_required'] strictly before state['worker_degraded'], and the worker_degraded expression reads it"
    - "dashboard/beacon/diagnosis.py: get_current_diagnosis's safety['worker_degraded'] reads the local recovery_required already computed above the return dict"
---

<objective>
Close the two confirmed, reproducible defects `05-VERIFICATION.md` records as gaps, and nothing else.

Gap 1 (CR-01, blocks ROADMAP SC3 / UX-06): the keyboard chart-range-selection gesture leaks its
module-level pending anchor across an intervening mouse drag, so a later plain Enter on an
unrelated chart point silently applies a range built from a pre-drag timestamp.

Gap 2 (WR-01, blocks ROADMAP SC4 / UX-07): the degraded-worker banner and the recovery banner can
render at the same instant with directly contradictory copy, because `worker_degraded` is guarded
against `worker_stale` but never against `recovery_required`.

Purpose: both defects invalidate a truth this phase already declared. Gap 1 contradicts
`05-04-PLAN.md`'s own must-have ("a pending keyboard range anchor can always be abandoned without
applying anything"). Gap 2 contradicts SC4's "visibly and meaningfully distinct" at exactly the
surface — safety banners — where a contradiction is most costly to the operator.

Output: two production-code fixes across three files, and five regression tests, each of which is
demonstrated RED against the unfixed code before the fix lands. No new files, no new architecture,
no new dependency.

`05-CONTEXT.md` decision coverage for this gap-closure scope: **D-01** (`degraded` is server-emitted,
never a client-side guess) is binding and is implemented by Task 3, which narrows the server
classification and adds no client-side precedence rule. **D-02** (density drives progressive
disclosure) and **D-03** (main-dashboard light-mode calm) are already discharged by the executed
plans 05-05 and 05-03 respectively, are recorded ✓ SATISFIED in `05-VERIFICATION.md`, and are
deliberately not re-planned here — no task in this plan touches either surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-parity-analytics-experience/05-VERIFICATION.md
@.planning/phases/05-theme-parity-analytics-experience/05-REVIEW.md
@.planning/phases/05-theme-parity-analytics-experience/05-CONTEXT.md
@.planning/phases/05-theme-parity-analytics-experience/05-01-SUMMARY.md
@.planning/phases/05-theme-parity-analytics-experience/05-04-SUMMARY.md
</context>

<scope_and_shape>

## Why one plan file, not two

`05-VERIFICATION.md`'s two gaps touch disjoint production files and are logically independent, so
by dependency analysis alone they could share a wave. They are deliberately **not** split into
`05-07` and `05-08`.

This project has already recorded the defect that makes splitting them risky: commit
`8cf22c8 docs(05): record wave-grouping defect — semantic coupling invisible to files_modified`.
Wave grouping is computed from `files_modified`, so it cannot see semantic coupling. Gap 1 modifies
`dashboard/advanced.js`; gap 2's rendered-page regression test loads `/advanced`, which executes
that same file's `renderSafety`. That coupling is real but invisible to a `files_modified` diff — a
two-plan split placed in one wave would let a half-applied `advanced.js` be under two executors at
once. Keeping both gaps in a single plan file makes the coupling structurally impossible rather
than merely declared.

**Wave 5.** `dashboard/advanced.js` and `tests/test_history_investigation_ui.py` were last modified
by `05-05` (wave 4); `dashboard/app.py`, `dashboard/beacon/diagnosis.py`,
`tests/test_api_and_auth.py`, `tests/test_advanced_diagnosis_api.py` and
`tests/test_ui_safety_integration.py` by `05-01` (wave 1); `dashboard/advanced.js` and
`tests/test_history_investigation_ui.py` also by `05-04` (wave 3). `max(1, 3, 4) + 1 = 5`.

## Tracer

`TRACER_MODE` is on, and no new tracer task is synthesized. This is gap closure against already-
shipped code: plan `05-01` was this phase's tracer (`degraded` end to end — one shared server
classifier, both payloads, one banner in both documents, proven by an unstubbed dual-theme test)
and it is executed and verified. Every layer this plan touches is already wired end to end; the
work here narrows two existing behaviors, it does not prove a new path. Stating this rather than
silently skipping it.

## Reversibility ratings (REVERSIBILITY_GATES on)

| # | Decision | Rating | Rationale |
|---|---|---|---|
| R1 | Clear the pending anchor from the drag lifecycle (`beginDragSelect` + `cancelDragSelect`) rather than redesigning `pendingRangeAnchor` into per-target state | reversible | Two call sites, no signature change; a later redesign replaces them without migrating any stored state |
| R2 | Fix gap 2 server-side only — no mutual-exclusion guard added to `app.js` or `advanced.js` | reversible | Honors 05-CONTEXT D-01 (server classifies, client renders). A client-side guard remains addable later if a third banner source ever appears |
| R3 | Reorder `state['recovery_required']` above `state['worker_degraded']` in `api_scan_status` | reversible | A pure statement reorder inside one function; both values are already assigned unconditionally in the same block |

No decision rates `one-way` or `costly`, so no `checkpoint:decision` is inserted. Recording the
ratings, not inventing a gate.

## Explicitly out of scope

- `05-DEBT.md` **D-DEBT-01** (light-mode `--green` at 3.30:1 fails WCAG AA). `05-VERIFICATION.md`
  records it as a disclosed, deliberately-deferred product decision about retuning an app-wide
  colour token, and explicitly "not a phase-closure blocker". Not planned here.
- `05-REVIEW.md` **IN-01** (`freshnessWord()`'s `'aging'` branch is unreachable today).
  `05-VERIFICATION.md` classifies it Info, "No functional impact… Not a gap." Not planned here.
- Everything in `deferred-items.md`.
- The five requirements `05-VERIFICATION.md` marks ✓ SATISFIED (UX-01, UX-03, UX-04, UX-05,
  OPS-06). Their coverage already exists in plans `05-01`..`05-06`; re-planning them would add
  churn and risk to green code.

## Detector results (injected plan:pre contributions)

- **API Coverage Gate:** `api-coverage.cjs --json` returns `detected: true`, on the single signal
  ``consumes `/api/scan-status` `` — Beacon's own first-party Flask route. `COVERAGE.md` already
  exists in this phase directory and already carries the reasoned declaration
  `No external API integration: …` naming that exact false-positive signal. Per the injected
  instruction it is read, not overwritten. No matrix is produced and none is owed.
- **Assumption-Delta:** `assumption-delta scan 5 --json` returns `detected: false`. The checkpoint
  does not fire; not raised with the user.
- **Schema Push Detection:** no file in scope matches any Payload / Prisma / Drizzle / Supabase /
  TypeORM pattern. Beacon persists through stdlib `sqlite3` with hand-written migrations in
  `dashboard/beacon/migrations.py`, which is not an ORM schema surface. No `[BLOCKING]` push task.
- **Security enforcement:** on, ASVS L1, block on `high`. `<threat_model>` below; no threat rates
  `high` or `critical`, so no blocking gate is inserted.

</scope_and_shape>

<multi_source_coverage_audit>

Gap-closure scope. Every source item below is either COVERED by a task in this plan or excluded on
a recorded, non-discretionary basis. No item is silently dropped and nothing is reduced to a "v1".

| Source | Item | Disposition |
|---|---|---|
| GOAL | ROADMAP SC3 — keyboard-accessible chart interactions usable at supported widths | COVERED — Task 1 (the only SC3 element `05-VERIFICATION.md` marks failed) |
| GOAL | ROADMAP SC4 — loading/empty/stale/unknown/degraded/error states meaningfully distinct in both themes | COVERED — Tasks 2 + 3 (the only SC4 element marked failed) |
| GOAL | ROADMAP SC1, SC2, SC5 | EXCLUDED — ✓ VERIFIED in `05-VERIFICATION.md`; not in gap scope |
| REQ | UX-06 — status/chart info via text, labels and keyboard rather than colour alone | COVERED — Task 1 |
| REQ | UX-07 — loading/empty/stale/unknown/degraded/error visibly distinct | COVERED — Tasks 2 + 3 |
| REQ | UX-01, UX-03, UX-04, UX-05, OPS-06 | EXCLUDED — ✓ SATISFIED by plans 05-01..05-06 per `05-VERIFICATION.md` Requirements Coverage |
| RESEARCH | "server classifies, client renders" is Beacon's established split | COVERED — Task 3 keeps the fix server-side (R2) |
| RESEARCH | Pitfall 3 — do not invent a time-threshold heuristic for `degraded` | COVERED — Task 3 adds a boolean conjunct only; no new constant, no new threshold |
| RESEARCH | OPS-06 tooling: extend the `getComputedStyle`/Playwright DOM-contract pattern, no pixel snapshots | COVERED — Tasks 1 and 2 extend existing Playwright modules; no screenshot API is introduced |
| CONTEXT | D-01 — `degraded` is server-emitted, never a client-side guess | COVERED — Task 3 narrows the server classification; Task 2 asserts the outcome at the payload and at the DOM |
| CONTEXT | D-02 — density drives disclosure | EXCLUDED — ✓ SATISFIED via 05-05; untouched here |
| CONTEXT | D-03 — main-dashboard light-mode calm | EXCLUDED — ✓ SATISFIED via 05-03; untouched here |
| VERIFICATION | Gap 1 `missing:` — call `cancelKeyboardRangeAnchor()` at the start of `beginDragSelect` (and defensively on drag commit/cancel) | COVERED — Task 1 |
| VERIFICATION | Gap 1 `missing:` — regression test: keyboard-anchor → mouse drag → plain Enter → range unchanged | COVERED — Task 1 |
| VERIFICATION | Gap 2 `missing:` — gate `worker_degraded` on `not recovery_required` in BOTH `app.py` and `diagnosis.py` | COVERED — Task 3 |
| VERIFICATION | Gap 2 `missing:` — regression test seeding a recovery marker alongside an aging (not stale) heartbeat, asserting both banners never render together | COVERED — Task 2 |

No item is MISSING. No phase split is warranted: neither fix exceeds a single agent's context
budget, no required information is absent from the source artifacts, and no dependency blocks
either fix.

</multi_source_coverage_audit>

<flagged_assumptions>

Surfaced explicitly rather than silently dropped, per the spec-less probe fallback (§A/§C). This
phase has no plain `05-SPEC.md`, so both `EDGE_ABSENT` and `PROHIB_ABSENT` apply.

## Edge-probe fallback report — no-silent-drop accounting

The deterministic edge probe returned **8 applicable, 0 resolved, 8 unresolved**. Nothing is
`resolved`, so **0 rows are lifted into `must_haves.truths`**. Accounting: 8 rows = 0 authored
+ 3 surfaced below as flagged assumptions (the two in-scope requirements) + 5 recorded as
already-covered by executed plans.

| Requirement | Category | Probe | Planner judgement |
|---|---|---|---|
| UX-06 | `empty` | "What is the result for empty, single-element, or null input?" | **FLAGGED — A-01 below.** A partial fit, not forced into a predicate. The nearest genuine reading (zero or one chart point targets) is already covered by the executed `test_empty_points_fixture_renders_no_point_targets_and_no_key_applies_a_range`. The gap-1 sequence needs *two* point targets, so the empty case is orthogonal to it. Not converted into a truth. |
| UX-06 | `encoding` | "Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form?" | **FLAGGED — A-02 below.** Judged honestly as a poor fit: UX-06 is a keyboard-accessibility requirement over SVG chart points and integer epoch timestamps. No string length or equality semantics exist anywhere on this surface. Deliberately not forced into a predicate. |
| UX-07 | `unclassified` | "unclassified — review manually" | **FLAGGED — A-03 below.** Reviewed manually; the concrete UX-07 state-distinctness content used here is taken from `05-UI-SPEC.md`'s E2/E3/E4 rows instead, which are `resolved (explicit)` and are lifted into `must_haves.truths`. |
| UX-01, UX-03, UX-04, UX-05, OPS-06 | `unclassified` ×5 | "unclassified — review manually" | **Not re-authored.** All five are ✓ SATISFIED by plans 05-01..05-06. Recorded here for the no-silent-drop count, not lifted into this plan. |

- **A-01** — The empty/single-element boundary for the keyboard range gesture is assumed already
  discharged by the executed empty-points test and is not re-asserted by this plan. If a verifier
  disagrees, the missing predicate is "with fewer than two point targets rendered, no key sequence
  applies a range" — which that existing test already asserts for the zero case.
- **A-02** — No encoding/equality predicate is authored for UX-06. Assumption: this surface has no
  string-comparison semantics to pin. The only equality that matters is integer timestamp ordering,
  and `applySelectedRange`'s existing degenerate-pair guard (`end_ts <= start_ts` returns false)
  already owns it.
- **A-03** — UX-07's unclassified probe row is treated as subsumed by the UI-SPEC E2/E3/E4
  considerations. Assumption: the safety-banner contradiction is the whole of UX-07's remaining
  gap, exactly as `05-VERIFICATION.md` scopes it.

## UI-SPEC `## UI Considerations` lift — no-silent-drop accounting

All 11 rows are `resolved (explicit)`. Scoped to UX-06 / UX-07 as instructed: **3 lifted** into
`must_haves.truths` (E7 for UX-06; E2 and E4 for UX-07), **8 recorded as already discharged** by
the executed plans and deliberately not re-lifted into a gap-closure plan — E1 (05-03), E3 (05-02,
the box-vs-inline rule, untouched by these gaps), E5 (05-05), E6 (05-04), E8 (05-04), E9 (05-04),
E10 (05-06), E11 (05-06). 3 + 8 = 11.

## Prohibition recall (§B) — no-silent-drop accounting

LLM prose pass, no engine. Stage 1 adversarial recall produced 20 raw candidates across the two
gap surfaces (silent stale-range application; contradictory safety copy). Stage 2 precision
dropped 16 as routine engineering — normal correctness and hygiene already owned by code review or
by executed plans (single-fire-per-gesture, Escape swallowing, anchor expiry-by-timeout, payload
agreement between the two surfaces, banner count, marker semantics, and the mouse/keyboard parity
prohibition already minted in `05-04-PLAN.md`). Canon-referral drop: 0 candidates were canon
security/compliance. **4 survivors**, all authored into `must_haves.prohibitions:` above,
**descriptor-less** (no `check_*` scalar) so each disposes flagged-unverified rather than being
auto-dismissed. 4 kept = 4 authored + 0 dropped-after-keeping.

</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A mouse gesture always abandons a pending keyboard range anchor (gap 1 / CR-01)</name>

  <precondition>Playwright's bundled Chromium is installed and launchable — `uv run --project dashboard python -m pytest -q tests/test_history_investigation_ui.py -k test_pending_range_anchor_is_abandoned_without_applying_anything` passes today. Halt and report if the browser cannot launch; this task cannot be verified without it.</precondition>

  <files>dashboard/advanced.js, tests/test_history_investigation_ui.py</files>

  <read_first>
    - `dashboard/advanced.js` lines ~2225-2365 — the whole drag lifecycle (`dragEscapeListener`, `endDragListeners`, `cancelDragSelect`, `beginDragSelect`, `updateDragSelect`, `applySelectedRange`, `commitDragSelect`) and the keyboard anchor gesture (`pendingRangeAnchor`, `beginKeyboardRangeAnchor`, `cancelKeyboardRangeAnchor`, `completeKeyboardRange`). Note the exact statement order inside `beginDragSelect`: the overlay is un-hidden by `overlay.hidden = false` and then positioned by `updateDragOverlayGeometry()`.
    - `dashboard/advanced.js` lines ~2655-2690 — the point-target `keydown` handler in `renderPointTargets`, the consumer whose plain-Enter branch is what fires wrongly today.
    - `tests/test_history_investigation_ui.py` lines ~1931-1975 — the `_drag_ready_page()` helper (a fixed, test-controlled 1_700_000_000 → +86_400 range with exactly two chart points and a `counters['history']` request counter).
    - `tests/test_history_investigation_ui.py` lines ~1969-2115 — `test_drag_across_chart_applies_fraction_of_rendered_span` and `test_escape_during_drag_cancels_no_range_change_and_overlay_hidden`, the mouse-drag idiom this test reuses (`page.mouse.move` / `down` / `move` / `up`, `page.expect_request` on `/api/telemetry/history`).
    - `tests/test_history_investigation_ui.py` lines ~2133-2220 — `test_keyboard_range_selection_applies_the_same_range_as_the_mouse_drag` and `test_pending_range_anchor_is_abandoned_without_applying_anything`, the keyboard idiom (`targets.nth(0).press('Shift+Enter')`, overlay visible/hidden waits, `#range-start` / `#range-end` `input_value()` comparisons). The new test belongs directly after these two, inside `class HistoryInvestigationUiTests`.
    - `05-VERIFICATION.md` gap 1 `missing:` block and `05-REVIEW.md` CR-01 — the authoritative statement of the defect and the reproduction.
  </read_first>

  <behavior>
    Write these two test expectations before changing `dashboard/advanced.js`, and run them against
    the unfixed file so both are observed RED first.

    - Test 1 (the gap-1 reproduction, the load-bearing one): on a `_drag_ready_page()` page —
      (a) press `Shift+Enter` on `#chart-cpu .hist-point-target` index 0 and wait for
      `#history-drag-overlay` to become visible, proving the anchor is armed;
      (b) perform a real mouse drag on `#chart-cpu` from 20% to 80% of its bounding box width,
      awaiting the `/api/telemetry/history` request it fires, then read `#range-start` and
      `#range-end` — these two values are the post-drag range the operator actually chose, and are
      the values the assertions compare against;
      (c) press a plain `Enter` on `#chart-cpu .hist-point-target` index 1 — a target the operator
      never anchored in this sequence;
      (d) assert `#range-start` and `#range-end` still equal the post-drag values, that
      `counters['history']` did not increment after step (b), and that `#history-drag-overlay` is
      hidden. On the unfixed file step (c) fires `completeKeyboardRange`, so the fields change and
      the counter increments — the test fails there and only there.
    - Test 2 (the cancelled-drag variant, guarding the `cancelDragSelect` half): same page —
      `Shift+Enter` on index 0, then a mouse drag on `#chart-cpu` that is abandoned with `Escape`
      before `pointerup` (the existing `test_escape_during_drag_cancels_no_range_change_and_overlay_hidden`
      idiom), then a plain `Enter` on index 1; assert the range fields are unchanged from their
      pre-sequence values and `counters['history']` is unchanged throughout.

    Both tests must be red before the fix and green after it. If either passes before the fix is
    applied, it does not reproduce the gap — rewrite it rather than accepting the pass.
  </behavior>

  <action>
    Author the two tests above as new methods on `class HistoryInvestigationUiTests` in
    `tests/test_history_investigation_ui.py`, placed immediately after
    `test_pending_range_anchor_is_abandoned_without_applying_anything`. Name them
    `test_mouse_drag_abandons_a_pending_keyboard_anchor_so_a_later_plain_enter_applies_nothing`
    and `test_cancelled_mouse_drag_also_abandons_a_pending_keyboard_anchor`. Reuse
    `self._drag_ready_page()` and the surrounding `try` / `finally: page.close()` shape every
    sibling test in this class uses. Commit them red as
    `test(05-07): reproduce the keyboard range-anchor leak across an intervening mouse drag`.

    Before committing them, gate the RED state explicitly — this command must exit 0 against the
    still-unfixed `dashboard/advanced.js`, and it is the only place Task 1's RED evidence is
    mechanically enforced (this task's `<verify>` gate is a post-fix GREEN gate, correctly, because
    unlike Task 2 this task's terminal state is green):

    `uv run --project dashboard python -m pytest -q --co tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_mouse_drag_abandons_a_pending_keyboard_anchor_so_a_later_plain_enter_applies_nothing tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_cancelled_mouse_drag_also_abandons_a_pending_keyboard_anchor && ! uv run --project dashboard python -m pytest -q tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_mouse_drag_abandons_a_pending_keyboard_anchor_so_a_later_plain_enter_applies_nothing tests/test_history_investigation_ui.py::HistoryInvestigationUiTests::test_cancelled_mouse_drag_also_abandons_a_pending_keyboard_anchor`

    The `--co` half proves both test ids exist and are importable (pytest exits 4 for an unknown
    id, so the inverted run alone would accept an unwritten test); the inverted half proves they
    fail. If it exits non-zero because the tests already pass, they do not reproduce the gap —
    rewrite them, do not proceed.

    Then fix `dashboard/advanced.js`:

    1. In `beginDragSelect`, call `cancelKeyboardRangeAnchor()` as the first statement after the
       existing non-primary-button early return (`if (event.pointerType === 'mouse' && event.button !== 0) return;`).
       It must precede the `overlay.hidden = false` assignment, because `cancelKeyboardRangeAnchor`
       hides the overlay and resets its width to `0px`; calling it later would hide the overlay the
       drag has just un-hidden. This is exactly the placement `05-REVIEW.md` CR-01 specifies.
    2. In `cancelDragSelect`, call `cancelKeyboardRangeAnchor()` so every drag exit path clears a
       pending anchor defensively — `commitDragSelect` already calls `cancelDragSelect()`
       unconditionally before its `HIST_DRAG_MIN_PIXELS` guard, `dragEscapeListener` calls it on
       `Escape`, and it is the registered `pointercancel` handler, so these two call sites together
       cover begin, commit, escape and cancel, which is the full set `05-VERIFICATION.md` names.

    `cancelKeyboardRangeAnchor` (defined just below `beginKeyboardRangeAnchor`) sets
    `pendingRangeAnchor = null` and hides the overlay, touches neither `dragState` nor the pointer
    listeners, and is documented as safe to call whether or not an anchor is held — so no guard is
    needed at either call site and no re-entrancy is possible.

    Do not restructure `pendingRangeAnchor` into per-target state, do not add a timeout-based
    anchor expiry, and do not change the point-target `keydown` handler — the plain-Enter branch is
    correct once the anchor can no longer be stale, and narrowing it would remove the completion
    path the gesture depends on (prohibition 2).

    Update the comment immediately above `beginDragSelect` so a reader learns that the two gestures
    share `#history-drag-overlay` and that a mouse gesture supersedes a pending keyboard anchor;
    keep the existing `05-04 Task 3 (R-03 closed)` marker intact, because
    `test_drag_entry_point_documents_the_keyboard_equivalent_that_closed_the_debt` asserts on it.
  </action>

  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_history_investigation_ui.py -k "keyboard_anchor or range_anchor or drag"</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_history_investigation_ui.py` defines `test_mouse_drag_abandons_a_pending_keyboard_anchor_so_a_later_plain_enter_applies_nothing` and `test_cancelled_mouse_drag_also_abandons_a_pending_keyboard_anchor` on `class HistoryInvestigationUiTests`.
    - Both new tests exercise the full three-step sequence: the source of each contains a `Shift+Enter` press, a `page.mouse.down()` and `page.mouse.up()` (or an `Escape`-abandoned drag) between it and the final press, and a plain `Enter` press afterwards. A test that asserts only the post-condition without the intervening mouse gesture does not satisfy this criterion.
    - Both new tests were observed failing before the `dashboard/advanced.js` change and are committed in a `test(05-07): …` commit that precedes the fix commit — `git log --oneline` shows the test commit first.
    - `uv run --project dashboard python -m pytest -q tests/test_history_investigation_ui.py` exits 0 with zero failures after the fix.
    - Source assertion — this exits 0:
      `python3 -c "import pathlib; s=pathlib.Path('dashboard/advanced.js').read_text(); c={x.split('(')[0]:x for x in s.split(chr(10)+'  function ')}; body=lambda k: chr(10).join(l for l in c[k].split(chr(10)+'  }')[0].split(chr(10)) if not l.strip().startswith('//')); b=body('beginDragSelect'); d=body('cancelDragSelect'); assert 'cancelKeyboardRangeAnchor(' in b, 'beginDragSelect body'; assert b.index('cancelKeyboardRangeAnchor(') < b.index('overlay.hidden = false'), 'order'; assert 'cancelKeyboardRangeAnchor(' in d, 'cancelDragSelect body'"`

      Read `body(k)` before changing it — it is the load-bearing part, and both halves use the
      same one idiom deliberately. Splitting `advanced.js` on `chr(10)+'  function '` gives each
      chunk the comment block belonging to the *next* function, so a raw-chunk assertion is
      satisfiable by prose alone: the 05-04 comment this task's own action tells you to rewrite
      sits at the tail of the `cancelDragSelect` chunk, and the comment preceding
      `updateDragSelect` sits at the tail of the `beginDragSelect` chunk. `body(k)` therefore
      (a) truncates the chunk at the function's closing `chr(10)+'  }'`, discarding any trailing
      comment, and (b) drops every `//` line, so a comment *inside* the body cannot satisfy it
      either. Only a real call in executable code passes. Verified against the current tree: the
      assertion fails pre-fix, fails when only the comment is rewritten to name
      `cancelKeyboardRangeAnchor()`, fails when the name is added as an in-body comment, and
      passes only once both functions actually call it.
    - `pendingRangeAnchor` remains a single module-level variable — `grep -c "let pendingRangeAnchor" dashboard/advanced.js` returns 1.
    - The four pre-existing gesture tests still pass by name: `test_keyboard_range_selection_applies_the_same_range_as_the_mouse_drag`, `test_pending_range_anchor_is_abandoned_without_applying_anything`, `test_drag_across_chart_applies_fraction_of_rendered_span`, `test_escape_during_drag_cancels_no_range_change_and_overlay_hidden`.
    - `grep -c "05-04 Task 3 (R-03 closed)" dashboard/advanced.js` returns at least 1, so `test_drag_entry_point_documents_the_keyboard_equivalent_that_closed_the_debt` still passes.
  </acceptance_criteria>

  <done>
    Arming a keyboard range anchor and then performing any mouse drag leaves no pending anchor: a
    subsequent plain Enter on any chart point applies no range, issues no history request, and
    leaves the range the operator actually selected untouched. The keyboard gesture itself is
    unchanged and still applies the identical range the equivalent mouse drag applies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reproduce the contradictory-banner combination — three failing regression tests (gap 2 / WR-01, RED)</name>

  <precondition>`tests/fixtures/legacy/operator/production.db` exists and Playwright's bundled Chromium is launchable — `uv run --project dashboard python -m pytest -q tests/test_ui_safety_integration.py` passes today. Halt and report if either is unavailable.</precondition>

  <files>tests/test_ui_safety_integration.py, tests/test_api_and_auth.py, tests/test_advanced_diagnosis_api.py</files>

  <read_first>
    - `05-VERIFICATION.md` gap 2 — the `reason` and `missing:` block, which is the authoritative scope of what must be reproduced.
    - `dashboard/app.py` lines ~3033-3052 — `api_scan_status`: `worker_stale`, `state['worker_freshness']`, the A-04 comment, `state['worker_degraded']` at line 3047, and `state['recovery_required']` at line 3051. Note that `recovery_required` is assigned *after* `worker_degraded` today.
    - `dashboard/beacon/diagnosis.py` lines ~660-682 — `get_current_diagnosis`: `recovery_required = (Path(db_path).parent / RECOVERY_MARKER).exists()` and the `safety` dict's `worker_degraded` entry.
    - `dashboard/beacon/migrations.py` line 29 — `RECOVERY_MARKER = 'recovery-required.json'`, the file name to seed.
    - `dashboard/app.js` lines ~144-152 (`updateScanStatus`) and `dashboard/advanced.js` `renderSafety` (~line 3175) — the two independent banner toggles. Read them to confirm no client change is needed; do not modify either file.
    - `tests/test_ui_safety_integration.py` — the whole module: `setUp`'s `tempfile.TemporaryDirectory` + `DB_PATH` env + `importlib.reload` pattern, and `test_degraded_banner_reads_identically_in_both_themes_on_both_documents` (lines ~163-227), whose `seed_aging_heartbeat()` closure, `cadence = worker_heartbeat_cadence_seconds(SETTINGS)` / `age = 2 * cadence` derivation, its `assertGreater(age, cadence)` / `assertLessEqual(age, SETTINGS.worker_ready_seconds)` bounds, and its `for theme in ('dark','light'): for path in ('/','/advanced')` subtest loop are the exact idiom the new test reuses.
    - `tests/test_api_and_auth.py` lines ~305-350 — `test_scan_status_never_reports_degraded_and_stale_together` on `class ApiAndAuthTests`: the `load_app(...)` / `cleanup_db(db_path)` pattern, the `os.environ` restore `addCleanup` block, and the direct `runtime_state` heartbeat insert.
    - `tests/test_advanced_diagnosis_api.py` lines ~152-195 — `test_worker_freshness_is_one_shared_classification_for_both_surfaces` on `class AdvancedDiagnosisApiTests`: `load_app` + `self._freeze_clock(...)` + `client.get('/api/advanced/current')` and the existing `assertFalse(worker_stale and worker_degraded)` exclusivity idiom.
    - `tests/helpers.py` lines ~51-101 — `load_app` (each test DB gets its own directory, so the recovery marker written beside it is isolated) and `cleanup_db`.
  </read_first>

  <action>
    Author three regression tests. Every one of them must FAIL against the current unfixed
    `dashboard/app.py` and `dashboard/beacon/diagnosis.py`. Run them and record the failure output
    before Task 3 touches any production file. Commit them red as
    `test(05-07): reproduce the simultaneous degraded and recovery banners`.

    **Test A — the rendered page, both documents, both themes** (`tests/test_ui_safety_integration.py`,
    `class UiSafetyIntegrationTests`), named
    `test_recovery_marker_with_aging_heartbeat_renders_only_the_recovery_banner_on_both_documents`.
    This is the test `05-VERIFICATION.md` gap 2 names, and it must seed real state, not stub a
    payload. Model it on `test_degraded_banner_reads_identically_in_both_themes_on_both_documents`:
    - Derive `cadence` from `self.appmod.beacon_diagnosis.worker_heartbeat_cadence_seconds(self.appmod.SETTINGS)`
      and use `age = 2 * cadence`, keeping that test's two self-checking bounds
      (`assertGreater(age, cadence)` and `assertLessEqual(age, self.appmod.SETTINGS.worker_ready_seconds)`)
      so the seeded heartbeat is provably in the `aging` tier and provably not `stale`. Do not
      hard-code a second count.
    - Re-seed the heartbeat against a freshly read `int(time.time())` immediately before each
      navigation, for the same real-wall-clock-drift reason that test documents, and assert
      `worker_freshness(...)['state'] == 'aging'` after seeding.
    - Write a real recovery marker file at
      `pathlib.Path(self.tmpdir.name) / self.appmod.RECOVERY_MARKER` — the same directory
      `DB_PATH`'s parent resolves to, and the same name `dashboard/beacon/migrations.py` defines.
      Give it plausible JSON content in the shape `_write_recovery_marker` produces
      (`failed_target_version`, `reason_class`, `backup_catalog_id`, `timestamp`,
      `restore_in_progress`). The file lives in the per-test `TemporaryDirectory`, so `tearDown`
      already removes it; do not write anywhere outside it.
    - Loop `for theme in ('dark', 'light'): for path in ('/', '/advanced'):` with a `subTest`,
      seeding the aging heartbeat before each navigation and setting the light theme through the
      existing `page.add_init_script("localStorage.setItem('beacon-theme', 'light');")` idiom.
    - Wait for `#recovery-warning` to become visible, then assert `#degraded-warning` is not
      visible and `#worker-warning` is not visible — exactly one worker safety banner. Also assert
      `#recovery-warning`'s text still reads as the paused-monitoring copy it reads today, so a
      future change cannot satisfy this test by softening that banner (prohibition 4).
    - Before the fix this fails on the `#degraded-warning` assertion for every one of the four
      subtests. Confirm that is where it fails, on both `/` and `/advanced` — that is what proves
      the test reaches both `app.py` and `diagnosis.py`.

    **Test B — the `/api/scan-status` payload** (`tests/test_api_and_auth.py`, `class ApiAndAuthTests`),
    named `test_scan_status_never_reports_degraded_while_recovery_is_required`. Mirror
    `test_scan_status_never_reports_degraded_and_stale_together`, including its `os.environ`
    restore `addCleanup` block. Use `load_app({'WORKER_READY_SECONDS': '30'})`.
    Set only that one variable, and set it for this reason: the J1 heartbeat cadence is the fixed
    literal `('seconds', 5)` in `WORKER_CALLBACK_INVENTORY` (`dashboard/beacon/worker_main.py:84`),
    so `cadence == 5` and the `aging` band is `4 * cadence == 20`; `WORKER_READY_SECONDS='30'`
    puts the readiness cutoff above that whole band, so the seeded 10s age is unambiguously
    `aging` and unambiguously not past the cutoff. Do **not** pass `METRIC_SAMPLE_SECONDS` here —
    it is a no-op for this cadence and passing it teaches a false causal model of the config
    surface. `callback_schedule_evidence` substitutes `settings.metric_sample_seconds` **only**
    for J2 (`dashboard/beacon/diagnosis.py:343-344`); `worker_heartbeat_cadence_seconds`
    (`diagnosis.py:121`) resolves J1, which never reads it. This is the same finding
    `05-DEBT.md` §2 W-6 already records ("the J1 heartbeat cadence is the literal `('seconds', 5)`;
    only `WORKER_READY_SECONDS` is configurable"), and it is confirmed by live measurement:
    `worker_heartbeat_cadence_seconds` returns 5 for `METRIC_SAMPLE_SECONDS` of unset, 5, 60 and
    900 alike. Insert a `worker_heartbeat` runtime_state row at
    `int(time.time()) - 2 * cadence`, write the recovery marker beside the test database
    (`os.path.dirname(db_path)` joined with `appmod.RECOVERY_MARKER`), then assert on
    `client.get('/api/scan-status').get_json()`: `worker_freshness['state'] == 'aging'`,
    `worker_stale is False`, `recovery_required is True`, and `worker_degraded is False`. Also
    assert the invariant directly: not (`recovery_required` and `worker_degraded`). Remove the
    marker in the `finally` block before `cleanup_db(db_path)`.

    **Test C — the `/api/advanced/current` safety block** (`tests/test_advanced_diagnosis_api.py`,
    `class AdvancedDiagnosisApiTests`), named
    `test_safety_never_reports_degraded_while_recovery_is_required`. Mirror
    `test_worker_freshness_is_one_shared_classification_for_both_surfaces`, but call plain
    `load_app()` with no env override. That sibling passes `{'METRIC_SAMPLE_SECONDS': '5'}`; do not
    copy it. For the same reason as Test B it is a no-op on the J1 cadence, which is the fixed
    literal 5 (`worker_main.py:84`), and this surface needs nothing else: `get_current_diagnosis`'s
    `safety['worker_degraded']` is derived from the freshness tier alone and never consults
    `worker_ready_seconds`, so `age = 2 * cadence == 10` lands in the `aging` band `(5, 20]` under
    the defaults. Leave that pre-existing sibling test unmodified — it is out of scope here.
    Then insert the heartbeat row at a fixed `now`, `self._freeze_clock(now + 2 * cadence)`, write the
    recovery marker beside the test database, then assert on the payload:
    `pipeline['worker']['freshness']['state'] == 'aging'`, `safety['worker_stale'] is False`,
    `safety['recovery_required'] is True`, `safety['worker_degraded'] is False`, and the direct
    invariant not (`recovery_required` and `worker_degraded`). Clean up the marker in `finally`.

    Do not modify `dashboard/app.py`, `dashboard/beacon/diagnosis.py`, `dashboard/app.js` or
    `dashboard/advanced.js` in this task — Task 3 owns the fix. Assert only through DOM queries,
    `getComputedStyle` and text content, exactly as every sibling test in these modules does;
    Playwright's pixel-snapshot APIs stay out of the repository entirely, because OPS-06 requires
    DOM/computed-style contracts and `05-VERIFICATION.md` pins the zero-pixel-snapshot property as
    verified evidence.
  </action>

  <verify>
    <automated>uv run --project dashboard python -m pytest -q --co tests/test_ui_safety_integration.py::UiSafetyIntegrationTests::test_recovery_marker_with_aging_heartbeat_renders_only_the_recovery_banner_on_both_documents tests/test_api_and_auth.py::ApiAndAuthTests::test_scan_status_never_reports_degraded_while_recovery_is_required tests/test_advanced_diagnosis_api.py::AdvancedDiagnosisApiTests::test_safety_never_reports_degraded_while_recovery_is_required && ! uv run --project dashboard python -m pytest -q tests/test_ui_safety_integration.py::UiSafetyIntegrationTests::test_recovery_marker_with_aging_heartbeat_renders_only_the_recovery_banner_on_both_documents tests/test_api_and_auth.py::ApiAndAuthTests::test_scan_status_never_reports_degraded_while_recovery_is_required tests/test_advanced_diagnosis_api.py::AdvancedDiagnosisApiTests::test_safety_never_reports_degraded_while_recovery_is_required</automated>
  </verify>

  <verify_rationale>
    This task's terminal state is RED by design — its `<done>` requires all three tests to fail —
    so a plain `pytest` invocation would be a gate the task must *violate* to succeed, and an
    executor treating non-zero as failure would pull Task 3's fix forward, collapsing Tasks 2 and
    3 and destroying the RED-before-GREEN evidence `<verification>` step 1 and `<success_criteria>`
    both depend on. The gate is therefore two commands joined by `&&`, and both halves are needed:

    - `pytest --co` (collection only) proves all three test ids **exist and are importable**, and
      exits 0. It launches no browser and takes ~0.1s.
    - `! pytest` proves those same three tests **fail**, and exits 0 only when they do.

    `!` alone is not sufficient and must not be used on its own: pytest exits 4 for an unknown
    test id, so a typo'd id, an unwritten test or an import error would invert to 0 and let an
    executor satisfy the gate **without writing the tests at all**. The `--co` half closes that
    masquerade. Smoke-tested against this tree: three tests that exist and fail → exit 0; a typo'd
    id → exit 4; three tests that exist and pass (the Tasks-2-and-3-collapsed case) → exit 1.

    Task 1 is deliberately *not* given this treatment and its plain `pytest` gate is correct: Task
    1 is a complete RED→GREEN cycle whose terminal state is green, so its verify command passing
    is the right success condition. Task 1's intermediate RED observation is gated inside its
    `<action>` instead.
  </verify_rationale>

  <acceptance_criteria>
    - All three named tests exist and are collected: the `<automated>` command above collects exactly 3 test ids.
    - All three FAIL when run against the tree as it stands at the start of this task, and the recorded failure for Test A is on the `#degraded-warning` visibility assertion (not on setup, navigation, or a timeout). Capture the failure output in the plan SUMMARY.
    - Test A seeds a real on-disk marker: its source writes a file whose name comes from `self.appmod.RECOVERY_MARKER` (not a hard-coded literal) into `self.tmpdir.name`, and it seeds an aging heartbeat derived from `worker_heartbeat_cadence_seconds(...)` (not a hard-coded second count). A test that stubs `/api/scan-status` or `/api/advanced/current` instead of seeding real state does not satisfy this criterion.
    - Test A covers all four combinations — its source contains a loop over both `'dark'` and `'light'` and over both `'/'` and `'/advanced'`, each under a `subTest`.
    - Tests B and C each assert all four facts (`aging` freshness, `worker_stale` false, `recovery_required` true, `worker_degraded` false) plus the direct exclusivity invariant.
    - `git diff --name-only` for this task's commit lists only the three test files — `dashboard/app.py`, `dashboard/beacon/diagnosis.py`, `dashboard/app.js` and `dashboard/advanced.js` are untouched.
    - `grep -rho 'to_have_screenshot\|page.screenshot' tests/` still returns nothing.
    - The three tests are committed in a `test(05-07): …` commit that precedes Task 3's fix commit.
  </acceptance_criteria>

  <done>
    The contradictory-banner combination `05-VERIFICATION.md` describes is reproduced by automated
    tests at both the payload layer and the rendered-page layer, for both documents and both
    themes, and all three tests fail against the unfixed code for the right reason.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate worker_degraded on recovery_required in both server surfaces (gap 2 / WR-01, GREEN)</name>

  <files>dashboard/app.py, dashboard/beacon/diagnosis.py</files>

  <read_first>
    - `dashboard/app.py` lines ~3033-3052 — the exact statement order in `api_scan_status`. `state['worker_degraded']` is assigned at line 3047; `state['recovery_required']` at line 3051. Read the A-04 comment above `worker_degraded` (it explains why the `not worker_stale` conjunct exists) before editing it.
    - `dashboard/beacon/diagnosis.py` lines ~660-682 — `get_current_diagnosis`, where the local `recovery_required` is already computed at line 666, above the returned dict, so no reorder is needed on this side.
    - `05-REVIEW.md` WR-01's **Fix** paragraph — it specifies both the conjunct and the reorder.
    - `tests/test_api_and_auth.py::ApiAndAuthTests::test_scan_status_never_reports_degraded_and_stale_together` and `tests/test_advanced_diagnosis_api.py::AdvancedDiagnosisApiTests::test_worker_freshness_is_one_shared_classification_for_both_surfaces` — the two executed tests that pin the existing `not worker_stale` behavior and must keep passing unchanged.
    - The three tests authored in Task 2.
  </read_first>

  <behavior>
    After this task, and with no change to any other input:
    - Recovery marker present + heartbeat aging → `worker_degraded` is `false` and
      `recovery_required` is `true`, on both `/api/scan-status` and `/api/advanced/current`'s
      `safety` block. (Task 2's tests B and C.)
    - Recovery marker absent + heartbeat aging → `worker_degraded` stays `true`, exactly as today.
      (The executed `test_degraded_banner_reads_identically_in_both_themes_on_both_documents` and
      `test_worker_freshness_is_one_shared_classification_for_both_surfaces` continue to pass
      untouched — this is the regression guard proving the fix narrows the state rather than
      disabling it.)
    - Heartbeat stale → `worker_degraded` stays `false`, exactly as today.
    - No heartbeat row at all → freshness `unknown`, `worker_degraded` false, exactly as today.
  </behavior>

  <action>
    Apply the guard `05-VERIFICATION.md` gap 2 specifies, in both files. Two edits, no new
    constants, no new helper, no threshold.

    **`dashboard/app.py`, `api_scan_status`:**
    1. Move the `state['recovery_required'] = worker_stale or (Path(DB_PATH).parent / RECOVERY_MARKER).is_file()`
       statement so it is assigned strictly above the `state['worker_degraded']` assignment. It has
       no dependency on any statement between the two positions, so this is a pure reorder (R3).
    2. Change the `state['worker_degraded']` expression to require `not state['recovery_required']`
       in addition to the existing `not worker_stale` and `state['worker_freshness']['state'] == 'aging'`
       conjuncts. All three conjuncts must be present in the final expression.
    3. Extend the existing A-04 comment above the assignment so it also records why
       `recovery_required` is a conjunct: an on-disk recovery marker is computed independently of
       heartbeat freshness, so without this conjunct the page could assert both
       "monitoring is paused" and "monitoring continues" about the same worker at the same instant.
       Keep the existing A-04 reasoning about `WORKER_READY_SECONDS` versus the fixed 4x-cadence
       boundary intact — it is still true and still load-bearing.

    **`dashboard/beacon/diagnosis.py`, `get_current_diagnosis`:**
    4. Change the `safety` dict's `'worker_degraded'` value to require `not recovery_required` in
       addition to the existing `pipeline['worker']['freshness']['state'] == 'aging'`. The local
       `recovery_required` is already in scope at that point, so no reorder is needed here.
    5. Add a one-line comment beside it pointing at the same reason, so the two surfaces' guards
       read as one decision rather than two coincidences.

    Do not add any mutual-exclusion logic to `dashboard/app.js` or `dashboard/advanced.js`. The
    banner toggles there are correct once the two server booleans are mutually exclusive, and
    05-CONTEXT D-01 makes the server the only place this classification may be decided (R2,
    prohibition 4). Do not suppress, soften, retext or remove the recovery banner — it is the
    reading that wins, and the degraded reading is the one that yields (prohibition 3).

    Run Task 2's three tests, then the full phase-relevant suite, and confirm the pre-existing
    aging-without-marker behavior is unchanged. Commit as
    `fix(05-07): gate worker_degraded on recovery_required in both server surfaces`.
  </action>

  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_api_and_auth.py tests/test_advanced_diagnosis_api.py tests/test_ui_safety_integration.py</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion for `dashboard/app.py` — this exits 0:
      `python3 -c "import re,pathlib; s=pathlib.Path('dashboard/app.py').read_text(); m=re.search(r\"state\[.worker_degraded.\] = (.+)\", s); assert m, 'assignment not found'; e=m.group(1); assert 'recovery_required' in e and 'worker_stale' in e and 'aging' in e, e"`
    - Ordering assertion for `dashboard/app.py` — this exits 0:
      `python3 -c "import re,pathlib; s=pathlib.Path('dashboard/app.py').read_text(); r=re.search(r\"state\[.recovery_required.\] = \", s).start(); d=re.search(r\"state\[.worker_degraded.\] = \", s).start(); assert r < d, (r, d)"`
    - Source assertion for `dashboard/beacon/diagnosis.py` — this exits 0:
      `python3 -c "import re,pathlib; s=pathlib.Path('dashboard/beacon/diagnosis.py').read_text(); m=re.search(r\"'worker_degraded': (.+),\", s); assert m, 'entry not found'; e=m.group(1); assert 'recovery_required' in e and 'aging' in e, e"`
    - The three tests authored in Task 2 now pass: `uv run --project dashboard python -m pytest -q tests/test_ui_safety_integration.py::UiSafetyIntegrationTests::test_recovery_marker_with_aging_heartbeat_renders_only_the_recovery_banner_on_both_documents tests/test_api_and_auth.py::ApiAndAuthTests::test_scan_status_never_reports_degraded_while_recovery_is_required tests/test_advanced_diagnosis_api.py::AdvancedDiagnosisApiTests::test_safety_never_reports_degraded_while_recovery_is_required` exits 0.
    - No-regression assertion: `uv run --project dashboard python -m pytest -q tests/test_ui_safety_integration.py::UiSafetyIntegrationTests::test_degraded_banner_reads_identically_in_both_themes_on_both_documents tests/test_advanced_diagnosis_api.py::AdvancedDiagnosisApiTests::test_worker_freshness_is_one_shared_classification_for_both_surfaces tests/test_api_and_auth.py::ApiAndAuthTests::test_scan_status_never_reports_degraded_and_stale_together` exits 0 with those three test bodies unmodified by this task.
    - `git diff --name-only` for this task's commit lists exactly `dashboard/app.py` and `dashboard/beacon/diagnosis.py` — `dashboard/app.js` and `dashboard/advanced.js` are untouched.
    - No new module-level constant is introduced: `git diff` for this task shows no added line matching `^\+[A-Z_]+ = `.
    - The recovery banner copy is unchanged: `grep -c "Upgrade recovery is required" dashboard/index.html dashboard/advanced.html` returns 1 for each file.
  </acceptance_criteria>

  <done>
    `worker_degraded` is false whenever `recovery_required` is true on both server surfaces, the
    operator can never see the reassuring degraded banner beside the paused-monitoring recovery
    banner, and the ordinary aging-without-a-marker degraded state behaves exactly as it did
    before.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>

Every symbol this plan creates or changes. No new files.

**Modified functions (production code):**

| Symbol | File | Change |
|---|---|---|
| `beginDragSelect` | `dashboard/advanced.js` | Adds a `cancelKeyboardRangeAnchor()` call before the overlay is un-hidden |
| `cancelDragSelect` | `dashboard/advanced.js` | Adds a defensive `cancelKeyboardRangeAnchor()` call |
| `api_scan_status` | `dashboard/app.py` | `state['recovery_required']` reordered above `state['worker_degraded']`; `worker_degraded` expression gains a `not state['recovery_required']` conjunct |
| `get_current_diagnosis` | `dashboard/beacon/diagnosis.py` | `safety['worker_degraded']` gains a `not recovery_required` conjunct |

**New test methods (all new symbols — none of these exist in the tree today):**

| Test method | File | Class |
|---|---|---|
| `test_mouse_drag_abandons_a_pending_keyboard_anchor_so_a_later_plain_enter_applies_nothing` | `tests/test_history_investigation_ui.py` | `HistoryInvestigationUiTests` |
| `test_cancelled_mouse_drag_also_abandons_a_pending_keyboard_anchor` | `tests/test_history_investigation_ui.py` | `HistoryInvestigationUiTests` |
| `test_recovery_marker_with_aging_heartbeat_renders_only_the_recovery_banner_on_both_documents` | `tests/test_ui_safety_integration.py` | `UiSafetyIntegrationTests` |
| `test_scan_status_never_reports_degraded_while_recovery_is_required` | `tests/test_api_and_auth.py` | `ApiAndAuthTests` |
| `test_safety_never_reports_degraded_while_recovery_is_required` | `tests/test_advanced_diagnosis_api.py` | `AdvancedDiagnosisApiTests` |

**Referenced but NOT created or modified by this plan** (existing symbols, read-only):
`cancelKeyboardRangeAnchor`, `beginKeyboardRangeAnchor`, `completeKeyboardRange`,
`pendingRangeAnchor`, `applySelectedRange`, `commitDragSelect`, `updateDragSelect`,
`dragEscapeListener`, `renderPointTargets`, `renderSafety`, `updateScanStatus`,
`worker_freshness`, `worker_heartbeat_cadence_seconds`, `RECOVERY_MARKER`, `load_app`,
`cleanup_db`, `_drag_ready_page`, `_freeze_clock`.

</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Flask `/api/scan-status` | Untrusted client polls a safety-state payload; the response drives which banner an operator acts on |
| browser → Flask `/api/advanced/current` | Same, for the advanced workspace's `safety` block |
| filesystem → server classification | The presence of `recovery-required.json` beside `DB_PATH` is durable evidence that now also suppresses a second banner |
| test harness → real Flask app + real SQLite | Task 2's tests reload `dashboard.app` against a temp DB and write a marker file into a temp directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-01 | Spoofing | `dashboard/app.py` `state['recovery_required']` via `Path(DB_PATH).parent / RECOVERY_MARKER).is_file()` | low | accept | Pre-existing and unchanged by this plan: `is_file()` follows a symlink, so a symlink to a regular file inside the data volume reads as a marker. The privileged path (`dashboard/beacon/recovery.py::_read_regular_marker`) already hardens the *authorization* read with `lstat` + `O_NOFOLLOW`; this read only decides banner visibility and now only ever suppresses a reassuring banner in favour of an alarming one, so the failure direction is safe. The data volume is the only writable mount and the threat requires local write access already sufficient to alter the DB |
| T-05-07-02 | Tampering | `dashboard/app.py` `api_scan_status` statement reordering | low | mitigate | Task 3 pins the ordering with an executable source assertion (`recovery_required` assigned strictly above `worker_degraded`) so a reference-before-assignment cannot ship; the three payload tests exercise the endpoint end to end, and `test_scan_status_never_reports_degraded_and_stale_together` re-runs unmodified as the regression guard |
| T-05-07-03 | Denial of Service | Task 2's recovery-marker seeding in `tests/test_ui_safety_integration.py` | low | mitigate | The marker is written only into the per-test `tempfile.TemporaryDirectory` that `setUp` creates and `tearDown` removes, and into `load_app`'s per-test `beacon-test-*` directory for tests B and C, both removed in `finally`. Task 2's acceptance criteria forbid writing outside the temp directory, so a stray marker can never survive into the real `/data` volume or a later test module |
| T-05-07-04 | Repudiation | `dashboard/advanced.js` keyboard range anchor (the gap-1 defect) | medium | mitigate | This is the defect being closed. Today a range the operator did not select is attributed to a keypress they did make, with no overlay and no Shift held — the operator cannot tell the system acted on stale state. Task 1 clears the anchor on every mouse-gesture entry and exit path and pins it with a mixed-gesture regression test |
| T-05-07-05 | Information Disclosure | `#recovery-warning` / `#degraded-warning` copy rendered to the browser | low | accept | The banners disclose only operational state (recovery pending, heartbeat aging) already exposed by the two existing endpoints. No new field is added to any payload by this plan, and the recovery banner's text is unchanged (pinned by a Task 3 acceptance criterion) |
| T-05-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan — every file touched is existing first-party source or an existing test module, and no dependency is added. `05-RESEARCH.md`'s Package Legitimacy Audit records the phase as "Not applicable… No new third-party package in any ecosystem", and `05-UI-SPEC.md`'s Registry Safety records registry `none`. No `[ASSUMED]`/`[SUS]` package exists, so no blocking legitimacy checkpoint is owed |

No threat rates `high` or `critical`, so the configured `security_block_on: high` threshold inserts
no blocking checkpoint. ASVS L1.
</threat_model>

<verification>

Run in this order.

1. **Reproduction is real (the load-bearing check).** `git log --oneline` shows two `test(05-07): …`
   commits, each landing before the fix commit it belongs to, and the plan SUMMARY records the
   observed failure output for all five new tests against the unfixed code. A test that was written
   after its fix, or that passed before its fix, does not close its gap and must be rewritten.

2. **Gap 1 closed.**
   `uv run --project dashboard python -m pytest -q tests/test_history_investigation_ui.py` exits 0.

3. **Gap 2 closed.**
   `uv run --project dashboard python -m pytest -q tests/test_ui_safety_integration.py tests/test_api_and_auth.py tests/test_advanced_diagnosis_api.py` exits 0.

4. **No regression across the phase's own surface.**
   `uv run --project dashboard python -m pytest -q tests/test_ui_safety_integration.py tests/test_advanced_ui.py tests/test_theme_parity_ui.py tests/test_ui_contract.py tests/test_ui_states.py tests/test_history_investigation_ui.py` matches or exceeds the 277 passed / 140 subtests / 0 failures that `05-VERIFICATION.md` recorded, with zero failures.

5. **No regression project-wide.**
   `uv run --project dashboard python -m pytest -q` exits 0 (the baseline `05-VERIFICATION.md`
   recorded is 771 passed, 548 subtests, 0 failures).

6. **OPS-06 property preserved.**
   `grep -rho 'to_have_screenshot\|page.screenshot' tests/` returns nothing.

7. **Scope fence held.** `git diff --stat` across this plan's commits touches exactly the seven
   files in `files_modified` and no others. In particular `dashboard/style.css`,
   `dashboard/advanced.css`, `dashboard/index.html`, `dashboard/advanced.html`,
   `dashboard/app.js` and `dashboard/beacon/recovery.py` are untouched — D-DEBT-01 and IN-01 are
   not addressed here by design.

8. **Debt-marker scan.** `grep -n -E "TBD|FIXME|XXX" dashboard/advanced.js dashboard/app.py dashboard/beacon/diagnosis.py tests/test_history_investigation_ui.py tests/test_api_and_auth.py tests/test_advanced_diagnosis_api.py tests/test_ui_safety_integration.py` returns nothing, matching the phase's existing clean state.

No `checkpoint:human-verify` task is created: `workflow.human_verify_mode` is `end-of-phase`, and
`05-VERIFICATION.md` explicitly records "None required to reach a status determination — both open
issues are code-level defects with a clear, deterministic reproduction and fix, not matters of
visual/UX judgment." Step 4's browser tests assert the rendered banner state directly in both
themes on both documents, which is the same evidence a human check would gather.

</verification>

<success_criteria>

- Both `05-VERIFICATION.md` gaps are closed, and each gap's two `missing:` items — the code fix
  **and** the regression test — are both delivered.
- Each regression test exercises the full triggering sequence, not merely the post-condition: gap
  1's tests perform anchor → mouse gesture → plain Enter; gap 2's page test seeds a real
  on-disk recovery marker beside a real aging-not-stale heartbeat.
- Every new test was observed failing against the unfixed code, in a commit preceding its fix.
- UX-06 and UX-07 are no longer blocked: SC3's keyboard chart interaction is safe, and SC4's
  safety-banner states are mutually exclusive and meaningfully distinct in both themes.
- The five satisfied requirements and their code are untouched; the project-wide suite is green.
- D-DEBT-01, IN-01 and `deferred-items.md` remain unaddressed, as scoped.

</success_criteria>

<output>
Create `.planning/phases/05-theme-parity-analytics-experience/05-07-SUMMARY.md` when done.

The SUMMARY must record, for each of the five new tests, the observed pre-fix failure output — that
evidence is what distinguishes a closed gap from a test written to match code that was already
changed.
</output>
