---
context: phase
phase: 06-workload-resilience-pi-acceptance
plan: 06-12
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-02T14:15:18.907Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Full-suite runs under concurrent worktree load | While two executor agents ran in parallel worktrees, full-suite runs reported 15–16 failures, every one in `test_advanced_ui.py` / `test_history_investigation_ui.py` / `test_ui_states.py`. Each passed when re-run in isolation. Playwright/Chromium contention between sibling agents, not a regression — but it makes the `837 passed / 561 subtests` floor unreadable while a sibling is running. | advisory | Run the full suite only on a quiet tree — after every agent in the wave has returned and its worktree is merged. Do not accept or reject the suite floor from a run taken during parallel execution. |

_No `blocking` rows. Resumption is not gated by an understanding check._

<current_state>
Phase 6, second gap-closure round. `/gsd-execute-phase 6` was mid-**Wave 1 of 3** when work paused.

- **06-11 — COMPLETE.** 6 commits on `worktree-agent-a681a3b495988ef52`, 3/3 tasks, SUMMARY committed.
- **06-12 — IN FLIGHT.** 3 commits on `worktree-agent-ac95044107eeefc28` (Tasks 1 and 2 done). Task 3's `06-PROFILE.md` (20k) exists in the worktree but is **UNCOMMITTED**. The agent was still running at pause time.
- **Neither branch has been merged into `main`.** `main` is at `f672832`, which is also both worktrees' expected base.

`.planning/STATE.md` was modified by `state.begin-phase` and is committed as part of this pause.
</current_state>

<completed_work>

Planning (all committed):
- `d316090` — plans 06-11..06-14 created
- `a58f02a` — revision pass 1, closed plan-checker warnings W1–W7
- `aedd027` — revision pass 2, closed W8–W9
- `f672832` — STATE/ROADMAP recorded planning completion
- Plan checker returned `## VERIFICATION PASSED`, 0 blockers 0 warnings, go/no-go **GO**

Execution:
- Task 06-11 T1/T2/T3 + SUMMARY — done (`6382726`, `87bb934`, `3455434`, `aa354c2`, `bc44c6e`, `23b830c`)
- Task 06-12 T1 + T2 — done (`cb4d845`, `1739a18`, `29b0ed7`)
- Task 06-12 T3 — in progress; report written, not committed
</completed_work>

<remaining_work>

1. Confirm 06-12 finished (agent id `ac95044107eeefc28`). **Inspect its worktree for uncommitted work before removing anything.**
2. Merge `worktree-agent-a681a3b495988ef52` and `worktree-agent-ac95044107eeefc28` into `main`; remove both worktrees (currently `locked`).
3. Re-run `uv run --project dashboard python -m pytest -q` on a quiet tree and record the real number against the 837/561 floor.
4. Wave 2 — dispatch `06-13`. Its Task 1 is a **blocking decision checkpoint** requiring the user.
5. Wave 3 — `06-14`, a **blocking human-verify checkpoint** the operator runs on real Pi hardware.
6. Phase close: `code_review_gate` → `verify_phase_goal` → `update_roadmap`.
</remaining_work>

<decisions_made>

- **Skipped the UI safety gate** for this round — every gap is server-side or test-harness. User confirmed.
- **Appended 06-11..06-14** rather than replanning — 06-01..06-10 are executed with matching SUMMARYs. User confirmed.
- **`--workers N>1` is a one-way door.** `_db_lock` is a module-global `threading.Lock` (`dashboard/app.py:127`); a second gunicorn worker is a second interpreter with a second independent lock against one SQLite file, granting unserialized cross-process write access *while `T-06-24`'s diff-based closure evidence still passes* (`06-SECURITY.md:68`). Minted `PROH-OPS-04-05`; gated behind `06-13` Task 1.
- **The 90.0 attribution contract is asserted only by `06-12` Task 3's full-dataset run.** The shape gates pass `--min-attributed 0` on purpose — fixed Flask dispatch overhead dominates the `other` bucket proportionally at small dataset sizes. **Do not "helpfully" restore a 90.0 assertion to the shape gates**; that exact contradiction was found and removed in planning revision pass 2.
</decisions_made>

<blockers>
- **06-12's uncommitted `06-PROFILE.md`** — if the agent was killed before its SUMMARY commit, the 20k report is lost when the worktree is force-removed (#2070). Its three code commits survive regardless. Regenerate by running `tests/services_route_profile.py` at `--services 8 --days 8 --repeats 5` with `--min-attributed` omitted.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state, including the two pending human actions
2. `06-VERIFICATION.md` — the gap contract; its `gaps[0].missing` is what 06-11..06-14 close
3. `06-12-PLAN.md` Task 3 — what the profile must state, including that it may **refute** `06-VERIFICATION.md`'s own prediction
4. `06-13-PLAN.md` Task 1 and Task 2 — the decision options and the unconditional row-bound clause
5. `06-DEBT.md` — `D-DEBT-06-01` (GIL evidence), `D-DEBT-06-02` (CPU revisit trigger), `D-DEBT-06-06` (the CPU defect 06-11 fixed)

## Infrastructure State

- `main` @ `f672832`, clean apart from this pause commit. Untracked: `.gsd/`, `.planning/debug/`.
- Two **locked** worktrees under `.claude/worktrees/`, both based on `f672832`. Neither merged.
- Isolation resolved to `harness-worktree`; no fork-base divergence.
- No Pi hardware access from this environment. No running servers started by this session.

<context>
The phase failed verification on one truth: OPS-07's Pi acceptance run returned `overall_passed: false`
with `/api/services` p95 2465.9ms (budget 500ms), `/api/scan-status` 1797.4ms (budget 500ms), and
`/api/advanced/current` 2742.1ms (budget 2000ms). Cadence and RSS budgets passed. Separately, the
harness's CPU column had never been measured on any run — `06-11` fixed that.

The evidence already rules out `_db_lock` narrowing as sufficient on its own: `/api/advanced/current`
takes no lock at all and still degraded 29x under concurrency 8. That points at
`dashboard/Dockerfile:27` (`--workers 1 --threads 8`) plus `/api/services`' residual Python cost.
`06-12` exists to say which, with numbers, before anyone commits to a fix.

The single constraint most likely to be lost during `06-13`'s implementation: **`/api/services` must
remain bounded in rows read per request on whatever path it ends up using.** `dashboard/app.py:2806`
is an unbounded `SELECT ... ORDER BY ts ASC`, and the existing
`OfflineIntervalsBulkReadTests::test_the_bulk_read_is_bounded` calls the repository directly, so it
stays green while the route silently loses its 20,000-row cap.
</context>

<next_action>
Check whether agent `ac95044107eeefc28` (plan 06-12) finished.

- **If it completed:** merge both wave-1 branches into `main`, remove the worktrees, then re-run the
  full suite on the now-quiet tree and record the real pass count before dispatching `06-13`.
- **If it was killed mid-task:** `git -C .claude/worktrees/agent-ac95044107eeefc28 status` first.
  Commit or copy out `06-PROFILE.md` before removing that worktree — it is not recoverable afterward.
</next_action>
