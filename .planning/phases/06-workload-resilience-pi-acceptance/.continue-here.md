---
context: phase
phase: 06-workload-resilience-pi-acceptance
plan: null
task: null
status: awaiting_direction
last_updated: 2026-09-05T10:07:48.417Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through failure, not predicted. Acknowledge before proceeding.

- [ ] CONSTRAINT: **`D-DEBT-06-19` names a remediation path that is refuted.** It says to wire
  `/api/services` to `service_rollups`. `D-DEBT-06-21` proves that table contains **zero buckets
  inside the uptime window**. Read `D-DEBT-06-21` before acting on `D-DEBT-06-19`, and before
  planning anything that reads rollups for the uptime path.

**Do not proceed until that box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Decomposing an inherited premise without verifying it | The orchestrator asserted twice that `service_rollups` already held the buckets `/api/services` recomputes, citing `/api/history` at "33x, same data two ways". All three claims were false. A whole round was scoped on it, and the golden fixtures nominated as the safety net would have *hidden* the error — a route falling back to raw for 100% of the window reproduces those bytes trivially. Caught only because the round-6 planner checked a premise handed to it as established fact. | **blocking** | Before decomposing any claim about what data exists where, run the query. `D-DEBT-06-21` records the exact reproduction. This is the ninth instance in this project of a guard or claim asserting what it cannot detect — the failure mode is structural, not incidental. |
| Absolute thresholds tied to machine or dataset | `services_median_hold_in_band` broke when the dataset grew 2.24x while the relational check over the same data held. `PYTHON_SHARE_CEILING` was set from a Pi figure and applied on a dev machine. A "relational" comparator was added against a reference saturated at 0.9999, making it a disguised absolute. | advisory | Thresholds are relational or explicitly dataset-scaled. Compare two same-run measurements, not a measurement against a remembered number. See `D-DEBT-06-14`. |
| Full-suite runs treated as a clean gate | Two tests fail intermittently under whole-suite load and pass in isolation. | advisory | Judge by "no NEW failures outside the known-flaky set" in `D-DEBT-06-13`; re-run a suspect in isolation before attributing it. |

<current_state>
Nothing is mid-flight. No worktrees, no running agents, working tree clean, suite green at
**966 passed / 593 subtests / 0 failed**. Pi is on the current build.

**Phase 6** — 4/5 truths. OPS-07 is the only gap and remains `Pending`. Its criterion was amended
(`D-DEBT-06-20`) to single-operator load: gating run **concurrency 3 / 600s**, concurrency-8 optional.
The first gating run under it (`06-ACCEPTANCE-C3.md`, build `a33af15`) **failed on exactly one route**:
`/api/services` p95 635.6ms against a 500ms budget, p50 577.3ms. Every other route cleared with room;
cadence and resources passed.

**Phase 7** — executed 3/3. `BEACON_ADVANCED_DIAGNOSTICS` toggle ships, defaults on. DIA-09 stays
`Pending` — promotion belongs to an independent verification round per the `TEL-06` precedent.

**Round 6 was not planned.** The planner refuted its premise and returned `PLANNING INCONCLUSIVE`.
</current_state>

<remaining_work>

1. **Option D — one more acceptance run** at `--concurrency 3 --duration 600`, uninstrumented, fully
   enabled. The 2026-09-04 run carried an unresolved confound (worker peak RSS 786.9MB and 71% CPU
   against ~54.9MB / ~8.9% in every prior run — almost certainly a Chromium preview job in the
   window). No optimization can be attributed against a single confounded baseline.
2. **Option C — move the 168-bucket uptime computation into SQL** via the existing
   `SERVICE_QUERY_SHAPES['raw']` shape. No new infrastructure, no schema change, reversible. Targets
   ~35% of the profiled residual (`uptime_sweep` 29.975% + `row_grouping` 5.083%).
3. If C lands and the route still misses, escalate **option A** (a live rollup tier covering the
   recent window) as a roadmap decision — it is **one-way** and needs a blocking
   `checkpoint:decision`.
4. Independent verification round to promote OPS-07 and DIA-09 once their evidence supports it.
5. `06-23`/`06-24` are superseded by the `ea8689e` revert — do not execute them.
</remaining_work>

<decisions_made>

- **OPS-07's criterion amended to concurrency 3** — the operator is the sole user; `app.js` generates
  ~0.5 req/s from one tab. Justification is **usage, not difficulty**, recorded in three places
  because from the outside that is indistinguishable from the move `PROH-OPS-07-01`/`-10` forbid.
- **Round 5 reverted** (`ea8689e`) — the lock narrowing and the memo both measured worse on hardware.
  All test infrastructure retained.
- **The rollup path is refuted, not deferred** — see `D-DEBT-06-21`.
</decisions_made>

<blockers>
- `D-DEBT-06-19`'s stated remediation is refuted by `D-DEBT-06-21`. Options are enumerated there.
  Recommended: **D then C**.
</blockers>

## Required Reading (in order)

1. `06-DEBT.md` — `D-DEBT-06-21` **first**, then `-06-19`, `-06-20`, `-06-13`, `-06-14`
2. `06-ACCEPTANCE-C3.md` — including the **CORRECTION** section at the end
3. `06-PROFILE.md` — the cost attribution, which still stands
4. `06-LOCK-DIAGNOSTIC.md`, `-R5A.md`, `-R5B.md` — why round 5 was reverted

## Infrastructure State

- `main` clean and pushed; Pi on the current build, uninstrumented, advanced diagnostics enabled.
- Suite **966 passed / 593 subtests**. Known-flaky set in `D-DEBT-06-13`.
- `_db_lock`: 28 sites, scope unchanged, invariant and scope pin both passing and mutation-verified.
- **`06-LOCK-AUDIT.md` pins absolute line numbers**; any `dashboard/app.py` edit desyncs it and its
  invariant test fails. Realigned from the AST twice. Pinning `(function, ordinal)` instead would fix
  it permanently — deliberately not done, because that file carries `T-06-24`'s closure evidence and
  is a Phase 6 decision.

<next_action>
Read `D-DEBT-06-21` in full. Then ask the operator to run option D — one uninstrumented acceptance
pass at `--concurrency 3 --duration 600` on the current build — to get an unconfounded baseline.
Then plan option C. **Do not plan a `service_rollups`-backed uptime path without re-reading
`D-DEBT-06-21` first.**
</next_action>
