---
phase: 06-workload-resilience-pi-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/beacon/migrations.py
  - dashboard/beacon/support_floor.json
  - tests/fixtures/legacy/support-floor.json
  - tests/test_migrations.py
  - tests/test_maintenance_windows.py
  - tests/test_advanced_diagnosis_api.py
  - dashboard/beacon/repositories.py
  - dashboard/app.py
  - dashboard/beacon/worker_main.py
  - tests/worker_ownership_contract.py
  - tests/test_workload_resilience.py
  - tests/test_module_boundaries.py
  - tests/test_api_and_auth.py
  - tests/test_security_and_scanning.py
  - tests/test_runtime_ownership.py
  - .planning/phases/06-workload-resilience-pi-acceptance/06-VALIDATION.md
autonomous: false
requirements: [OPS-03]

estimate:
  tokens: 96000
  raw_tokens: 96000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A thumbnail captured by the worker is served byte-for-byte by GET /api/thumbnail/<port> from the new thumbnails table, not from services.thumb_data"
    - "After migration 10 runs on a database that had captured thumbnails, every services.thumb_data value is NULL and the same bytes are present in thumbnails.data for the same port"
    - "GET /api/services reports has_thumb true for a port whose thumbnail lives in thumbnails and is not expired, and false for a port with no unexpired row"
    - "The full pytest suite is green after the relocation — no existing thumbnail assertion is left asserting a column the product no longer writes"
    - "A deployment sitting at schema version 9 upgrades to 10 without UnsupportedSchemaError — the support floor carries a fingerprint for every tracked lineage at v9, and every manifest entry's target_version equals MIGRATIONS[-1].version"
    - "No test in the repository pins the newest migration version as a literal — the migration-tail assertions are expressed against MIGRATIONS[-1].version, so shipping migration 11 will not turn the suite red for bookkeeping reasons"
    - statement: "OPS-03: relocating thumbnail blobs off the primary telemetry path does not change which services appear, their online/offline state, or any other services column"
      verification: backstop
  artifacts:
    - path: "dashboard/beacon/migrations.py"
      provides: "_migration_10_bounded_thumbnail_store — creates thumbnails, backfills from services, empties services.thumb_data/thumb_mime, adds preview_requests.next_attempt_ts"
      contains: "_migration_10_bounded_thumbnail_store"
    - path: "dashboard/beacon/repositories.py"
      provides: "ThumbnailStoreRepository upsert write path plus read_thumbnail bounded read projection"
      contains: "class ThumbnailStoreRepository"
    - path: "dashboard/beacon/support_floor.json"
      provides: "Schema-version-10 support floor admitting every tracked lineage at v9"
      contains: "\"target_version\": 10"
    - path: "tests/fixtures/legacy/support-floor.json"
      provides: "The byte-equal copy of the packaged manifest the suite compares against"
      contains: "\"target_version\": 10"
    - path: "tests/test_workload_resilience.py"
      provides: "New OPS-01/02/03/04 integration suite; this plan seeds it with the thumbnail relocation end-to-end test"
      min_lines: 40
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-VALIDATION.md"
      provides: "The measured full-suite runtime recorded in § Test Infrastructure, replacing the TBD placeholder the nine full-suite task assertions are budgeted against"
  key_links:
    - from: "dashboard/app.py"
      to: "dashboard/beacon/repositories.py"
      via: "_store_thumbnail_result delegates the blob write to ThumbnailStoreRepository"
      pattern: "ThumbnailStoreRepository"
    - from: "dashboard/app.py"
      to: "dashboard/beacon/migrations.py"
      via: "api_thumbnail and the three has_thumb projections read the thumbnails table created by migration 10"
      pattern: "FROM thumbnails"
    - from: "dashboard/beacon/worker_main.py"
      to: "tests/worker_ownership_contract.py"
      via: "J6/J8 database_surfaces declare 'thumbnails'; DATABASE_SURFACES admits it"
      pattern: "thumbnails"
  prohibitions:
    - id: PROH-OPS-03-01
      requirement: OPS-03
      statement: "The migration must never destroy the only copy of a captured thumbnail — the backfill INSERT and the services blob-emptying UPDATE must execute inside the one migration transaction, so a failure leaves the original blobs intact."
    - id: PROH-OPS-03-02
      requirement: OPS-03
      statement: "The product must never advertise a thumbnail it can no longer serve — has_thumb must be derived from an unexpired row in the thumbnails table, never from a leftover services.thumb_source value."
  flagged_assumptions:
    - id: EDGE-OPS-03
      source: "edge-probe (spec-less fallback §A)"
      status: unresolved
      reason: "probe returned category=unclassified for OPS-03; no edge category could be derived from the requirement text, so it was neither auto-resolved nor backstopped"
      note: "Surfaced, not dropped. The reviewer must decide whether OPS-03 needs boundary predicates beyond the truths above (candidate boundaries: a zero-byte blob, a blob exactly at THUMB_MAX_BYTES, a service row with thumb_source set but thumb_data already NULL)."
---

<objective>
Move Beacon's preview thumbnail blobs off the primary `services` telemetry table into a dedicated
`thumbnails` table, proven end-to-end: the worker writes a capture into the new store, the web tier
serves those exact bytes, and the dashboard's `has_thumb` signal is derived from the new store.

Purpose: `services` is the row every scheduled sampling job (J3, J4, J5, discovery) reads and writes
for basic online/offline state. A 2 MiB BLOB column on that row is exactly the "large preview blobs
on the primary telemetry path" OPS-03 forbids. This plan is the tracer slice for the phase — it
crosses schema, repository, worker write path, web read path, and worker-ownership contract in one
end-to-end path.

Output: a version-agnostic migration-tail contract and a support floor that already admits every
tracked lineage at schema version 9, migration 10, `ThumbnailStoreRepository`, relocated read/write
paths, the eight existing test sites realigned onto the new store so the tracer commits against a
green suite, the new `tests/test_workload_resilience.py` suite file seeded with its first end-to-end
test, and the phase's measured full-suite runtime recorded in `06-VALIDATION.md`.

The schema-version bump and the contract that pins the newest migration version are one indivisible
change, so they live in one wave-1 plan. Splitting them across a wave boundary makes the tracer
un-signable: `test_support_floor_covers_history_and_confirmed_operator_evidence` asserts every
manifest entry's `target_version` equals `MIGRATIONS[-1].version`, and
`test_support_floor_admits_every_tracked_lineage_at_the_previous_version` demands floor coverage at
`MIGRATIONS[-1].version - 1`. Neither can be satisfied from a later wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-RESEARCH.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-PATTERNS.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-VALIDATION.md
</context>

## Artifacts this phase produces

Every symbol below is **created by this phase** and does not exist in the codebase before it. Drift
verification must treat these as new, not as unacknowledged references.

**Created in this plan (06-01):**
- `dashboard/beacon/migrations.py` — `_migration_10_bounded_thumbnail_store` (function),
  `THUMBNAIL_BACKFILL_TTL_SECONDS` (module constant), `Migration(10, 'bounded_thumbnail_store', True, ...)`
  tuple entry
- SQLite table `thumbnails` — columns `port`, `data`, `mime`, `captured_ts`, `source`, `expires_ts`;
  index `idx_thumbnails_expires`
- SQLite column `preview_requests.next_attempt_ts`
- `dashboard/beacon/repositories.py` — `ThumbnailStoreRepository` (class), its `store_thumbnail_result`
  method, and module function `read_thumbnail`
- `dashboard/app.py` — `_thumbnail_store` (function), `THUMBNAIL_TTL_SECONDS` (module constant)
- `dashboard/beacon/worker_main.py` — `'thumbnails'` entry in J6's `database_surfaces` tuple
- `tests/worker_ownership_contract.py` — `'thumbnails'` entry in `DATABASE_SURFACES` and in J6's `_row`
- `tests/test_workload_resilience.py` — new file
- `tests/test_migrations.py` — `V9_FINGERPRINTS` (constant), `LINEAGE_FINGERPRINTS` (constant),
  `test_migration_ten_thumbnail_relocation_preserves_every_captured_blob`
- `dashboard/beacon/support_floor.json` / `tests/fixtures/legacy/support-floor.json` — v9 lineage
  entries and `"target_version": 10` across every entry

**Created later in this phase (named here so drift checks see the whole set):**
- `dashboard/beacon/config.py` — `Settings.thumbnail_ttl_days`, `Settings.thumbnail_store_max_bytes`,
  `Settings.preview_max_attempts`, `Settings.preview_retry_base_seconds`,
  `Settings.preview_retry_max_seconds`; env keys `THUMBNAIL_TTL_DAYS`, `THUMBNAIL_STORE_MAX_BYTES`,
  `PREVIEW_MAX_ATTEMPTS`, `PREVIEW_RETRY_BASE_SECONDS`, `PREVIEW_RETRY_MAX_SECONDS`
- `dashboard/beacon/repositories.py` — `delete_expired_thumbnails`, `evict_thumbnails_over_budget`,
  `thumbnail_store_bytes`, `THUMBNAIL_EVICTION_SCAN_LIMIT`
- `dashboard/beacon/queues.py` — `PREVIEW_STATUS_DEGRADED`, `preview_retry_decision`,
  `schedule_preview_retry_in_transaction`, `schedule_preview_retry_for_worker_in_transaction`,
  `QueueRequest.attempt_count` field
- `dashboard/beacon/worker_main.py` — `'cleanup'` executor lane
- `dashboard/beacon/db.py` — `JOURNAL_MODE` (module constant), `configured_journal_mode` (function)
- `dashboard/app.js` — `previewCopy.degraded` entry; `svc-preview-fallback degraded` class usage
- `dashboard/style.css` — `.svc-preview-fallback.degraded`
- `tests/pi_load_acceptance.py` — new file
- `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` — new file

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the one-way door — migration 10 empties services.thumb_data in place</name>
  <decision>Migration 10 will, in one transaction on the operator's live database, copy every non-NULL `services.thumb_data` blob into a new `thumbnails` table and then set `services.thumb_data` and `services.thumb_mime` to NULL for those rows.</decision>
  <context>
This is a one-way door. Once the migration commits on the Pi, the only way back to the previous
on-disk shape is restoring the verified backup `create_verified_backup` writes to
`/data/backups/dashboard-*-pre-v10.db` immediately before the migration runs (`migrations.py:602-620`,
`_apply_pending_migrations` at `migrations.py:709-716`). There is no down-migration in this codebase
— `MIGRATIONS` is forward-only and `schema_migrations` records applied versions, never reversals.

What is NOT at risk: the blobs themselves. The backfill INSERT and the emptying UPDATE run inside the
same `BEGIN IMMEDIATE` transaction, so a failure at any point rolls both back and leaves
`services.thumb_data` untouched. What IS at risk: an operator who later needs the previous schema
shape must restore the pre-v10 backup, losing everything written after the upgrade.

The alternative — leaving the blobs on `services` and dual-writing — does not satisfy OPS-03, whose
whole subject is "without placing large preview blobs in the primary telemetry path."
  </context>
  <options>
    <option id="proceed">
      <name>Proceed: relocate and empty in migration 10, as planned</name>
      <pros>Satisfies OPS-03 directly. Single migration version, so the support-floor/fingerprint work in 06-02 is done once. A verified pre-v10 backup is created automatically before the migration touches anything.</pros>
      <cons>One-way on the live database. Rolling back to the pre-phase Beacon image after the migration has run requires restoring the pre-v10 backup.</cons>
    </option>
    <option id="backup-first">
      <name>Proceed, but take an additional operator-side snapshot of /data before deploying</name>
      <pros>Same outcome as `proceed`, plus an off-container copy the operator controls independently of Beacon's own backup retention (`BACKUP_RETENTION` prunes old backups).</pros>
      <cons>Requires a manual step on the Pi before deployment.</cons>
    </option>
    <option id="defer">
      <name>Defer OPS-03 to a later phase and plan only OPS-01/02/04/07 now</name>
      <pros>No schema change lands this phase.</pros>
      <cons>OPS-03 is a phase requirement; deferring it makes the phase incomplete and leaves the unbounded blob store on the telemetry path. Requires roadmap surgery.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, backup-first, or defer</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Make the migration-tail contract version-agnostic and admit every v9 lineage into the support floor</name>
  <files>tests/test_migrations.py, tests/test_maintenance_windows.py, tests/test_advanced_diagnosis_api.py, dashboard/beacon/support_floor.json, tests/fixtures/legacy/support-floor.json</files>
  <read_first>
    - `dashboard/beacon/support_floor.json` — the full current manifest: 11 entries, every one `"target_version": 9`, keys `fingerprint` / `fixture` / `source` / `minimum_schema_version` / `target_version`
    - `tests/fixtures/legacy/support-floor.json` — currently byte-identical to the packaged manifest; the suite asserts `packaged_manifest == manifest`
    - `tests/test_migrations.py` lines 32-80 — `FIXTURE_DIR`, `FIXTURES`, `CURRENT_V4_FIXTURE`, `CURRENT_V6_FIXTURE`, `OPERATOR_V7_FINGERPRINT`, `V8_FINGERPRINTS` (six lineage keys, four distinct values) and `EXPECTED_FINGERPRINTS` (hashes of the *raw, unmigrated* fixtures — these do not move)
    - `tests/test_migrations.py` lines 154-345 — `test_support_floor_covers_history_and_confirmed_operator_evidence`, `test_operator_production_path_to_v7_is_admitted_and_migration_eight_preserves_rows`, `_carry_lineage_to`, `test_support_floor_admits_every_tracked_lineage_at_the_previous_version`, `test_a_deployment_already_at_v8_applies_only_migration_nine`, `test_unsupported_schema_error_names_the_fingerprint_and_the_evidence_command`, `test_current_v4_fixture_is_canonical_and_migrates_preserving_rows`, `test_current_v6_legacy_host_state_migrates_once_without_overlap`
    - `tests/test_migrations.py` lines 504-532 — `test_migration_seven_absent_legacy_pressure_key_is_a_successful_no_op`, the third `(7, 8, 9)` pin
    - `tests/test_migrations.py` lines 664-684 and 826-836 — `test_each_supported_fixture_upgrades_once_preserving_representative_rows` and `test_a_contention_window_that_ends_is_survived_rather_than_fatal`, which already assert against `MIGRATIONS[-1].version`; they are the in-file precedent for the style this task applies everywhere else
    - `tests/test_maintenance_windows.py` lines 480-515 — the fresh-database schema check ending in `self.assertEqual(MIGRATIONS[-1].version, 9)` at line 511
    - `tests/test_advanced_diagnosis_api.py` lines 280-312 — the `background_job_health` schema check, its `versions` list read from `schema_migrations`, and `self.assertEqual(migrations.MIGRATIONS[-1].version, 9)` at line 309 sitting beside `self.assertIn(8, versions)`
    - `dashboard/beacon/migrations.py` — `_support_floor` (626-640) and `_apply_pending_migrations` (684-739): the floor is consulted only for a NON-empty database below the target version, and the backup loop at 709-716 creates one verified backup per **schema-changing** pending migration when the database is not new
    - `dashboard/beacon/inventory.py` lines 122-175 — `collect_inventory` and `classify_schema`, the only source of a fingerprint
  </read_first>
  <action>
This task runs **before** migration 10 exists and must leave the suite green on its own commit. Its
job is to remove every reason the suite would go red for bookkeeping rather than behavior when the
next migration lands — so that Task 3 can add migration 10 and still sign the tracer off against a
green repository.

Two things break when `MIGRATIONS[-1].version` moves: the support floor stops covering the lineage
shape a deployment of the previous release actually sits at, and a scatter of assertions that pinned
the tail version as a literal go stale. The first needs new data computed now; the second needs the
assertions rewritten to say what they actually mean. Both are doable and green at today's tail
version of 9.

**Compute the v9 lineage fingerprints — never guess them.** They are SHA-256 hashes of raw
`sqlite_master` DDL text and cannot be reasoned about (the existing `V8_FINGERPRINTS` comment says
exactly this). Write a throwaway script under the scratchpad that, for each of the six tracked
lineages (`initial-2026-04.db`, `metadata-events-2026-04.db`, `runtime-queues-2026-07.db`,
`current-v4.db`, `current-v6.db`, `operator/production.db`), copies the fixture to a temp path, runs
`run_migrations` with `MIGRATIONS` patched to `tuple(m for m in MIGRATIONS if m.version <= 9)` — the
exact idiom `_carry_lineage_to` uses — then prints `collect_inventory(target)['schema_fingerprint']`.
Record the six values. Lineages that converge on one v9 shape print identical hashes; that is expected
and matches the v8 precedent where three lineages share one fingerprint.

**`tests/test_migrations.py` — record the fingerprints and make the lineage guard version-driven.**
Add `V9_FINGERPRINTS`, keyed by the same six fixture names, directly below `V8_FINGERPRINTS`, carrying
a comment that mirrors `V8_FINGERPRINTS`' own warning that these are DDL-text hashes and must be
recomputed rather than reasoned about. Then add, below both:

`LINEAGE_FINGERPRINTS = {8: V8_FINGERPRINTS, 9: V9_FINGERPRINTS}` — a map from schema version to the
per-lineage fingerprints at that version, with a comment recording that
`test_support_floor_admits_every_tracked_lineage_at_the_previous_version` looks its expectations up by
`MIGRATIONS[-1].version - 1`, so shipping migration N means adding an `N - 1` entry here and the
matching floor entries, and nothing else.

Rewrite `test_support_floor_admits_every_tracked_lineage_at_the_previous_version` to resolve
`previous = LINEAGE_FINGERPRINTS[newest - 1]` and iterate `previous` instead of `V8_FINGERPRINTS`,
asserting each carried fingerprint equals `previous[fixture_name]`. The test keeps its name, its
`newest - 1` carry, its missing-floor assertion and its failure message unchanged. Rewrite the
docstring's worked example so it explains the version-driven lookup rather than naming 9 and 8 as
fixed numbers — the guard's subject (a deployment of the previous release must be admitted by the
newest one) is unchanged and must stay stated.

**Both support floor manifests — admit the v9 shapes now.** Append to
`dashboard/beacon/support_floor.json` one entry per **distinct** v9 fingerprint that is not already
present in the manifest, each with `"minimum_schema_version": 9`, `"target_version": 9` (which is what
`MIGRATIONS[-1].version` is at this commit — Task 3 moves every entry to 10 in the same commit that
makes 10 the tail), a `"fixture"` naming the lineage, and a `"source"` reading
`canonical migrations to 9 applied to <fixture list>`, matching the existing entries' phrasing. Remove
no existing entry: the floor is cumulative and
`test_support_floor_covers_history_and_confirmed_operator_evidence` asserts exact set equality against
the union of every historical fingerprint set. Write the identical content to
`tests/fixtures/legacy/support-floor.json`; the suite asserts the two are equal, so any divergence
fails. In `test_support_floor_covers_history_and_confirmed_operator_evidence`, add
`set(V9_FINGERPRINTS.values())` to the `expected` set union so the new entries are expected rather
than stray.

Adding v9 entries is safe at this commit precisely because the floor is consulted only for a database
*below* the target version: at tail version 9 a v9 database is already current and never reaches the
floor, so the new entries are inert until Task 3 makes 10 the tail.

**Replace every literal tail-version pin with what the assertion actually means.** Each of these is
green both at this commit and after any later migration ships. Change nothing about what each test
covers — only how it names the tail:

*In `tests/test_migrations.py`:*
1. `test_operator_production_path_to_v7_is_admitted_and_migration_eight_preserves_rows` (line 211):
   `applied_versions` becomes `tuple(range(8, MIGRATIONS[-1].version + 1))`, and the
   `MAX(version) FROM schema_migrations` assertion (215-217) compares against `MIGRATIONS[-1].version`.
   Leave the `mock.patch(..., MIGRATIONS[:7])` block and its `(2, 3, 4, 5, 6, 7)` assertion exactly as
   they are — that slice is deliberately pinned to the canonical path to v7 and is not a tail pin.
2. `test_a_deployment_already_at_v8_applies_only_migration_nine` (267-303): rename to
   `test_a_deployment_at_the_previous_version_applies_only_the_newest_migration`. Bind
   `newest = MIGRATIONS[-1].version` at the top; carry the lineage to `newest - 1` instead of the
   literal 8; assert `migration_versions == list(range(2, newest))`; assert the fingerprint equals
   `LINEAGE_FINGERPRINTS[newest - 1]['operator/production.db']`; assert
   `applied_versions == (newest,)` and `MAX(version) == newest`. Keep the `maintenance_windows`
   existence check as the final assertion — that table is created by migration 9 and stays present
   under every later tail, so it remains a true post-condition. Keep the docstring's operator-incident
   provenance; update only the sentences that name 8 and 9 as fixed numbers.
3. `test_current_v4_fixture_is_canonical_and_migrates_preserving_rows` (line 329): `applied_versions`
   becomes `tuple(range(5, MIGRATIONS[-1].version + 1))`.
4. `test_current_v6_legacy_host_state_migrates_once_without_overlap` (lines 341-342):
   `applied_versions` becomes `tuple(range(7, MIGRATIONS[-1].version + 1))`, and the backup count stops
   being the literal 3. Replace it with the count of schema-changing migrations among the applied set —
   `sum(1 for migration in MIGRATIONS if migration.version in result.applied_versions and
   migration.schema_changing)` — which is exactly what the backup loop in `_apply_pending_migrations`
   produces. The property under test is "one verified backup per schema-changing migration, and no
   duplicate for a lineage that overlaps", and that property is what the rewritten assertion states.
5. `test_migration_seven_absent_legacy_pressure_key_is_a_successful_no_op` (lines 519, 531):
   `applied_versions` becomes `tuple(range(7, MIGRATIONS[-1].version + 1))` and the `MAX(version)`
   assertion compares against `MIGRATIONS[-1].version`.

*In `tests/test_maintenance_windows.py` (line 511):* replace
`self.assertEqual(MIGRATIONS[-1].version, 9)` with an assertion that migration 9 was actually applied
to the database this test just migrated —
`self.assertIn(9, {row[0] for row in conn.execute('SELECT version FROM schema_migrations')})`. The
surrounding assertions already prove migration 9's schema is present; this replacement proves the
migration ran, which is the real subject, instead of pinning where the tail happens to sit.

*In `tests/test_advanced_diagnosis_api.py` (line 309):* replace
`self.assertEqual(migrations.MIGRATIONS[-1].version, 9)` with `self.assertIn(9, versions)`, sitting
beside the existing `self.assertIn(8, versions)` and reading from the same `versions` list the test
already collected from `schema_migrations`.

**Leave alone**, because they are already version-driven or deliberately pinned to a historical path:
`test_each_supported_fixture_upgrades_once_preserving_representative_rows` (664-684),
`test_a_contention_window_that_ends_is_survived_rather_than_fatal` (826-836),
`tests/test_startup_ordering.py:48`, `test_unsupported_schema_error_names_the_fingerprint_and_the_evidence_command`
(it carries to a literal 8 on purpose and asserts that literal appears in the error text — still true
under any later tail), every `failed_target_version` assertion in
`tests/test_backup_recovery.py` / `tests/test_ui_safety_integration.py` / `tests/test_api_and_auth.py`
(those name the migration that failed, not the tail), and `MigrationNineTests` in
`tests/test_migrations.py`, whose subject is migration 9 itself.

Do not add migration 10 in this task, and do not touch `dashboard/beacon/migrations.py` at all.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q` exits 0 at this task's commit — the whole point of this task is that the repository is green both before and after migration 10 lands
    - `diff dashboard/beacon/support_floor.json tests/fixtures/legacy/support-floor.json` produces no output
    - `python3 -c "import json;m=json.load(open('dashboard/beacon/support_floor.json'));e=m['supported_schemas'];assert len(e)>11;assert {x['target_version'] for x in e}=={9};assert len({x['fingerprint'] for x in e})==len(e)"` exits 0 — the manifest grew, still sits at the current tail, and carries no duplicate fingerprint
    - `tests/test_migrations.py` contains `V9_FINGERPRINTS` and `LINEAGE_FINGERPRINTS`
    - `grep -v '^#' tests/test_migrations.py | grep -c "V9_FINGERPRINTS"` is at least 2
    - `grep -v '^#' tests/test_migrations.py | grep -c "MIGRATIONS\[-1\].version + 1"` is at least 3 — the three range-derived `applied_versions` assertions
    - `grep -v '^#' tests/test_migrations.py | grep -c "LINEAGE_FINGERPRINTS\[newest - 1\]"` is at least 1
    - `grep -v '^#' tests/test_maintenance_windows.py | grep -c "SELECT version FROM schema_migrations"` is at least 1, and `grep -v '^#' tests/test_advanced_diagnosis_api.py | grep -c "assertIn(9, versions)"` is 1
    - `uv run --project dashboard python -m pytest -q tests/test_migrations.py tests/test_maintenance_windows.py tests/test_advanced_diagnosis_api.py tests/test_startup_ordering.py` exits 0
    - Behavior: a copy of `tests/fixtures/legacy/operator/production.db` carried to schema version 9 and inspected with `collect_inventory` reports a `schema_fingerprint` that is present as a `fingerprint` in `dashboard/beacon/support_floor.json`, with `minimum_schema_version` 9
    - Falsification: temporarily append a schema-changing no-op `Migration(11, ...)` to `MIGRATIONS`, run `uv run --project dashboard python -m pytest -q tests/test_migrations.py`, and confirm the only failures reported are the two support-floor tests (missing v10 coverage and the stale `target_version`) — no `applied_versions`, `MAX(version)`, `migration_versions` or backup-count assertion fails. Revert the temporary migration immediately and re-run to confirm green. Record the observed failure list in the plan summary.
  </acceptance_criteria>
  <done>The support floor carries a fingerprint for every tracked lineage at schema version 9, both manifests are identical, `LINEAGE_FINGERPRINTS` drives the lineage guard from `MIGRATIONS[-1].version`, no test in the repository pins the tail migration version as a literal, and the full suite is green.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: Tracer — a captured thumbnail is stored in, and served from, the bounded store</name>
  <reversibility rating="one-way">Migration 10 empties `services.thumb_data` on the live database; undoing it requires restoring the pre-v10 verified backup, and this codebase has no down-migration path.</reversibility>
  <files>dashboard/beacon/migrations.py, dashboard/beacon/repositories.py, dashboard/app.py, dashboard/beacon/support_floor.json, tests/fixtures/legacy/support-floor.json, tests/test_migrations.py, tests/test_workload_resilience.py, tests/test_api_and_auth.py, tests/test_security_and_scanning.py, tests/test_runtime_ownership.py</files>
  <read_first>
    - `dashboard/beacon/migrations.py` — read `_migration_2_service_diagnostics` (152-158) for the `thumb_*` column set, `_migration_3_metadata_and_state` (163-172) for the exact create/backfill/empty precedent, `_migration_8_background_job_health` (513-538) for the `CREATE TABLE IF NOT EXISTS` + index DDL style, `_migration_9_planned_maintenance` (541-571) for the additive-migration docstring convention, and the `MIGRATIONS` tuple (574-584)
    - `dashboard/beacon/repositories.py` — read `ThumbnailRepository.store_thumbnail_result` (709-727) for the current write path, `record_background_job_started` (26-37) for the `INSERT ... ON CONFLICT(<key>) DO UPDATE SET ... excluded.<col>` upsert idiom, and `read_background_job_health` (66-73) for the clamped-`limit` + `dict(row)` bounded read projection
    - `dashboard/beacon/previews.py` — read `ThumbnailResultRepository` Protocol (38-44), `PreviewOperations` (47-58) and `store_thumbnail_result` (84-88) to see the collaborator boundary the write path must keep
    - `dashboard/app.py` — read `_store_thumbnail_result` (1766-1769), `api_thumbnail` (2974-2987), and the three `has_thumb` projections at 1355, 1504, 2716; also read 1425-1440 (the discovery refresh gate that consumes `has_thumb`/`thumb_ts`/`thumb_source`) and 1610-1626 (the uptime-check re-enqueue that consumes `has_thumb`)
    - `dashboard/beacon/db.py` — read `write_transaction`/`database_access` (91-129) for the transaction discipline every new DB touch point must use
    - `tests/helpers.py` — read `load_app`/`cleanup_db` for the test fixture pattern
    - `tests/test_migrations.py` lines 32-80 — imports, `FIXTURE_DIR`/`FIXTURES` layout, and the `V8_FINGERPRINTS` / `V9_FINGERPRINTS` / `LINEAGE_FINGERPRINTS` constants **Task 2 added**
    - `tests/test_migrations.py` lines 89-125 — `snapshot_legacy_rows` and `assert_legacy_rows_preserved`, the preservation helpers the new migration test reuses
    - `tests/test_migrations.py` lines 154-345 — the two support-floor tests and `_carry_lineage_to`, all rewritten by Task 2 to resolve the tail from `MIGRATIONS[-1].version`; read them as they now stand, not as Task 2 found them
    - `dashboard/beacon/support_floor.json` and `tests/fixtures/legacy/support-floor.json` — the two byte-identical manifests Task 2 extended with the v9 lineage entries; every entry currently carries `"target_version": 9`
    - `tests/test_workload_resilience.py` — **this file does not exist yet; this task creates it**, so there is no prior content to read. It becomes the shared Phase 6 integration module that 06-02, 06-03, 06-04, 06-05 and 06-06 each append to in turn; every later plan reads it before appending.
    - `tests/test_api_and_auth.py` — read, in full, the five tests this task realigns: `test_browser_route_and_response_compatibility_matrix` (def 205; `services` blob seed at 212), `test_only_screenshot_thumbnails_are_served_and_reported` (def 268; blob seed at 279), `test_service_meta_refresh_success_updates_title_and_thumbnail` (def 617; blob projection at 660, assertions at 665-666), `test_thumbnail_result_records_error_and_clears_it_on_success` (def 669; blob projections at 675 and 679, assertions at 683 and 687-688), `test_service_meta_refresh_failure_keeps_existing_values_and_warns` (def 714; blob seed at 719, projection at 755, assertions at 761-762)
    - `tests/test_security_and_scanning.py` — read, in full, the two tests this task realigns: `test_discovery_queues_legacy_thumbnail_without_source` (def 218; blob seed at 223, projection at 276, assertion at 281) and `test_discovery_skips_recent_screenshot_thumbnail` (def 289; blob seed at 294)
    - `tests/test_runtime_ownership.py` — read `test_thumbnail_repository_persists_success_and_failure_for_api_reads` (def 178) in full, including the direct repository import at 179, its construction at 188, and its two blob projections at 193 and 200
  </read_first>
  <behavior>
    - Given a service row and a successful capture, `_store_thumbnail_result` writes the blob to `thumbnails` (upsert on `port`) and leaves `services.thumb_data` NULL.
    - `GET /api/thumbnail/<port>` returns those exact bytes with the stored `mime` as `Content-Type`, and 404 when no unexpired screenshot row exists for that port.
    - `GET /api/services` reports `has_thumb: true` for that port, and `false` once the row's `expires_ts` is in the past.
    - Running migration 10 against a database whose `services` rows carry non-NULL `thumb_data` leaves `thumbnails.data` equal to the original bytes and `services.thumb_data` NULL for every migrated port.
    - A failed capture still records `thumb_attempt_ts`/`thumb_error` on `services` and leaves no orphan row in `thumbnails`.
    - A tracked lineage carried to schema version 9 is admitted by the support floor and applies exactly `(10,)` — the shape every live deployment sits at upgrades rather than raising `UnsupportedSchemaError`.
  </behavior>
  <action>
Add migration 10 and relocate the thumbnail blob read/write paths to it. Concretely:

**`dashboard/beacon/migrations.py`** — add module constant `THUMBNAIL_BACKFILL_TTL_SECONDS = 7 * 86400`
with a comment recording that 7 days is the phase's chosen default TTL (D-02; the runtime-configurable
`THUMBNAIL_TTL_DAYS` setting lands in 06-02, and this constant only seeds `expires_ts` for rows that
existed before the setting did). Add `_migration_10_bounded_thumbnail_store(conn)` following
`_migration_9_planned_maintenance`'s docstring convention, performing exactly four steps in this order:
(1) `CREATE TABLE IF NOT EXISTS thumbnails` with columns `port INTEGER PRIMARY KEY`, `data BLOB`,
`mime TEXT`, `captured_ts INTEGER`, `source TEXT`, `expires_ts INTEGER`, plus
`CREATE INDEX IF NOT EXISTS idx_thumbnails_expires ON thumbnails(expires_ts)`;
(2) backfill with `INSERT OR IGNORE INTO thumbnails(port, data, mime, captured_ts, source, expires_ts)
SELECT port, thumb_data, thumb_mime, thumb_ts, thumb_source, COALESCE(thumb_ts, 0) + ? FROM services
WHERE thumb_data IS NOT NULL`, bound with `THUMBNAIL_BACKFILL_TTL_SECONDS`;
(3) `UPDATE services SET thumb_data=NULL, thumb_mime=NULL WHERE thumb_data IS NOT NULL`;
(4) `_add_column(conn, 'preview_requests', 'next_attempt_ts INTEGER')`.
Register it as `Migration(10, 'bounded_thumbnail_store', True, _migration_10_bounded_thumbnail_store)`
at the end of the `MIGRATIONS` tuple.

Steps 1-3 are the OPS-03 relocation. Step 4 rides in the same migration deliberately: adding a
second migration version would force a second full support-floor fingerprint round (six lineage
fingerprints across two JSON manifests plus the `test_migrations.py` guard), which is real context
cost for no schema benefit. `next_attempt_ts` stays unused until 06-03. Record that reasoning in the
migration docstring.

Leave `services.thumb_ts`, `thumb_source`, `thumb_attempt_ts` and `thumb_error` in place and still
written: they are small per-service diagnostic facts already read by `/api/thumbnail-status`
(`app.py:2990-3012`) and by the discovery refresh gate (`app.py:1433`). Only the blob and its content
type leave the primary table.

**`dashboard/beacon/support_floor.json` and `tests/fixtures/legacy/support-floor.json`** — set
`"target_version": 10` on **every** entry in both files, and change nothing else. Task 2 already added
the v9 lineage fingerprints and made the lineage guard resolve its expectations from
`MIGRATIONS[-1].version - 1`, so this one field is all that is left. It has to happen in *this*
commit and not in Task 2's, because `test_support_floor_covers_history_and_confirmed_operator_evidence`
asserts `entry['target_version'] == MIGRATIONS[-1].version` — the field is only correct at 10 once
migration 10 is registered, and only correct at 9 before that. Adding migration 10 and moving the
manifests are one indivisible change. Keep the two files byte-identical; the suite asserts equality.

`target_version` is manifest bookkeeping only — `_apply_pending_migrations` reads just `fingerprint`
and `minimum_schema_version` from a floor entry (`migrations.py:684-703`). Nothing about admission
behavior changes here; the guard that keeps the field honest is the test.

**`tests/test_migrations.py`** — add `test_migration_ten_thumbnail_relocation_preserves_every_captured_blob`
using the file's existing preservation-snapshot pattern (`_carry_lineage_to`, `snapshot_legacy_rows`,
`assert_legacy_rows_preserved`). Carry `operator/production.db` to `MIGRATIONS[-1].version - 1`, seed
two `services` rows with distinct `thumb_data` blobs and one row with NULL `thumb_data`, snapshot the
legacy rows, run `run_migrations`, then assert: every legacy row is preserved; both blobs are present
byte-identically in `thumbnails` keyed by port; the NULL-blob port has no `thumbnails` row; and
`services.thumb_data IS NULL` for every row. This is the `thumbnail_relocation` test `06-VALIDATION.md`
names as the OPS-03 migration evidence, and it is the lineage-level counterpart to the product-level
round trip added to `tests/test_workload_resilience.py` below — one proves a real deployed shape
upgrades without losing a blob, the other proves the running product reads and writes the new store.

**`dashboard/beacon/repositories.py`** — add `class ThumbnailStoreRepository` next to the existing
`ThumbnailRepository`, satisfying the same `ThumbnailResultRepository` Protocol signature
(`store_thumbnail_result(self, conn, port, thumb_data, thumb_mime, thumb_source, thumb_error, ts=None)`),
taking `ttl_seconds` as a constructor argument defaulting to `7 * 86400`. On a successful screenshot
(`thumb_data` truthy and `thumb_source == 'screenshot'`) it must (a) upsert into `thumbnails` using
`INSERT INTO thumbnails(port, data, mime, captured_ts, source, expires_ts) VALUES(?,?,?,?,?,?)
ON CONFLICT(port) DO UPDATE SET data=excluded.data, mime=excluded.mime, captured_ts=excluded.captured_ts,
source=excluded.source, expires_ts=excluded.expires_ts` with `expires_ts = timestamp + ttl_seconds`,
and (b) keep updating `services` for the metadata columns only — `thumb_ts`, `thumb_source`,
`thumb_attempt_ts`, and clearing `thumb_error` — never the blob columns. On a failure it must delete
nothing from `thumbnails` and update only the `services` metadata columns
(`thumb_ts=NULL, thumb_source=NULL, thumb_attempt_ts=?, thumb_error=?` truncated to 240 chars, matching
the existing behavior). Add module function `read_thumbnail(conn, port, *, now)` returning
`dict(row)` or `None` for `SELECT data, mime, captured_ts, source, expires_ts FROM thumbnails WHERE
port=? AND data IS NOT NULL AND source='screenshot' AND (expires_ts IS NULL OR expires_ts > ?)`.
All SQL uses `?` placeholders — no f-strings, no `.format()`.

Leave `ThumbnailRepository` in the module for now, untouched, and simply stop wiring it into the
composition root. It is retired in Task 4, deliberately not here: its `services`-blob UPDATE statement
is the exact source-text marker `tests/test_module_boundaries.py` pins the repository-ownership
boundary on (assertions at 142, 145 and 146), so deleting it in this task would turn that suite red
and cost this tracer the green full-suite verify that is the whole point of a tracer.

**`dashboard/app.py`** — add module constant `THUMBNAIL_TTL_SECONDS = 7 * 86400` beside the existing
`THUMB_MAX_BYTES`/`THUMB_REFRESH_DAYS` block (`app.py:65-68`), and a `_thumbnail_store()` accessor
returning a `ThumbnailStoreRepository(ttl_seconds=THUMBNAIL_TTL_SECONDS)`. Wire it as the
`thumbnail_repository` collaborator in the composition root that currently supplies
`ThumbnailRepository` to `PreviewOperations`. Then relocate the four read sites:
- `api_thumbnail` (2974-2987): read via `beacon_repositories.read_thumbnail(conn, port, now=int(time.time()))`
  instead of selecting the blob off `services`; keep the existing `_db_lock`, `make_response`,
  `Content-Type`, `Cache-Control: public, max-age=300` and 404 behavior byte-identical.
- The three `has_thumb` projections (1355, 1504, 2716): replace the `services`-column expression with
  `EXISTS (SELECT 1 FROM thumbnails t WHERE t.port = s.port AND t.data IS NOT NULL AND
  t.source='screenshot' AND (t.expires_ts IS NULL OR t.expires_ts > ?)) AS has_thumb`, bound with the
  same `now` each of those three queries already computes. Bind order matters — each of those queries
  already has positional parameters; place the new one correctly relative to the existing ones.

Do not touch `_db_lock`'s scope at any call site (D-01, RESEARCH Pitfall 4). Every new DB touch point
goes through the existing `write_transaction`/`database_access` context managers.

**`tests/test_workload_resilience.py`** — create the file with the module docstring naming it as the
Phase 6 OPS-01/02/03/04 integration suite, and add the end-to-end test `test_thumbnail_is_stored_in_and_served_from_the_bounded_store`
covering the five behaviors above. Use `tests/helpers.load_app`/`cleanup_db` for the app fixture and
`tests/test_migrations.py`'s fixture idiom for the migration case. Name the migration-path test
`test_migration_ten_moves_existing_blobs_off_services`. Add a module docstring line recording that
later plans in this phase append to this file: 06-02 (TTL and byte budget), 06-03 (bounded preview
retry), 06-04 (cadence under contention), 06-05 (WAL, concurrency, restart recovery), 06-06 (harness
self-test).

**Realign the eight existing test sites that read thumbnail bytes off `services`.** These move in this
task, not a later one, because the moment `api_thumbnail` and the three `has_thumb` projections read
the new table these eight sites are describing a column the product no longer writes — leaving them
for a follow-up task would sign this tracer off against a red repository. The list is exhaustive; it
was produced by grepping `thumb_data|thumb_mime` across `tests/` and is closed. Do not go looking for
others, and do not rewrite anything not named here.

*`tests/test_api_and_auth.py`* — five tests:
1. `test_browser_route_and_response_compatibility_matrix` (def 205): the seed at 212 currently sets the
   blob and mime on `services` together with `thumb_ts`/`thumb_source`. Split it — keep the
   `thumb_ts`/`thumb_source` metadata UPDATE on `services`, and insert the bytes and mime into
   `thumbnails` for the same port with `captured_ts` matching `thumb_ts`, `source='screenshot'`, and an
   `expires_ts` far in the future.
2. `test_only_screenshot_thumbnails_are_served_and_reported` (def 268): the INSERT at 279 seeds a whole
   `services` row including the blob columns. Drop the two blob columns from that INSERT and add a
   companion `thumbnails` INSERT for the same port. This test's subject — that a non-screenshot source
   is not served — is now enforced by `read_thumbnail`'s `source='screenshot'` filter, so keep the
   assertions as they are and only move the seed.
3. `test_service_meta_refresh_success_updates_title_and_thumbnail` (def 617): the projection at 660
   selects `title, thumb_data, thumb_mime, thumb_source` from `services`. Split into two reads — `title`
   and `thumb_source` from `services`, `data` and `mime` from `thumbnails` for that port — and retarget
   the assertions at 665-666 accordingly.
4. `test_thumbnail_result_records_error_and_clears_it_on_success` (def 669): the projections at 675 and
   679 mix blob columns with `thumb_source`/`thumb_attempt_ts`/`thumb_error`. Keep the metadata columns
   read from `services` exactly as they are — they did not move — and read `data`/`mime` from
   `thumbnails`. The failure assertion at 683 (previously "the blob is NULL on `services`") becomes
   "no unexpired `thumbnails` row exists for that port"; the success assertions at 687-688 read
   `thumbnails.data`/`thumbnails.mime`.
5. `test_service_meta_refresh_failure_keeps_existing_values_and_warns` (def 714): the seed at 719 sets
   `title` plus the blob columns and `thumb_ts` — keep `title`/`thumb_ts` on `services`, seed the bytes
   and mime into `thumbnails`. Retarget the projection at 755 and the assertions at 761-762 the same
   way as (3).

*`tests/test_security_and_scanning.py`* — two tests:
6. `test_discovery_queues_legacy_thumbnail_without_source` (def 218): the INSERT at 223 seeds a
   `services` row with blob columns and no `thumb_source` — the "legacy" shape this test is named for.
   Keep the `services` row without the blob columns, and seed the bytes into `thumbnails` with
   `source=NULL` so the legacy-source condition the test exercises is preserved exactly. Retarget the
   projection at 276 and the assertion at 281 at `thumbnails`.
7. `test_discovery_skips_recent_screenshot_thumbnail` (def 289): the INSERT at 294 seeds blob columns
   alongside `thumb_ts`/`thumb_source`. Keep `thumb_ts`/`thumb_source` on `services` — the recency gate
   this test exercises reads `thumb_ts`, which did not move — and seed the bytes into `thumbnails`.

*`tests/test_runtime_ownership.py`* — one test:
8. `test_thumbnail_repository_persists_success_and_failure_for_api_reads` (def 178): change the import
   at 179 and the construction at 188 to `ThumbnailStoreRepository`, and split the two projections at
   193 and 200 — `thumb_ts`/`thumb_source`/`thumb_attempt_ts`/`thumb_error` stay read from `services`,
   `data`/`mime` come from `thumbnails`.

Rules for all eight: preserve each test's name, docstring and original intent — this is a
data-location change, not a behavior change. Assertions on `thumb_ts`, `thumb_source`,
`thumb_attempt_ts` and `thumb_error` stay on `services` untouched, because those columns did not move.
Every new `thumbnails` seed carries `captured_ts`, `source` and an `expires_ts` far enough in the
future that the test's own clock cannot expire it.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q` exits 0 at this task's commit — the tracer is signed off against a green repository, not a repository that a later task will repair. This is achievable because Task 2 already removed every literal tail-version pin and pre-loaded the v9 support-floor entries, leaving this task only the `target_version` field to move; if the suite is red here for any migration-bookkeeping reason, Task 2 was incomplete and must be finished before this task commits
    - `dashboard/beacon/migrations.py` contains `def _migration_10_bounded_thumbnail_store(` and `Migration(10, 'bounded_thumbnail_store', True,`
    - `dashboard/beacon/repositories.py` contains `class ThumbnailStoreRepository` and `def read_thumbnail(`
    - `dashboard/app.py` contains `THUMBNAIL_TTL_SECONDS` and `def _thumbnail_store(`
    - `grep -c "FROM thumbnails" dashboard/app.py` is at least 3 (the three relocated `has_thumb` projections)
    - `uv run --project dashboard python -m pytest -q tests/test_workload_resilience.py` exits 0 with at least 2 tests collected
    - Behavior: with a stored capture of bytes `B` and mime `image/png` for port 8080, `GET /api/thumbnail/8080` returns 200, body `B`, `Content-Type: image/png`
    - Behavior: after setting that row's `expires_ts` into the past, `GET /api/thumbnail/8080` returns 404 and `GET /api/services` reports `has_thumb` falsy for port 8080
    - Behavior: applying migration 10 to a fixture database with `services.thumb_data = b'old-bytes'` for port 8080 yields `thumbnails.data = b'old-bytes'` and `services.thumb_data IS NULL` for port 8080
    - `uv run --project dashboard python -m pytest -q tests/test_api_and_auth.py tests/test_security_and_scanning.py tests/test_runtime_ownership.py` exits 0, with all eight realigned tests still present under their original names
    - `python3 -c "import json;m=json.load(open('dashboard/beacon/support_floor.json'));assert {e['target_version'] for e in m['supported_schemas']}=={10}"` exits 0, and `diff dashboard/beacon/support_floor.json tests/fixtures/legacy/support-floor.json` produces no output
    - `uv run --project dashboard python -m pytest -q tests/test_migrations.py -k thumbnail_relocation` exits 0 with exactly 1 test selected
    - Behavior: a copy of `tests/fixtures/legacy/operator/production.db` carried to schema version 9, then run through `run_migrations`, returns `applied_versions == (10,)` and does not raise `UnsupportedSchemaError`
    - `uv run --project dashboard python -m pytest -q tests/test_module_boundaries.py` exits 0 — it is untouched by this task and stays green because the old repository class is left in place until Task 4
  </acceptance_criteria>
  <done>Migration 10 is registered and the support floor moved with it in the same commit, the worker's thumbnail write path and the web tier's thumbnail read path both target the `thumbnails` table, existing blobs are migrated with a lineage-level preservation guard in `tests/test_migrations.py`, `tests/test_workload_resilience.py` proves the round trip end-to-end, and the full pytest suite is green at this commit.</done>
</task>

<task type="auto">
  <name>Task 4: Retire the old write path, declare the new surface, and record the suite's measured runtime</name>
  <files>dashboard/beacon/repositories.py, dashboard/beacon/worker_main.py, tests/worker_ownership_contract.py, tests/test_module_boundaries.py, .planning/phases/06-workload-resilience-pi-acceptance/06-VALIDATION.md</files>
  <read_first>
    - `dashboard/beacon/repositories.py` — read `ThumbnailRepository` (709-727) in full, and the `ThumbnailStoreRepository` added in Task 3 directly beside it, so the retirement removes exactly one class and no shared helper
    - `dashboard/app.py` line 1694 — the composition-root keyword argument Task 3 rewired; confirm it now names the new store and that nothing else constructs the old class
    - `tests/worker_ownership_contract.py` — read `DATABASE_SURFACES` (11-31), the `_row` helper, and the J6 row (134-139); note J8 is deliberately left alone here and gains its own entry in 06-02
    - `dashboard/beacon/worker_main.py` — read the `WORKER_CALLBACK_INVENTORY` J6 entry (line 89) for the `database_surfaces` tuple position
    - `tests/test_module_boundaries.py` lines 137-150 — `test_thumbnail_sql_stays_in_the_repository_boundary` in full: one positive assertion against `repositories.py` (142) and two negative assertions against `previews.py` (145) and `app.py` (146), all three pinned on the same source-text marker
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-VALIDATION.md` § Test Infrastructure and § Sampling Rate — the `Estimated runtime` row this task fills in, and the latency budget it is reconciled against
  </read_first>
  <action>
Close out the relocation: one module owns the thumbnail write, the worker declares the new database
surface, and the phase's feedback-latency contract stops being a placeholder.

**`dashboard/beacon/repositories.py`** — delete `ThumbnailRepository` and its `services`-blob UPDATE
statement. After Task 3 nothing in `dashboard/` or `tests/` constructs it: the composition root at
`app.py:1694` was rewired, and `tests/test_runtime_ownership.py` was switched to the new class. Verify
that with a repo-wide search before deleting, and if the search finds a caller Task 3 missed, fix that
caller here rather than keeping the class alive.

**`tests/test_module_boundaries.py`** — retarget all three assertions in
`test_thumbnail_sql_stays_in_the_repository_boundary` at the new store's write statement in
`repositories.py`: the positive assertion proves that module still owns the write, and the two
negative assertions prove `previews.py` and `app.py` still do not. The boundary being protected is
unchanged — exactly one module writes thumbnail bytes — only the marker text moves. Keep the test's
name and docstring.

**`dashboard/beacon/worker_main.py`** — add `'thumbnails'` to J6's `database_surfaces` tuple. J6 is the
preview poller, which now writes the new table. Change nothing else on that row — `executor`,
`scheduler_id`, `trigger`, `trigger_kwargs`, `misfire_grace_time` and `effect_surfaces` all stay as
they are. Leave J8 untouched; it gains `'thumbnails'` in 06-02, when the TTL reap is wired into it.

**`tests/worker_ownership_contract.py`** — add `'thumbnails'` to `DATABASE_SURFACES` and to the J6
`_row` database-surface tuple, in the same order as `worker_main.py`. The suite asserts
`declared_databases - DATABASE_SURFACES == set()` and `DATABASE_SURFACES <= declared_databases`, so
the two must move together or neither.

**Measure the full suite and record it.** Run `time uv run --project dashboard python -m pytest -q`
once, from a clean state, and replace the `Full suite runtime` cell in `06-VALIDATION.md`
§ Test Infrastructure — which currently says the runtime is not yet measured and names this task as
the one that measures it — with the observed wall-clock duration and the date measured. Then apply
the reconciliation rule already written into § Sampling Rate: if the measured runtime is at or under
120 seconds, the `Max feedback latency (full suite)` line stands unchanged. If it exceeds 120 seconds,
replace that line's provisional 120 with the measured value rounded up to the next 30 seconds,
annotated as measured rather than assumed. Do not touch the `Max feedback latency (quick command)`
line in either case — it is fixed at 120 seconds by contract. Record the measured value in the plan
summary either way; nine tasks across this phase are budgeted against it.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q` exits 0 — the full suite, no failures, no errors
    - `uv run --project dashboard python -m pytest -q tests/test_module_boundaries.py tests/test_worker_ownership_matrix.py` exits 0
    - `grep -v '^#' tests/worker_ownership_contract.py | grep -c "'thumbnails'"` is at least 2 (the `DATABASE_SURFACES` entry and the J6 row entry)
    - `grep -v '^#' dashboard/beacon/worker_main.py | grep -c "'thumbnails'"` is at least 1
    - `grep -v '^#' dashboard/beacon/repositories.py | grep -c "INTO thumbnails"` is at least 1 — the single surviving thumbnail write statement
    - CLI: `uv run --project dashboard python -c "from dashboard.beacon import repositories; assert not hasattr(repositories, chr(84)+chr(104)+chr(117)+chr(109)+chr(98)+chr(110)+chr(97)+chr(105)+chr(108)+chr(82)+chr(101)+chr(112)+chr(111)+chr(115)+chr(105)+chr(116)+chr(111)+chr(114)+chr(121))"` exits 0 — the retired class name is no longer an attribute of the module (the name is assembled at runtime so this check cannot be satisfied by a comment)
    - `06-VALIDATION.md` § Test Infrastructure `Full suite runtime` cell states a measured wall-clock duration and the date it was measured, and no longer says the runtime is not yet measured
    - `06-VALIDATION.md` § Sampling Rate still carries `Max feedback latency (quick command)` at 120 seconds, and its `Max feedback latency (full suite)` line is consistent with the measured value under the reconciliation rule
    - Behavior: `tests/test_module_boundaries.py` still fails if the thumbnail write statement is moved from `repositories.py` into `app.py` (verify by temporarily moving it, confirming the test goes red, then reverting)
  </acceptance_criteria>
  <done>Exactly one module writes thumbnail bytes, J6 declares `thumbnails` as a database surface in both the inventory and the ownership contract, the full suite is green, and the phase's full-suite feedback latency is a measured number in `06-VALIDATION.md` rather than a placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker process → SQLite `thumbnails` | Chromium-derived bytes cross from a browser-rendered page into durable storage |
| SQLite `thumbnails` → browser (`GET /api/thumbnail/<port>`) | Stored bytes and a stored `mime` string are echoed to the operator's browser |
| migration process → operator's live database | A one-way schema+data transformation runs against production state |
| support floor manifest → migration admission | A JSON manifest decides which on-disk shapes are allowed to upgrade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Denial of Service | `thumbnails.data` growth | high | mitigate | Per-row cap already enforced at capture (`THUMB_MAX_BYTES = 2 MiB`, `app.py:65`); `expires_ts` written on every store, TTL reap and total-store byte budget land in 06-02 (`delete_expired_thumbnails`, `evict_thumbnails_over_budget`). This plan writes `expires_ts` so the reap has something to act on from day one. |
| T-06-02 | Tampering | migration 10 backfill/empty | high | mitigate | Backfill INSERT and services-emptying UPDATE execute inside the single `BEGIN IMMEDIATE` transaction `_apply_pending_migrations` already wraps every migration in (`migrations.py:715-724`); a verified integrity-checked backup is created first (`create_verified_backup`, `migrations.py:602`). PROH-OPS-03-01 encodes the invariant. |
| T-06-03 | Spoofing | `Content-Type` from stored `mime` | medium | mitigate | `api_thumbnail` keeps its existing `row['mime'] or 'image/jpeg'` fallback; `mime` is only ever written by Beacon's own capture path from Playwright's screenshot type, never from a request parameter. `read_thumbnail` filters `source='screenshot'`, so an operator-seeded row with a foreign source is never served. |
| T-06-04 | Information Disclosure | thumbnail bytes served without auth | low | accept | Beacon is trusted-LAN-only with no auth model (locked project constraint); `/api/thumbnail/<port>` had exactly this exposure before this phase and the relocation does not widen it. |
| T-06-08 | Tampering | over-permissive support floor | high | mitigate | Fingerprints are computed from real fixture upgrades, never hand-written; `test_support_floor_covers_history_and_confirmed_operator_evidence` asserts exact set equality so a stray entry fails the suite; both manifests must be byte-identical. |
| T-06-09 | Denial of Service | under-permissive support floor | high | mitigate | `test_support_floor_admits_every_tracked_lineage_at_the_previous_version` is the guard that exists precisely because migration 9 shipped without its N-1 entries and locked every live deployment out. Task 2 adds the v9 entries *before* migration 10 exists and drives the guard from `MIGRATIONS[-1].version - 1` via `LINEAGE_FINGERPRINTS`, so the lockout cannot recur silently. |
| T-06-05 | Elevation of Privilege | SQL injection via port parameter | low | mitigate | Every new statement uses `?` placeholders per the codebase-wide parameterized-query discipline; `port` reaches the route as a Flask `<int:port>` converter. |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages this phase (`06-RESEARCH.md` § Package Legitimacy Audit: "No new external packages are introduced by this phase"). No install task exists, so no legitimacy checkpoint is required. If any task later needs a package, run `gsd_run query package-legitimacy check --ecosystem pypi <pkg>` first and gate `[ASSUMED]`/`[SUS]` behind a blocking human checkpoint. |
</threat_model>

<verification>
- `uv run --project dashboard python -m pytest -q` exits 0.
- Both support floor manifests are byte-identical and every entry's `target_version` equals
  `MIGRATIONS[-1].version`.
- A tracked lineage carried to schema version 9 upgrades to 10 without `UnsupportedSchemaError`.
- `uv run --project dashboard python -m pytest -q tests/test_workload_resilience.py` exits 0 with the thumbnail relocation tests present.
- `grep -c "thumb_data" dashboard/app.py` counts only the in-memory capture variables (`app.py:1115-1141`) and the `_store_thumbnail_result` parameter list — no remaining `services`-blob SELECT or UPDATE.
- A fixture database carrying pre-migration blobs upgrades to schema version 10 with byte-identical blobs in `thumbnails`.
</verification>

<success_criteria>
Thumbnail blobs no longer live on the `services` table; the worker writes them to `thumbnails` and the
web tier serves them from there; existing captures are migrated without loss; a deployment at schema
version 9 is admitted by the version-10 support floor; no test pins the tail migration version as a
literal; the full test suite is green at every task's commit; and the worker-ownership contract
declares `thumbnails` as a J6 database surface.
</success_criteria>

<output>
Create `.planning/phases/06-workload-resilience-pi-acceptance/06-01-SUMMARY.md` when done
</output>
