---
phase: 06-workload-resilience-pi-acceptance
plan: 13
type: execute
wave: 8
depends_on: ["06-11", "06-12"]
gap_closure: true
gap_ids: [G-06-1]
files_modified:
  - dashboard/app.py
  - dashboard/Dockerfile
  - dashboard/beacon/repositories.py   # branch-conditional: reduce-request-cost may need a shared offline-interval reconstruction helper here; narrow-db-lock may reach its call sites
  - docker-compose.yml                 # branch-conditional on Task 1: the add-workers branch must re-derive the web service's mem_limit from measured per-worker RSS
  - tests/test_services_route_scaling.py
  - tests/test_module_boundaries.py
  - tests/test_workload_resilience.py  # branch-conditional on Task 1: the add-workers branch adds a separate-process concurrent-writer integrity test. Also 06-11's file — this is a wave-8 write against a wave-7 file, sequentially safe, declared here so the overlap is not implicit
  - .planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
autonomous: false
requirements: [OPS-07, OPS-01, OPS-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The fix path is chosen by the user against 06-PROFILE.md's measured attribution plus D-DEBT-06-01's existing GIL evidence, and the numbers that justified it are recorded — not assumed in either direction"
    - "Whatever path is chosen, /api/services returns byte-identical uptime percentages and bucket values afterwards, pinned by the pre-existing differential oracle running unmodified"
    - "The route's SQL statement count still does not grow with service count"
    - "The deployment's gunicorn worker and thread topology is pinned at the source level, so a future change to it fails a test instead of silently altering the concurrency model the phase's evidence was gathered under"
    - "No second OS process gains unserialized concurrent write access to the database as a side effect of a topology change"
    - "The full suite stays green at its current 837 passed / 561 subtests, so truths 1-4 of the phase goal are not regressed"
  artifacts:
    - path: ".planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md"
      provides: "The recorded fix-path decision with the profile numbers behind it, and the updated disposition of D-DEBT-06-01"
      contains: "D-DEBT-06-01"
    - path: "tests/test_module_boundaries.py"
      provides: "A source-level pin on dashboard/Dockerfile's gunicorn worker and thread counts, so the deployment concurrency model cannot drift silently from the model the acceptance evidence was gathered under"
      contains: "Dockerfile"
    - path: "tests/test_services_route_scaling.py"
      provides: "A cost guard for whichever sub-computation the profile named, pinning that it is no longer proportional to stored check count"
      contains: "growth"
  key_links:
    - from: ".planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE.md"
      to: "this plan's fix-path decision checkpoint"
      via: "the checkpoint's options are weighed against the profile's named top cost centre and its measured growth ratios"
      pattern: "06-PROFILE.md"
    - from: "dashboard/Dockerfile"
      to: "dashboard/app.py _db_lock"
      via: "the process-global lock serializes threads within one interpreter only; raising the worker count puts a second interpreter, and therefore a second independent lock, on the same database file"
      pattern: "workers"
  prohibitions:
    - id: PROH-OPS-07-01
      requirement: OPS-07
      statement: "The acceptance harness must never be tuned to pass. No response-time budget, cadence threshold, or memory limit may be adjusted in response to what the profile or any run measures. The budgets are the contract; the code is made to meet them."
    - id: PROH-OPS-07-05
      requirement: OPS-07
      statement: "No existing test asserting an uptime percentage or a bucket value may be modified, relaxed, or deleted to accommodate a cost reduction. The output contract is fixed; only its cost changes. If a pre-existing test fails, the new implementation is wrong."
    - id: PROH-OPS-01-05
      requirement: OPS-01
      statement: "Response-time budgets in tests/pi_load_acceptance.py must not be widened to accommodate this route. The route is made fast enough for its declared budget; the budget is not moved to meet the route."
    - id: PROH-OPS-04-02
      requirement: OPS-04
      statement: "No route or job may gain unserialized database access as a side effect of this work. Per decision D-01, _db_lock's scope stays exactly as it is unless the decision checkpoint in this plan explicitly resolves in favour of narrowing it."
    - id: PROH-OPS-04-05
      requirement: OPS-04
      statement: "The process-global database lock serializes threads inside one interpreter. Raising gunicorn's worker count therefore grants a second OS process unserialized concurrent write access to the same database file without any line of _db_lock changing. Such a change must never be made as an incidental performance tuning: it requires the same cross-process audit D-DEBT-06-01 names for narrowing the lock, a mem_limit re-derived from the measured per-worker RSS, and /gsd-secure-phase 06 re-run because T-06-24's closure evidence is invalidated by construction."
    - id: PROH-OPS-07-09
      requirement: OPS-07
      statement: "A profile taken on a development machine must never be presented as Raspberry Pi-class latency evidence. Its proportional attribution and growth ratios may inform this decision; its absolute milliseconds may not be quoted as the route's cost on the Pi."
---

<objective>
Choose the fix path on the evidence, then build it.

Purpose: `06-VERIFICATION.md`'s gap 2 is a genuine decision whose answer was not knowable before
`06-12`'s profile existed. Three candidate paths were named: further per-request cost reduction in
`dashboard/app.py`, a gunicorn worker/thread reconfiguration in `dashboard/Dockerfile`, or narrowing
`_db_lock`. The phase's own evidence already constrains the third — `/api/advanced/current` takes no
lock at all (`dashboard/beacon/diagnosis.py` holds none; this is the pre-existing `AR-03-01`
accepted-risk route) and still degraded 29x under concurrency 8, which a process-wide lock cannot do
to a caller that never acquires it. That rules `_db_lock` out as *sufficient* on its own; it does not
rule it out as contributing, and `06-10`'s blocking checkpoint already chose `defer-again` on it.

The evidence-backed live suspect from the phase's own analysis is `dashboard/Dockerfile:27` —
`gunicorn --workers 1 --threads 8`, one interpreter, one GIL, eight HTTP threads — combined with
`/api/services`'s residual Python-side cost, which `06-12` has now attributed by measurement.

Prior-phase decision disposition: `06-CONTEXT.md` `D-01` fenced `_db_lock`'s scope for this phase and
routed the narrowing to `D-DEBT-06-01`. That fence still stands entering this plan; only Task 1's
blocking user decision can lift it, and lifting it carries the consequences `PROH-OPS-04-02` and
`PROH-OPS-04-05` name. `06-CONTEXT.md` `D-02` (preview retry budget and thumbnail TTL) was implemented
in `06-01`/`06-02`/`06-03` and is untouched by this round.

Output: a recorded decision with the numbers behind it, the chosen fix implemented and pinned, and a
deployment topology that can no longer drift silently.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-CONTEXT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE.md
@.planning/phases/06-workload-resilience-pi-acceptance/06-VERIFICATION.md
@dashboard/Dockerfile
@docker-compose.yml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the fix path against the measured profile</name>
  <read_first>
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE.md` in full — the attribution table, the growth table, the named finding, and the "what this does not establish" section. This decision is made against those numbers plus `D-DEBT-06-01`'s existing hardware evidence, and nothing else.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` § `D-DEBT-06-01` — the 29x regression on the unlocked `/api/advanced/current` route, the closed-loop arithmetic model, the stated honest limit of that evidence, and the two named reopening conditions.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-CONTEXT.md` § `D-01` — the user's own scope fence on `_db_lock` and its stated blast-radius reasoning.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-SECURITY.md` § `T-06-24` — closed on the specific evidence that no lock call site changed. Understand exactly what that closure rests on before choosing anything that breaks it.
    - `dashboard/Dockerfile` line 27 — the current `gunicorn --bind 0.0.0.0:8080 --workers 1 --threads 8 --timeout 60 --access-logfile - app:app` invocation.
    - `docker-compose.yml` — the `web` service's `mem_limit: 256m` and `pids_limit: 128`, against which `06-10`'s hardware reports measured web mean RSS rising from ~106.8MB at concurrency 1 to ~128.0MB at concurrency 8 for a single worker.
    - `.planning/PROJECT.md` § `AR-03-01` — the one route already outside the lock and why; `D-DEBT-06-01` names its reasoning as the baseline any lock evaluation must reconcile with rather than relitigate.
  </read_first>
  <precondition>`06-PROFILE.md` exists and reports `attributed_pct` at or above 90.0 with a measured `check_row_ratio`, and `06-11` has landed so the acceptance harness's CPU column will be a real measurement on the run that judges this decision. Halt and report if either is missing — this checkpoint's whole purpose is that the choice is made on evidence rather than ahead of it.</precondition>
  <decision>
Given `06-PROFILE.md`'s measured attribution and `D-DEBT-06-01`'s hardware evidence, which path closes
the OPS-07 responsiveness gap: reduce `/api/services`'s per-request cost further in `dashboard/app.py`,
change the gunicorn worker/thread topology in `dashboard/Dockerfile`, narrow `_db_lock`, or a named
combination?
  </decision>
  <context>
The Pi control pass at concurrency 1 measured `/api/services` p50 at 289.0ms against a 500ms budget —
58% of the budget consumed with nothing else running. The acceptance pass at concurrency 8 failed with
`/api/services` p95 2465.9ms, `/api/scan-status` p95 1797.4ms (its own work is 3.4ms), and
`/api/advanced/current` p95 2742.1ms. Cadence and RSS budgets passed on both runs.

Two mechanisms are in play and the phase's evidence separates them only partially. The first is
per-request service time: `/api/services` costs enough at zero contention that it saturates whatever
serializing mechanism sits behind it. The second is the serializing mechanism itself. `_db_lock` is
ruled out as *sufficient* by the unlocked route's 29x regression; the GIL under `--workers 1
--threads 8` is not ruled out by anything, and `D-DEBT-06-01`'s closed-loop model predicts the
deployment behaving as effective concurrency ~1 regardless of lock scope.

`06-PROFILE.md` now supplies what was missing: which named sub-computation owns the 289ms, and which
buckets grow with stored check count. Weigh the options against that table, not against the summary
below.

**Reversibility is not uniform across these options.** Reducing per-request cost is ordinary,
git-revertible work. Reducing the thread count is a deployment-config change that trades concurrency
for predictability and is revertible, but it changes the shape of the evidence every prior run was
gathered under. Raising the worker count above 1 is not merely a config change: `_db_lock` is
process-global, so a second gunicorn worker is a second interpreter holding a second, independent lock
against the same SQLite file — it grants unserialized cross-process write concurrency without a single
line of `_db_lock` changing, which is the same boundary `T-06-24` is closed on and `PROH-OPS-04-05`
now names explicitly. It also multiplies web RSS against a `mem_limit: 256m` that a single worker
already reaches ~128MB mean under load.
  </context>
  <options>
    <option id="reduce-request-cost">
      <name>Reduce `/api/services`'s per-request cost further in `dashboard/app.py`</name>
      <pros>Attacks the input rather than the amplifier, so it helps under any concurrency model. Touches only the route and its collaborators, changes no deployment topology, grants no new database access, leaves `T-06-24` and `AR-03-01` untouched, and is guarded by an existing differential oracle that makes an output change a test failure. If `06-PROFILE.md` names a cost centre whose growth ratio tracks the check-row ratio, this is the option the measurement supports.</pros>
      <cons>Does not remove the architectural constraint: eight threads still serialize to one, so a future slow route can stall the deployment again. May not be sufficient on its own if the profile shows the residual is already small and the remaining latency is queueing.</cons>
    </option>
    <option id="reduce-threads">
      <name>Reduce gunicorn's `--threads` count in `dashboard/Dockerfile`, keeping `--workers 1`</name>
      <pros>Reduces queue depth and head-of-line blocking on a GIL-serialized interpreter, which can lower p95 without touching any application code. Grants no new database access — one interpreter, one `_db_lock`, unchanged semantics. Git-revertible in one line.</pros>
      <cons>Trades concurrency for predictability: fewer threads serve fewer simultaneous browser requests. Invalidates the comparability of every prior run's numbers, which were gathered at eight threads. `PROH-OPS-07-10` still requires the acceptance run to be driven at `--concurrency 8` regardless of the deployment's thread count, so this option must be shown to help at that load, not at a reduced one.</cons>
    </option>
    <option id="add-workers">
      <name>Raise gunicorn's `--workers` above 1</name>
      <pros>Removes GIL serialization outright by putting independent interpreters on separate cores, which is the only option that directly addresses `D-DEBT-06-01`'s named live suspect.</pros>
      <cons>Grants a second OS process unserialized concurrent write access to the same SQLite file with no line of `_db_lock` changing — the exact boundary `T-06-24` is closed on. Invalidates `T-06-24`'s closure evidence by construction and requires `/gsd-secure-phase 06` to re-run before the phase seals. Requires the cross-process audit `D-DEBT-06-01` names, plus a demonstration that WAL genuinely covers cross-process writer contention here rather than an assumption that it does. Multiplies web RSS against `mem_limit: 256m`, which one worker already reaches ~128MB mean under load, so `mem_limit` must be re-derived from measurement before this can ship. `PROH-OPS-04-05` binds.</cons>
    </option>
    <option id="narrow-db-lock">
      <name>Narrow `_db_lock`'s scope</name>
      <pros>Removes the in-process serializing amplifier structurally. Justified only if the profile shows `/api/services`'s residual is already small and the remaining latency is attributable to lock waiting.</pros>
      <cons>The phase's own evidence already found this insufficient on its own — the worst-regressed route in the failing run is not locked — and `06-10`'s blocking checkpoint chose `defer-again` on exactly this question. Touches 30 call sites in one file, lifts the fence the user set in `D-01`, invalidates `T-06-24`, and requires the three prerequisites `D-DEBT-06-01` names (per-call-site audit, a WAL read-blocking demonstration, and a regression that fails if any route regains access it should not have) — none of which exists. Choosing this reopens a decision made two rounds ago on hardware evidence.</cons>
    </option>
    <option id="combination">
      <name>A named combination — `reduce-request-cost` plus one topology change</name>
      <pros>Addresses input and amplifier together, which is what `D-DEBT-06-01`'s model suggests is actually happening. Appropriate if the profile shows a substantial reducible cost centre *and* the arithmetic says reducing it alone still leaves the deployment saturated.</pros>
      <cons>Two variables move at once, so the third hardware run cannot attribute its own outcome to either change. If chosen, name which change is expected to carry the improvement so the run's result can still be interpreted. Inherits every consequence of whichever topology change is included.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The chosen option is recorded together with the specific `06-PROFILE.md` numbers that justify it: the named top cost centre, its share, and its growth ratio against the measured `check_row_ratio`.
    - If `add-workers` or `narrow-db-lock` is chosen, the record states that `T-06-24`'s closure evidence in `06-SECURITY.md` is invalidated by construction and that `/gsd-secure-phase 06` must re-run before the phase seals, and Task 2 additionally delivers the prerequisites named in that option's cons.
    - If `reduce-threads` or `add-workers` is chosen, the record names the exact new value and states that the acceptance run in `06-14` is still driven at `--concurrency 8` and `--duration 600` regardless (`PROH-OPS-07-10`).
    - No option is implemented before this checkpoint resolves. The decision is the user's; the plan does not proceed past it on an assumed answer.
    - Whichever option is chosen, no budget in `tests/pi_load_acceptance.py` is adjusted (`PROH-OPS-07-01`, `PROH-OPS-01-05`).
  </acceptance_criteria>
  <reversibility rating="one-way">Two of the five options are one-way in the sense that matters: `add-workers` grants a second OS process unserialized concurrent write access to the database with no lock line changing, and `narrow-db-lock` removes in-process serialization across 30 call sites. Both are git-revertible in the mechanical sense, but both have a failure mode — a data race in production — that may not surface for a long time and is not attributable to the change when it does. That profile is exactly why this is a blocking human decision rather than a planner's judgment. `reduce-request-cost` is reversible; `reduce-threads` is costly but reversible.</reversibility>
  <resume-signal>Select: reduce-request-cost, reduce-threads, add-workers, narrow-db-lock, or combination — naming the `06-PROFILE.md` figures that justify it, and for any topology option the exact new value.</resume-signal>
  <done>The fix path is chosen by the user against the measured profile and the existing hardware evidence, with the numbers recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the chosen path, output unchanged</name>
  <files>dashboard/app.py, dashboard/Dockerfile, tests/test_services_route_scaling.py</files>
  <read_first>
    - Task 1's recorded decision — this task implements that and only that.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-PROFILE.md` — the attribution and growth tables naming what to change.
    - `dashboard/app.py` lines 2781-2880 (`api_services`), 1156-1246 (`_legacy_uptime_summary` and its inline correctness argument), 1828-1830 (`_uptime_summary`'s indirection through `_monitoring_operations()`), and 86-88 (`UPTIME_WINDOW_SECONDS`, `UPTIME_BUCKETS`, `CHECK_RETENTION_SECONDS`).
    - `dashboard/beacon/repositories.py` lines 1105-1168 — `read_service_offline_intervals_by_port`, including its boundary query, its in-window query over the same `service_checks` window `api_services` has already fetched into `checks_by_port`, and the `_OFFLINE_INTERVALS_BULK_ROW_LIMIT` bound. Its docstring commits to the bulk result equalling the single-port result per port; anything built on top of it must preserve that.
    - `dashboard/beacon/maintenance.py` lines 457-499 (`attributed_downtime_seconds`) and 169-194 (`coverage`) — the segment-splitting rule and the per-segment `coverage()` call. The docstring states the function never re-implements the interval-overlap rule; a cost reduction must keep that property.
    - `tests/test_services_route_scaling.py` in full — `_reference_uptime_summary` (the verbatim pre-optimization oracle that must never be edited), `UptimeSummaryDifferentialTests`, `OfflineIntervalsBulkReadTests::test_the_bulk_result_matches_the_single_port_result_per_port`, `test_attributed_downtime_is_unchanged_through_the_route`, and `ServiceCountQueryGuardTests::test_query_count_is_independent_of_service_count`. These are the guards the change must survive unmodified.
  </read_first>
  <behavior>
    - Test: for randomized check histories, the route's `uptime_pct` and `uptime_buckets` after the change equal the values `_reference_uptime_summary` produces — the existing differential oracle passes unmodified.
    - Test: `maintenance_attributed_seconds` for a port with a seeded maintenance window is identical before and after the change.
    - Test: the route's SQL statement count is unchanged by service count and is not higher than before the change.
    - Test: the sub-computation the profile named has a growth ratio at the two seeded volumes materially below the measured `check_row_ratio` after the change, where it tracked the ratio before.
    - Test (topology branches only): `dashboard/Dockerfile`'s gunicorn invocation carries exactly the decided worker and thread values.
  </behavior>
  <action>
Implement the option Task 1 recorded, and only that option. Write the failing guard first in each case
— a cost or topology change with no test that fails against the previous state is not evidence.

**If `reduce-request-cost` (or the cost half of `combination`) was chosen.** Reduce the cost centre
`06-PROFILE.md` named. Implement what the profile actually identified, not what was expected: if the
report's named finding differs from `06-VERIFICATION.md`'s recorded expectation, follow the report.
One concrete candidate the route's structure makes available, to evaluate against the profile rather
than to adopt on faith: `api_services` fetches the whole retention window of `service_checks` rows once
into `checks_by_port`, and `read_service_offline_intervals_by_port`'s in-window query then reads the
same `(ts, port, online)` rows over the same `[now - CHECK_RETENTION_SECONDS, now]` bounds a second
time — if the profile attributes a material share to that second read, the offline intervals can be
reconstructed from the already-materialized `checks_by_port` points through the same
`_offline_intervals_from_points` helper both existing readers share, keeping the boundary-sample query
(which reads rows outside the window and therefore is not duplicative) and keeping
`read_service_offline_intervals_by_port` itself intact for its other caller. Whatever is chosen, the
constraints are fixed: `uptime_pct`, `uptime_buckets`, and `maintenance_attributed_seconds` must be
byte-identical for every input; `_reference_uptime_summary` must not be touched; no existing test in
`tests/test_services_route_scaling.py` may be modified or relaxed (`PROH-OPS-07-05`); and the route's
statement count must not grow. Do not touch any `_db_lock` call site (`PROH-OPS-04-02`).

**The row bound is unconditional, and it is a bound on the route, not on a helper.** `/api/services`
must remain bounded in the number of rows it reads per request on whatever path it ends up using.
This is stated unconditionally because the candidate above would otherwise quietly remove the bound:
`read_service_offline_intervals_by_port` enforces `_OFFLINE_INTERVALS_BULK_ROW_LIMIT = 20000` on its
in-window query, but the `checks_by_port` query the candidate would reconstruct from
(`dashboard/app.py:2806`, `SELECT ts, port, online FROM service_checks WHERE port IN (...) AND ts >= ?
ORDER BY ts ASC`) carries **no LIMIT at all**. Dropping the bulk read from the route therefore deletes
the 20,000-row cap from `/api/services` while leaving the constant and its guard untouched — and the
existing guard cannot catch it, because
`OfflineIntervalsBulkReadTests::test_the_bulk_read_is_bounded`
(`tests/test_services_route_scaling.py:335`) calls `read_service_offline_intervals_by_port` directly
through the test's own `_bulk` helper rather than through the route, so it stays green while the
route loses its bound. So: if the bulk read is removed from `/api/services`, the `checks_by_port`
query gains the equivalent `LIMIT`, bound to the same constant rather than to a second, drifting
literal, and a **new** test asserts the bound **through the route** — patch the constant to a small
value, seed more rows than that, issue a real `test_client().get('/api/services')`, and assert the
route reads no more rows than the bound permits. Do not modify
`test_the_bulk_read_is_bounded` to cover this; it guards the repository contract and must keep
guarding it (`PROH-OPS-07-05`).

**If `reduce-threads` was chosen.** Change only the `--threads` value on `dashboard/Dockerfile` line
27 to the decided number, leaving `--workers 1`, `--bind`, `--timeout`, `--access-logfile` and
`app:app` exactly as they are. Do not change `tests/pi_load_acceptance.py`'s `--concurrency` default or
its help text: `PROH-OPS-07-10` requires the acceptance run to be driven at 8 regardless of the
deployment's thread count, because the scenario is the contract and the deployment is what must meet
it.

**If `add-workers` was chosen.** Change the `--workers` value on `dashboard/Dockerfile` line 27 to the
decided number, and additionally deliver the prerequisites `PROH-OPS-04-05` names, because a second
interpreter means a second independent `_db_lock` against one SQLite file: a per-call-site audit of
every `_db_lock` use recording which are safe under cross-process concurrency and which are not; a
test in `tests/test_workload_resilience.py` demonstrating that concurrent writers in *separate
processes* (not separate threads — `ConcurrentAccessTests` already covers threads) leave
`PRAGMA integrity_check` returning `ok` with every reported-committed write present, extending
`PROH-OPS-04-01`'s guarantee across the process boundary; and a `mem_limit` for the `web` service in
`docker-compose.yml` re-derived from the measured per-worker RSS (`06-10`'s reports measured ~128.0MB
mean for one worker under concurrency 8 against the current 256m) rather than left at a value one
worker already half-consumes. Record in the summary that `T-06-24`'s closure evidence is invalidated
and `/gsd-secure-phase 06` must re-run before the phase seals.

**If `narrow-db-lock` was chosen.** Deliver the three prerequisites `D-DEBT-06-01` names before any
call site changes: the per-call-site audit of all 30 `_db_lock` uses; a demonstration that read-only
routes no longer block behind a writer under WAL specifically, rather than an assumption that WAL
implies it; and a regression that fails if any route regains unserialized access it should not have.
Only then narrow the scope. Record that `T-06-24` is invalidated and `/gsd-secure-phase 06` must
re-run.

Add the cost guard to `tests/test_services_route_scaling.py` in every branch that changed application
code: a test in the `ServicesRouteProfilerGuardTests` style using `tests.services_route_profile`'s
`profile_growth` at two seeded volumes, asserting the named bucket's `growth_ratio` is below half the
measured `check_row_ratio`. Give it a docstring naming the `06-PROFILE.md` figure it descends from, in
the style `tests/test_services_route_scaling.py`'s module docstring already uses for the 2504.6ms
measurement, so a future reader who trips it knows which hardware finding it protects.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_services_route_scaling.py tests/test_uptime_integration.py tests/test_maintenance_windows.py tests/test_module_boundaries.py && uv run --project dashboard python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --project dashboard python -m pytest -q` passes with no fewer than 837 tests passed and 561 subtests passed.
    - `git diff -- tests/test_services_route_scaling.py` shows additions only: `_reference_uptime_summary` is byte-identical and no assertion in `UptimeSummaryDifferentialTests`, `UptimeSummaryScalingTests`, `OfflineIntervalsBulkReadTests` or `ServiceCountQueryGuardTests` is modified or deleted (`PROH-OPS-07-05`).
    - `git diff -- tests/pi_load_acceptance.py` shows no change to `ROUTE_BUDGETS_MS`, `assert_response_times`, `assert_resource_budget`, `assert_cadence`, or the `--concurrency` / `--duration` defaults (`PROH-OPS-07-01`, `PROH-OPS-01-05`, `PROH-OPS-07-10`).
    - `git diff -- dashboard/app.py | grep -c '_db_lock'` is 0 unless `narrow-db-lock` was the recorded decision (`PROH-OPS-04-02`, `06-CONTEXT.md` `D-01`).
    - If application code changed: the new cost guard exists, names its `06-PROFILE.md` origin in its docstring, and was observed to FAIL against the pre-change implementation — record that observation in the summary.
    - `/api/services` is bounded in rows read per request: either `read_service_offline_intervals_by_port` still runs on the route path with `_OFFLINE_INTERVALS_BULK_ROW_LIMIT` enforced, or a route-level test proves the bound holds through `test_client().get('/api/services')` with the constant patched to a small value and more rows than that seeded. `grep -n 'LIMIT' dashboard/app.py | grep -v '^[0-9]*: *#'` must show a bound on the `service_checks` fetch if that fetch became the route's only path to the offline intervals.
    - `OfflineIntervalsBulkReadTests::test_the_bulk_read_is_bounded` is unmodified and still passes — the repository-level contract keeps its own guard (`PROH-OPS-07-05`).
    - If `dashboard/Dockerfile` changed: `grep -n 'gunicorn' dashboard/Dockerfile` shows the decided worker and thread values and no other flag altered; `uv run --project dashboard python -m pytest -q tests/test_module_boundaries.py tests/test_maintenance_windows.py` passes, confirming the existing Dockerfile assertions still hold.
    - If `add-workers` or `narrow-db-lock` was chosen: the prerequisite audit and the cross-process (or WAL read-blocking) demonstration exist and pass, and the summary records that `/gsd-secure-phase 06` must re-run before the phase seals.
  </acceptance_criteria>
  <reversibility rating="one-way">Carries the reversibility of whichever option Task 1 selected. `add-workers` and `narrow-db-lock` both widen a database-access boundary whose failure mode is a data race that may not surface for a long time and is not attributable to the change when it does; that is why the blocking decision checkpoint precedes this task. `reduce-request-cost` is reversible; `reduce-threads` is costly but reversible.</reversibility>
  <done>The chosen path is implemented, the route's output is unchanged, and a guard exists that fails against the pre-change state.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the deployment topology and record the decision</name>
  <files>tests/test_module_boundaries.py, .planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md</files>
  <read_first>
    - `tests/test_module_boundaries.py` lines 95-125 — `test_settings_are_immutable_and_container_copies_package` and `test_production_image_manifest_includes_the_advanced_document_bundle`, the two existing `dashboard/Dockerfile` source assertions. Add alongside them in the same style; do not fold the new assertion into either.
    - `dashboard/Dockerfile` line 27 as it stands after Task 2.
    - `docker-compose.yml` — the `web` service's `mem_limit` and `pids_limit`, and the `x-beacon-common` anchor.
    - `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` in full — its three-section structure (Deferred / Decided / Discharged), its per-entry table format, and the "what would need to be true to proceed / close this entry" convention every entry follows. Match it exactly rather than introducing a second shape.
    - Task 1's recorded decision and `06-PROFILE.md`'s figures.
  </read_first>
  <action>
Two things that make this round's decision durable.

**Pin the topology.** `06-VERIFICATION.md` records `dashboard/Dockerfile` line 27 as a warning-level
anti-pattern precisely because the deployment's concurrency model is load-bearing for every latency
number this phase produced and nothing pins it. Add
`test_the_deployment_pins_its_gunicorn_concurrency_model` to the class in
`tests/test_module_boundaries.py` that already reads the Dockerfile. Parse the `CMD` line's argument
list and assert the `--workers` and `--threads` values equal the values in force after Task 2, and
that `app:app` is still the application target. Give the test a docstring stating why the numbers are
pinned rather than merely observed: the process/thread topology determines whether `_db_lock`
serializes all database access (one interpreter) or only some of it (several interpreters), so a
change to these two numbers is a change to the database-access boundary `T-06-24` is closed on and
must go through the decision `PROH-OPS-04-05` requires — not through an unremarked edit. Reference
`D-DEBT-06-01` and `PROH-OPS-04-05` by name in the assertion messages so a developer who trips the
test is routed to the right reading rather than tempted to update the constant.

**Record the decision.** Update `06-DEBT.md`, matching its existing format:
  - `D-DEBT-06-01` — add this round's outcome: `06-PROFILE.md` now supplies the attribution
    `D-DEBT-06-01` previously argued about, so record what the profile actually measured, which of the
    entry's two named reopening conditions it bears on, and Task 1's chosen option with the figures
    behind it. If `defer-again` was effectively reaffirmed (any option other than `narrow-db-lock`),
    state plainly that the deferral now rests on a measured attribution as well as on the 29x
    unlocked-route evidence. If `narrow-db-lock` was chosen, record that the fence set in
    `06-CONTEXT.md` `D-01` is lifted, that `T-06-24` is invalidated, and that `/gsd-secure-phase 06`
    must re-run.
  - Add a new entry for the deployment topology under the **Decided** section, carrying: the values in
    force, why they are now pinned by a test, what `PROH-OPS-04-05` requires of anyone changing them,
    and the `mem_limit` arithmetic (one worker measured ~128.0MB mean under concurrency 8 against
    `mem_limit: 256m`) that constrains any future worker-count increase.
  - Do not touch `D-DEBT-06-03` (still open, unchanged — no journal-mode reading was captured by this
    round) and do not touch `D-DEBT-06-05`. Do not mark `D-DEBT-06-06` discharged here: it discharges
    on `06-14`'s hardware run producing a real CPU reading, not on `06-11` landing the code fix.
  - Add no entry for work this round completed. Recording fixed work as debt would misstate the
    phase's position.
  </action>
  <verify>
    <automated>D=.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md; test -f "$D" && uv run --project dashboard python -m pytest -q tests/test_module_boundaries.py && uv run --project dashboard python -m pytest -q && { for k in D-DEBT-06-01 D-DEBT-06-03 D-DEBT-06-05 D-DEBT-06-06 PROH-OPS-04-05 06-PROFILE.md; do grep -q -- "$k" "$D" || { echo "MISSING: $k"; exit 1; }; done; echo DEBT_AND_PIN_OK; }</automated>
  </verify>
  <acceptance_criteria>
    - `test_the_deployment_pins_its_gunicorn_concurrency_model` exists in `tests/test_module_boundaries.py`, passes, and asserts both the `--workers` and the `--threads` values in force after Task 2.
    - The test was observed to fail when either value is temporarily altered — record that mutation check in the summary, so the pin is proven non-tautological rather than merely present.
    - The test's assertion messages name `D-DEBT-06-01` and `PROH-OPS-04-05`, so a developer who trips it is routed to the decision record rather than to the constant.
    - `06-DEBT.md`'s `D-DEBT-06-01` entry carries Task 1's chosen option, the `06-PROFILE.md` figures behind it, and which of its two named reopening conditions the measurement bears on.
    - A new Decided-section entry records the deployment topology, its pin, `PROH-OPS-04-05`'s requirement, and the `mem_limit` arithmetic.
    - `D-DEBT-06-03` and `D-DEBT-06-05` are unchanged, and `D-DEBT-06-06` is not marked discharged (`git diff` on those entries shows no substantive edit).
    - `uv run --project dashboard python -m pytest -q` passes with no fewer than 837 tests passed and 561 subtests passed. This task adds an assertion to `tests/test_module_boundaries.py`, which other modules' Dockerfile assertions share a file with, so a `-k`-filtered subset is not sufficient evidence.
    - The verify command prints `DEBT_AND_PIN_OK`. The debt-record path is resolved and existence-checked before the test commands run, so a pytest failure surfaces as a test failure rather than as a spurious `MISSING:` line against an unassigned path. The chain uses `&&` throughout rather than `set -e`, because `set -e` is silently a no-op in a zsh subshell and would fail open on exactly the step this gate exists to catch.
  </acceptance_criteria>
  <reversibility rating="reversible">One additive test plus documentation.</reversibility>
  <done>The decided concurrency model cannot drift silently, and the decision and its evidence are recorded in the phase's debt record.</done>
</task>

</tasks>

<!-- planner-discipline-allow: _db_lock -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gunicorn process topology → SQLite file | How many interpreters hold the process-global lock decides whether database access is serialized at all |
| decision checkpoint → security posture | Two of the five options invalidate a threat closure recorded in `06-SECURITY.md` |
| cost reduction → route output | A change made for speed must not alter a single uptime percentage or bucket value |
| `dashboard/Dockerfile` → future developer | An unpinned concurrency constant is an invitation to change it without reading why it is what it is |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-64 | Elevation of Privilege | a second gunicorn worker gaining unserialized cross-process database write access | critical | mitigate | `PROH-OPS-04-05` names it explicitly; gated behind Task 1's blocking user decision; the branch additionally requires a per-call-site `_db_lock` audit, a separate-process concurrent-writer integrity test extending `PROH-OPS-04-01` across the process boundary, a re-derived `mem_limit`, and `/gsd-secure-phase 06` re-run because `T-06-24`'s closure evidence is invalidated by construction |
| T-06-65 | Denial of Service | multiple web workers against `mem_limit: 256m` | high | mitigate | One worker measured ~128.0MB mean RSS under concurrency 8 on `06-10`'s hardware run; the `add-workers` branch may not ship without re-deriving `mem_limit` in `docker-compose.yml` from that measurement |
| T-06-66 | Tampering | uptime or attributed-downtime output drifting under a cost reduction | high | mitigate | `PROH-OPS-07-05` binds; the pre-existing differential oracle (`_reference_uptime_summary`, a verbatim copy of the pre-optimization algorithm) must run unmodified and green, and `test_attributed_downtime_is_unchanged_through_the_route` must pass |
| T-06-67 | Tampering | budgets adjusted instead of code | high | mitigate | `PROH-OPS-07-01` and `PROH-OPS-01-05` bind; an acceptance criterion requires `git diff -- tests/pi_load_acceptance.py` to show no change to `ROUTE_BUDGETS_MS`, the assertion functions, or the scenario defaults |
| T-06-68 | Repudiation | the deployment concurrency model drifting from the model the evidence was gathered under | medium | mitigate | `test_the_deployment_pins_its_gunicorn_concurrency_model` asserts both values at the source level with assertion messages routing a tripping developer to `D-DEBT-06-01` and `PROH-OPS-04-05`; proven non-tautological by a mutation check |
| T-06-69 | Elevation of Privilege | narrowing `_db_lock` reintroducing the race it was added to prevent | high | mitigate | Gated behind the same blocking decision; the branch may not proceed without the three prerequisites `D-DEBT-06-01` names, and `06-CONTEXT.md` `D-01`'s fence stands unless the user lifts it |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing — no dependency is added, removed, or re-pinned, and no branch introduces a package-manager invocation; `dashboard/uv.lock` and `dashboard/pyproject.toml` are absent from `files_modified` |
</threat_model>

## Flagged Assumptions (spec-less probe fallback)

No SPEC.md exists for this phase, so `## Edge Coverage` and `## Prohibitions` were absent. The
deterministic edge probe ran over all 5 phase requirement IDs and returned 5 applicable / 0 resolved /
5 unresolved, every row `category: unclassified`. An `unclassified` row is never auto-resolved with a
backstop marker and never auto-dismissed, so all five are surfaced here and none were authored into
`must_haves.truths` from the probe. No-silent-drop equality holds: 5 surfaced == 0 authored + 5 flagged.

| # | Requirement | Probe status | Flagged assumption carried into this round |
|---|---|---|---|
| 1 | OPS-01 | unresolved (unclassified) | Assumed: the fix chosen here does not change J1–J4 cadence. Not established by the probe; covered only by the full suite staying green and by `assertions.cadence.passed` on `06-14`'s hardware run. |
| 2 | OPS-02 | unresolved (unclassified) | Assumed: this round touches neither the preview retry budget nor the degraded state. Not established by the probe; covered only by the full suite staying green. |
| 3 | OPS-03 | unresolved (unclassified) | Assumed: this round touches neither the thumbnail TTL nor the store byte budget. Not established by the probe; covered only by the full suite staying green. |
| 4 | OPS-04 | unresolved (unclassified) | Assumed: no change here widens database access concurrency. Not established by the probe; covered by `06-CONTEXT.md` `D-01`'s fence, by `PROH-OPS-04-02`, and by `PROH-OPS-04-05`'s explicit treatment of the worker-count case the existing prohibition did not reach. |
| 5 | OPS-07 | unresolved (unclassified) | Assumed: the six routes in `ROUTE_BUDGETS_MS` are the representative set and their declared budgets are the contract. Not established by the probe; `PROH-OPS-07-01` forbids revisiting either in this round. |

**Canon-referral breadcrumbs (surfaced by the prohibition recall, deliberately not minted):**
SQL parameterisation on any new or altered query is canon injection hygiene already owned by
`/gsd-secure-phase` and closed as `T-06-05` / `T-06-10` in `06-SECURITY.md`; not minted here. Container
image provenance and the pinned base-image digest in `dashboard/Dockerfile` line 1 are canon supply-chain
concerns owned by `/gsd-secure-phase` and covered by `T-06-SC`; not minted here.

## Artifacts this phase produces

| Symbol | File | Kind |
|---|---|---|
| `test_the_deployment_pins_its_gunicorn_concurrency_model` | `tests/test_module_boundaries.py` | new test (source-level pin on the `--workers` / `--threads` values) |
| the named cost guard (branch-dependent, in the `ServicesRouteProfilerGuardTests` style) | `tests/test_services_route_scaling.py` | new test pinning the reduced bucket's growth ratio |
| the deployment-topology entry | `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` | new Decided-section entry |
| `D-DEBT-06-01` (updated) | `.planning/phases/06-workload-resilience-pi-acceptance/06-DEBT.md` | entry updated with the measured attribution and the chosen option |
| `PROH-OPS-04-05` | this plan's frontmatter | new prohibition (worker-count change grants cross-process unserialized write access) |
| branch-only, if the bulk read leaves the route path: a route-level row-bound guard | `tests/test_services_route_scaling.py` | new test asserting `/api/services` stays bounded in rows read per request, through `test_client()` rather than at the repository layer |
| branch-only, if `add-workers` is chosen: the separate-process concurrent-writer integrity test | `tests/test_workload_resilience.py` | new test extending `PROH-OPS-04-01` across the process boundary |
| branch-only, if `add-workers` is chosen: a re-derived `web` `mem_limit` | `docker-compose.yml` | changed value |
| branch-only, if `reduce-request-cost` reconstructs intervals in the route: a shared offline-interval reconstruction helper | `dashboard/beacon/repositories.py` | new or widened helper reusing `_offline_intervals_from_points`, so the two readers cannot drift apart |

<verification>
- The fix path is recorded with the `06-PROFILE.md` figures behind it, chosen by the user at a blocking checkpoint.
- `uv run --project dashboard python -m pytest -q` passes with no fewer than 837 tests passed and 561 subtests passed.
- The pre-existing differential oracle runs unmodified and green: `uptime_pct`, `uptime_buckets`, and `maintenance_attributed_seconds` are unchanged (`PROH-OPS-07-05`).
- `/api/services` is still bounded in rows read per request on whatever path it uses, proven at the route level if the bulk read left that path — the 20,000-row cap does not disappear as a side effect of removing the read that happened to carry it.
- `git diff -- tests/pi_load_acceptance.py` shows no budget, threshold, or scenario-default change (`PROH-OPS-07-01`, `PROH-OPS-01-05`, `PROH-OPS-07-10`).
- `git diff -- dashboard/app.py | grep -c '_db_lock'` is 0 unless `narrow-db-lock` was the recorded decision (`06-CONTEXT.md` `D-01`, `PROH-OPS-04-02`).
- The gunicorn topology is pinned by a test proven non-tautological by a mutation check.
- If `add-workers` or `narrow-db-lock` was chosen, `/gsd-secure-phase 06` is recorded as required before the phase seals.
</verification>

<success_criteria>
- The fix path is the user's decision, made against a measured profile rather than ahead of it.
- The chosen fix is implemented with the route's output byte-identical, its query count unchanged, and its per-request row read still bounded.
- The deployment's concurrency model is pinned at the source level and its change conditions are recorded.
- No budget was tuned and no database-access boundary widened without the decision and prerequisites that widening requires.
</success_criteria>

<output>
Create `.planning/phases/06-workload-resilience-pi-acceptance/06-13-SUMMARY.md` when done
</output>
