---
phase: 07-optional-advanced-diagnostics
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - dashboard/app.py
  - dashboard/app.js
  - dashboard/beacon/frontpage.py
  - tests/test_optional_advanced_diagnostics.py
autonomous: true
requirements: [DIA-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07-04: with the toggle off, all four advanced routes answer 404 — the API route from `07-01`, and now `/advanced`, `/advanced.css` and `/advanced.js`"
    - "D-07-05: with the toggle off, the front page's link to the advanced workspace is absent from the bytes the server sends, not hidden by styling and not removed later by script — a client with scripting disabled receives a document that never contained it"
    - "D-07-06: with the toggle off, the services front page still works — it boots, renders services, and its theme toggle responds — proven by driving the real page in real Chromium in both themes, never by reading `dashboard/app.js` for a null check"
    - "With the toggle on, every one of the four surfaces serves exactly the bytes of its file on disk, so the enabled path demonstrably does not transform anything"
    - "The document transform is performed at most once per process however many front-page requests arrive, measured by counting invocations across repeated requests"
  artifacts:
    - path: "dashboard/beacon/frontpage.py"
      contains: "`without_advanced_entry_point`, a pure string-to-string transform that raises `AdvancedEntryPointNotFound` rather than silently returning a document that still carries the link"
    - path: "tests/test_optional_advanced_diagnostics.py"
      contains: "`DisabledAdvancedAssetTests`, `FrontPageEntryPointTests` and `FrontPageBootWithoutAdvancedTests`, the last driving real Chromium in both themes"
  key_links:
    - "`dashboard/index.html:24` -> `without_advanced_entry_point` -> the bytes `/` sends: the markup is the transform's only input, so a standing test pins the markup shape the transform depends on and any future edit to that line fails the suite instead of the deployment"
    - "The removed anchor -> `dashboard/app.js:772`'s unguarded `addEventListener` -> the entire `DOMContentLoaded` handler: removing the element without this task's second half kills the front page's whole boot sequence, which is the opposite of what the operator asked for"
    - "`07-01`'s zero-work guard on `GET /` -> this plan's front-page branch: the guard is what forbids delivering the toggle to the front page through a database read"
---

<objective>
The rest of the surface, and the front page that has to survive it.

`07-01` proved the toggle end to end on `/api/advanced/current`. This plan expands the same shape
horizontally to the three remaining routes, and then does the one genuinely delicate thing in this
phase: it takes the entry point out of the front page's served bytes without breaking the front page.

**The front page's script is not as decoupled as it looks.** `dashboard/app.js:772` runs
`$('advanced-diagnosis-link').addEventListener('click', captureDashboardScroll)` inside the
`DOMContentLoaded` handler with no guard. Remove the anchor and that expression raises a
`TypeError`, the handler dies at line 772, and **every statement after it never runs** — the meta
form, the modal, the maintenance-suggestion buttons, the initial `Promise.allSettled` fetch of stats,
history, scan, services and events, and all four polling intervals. The services front page the
operator explicitly said they like would be a static shell. This is the single highest-risk change in
the phase and the reason its proof is a real browser rather than a source assertion.

**How the front page learns the toggle: the server decides, before the bytes leave.** The open
question `07-CONTEXT` posed is settled here. `/` serves `dashboard/index.html` through `send_file`
today, and criterion 2 requires the entry point be absent rather than styled away, so the choice is
where the removal happens.

- *Rejected — client-side removal after a configuration fetch.* `dashboard/app.js` does not fetch
  `/api/config` today (only `dashboard/advanced.js` does, at its line 468), so this adds a boot
  round-trip to the page whose responsiveness is the product's core value; the link still ships in
  the document and is only taken out afterwards; and a client with scripting disabled keeps it.
- *Rejected — a wrapper element around the anchor in `index.html`, toggled server-side.* Editing
  `index.html` moves the enabled bytes, which is precisely what criterion 4 and `07-01`'s captured
  digests forbid.
- *Rejected — a second front-page file for the disabled mode.* Two documents to keep in step, and
  every future front-page change silently becomes two changes.
- **Chosen (D-07-05):** `/` keeps its current `send_file` statement verbatim on the enabled path, and
  on the disabled path serves a once-computed transform of the same file with the anchor and its
  trailing separator removed. The enabled path is byte-identical by construction rather than by
  test — the test exists to catch a future edit that breaks the construction.

**Purpose:** complete criterion 2, without costing criterion 3 or breaking the page criterion 1
protects.
**Output:** three more gated routes; `dashboard/beacon/frontpage.py`; a front page that boots with or
without its advanced link; and browser-level proof of both.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-optional-advanced-diagnostics/07-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The three remaining routes answer 404 off, and serve their file unchanged on</name>
  <files>dashboard/app.py, tests/test_optional_advanced_diagnostics.py</files>
  <read_first>
    - `dashboard/app.py` lines 2497-2526 — `index`, `advanced_index`, `serve_css`,
      `serve_advanced_css`, `serve_js`, `serve_advanced_js`, in the order they appear.
    - `dashboard/app.py`'s `api_advanced_current` gate as `07-01` left it — this task reproduces that
      shape three times and must not invent a fourth.
    - `tests/test_advanced_diagnosis_api.py` lines 254-278 —
      `test_direct_route_tracer_preserves_middleware_assets_and_get_only_api`, which already asserts
      that each of the three assets is served with the right mimetype, with the security headers, and
      with bytes equal to the file on disk. That test runs at the default and must keep passing; this
      task's enabled-path guard is its complement, not its replacement.
    - `tests/test_module_boundaries.py` lines 108-125 —
      `test_production_image_manifest_includes_the_advanced_document_bundle`. The three asset files
      stay in the image regardless of the toggle: a deployment that turns advanced diagnostics off
      must be able to turn it back on by restarting, which requires the assets to be present. Do not
      remove them from `dashboard/Dockerfile`.
  </read_first>
  <behavior>
    - With the toggle off, `GET /advanced`, `GET /advanced.css` and `GET /advanced.js` each return
      404 with an empty body.
    - With the toggle off, `GET /` and `GET /style.css` and `GET /app.js` still return 200 — the
      front page's own assets are never gated, only the advanced bundle is.
    - With the toggle on, each of the four advanced surfaces returns bytes equal to its file under
      `dashboard/`, and `/` returns bytes equal to `dashboard/index.html`.
    - With the toggle on, the four asset digests recorded in
      `tests/fixtures/07_advanced_current_enabled_response_golden.json` still match the files on
      disk — this phase changed no served asset.
  </behavior>
  <reversibility rating="reversible">Three early returns and one test class. No file is moved, removed from the image, or rewritten.</reversibility>
  <action>
    Gate `advanced_index`, `serve_advanced_css` and `serve_advanced_js` in `dashboard/app.py` by
    returning an empty body with status 404 as each handler's first statement, using the identical
    shape `07-01` applied to `api_advanced_current` (D-07-02). Leave `index`, `serve_css` and
    `serve_js` alone in this task; `/` is Task 2's subject.

    Add `DisabledAdvancedAssetTests` to `tests/test_optional_advanced_diagnostics.py`, building its
    application through `load_app({'ENABLE_ADVANCED_DIAGNOSTICS': '0'})`, covering every line of the
    `<behavior>` list. Drive the three gated paths and the three ungated front-page paths in one
    subtest loop so a regression names the path that moved.

    Add the enabled-path byte-identity guard as a separate test class method built through
    `load_app({})`: for each of `/`, `/advanced`, `/advanced.css`, `/advanced.js`, assert the
    response body equals the corresponding file's bytes read from the directory holding
    `dashboard/app.py`, resolved the way
    `test_direct_route_tracer_preserves_middleware_assets_and_get_only_api` resolves it. This
    assertion is deliberately an invariant rather than a frozen digest: it stays true across any
    legitimate future edit to those files and still fails the moment the enabled path starts
    transforming a document — which is the property criterion 4 actually needs and the property
    Task 2 puts at risk.

    Separately assert that the four `asset_sha256` values recorded in `07-01`'s fixture still match
    the files on disk, and give that assertion a message stating that this phase must not change a
    served asset and that a later phase legitimately editing one of these files is expected to update
    the fixture. Keeping this as a distinct, self-describing assertion is what stops it from being
    mistaken for the invariant above.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_optional_advanced_diagnostics.py tests/test_advanced_diagnosis_api.py tests/test_module_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - With the toggle off, each of `/advanced`, `/advanced.css`, `/advanced.js` has status 404 and an
      empty body; each of `/`, `/style.css`, `/app.js` has status 200.
    - With the toggle on, each of the four advanced surfaces returns bytes equal to its on-disk file.
    - `test_direct_route_tracer_preserves_middleware_assets_and_get_only_api` and
      `test_production_image_manifest_includes_the_advanced_document_bundle` both still pass.
    - Sensitivity, demonstrated and recorded with observed values: removing any one of the three new
      early returns makes the disabled subtest FAIL naming that path with its observed status pair.
      Record all three observed pairs.
  </acceptance_criteria>
  <done>All four advanced routes are absent when the toggle is off, all four are byte-unchanged when it is on, and the assets remain in the production image so the toggle can be reversed by restart alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The entry point leaves the served document, once per process, or the server refuses</name>
  <files>dashboard/beacon/frontpage.py, dashboard/app.py, tests/test_optional_advanced_diagnostics.py</files>
  <read_first>
    - `dashboard/index.html` line 24 — the exact markup: an anchor with `href="/advanced"` and
      `id="advanced-diagnosis-link"`, immediately followed by a separator `div` carrying class
      `tb-sep`. Both are removed together: leaving the separator behind puts a stray divider at the
      left edge of the top bar's right-hand group, which the operator would see.
    - `dashboard/app.py` lines 2497-2500 — `index`, and line 157's `get_db` for the file-resolution
      idiom used elsewhere in the module.
    - `dashboard/beacon/web.py` — the package's module conventions, and `AGENTS.md`'s constraint that
      new features use explicit module boundaries rather than growing the monolith.
    - `tests/test_module_boundaries.py` lines 38-57 — the AST rule forbidding any module under
      `dashboard/beacon/` from importing the legacy `app` module. The new module must not import it.
  </read_first>
  <behavior>
    - `without_advanced_entry_point` applied to the real `dashboard/index.html` returns a document
      that contains neither the anchor's `id` nor its `href` nor its visible label, and that is
      otherwise character-for-character the original.
    - Applied to a document containing no such anchor, it raises `AdvancedEntryPointNotFound`. It
      never returns the document unchanged: a silent no-op would serve the link on a deployment that
      turned the feature off, which is the exact failure criterion 2 forbids.
    - Applied to a document containing two such anchors, it raises. Removing one of two and leaving
      the other is a silent half-failure.
    - With the toggle off, `GET /` returns 200 with a body that contains none of those three strings
      and that still contains the services grid's element id and the theme toggle's element id.
    - With the toggle on, `GET /` returns bytes equal to `dashboard/index.html`, and the transform is
      never invoked at all.
    - Across five successive disabled `GET /` requests in one process, the transform is invoked
      exactly once.
  </behavior>
  <reversibility rating="reversible">A new module, one branch in `index`, and a process-local cache. No file on disk is rewritten and no stored data is touched; reverting restores the current `send_file` behaviour exactly.</reversibility>
  <action>
    Create `dashboard/beacon/frontpage.py` exposing `AdvancedEntryPointNotFound` (a `ValueError`
    subclass), the constant `ADVANCED_ENTRY_POINT_ID` holding the anchor's element id, and the pure
    function `without_advanced_entry_point(document)`.

    The function matches the anchor by its element id together with its immediately-following
    separator element, using a compiled regular expression that requires exactly one match across the
    whole document, and returns the document with that single span excised. Exactly one match is the
    contract: zero matches and more than one match both raise `AdvancedEntryPointNotFound` carrying
    the observed match count. Its docstring states plainly that the function is coupled to
    `dashboard/index.html`'s top-bar markup, that this coupling is deliberate because the alternative
    — editing `index.html` to carry a wrapper — would move the enabled bytes criterion 4 protects,
    and that the coupling is held safe by the markup invariant test below rather than by hope.

    The module imports nothing from `dashboard.app` and nothing from any other `beacon` module; it is
    a string-to-string function with no I/O, so `tests/test_module_boundaries.py`'s AST rule and the
    package's dependency direction are both satisfied by construction.

    In `dashboard/app.py`, add `_index_document_without_advanced_entry_point()`: a module-level
    function holding a module-level cache initialised to `None`, which on first call reads
    `index.html` from the directory containing `dashboard/app.py`, passes it through
    `beacon_frontpage.without_advanced_entry_point`, encodes the result, stores it and returns it;
    on every later call it returns the stored bytes. Any exception the transform raises propagates:
    a deployment whose front-page markup no longer matches must fail loudly rather than serve a page
    carrying a link to a route that answers 404.

    Change `index` to return that document with a `text/html` content type when
    `ENABLE_ADVANCED_DIAGNOSTICS` is false, and to keep its existing `send_file` call verbatim
    otherwise. The enabled statement must remain textually what it is today — that is what makes
    byte-identity a property of the code rather than only of a test (D-07-03). The transform is never
    reached, and `index.html` is never read a second time, on an enabled deployment.

    Add `FrontPageEntryPointTests` to `tests/test_optional_advanced_diagnostics.py` covering the
    `<behavior>` list. Three of its members deserve care:

    - The **markup invariant**: a test asserting that `dashboard/index.html` as it stands yields
      exactly one match for the transform's pattern. This is what converts a future edit to line 24
      into a failing test instead of a failing deployment, and its message must say so.
    - The **absent-not-hidden** assertion: the disabled body must not contain the anchor's id, its
      href, or its label text, and must still contain the services grid and theme toggle ids. Both
      halves matter — the first is criterion 2, the second is that the transform excised only what it
      was aimed at.
    - The **transform-once** measurement: wrap
      `dashboard.beacon.frontpage.without_advanced_entry_point` in a counting wrapper, issue five
      disabled `GET /` requests, and assert the observed invocation count is exactly one. This is a
      count of real invocations during real requests, not a claim about a cache's existence.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_optional_advanced_diagnostics.py tests/test_module_boundaries.py tests/test_advanced_ui.py</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/beacon/frontpage.py` exists and `test_factory_isolation_and_dependency_direction`
      still passes with it present.
    - With the toggle off, `GET /`'s body contains none of the anchor's id, href or label, and does
      contain the services grid and theme toggle element ids.
    - With the toggle on, `GET /`'s bytes equal `dashboard/index.html`'s bytes.
    - Five disabled `GET /` requests record exactly one transform invocation.
    - `dashboard/index.html` is unmodified by this task: its sha256 still equals the value recorded in
      `07-01`'s fixture.
    - Sensitivity, demonstrated and recorded with observed values: (a) making
      `without_advanced_entry_point` return its input unchanged makes the absent-not-hidden assertion
      FAIL, and the failure must name which of the three strings was found; (b) removing the cache
      makes the transform-once measurement FAIL with the observed pair `1` against `5`; (c) feeding
      the transform a document with the anchor duplicated makes it raise with the observed match
      count. Record all three.
  </acceptance_criteria>
  <done>A disabled deployment sends a front page that never contained the link; an enabled one sends the file untouched; and a future edit to the markup the transform depends on breaks a test rather than a server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The services front page still works without its advanced link — proven in a real browser</name>
  <files>dashboard/app.js, tests/test_optional_advanced_diagnostics.py</files>
  <read_first>
    - `dashboard/app.js` lines 749-795 — `captureDashboardScroll`, `restoreDashboardScroll` (note the
      `link.focus` call at line 762, reached only when a stored scroll offset exists), and the
      `DOMContentLoaded` handler, paying attention to the ordering: the unguarded
      `advanced-diagnosis-link` lookup at line 772 sits **above** the meta-form wiring, the modal
      wiring, the initial `Promise.allSettled` data fetch and all four `setInterval` registrations.
    - `tests/test_advanced_ui.py` lines 264-286 — `AdvancedUiTests.setUpClass`/`tearDownClass`: the
      `make_server` plus `sync_playwright` fixture idiom, including launching Chromium at
      `playwright.chromium.executable_path`.
    - `tests/test_advanced_ui.py` lines 1085-1112 — the pattern for driving the real front page,
      seeding a theme with `add_init_script`, and waiting on locators.
    - `tests/test_ui_states.py` line 1607 — the other existing consumer of the advanced link locator.
  </read_first>
  <behavior>
    - With the toggle off, loading `/` in Chromium: the services section renders its content (the
      grid element is populated rather than left at its `waiting for worker` placeholder), which is
      only possible if the `DOMContentLoaded` handler ran past line 772 and reached its
      `Promise.allSettled` call.
    - With the toggle off, the theme toggle responds to a click by moving the document's theme class,
      which is registered above line 772 and would survive the break — so it is asserted as a
      *control*, not as the evidence, and the test says so.
    - With the toggle off, the page reports no uncaught page error over the load, collected from
      Chromium's own `pageerror` event.
    - With the toggle off, the advanced link locator resolves to zero elements.
    - Both of the above hold in dark and in light theme.
    - With the toggle on, the existing browser coverage in `tests/test_advanced_ui.py` and
      `tests/test_ui_states.py` continues to pass unchanged.
  </behavior>
  <reversibility rating="reversible">A guarded lookup in one script and a browser test class.</reversibility>
  <action>
    Make `dashboard/app.js` tolerate the anchor's absence at both of its two sites. At the
    `DOMContentLoaded` registration, resolve the element once into a local, and register the click
    listener only when it resolved — so a missing anchor skips one registration instead of aborting
    the remaining fifteen statements. In `restoreDashboardScroll`, guard the focus call the same way,
    so the requestAnimationFrame callback cannot raise on a page that has no such element.

    Carry a comment at the `DOMContentLoaded` site stating that the element is optional because the
    deployment may have advanced diagnostics turned off, and that everything below this line depends
    on the lookup not raising. State the consequence, not the mechanism: a reader must be able to see
    why the guard is load-bearing without reconstructing the failure.

    Add `FrontPageBootWithoutAdvancedTests` to `tests/test_optional_advanced_diagnostics.py`. Build
    it on the `make_server` plus `sync_playwright` class fixture `AdvancedUiTests` uses, but with the
    application constructed through `load_app({'ENABLE_ADVANCED_DIAGNOSTICS': '0'})`. Seed at least
    one service row into the database in `setUpClass` so the services grid has something real to
    render and the assertion is about the boot sequence completing rather than about an empty state.

    Subscribe to Chromium's `pageerror` event and collect every error over the page's lifetime;
    assert the collected list is empty and, on failure, include the collected messages so the failure
    names the actual JavaScript error rather than only its downstream symptom.

    Run the whole body in both themes by seeding `beacon-theme` through `add_init_script` before
    navigation, mirroring line 1090's idiom, and use a subtest per theme so a single-theme regression
    is identifiable.

    The docstring must record the honest hierarchy of the three assertions: the populated services
    grid is the load-bearing evidence that boot completed, the empty page-error list is corroborating,
    and the theme toggle is a control that would pass even with the defect present. Presenting the
    theme toggle as evidence would be a guard that looks stronger than it is.
  </action>
  <verify>
    <automated>uv run --project dashboard python -m pytest -q tests/test_optional_advanced_diagnostics.py tests/test_advanced_ui.py tests/test_ui_states.py</automated>
  </verify>
  <acceptance_criteria>
    - With the toggle off, in both themes, the services grid renders seeded service content in real
      Chromium.
    - With the toggle off, the collected `pageerror` list is empty in both themes.
    - With the toggle off, the advanced link locator count is zero in both themes.
    - `tests/test_advanced_ui.py` and `tests/test_ui_states.py` pass unchanged.
    - Sensitivity, demonstrated and recorded with observed values: reverting the guard in
      `dashboard/app.js` while leaving the server-side removal in place makes this test FAIL, and the
      recorded evidence must include the collected `pageerror` message text and the observed services
      grid content at failure — that pair is the proof that this test measures boot completion rather
      than element presence.
  </acceptance_criteria>
  <done>The page the operator said they like keeps working with its advanced link gone, and the proof is a real browser rendering real data, not a source assertion about a null check.</done>
</task>

</tasks>

<plan_review>
**The one place a source assertion appears, and why it is not the evidence.** Task 2's markup
invariant reads `dashboard/index.html` and counts pattern matches. That is inspection, and it is
knowingly so: it is not evidence that the feature works, it is a tripwire that converts a future edit
to line 24 from a production failure into a test failure. The evidence that the feature works is the
served body in Task 2 and the rendered page in Task 3. The invariant's message must say which of the
two it is, so a later reader cannot mistake it for a behavioural guard.

**Task 3's assertion hierarchy is stated rather than blurred.** Three things are checked after the
front page loads with the link gone. Only one of them can actually fail if the defect is present.
The theme toggle is registered *above* the breaking line, so it would keep working with the boot
broken — it is a control that proves the page is alive at all, not that the boot completed. The empty
`pageerror` list is genuine but indirect: a swallowed error would not appear. The populated services
grid is the real evidence, because populating it requires execution to have reached the
`Promise.allSettled` call fifteen statements below the break. The plan says which is which because a
test that presents three assertions as three proofs, when two of them cannot fail, is exactly the
"looks stronger than it is" pattern Phase 6 found six times.

**Outcome over mechanism, and where the exception is.** Criterion 2's outcome is "the entry point is
absent". This plan fixes a mechanism — server-side excision of a specific markup span — because the
three alternatives each fail a different criterion, and the objective records which. The mechanism
can be wrong in exactly one way: `without_advanced_entry_point`'s pattern is coupled to markup it
does not own. The consequences of that coupling breaking are bounded deliberately — the transform
raises rather than returning a document that still carries the link, and the markup invariant fires
first, in the test suite, before any deployment sees it.

**No absolute thresholds.** The only literals compared against are counts of one and zero, and the
identity of two byte strings. Nothing is tied to a machine, a duration or a dataset size.

**Every new guard is mutation-verified with recorded observed values**, and each acceptance criterion
names the specific mutation and the specific values to record. Task 3's mutation is the important
one: reverting only the JavaScript guard, leaving the server change in place, is precisely the
half-shipped state that would otherwise reach the operator.

**Reversibility.** Nothing here is one-way. A new pure module, one branch, a process-local cache, and
two guarded lookups in a script. No migration, no schema change, no write. The advanced asset files
stay in the production image specifically so that turning the toggle back on and restarting restores
the workspace with nothing else to do, which is criterion 5's requirement and is asserted by keeping
`test_production_image_manifest_includes_the_advanced_document_bundle` green.
</plan_review>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Flask static routes | An untrusted LAN client requests the advanced bundle; the toggle decides whether those paths answer. |
| `dashboard/index.html` on disk → the bytes `/` sends | A transform now sits between a file and a response on one code path; it must not be able to emit a partially-transformed or corrupted document. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Information disclosure | `index` disabled branch | medium | mitigate | The transform excises exactly one matched span and raises on any other match count, so it cannot emit a document that still advertises a route the deployment closed. Pinned by the absent-not-hidden assertion and the duplicate-anchor raise. |
| T-07-05 | Tampering | `without_advanced_entry_point` | low | mitigate | The function is pure, performs no I/O, takes no request-derived input, and its only argument is a file the container ships read-only. No untrusted data reaches it. |
| T-07-06 | Denial of service | `_index_document_without_advanced_entry_point` | low | mitigate | The document is read and transformed at most once per process, measured by the transform-once test, so front-page load cannot be amplified into repeated file reads. |
| T-07-07 | Denial of service | `dashboard/app.js` boot | high | mitigate | An unguarded element lookup would abort the front page's entire initialisation. Guarded at both sites and proven by a real-Chromium render of seeded service data in both themes. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package; `dashboard/pyproject.toml` and `uv.lock` are untouched. |
</threat_model>

<verification>
- `uv run --project dashboard python -m pytest -q` — judged against the phase floor of 939 passed /
  564 subtests, 0 failed, per `D-DEBT-06-13`'s "no NEW failures outside the known-flaky set" rule,
  re-running any suspect module in isolation first.
- `dashboard/index.html`, `dashboard/advanced.html`, `dashboard/advanced.css` and
  `dashboard/advanced.js` are unmodified: their sha256 values still equal `07-01`'s recorded fixture.
- `tests/test_advanced_ui.py` and `tests/test_ui_states.py`, the two existing consumers of the
  advanced link locator, pass unchanged.
</verification>

<success_criteria>
- With the toggle off, all four advanced routes answer 404 and the front page's served bytes never
  contained the entry point.
- With the toggle off, the services front page boots and renders real service data in real Chromium,
  in both themes, with no uncaught page error.
- With the toggle on, all four surfaces and the front page serve exactly their file's bytes.
- Every sensitivity demonstration named in the acceptance criteria is recorded in the SUMMARY with
  its observed values.
</success_criteria>

<output>
Create `.planning/phases/07-optional-advanced-diagnostics/07-02-SUMMARY.md` when done.
</output>
