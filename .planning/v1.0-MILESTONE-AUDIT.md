---
milestone: v1.0
audited: 2026-08-24T17:53:58Z
status: gaps_found
audit_scope: partial_milestone
scope_note: "4 of 7 roadmap phases are built (1, 2, 3, 03.1). Phases 4, 5 and 6 have no plans and no code. This audit therefore verifies the built subset and records the unbuilt remainder as unsatisfied."
scores:
  requirements: 22/45
  requirements_in_built_phases: 22/23
  phases: 3/4
  integration: 5/6
  flows: 4/4
gaps:
  requirements:
    - id: "DIA-08"
      status: "unsatisfied"
      phase: "Phase 3 (traceability) / deferred to Phase 4 (actual)"
      claimed_by_plans: ["03-02-PLAN.md", "03-03-PLAN.md", "03-04-PLAN.md", "03-05-PLAN.md", "03-06-PLAN.md", "03-07-PLAN.md"]
      completed_by_plans: ["03-02-SUMMARY.md", "03-03-SUMMARY.md", "03-04-SUMMARY.md", "03-05-SUMMARY.md", "03-06-SUMMARY.md", "03-07-SUMMARY.md"]
      verification_status: "passed (requirement explicitly marked DEFERRED within a passed phase)"
      evidence: "03-VERIFICATION.md:160 records DIA-08 as '⏸ DEFERRED to Phase 4' because dashboard/advanced.html still has no range control. Six Phase 3 SUMMARY.md files list DIA-08 in requirements-completed, contradicting that. REQUIREMENTS.md marks it '- [ ]' with traceability status 'Deferred to Phase 4'. Three sources disagree; the deferral is authoritative. Also violates the Definition of Done clause 'every v1 requirement maps to exactly one roadmap phase' — DIA-08 is mapped to Phase 3 but owned by Phase 4."
    - id: "TEL-06"
      status: "partial"
      phase: "Phase 3"
      claimed_by_plans: ["03-01-PLAN.md through 03-22-PLAN.md"]
      completed_by_plans: ["03-20-SUMMARY.md", "03-21-SUMMARY.md"]
      verification_status: "passed"
      evidence: "Five of six clauses (effective retention, database pressure, worker freshness, collection gaps, background-job health) are wired and rendered. The 'displayed resolution' clause is not: dashboard/beacon/diagnosis.py:432 emits a hardcoded resolution_policy literal {60, 300, 3600} that does not derive from telemetry.py:11 RESOLUTION_LADDER_SECONDS (ten steps) nor from select_resolution() at telemetry.py:86, and dashboard/advanced.js never reads the field at all (only the static heading 'Retention and resolution' at advanced.js:367). Coherent as a deferral — there is no range selector yet to display a resolution for — but not observably satisfied today."
    - id: "DIA-04, DIA-05, DIA-06, DIA-07"
      status: "unsatisfied"
      phase: "Phase 4 (not started)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 4 has no phase directory, no plans, no VERIFICATION.md. No implementation exists."
    - id: "HIS-01, HIS-02, HIS-03, HIS-04, HIS-05, HIS-06"
      status: "unsatisfied"
      phase: "Phase 4 (not started)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 4 not started. Note: /api/telemetry/history (app.py:2503) is built and tested but has zero frontend consumers — it is the API this phase will consume."
    - id: "UX-01, UX-03, UX-04, UX-05, UX-06, UX-07"
      status: "unsatisfied"
      phase: "Phase 5 (not started)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phase 5 has no phase directory, no plans, no VERIFICATION.md."
    - id: "OPS-01, OPS-02, OPS-03, OPS-04, OPS-06, OPS-07"
      status: "unsatisfied"
      phase: "Phase 5 (OPS-06) / Phase 6 (rest), not started"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: "Phases 5 and 6 have no phase directories, no plans, no VERIFICATION.md."
  phases:
    - phase: "03.1-planned-maintenance-recognition"
      status: "gate open"
      evidence: "03.1-VERIFICATION.md frontmatter reads status: human_needed, not passed. 9/9 must-haves verified and gaps: [] — the only thing holding the gate is one undispositioned Warning-severity finding (WR-06) that this round's own code review raised on code this round introduced. ROADMAP.md also still carries this phase as '- [ ]'."
  integration:
    - from: "Phase 2 (telemetry resolution engine)"
      to: "Phase 3 (advanced diagnosis surface)"
      issue: "diagnosis.py:432 maintains a parallel, incomplete description of the resolution ladder instead of reading telemetry.py's own RESOLUTION_LADDER_SECONDS / select_resolution. The two can drift silently."
      requirements: ["TEL-06", "TEL-05"]
      severity: "warning"
  flows: []
tech_debt:
  - phase: 03-advanced-current-diagnosis
    items:
      - "WR-01 (03-REVIEW.md round 5) — dashboard/beacon/diagnosis.py:482-508: a job whose start-write silently fails reads as permanently healthy; no regression test drives this path."
      - "WR-02 (03-REVIEW.md round 5) — dashboard/beacon/repositories.py:143-149: a noisy telemetry stream can starve other services' gap evidence."
      - "WR-03 (03-REVIEW.md round 5) — dashboard/app.py:1831: lease-loss-on-busy-branch escalation."
      - "WR-05 (03-REVIEW.md round 5) — dashboard/app.py:1334-1343: uptime-disclosure boundary jitter comment."
      - "IN-07 (03-REVIEW.md round 5) — dashboard/app.py: dead _table_columns f-string SQL builder, unreachable, not a live vulnerability."
      - "Human verification item 2 of 5 (03-VERIFICATION.md:196) — hover-affordance perceptibility across the /advanced workspace in both themes has never been checked on real hardware."
      - "Human verification item 1 of 5 (03-VERIFICATION.md:190) — the operator's real-Pi confirmation that 'Refresh now' reads as the primary action exists only in session context, not as a git-tracked 03-UAT.md entry."
      - "Deferred follow-up (03-UAT.md test 3) — the J6 machinery-fault render was not photographed within its transient failure window."
  - phase: 03.1-planned-maintenance-recognition
    items:
      - "WR-06 (03.1-REVIEW.md, new) — dashboard/beacon/maintenance.py:433-442: the ceiling-bound warning is logged unthrottled from inside a per-service, per-window, per-interval loop reached on every /api/advanced/current request. Not reachable at the documented EXPIRE_DAYS=7 default; a log flood only under operator misconfiguration. UNDISPOSITIONED — this is what holds the phase gate open."
      - "IN-01 (03.1-REVIEW.md) — dashboard/beacon/maintenance.py:276-280,312: docstring claims the circular median is 'rounded to a whole minute'; the implementation truncates via int(). Cosmetic, pre-existing semantics."
  - phase: cross-cutting
    items:
      - "dashboard/beacon/diagnosis.py:432 — resolution_policy is a hardcoded three-value literal, a parallel source of truth against telemetry.py:11's ten-step RESOLUTION_LADDER_SECONDS, and is never consumed by any frontend."
      - "dashboard/advanced.js:369-371 — the retention tier labels ('7-day raw', '5-minute through day 30', 'hourly through day 90') are hardcoded strings rendered beside settings-driven values. Under non-default retention the label and the value it labels disagree."
      - "dashboard/app.py:2503 /api/telemetry/history — built and covered by tests/test_historical_telemetry_api.py and tests/test_telemetry_retention.py, but has zero frontend consumers. Built ahead of its Phase 4 consumer; acceptable, tracked so it is not forgotten."
      - ".planning/STATE.md is stale — records total_phases: 4 (roadmap has 7), completed_plans: 73/76, and a per-phase metrics table showing Phase 03.1 at 10 plans when 18 SUMMARY.md files exist. Phase 03.1 is also still listed as 'Plan 1 of 15'."
  - phase: 01-behavioral-safety-runtime-ownership
    items:
      - "No 01-SECURITY.md exists. workflow.security_enforcement is true and Phases 02, 03 and 03.1 each carry a verified SECURITY.md with threats_open: 0. Phase 1 — which owns the outbound-target, TLS, redirect, webhook and mutation-protection surface (FND-07, OPS-05) — has no threat register. Most likely predates the capability; it is the phase where one matters most."
nyquist:
  compliant_phases: ["02-bounded-telemetry-retention", "03.1-planned-maintenance-recognition"]
  partial_phases: []
  not_validated_phases: ["01-behavioral-safety-runtime-ownership", "03-advanced-current-diagnosis"]
  missing_phases: []
  overall: partial
security:
  verified_phases: ["02-bounded-telemetry-retention", "03-advanced-current-diagnosis", "03.1-planned-maintenance-recognition"]
  missing_phases: ["01-behavioral-safety-runtime-ownership"]
  threats_open: 0
---

# Milestone v1.0 Audit — Beacon

**Audited:** 2026-08-24T17:53:58Z
**Status:** `gaps_found`
**Scope:** Partial milestone — 4 of 7 phases built

## Scope

This audit was run mid-milestone. The v1.0 roadmap defines seven phases; four are built:

| Phase | Plans | Roadmap status | VERIFICATION status |
|-------|-------|----------------|---------------------|
| 1. Behavioral Safety & Runtime Ownership | 23/23 | Complete (2026-08-07) | `passed` — 8/8 must-haves |
| 2. Bounded Telemetry & Retention | 12/12 | Complete (2026-08-11) | `passed` — 4/4 must-haves |
| 3. Advanced Current Diagnosis | 23/23 | Complete (2026-08-20) | `passed` — 5/5 must-haves (round 9) |
| 03.1. Planned Maintenance Recognition | 18/18 | Reopened, `[ ]` | `human_needed` — 9/9 must-haves, 1 open disposition |
| 4. Historical Investigation | 0 | Not started | — |
| 5. Theme-Parity Analytics Experience | 0 | Not started | — |
| 6. Workload Resilience & Pi Acceptance | 0 | Not started | — |

The milestone cannot pass its own Definition of Done while Phases 4, 5 and 6 are unbuilt. Twenty-two of forty-five v1 requirements have no implementation. Everything below therefore evaluates the built subset, and reports the unbuilt remainder as unsatisfied rather than pretending the milestone was ready for closure.

## Requirements Coverage

Cross-referenced across three independent sources: the REQUIREMENTS.md traceability table, each phase's VERIFICATION.md requirements table, and each plan SUMMARY.md's `requirements-completed` frontmatter.

**22/45 satisfied. 1 partial. 22 unsatisfied.**

### Satisfied (22)

| Requirement | Phase | Three-source agreement |
|-------------|-------|------------------------|
| FND-01 … FND-07 (7) | 1 | VERIFICATION `passed` + SUMMARY frontmatter + `[x]` Complete |
| OPS-05 | 1 | VERIFICATION `passed` + SUMMARY frontmatter + `[x]` Complete |
| TEL-01 … TEL-05 (5) | 2 | VERIFICATION `passed` + SUMMARY frontmatter + `[x]` Complete |
| DIA-01, DIA-02, DIA-03 | 3 | VERIFICATION `passed` + SUMMARY frontmatter + `[x]` Complete |
| UX-02 | 3 | VERIFICATION `passed` + SUMMARY frontmatter (03-23) + `[x]` Complete |
| MNT-01 … MNT-04 (4) | 03.1 | VERIFICATION 9/9 truths + SUMMARY frontmatter + `[x]` Complete |

The TEL-06 traceability reconciliation that Phase 3's rounds 8 and 9 both flagged as outstanding documentation debt **has since landed** — `REQUIREMENTS.md` now reads `- [x] **TEL-06**` / `Complete` (commit `b7796c7`). That debt is closed.

MNT-01…04 are counted satisfied on evidence, but their phase gate is still open (see Phase Gate below). If WR-06's disposition turns out to require a code change, they should be re-confirmed after it lands.

### Partial (1)

**TEL-06 — "displayed resolution" clause.** Five of six clauses are wired and rendered. The sixth is not, and this is a genuine finding that nine rounds of phase verification did not surface because they checked the API payload rather than the render path:

- `dashboard/beacon/diagnosis.py:432` emits `'resolution_policy': {'raw_seconds': 60, 'five_minute_seconds': 300, 'hourly_seconds': 3600}` — a hardcoded literal.
- The real ladder is `dashboard/beacon/telemetry.py:11` `RESOLUTION_LADDER_SECONDS = (60, 300, 900, 1800, 3600, 7200, 14400, 21600, 43200, 86400)` — ten steps, not three — and the real selection happens in `select_resolution()` at `telemetry.py:86`.
- `dashboard/advanced.js` never reads `resolution_policy`. A grep for it, and for `resolution` generally, returns only the static heading text `'Retention and resolution'` at `advanced.js:367`. The section beneath renders four retention values and no resolution value.

This is coherent as a deferral: there is no range selector on `/advanced` yet (DIA-08's range control is Phase 4's), so there is no *displayed* resolution to display. But the requirement is not observably satisfied today, and the traceability table says it is.

### Unsatisfied (22)

**DIA-08 (1) — deferred within a "Complete" phase, with three sources disagreeing.**

- `03-VERIFICATION.md:160` records `⏸ DEFERRED to Phase 4` — `dashboard/advanced.html` has no range control.
- `REQUIREMENTS.md` records `- [ ]` and traceability status `Deferred to Phase 4`, while still mapping the row to **Phase 3**.
- Six Phase 3 SUMMARY.md files (`03-02` through `03-07`) list `DIA-08` in `requirements-completed`.

The deferral is authoritative; the SUMMARY frontmatter is over-credit. Two consequences worth acting on: the traceability row should move to Phase 4 so the Definition of Done clause *"every v1 requirement maps to exactly one roadmap phase"* stays true, and Phase 4's plan set must actually pick DIA-08 up — right now nothing owns it.

**Phase 4 (10)** — DIA-04, DIA-05, DIA-06, DIA-07, HIS-01 … HIS-06. No phase directory, no plans, no code.

**Phase 5 (7)** — UX-01, UX-03, UX-04, UX-05, UX-06, UX-07, OPS-06. No phase directory, no plans, no code.

**Phase 6 (5)** — OPS-01, OPS-02, OPS-03, OPS-04, OPS-07. No phase directory, no plans, no code.

**Orphan detection:** no requirement appears in the traceability table but is absent from every VERIFICATION.md within its own phase's scope. All 23 built-phase requirements are claimed and verified by their assigned phase. The 22 unbuilt ones are unstarted, not orphaned.

## Cross-Phase Integration

**5 of 6 seams sound. 4 of 4 E2E flows complete.**

| Seam | Verdict | Evidence |
|------|---------|----------|
| Phase 1 → Phase 2 — retention/rollup work runs through the epoch fence | Sound | `worker_cleanup_history` → `run_retention_batch` executes inside `_worker_write_transaction` with `assert_current_worker_authority`. No side path. |
| Phase 2 → Phase 3 — diagnosis reads the real telemetry source of truth | **Partial** | Retention, pressure, worker freshness and coverage all read Phase 2's real tables and `RetentionPolicy.from_settings`. Resolution does not — see TEL-06 above. |
| Phase 3 → Phase 03.1 — maintenance suppression does not corrupt diagnosis | Sound | `attributed_downtime_seconds` reached from `get_current_diagnosis`; probes inserted unconditionally at `app.py:1395,1591`; only `state_change` entries are tagged `suppressed_reason` at `app.py:965-978`, never withheld from storage. |
| Phase 1 → Phase 03.1 — migration ownership vs backup/recovery | Sound | `docker-compose.yml:69-100`: `web` and `worker` both `depends_on: migrate: service_completed_successfully`; `recovery` sits behind `profiles: ["recovery"]` and never runs on an ordinary `up`. `recovery.py:117-158` still enforces marker-authorized, catalog-bound restore. |
| Phase 1 → Phase 03.1 — connection-lease gate covers Phase 2/3 routes too | Sound, and structurally so | The lease lives in the connection factory (`beacon/db.py:74-99`, `ManagedConnection`), not in a per-route checklist, so every consumer is covered whether or not anyone remembered it. The only raw `sqlite3.connect` calls are in `recovery.py`, `migrations.py` and `inventory.py`'s read-only scan — all outside the live web/worker processes by design. |
| Frontend field parity | Sound, one dead field | Every route has a caller and the field names match, except `resolution_policy` (emitted, never read) and `/api/telemetry/history` (built, no consumer yet). |

### E2E flows

1. **Fresh deploy on an existing database** — complete. This is the exact flow that failed as G-03.1-1/G-03.1-2 during human UAT; the one-shot `migrate` step both long-running services wait on is genuinely wired.
2. **Operator diagnoses a live problem** — complete. Theme survives the `/` ↔ `/advanced` round trip via the shared `localStorage['beacon-theme']` key (`app.js:735,758`, `advanced.js:48`).
3. **Maintenance window lifecycle** — complete. Suggestion → confirm → suppression → overrun. The overrun event fires exactly once via `overrun_already_raised`/`mark_overrun_raised` gating (`app.py:1556-1575`), and its alert dispatches outside the write transaction (`app.py:1633-1640`).
4. **Backup and recovery** — complete. Profile-gated, network-isolated, runs before any writer-holding process starts.

## Phase Gate

**Phase 03.1 is not closed.** `03.1-VERIFICATION.md` reads `status: human_needed`, and `ROADMAP.md` carries the phase as `- [ ]`. Nothing failed: 9/9 must-haves are verified, `gaps: []`, and all three of the round's UAT gaps (G-03.1-69/70/71) are confirmed closed by direct source inspection. The gate is held open by exactly one thing:

> **WR-06** — `dashboard/beacon/maintenance.py:433-442`. The ceiling-bound warning added to close WR-05 is logged unthrottled from inside a per-service, per-window, per-interval loop reached on every `/api/advanced/current` request. Not reachable at the documented `EXPIRE_DAYS=7` default; produces a sustained log flood only under an operator misconfiguration (e.g. a typo adding a digit). Not a regression, not a correctness defect — but undispositioned.

This needs a human decision recorded in `03.1-UAT.md`: rate-limit the warning (once per process, or once per `(port, interval)` per cooldown), hoist the check to a single per-top-level-call site, or accept it as documented. It is the single smallest piece of work standing between this phase and a closed gate.

## Nyquist Coverage

| Phase | VALIDATION.md | `status` | `nyquist_compliant` | Verdict | Action |
|-------|---------------|----------|---------------------|---------|--------|
| 01-behavioral-safety-runtime-ownership | exists | `draft` | false | NOT-VALIDATED | `/gsd-validate-phase 01` |
| 02-bounded-telemetry-retention | exists | `validated` | true | COMPLIANT | — |
| 03-advanced-current-diagnosis | exists | `draft` | false | NOT-VALIDATED | `/gsd-validate-phase 03` |
| 03.1-planned-maintenance-recognition | exists | `validated` | true | COMPLIANT | — |

**Overall: partial.** Phases 01 and 03 read `status: draft`, meaning `validate-phase` never reconciled them (or they predate the `status` field). Their `nyquist_compliant: false` is therefore *not authoritative* — it is a coverage TODO, not a compliance failure. Both are the two largest phases in the milestone (23 plans each), so this is the biggest unmeasured surface in v1.0. Running `/gsd-validate-phase` on each yields the real verdict and may well close the gap retroactively.

## Security Coverage

| Phase | SECURITY.md | Status | Threats open |
|-------|-------------|--------|--------------|
| 01 | **absent** | — | unknown |
| 02 | exists | `verified` | 0 |
| 03 | exists | `verified` | 0 (142 total, 142 closed, 128 distinct) |
| 03.1 | exists | `verified` | 0 |

`workflow.security_enforcement` is enabled and three of four phases carry a verified register with zero open threats. Phase 1 has none — and Phase 1 is the phase that owns the outbound-target policy, TLS handling, redirect following, webhook dispatch and mutation-request protections (FND-07, OPS-05). Almost certainly it predates the capability rather than having skipped it, but it is the phase where a threat register matters most. `/gsd-secure-phase 01` would close it retroactively.

## Tech Debt

**13 items across 4 groupings.** None is a blocker. Full detail in the frontmatter above; the ones worth attention:

**Correctness-adjacent (2)**
- `diagnosis.py:482-508` (WR-01) — a job whose start-write silently fails reads as permanently healthy, and no regression test drives that path. This survived nine rounds of background-job-health work in Phase 3.
- `repositories.py:143-149` (WR-02) — a noisy telemetry stream can starve other services' gap evidence.

**Truth drift (3)**
- `diagnosis.py:432` — hardcoded resolution literal, parallel to `telemetry.py`'s real ladder.
- `advanced.js:369-371` — retention tier labels are hardcoded strings ("7-day raw", "through day 30", "through day 90") rendered beside settings-driven values. Under non-default retention, the label contradicts the number it labels.
- `maintenance.py:276-280,312` (IN-01) — docstring says "rounded to a whole minute"; the code truncates.

**Process drift (1)**
- `.planning/STATE.md` is stale in three ways: `total_phases: 4` against a seven-phase roadmap, `completed_plans: 73/76` when all 76 SUMMARYs exist, and a metrics table recording Phase 03.1 at 10 plans when 18 ran. It also still reads "Plan 1 of 15".

**Unfinished human verification (3)**
- Phase 3 item 2 — hover-affordance perceptibility across `/advanced` in both themes, never checked on hardware.
- Phase 3 item 1 — the operator's real-Pi confirmation exists only in session context, not as a git-tracked `03-UAT.md` entry.
- Phase 3 deferred follow-up — the J6 machinery-fault render was not photographed within its transient failure window.

**Inert (2)** — `app.py` `_table_columns` dead SQL builder (IN-07); `app.py:1831` and `app.py:1334-1343` review warnings carried forward.

**Undispositioned (1)** — WR-06, covered under Phase Gate above.

## Verdict

`gaps_found`. Not because the built work is weak — the four built phases are unusually well verified, all four E2E flows complete, five of six integration seams are sound, and zero security threats are open across three registers. The milestone is simply **not finished**: three phases and twenty-two requirements have not been started, one requirement (DIA-08) is deferred with no phase owning it, one clause of TEL-06 is unwired, and Phase 03.1's gate is still open on a single undispositioned log-hygiene finding.

---

*Audit generated by `/gsd-audit-milestone`. Integration check performed by `gsd-integration-checker` against source, not against SUMMARY/VERIFICATION prose; the TEL-06 and label-drift findings were independently re-confirmed by the orchestrator.*
